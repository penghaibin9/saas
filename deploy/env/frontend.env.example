# =============================================================================
# PC 管理端前端 · 环境变量示例（P5 部署准备 · docker-compose 本地预演命名）
# 用法：复制到 frontend/.env.production（或 .env），去掉 .example，按需修改后
#      重新 npm run build（Vite 环境变量只在构建时注入，改了要重新 build 才生效）。
#
# 与 deploy/env/pc-frontend.env.example 关系：两者内容等价、变量名一致；
# 本文件是本轮部署准备（P5）按任务清单要求新增的命名（deploy/env/frontend.env.example），
# 供 docker-compose 本地预演场景对照 backend.env / miniapp.env 统一命名风格使用。
# 两个文件不冲突、可只选其一复制使用，均不影响 frontend/src 源码。
#
# ★ 当前阶段：前端默认用 mock 假数据。若要在本地 docker-compose 预演里连真实后端
#   （nginx 已把 /api/ 反代到 backend，见 deploy/nginx/nginx.conf），可按下方填写
#   VITE_API_BASE_URL 指向 nginx 地址；不填则继续用 mock，不受影响。
# =============================================================================

# 后端接口基础地址：
#   - 留空/不配 → 前端继续用 mock 假数据（当前默认，最安全）
#   - 经本地 docker-compose 的 Nginx 访问真实后端 → http://localhost/api/v1
#   - 直接绕过 Nginx 访问 backend 容器端口（仅本地调试）→ http://localhost:8000/api/v1
VITE_API_BASE_URL=

# 站点标题（可选）
VITE_APP_TITLE=高校学生全生命周期管理平台

# 部署子路径（可选，默认部署在根路径 "/" 时无需设置；本项目 docker-compose 预演固定用根路径）
# VITE_BASE_PATH=/
