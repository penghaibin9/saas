# =============================================================================
# PC 管理端前端 · 环境变量示例（Vite）
# 用法：复制到 frontend/.env.production（或 .env），去掉本文件的 .example，按需填写。
# 说明见 docs/deploy/08-环境变量说明.md
# 注意：Vite 只暴露以 VITE_ 开头的变量给前端；真实 .env 不要提交 git。
#
# ★ 当前阶段：前端使用 mock 假数据、后端未接入，通常【无需填写】即可部署演示。
#   下列变量主要供【后端接入阶段】使用，现在了解即可。
# =============================================================================

# 后端接口基础地址（后端接入后再填；当前 mock 阶段留空/不配）
# 例：VITE_API_BASE_URL=https://admin.example.com/api/v1
VITE_API_BASE_URL=

# 站点标题（可选）
VITE_APP_TITLE=高校学生全生命周期管理平台

# 部署子路径（可选，默认部署在根路径 "/" 时无需设置）
# 若必须部署到 /admin/ 这类子路径，需在 vite.config.js 配 base 后重新 build（谨慎，见 docs/deploy/08）
# VITE_BASE_PATH=/
