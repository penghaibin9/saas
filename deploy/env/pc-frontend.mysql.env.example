# =============================================================================
# PC 管理端 / miniapp H5 · 前端构建环境变量示例（MySQL 版部署）
# 用法：在各自项目根目录（frontend/ 或 miniapp/）复制为 .env 或 .env.production 使用。
#      这些是 Vite 构建期变量，只影响前端「请求打到哪个后端」，不含任何密钥。★ 勿提交真实地址。
#
# VITE_API_BASE_URL：只填「源」，前缀 /api/v1 会自动拼接，勿带 /api 或末尾斜杠。
# =============================================================================

# 【PC 管理端 frontend/】
#   本地开发：       VITE_API_BASE_URL=http://localhost:8000
#   同源部署(推荐)： 留空 —— 请求走 /api/v1/...，由 Nginx location /api/ 反代到后端
#   独立 API 域名：  VITE_API_BASE_URL=http://服务器IP   或   http://api.example.com
VITE_API_BASE_URL=

# 【miniapp H5 / 小程序 miniapp/】
#   H5 服务器演示： VITE_API_BASE_URL=http://服务器IP        （经 Nginx /api/ 反代）
#   mp-weixin 真机：VITE_API_BASE_URL=http://服务器公网IP:端口（小程序需绝对可达地址，
#                   并在微信公众平台配置 request 合法域名）
#
# 说明：前端代码已支持——
#   frontend/src/services/http/config.js  ：显式 VITE_API_BASE_URL > 开发默认 localhost:8000 > 生产同源
#   miniapp/src/config/env.js             ：VITE_API_BASE_URL > 默认 localhost:8000
