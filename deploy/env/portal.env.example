# =============================================================================
# 统一门户首页（主域名根路径 /）· 前端构建环境变量示例
# -----------------------------------------------------------------------------
# 用法：在 frontend/ 目录复制为 .env.production（或追加到已有 .env.production）后再执行
#      npm run build。这些是 Vite 构建期变量，只影响门户展示与入口地址，不含任何密钥。
#
# 全部为「可选」：不配置时门户使用同源相对路径默认值，或按缺失项安全降级
# （入口禁用 / 占位提示 / 隐藏链接），不会出现 localhost、破图或假按钮。
# 唯一来源：frontend/src/config/portalConfig.js
# =============================================================================

# ── 登录入口 ──
# 默认值即仓库真实路由，单服务器同域部署无需配置：
#   教师/管理端 = /login          （frontend 应用自身路由）
#   学生端      = /portal/login   （student-portal 应用，VITE_BASE 默认 /portal/）
# 仅当学生端/教师端改用独立子域名时才需覆盖，例如 https://teacher.example.edu.cn/login
# 安全限制：只接受同源相对路径（/ 开头）或 http(s):// 绝对地址，其余一律按未配置处理。
#VITE_PORTAL_TEACHER_LOGIN_URL=/login
#VITE_PORTAL_STUDENT_LOGIN_URL=/portal/login

# ── 微信小程序码 ──
# 未配置时门户显示「即将接入」占位，绝不展示伪造二维码。
# 建议把小程序码图片放到 frontend/public/ 下，用同源相对路径引用，例如 /brand/mp-teacher.png
#VITE_PORTAL_TEACHER_MP_QR=
#VITE_PORTAL_STUDENT_MP_QR=

# ── 系统运行状态页 ──
# 仓库当前不存在真实状态页，默认留空 → 门户不渲染状态入口（不展示假的「运行正常」）。
# 接入 UptimeRobot / 自建状态页后再填写。
#VITE_PORTAL_STATUS_URL=

# ── 法务与支持页 ──
# 仓库当前不存在这些公开页面，默认留空 → 页脚不渲染对应链接（不放虚假链接）。
#VITE_PORTAL_PRIVACY_URL=
#VITE_PORTAL_TERMS_URL=
#VITE_PORTAL_SUPPORT_URL=

# ── 客服联系方式 ──
# 复用登录页既有变量，不新增重复配置；填写后门户头部显示「服务支持 <内容>」。
#VITE_SUPPORT_CONTACT=

# =============================================================================
# 说明：
# 1. 平台名称/主色/水印优先取自后端公开接口 /api/v1/authz/tenant/brand（租户品牌配置），
#    接口不可用时回落到默认「高校学生全生命周期管理平台」，无需在此配置。
# 2. 主体名称与 ICP 备案号属主体资质，固定写在 portalConfig.js，不做环境变量。
# 3. API 地址沿用既有 VITE_API_BASE_URL（见 pc-frontend.mysql.env.example），
#    同源部署留空即可，门户不会把服务器地址打进构建产物。
# =============================================================================
