# 二级模块下三级模块真实生产级施工卡提示词

## 使用方法

每次只替换下面一处：

`【业务中心 / 二级模块】`

例如：`学工中心 / 谈心家校`。

默认执行方式是：先完成一个二级模块总施工包和其全部三级模块独立施工卡，通过门禁后在同一次任务继续真实施工。只有用户明确写“本次只要施工卡，不改代码”时才停在文档，但仍不得降低证据和设计标准。由 `02-多智能体并行生产级施工包编排提示词.md` 调用时，第一阶段必须服从总控的“只完成证据和施工卡、暂不改代码”，等总控四审通过后再进入代码阶段。

---

## 可直接复制给 Claude Code 的完整提示词

你现在处理《职校学生全生命周期 SaaS 平台》的：

`【业务中心 / 二级模块】`

本任务不是批量写文档，不是整理旧总册，不是根据菜单名想象页面。一次只处理这一个二级模块，以及它下面的全部三级模块。

最终必须交付：

1. 一个二级模块生产级总施工包；
2. 该二级模块下每个三级模块各一份独立、真实、可编码、可测试、可部署、可验收的生产级施工卡；
3. 默认在全部施工卡达到 `design_ready` 后，同一次任务继续完成真实代码施工、测试、构建、部署检查和状态同步。

### 一、强制任务模式

本任务强制按根目录 `CLAUDE.md` 的 A/B 模式执行，不得归类为“单文档任务”或“小范围文档修复”。

- 没有有效二级施工包：A 模式；
- 已有有效二级施工包：B 模式，先审计其证据和当前代码是否过期；
- 旧 `施工卡/` 目录中的批量卡片只能作为草稿素材，不能直接视为 `design_ready`；
- 本提示词供一个执行智能体处理一个二级模块。总控智能体可以把本提示词分别交给多个子智能体并行执行，但每个子智能体仍只能负责一个二级模块，且必须独立通过全部门禁。

### 二、先做真实代码基线审计，禁止先写卡

先运行并记录：

- `git status --short`
- 当前 branch 和 HEAD commit
- 当前模块在 `navPlan.js`、施工图和中心索引中的二级/三级结构

然后用 `rg` / `rg --files` 实际查清：

1. 前端路由、页面、组件、API/provider；
2. 后端 router、schema、service、model；
3. Alembic migration 和真实 MySQL 表设计；
4. 权限、数据范围、业务关系和审计调用链；
5. 自动化测试、seed、初始化、部署和 health check；
6. 与本模块相关的历史 commit 和施工记录。

输出“代码基线收据”，每项事实必须带：

`commit + 文件路径 + symbol/路由/表/测试名（必要时行号）`

硬规则：

- `navPlan.js` 的 `planned` 只代表菜单状态，不代表代码不存在；
- 设计文档写“已实现”也不代表代码存在；
- 技术事实能从仓库查明时，禁止写“需人工确认”“开发时再看”“本卡不查代码”；
- 不运行测试时只能写“测试文件存在/未执行”，不能写“测试通过”。

### 三、必须取得三家可核验外部证据（实时联网或已核验本地证据包）

先执行：

`rg --files docs/03-业务模块设计 | rg '外部对标证据.*外部对标证据包\.md$'`

若当前二级模块已有符合 `CLAUDE.md §5.1` 的 `verified_external_evidence_v1` 本地证据包，必须优先复用，不得因本智能体不能联网而重复标 `BLOCKED`。没有有效证据包或证据包门禁不足时，再由具备联网能力的智能体补查；两种通道都不可用时才按证据失败处理。

为当前二级模块选择三家**能够取得有效来源**的成熟系统，不预设必须是正方、强智、青果。只有实时打开并核验的原文，或已按统一模板落盘的有效证据包，才可作为对标对象。

每家必须同时具备两类证据：

1. **模块能力证据**：能直接证明当前二级/三级模块的功能、流程、角色、页面或交付要求；
2. **高校采用证据**：学校官网使用/培训/验收材料、采购中标/合同公告或公开案例，能证明不是只有厂商宣传。

同一份高校官方操作手册、培训材料或采购验收文件可以同时满足两类，但必须分别说明它证明了什么。

每条来源至少登记：

- 产品/系统和厂商；
- 资料发布方；
- 资料标题；
- 可打开 URL；
- 查阅日期；
- 页码、章节或页面位置；
- 该来源能直接证明的功能/流程；
- 不能证明的内容；
- 来源类型：厂商宣传 / 产品手册 / 高校使用 / 招标采购 / 制度标准。

复用本地证据包时，每个正式设计项必须写全：

- `designSource=external_evidence`；
- 稳定 `evidenceId`；
- 原始 `sourceUrl`；
- 页码/章节/正文 `locator`；
- `localEvidencePath`；
- 由原文直接支持的事实与本项目 `design_inference` 分界。

只写“参考本地证据包”或只写本地路径，视为引用失败。

另补至少一份真实学校操作手册、采购/招标需求、业务制度或行业标准作为学校侧证据。

以下一律判定外部对标失败：

- 只写厂商名称；
- 只看搜索结果摘要；
- 没有 URL、标题、发布方、日期和证据位置；
- 用不符合 `CLAUDE.md §5.1` 的普通仓库“对标总结”循环证明三家产品；
- 只有厂商宣传，没有高校采用证据；
- 只有高校使用产品的新闻，但无法证明当前模块能力；
- 把“行业常见做法”分配给某家厂商；
- 先写“未核验”，后面又写成该厂商亮点、缺点或三家共同能力；
- 把学校采购需求误写成某厂商已经实现；
- 为凑三家编造产品、模块、客户或功能。

如果无法完成三家证据：

1. 二级施工包状态写 `blocked`；
2. 只输出证据缺口、已经找到的有效来源和下一步检索方向；
3. 不写厂商具体功能；
4. 不生成正式三级施工卡；
5. 不进入代码施工。

### 四、把证据转成设计，不是堆链接

先形成横向对标矩阵，至少比较：

- 使用角色；
- 真实业务场景；
- 主流程和异常流程；
- 页面/工作区；
- 字段与校验；
- 按钮和批量动作；
- 状态机；
- 权限、数据范围和业务关系；
- 敏感字段与审计；
- Excel、附件、通知、待办、统计、打印、归档；
- 初始化、部署和学校验收。

分级：

- 三家均有：核心候选；
- 两家有：常见能力；
- 一家有：差异化能力；
- 学校采购/制度明确要求：学校刚需候选；
- 仅 AI 建议：`ai_proposal`，不得直接施工。

“共同能力”仍须通过本项目边界、职业院校适用性、权限安全、现有架构和可交付性判断，不能机械照搬。

每项正式设计必须写 `designSource`：

- `external_evidence`
- `project_rule`
- `existing_code`
- `user_decision`
- `ai_proposal`

### 五、完整读取并实例化权限总控

必须完整读取：

`docs/03-业务模块设计/系统管理中心/00-系统管理中心-权限角色模块授权与权责边界设计.md`

不能只写“已读”。必须在二级总包和每张三级卡中把权限落到实际页面、按钮和 API，覆盖：

- 当前激活角色；
- 租户和学校模块授权；
- menu/API/button permission key；
- 数据范围；
- 真实业务关系及来源表/规则；
- 字段权限、敏感字段、脱敏、明文查看；
- 状态和审批节点限制；
- 审计事件、原因、二次确认、水印、责任快照；
- 401/403/404/409/422 等拒绝码；
- 正向、反向、跨范围、跨租户、敏感查看和导出测试。

只有学校才能决定的审批层级、时限、统计口径、开关策略可以列为“用户决定项”。代码路径、接口、表、迁移、测试是否存在等问题必须由你查清，禁止问非技术用户。

### 六、生成二级模块生产级总施工包

使用：

`docs/06-开发施工与质量验收/二级模块生产级施工包标准模板.md`

保存到：

`docs/03-业务模块设计/<业务中心>/施工包/<二级模块>-生产级施工包.md`

总包必须覆盖：

- 当前事实审计；
- 三家外部证据和学校侧证据；
- 横向对标与采用/不采用决定；
- 全部三级模块；
- 跨三级模块共享的状态机、数据模型、API、权限、审计和依赖；
- 测试、部署、升级、回滚和甲方验收；
- 每张三级施工卡的路径和状态。

### 七、为全部三级模块逐一生成独立施工卡

使用：

`docs/06-开发施工与质量验收/三级模块生产级施工卡标准模板.md`

保存到：

`docs/03-业务模块设计/<业务中心>/施工包/<二级模块>/三级施工卡/<序号>-<三级模块>-生产级施工卡.md`

每个三级模块必须有且只有一张主施工卡。每张卡必须独立写清：

1. 当前真实代码资产和缺口；
2. 使用角色、业务结果、进入/退出条件；
3. 页面布局、路由、深链、三态和公共组件；
4. 全部字段、默认值、校验和数据映射；
5. 全部按钮、出现/可用条件、API、状态和审计；
6. 主流程、异常、撤回、退回、超时、关闭、重开、重复和并发；
7. 可编码 API 契约；
8. MySQL 表、字段、索引、约束、Alembic 升级和回滚；
9. 逐动作权限、数据范围、业务关系、敏感字段和拒绝码；
10. Excel、附件、通知、待办、统计、打印、归档的适用性；
11. 正常、异常、无权限、跨范围、跨租户、敏感、并发测试；
12. 非技术甲方验收步骤；
13. 部署、初始化、health check、日志、备份和外部服务降级；
14. 精确到文件和 symbol 的施工顺序与验证命令。

禁止用“见总册”“见 API 文档”“开发时补”“按公共规则处理”省略关键施工信息。

### 八、三级施工卡完成门禁

逐张检查：

- 外部设计均能回链二级总包的有效证据 ID；
- 当前代码、路由、model、migration、测试已经实际核验；
- 字段、按钮、API、状态、权限、审计可直接编码；
- 跨租户、越权、敏感、非法状态和并发测试完整；
- MySQL升级、历史兼容、部署和回滚完整；
- 技术事实没有甩给用户确认；
- 未验证厂商能力和 `ai_proposal` 没进入正式范围；
- 与其他三级卡的共享表、接口、路由和状态无冲突。

一项不满足，该卡只能标 `draft / blocked`。全部满足才能标 `design_ready`。

二级总包只有在全部三级卡都达到 `design_ready` 后，才能达到 `design_ready`。

### 九、默认继续真实施工

除非用户明确写“本次只要施工卡，不改代码”，二级总包和全部三级卡通过门禁后，必须在同一次任务继续：

1. MySQL model/Alembic；
2. 后端 router/schema/service；
3. 权限、数据范围、业务关系、敏感字段和审计；
4. 前端路由、页面、公共组件和真实 API；
5. Excel/附件/通知/统计等适用能力；
6. 自动化测试、build/lint、MySQL验证、smoke；
7. 部署、初始化和回滚检查；
8. navPlan、施工图、施工记录和历史欠账同步。

未经用户明确要求不得 `git add/commit/push`。

### 十、最终报告必须给证据

最终只报告可核验事实：

- 二级总包路径和状态；
- 全部三级施工卡路径和状态；
- 三家外部证据数量、学校侧证据数量；
- 当前代码基线 commit；
- 实际新增/修改文件；
- 实际执行的测试命令和结果；
- 未执行、超时或失败项；
- MySQL、权限、敏感数据、部署和回滚影响；
- 哪些属于学校业务决定；
- `git status --short`。

不得把“文档很长”“有三家名称”“存在测试文件”“计划运行 pytest”写成完成证据。
