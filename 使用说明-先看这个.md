# 跃科 Codex 规则包｜先看这里

编制日期：2026-09-06。用途：把开发重心从泛化架构治理，收回到现有四端核心业务的前端重构、真实流程和易用性。
本包是项目规则建议及可安装文件，不是测试程序，也不代表已经修改 GitHub、跑通业务或完成学校验收。

## 一、只维护一份主规则

- `AGENTS.md`：Codex 主规则，放仓库根目录。
- `CLAUDE.md`：替换旧版为共用规则入口，放仓库根目录；Claude Code 按文字指引读取 AGENTS.md，不维持两套相反政策。
- `docs/06-开发施工与质量验收/Codex-前端重构与四端验收.md`：只在相关任务按需读取的办理流程/验收细则。
- 本说明：给负责人和首次安装者阅读，无须自动装入每个开发任务上下文，也不要覆盖仓库 README.md。

不要把新内容追加到旧 CLAUDE.md 末尾。采用新规则时，迁移仍有效的项目限制，修订原冲突条款；不要把过期“必须兼容旧 route”重新合并回来。
无需为这套规则安装 Spec Kit、插件或新框架；它们的原理用于编写规则，不构成新的项目依赖。

## 二、交给 Codex 安装，无需自己懂 Git

把 ZIP 放在本地 SaaS 仓库可访问的位置，然后将下面一段发给 Codex。安装只涉及规则文件，不包含业务施工。

```text
读取我提供的“跃科SaaS-Codex前端重构与四端验收规则-20260906.zip”。
先核验当前仓库、工作区、已有授权分支，以及现有 AGENTS.md、AGENTS.override.md、CLAUDE.md 和目录级规则。
将包内 AGENTS.md、CLAUDE.md、docs/06-开发施工与质量验收/Codex-前端重构与四端验收.md 安装到对应位置。
同名文件有新改动时先比较再合并；保留有效安全和明确冻结限制，替换过期条款，不并存两套相反规则。
重点是前端重构、四端同一业务故事跑通、老师学生便捷；旧 route 不默认兼容，但不能误删真实数据或未迁移的消费者。
本次只改这三份规则，不改业务源码，不新建分支/worktree，不推送、不合并、不部署；本地是否提交沿用已明确的提交授权。
检查相对路径和规则冲突，列出实际改动与未验证项；本包使用说明无需覆盖仓库 README。
```

安装后在仓库根目录新开一次 Codex 会话，让它说明读到了哪些规则，并用业务语言概括本轮目标。
官方文档说明项目规则在运行开始时发现，存在全局/目录覆盖与读取大小限制；不能仅因文件已复制就假定所有旧会话已刷新规则。[E1]
不要为了省确认启用全盘权限、关闭沙箱或绕过生产审批。

## 三、后续发任务，只说业务目标

例如：“继续重构学工请假办理，按照认可的原型，把学生申请、老师处理、退回重交和销假走通。”
不必替 Codex 指定 Vue 文件、SQL 或测试 fixture；它应自行核验实际代码和流程。

可复用下面的施工提示词（安装规则完成后使用）：

```text
按照仓库 AGENTS.md，继续当前已授权的前端重构任务。
本轮聚焦一个核心业务故事，沿用认可的设计，目标是老师学生容易找到、看懂并办完，不是只换皮或堆说明文字。
先核验原分支最新代码和真实入口，再修本故事的前端及直接关联阻断；不要顺手全面重构后端，也不要默认兼容旧 route。
用正常测试角色验证同一业务对象在管理/教师 PC、学生 PC、教师移动端、学生移动端的适用交接，包含退回重交、写后回读和关键异常。
后端、权限、文件、待办、台账或 xlsx 真有问题就定位同一根因，不能用假数据、超级权限或改弱测试制造成功。
测试、MySQL、H5、微信构建与微信宿主实测分别报告；没跑的明确未跑。
在原授权范围连续核验、修复、验证和复审，不需要我替你判断技术问题；推送、合并和生产操作沿用明确授权。
最后给我业务结果、前后界面证据、四端通过/未验表和一个最重要的下一步。
```

## 四、比上一版调整了什么

保留用户提供的 179 行 CLAUDE.md 中的事实核验、单分支、真实数据保护、唯一业务真值、MySQL、租户权限、旧链路退役和同 SHA 验证纪律。
主线从“平台后端治理条目”前移为“当前业务故事 → 前端办理 → 角色交接 → 易用性 → 真实回归”。

具体取舍：
- 不要求先解决全仓后端债务才允许改当前页面；仅收口本故事的直接阻断与共享风险。
- 不把“前端重构”默认为只能改 CSS，也不把明确 UI-only 任务扩成全栈重构。
- 不要求四端复制同一功能；按职责验证同一事实链，N/A 必须有业务依据。
- 不把 H5 构建、微信构建当成两个业务端，或当作微信宿主已经测试。
- 不默认保留旧 route；迁移消费者后退役，但不自动解冻明确冻结文件、不删真实数据。
- 不预装 Spec Kit、多智能体、自动全仓测试或新组件库；先用现有 Vue 和 Playwright 体系。

## 五、来源与采用边界

### 用户材料

[P1] 本次会话提供的 `CLAUDE.md`，179 行，标题“跃科大型 SaaS 工程执行规则”，2026-09-06。
采用其工程与安全原则；本包不是只改文件名，而是按用户最新“Codex 主力、前端主业、四端流程和便捷性”要求重新排列和补充执行细则。

[P2] 文件库《学工中心四端上线前业务流程顺畅与师生便捷性重构施工总控 V4.0》，2026-08-30。
本次读取开头四端定义与体验目标、流程验收条件相关片段；未把 2268 行全文作为已复审实现事实。
采用 Staff PC / Student PC / Teacher Mini / Student Mini、六个流程问题、同一业务对象交接、写后回读、移动宽度检查。
该历史文档“全部旧路由继续兼容”与用户后续明确要求相反，未沿用；其历史 GO、PR、SHA 不进入新规则。

### 当前仓库事实（只作此次编制依据，不写死到长期主规则）

[R1] 读取 `penghaibin9/saas` 原 PR #261 的 HEAD `00119fbcef59b73feec997af9cc533476898e266`。
根目录树包含 CLAUDE.md、frontend、student-portal、miniapp、e2e、enterprise-portal 等，未列出根 AGENTS.md。
这只说明本次读取的该分支根目录，不代表用户本地工作区或全部子目录没有规则。

[R2] 同一 HEAD 的 `frontend/package.json`、`student-portal/package.json`、`miniapp/package.json`、`e2e/package.json`。
采用实际 npm scripts。管理 PC 是 Vue3/Vite；移动工程声明教师与学生入口；E2E 已使用 Playwright。
未在此任务启动这些应用或运行仓库测试；“脚本存在”不是“测试通过”。以后执行前必须重新核对当前代码。

### 外部一手资料（2026-09-06 查阅）

[E1] OpenAI：Custom instructions with AGENTS.md。
https://developers.openai.com/codex/guides/agents-md
本次访问重定向至 https://learn.chatgpt.com/docs/agent-configuration/agents-md 。
采用 AGENTS.md 主入口、覆盖规则与按需组织；官方说明默认合并读取上限为 32 KiB。未把 CLAUDE.md 说成 Codex 默认自动发现文件。

[E2] OpenAI：Best practices。
https://developers.openai.com/codex/learn/best-practices
本次访问重定向至 https://learn.chatgpt.com/guides/best-practices 。
采用简短准确的长期规则、真实命令、明确完成条件、必要计划与测试复审；不照抄特定版本的界面操作或扩大执行权限。

[E3] OpenAI Codex 仓库的 AGENTS.md。
https://github.com/openai/codex/blob/main/AGENTS.md
参考可复审变更、相关测试和工程约束；不把其 Rust/TUI 命令、专属目录、行数阈值照搬到 Vue/FastAPI 项目。

[E4] GitHub Spec Kit 与宪法模板。
https://github.com/github/spec-kit
https://github.com/github/spec-kit/blob/main/templates/constitution-template.md
采用可验证原则、质量/测试/体验/性能和治理的组织思想；不导入整套规范驱动流水线，不要求普通 Bug 重做产品立项。

[E5] Microsoft Playwright：Best Practices。
https://playwright.dev/docs/best-practices
采用验证用户可见行为、隔离测试身份/状态、稳健定位和条件断言；不将浏览器测试等同于所有外部系统/微信宿主实测。

四端职责、具体教育业务、旧 route 退役和 Git 授权限制来自用户项目；易用性检查表与故事候选是本包设计建议，不是上述开源项目替学校确认的制度。

## 六、本次交付边界

本包只生成 Markdown 文件与 ZIP；检查文件可读、引用路径、关键规则及压缩包完整性。
未覆盖 GitHub 原文件，未新增分支或提交，未执行应用测试，未修改 PR 状态。
规则需要在实际 Codex 任务中落实为代码、测试和审查；规则文本本身不能保证 AI 永不出错或学校业务自动验收。
