# AGENTS.md

> GPT / Codex 等代码智能体的仓库级入口规则。目标不是重复已有长提示词，而是让每次施工都遵循同一套工程边界、验证顺序和完成标准。

## 1. 适用范围

本文件适用于整个仓库。

当某个子目录存在自己的 `AGENTS.md` 时：

- 先遵守本文件的全仓红线；
- 再遵守离目标代码最近的子目录 `AGENTS.md`；
- 业务行为以现有源码、数据库迁移、接口契约、测试和对应模块验收文档共同佐证；
- `CLAUDE.md` 是已有的综合工程规则和历史约束来源，涉及跨模块、生产验收或不确定约定时必须一并阅读；
- 如果不同规则真实冲突，不允许静默选择更容易通过测试的一条，必须在结论中指出冲突及影响。

## 2. 仓库地图

主要工程目录包括：

- `backend/`：FastAPI 后端、SQLAlchemy 数据访问、Alembic 数据库迁移、Pytest 测试；
- `frontend/`：Vue 3 + Vite 的 PC 管理端；
- `student-portal/`：学生 PC 门户；
- `enterprise-portal/`：企业相关门户；
- `miniapp/`、`mobile/`：移动端 / 小程序相关工程；
- `e2e/`：Playwright 真实浏览器端到端验收；
- `docs/`：架构、业务、验收、施工和运维文档；
- `.github/workflows/`：GitHub Actions 自动化门禁。

修改前先确认代码真正属于哪个工程，不要因为名称相似就在错误端重复实现一套。

## 3. 不可违反的工程红线

### 3.1 先找现有实现，再写新代码

新增任何以下对象前，必须先在仓库中搜索同义实现：

- route / endpoint（路由 / 接口）；
- service（业务服务）；
- schema / DTO（接口数据结构）；
- model（数据模型）；
- enum / status（枚举 / 状态）；
- helper / utility（辅助函数）；
- permission / data-scope（权限 / 数据范围）；
- API client（前端接口客户端）；
- E2E bootstrap / fixture（端到端测试前置数据工具）。

优先扩展唯一权威实现。除非确有边界差异，不得复制一份“新版”“临时版”“v2 helper”绕过旧实现。

### 3.2 修根因，不修绿灯

禁止为了让测试通过而：

- 放宽认证、授权、租户隔离、数据范围或业务校验；
- 修改生产状态机以迎合测试脚本；
- 删除必要的审计、幂等、并发或事务约束；
- 在前端伪造后端成功结果；
- 在 E2E 中用 API、数据库直改或脚本注入替代本应由真人完成的业务动作；
- 把真实产品缺陷归类为“测试不稳定”后跳过。

遇到红灯先分类：

1. `PRODUCT_BUG`：生产代码 / 业务合同缺陷；
2. `RUNNER_BUG`：测试执行器、定位器、等待条件或脚本缺陷；
3. `ENVIRONMENT_BUG`：依赖、端口、服务、浏览器、CI 环境缺陷；
4. `TEST_DATA_BUG`：前置数据或数据隔离缺陷。

只有完成分类后才修改对应层。

### 3.3 正式数据库验收使用 MySQL

仓库后端正式验收为 MySQL-only（正式验收只能以 MySQL 为准）。

- SQLite 可以用于与正式验收无关的局部实验，但不得作为 MySQL 行为的通过证据；
- 涉及迁移、索引、唯一约束、锁、并发、事务、字符集、排序规则时，必须在 MySQL 上验证；
- 不得为了兼容 SQLite 改弱正式 MySQL 约束。

### 3.4 最小生产安全改动

每次修复默认采用最小改动面：

- 不顺手重构无关模块；
- 不无故改公共组件、公共枚举、公共 API 和共享权限体系；
- 不批量格式化与任务无关的文件；
- 不引入第二套状态源、第二套路由或第二套数据模型；
- 不修改生成产物来掩盖源代码问题；
- 不提交密钥、Token、账号密码、生产数据或隐私数据。

如果必须做跨模块改动，要先说明为什么局部修复不足。

## 4. 标准施工流程

每个编码任务按以下顺序执行：

1. **读取上下文**：目标代码、最近的 `AGENTS.md`、相关测试、迁移、接口和模块验收文档；
2. **搜索复用**：确认没有已有 route / service / schema / helper / test driver 可以复用；
3. **建立事实**：复现问题或明确需求当前行为，不凭文件名猜；
4. **分类问题**：`PRODUCT_BUG / RUNNER_BUG / ENVIRONMENT_BUG / TEST_DATA_BUG`；
5. **做最小修复**：只改根因相关文件；
6. **精准验证**：先跑最窄测试或单一失败断言；
7. **受影响门禁**：精准测试通过后，再跑受影响业务门禁；
8. **最终封板**：只有需要发布 / 合并时才跑对应完整门禁，不要每次小修都全量重跑；
9. **报告证据**：列出修改文件、测试命令、PASS / FAIL、仍未证明的部分。

## 5. 测试原则

测试层级从窄到宽：

`静态检查 / 单测 → API / Service → MySQL 集成 → 真实浏览器 E2E → 受影响 CI → 最终同 SHA 封板`

要求：

- 优先精准重跑，不反复重跑已经证明无关的整套流程；
- 失败必须保留第一处真正失败证据，不能只报最后一个连锁报错；
- 用户可见业务流如果声明“已完成”，必须满足根目录 `ACCEPTANCE.md`；
- 测试代码本身也属于生产质量体系，不允许通过复制 Runner、堆 sleep 或宽松断言制造偶然绿灯。

## 6. 代码质量要求

修改代码时同时检查：

- 正确性：状态流转、边界条件、错误处理是否完整；
- 安全性：认证、授权、租户、数据范围、输入校验是否保持；
- 一致性：前后端枚举、字段、状态和错误语义是否一致；
- 数据完整性：事务、唯一约束、幂等、并发冲突是否正确；
- 可维护性：是否复用现有抽象，是否引入死代码或重复代码；
- 可观测性：必要错误是否可定位，审计事件是否可追踪；
- 回归风险：是否影响共享路由、组件、模型、迁移或公共权限。

禁止用“理论上可以”代替证据。

## 7. 完成定义

任何任务只有达到 `ACCEPTANCE.md` 中与本次改动相关的门禁，才能声明完成。

最低要求：

- 代码实现与现有合同一致；
- 最窄相关测试通过；
- 数据库 / 权限 / 审计等受影响维度已验证；
- 用户可见流程需要真实页面证据时，真实浏览器已通过；
- 没有通过跳过、mock、数据库直改或放宽断言制造假绿；
- 清楚列出尚未执行的验证，不把“未运行”写成 PASS。

## 8. Git 与变更边界

- 一个任务尽量保持单一目的；
- 只提交本任务相关文件；
- 未明确授权时，不合并到 `main`，不强推分支；
- 发现工作区存在其他任务改动时，不覆盖、不回滚、不顺手清理；
- 合并前以目标 PR 的 exact head SHA（精确提交版本）对应证据为准，避免拿旧提交的绿灯替代当前提交。

## 9. 输出要求

每次施工结果至少说明：

- 根因 / 需求落点；
- 修改了哪些文件；
- 为什么这是最小修复；
- 实际跑了哪些测试；
- 哪些是已证明 PASS，哪些仍是未验证；
- 是否存在产品 Bug、Runner Bug、环境问题或测试数据问题；
- 下一步最窄动作是什么。

不要用模糊的“基本完成”“应该没问题”“理论通过”作为封板结论。
