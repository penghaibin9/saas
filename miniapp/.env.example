# 小程序 / H5 端环境变量示例（复制为 .env / .env.production；勿提交真实服务器地址）
#
# VITE_API_BASE_URL：后端「源」地址，前缀 /api/v1 自动拼接，勿带 /api 或末尾斜杠。
#   本地开发：       http://localhost:8000
#   H5 服务器演示：  http://服务器IP        （经 Nginx location /api/ 反代到后端）
#   mp-weixin 真机： http://服务器公网IP:端口 （小程序需绝对可达地址，并在微信后台配置合法域名）
VITE_API_BASE_URL=
#
# VITE_USE_MOCK：数据来源开关。
#   缺省 / true / 1 → 纯 mock 演示（无需后端，秒开）
#   false / 0       → 真实后端优先（失败回退 mock 骨架，不白屏）
#   真实/MySQL 联调：置 false 且填好 VITE_API_BASE_URL，并先启动后端(uvicorn)+种子数据。
VITE_USE_MOCK=
# 仅本地开发可启用演示账号填充；production 构建会强制关闭。
#
# VITE_PRIVACY_URL / VITE_TERMS_URL：可选。协议与隐私政策正文已内置在小程序包内
#   （src/config/legalDocs.js），留空即可正常打开、无需业务域名配置。仅当学校发布了
#   正式法务版本、希望改用外链时才填；填了必须同步在小程序后台配置「业务域名」，
#   否则 web-view 会白屏。
VITE_PRIVACY_URL=
VITE_TERMS_URL=

# 微信小程序 AppID（wx 开头 18 位）。发布脚本按 环境变量 WECHAT_APPID → 本项 → manifest.json
# 的顺序解析；配置后 npm run build:mp-weixin:release 的产物可直接在微信开发者工具上传。
VITE_WECHAT_APPID=
