# CLAUDE.md
## 0.0 最高必读：市场验证优先与成熟商业系统复刻规则

本项目所有模块开发，必须坚持：

效果第一。
真实好用第一。
市场验证第一。
成熟商业系统对标第一。

本项目不是让 AI 自己发明功能，也不是按菜单名硬造页面。本项目当前还没有经过真实学校大规模使用验证，因此不得把本项目早期设计当成最终标准。

凡是毕业设计、岗位实习、教务、学工、迎新等核心模块，必须优先对标已经被大量学校采购、部署、使用、验收过的成熟商业系统、招标需求、操作手册、业务流程资料。

经过大量学校真实使用过的系统，已经踩过很多业务坑、交付坑、验收坑、使用习惯坑。它们的核心流程、字段、权限、台账、导入导出、审核确认、学生端、教师端、移动端设计，默认优先级高于本项目早期浅设计，也高于 AI 自己想象。

本项目的目标不是从零发明，而是：

1. 找到成熟系统怎么做；
2. 至少对标三家成熟产品或真实业务资料；
3. 提炼三家以上成熟产品的共同核心；
4. 吸收其中最好用、最容易交付、最容易验收的设计；
5. 避开别人已经踩过的坑；
6. 用本项目自己的 SaaS 架构、代码、UI、MySQL、权限、审计和测试重新实现；
7. 在成熟系统基础上做得更统一、更好用、更适合多租户交付。

核心原则：

1. 市场验证过的成熟产品流程，优先级高于 AI 想象。
2. 市场验证过的成熟产品流程，优先级高于本项目早期浅设计。
3. 三家成熟系统共同具备的能力，默认就是本项目必须具备的基础能力。
4. 早期文档没写，不代表不需要；成熟系统普遍有，就必须认真评估补齐。
5. 如果现有文档设计过浅，必须按成熟商业系统补强。
6. 不允许 AI 只凭想象补三级菜单、补页面、补按钮。
7. 不允许做卖不出去的自嗨功能。
8. 不允许自己瞎造轮子。
9. 没有真实业务场景、字段、流程、状态机、权限、审计、验收标准的目录，不得进入正式菜单，不得标 implemented。
10. 如果暂时做不到成熟系统同等效果，必须标 partial，写入历史欠账，不能假装 implemented。

每次模块开发前，必须先输出“三家成熟系统对标表”：

1. 对标对象 A：核心流程、角色、字段、亮点、缺点；
2. 对标对象 B：核心流程、角色、字段、亮点、缺点；
3. 对标对象 C：核心流程、角色、字段、亮点、缺点；
4. 三家共同具备的核心功能；
5. 三家里面最值得吸收的最佳做法；
6. 本项目当前已有能力；
7. 本项目缺失的生产级闭环；
8. 本轮必须补齐的内容；
9. 暂时进入 backlog 的内容；
10. 禁止做成假功能的内容。

复刻标准：

1. 复刻成熟系统的业务闭环；
2. 复刻成熟系统的用户使用效果；
3. 复刻成熟系统的核心流程；
4. 复刻成熟系统的字段完整度；
5. 复刻成熟系统的状态机严谨度；
6. 复刻成熟系统的权限和数据范围；
7. 复刻成熟系统的 Excel 导入导出、台账、报表、审计；
8. 复刻成熟系统的学生端、教师端、管理端协同方式；
9. 复刻成熟系统的学校验收口径；
10. 在本项目中重新实现，不照搬旧代码结构；
11. 在本项目中做得更统一、更可维护、更适合 SaaS 多租户。

执行标准：

成熟系统怎么真实好用，本项目就按那个效果做。
成熟系统怎么让学校验收，本项目就按那个标准做。
成熟系统有的关键闭环，本项目不能缺。
三家成熟系统都有的能力，默认就是本项目必须具备的基础能力。

如果本项目早期文档和成熟系统对标结果冲突：

1. 业务流程、功能闭环、字段、台账、导入导出、学生端、教师端、验收效果，以成熟商业系统为准；
2. 权限核心、数据范围核心、租户隔离、真实数据、审计日志、MySQL、Git 安全规则，以 CLAUDE.md 和权限总控文档为准；
3. 不能静默冲突，必须输出冲突点、采用方案、理由和是否进入 backlog。

如果当前 AI 无法联网搜索，必须说明无法联网，并改为搜索仓库内已有资料：

1. 竞品研究；
2. 招标需求；
3. 操作手册；
4. 历史设计文档；
5. 已有模块实现；
6. 历史欠账；
7. 同类成熟模块代码。

不得假装已经完成外部对标。

一句话：

不迷信自己的早期设计，不闭门造车，不让 AI 自己发明产品；优先复刻经过市场验证、学校真实使用、客户真实付费的成熟商业软件效果，再用本项目自己的代码、UI、架构和 SaaS 能力做得更好。
## 0. 项目最高定位
本项目是《高校学生全生命周期管理平台》，目标是建设真实可落地、可维护、可部署、可销售、可持续迭代的生产级多租户 SaaS 系统。
本项目不是演示系统，不是 Demo，不是样板页，不是单校定制后台。开发模块前必须对标经过市场验证的同类商业软件公司开发的同类产品进行最高规格的模仿设计以达到生产级要求。允许复刻，目标就是对标超过同类软件。
开发时必须默认按真实商业化产品处理：
- 能配置；
- 能授权；
- 能审计；
- 能验收；
- 能部署；
- 能扩展；
- 能按学校真实角色使用；
- 能支撑后续前后端真实接入；
- 能保留旧系统成熟能力；
- 能支撑学校试点、销售、交付、运维和长期迭代。
禁止把任何页面做成只有菜单、表格、假按钮、假数据的演示页。
本项目当前核心方向是：
- 工作台；
- 学工中心；
- 教务中心；
- 毕业设计中心；
- 岗位实习中心；
- 系统管理；
- 多租户权限；
- 角色切换；
- 数据范围；
- 业务关系授权；
- 敏感数据审计；
- 真实学校可用的商业化产品。
---
## 1. AI 开发总原则
执行任何任务前，必须先确认当前任务属于哪一类：
1. 文档补强；
2. 前端导航 / 路由 / 页面施工；
3. 后端接口 / 权限 / 数据范围；
4. 数据库 / 迁移；
5. 测试 / 验收；
6. 部署 / 运维；
7. 安全 / 审计；
8. 商业化 / 交付材料。
不同任务只能改对应范围内的文件，不允许跨范围乱改。
### 1.1 禁止事项
任何时候都禁止：
- 不读文档就直接改代码；
- 把系统说成演示版 / Demo / MVP / 简化版；
- 推翻已有架构重做；
- 禁止无依据、无验收、无回滚地删除已有页面、路由、旧业务能力。
- 用未开发的新设计覆盖已有可运行页面；
- 新建空页面；
- 新建假按钮；
- 新建假数据页；
- 随意新增一堆文档；
- 批量替换全文；
- 自动格式化全仓；
- 修改无关文件；
- 绕过权限、数据范围、审计；
- 前端写死角色判断；
- 只靠前端隐藏菜单控制权限；
- 未经要求提交 git；
- 未经要求推送远程；
- 未经要求打 tag；
- 直接改生产数据库；
- 为了菜单完整强行创建未开发页面；
- 用浅的新设计覆盖老项目成熟能力；
- 把已有强业务缩水成简单 CRUD；
- 把老师、辅导员、学生、企业导师权限混在一起；
- 把多个角色权限叠加成超级权限；
- 在没有接口和权限约束的情况下做假交互。

新生产级模块完成并验收后：
- 旧菜单、旧搜索入口、旧 mock、旧空页面、旧 demo 页面、旧重复页面、无引用旧页面，可以按安全下线流程清理。
- 旧路由优先 redirect 兼容。
- 旧业务能力必须迁移或保留，不得缩水。
- 真实数据、审计、附件、租户、用户、权限核心表不得乱删。
### 1.2 默认工作方式
用户是非专业程序员，开发指导必须新手可执行。
每次任务输出必须包含：
1. 实际做了什么；
2. 修改了哪些文件；
3. 没有修改哪些禁止文件；
4. 运行了哪些检查命令；
5. 检查结果；
6. `git status --short`；
7. 是否存在无关改动；
8. 是否需要人工确认；
9. 下一步建议。
---
## 2. 文档阅读顺序
任何开发任务开始前，必须先按以下顺序读取文档。
### 2.1 第一优先级：项目入口
优先读取：
```text
README.md
00-只看这个-项目入口.md
```
存在 docs 目录下的入口文件时，也要读取：
```text
docs/00-只看这个-项目入口.md
docs/README.md
```
### 2.2 第二优先级：权限总控文档
凡是涉及以下内容：
- 工作台；
- 学工中心；
- 教务中心；
- 毕业设计中心；
- 岗位实习中心；
- 系统管理；
- 用户；
- 角色；
- 菜单权限；
- 按钮权限；
- 数据范围；
- 审批流程；
- 模块授权；
- 导入导出；
- 打印；
- 敏感字段；
- 学生数据可见范围；
- 老师能看哪些学生；
- 企业导师能看哪些学生；
- 学生本人数据；
- 审计日志；
- 工作台待办；
- 角色切换；
- 顶部搜索；
- 移动端入口；
- 数据驾驶舱；
- 权限 key；
- 旧权限兼容；
- 临时代理、转办、代管；
- 家长 / 监护人 / 企业导师外部身份；
必须先读取：
```text
docs/modules/00-系统管理中心-权限角色模块授权与权责边界设计.md
```
该文档是本项目关于权限、角色、模块授权、数据范围、业务关系授权、敏感字段、审计责任的最高约束文档。
当前任务涉及权限但没有读取该文档时，必须停止开发。
### 2.3 第三优先级：当前模块文档
根据当前任务读取对应模块文档。
学工中心读取：
```text
docs/modules/13A-学工中心全业务流程设计总册.md
docs/modules/13A-学工中心页面树与路由设计.md
docs/modules/13A-学工中心API契约草案.md
docs/modules/13A-学工中心状态机与权限矩阵.md
docs/modules/13A-学工中心表单字段与校验规则.md
docs/modules/13A-学工中心页面级交互与按钮动作矩阵.md
docs/modules/13A-学工中心移动端入口设计.md
docs/modules/13A-学工中心-商业化对标审计与补丁建议(第一轮).md
docs/modules/13A-学工中心-商业化补强-Opus开发输入包.md
```
教务中心读取：
```text
docs/modules/13B-教务中心全业务流程设计总册.md
docs/modules/13B-教务中心页面树与路由设计.md
docs/modules/13B-教务中心API契约草案.md
docs/modules/13B-教务中心状态机与权限矩阵.md
docs/modules/13B-教务中心表单字段与校验规则.md
docs/modules/13B-教务中心页面级交互与按钮动作矩阵.md
docs/modules/13B-教务中心移动端入口设计.md
docs/modules/13B-教务中心-商业化对标审计与补丁建议(第一轮).md
docs/modules/13B-教务中心-商业化补强-Opus开发输入包.md
```
毕业设计中心读取（2026-07-11 起：先读模块 README，再按 README 推荐顺序；**生产级商业化开发总册为模块施工唯一总控**）：
```text
docs/毕业设计中心/README.md
docs/毕业设计中心/05-毕业设计中心生产级商业化开发总册.md
```
以及 README 索引的来源文档、施工记录、代码事实（navPlan.js、graduation*.py、models/graduation.py）。
岗位实习中心读取（2026-07-11 起：先读模块 README，再按 README 推荐顺序；**生产级商业化开发总册为模块施工唯一总控**）：
```text
docs/岗位实习中心/README.md
docs/岗位实习中心/06-岗位实习中心生产级商业化开发总册.md
```
以及 README 索引的页面树/升级总册/api/施工记录、代码事实（navPlan.js、internship*.py、models/internship.py）。
公共配置读取：
```text
docs/modules/13A-13B-学校参数配置中心设计.md
docs/modules/13A-13B-数据表与迁移策略草案.md
```
### 2.4 文档缺失处理
如果上述文档不存在：
1. 不允许编造文档内容；
2. 必须先搜索相近文件名；
3. 必须报告缺失文档；
4. 只能基于实际存在的文件继续；
5. 涉及权限的任务，在找不到权限总控文档时必须停止。
---
## 3. 权限、角色、模块授权最高规则
本项目是多租户 SaaS 系统，权限不能按单校固定后台写死。
权限判断必须按以下组合判断：
```text
系统内置角色模板
+
学校自定义角色
+
平台模块授权
+
菜单权限
+
按钮权限
+
数据范围
+
业务关系授权
+
敏感字段权限
+
审计要求
```
### 3.1 模块授权分两层
第一层：
```text
平台运营方 → 授权学校使用哪些模块
```
例如：
- 学工中心；
- 教务中心；
- 毕业设计中心；
- 岗位实习中心；
- 工作台；
- 系统管理；
- 数据驾驶舱；
- AI 能力；
- 移动端能力；
- 导入导出能力；
- 短信通知能力；
- 高级报表能力。
第二层：
```text
学校平台管理员 → 在已授权模块内给本校角色分配权限
```
学校管理员不能启用平台未授权模块。
学校自定义角色不能超过学校已授权模块。
学校自定义角色不能超过创建者自身权限。
### 3.2 学校可以自定义角色
学校必须可以自己新增角色，例如：
- 二级学院副书记；
- 专业负责人；
- 辅导员助理；
- 宿舍片区负责人；
- 奖助专员；
- 心理二级站老师；
- 教务秘书；
- 成绩复核老师；
- 实习专干；
- 就业专干；
- 毕设秘书；
- 班主任助理；
- 宿舍片区管理员；
- 活动审核老师；
- 第二课堂管理员；
- 外聘教师；
- 兼职教师；
- 企业联系人。
自定义角色规则：
- 只能在本校内生效；
- 必须绑定角色类型；
- 必须绑定数据范围；
- 不能超过创建者权限；
- 不能超过学校已授权模块；
- 权限变更必须写审计；
- 停用角色不删除历史业务记录；
- 历史业务记录保留原责任人和角色快照。
### 3.3 业务关系授权必须生效
教师能看哪些学生，必须来自真实业务关系，不是因为他叫“老师”。
必须遵守：
- 辅导员能看哪些学生，来自辅导员-班级 / 学生绑定关系；
- 班主任能看哪些学生，来自班主任-班级绑定关系；
- 专业课教师能看哪些学生，来自教学任务、课表、教学班、授课关系；
- 公共课教师能看哪些学生，来自本人授课教学班；
- 实训课教师能看哪些学生，来自校内实训课程和教学任务；
- 毕设导师能看哪些学生，来自毕业设计导师分配关系；
- 评阅教师能看哪些材料，来自评阅任务分配关系；
- 答辩专家能看哪些学生，来自答辩分组关系；
- 实习指导老师能看哪些学生，来自实习指导分配关系；
- 企业导师能看哪些学生，来自企业、岗位、协议、实习学生绑定关系；
- 宿管老师能看哪些学生，来自楼栋、楼层、宿舍片区关系；
- 审批人能处理哪些事项，来自审批流程节点；
- 创建人 / 责任人能处理哪些事项，来自本人创建、本人负责、被分派任务；
- 学生只能查看本人数据。
审批权限不等于永久业务管理权限。
业务关系变更后，后续权限必须同步变化，历史业务记录必须保留原责任人、角色、数据范围快照。
### 3.4 后端必须校验权限
禁止只靠前端隐藏菜单控制权限。
后端接口必须校验：
```text
模块授权
角色权限
菜单权限
按钮权限
数据范围
业务关系
敏感字段权限
审批节点权限
```
前端负责展示和交互体验，不能作为最终权限裁定。
---
## 4. 角色切换规则
一个用户可以拥有多个角色，但同一时间只能激活一个当前角色。
切换角色后必须同步变化：
- 左侧菜单；
- 二级菜单；
- 三级菜单；
- 工作台；
- 待办；
- 按钮权限；
- 数据范围；
- 统计口径；
- 移动端入口；
- 搜索入口；
- 可见数据；
- 可审批事项；
- 可导出范围；
- 敏感字段可见性。
示例：
```text
李老师
  - 辅导员：看自己负责班级学生
  - 专业课教师：看本人授课教学班
  - 毕设导师：看本人指导毕设学生
  - 实习指导老师：看本人指导实习学生
```
切换为辅导员时，不能录入课程成绩。
切换为专业课教师时，不能看学生心理、奖助、处分明细。
切换为毕设导师时，不能看其他导师学生材料。
切换为实习指导老师时，不能看无关实习学生。
多个角色权限不能合并成超级权限。
---
## 5. 默认角色体系
系统必须支持“内置角色模板 + 学校自定义角色 + 数据范围 + 业务关系授权”。
### 5.1 通用管理角色
至少包含：
- 平台超级管理员；
- 平台运营管理员；
- 平台运维支持；
- 学校系统管理员；
- 学校领导；
- 学院领导；
- 部门管理员；
- 数据管理员；
- 安全审计员；
- 流程管理员。
### 5.2 学工中心角色
至少包含：
- 学工处管理员；
- 学院学工负责人；
- 辅导员；
- 班主任；
- 宿管老师；
- 资助老师；
- 心理老师；
- 团委老师；
- 社团管理员；
- 学生工作助理。
### 5.3 教务中心角色
至少包含管理型角色：
- 教务处管理员；
- 学院教务员；
- 教学秘书；
- 课程管理员；
- 排课管理员；
- 考务管理员；
- 成绩管理员；
- 学籍管理员；
- 教材管理员；
- 教学质量管理员。
教务中心必须包含一线教学角色：
- 专业课教师；
- 公共课教师；
- 实训课教师；
- 教学班任课教师；
- 课程负责人；
- 专业负责人；
- 教研室主任；
- 监考教师；
- 阅卷教师；
- 成绩复核教师；
- 教学督导；
- 外聘教师；
- 兼职教师；
- 实训室管理员；
- 教材负责人。
专业课教师权限：
- 可查看本人授课课程；
- 可查看本人教学班；
- 可查看本人课程学生名单；
- 可查看本人课表；
- 可查看本人考试安排；
- 可录入本人课程成绩；
- 可暂存本人课程成绩；
- 可提交本人课程成绩；
- 可发起成绩更正申请；
- 可申请调停课；
- 不可审核自己提交的成绩；
- 不可发布成绩；
- 不可查看无关班级学生数据；
- 不可修改学籍；
- 不可修改培养方案；
- 不可修改毕业资格终审结果。
公共课教师权限：
- 数据范围以本人授课教学班为主；
- 不按某个专业天然拥有全专业学生数据；
- 可处理本人课程成绩、课表、考试、监考事项。
实训课教师权限：
- 面向校内实训课程；
- 可维护实训考勤；
- 可维护实训成绩；
- 可维护实训项目记录；
- 可维护实训材料；
- 与岗位实习指导老师区分，后者属于岗位实习中心，管理校外实习。
课程负责人权限：
- 可维护本课程大纲；
- 可维护本课程考核方案；
- 可维护本课程教学材料；
- 可查看本课程下多个教学班的教学运行；
- 可查看本课程成绩提交进度；
- 不等于成绩审核人；
- 不自动拥有成绩发布权限。
专业负责人权限：
- 可参与本专业培养方案；
- 可参与教学计划；
- 可参与毕业资格预审；
- 可参与毕业设计题目审核；
- 数据范围限本专业；
- 不等于教务处管理员。
教研室主任权限：
- 可查看本教研室教师教学任务；
- 可查看本教研室教师课表；
- 可查看本教研室成绩提交进度；
- 可查看本教研室教学评价；
- 可协调教学任务和课程材料；
- 不等于学院教务管理员。
教学督导权限：
- 可查看听课；
- 可查看评教；
- 可查看教学质量；
- 可查看课堂巡查相关数据；
- 不可修改成绩；
- 不可修改学籍；
- 不可修改学生核心档案。
监考教师权限：
- 只查看本人监考安排；
- 只查看本人考场；
- 只查看本人监考时间；
- 只查看学生名单必要字段；
- 不可查看学生完整成绩；
- 不可查看学籍隐私。
阅卷教师权限：
- 只处理分配给自己的阅卷任务；
- 不自动拥有成绩发布权限。
成绩复核教师权限：
- 只处理分配的成绩复核事项；
- 操作必须留审计；
- 不能绕过成绩更正流程直接改成绩。
### 5.4 毕业设计中心角色
至少包含：
- 毕设管理员；
- 学院毕设负责人；
- 专业负责人；
- 毕设导师；
- 评阅教师；
- 答辩秘书；
- 答辩专家；
- 学生。
### 5.5 岗位实习中心角色
至少包含：
- 实习管理员；
- 学院实习负责人；
- 实习指导老师；
- 企业导师；
- 企业联系人；
- 就业老师；
- 学生。
### 5.6 学生端角色
至少包含：
- 在校学生；
- 新生；
- 实习学生；
- 毕业生；
- 学生干部；
- 社团负责人。
学生不同阶段权限不同：
- 新生：迎新报到、材料上传、宿舍确认；
- 在校生：请假、奖助、活动、成绩、课表；
- 实习生：打卡、周报、实习材料；
- 毕业生：毕设、就业、离校、归档。
---
## 6. 敏感数据和审计规则
以下行为必须特殊控制：
- 查看完整手机号；
- 查看身份证号；
- 查看家庭经济信息；
- 查看心理明细；
- 查看处分详情；
- 查看困难认定材料；
- 导出学生名单；
- 导出奖助名单；
- 导出心理 / 处分 / 困难数据；
- 修改角色权限；
- 修改数据范围；
- 启用 / 停用账号；
- 模块授权变更；
- 导出；
- 打印；
- 批量操作；
- 查看完整家长联系方式；
- 查看教师内部备注；
- 查看企业导师评价明细。
要求：
- 最小授权；
- 二次确认；
- 填写查看原因；
- 水印；
- 审计日志；
- 导出留痕；
- 可追溯到人。
心理明细、困难材料、处分详情等敏感信息，不得因为老师拥有普通菜单权限就直接展示。
---
## 7. 当前产品模块总结构
前端一级导航最终按 6 个一级模块组织：
```text
工作台
学工中心
教务中心
毕业设计中心
岗位实习中心
系统管理
```
旧一级模块不再作为一级出现：
```text
学生中心
教学实践
数据中心
权限与流程
```
但旧模块中的成熟业务能力必须迁移或吸收，不得缩水。
旧页面不是永久资产。
新模块未完成前，旧页面可临时保留。
新模块完成并验收后，旧菜单和旧页面按安全下线规则处理（见 §37）。

**说明**：本节各子模块「可包含」只是产品能力池，不是立即开发清单。
没有业务设计、字段、流程、状态机、权限、审计、验收标准的目录，不得直接开发，不得挂正式菜单。
### 7.1 工作台
定位：
不同角色每天处理自己工作的地方。
可包含：
- 我的工作台；
- 我的待办；
- 审批中心；
- 消息通知；
- 领导驾驶舱；
- 最近访问。
原审批中心里的我的待办、已办、抄送、退回记录，归入工作台。
原数据中心里的生命周期总览、领导总览、数据驾驶舱，归入工作台或各业务中心统计。
### 7.2 学工中心
定位：
学生事务、辅导员工作、迎新、请假、奖助、宿舍、心理、风险、活动、谈话、家校等。
可包含：
- 学工看板；
- 学生画像；
- 班级管理；
- 数字迎新；
- 请假销假；
- 困难认定；
- 奖助勤贷补；
- 违纪处分；
- 心理健康；
- 风险预警；
- 谈心谈话；
- 家校联系；
- 宿舍管理；
- 学生活动；
- 第二课堂；
- 辅导员考评；
- 学工归档；
- 学工统计。
数字迎新作为学工中心二级模块，下面允许三级页面：
```text
学工中心
  - 数字迎新
      - 迎新看板
      - 新生报到
      - 报到进度
      - 缴费 / 绿色通道
      - 材料审核
      - 宿舍入住
      - 异常学生
```
不要把数字迎新的三级页面全部平铺到学工中心二级菜单。
### 7.3 教务中心
定位：
学籍、注册、培养方案、课程、课表、考试、成绩、学业预警、毕业资格审核。
可包含：
- 教务看板；
- 学年学期；
- 校历节次；
- 学籍管理；
- 注册管理；
- 学籍异动；
- 学院专业班级；
- 培养方案；
- 课程库；
- 教学计划；
- 教学任务；
- 课表管理；
- 调停课；
- 选课管理；
- 考务管理；
- 补考重修缓考免修；
- 成绩管理；
- 成绩审核发布更正；
- 学业预警；
- 毕业资格审核；
- 教材管理；
- 教学资源；
- 教务归档；
- 教务统计。
### 7.4 毕业设计中心
定位：
毕业设计全过程独立一级模块。
可包含：
- 毕设看板；
- 毕设学生；
- 选题管理；
- 导师分配；
- 开题材料；
- 过程检查；
- 成果提交；
- 查重记录；
- 答辩安排；
- 成绩评定；
- 问题预警；
- 毕设归档；
- 毕设统计。
老毕业设计项目里的强能力是成熟业务资产，新系统必须吸收，不得缩水。
### 7.5 岗位实习中心
定位：
岗位实习、企业岗位、打卡、周报、风险、就业跟进统一归这里。
可包含：
- 实习看板；
- 实习批次；
- 实习学生；
- 企业与岗位；
- 实习协议；
- 打卡管理；
- 周报批阅；
- 请假异常；
- 风险学生；
- 指导记录；
- 就业跟进；
- 未就业帮扶；
- 实习归档；
- 实习统计。
老岗位实习项目里的强能力是成熟业务资产，新系统必须吸收，不得缩水。
### 7.6 系统管理
定位：
用户、角色、菜单、数据范围、组织、品牌、参数、流程模板、日志、审计、第三方接口、租户配置。
可包含：
- 管理看板；
- 用户账号；
- 角色权限；
- 菜单权限；
- 数据范围；
- 组织结构；
- 学校参数；
- 系统与品牌；
- 流程配置；
- 审批模板；
- 日志中心；
- 安全审计；
- 第三方接口；
- 租户配置。
---
## 8. 已有页面和未开发页面冲突处理
已有可运行页面在新模块未完成前优先保留。
新模块完成并验收后，新模块成为唯一主入口。
旧菜单不再展示给普通用户。
旧路由 redirect 到新页面。
旧搜索入口改指向新页面。
旧页面文件确认无菜单、无路由、无搜索、无 import、无审计跳转引用后，可以进入删除清单（见 §37）。
1. 保留已有页面组件（新模块未完成前）；
2. 保留旧路由兼容（redirect / alias，不 404）；
3. 只调整菜单归属、显示名称、面包屑、activeMenu、搜索映射；
4. 在迁移表中标记“现有页面，后续按商业化设计补强”；
5. 不新建重复页面；
6. 不用未开发的新模块替换已有页面。
未设计、未开发、无真实业务场景的目录只进 backlog / hidden，不进正式菜单。
某个模块只有设计文档、没有前端页面时：
1. 不创建空页面；
2. 不创建假按钮；
3. 不显示可点击入口；
4. 放入待施工清单；
5. 后续施工包真正开发完成后再挂入菜单。
必须提前展示未完成菜单时：
1. 必须 disabled 或 hidden；
2. 必须标记“待施工”；
3. 不能点击进入空白页；
4. 不能影响当前已有页面使用。
---
## 9. 路由和菜单规则
### 9.1 旧路由必须兼容
旧路由不要直接删除。
以下旧入口存在时，必须保留或做 alias / redirect：
```text
/admin/student
/admin/orientation
/admin/campus-service
/admin/academic
/admin/internship
/admin/graduation
/admin/employment
/admin/data-center
/admin/approval
/admin/workflow
/admin/system
/admin/system/log
```
目标：
- 旧地址刷新不 404；
- 菜单显示新归属；
- route path 尽量兼容旧 path；
- 新 path 只能作为增强，不得导致旧 path 失效。
补充：
- 旧路由兼容不等于旧菜单继续展示；
- 旧路由可以 redirect 到新主页面；
- 旧菜单和旧搜索入口应指向新页面；
- 旧地址刷新不 404，但普通用户不再从菜单进入旧页面。
### 9.2 activeMenu / selectedKey / breadcrumb
调整导航后必须保证：
- 点击新菜单，高亮正确；
- 进入旧路由，高亮到新的一级模块；
- 二级菜单正确展开；
- 三级页面正确选中；
- 刷新页面后高亮不丢；
- 顶部搜索跳转后左侧菜单仍正确选中；
- 面包屑显示新结构。
面包屑示例：
```text
学工中心 / 数字迎新 / 新生报到
学工中心 / 请假销假
教务中心 / 成绩管理
教务中心 / 学业过程 / 课程成绩
毕业设计中心 / 选题管理
岗位实习中心 / 打卡管理
系统管理 / 角色权限
```
### 9.3 三级菜单规则
最终导航层级：
```text
一级模块：最左侧深蓝导航
二级模块：中间浅色侧栏业务分组
三级页面：二级模块展开后的具体页面，或进入二级模块后的页面内子导航
```
要求：
- 最左侧深蓝导航只放 6 个一级模块；
- 中间浅色侧栏显示当前一级模块下的二级模块；
- 二级模块下面有多个具体页面时，允许展开三级页面；
- 三级页面不要放到最左侧一级导航；
- 三级页面不要全部平铺到一级模块下面；
- 已真实闭环、仍在主业务链路中的三级页面保留。
- 未设计、未开发、重复、空壳、低价值、无验收标准的三级页面，不进入正式菜单，统一 hidden / backlog。
- 无引用旧三级页面可按安全下线规则删除（见 §37）。
- 三级页面旧路由必须兼容；
- 当前页面选中时，必须同时正确高亮一级模块、二级模块、三级页面。

### 9.4 菜单高亮与点击铁律（防「三级连成一片 + 再点不动」）

这是**反复出现的编码臭毛病**，必须永久遵守：

1. **高亮按唯一 leafKey，不按 path。** 侧栏三级高亮一律用唯一叶子标识（`模块key + '/' + label`）匹配，任何路由下**最多一个三级叶子高亮**。**禁止**用 `navRefMatches(path)` 逐叶判断（会把同 path 多叶子全部加 `is-active`，连成一片）。
2. **允许多个三级叶子指向同一 path**（同页不同动作/面板），但：
   - 点击**必须有反馈**：记录被点叶子的 leafKey，让高亮跟随点击移动；**禁止**因「path 已匹配就 return、跳过 router.push」导致点了没反应。
   - 路由定位取**「最具体」匹配**（路径最长 + 带 `?query=` 精确匹配优先），避免形如 `/admin/graduation` 的看板叶子用前缀规则抢占其所有子页高亮。
3. **跳转判断用「精确比较完整 URL」，不用前缀匹配。** 决定是否 `router.push` 时必须 `path !== currentFullPath`，**禁止**用 `navRefMatches(path)`（前缀规则）判断——否则形如 `/admin/graduation` 这类「其它子页的前缀路径」的看板/总览叶子，从子页点击时会被判为「已在此」而不跳转（表现为「点了看不了东西」）。菜单模块行（二级）同理。
4. **新增或改动 navPlan 三级项后，必须在侧栏实测**：① 当前页只亮一个叶子；② 从别的子页点该叶子能真跳过去且页面渲染；③ 每个可点叶子点击都有高亮/跳转反馈。不测不算完成。
5. 参考实现：`BasePortalLayout.vue` 的 `leafKey / isLeafActive / routeActiveLeafKey / effectiveActiveLeafKey / clickedLeafKey / onPlanLeaf / onTreeMod`（2026-07-08 收口，含前缀路径跳转修复）。

---
## 10. 权限 key 命名规范
权限 key 必须统一命名，不允许多套写法混用。
### 10.1 moduleKey
使用 camelCase：
```text
workbench
studentAffairs
academicAffairs
graduationDesign
internship
systemAdmin
```
### 10.2 menuKey
使用 module.page 格式：
```text
studentAffairs.leave
studentAffairs.orientation
academicAffairs.grades
academicAffairs.schedule
graduationDesign.topic
internship.attendance
systemAdmin.roles
```
### 10.3 permissionCode
使用 module.domain.action 格式：
```text
studentAffairs.leave.view
studentAffairs.leave.approve
studentAffairs.risk.handle
academicAffairs.grade.submit
academicAffairs.grade.review
academicAffairs.grade.publish
graduationDesign.topic.review
internship.weeklyReport.review
systemAdmin.role.update
```
### 10.4 buttonCode
使用 page.action 格式：
```text
leave.approve
leave.reject
grade.submit
grade.review
grade.publish
role.assignPermission
```
### 10.5 scopeType
使用大写枚举：
```text
SCHOOL
COLLEGE
MAJOR
CLASS
TEACH_CLASS
ADVISOR
MENTOR_STUDENT
ENTERPRISE_STUDENT
DORM_BUILDING
SELF
```
禁止：
- 中文权限 key；
- 前端临时拼权限 key；
- 同一权限出现多套命名；
- 只在前端定义权限 key；
- 新旧权限 key 无兼容映射。
---
## 11. 旧权限 key 与新模块兼容
旧模块迁移到新模块后，旧权限 key 不允许直接删除。
旧模块：
```text
student-center
orientation
campus-service
academic-process
teaching-practice
data-center
workflow
approval
system
```
新模块：
```text
studentAffairs
academicAffairs
graduationDesign
internship
workbench
systemAdmin
```
必须遵守：
1. 旧权限 key 不允许直接删除；
2. 兼容期内旧 key 可以映射到新 key；
3. 权限迁移必须保留历史审计可读性；
4. 旧业务记录里的历史 permissionCode 不批量改写；
5. 新开发统一使用新 key；
6. 权限迁移必须输出旧 key 到新 key 的映射表；
7. 涉及角色权限的迁移必须人工确认。
---
## 12. 学校内部临时代理、转办与代管规则
学校内部必须支持临时代理、转办、代管，但不能变成永久越权。
典型场景：
- 辅导员请假，临时代管班级；
- 教务员请假，临时代办审批；
- 毕设导师更换；
- 实习指导老师更换；
- 审批人出差，待办转办；
- 老师离职，账号停用与事项交接；
- 班主任临时代管；
- 学院管理员临时授权某老师处理指定批次。
必须明确：
- 临时代理不是永久授权；
- 必须有开始时间；
- 必须有结束时间；
- 必须有授权原因；
- 必须有授权范围；
- 必须有授权人；
- 必须有被授权人；
- 到期自动回收；
- 只能授权自己有权管理的数据范围；
- 不能授权敏感字段权限，除非单独审批；
- 所有临时代理、转办、代管必须写审计；
- 历史业务记录保留实际处理人和代理关系快照。
---
## 13. 家长 / 监护人 / 校外访客权限边界
家长 / 监护人默认不是学校后台角色。
未来开通家长端时，只能作为独立外部访问身份。
必须遵守：
- 家长只能查看本人孩子经学校授权开放的信息；
- 家长不得查看班级其他学生数据；
- 家长不得查看心理明细；
- 家长不得查看处分详情；
- 家长不得查看困难认定材料；
- 家长不得查看教师内部备注；
- 家长通知不等于家长登录权限；
- 家校联系中的联系方式查看仍需敏感字段权限和审计；
- 家长端开通必须受平台模块授权和学校开关控制。
企业导师、企业联系人也属于外部身份，必须限制在本企业、本岗位、本协议范围内，不能查看学生完整学籍、心理、奖助、处分等敏感数据。
---
## 14. 文档修改规则
用户不希望无意义新增大量文档。
默认规则：
```text
能追加到原文档，就不新建文档。
能补附录，就不重写总册。
能局部补强，就不推翻原设计。
```
除非用户明确要求，否则禁止：
- 新建一堆新总册；
- 重写已有文档；
- 用新文档覆盖旧文档；
- 批量替换历史 V1/P2/P3 字样；
- 删除原始设计内容；
- 改动无关模块文档。
### 14.1 文档补强方式
优先采用：
```text
在原文件末尾追加：
## 附录：xxx补强
```
或：
```text
## 附录A：xxx
## 附录B：xxx
```
### 14.2 历史口径解释
历史文档中的：
```text
V1
P2
P3
后置
只预留
不做
```
不能简单理解为缩水。
统一按以下口径解释：
```text
高频核心底座
+
商业化能力池
+
补强包
+
授权启用
+
人工替代流程
+
算法接口预留
```
禁止批量替换历史字样。
只允许在新增附录或说明中解释。
---
## 15. Git 和工作区规则
每次执行前必须运行：
```bash
git status --short
```
存在无关改动时：
- 只报告；
- 不处理；
- 不格式化；
- 不提交；
- 不覆盖；
- 不删除。
禁止执行会格式化全仓的命令。
必须格式化时，只格式化本次实际修改的文件。
未经用户明确要求，不允许：
```bash
git add
git commit
git push
git tag
git reset
git clean
```
最终必须输出：
```text
git status --short
```
并说明哪些改动是本次产生，哪些是任务前已有。
---
## 16. 开发执行流程
每次开发按 4 步执行。
### 第一步：只读检查
必须先输出：
1. 当前任务目标；
2. 必读文档是否已读取；
3. 当前相关文件清单；
4. 拟修改文件清单；
5. 禁止修改文件清单；
6. 当前 git status；
7. 是否存在无关改动；
8. 是否需要人工确认。
### 第二步：实施修改
只修改任务允许范围内的文件。
禁止扩大范围。
禁止顺手修无关问题。
禁止因为发现别的问题而跨模块乱改。
### 第三步：自检
按项目实际命令运行检查，例如：
```bash
npm run lint
npm run build
npm run test
pytest
```
项目没有对应命令时，说明原因，不要编造。
### 第四步：输出报告
必须输出：
1. 是否成功；
2. 修改了哪些文件；
3. 每个文件改了什么；
4. 未修改哪些禁止文件；
5. 检查命令结果；
6. `git status --short`；
7. 是否存在无关改动；
8. 是否有风险；
9. 是否需要人工确认；
10. 下一步建议。
---
## 17. 前端开发规则
前端开发必须遵守：
- 不做演示页；
- 不做空页面；
- 不做假按钮；
- 不把 mock 写死在组件里；
- 不破坏顶部栏；
- 不破坏角色切换器；
- 不破坏数据范围胶囊；
- 不破坏水印；
- 不破坏登录；
- 不破坏已有权限；
- 不破坏旧路由；
- 不破坏刷新高亮；
- 不破坏面包屑；
- 不破坏搜索入口。
页面按钮必须满足：
```text
按钮名称
出现条件
所需权限
当前状态是否可点
点击后打开什么
调用什么接口
成功状态
失败状态
是否写审计
```
列表页面必须包含：
```text
loading
empty
error
no permission
network error
validation error
submit success
submit failed
```
涉及导出、打印、查看敏感信息的按钮，必须带权限和审计要求。
---
## 18. 后端开发规则
后端开发必须遵守：
- 多租户隔离；
- 后端校验权限；
- 后端校验数据范围；
- 后端校验业务关系；
- 后端校验按钮权限；
- 后端校验敏感字段权限；
- 后端写审计；
- 不能只信前端；
- 不能只按角色名判断；
- 不能绕过状态机；
- 不能绕过审批流；
- 不能直接改终态数据；
- 不能跨租户返回数据。
所有接口必须考虑：
```text
401 未登录
403 无权限
404 不存在
409 状态冲突
422 校验失败
500 服务异常
```
涉及越权访问必须记录审计。
---
## 19. 数据库与迁移规则
未经明确要求，不允许改数据库。
需要改数据库时必须先输出：
1. 为什么必须改；
2. 涉及哪些表；
3. 是否已有表可复用；
4. 是否影响历史数据；
5. 是否需要迁移脚本；
6. 是否需要回滚方案；
7. 是否影响测试数据；
8. 是否影响生产部署。
禁止为了方便随意加表。
禁止绕过 migration 直接改生产结构。
---
## 20. 测试与验收规则
每个任务都必须给验收标准。
最少包含：
- 页面能否进入；
- 旧路由是否兼容；
- 菜单是否正确；
- 权限是否正确；
- 数据范围是否正确；
- 角色切换是否正确；
- 按钮是否按权限显示；
- 接口错误是否正确提示；
- 敏感字段是否脱敏；
- 导出是否带水印；
- 审计是否记录；
- 是否没有控制台错误；
- 是否没有空页面；
- 是否没有假按钮；
- 是否没有无关文件改动。
---
## 21. 老项目强能力处理规则
老毕业设计系统、老岗位实习系统中的成熟能力是业务资产，不是废代码。
新系统抽取后变浅时，必须按以下方式处理：
```text
老项目 = 成熟业务资产
新系统 = 统一 SaaS 架构底座
下一步 = 把老项目强能力反向补回新系统文档和页面设计
```
禁止：
- 用新系统浅设计覆盖老项目强能力；
- 删除老项目成熟业务流程；
- 从零设计一个更浅的模块；
- 直接重写整个模块；
- 把老项目能力说成不需要。
正确做法：
1. 读取老项目能力；
2. 对比新系统文档；
3. 找出缩水点；
4. 只在新系统现有文档中追加补强附录；
5. 后续前端按补强后的设计施工。
---
## 22. 当前高优先级开发顺序
当前项目后续开发建议顺序：
```text
1. 导航结构归位
2. 权限角色总控文档进入 CLAUDE.md
3. 学工中心页面与权限补强
4. 教务中心页面与权限补强
5. 毕业设计中心老项目能力反向补强
6. 岗位实习中心老项目能力反向补强
7. 前端按施工包逐步实现
8. 后端接口和权限真实化
9. 测试、审计、部署、交付
```
不得同时大范围并行乱改。
---
## 23. 任务完成报告模板
每次任务完成后，必须按下面格式输出：
```text
任务结果：
1. 是否完成：
2. 本次实际修改文件：
3. 本次未修改文件：
4. 是否新增文件：
5. 是否删除文件：
6. 是否改动后端：
7. 是否改动数据库：
8. 是否改动权限：
9. 是否改动路由：
10. 是否保留旧路由兼容：
11. 是否存在无关改动：
12. git status --short：
13. 检查命令：
14. 检查结果：
15. 风险点：
16. 需要人工确认：
17. 下一步建议：
```
---
## 24. 最后一条硬规则
本项目要做的是能卖给学校、能试点、能部署、能验收的真实 SaaS 产品。
任何 AI 执行任务时，都必须优先保护：
```text
已有可运行能力
旧路由兼容
权限边界
数据范围
业务关系授权
敏感信息审计
多租户隔离
前后端可持续维护
老项目成熟能力
学校真实角色使用场景
```
不要为了快速完成任务，把系统改成空壳、演示页或不可维护的假系统。

---

## 34. 生产级全栈模块开发总提示词引用规则

本项目后续所有模块开发，必须优先参考：

00-生产级全栈模块开发总提示词.md

用户每次指定模块开发时，Claude Code 必须：

1. 先读取 CLAUDE.md；
2. 再读取 00-生产级全栈模块开发总提示词.md；
3. 将其中的【模块路径】替换为用户指定模块；
4. 按“一个模块一次做深”的标准执行；
5. 覆盖前端、后端、数据库、接口、状态机、权限、数据范围、审计、导入导出、测试、部署检查；
6. 不得只做前端；
7. 不得只做菜单；
8. 不得依赖 mock 标记完成；
9. 不得把半成品标记为 implemented。

一句话原则：

模块做完 = 能部署、能访问、能存数据、能流转、能审计、能测试、能交付。
## 35. 施工记录与历史欠账规则

本项目采用“模块生产级闭环 + 历史欠账统一记账”的开发方式。

每完成一个模块后，除了输出生产级收口报告，还必须同步更新：

docs/施工记录/历史欠账.md

如果该文件不存在，必须创建。

每条历史欠账必须包含：

1. 所属模块
2. 问题描述
3. 当前影响范围
4. 是否阻断当前模块交付
5. 是否阻断正式上线
6. 是否需要后续模块反向补全
7. 建议处理时机
8. 当前状态：待处理 / 处理中 / 已完成
9. 关联文件或接口
10. 备注

历史欠账分类：

A. 不阻断当前模块交付，但需要记账；
B. 不阻断试点，但正式上线前必须处理；
C. 后续关联模块完成后必须反向补全；
D. 阻断当前模块真实闭环，必须立即返工；
E. 高风险问题，必须暂停继续开发并报告。

判断规则：

1. 不阻断当前模块闭环的问题，可以记账后继续下一个模块。
2. 阻断当前模块真实使用的问题，不能只记账，必须返工。
3. 阻断正式上线的问题，可以先记账，但上线前必须集中收口。
4. 后续模块依赖的问题，必须写清楚“由哪个后续模块反向补”。
5. 不允许把假完成、mock 冒充、无后端、无数据库、无权限、无审计这类问题记账后强行标 implemented。
6. navPlan.js 的 implemented / partial / planned 状态必须和历史欠账一致。
7. 每次开发新模块前，必须先读取 docs/施工记录/历史欠账.md，避免重复造轮子或漏掉反向补全任务。
8. 每次模块完成报告中，必须说明本轮新增了哪些历史欠账、关闭了哪些历史欠账、还有哪些欠账未处理。

历史欠账不是失败记录，而是生产级交付的上线前收口清单。

## 36. MySQL-only 数据库规则

本项目从现在开始统一采用 MySQL 作为唯一正式数据库标准。

MySQL 用于：

1. 本地开发数据库
2. 测试数据库
3. 服务器部署数据库
4. Alembic 迁移
5. seed 初始化
6. demo 数据
7. 模块验收
8. 正式交付

SQLite 只允许作为历史临时演示环境或早期过渡环境存在，不再作为正式开发、测试、部署标准。

禁止：

1. 新增 SQLite 专属语法
2. 新增 PostgreSQL 专属语法
3. 用 SQLite 测试结果冒充生产级验收
4. 用 create_all 长期替代 Alembic 迁移
5. 新模块开发时连接不明数据库
6. 服务器继续把 SQLite 当正式生产库

每次模块开发前必须检查：

1. .env
2. backend/.env
3. docker-compose.yml
4. settings.py / config.py
5. DATABASE_URL
6. TEST_DATABASE_URL
7. alembic.ini
8. alembic/env.py
9. seed / demo 初始化脚本
10. pytest 配置

必须明确输出当前连接的是 MySQL。

如果发现当前连接 SQLite、PostgreSQL、测试内存库或其他数据库，必须停止开发并报告，不得继续假装生产级闭环。

MySQL 规范：

1. 默认字符集使用 utf8mb4
2. 默认排序规则使用 utf8mb4_unicode_ci 或项目统一配置
3. JSON / Boolean / Enum / Text / DateTime / 索引 / 唯一约束 / 外键必须兼容 MySQL
4. 所有新增表必须有 tenant_id，除非是明确的全局配置表
5. 所有业务表必须考虑软删除、审计字段、创建时间、更新时间
6. 所有正式表结构变更必须写 Alembic migration
7. pytest 必须逐步切换到 MySQL 测试库
8. 正式部署前必须确保 MySQL 迁移可重复执行
9. 服务器 SQLite 数据迁移到 MySQL 前必须先备份
10. 不得直接覆盖服务器旧数据

模块收口报告必须写清楚：

1. 当前数据库类型：MySQL
2. 当前连接库名，密码脱敏
3. 新增表是否真实存在于 MySQL
4. 新增数据是否真实写入 MySQL
5. Alembic 迁移是否覆盖
6. pytest 是否连接 MySQL 测试库
7. 是否还存在 SQLite / PostgreSQL 残留配置
8. 如果服务器仍是 SQLite，必须记入历史欠账

服务器迁移规则：

当前服务器如果仍使用 SQLite，先作为历史临时演示环境保留。
正式上线前必须迁移到 MySQL。
迁移前必须输出迁移方案、备份方案、回滚方案，不得直接操作生产数据。

## 37. 新模块替换旧代码与安全下线规则

本项目按生产级标准重构。保护旧业务能力、真实数据、审计、附件、权限和旧路由兼容；不永久保护无用旧菜单、旧页面、旧 mock、旧空页面、旧重复入口。

核心原则：

1. 新模块完成并验收后，新模块成为唯一主链路。
2. 旧菜单可以删除或 hidden，不能和新菜单并列干扰用户。
3. 旧搜索入口必须改指向新页面。
4. 旧路由优先 redirect / alias 兼容，不直接 404。
5. 旧页面确认无菜单、无路由、无搜索、无 import、无审计跳转引用后可以删除。
6. 旧 mock、旧 demo、旧空页面、旧重复入口，如果影响新模块验收，应清理。
7. 旧业务能力不得缩水，能复用就迁移，不能缩水。
8. 旧接口、旧权限 key、旧审计跳转涉及兼容时先标 legacy / compat，不直接删。
9. 真实数据库表、业务数据、审计日志、上传附件、租户、用户、权限核心表、生产迁移不得删除。
10. 删除旧页面前必须输出删除清单，等待用户确认后再执行。
11. 删除旧页面后必须 `npm run build` 通过，必要的 lint / pytest 通过。
12. 清理结果必须记录到 `docs/施工记录/历史欠账.md`。

老代码分类：

A. 可复用：字段、组件、工具函数、接口契约可迁入新模块；
B. 兼容保留：旧路由 redirect、旧审计跳转、历史入口；
C. 待迁移：旧数据、旧权限 key、旧菜单映射；
D. 可删除候选：确认无引用、无数据依赖、无路由依赖、无测试依赖后进入删除清单。

安全删除条件（删除清单须逐项核对）：

1. 全局搜索确认没有 import 引用；
2. 路由不再引用（或仅保留 redirect）；
3. 菜单不再引用；
4. 搜索不再引用；
5. 审计记录不再跳转；
6. 权限 key 已迁移或兼容；
7. 测试通过；
8. build 通过；
9. 已写入 `docs/施工记录/历史欠账.md`；
10. 有 git commit 可回滚。

禁止：

- 不允许在旧页面上继续堆新功能；
- 不允许新模块依赖旧 mock 冒充完成；
- 不允许旧入口绕过新模块主链路；
- 不允许因为「重新做系统」就无清单批量删除旧代码。

## 38. 真实学校业务场景与 Excel 导入导出规则

本项目正式面向学校使用，一切以真实学校业务场景为第一原则。

学校老师、辅导员、教务、学工、实习指导教师、学院管理员实际使用中，正式批量导入导出以 Excel / xlsx 为准。

规则：

1. 正式导入必须使用 Excel / xlsx；
2. 正式导出必须使用 Excel / xlsx；
3. CSV 不得作为正式交付主方案；
4. CSV 不得作为 implemented 验收依据；
5. 如果保留粘贴导入，只能作为「高级粘贴导入」辅助能力；
6. 页面文案必须使用：
   - 下载 Excel 模板
   - 上传 Excel
   - 导入 Excel
   - 导出 Excel 台账
   - 下载错误行 Excel
7. Excel 导入必须包含：
   - 下载模板
   - 上传 xlsx
   - 字段校验
   - 必填校验
   - 格式校验
   - 业务规则校验
   - 文件内重复校验
   - 数据库重复校验
   - 错误行预览
   - 错误行 Excel 下载
   - 确认导入
   - 导入记录
   - 操作审计
8. Excel 导出必须包含：
   - 按当前筛选条件导出
   - 权限控制
   - 敏感字段脱敏
   - 导出审计
   - 文件名包含模块名、学校/租户、时间
9. 写操作不得 mock 成功；
10. 后续新模块默认按 Excel / xlsx 实现导入导出；
11. 老模块如果已有 CSV，必须升级为 Excel；
12. 能复用校验逻辑就复用，不允许 Excel 和粘贴导入两套校验规则不一致。
13. 后续所有涉及导入导出的模块，必须优先接入公共 Excel 导入导出底座（`app/services/excel/`、`components/common/excel/`）；不得每个模块重复造一套 Excel 解析、校验、错误行、导入记录、导出审计逻辑。

一句话原则：

学校用户用 Excel，正式系统不拿 CSV 糊弄老师。
## 39. 成熟商业软件公司交付标准

本项目所有后续开发、验收、重构、清理、导入导出、权限、数据库、部署判断，必须以成熟商业软件公司的生产交付标准为准，不以“能跑”“能展示”“AI 已生成”为完成标准。

判断顺序：

1. 真实业务场景优先
   先判断学校老师、学生、辅导员、指导教师、管理员真实怎么用。
   不符合真实学校工作方式的方案，不得作为正式交付方案。

2. 产品可用优先
   页面不是为了好看，而是为了真实完成业务。
   按钮必须真实生效。
   数据必须刷新后仍存在。
   导入导出必须符合学校习惯，正式场景优先 Excel / xlsx。

3. 工程质量优先
   每个 implemented 模块必须具备：
   - 真实前端
   - 真实后端
   - 真实 MySQL 表
   - 真实接口
   - 状态机
   - 权限
   - 数据范围
   - 审计
   - 测试
   - build 通过
   - 施工记录

4. 安全与权限优先
   不能只靠前端隐藏按钮。
   高风险操作必须有后端权限校验或明确上线前安全欠账。
   关键操作必须审计。

5. 生产环境不允许假能力
   不允许 mock 冒充成功。
   不允许 SQLite 测试冒充 MySQL 验收。
   不允许 CSV 冒充学校正式 Excel 导入导出。
   不允许旧页面绕过新主链路。

6. 可维护优先
   新模块打穿一个，旧模块清理一个。
   老代码能复用就迁移，挡路就删除。
   真实数据、审计、附件、租户、权限核心表不得乱删。

7. 可交付优先
   每个模块必须能回答：
   - 怎么部署
   - 怎么初始化
   - 怎么导入数据
   - 怎么验收
   - 怎么培训老师
   - 出问题怎么查
   - 哪些欠账上线前必须补

8. 欠账必须统一记账
   不阻断当前闭环的问题可以记账。
   阻断真实使用的问题必须返工。
   阻断正式上线的问题必须进入上线前收口清单。

以后任何 AI Agent 输出方案时，必须按成熟商业软件公司标准判断，而不是按个人猜测、技术方便、演示效果判断。

一句话原则：

真实学校业务场景第一，商业交付标准第二，代码实现第三。

---

## 40. 公共日期组件与日期默认值规则

本项目所有模块不得各自重复实现日期选择、日期展示、截止时间、日期范围筛选逻辑。

凡涉及日期选择、日期范围、截止时间、批次时间、提交时间、审核时间、答辩时间、归档时间、统计时间范围的页面，必须优先使用全系统公共日期组件和 dateUtils。

统一原则：

1. 筛选类日期默认“不限日期 / 全部时间”，不得让用户每次进入页面都必须重新选择。
2. 业务表单日期优先从当前批次、流程节点、业务上下文自动带出默认值。
3. 截止时间默认到 23:59，不得默认 00:00。
4. 截止日期必须提供快捷项，例如今天 23:59、明天 23:59、本周五 23:59、7 天后、14 天后、30 天后。
5. 空日期统一显示“未设置”，不得显示 undefined、null、Invalid Date、yyyy/mm/日。
6. 过期、临近截止、剩余天数必须统一展示。
7. 开始时间不能晚于结束时间，结束时间不能早于开始时间。
8. 日期格式、时区、校验、展示必须全系统统一。
9. 不允许每个页面散落一套日期逻辑。
10. 新模块涉及日期时，如果未接入公共日期组件，不能标 implemented，只能标 partial，并写入历史欠账。

一句话：

筛选日期默认不限，业务日期从批次带出，截止日期给快捷按钮，日期展示全系统统一。

## 40. 公共组件强制调用规则

本项目已建成「交付级公共组件底座」（第一阶段 12 个 + 第二阶段 8 个），统一出口：

```text
业务组件：import { AppXxx } from '@/components/common'
UI 基元：import { AppButton, AppBadge, AppCard } from '@/components/ui'
```

使用手册：`docs/公共组件/公共组件-使用指南总册.md`
在线预览：登录后访问 `/dev/components`
总控表：`docs/公共组件/00-高校SaaS公共组件商业化底座总控表.md`

强制规则：

1. 后续所有业务模块（学工/教务/毕设/实习/工作台/系统管理）开发页面时，凡涉及以下场景，必须优先复用公共组件，禁止各模块另写一套：
   - 权限按钮 → AppPermissionButton
   - 导出 Excel → AppExportButton + AppExportConfirm
   - 二次确认（退回/驳回/删除）→ AppConfirmDialog
   - 状态/风险标签 → AppStatusTag / AppRiskTag
   - 敏感字段脱敏 → AppSensitiveText
   - 附件展示/上传态 → AppFilePreview / AppFileList
   - 批量操作 → AppBatchActionBar
   - 审批处理/审批历史 → AppApprovalPanel / AppWorkflowTimeline
   - 操作留痕展示 → AppAuditTrail
   - 工作台待办/消息/指标 → AppTodoPanel / AppNotificationPanel / AppMetricCard
   - 复制/帮助/字段提示 → AppCopyableText / AppHelpTooltip / AppFieldHint
2. 需要新能力时，先判断能否给公共组件加 prop 增强，不要在业务页面复制一份改。
3. 禁止重写清单：DataTable、AdvancedFilter、AppDrawer、AppConfirmDialog、公共日期族、Excel 管道、以及本底座已冻结组件，只允许增强不允许拆掉重做。
4. 安全红线（组件不替代后端）：
   - AppPermissionButton 只做前端体验，越权拦截必须后端校验；
   - AppExportButton/AppExportConfirm 不伪造导出成功，真实导出走后端并写审计；
   - AppAuditTrail 只展示后端真实审计，不伪造；
   - AppApprovalPanel/AppWorkflowTimeline 只做交互展示，状态流转由后端审批引擎；
   - 敏感字段一律 AppSensitiveText，查看明文由调用方写审计；
   - 附件业务表只存 file_id，上传/对象存储由文件中心对接，未接入前上传能力标 partial。
5. 未复用公共组件而自造重复实现的页面，必须写入 `docs/施工记录/历史欠账.md`。

一句话：公共组件是标准零件仓，业务页面只许取零件，不许各造一套。


## 41. 公共组件强制调用规则（第三批扩展：骨架/表单/选择器/展示/体验）

在 §40 基础上，第三批公共组件（约 48 个，统一出口 `@/components/common`，子目录 `layout/ form/ picker/ display/ experience/`）同样强制优先复用。使用手册见 `docs/公共组件/公共组件-使用指南总册.md` 第二篇。

后续新页面开发必须优先使用：

1. 页面骨架：AppPageShell / AppSectionCard / AppToolbar / AppActionBar / AppStickyFooter / AppResponsiveGrid / AppDrawerLayout。
2. 表单：AppForm / AppFormItem / AppFormSection + AppTextInput / AppNumberInput / AppTextarea / AppSelect / AppMultiSelect / AppRadioGroup / AppCheckboxGroup / AppSubmitBar。
3. 业务选择器：AppStudentPicker / AppTeacherPicker / AppMentorPicker / AppClassPicker / AppMajorPicker / AppCollegePicker / AppCoursePicker / AppRolePicker / AppTenantPicker / AppCompanyPicker / AppPositionPicker / AppBatchPicker / AppOrgCascader / AppAcademicYearPicker / AppTermPicker（基座 AppRemoteSelect）。
4. 数据展示：AppPagination / AppSearchBox / AppQuickFilterChips / AppDescriptionList / AppOperationResult / AppProgressBar / AppColumnConfig / AppChartCard。
5. 体验：AppWatermark / AppPrintButton / AppAvatarGroup / AppStepGuide / AppKeyboardShortcut / AppQRCode。

**业务页面禁止自写清单**（有公共组件就必须调用，能力不够就增强公共组件，不允许在业务页面绕开）：
1. 不允许自写权限按钮；2. 不允许自写脱敏函数；3. 不允许自写审计日志展示；4. 不允许自写附件列表；
5. 不允许自写导出确认；6. 不允许自写状态颜色；7. 不允许自写空/加载/错误状态；8. 不允许自写批量操作条；
9. 不允许自写审批区域；10. 不允许自写学生/教师/组织/批次选择器；11. 不允许自写分页/搜索框/详情键值表；
12. 不允许自写页面壳/区块卡/工具栏/表单控件；13. 不允许自写日期选择/截止时间。

**新页面接入检查清单**（不满足只能标 partial 并写入历史欠账，不得标 implemented）：见 `公共组件-使用指南总册.md` 第二篇 §8（页面壳/工具栏/表格/筛选/日期/状态/权限按钮/确认/脱敏/审计/附件/导出/Picker/表单/三态/build 共 16 项）。

**安全红线（组件不替代后端）**：
- 业务选择器（AppStudentPicker 等）只做前端 UI，可见的师生/组织/批次必须由后端按数据范围返回，前端不得放大范围；未接后端标 partial。
- AppPrintButton 不生成正式套打模板；AppRichTextEditor 的 HTML 必须后端 XSS 清洗；AppChartCard/AppQRCode 未接库前 partial。
- AppWatermark 是展示层防护，不替代后端导出审计与权限。

一句话：第三批公共组件是「页面标准件仓」，业务页面照单取件，缺件先增强公共组件，绝不各造一套。
