# CLAUDE.md — Claude Code 轻量总控

> 本文件只负责：判断任务类型、选择必读资料、守住生产红线。
> 详细施工标准放在专题文档中，不得再复制回本文件把它写成长篇总册。

## 1. 项目目标与用户协作

本项目是面向职业院校销售、部署和验收的学生全生命周期多租户 SaaS，不是 Demo、静态原型或只做前端的展示系统。

用户是独立推动项目的非技术产品负责人。执行时：

1. 用业务结果和可核验证据沟通，不要求用户替 AI 做技术判断。
2. 发现问题先查代码、配置、迁移、测试和 Git 事实，不凭文档标题或 AI 记忆下结论。
3. 不因项目规模大而擅自缩小用户已明确的交付目标。
4. 涉及产品取舍时给出“采用方案、证据、影响、待用户决定项”，不得自行编造学校需求。
5. 用户无需记住文档位置；AI 负责按索引找到当前任务资料。

默认交付单位是施工图中的**一个二级模块及其全部三级模块**。用户要求“开发一个二级模块”时，默认要求从设计证据到全栈实现、测试和交付收口连续完成。

产品一级导航保持：工作台、学工中心、教务中心、毕业设计中心、岗位实习中心、系统管理。数字迎新是学工中心的二级模块；`docs/03-业务模块设计/数字迎新中心/` 只是专项资料目录，不代表新增一级菜单。实际页面树仍须核对施工图、中心索引和 `navPlan.js`。

## 2. 事实与规则优先级

发生冲突时按以下顺序处理：

1. 用户本次明确指令；
2. 本文件的安全与执行红线；
3. 权限、数据、接口、部署等当前有效的专题总控文档；
4. 当前二级模块已通过门禁的专属生产级施工包；
5. 当前代码、数据库迁移、路由、配置和自动化测试事实；
6. 其他设计文档；
7. 历史归档。

补充规则：

- 文档写了“已完成”不代表代码已实现。
- 页面存在不代表后端、权限、数据库和测试已闭环。
- 施工图与代码冲突时先标明差异；未验证前不得把状态改绿。
- `docs/08-历史记录与归档/` 只能追溯历史，不能单独作为当前施工依据。
- 找不到资料就报告缺失，不得编造文件、接口、表、测试结果或外部证据。

## 3. 先判断任务模式

### A. 二级模块首次生产级施工

适用：用户要求开发、重做或一次做完一个二级模块，且没有已通过门禁的有效专属施工包。

执行重型流程：外部对标 → 专属施工包 → 设计就绪 → 全栈连续施工 → 验证 → 状态同步。

### B. 二级模块续工或已有施工包施工

适用：同一模块已有有效施工包，本次是继续实现、补缺、修复验收问题。

复用施工包，只核验：

- 外部来源是否仍可用、是否出现影响设计的新证据；
- 权限总控或代码基线是否变化；
- 本次缺口对应的页面、接口、数据、测试和部署项。

不得重复从零搜索三家产品，不得重写整份施工包。

### C. 小范围修复

适用：明确的 Bug、测试失败、样式、单接口、单组件、单文档或配置修复。

只读本文件、相关代码、相关测试及该问题直接依赖的专题规则。不得启动二级模块全量对标，不得顺手跨模块重构。

### D. 只读审计或答疑

适用：用户要求检查、解释、汇报状态、判断是否执行过。

只读核验并给证据。除非用户同时要求修改，否则不得写文件、改状态、提交或推送。

如果任务同时符合多类，选择能覆盖用户目标的最小模式；不确定时先从只读事实核验开始，不要先全仓扫描。

## 4. 阅读路由

### 4.1 所有任务的最小动作

1. 运行 `git status --short`，识别用户已有改动。
2. 用 `rg` / `rg --files` 定位当前任务文件和引用。
3. 只读取与当前任务直接相关的文件和章节。
4. 修改前确认真实入口、调用链、测试入口和状态来源。

禁止为了表示认真而机械输出大段“已读文件清单”。禁止默认遍历全部 `docs/`、全部前后端目录或历史归档。

### 4.2 A/B 模式的文档入口

按以下路线读取，不猜文件名：

1. `docs/README.md`
2. `docs/00-项目入口与总控/00-文档总导航.md`
3. `docs/03-业务模块设计/<业务中心>/README.md`
4. 同中心 `文档关联索引.md`
5. 索引指向的当前二级模块资料

A 模式还必须读取：

- `00-生产级全栈模块开发总提示词.md`
- `docs/06-开发施工与质量验收/二级模块生产级施工包标准模板.md`
- `docs/03-业务模块设计/系统管理中心/00-系统管理中心-权限角色模块授权与权责边界设计.md`

B 模式优先读取已有专属施工包，再按缺口补读专题文档。

凡本次要新建、补强或复核二级模块生产级施工包，即使属于 B 模式，也必须重新完整读取权限总控文档，并核对权限实例化矩阵；纯代码续工且施工包权限设计未变化时可只做版本与差异核验。

涉及新页面或公共交互时，读取：

- `docs/02-总体架构与公共底座/公共组件/公共组件-使用指南总册.md`

只在任务触发时读取数据库、API、安全、部署、移动端等专题资料，不做“所有任务全部必读”。

### 4.3 代码阅读边界

只检查当前模块相关的：

- `frontend/src/config/navPlan.js`、对应路由、页面、API/provider 和公共组件；
- 后端 router/service/schema/model、权限/审计调用链；
- 相关 Alembic 迁移、测试、seed 和部署配置。

先用搜索定位，再读取命中的上下文；不要先打开整个大型文件或整个目录。

## 5. 二级模块首次施工硬门禁

### 5.1 外部对标必须有证据

A 模式必须联网打开至少三家不同成熟系统的有效来源，并补充至少一份真实学校操作手册、采购/招标需求、业务制度或教育行业标准。

每条证据至少记录：

- 产品/厂商与资料发布方；
- 资料标题、可打开 URL、查阅日期；
- 页码、章节或可定位位置；
- 该来源直接证明的事实；
- 该来源不能证明的内容。

只写厂商名、只看搜索摘要、无 URL、把宣传材料当学校使用事实或由 AI 补齐未知能力，均不算完成。

三家共同能力只是生产级核心候选，仍须检查本项目边界、学校适用性、权限安全、现有架构和可交付性，不得机械照搬。只吸收业务能力、流程和交付标准，不复制第三方代码、文案、数据库结构或受保护内容。

无法联网且没有已有有效施工包时，只能形成 `draft/blocked`，不得用仓库资料冒充外部验证并继续写代码。

### 5.2 专属施工包

统一模板：

`docs/06-开发施工与质量验收/二级模块生产级施工包标准模板.md`

专属路径：

`docs/03-业务模块设计/<业务中心>/施工包/<二级模块>-生产级施工包.md`

施工包必须把证据转化为可施工设计，覆盖全部三级模块的角色、流程、异常、页面、字段、按钮、API、状态机、表/迁移、权限、审计、Excel、统计、测试、部署、回滚和非技术验收。

每项关键设计标明 `designSource`：

- `external_evidence`
- `project_rule`
- `existing_code`
- `user_decision`
- `ai_proposal`

`ai_proposal` 不得冒充市场事实；未确认的重大产品选择进入待确认项或 backlog。

### 5.3 权限理解必须实例化

不得只写“已阅读权限文档”。施工包必须覆盖全部三级页面与业务动作，至少明确：

- 当前激活角色、租户和模块授权；
- menu/API/button permission key；
- 数据范围与真实业务关系；
- 敏感字段、脱敏和查看明文；
- 状态/审批限制；
- 审计事件、原因、水印和历史责任快照；
- 越权错误码；
- 正向、反向、跨范围、跨租户测试；
- 当前代码 implemented / partial / missing 证据。

权限矩阵不完整时不得标 `design_ready`。

### 5.4 设计就绪后连续施工

施工包通过模板中的外部证据、权限理解和设计就绪门禁后，必须在同一次任务中继续完成前端、后端、MySQL、迁移、接口、状态机、权限、数据范围、审计、测试、构建、部署检查、施工记录和状态同步。

不得只交对标表或施工包后停下等待用户再次说“继续”。

## 6. 全局工程红线

### 6.1 真实能力

- 禁止 mock、硬编码成功、假按钮、空页面冒充完成。
- 前端隐藏不等于权限；后端必须校验租户、模块授权、权限、数据范围、业务关系和状态。
- 敏感字段必须最小返回、脱敏、明文查看授权和审计。
- 正式导入导出使用 Excel/xlsx；CSV 不得作为学校正式交付主方案。
- 涉及附件、通知、统计、归档时，必须明确适用性和真实落点；不适用也要写理由。

### 6.2 数据库

- 本项目生产与正式验收为 MySQL-only。
- SQLite、内存库和 mock 数据不能冒充 MySQL 验收。
- 数据结构变化必须使用现有 Alembic 迁移体系，说明升级、历史数据兼容和回滚。
- 不直接修改或删除生产数据，不绕过 migration 改生产结构。
- 禁止跨租户读写；租户、权限、审计、附件等核心数据不得擅自删除。

### 6.3 前端与公共能力

- 保留登录、顶部栏、角色切换、数据范围提示、水印、菜单高亮、面包屑和刷新可达性。
- 优先复用 `@/components/common` 和 `@/components/ui`；已有公共组件能力不足时优先增强公共组件，不在业务页复制一套。
- 日期、选择器、权限按钮、状态、脱敏、附件、审批、审计、Excel、分页和页面三态使用统一底座。
- 公共组件只解决展示和交互，不能替代后端权限、状态机、审计和真实文件处理。

### 6.4 兼容与清理

- 新链路完成后应成为唯一主入口，旧菜单不得长期并列干扰。
- 旧路由优先 redirect/alias，刷新不能 404。
- 老项目中的成熟业务能力是资产，应迁移或补强，不能被新系统浅设计覆盖。
- 删除旧页面、接口、表、真实数据或大批文件前，必须给出引用核验和删除清单，并获得用户明确确认。

## 7. Git 与工作区

1. 用户已有改动属于用户；只报告，不覆盖、不格式化、不删除。
2. 只修改当前任务范围；不顺手修无关问题，不运行全仓破坏性格式化。
3. 未经用户明确要求，不执行 `git add`、`commit`、`push`、`tag`、`reset`、`clean`。
4. 禁止使用 `git reset --hard`、`git checkout --` 等方式丢弃用户改动。
5. 收尾再次运行 `git status --short`，区分任务前已有与本次产生的改动。

## 8. 验证与完成标准

先运行最小相关测试，失败后定位修复；再按风险运行模块测试、前后端检查、构建和 MySQL/迁移验证。不要一开始反复跑耗时的全量测试。

命令超时、环境缺依赖、未执行或只有静态阅读，都不能写“测试通过”。必须记录实际命令、结果和未验证项。

只有同时具备并验证下列适用项，二级模块才能标 `implemented`：

- 全部三级页面和真实交互；
- 后端接口与 MySQL 持久化；
- 迁移可升级；
- 状态机、权限、数据范围、业务关系和审计；
- Excel/附件/通知/统计等适用闭环；
- 正常、异常、越权、跨范围和跨租户测试；
- 前端 build 与必要后端测试；
- 部署、初始化、升级、回滚和验收说明；
- 旧路由兼容及施工状态同步。

只完成部分链路必须标 `partial`；只有菜单或设计必须标 `planned`；不得为让施工图变绿而降低口径。

## 9. 文档与状态收口

- 优先补充当前主文档，不无意义新建总册或批量重写历史文件。
- A 模式允许且必须创建一个专属施工包；这不是“文档泛滥”。
- 施工后同步专属施工包、中心状态、`navPlan.js`、施工记录和必要的施工图状态。
- 状态同步必须引用代码、迁移、测试或构建证据；静态 HTML 施工图不是独立事实源。
- 非阻断欠账写入 `docs/06-开发施工与质量验收/施工记录/历史欠账.md`；阻断真实使用或上线的问题必须返工，不能只记账。

## 10. 允许停止的情况

以下情况需要暂停并向用户说明：

- 删除或覆盖真实数据；
- 批量删除/不可逆迁移；
- 权限核心或认证架构重构；
- 外部证据无法取得且首次施工门禁无法通过；
- 关键业务方向存在两种会显著改变产品的选择；
- 必须取得新的外部权限、密钥或生产环境授权。

除上述情况外，应在范围内自行查证、修复和继续，不因普通报错、测试失败、文档缺口或工作量大就把施工中途退回用户。

## 11. 沟通与最终报告

执行中更新要短，说明“正在核验什么、发现什么、下一步是什么”，避免粘贴长篇内部分析。

最终报告先给结果，再给证据，至少包含：

1. 完成/部分完成/阻断；
2. 实际修改文件与关键变化；
3. 实际执行的检查命令及结果；
4. 数据库、权限、兼容、部署影响；
5. 未验证项与真实风险；
6. 当前 `git status --short`；
7. 用户下一步只需要做什么。

不得声称“生产级”“已部署”“测试通过”，除非有相应证据。生产级代码就绪与已经部署到真实服务器必须明确区分。

## 12. 历史章节号兼容

已有设计和施工记录中的旧 `CLAUDE.md §x` 引用不要求批量改写，按下表理解：

| 历史引用 | 当前规则 |
|---|---|
| §0.0 | §5.1–§5.2 外部对标与施工包 |
| §1 / §1.1 | §1、§6 项目定位与不重复造轮子 |
| §3.3 / §5.3 / §10 | §5.3、§6.1 + 权限总控文档 |
| §7 | 当前中心 README、文档关联索引与 `navPlan.js` |
| §8 | §6.4 兼容、复用与清理 |
| §14 / §16 | §4、§8、§11 执行验证与报告 |
| §19 / §36 | §6.2 数据库与 MySQL |
| §23 | §11 最终报告 |
| §38 | §6.1 Excel/xlsx |
| §39 | §1、§8 商业交付与完成标准 |
| §40–§42 | §6.3 + 公共组件使用指南 |

兼容表只用于解释历史引用；新文档统一引用当前章节或直接链接专题总控文档。

## 13. CC 会话边界

- 一个二级模块使用一个新的 CC 任务；在该任务内连续做到验收收口。
- 只有继续同一二级模块时才恢复旧任务；切换到另一个二级模块、中心或独立问题时新建任务。
- 不用一个长期会话连续承载多个模块。旧会话工具输出越多，恢复、压缩上下文和判断当前范围越慢。
- 新任务的事实从专属施工包、代码和测试恢复，不依赖 AI 记忆上一段长对话。
