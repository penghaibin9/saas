# 学生 PC 门户环境变量示例（复制为 .env / .env.production；勿提交真实地址）
# VITE_API_BASE_URL：后端「源」地址，前缀 /api/v1 自动拼接，勿带 /api 或末尾斜杠。
#   本地开发：http://localhost:8000
#   同源部署：留空（推荐，经 Nginx location /api/ 反代）
VITE_API_BASE_URL=
# VITE_BASE：部署子路径（静态资源 + history base）。默认 /portal/
VITE_BASE=/portal/
