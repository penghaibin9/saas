# 生产级全栈模块开发总提示词

用途：

本文件是《高校学生全生命周期管理平台》后续所有模块开发的统一提示词模板。

以后每次开发一个模块时，只需要把文档中的【模块路径】替换成实际模块路径，例如：

- 学工中心 / 数字迎新
- 岗位实习中心 / 打卡管理
- 毕业设计中心 / 选题管理
- 教务中心 / 成绩管理

然后按本提示词执行。

核心原则：

一个模块一次做深。

每个模块必须覆盖：

前端 + 后端 + 数据库 + 接口 + 状态机 + 权限 + 数据范围 + 审计 + 导入导出 + 测试 + 部署检查。

只有真正达到生产级闭环的模块，才能标记为 implemented。

下面开始写入完整提示词正文：

你现在开发《高校学生全生命周期管理平台》的生产级全栈模块：

【模块路径】

本轮目标不是做前端页面，不是做菜单样子，不是做 mock 演示，不是做半成品。

本轮目标是：

把【模块路径】做成一个可真实部署、可真实使用、可单独试点、可持续迭代的生产级全栈模块。

核心标准：

一个模块一次做深。

必须覆盖：

1. 前端页面
2. 后端接口
3. 数据库表 / 字段
4. 数据库迁移
5. API 契约
6. 业务状态机
7. 权限控制
8. 数据范围
9. 业务关系授权
10. 敏感字段脱敏
11. 操作审计
12. 导入导出
13. 统计台账
14. 异常处理
15. 通知 / 待办预留
16. 旧路由兼容（redirect / alias，刷新不 404；不等于旧菜单继续展示）
17. 菜单状态同步
18. 自动化测试
19. 部署检查

完成后必须满足：

1. 前端能访问
2. 后端能调用
3. 数据库能落库
4. 状态能流转
5. 权限能控制
6. 数据范围能生效
7. 审计能记录
8. 导入导出按业务需要可用
9. 测试通过
10. build 通过
11. 部署检查通过
12. 旧路由刷新不 404（redirect / alias；不等于旧菜单继续展示）
13. 相关模块不冲突
14. 不依赖 mock 才能使用

如果只完成前端，最多只能标记为 partial。

如果只完成菜单，必须保持 planned。

如果依赖 mock 才能运行，不能标记为 implemented。

---

# 一、执行前必须读取资料

每个模块开发前必须按 CLAUDE.md §0.0 执行三家成熟商业系统对标，先输出对标表，再开发。

必须先读取：

1. 根目录 CLAUDE.md
2. docs/modules/00-系统管理中心-权限角色模块授权与权责边界设计.md
3. docs/modules 下与【模块路径】相关的全部设计文档
4. docs/modules 下学生主档 / 身份中心相关文档
5. docs/modules 下权限 / 流程 / 数据范围相关文档
6. docs/modules 下部署 / 交付 / 验收相关文档
7. 当前菜单施工地图 config/navPlan.js
8. 当前菜单渲染 layouts/BasePortalLayout.vue
9. 当前前端路由配置
10. 当前前端已有相关页面
11. 当前前端 api / service / provider / request 封装
12. 当前后端 app 结构
13. 当前后端 models / schemas / routers / services
14. 当前认证 / 权限 / 数据范围 / 审计实现
15. 当前数据库迁移体系
16. 当前 seed / demo 数据体系
17. 当前测试体系
18. 当前部署脚本 / health check / runbook

可以参考项目内所有文档。

如果当前环境可联网，可以调研成熟高校 SaaS 软件公开资料，只提炼业务能力、流程闭环、权限边界、交付标准，不复制任何第三方代码、UI、文案、数据库结构、接口路径。

如果某个文档找不到，必须报告缺失，不得编造。

先运行：

```bash
git status --short
```

如果存在无关改动，只报告，不处理。

---

# 二、先做生产级全栈施工审计

写代码前，必须先输出“【模块路径】生产级全栈施工审计”。

审计内容必须包括：

1. 当前已有前端页面
2. 当前已有前端路由
3. 当前已有后端接口
4. 当前已有数据库表
5. 当前已有测试
6. 当前菜单状态
7. 当前旧页面可复用程度
8. 哪些页面能复用
9. 哪些页面必须新建
10. 哪些后端对象已有
11. 哪些后端对象缺失
12. 哪些数据库表已有
13. 哪些数据库表缺失
14. 哪些字段缺失
15. 哪些接口缺失
16. 哪些状态机缺失
17. 哪些权限 key 缺失
18. 哪些数据范围缺失
19. 哪些审计缺失
20. 哪些导入导出缺失
21. 哪些通知 / 待办能力缺失
22. 哪些附件能力缺失
23. 与学生主档的关系
24. 与学院 / 专业 / 班级组织结构的关系
25. 与工作台待办的关系
26. 与通知中心的关系
27. 与审计日志的关系
28. 与移动端学生 / 教师入口的关系
29. 与其他业务模块的关系
30. 本轮具体修改计划
31. 是否存在必须停止项

如果没有触发必须停止项，可以继续开发，不需要等我确认。

必须停止项：

1. 需要删除旧表
2. 需要删除旧接口
3. 需要删除旧路由（须先输出删除清单并经确认；新模块验收后无引用旧路由可删）
4. 需要清空数据
5. 需要重构登录
6. 需要重构全局权限核心
7. 需要重构全局数据范围核心
8. 需要真实支付接口密钥
9. 需要真实短信接口密钥
10. 需要真实身份证核验接口
11. 需要修改生产服务器配置
12. 需要覆盖生产数据库
13. 需要批量迁移历史权限 key

---

# 三、开发范围

本轮只开发：

【模块路径】

不要开发无关模块。

不要顺手开发其他中心。

不要重做导航。

不要做全量权限 key 迁移。

不要提交 git。

本轮允许：

1. 合理改造本模块旧页面
2. 新增本模块前端页面
3. 新增本模块前端路由
4. 新增本模块 api / service / provider
5. 新增本模块后端 model
6. 新增本模块后端 schema
7. 新增本模块后端 router
8. 新增本模块后端 service
9. 新增本模块数据库 migration
10. 新增本模块 seed / demo 初始化数据
11. 新增本模块后端测试
12. 新增本模块前端基础测试或构建自检
13. 更新 navPlan.js 菜单状态
14. 保留旧路由 redirect / alias 兼容（不 404）
15. 新模块验收后按 §37 安全下线旧菜单 / 无引用旧页面
16. 修复本模块范围内发现的问题
17. 自主补齐标准导入导出能力（优先公共 Excel 底座）
18. 自主补齐状态机、权限、数据范围、审计

本轮禁止：

1. 禁止无依据删除旧页面、旧路由。
2. 新模块完成并验收后，旧菜单可以删除或 hidden。
3. 旧页面确认无菜单、无路由、无搜索、无 import、无审计跳转引用后，可以删除。
4. 旧路由优先 redirect / alias 兼容，不直接 404。
5. 真实数据、审计、附件、权限核心表不得乱删。
6. 不删除旧数据库表（未经评估与确认）。
7. 不清空数据。
8. 不破坏登录。
9. 不重构权限核心。
10. 不重构全局数据范围核心。
11. 不做全量权限 key 迁移。
12. 不开发无关模块。
13. 不破坏已有模块。
14. 不把未完成页面标 implemented。
15. 不写假按钮。
16. 不写假成功提示。
17. 不把 mock 写死在页面里。
18. 不接真实短信。
19. 不接真实支付。
20. 不接真实身份证核验。
21. 不提交 git。

---

# 四、不要依赖 mock

本项目不接受“mock 兜底当完成”。

允许存在：

1. seed 数据
2. demo 初始化数据
3. 本地开发测试数据
4. 测试 fixture
5. 外部服务未配置时的 disabled 状态

禁止：

1. 接口失败后静默显示 mock
2. 页面组件里写死假数据
3. 点击按钮显示假成功
4. 没有后端接口却标 implemented
5. 没有数据库落库却标 implemented

如果某个能力依赖外部系统，例如真实支付、短信、身份证核验、第三方系统，本轮不能伪造成功，必须显示为：

1. 未配置
2. 待接入
3. disabled
4. 有配置说明
5. 有 provider 抽象接口
6. 有审计预留
7. 不能假装已完成

---

# 五、数据库生产级要求

必须复用已有学生主档、组织、班级、教师、租户等基础表。

不得重复造一套学生表。

不得重复造一套学院 / 专业 / 班级表。

不得重复造一套用户 / 角色表。

本模块可以新增自己的业务表，但必须和主数据关联。

每张业务表必须考虑：

1. tenant_id / school_id
2. student_id / teacher_id / user_id 等业务主体
3. college_id
4. major_id
5. class_id
6. status
7. created_at
8. updated_at
9. created_by
10. updated_by
11. deleted_at 或项目现有软删除策略
12. remark / reason / audit fields

涉及审核的表必须有：

1. reviewer_id
2. reviewed_at
3. review_status
4. review_comment

涉及状态流转的表必须有：

1. status
2. previous_status
3. last_transition_at
4. last_transition_by
5. transition_reason

涉及归档的表必须有：

1. archive_status
2. archived_at
3. archived_by
4. archive_batch_no

如果已有类似表，不重复建，优先补强。

---

# 六、后端接口生产级要求

必须按项目现有风格实现，不要破坏统一响应格式。

接口必须处理：

1. 租户隔离
2. 数据范围
3. 权限不足
4. 记录不存在
5. 非法状态流转
6. 重复提交
7. 并发更新
8. 空数据
9. 参数校验
10. 审计日志

接口必须至少覆盖：

1. 列表查询
2. 详情查询
3. 新增
4. 编辑
5. 状态流转操作
6. 审核 / 确认 / 处理操作
7. 导入预校验
8. 导入确认
9. 导出
10. 统计
11. 归档
12. 操作日志

接口路径必须按项目现有 API 前缀风格处理，例如 /api/v1/...，不要自作主张破坏现有规范。

---

# 七、状态机要求

本模块必须实现业务状态机，不能只在文档里写。

必须输出状态机表：

当前状态 | 允许操作 | 操作角色 | 下一状态 | 是否审批 | 是否审计 | 备注

后端必须对关键操作做非法状态流转保护。

状态机至少要处理：

1. 初始状态
2. 待提交 / 待导入 / 待处理
3. 待审核
4. 审核通过
5. 审核退回
6. 已确认
7. 异常处理中
8. 已完成
9. 已归档
10. 已撤回 / 已取消
11. 已失效

具体状态按【模块路径】业务实际生成。

没有状态机的页面，只能标 partial，不能标 implemented。

---

# 八、权限、数据范围、业务关系授权

本轮不做全量权限迁移，但本模块必须有权限控制或权限预留。

权限 key 必须遵守：

module.domain.action

moduleKey 固定使用：

1. workbench
2. studentAffairs
3. academicAffairs
4. graduationDesign
5. internship
6. systemAdmin

后端接口必须预留或实现权限检查。

数据范围必须考虑：

1. 平台管理员按租户
2. 学校管理员看本校
3. 学工处 / 教务处看全校
4. 学院老师看本学院
5. 辅导员看自己班级 / 学生
6. 导师看自己指导学生
7. 实习指导老师看自己负责学生
8. 企业导师看本企业学生
9. 学生只能看本人

具体规则按【模块路径】业务实际生成。

不能只靠前端隐藏按钮。

后端必须做基础校验或明确预留校验点。

---

# 九、审计要求

关键操作必须写审计或操作日志。

至少记录：

1. 操作人
2. 当前角色
3. 租户 / 学校
4. 操作对象
5. 操作前状态
6. 操作后状态
7. 操作时间
8. 操作 IP / 设备，如项目已有
9. 操作原因 / 审批意见
10. 是否敏感操作

以下操作必须审计：

1. 审核
2. 退回
3. 驳回
4. 人工放行
5. 状态变更
6. 导入
7. 导出
8. 归档
9. 删除 / 作废
10. 查看敏感字段
11. 批量操作

---

# 十、敏感字段

前后端都必须处理脱敏。

常见敏感字段包括：

1. 身份证号
2. 手机号
3. 家庭地址
4. 家庭经济情况
5. 心理记录
6. 处分记录
7. 成绩明细
8. 材料附件
9. 监护人联系方式
10. 缴费金额 / 欠费金额

默认列表不得完整展示身份证和手机号。

查看完整敏感字段必须有权限和审计预留。

---

# 十一、导入导出

如果该模块涉及批量数据，Claude Code 必须自主生成标准导入导出能力。

涉及以下对象时，默认需要导入导出：

1. 学生
2. 新生
3. 教师
4. 企业
5. 岗位
6. 题目
7. 成绩
8. 材料
9. 协议
10. 周报
11. 台账
12. 归档包

至少实现：

1. 标准导入模板
2. 导入字段说明
3. 必填字段校验
4. 数据格式校验
5. 业务规则校验
6. 错误行提示
7. 导入预校验
8. 导入确认
9. 导入记录
10. 导入失败原因
11. 导出列表
12. 导出台账
13. 导出统计
14. 导出操作审计

正式导入导出只认 Excel / xlsx。
如果缺少 Excel 能力，必须补依赖或标 partial，不得用 CSV 冒充完成。
后续模块必须优先接入公共 Excel 导入导出底座。

如果该模块不适合导入，必须说明原因。

---

# 十二、通知、待办、异常、归档

必须按模块需要考虑：

1. 通知提醒
2. 工作台待办
3. 异常处理
4. 统计报表
5. 归档台账

通知可以先支持站内通知。

短信 / 邮件 / 小程序未配置时必须显示未配置或待接入，不能假装发送成功。

异常处理必须有：

1. 异常发现
2. 异常分派
3. 异常处理
4. 异常关闭
5. 异常记录
6. 异常统计

归档必须有：

1. 归档状态
2. 归档批次
3. 归档人
4. 归档时间
5. 归档日志
6. 归档导出入口

---

# 十三、与其他模块的相关性

开发【模块路径】时必须主动考虑与其他模块的关系。

至少检查：

1. 学生主档
2. 教师主档
3. 学院 / 专业 / 班级
4. 学年学期 / 业务批次
5. 宿舍管理
6. 通知中心
7. 工作台待办
8. 审计日志
9. 权限角色
10. 数据范围
11. 移动端学生入口
12. 移动端教师入口
13. 统计归档
14. 已有旧模块
15. 已有旧路由

不能造孤岛模块。

不能重复造主数据。

不能破坏其他模块。

---

# 十四、前端页面生产级要求

每个页面必须：

1. 接真实 API
2. 不靠 mock 展示
3. 有 loading
4. 有 empty
5. 有 error
6. 有 no permission
7. 有筛选
8. 有分页
9. 有详情抽屉
10. 有状态标签
11. 有操作按钮
12. 有二次确认
13. 有权限控制
14. 有脱敏展示
15. 有审计提示
16. 有导入 / 导出入口
17. 有刷新
18. 有异常提示
19. 有和菜单状态一致的路由

---

# 十五、菜单状态同步

完成后必须更新 navPlan.js。

规则：

1. 前后端闭环完成：
   status = implemented
   disabled = false
   path = 真实路由

2. 只完成部分能力：
   status = partial
   disabled = false
   badge = 待补强

3. 未完成：
   status = planned
   disabled = true
   badge = 待施工

不允许欺骗式点亮。

---

# 十六、测试与自修复闭环

完成开发后必须运行：

前端：

```bash
npm run lint
npm run build
```

后端：

```bash
pytest
```

如果项目实际命令不同，按项目实际命令执行。

必须自修复：

1. lint 不通过，修
2. build 不通过，修
3. pytest 不通过，修
4. 路由 404，修
5. 接口 500，修
6. 类型错误，修
7. 导入错误，修
8. 数据库迁移错误，修
9. 菜单状态错误，修
10. 旧路由断了，修

最多连续修复 3 轮。

如果 3 轮后仍无法修复，必须输出：

1. 剩余错误
2. 错误原因
3. 已尝试修复
4. 需要人工确认的问题
5. 最小可继续执行提示词

---

# 十七、部署可用性检查

本模块完成后必须输出服务器部署检查项：

1. 需要执行的数据库迁移
2. 需要的环境变量
3. 是否依赖外部服务
4. 外部服务未配置时系统表现
5. seed / demo 初始化命令
6. health check 是否通过
7. 前端 build 是否通过
8. 后端启动是否通过
9. API smoke test 是否通过
10. 回滚方式

---

# 十八、最终报告

完成后必须输出：

1. 是否完成生产级闭环
2. 本轮开发模块
3. 读取了哪些文档
4. 是否生成施工卡
5. designSource
6. 前端修改文件
7. 后端修改文件
8. 数据库迁移文件
9. 新增 / 修改测试文件
10. 新增接口清单
11. 新增表 / 字段清单
12. 状态机实现情况
13. 权限实现 / 预留情况
14. 数据范围实现 / 预留情况
15. 审计实现情况
16. 敏感字段脱敏情况
17. 导入导出实现情况
18. 通知 / 待办关联情况
19. 相关模块关联情况
20. 移动端预留情况
21. 本模块三级页面完成情况表
22. implemented 清单
23. partial 清单
24. planned 清单
25. 旧路由兼容情况
26. lint 结果
27. build 结果
28. pytest 结果
29. 数据库迁移验证结果
30. API smoke test 结果
31. git status --short
32. 是否存在无关改动
33. 服务器部署步骤
34. 风险点
35. 下一步建议

现在开始执行【模块路径】生产级全栈闭环开发。

完成后输出：

1. 是否创建成功；
2. 文件路径；
3. 是否只新增该 md 文件；
4. 是否修改代码；
5. git status --short；
6. 是否存在无关改动。
## 移动端同步开发规则

开发任何业务模块时，必须判断该模块是否存在学生端或教师端高频使用场景。

如果存在以下情况，必须同步开发对应小程序端入口：

1. 学生需要提交；
2. 学生需要查看结果；
3. 学生需要上传材料；
4. 学生需要确认；
5. 教师需要审核；
6. 教师需要批阅；
7. 教师需要处理待办；
8. 教师需要现场查看或移动处理；
9. 该功能属于迎新、请假、实习、毕业设计、成绩、课表、考试、通知待办等高频业务。

同步开发不是复制 PC 后台，而是开发移动端真实高频闭环。

PC 后台负责管理、配置、统计、导入导出、批量操作。
小程序端负责学生/教师日常操作、提交、确认、审核、查看结果、接收通知。

如果该模块只有管理员配置、权限、参数、模板、审计、统计大屏等后台管理场景，可以不开发小程序端，但必须在完成报告中说明原因。

如果模块存在学生/教师高频场景却没有开发小程序端，不能标 implemented，只能标 partial，并写入历史欠账。

# 公共日期组件接入要求

开发任何模块时，必须检查该模块是否涉及日期选择、日期范围、截止时间、批次时间、提交时间、审核时间、答辩时间、归档时间、统计时间范围。

如果涉及，必须优先接入公共日期组件：

1. AppDatePicker
2. AppDateTimePicker
3. AppDateRangePicker
4. AppDeadlinePicker
5. AppDateDisplay
6. dateUtils

禁止每个页面自己重复写日期选择、日期格式化、截止时间、日期范围校验、逾期判断、剩余天数展示逻辑。

筛选类日期规则：

1. 默认显示“不限日期 / 全部时间”。
2. 初次进入页面不能因为日期为空查不到数据。
3. 必须支持清空。
4. 必须支持快捷选择：今天、昨天、本周、本月、本学期、最近 7 天、最近 30 天。
5. 页面内应记住上次筛选条件。
6. 返回页面时应恢复上次筛选条件。

业务表单日期规则：

1. 优先从当前业务批次带出默认日期。
2. 截止时间默认 23:59。
3. 开始时间不能晚于结束时间。
4. 已过期要提示。
5. 临近截止要提示。
6. 日期字段必须有清晰 label 和说明。

截止时间快捷项规则：

AppDeadlinePicker 必须支持：

1. 今天 23:59
2. 明天 23:59
3. 本周五 23:59
4. 7 天后 23:59
5. 14 天后 23:59
6. 30 天后 23:59
7. 使用当前批次截止时间
8. 清空

日期展示规则：

1. 空值显示“未设置”。
2. 已过期显示“已逾期”。
3. 今天截止显示“今天截止”。
4. 明天截止显示“明天截止”。
5. 未过期显示“剩余 N 天”。
6. 已完成显示“已完成”。
7. 已归档显示“已归档”。

适用范围：

以下模块后续涉及日期时，都必须优先接入公共日期组件：

1. 毕业设计中心
2. 岗位实习中心
3. 学工中心
4. 教务中心
5. 数字迎新
6. 请假销假
7. 奖助勤贷补
8. 考务管理
9. 成绩管理
10. 系统日志 / 审计日志
11. 工作台待办 / 消息通知
12. 数据统计 / 台账筛选

验收要求：

1. 日期组件能正常选择。
2. 日期范围校验正常。
3. 清空可用。
4. 快捷项可用。
5. 筛选类日期默认显示“不限日期 / 全部时间”。
6. 业务表单日期能从批次或业务上下文自动带出。
7. 截止时间默认 23:59。
8. 空日期不显示 undefined / null / Invalid Date / yyyy/mm/日。
9. 日期格式全系统统一。
10. npm run build 通过。
11. 未接入公共日期组件的页面必须写入历史欠账。
