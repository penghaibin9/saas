# AI 执行状态（小程序端 miniapp）

> 回来先看这里，不用记流程、不用翻历史。只维护本文件，只改 miniapp/。

## 一句话现状
小程序端（uni-app + Vue3，纯 mock）已可演示：H5 能跑、能打开、能点、能填表、能返回；`build:h5` 与 `build:mp-weixin` 均构建成功。

## 怎么运行（H5）
```bash
cd miniapp
npm install        # 首次或换机才需要
npm run dev:h5
```
浏览器打开：**http://localhost:5188/**
- 选「我是学生」→ 学生端首页
- 选「我是老师」→ 选身份 → 教师工作台

## 最近一次验证结果
- npm run dev:h5：成功，运行在 http://localhost:5188/
- npm run build:h5：成功（DONE Build complete）
- npm run build:mp-weixin：成功（输出 dist/build/mp-weixin，用微信开发者工具导入）
- 模块编译自检：75/75 通过，0 报错
- 已肉眼确认渲染：登录、学生首页、身份选择、学生档案、服务申请表单、教师工作台等

## 已完成
- 学生端页面：登录、首页、我的档案、迎新报到、在校服务、服务申请(表单)、学业进度、岗位实习、填写周报(表单)、毕业设计、就业去向、我的申请、消息中心、个人中心
- 教师端页面：工作台、今日待办、审批处理、风险学生、学生详情(360)、实习周报批阅、毕设指导、就业跟进、消息中心、个人中心 + 身份切换
- 真实交互：请假/证明等服务申请表单（选择器+日期+事由+附件+校验），提交后进「我的申请」；实习周报填写表单，提交后回填状态
- 底部 tab、角色切换、加载/空/错误状态、脱敏、品牌配置占位

## 待办（后续可继续）
- 更多表单（毕设材料上传、就业去向登记）做成完整表单页
- 家长授权、企业导师入口
- 真实登录与后端对接（当前 ENV.useMock=true）
- 微信小程序真机联调（构建已通过，未真机跑）

## 红线（一直遵守）
未改 frontend/src、未改 PC router、未改根 package.json、未改 docs/ui、无后端、无数据库、未提交 git、未 push。
