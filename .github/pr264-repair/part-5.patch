diff --git a/backend/alembic/versions/20260906_aa_org_adjust_snapshot.py b/backend/alembic/versions/20260906_aa_org_adjust_snapshot.py
--- a/backend/alembic/versions/20260906_aa_org_adjust_snapshot.py
+++ b/backend/alembic/versions/20260906_aa_org_adjust_snapshot.py
@@ -1 +1 @@
-"""Preserve complete organization adjustment scope and precheck receipts.
+"""Compatibility anchor for the unmerged academic adjustment delivery.
@@ -3,2 +3,3 @@
-Revision ID: 20260906_aa_org_adjust_snapshot
-Revises: 20260901_orientation_self_activate_o6
+The draft's in-place VARCHAR widening was never released on main.  Preserve this
+revision identifier, but perform the additive expansion in 20260909_aa_adjust_expand.
+No legacy column, constraint or historical receipt is altered here.
@@ -6,4 +6,0 @@
-from alembic import op
-import sqlalchemy as sa
-from sqlalchemy.dialects import mysql
-
@@ -14 +10,0 @@
-TABLE = 't_aa_class_adjustment_request'
@@ -18,2 +14 @@
-    for column, limit, nullable in [('from_class_ids', 500, False), ('check_result_json', 2000, True)]:
-        op.alter_column(TABLE, column, existing_type=sa.String(limit), type_=mysql.LONGTEXT(), existing_nullable=nullable)
+    pass
@@ -23,9 +18 @@
-    # MySQL DDL commits implicitly: validate BOTH columns before shrinking either.
-    bind = op.get_bind()
-    oversized = bind.execute(sa.text(
-        f'SELECT COUNT(*) FROM {TABLE} WHERE CHAR_LENGTH(from_class_ids) > 500 OR CHAR_LENGTH(check_result_json) > 2000'
-    )).scalar_one()
-    if oversized:
-        raise RuntimeError('Existing organization adjustment receipts exceed the old limits; downgrade would truncate history.')
-    for column, limit, nullable in [('from_class_ids', 500, False), ('check_result_json', 2000, True)]:
-        op.alter_column(TABLE, column, existing_type=mysql.LONGTEXT(), type_=sa.String(limit), existing_nullable=nullable)
+    pass
diff --git a/backend/alembic/versions/20260909_aa_adjust_expand.py b/backend/alembic/versions/20260909_aa_adjust_expand.py
new file mode 100644
--- /dev/null
+++ b/backend/alembic/versions/20260909_aa_adjust_expand.py
@@ -0,0 +1,34 @@
+"""Add large adjustment evidence without changing N-1 columns.
+
+Legacy source JSON remains complete whenever it fits VARCHAR(500). Larger requests
+use a V2_ state unknown to N-1 writers: old precheck/execute/cancel state guards
+reject them rather than acting on an incomplete source scope. Full receipts have
+an additive payload plus a hash of the legacy projection; an N-1 write invalidates
+that payload instead of allowing a stale successful precheck to be replayed.
+"""
+from alembic import op
+import sqlalchemy as sa
+from sqlalchemy.dialects import mysql
+
+revision = '20260909_aa_adjust_expand'
+down_revision = '20260907_aa_classroom_buildings'
+branch_labels = None
+depends_on = None
+TABLE = 't_aa_class_adjustment_request'
+
+
+def upgrade():
+    op.add_column(TABLE, sa.Column('from_class_ids_expanded', mysql.LONGTEXT(), nullable=True))
+    op.add_column(TABLE, sa.Column('check_result_expanded', mysql.LONGTEXT(), nullable=True))
+
+
+def downgrade():
+    bind = op.get_bind()
+    count = bind.execute(sa.text(
+        'SELECT COUNT(*) FROM t_aa_class_adjustment_request '
+        'WHERE from_class_ids_expanded IS NOT NULL OR check_result_expanded IS NOT NULL'
+    )).scalar_one()
+    if count:
+        raise RuntimeError('Expanded adjustment evidence exists; application rollback must retain the additive columns.')
+    op.drop_column(TABLE, 'check_result_expanded')
+    op.drop_column(TABLE, 'from_class_ids_expanded')
diff --git a/e2e/pages/login.page.mjs b/e2e/pages/login.page.mjs
--- a/e2e/pages/login.page.mjs
+++ b/e2e/pages/login.page.mjs
@@ -133 +133,4 @@
-    if (await target.isDisabled()) return
+    if (await target.isDisabled()) {
+      await expect.poll(async () => roleMatches(await this.currentRoleText(), rolePattern)).toBe(true)
+      return
+    }
diff --git a/e2e/specs/academic-affairs-term-calendar-convenience.spec.mjs b/e2e/specs/academic-affairs-term-calendar-convenience.spec.mjs
--- a/e2e/specs/academic-affairs-term-calendar-convenience.spec.mjs
+++ b/e2e/specs/academic-affairs-term-calendar-convenience.spec.mjs
@@ -125 +125,7 @@
-    const copyPanel = page.getByText('快速复制上一学期校历', { exact: true }).locator('..').locator('..')
+    const copySection = page.locator('details.aa-calendar-copy')
+    await expect(copySection).toBeVisible()
+    if (!(await copySection.evaluate((element) => element.open))) {
+      await copySection.locator('summary').click()
+    }
+    await expect(copySection).toHaveAttribute('open', '')
+    const copyPanel = copySection.getByText('快速复制上一学期校历', { exact: true }).locator('..').locator('..')
diff --git a/e2e/specs/academic-d-w0-w1-graduation-archive.spec.mjs b/e2e/specs/academic-d-w0-w1-graduation-archive.spec.mjs
--- a/e2e/specs/academic-d-w0-w1-graduation-archive.spec.mjs
+++ b/e2e/specs/academic-d-w0-w1-graduation-archive.spec.mjs
@@ -145 +145,2 @@
-    await expect(page.getByText('审核结果', { exact: true }).first()).toBeVisible()
+    // The workspace owns navigation; assert the business view, not a hidden duplicate menu label.
+    await expect(page.locator('.agc-overview')).toBeVisible()
