name: One-shot BasePortal theme controls patch

on: [push]

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Validate temporary workflow
        run: echo "temporary workflow is valid"
