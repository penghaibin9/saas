name: 教务第四阶段状态说明（手动）

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: 第四阶段状态由总闸门直接发布
    runs-on: ubuntu-latest
    steps:
      - name: 说明
        run: |
          echo '第四阶段最终状态由“教务第四阶段合并前最终验收”的“第四阶段闸门汇总”任务直接裁决。'
          echo '本工作流不再自动轮询其它 Actions，避免并发取消产生假红状态。'
