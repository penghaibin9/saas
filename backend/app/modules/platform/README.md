# Platform Control Plane

This package is the canonical backend owner for platform-operations control-plane code.

During S0 Move Only, runtime behavior remains byte-identical to the legacy `app.api.v1.platform` implementation. The legacy module is a compatibility facade and route registration remains untouched until S0-06 after E-A01 releases the shared writer.
