# X2 复审修复证据（2026-09-01）

## 复审结论

本轮针对数字迎新、智慧宿舍、学校数据库导入及管理端/学生门户/教师小程序关键链路做了二次复审。发现的问题均已修复并补充回归覆盖，自动化测试、生产构建、全新 MySQL 迁移和真实浏览器验收全部通过。

## 本轮修复

- 现场报到：按学校本地自然日查询“今日”记录；已核销凭证允许过期后的同请求幂等重试，未使用的过期凭证仍拒绝；宿舍投影补齐租户与软删除隔离。
- 宿舍房源导入：确认阶段重新校验房间楼层、容量、类型和状态；锁定受影响房间床位并校验最终床位数；业务数据与导入批次成功回执合并为同一事务，消除中间提交窗口。
- 多端幂等：学生门户、学生小程序和教师小程序在失败重试时复用稳定的 `clientRequestId`；学生门户材料上传同时复用已成功上传的文件标识，避免重试重复上传。
- 教师绿色通道：移除系统原生 `showModal`，改为页面内审核对话框；提交失败时保留输入和错误；审核携带当前版本，防止并发覆盖。
- 管理端与文案：修正宿舍统计卡片非法强调色；生产错误提示不再误报“演示租户只读”。

## 自动化验证

- 后端相关全量：69 passed。
- 管理端迎新/宿舍目标契约：16 passed；管理端完整 Node 测试、lint、生产构建通过。
- 学生门户完整测试：124 passed；lint、生产构建通过。
- 小程序完整测试：244 passed；H5 与微信小程序生产构建通过。
- Python 语法检查与 `git diff --check` 通过。
- 已知输出仅含既有弃用、CSS `@import` 顺序及 bundle 体积提示，无失败项。

## 数据库验证

- 使用独立 MySQL 8.4 数据库 `saas_x2_review_runtime` 从空库执行完整 Alembic upgrade 成功。
- `alembic current`：`20260901_school_xlsx_x1 (head)`。
- `alembic heads`：`20260901_school_xlsx_x1 (head)`。

## 真实浏览器验收

- 管理端：宿舍统计、房源管理、调宿与退宿、宿舍异常四页均正常渲染，无 403、无原生弹窗。
- 学生门户：正式数据库登录后进入迎新页，迎新资格、材料、缴费、宿舍和绿色通道事实均由服务端正常返回。
- 教师小程序 H5：绿色通道待审数据正常；点击“退回”打开页面内对话框，确认不存在浏览器原生弹窗；取消后未写入业务数据。
- 教师小程序 H5：宿舍现场工作台正常渲染，归寝 Provider 未配置时明确展示 `DISABLED/未知`，不误判为未归。

## 分支说明

- 工作分支：`codex/orientation-dorm-20260901`。
- 本轮未合并 `origin/main`：复审前确认双方各有独立提交，但变更路径无重叠；保留当前工作分支提交历史，采用普通非强制推送。
