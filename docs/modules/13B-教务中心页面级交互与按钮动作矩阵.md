# 13B 教务中心页面级交互与按钮动作矩阵

> 版本：V1.1 页面级设计 · 依据《_13-需求输入-V1.1.md》§3/§11、《_13-现有系统集成事实速查.md》、《13B-教务中心页面树与路由设计.md》（路由与页面清单唯一基准）
> 范围：V1 核心 22 页逐页规格（按 §11 的 41 项分组紧凑呈现）+ 其余页面精简矩阵。表单字段级校验详见《13B-教务中心表单字段与校验规则.md》。
> 本文档不写代码、不改现有路由；PC 路由前缀一律 `/admin/academic-affairs`（下文页内路由均省略该前缀）。

---

## 一、全局约定（各页不再重复）

### 1.1 统一响应体与调用约定

- 响应体（冻结）：`{ code(数字), bizCode(字符串), message, data, traceId, timestamp }`；成功 `code=0 / bizCode=SUCCESS`。业务错误绝不 500。
- PC 全部接口 `require_staff`（学生令牌 403）；学生新建教务走 `/api/v1/mobile/academic-affairs/*`（复用既有 `/api/v1/mobile/academic/my` 学业过程聚合）。
- 新增端点前缀：`/api/v1/academic-affairs/terms|programs|courses|teaching-tasks|schedule|roll-changes|roll-registrations|graduation-audit`；成绩、预警**复用既有** `/api/v1/academic/grades`、`/api/v1/academic/warnings`（复用既有 academic 学业过程模块）；学籍档案读复用 `/api/v1/academic/students`（复用既有 academic 学业过程模块）。
- 审批一律复用 Workflow：`/api/v1/approvals/tasks/{taskId}/approve|reject|transfer`；待办写 t_unified_todo、通知写 t_unified_message（类型限 WORKFLOW_TODO / WORKFLOW_RESULT / RISK_ALERT / DEADLINE_REMINDER / STATUS_CHANGED / RETURNED_NOTICE / PUBLISHED_NOTICE / ARCHIVE_NOTICE）。
- 权限点命名 `academicAffairs.<biz>.<action>`；无 `<biz>.view` 则菜单整项隐藏。

### 1.2 错误码 → UI 提示映射（全模块统一，按钮矩阵"失败提示"列均引用此表语义）

| 错误码 | bizCode | UI 处理 |
|---|---|---|
| 401001 | UNAUTHORIZED | 静默单飞刷新 token；失败清 session 跳 /login |
| 403001 | NO_PERMISSION | toast「无操作权限」；无权限按钮应事先隐藏，此码为兜底 |
| 403002 | NO_DATA_SCOPE | 详情页整页 noPermission 态「无该学生/学院数据范围权限」；列表由后端 scope 过滤不展示越权行 |
| 404001 | DATA_NOT_FOUND | 提示「数据不存在或已被删除」，列表刷新移除该行 |
| 409001 | DATA_CONFLICT | 「已存在重复/在途数据」，引导查看已有单据 |
| 409001 | APPROVAL_VERSION_CONFLICT | 弹窗「该任务已被他人处理」，自动刷新任务最新状态与流水 |
| 409001 | IDEMPOTENCY_CONFLICT | 「请勿重复提交」，按已成功处理刷新页面 |
| 422001 | VALIDATION_ERROR | 表单逐字段红字回显后端 message；非表单场景 toast 具体原因 |
| 429001 | RATE_LIMITED | 「操作过于频繁（导出 5 次/分），请稍后再试」 |
| 400001 | BAD_REQUEST | 「请求参数错误」+ traceId，引导反馈 |

### 1.3 五态全局规范（各页【状态视图】仅写差异项）

| 态 | 规范 |
|---|---|
| loading | 列表骨架屏/详情占位；按钮 loading 并锁定 |
| empty | 空态插画 + 引导文案；有创建权限时附主操作按钮 |
| error | 错误插画 + message + traceId + 【重试】 |
| noPermission | 403 整页态（403001/403002 文案区分）；菜单级无 view 权限不渲染 |
| readonly | 页顶只读横幅并注明原因（业务状态只读 / 角色只读 / demo-school 演示只读 / 租户到期只读），全部写按钮隐藏或置灰 |

### 1.4 审批页通用交互（8/10/12/20 等审批类页共用）

1. 载入审批任务时携带 `version`（乐观锁）；【通过】【驳回】提交带回 version。
2. 已处理再审 → 409001 APPROVAL_VERSION_CONFLICT：弹窗告知已被处理，刷新显示最新节点与流水，本页转只读。
3. 驳回/退回意见必填（长度规则接平台规则中心 safe_rule 配置），422001 逐字段回显。
4. 所有提交按钮统一提交锁（同小程序 createSubmitLock 思路：点击即锁 → 返回后解锁），防连点重复请求；幂等冲突按 IDEMPOTENCY_CONFLICT 处理。
5. 每次审批动作：写 t_workflow_task（acted_at/action_reason）→ 下一节点生成 t_unified_todo + WORKFLOW_TODO 消息 → 终态给发起人/学生 WORKFLOW_RESULT/RETURNED_NOTICE → `record(action, resource)` 审计。
6. 审批中单据对发起人只读，仅当前节点未处理时发起人可【撤回】（回 DRAFT/可编辑态）。

### 1.5 演示租户与审计

- demo-school 中间件只读锁：本模块全部写操作返回 403，前端统一横幅「演示环境只读，请登录沙箱租户 sandbox-school（admin2/123456，每日 0 点重置）体验写操作」。
- 审计双层：安全审计 `app.services.audit_log.record(action, resource, detail, result)` → t_security_audit_log（含 ip/ua/traceId）；域级留痕 t_aa_audit_trail（append-only，状态流转/发布/终审必写）。导出一律登记 t_export_task（用途必填≥5 字、行数、sha256、水印）。
- 导入统一走导入管线 `/api/v1/import/domain/{domain}/validate|confirm`（dry-run 行级错误 → 批次确认，5000 行/20MB 上限）；导出统一走 `/api/v1/export/domain/{domain}`（脱敏列、首行水印、5 次/分、5000 行）。

---

## 二、V1 核心页面逐页规格（22 页）

### 2.1 教务首页（/admin/academic-affairs）

**【定位】** 一级模块=教务中心；二级菜单=教务首页；三级页面=无（驾驶舱本体）；路由 `/admin/academic-affairs`；谁能进=教务处（全校）/学院教务（本学院）/教师（本人课程与授课学生），角色 preset 出不同视图；从哪进=侧边栏「教务中心」；用途=按角色聚合本学期教学运行指标、待办与快捷入口。

**【展示】** 顶部：当前学期（已发布校历）+ 角色/数据范围标识。教务处视图卡片：教学任务/排课完成率/课表发布/成绩审核待办/学籍异动待审/学业预警数/毕业资格异常数；学院教务视图：本学院同类指标（未排课程/冲突/异动/毕业异常）；教师视图：今日课程/授课班级/成绩录入任务/调停课申请（P2 隐）/学生名单。筛选：学期切换。列表：待办 TOP N（t_unified_todo）、最近消息。详情字段：无（下钻至各业务页）。

**【表单】** 无表单页。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 刷新 | 全部可进角色 | 重拉驾驶舱数据 | GET /api/v1/academic-affairs/dashboard | 卡片/待办更新 | error 态+traceId+重试 | 只读不审计 |
| 切换学期 | 全部可进角色 | 按学期过滤指标 | 同上 ?termId= | 指标切换 | 404001 学期不存在 | 不审计 |
| 待办【去处理】 | 待办分配人（assignee 本人） | 跳对应审批/处置页 | GET /api/v1/approvals/tasks | 进入目标页 | 403002 非本人待办不可见 | 在目标页动作时审计 |
| 预警卡片下钻 | 教务处/学院教务（scope） | 跳 /warnings | 复用 GET /api/v1/academic/warnings（复用既有 academic 学业过程模块） | 列表按 scope 过滤展示 | 403002 无数据范围 | 处置时审计 |
| 【更多统计】 | 教务处/学院教务 | 跳 /stats | /api/v1/stats/* 扩展聚合 | 打开统计总览 | 403001 | 不审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「本学期暂无教学数据」+（教务处）【去创建学期】；readonly 不适用（纯只读页） |
| 业务态 | 无独立业务态；卡片随各业务状态实时聚合（校历未发布时提示先发布校历） |

**【治理】** 脱敏=学生列表电话/证件不出现在首页；水印=无；审计=下钻处置时记；进360=否；进统计=本页即统计消费端（扩展 stats_service 角色 preset）；导入/导出=无；移动端=学生小程序无对应（学生视图在小程序各页）、教师端「今日课程」对应教师视图；跳转=统一待办、/warnings、/stats、各业务列表。

**【验收】** 1) 学院教务登录仅见本学院指标，请求他院数据 403002；2) 教师视图仅见本人课程/任务卡片，无管理入口。

### 2.2 学期列表（/terms）

**【定位】** 一级=教务中心；二级=学年学期与校历；三级=列表页；路由 `/terms`；谁能进=教务处（管理，全校）/学院教务（只读查看）；从哪进=菜单「学年学期与校历」；用途=学年学期全生命周期管理入口（DRAFT/PUBLISHED/FROZEN/ARCHIVED）。

**【展示】** 顶部：当前学期高亮。筛选：学年/状态/关键词。列表字段：学年、学期、起止日期、教学周数、状态、校历配置完整度（校历/节次是否配齐）、发布时间、操作。详情字段：下钻校历/节次页。

**【表单】** 新增/编辑在 `/terms/create`、`/terms/:termId/edit`（学年、学期名、起止日期必填；起止不重叠、结束>开始；详见字段文档）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 新增 | 教务处 | 跳 /terms/create | POST /api/v1/academic-affairs/terms | 新增 DRAFT 行 | 409001 学年学期重叠/重复；422001 | record(create, term) |
| 编辑 | 教务处 + DRAFT | 跳 /terms/:termId/edit | PUT /api/v1/academic-affairs/terms/{termId} | 行信息更新 | 409001 version 冲突；422001 | record(update, term) |
| 校历 | 教务处（DRAFT 可编，其余只读） | 跳 /terms/:termId/calendar | — | 进校历编辑 | 404001 | 编辑页内审计 |
| 节次 | 教务处（DRAFT 可编，其余只读） | 跳 /terms/:termId/time-slots | — | 进节次配置 | 404001 | 配置页内审计 |
| 发布 | 教务处 + DRAFT 且校历/节次完整 | 跳 /terms/:termId/publish 确认 | POST /api/v1/academic-affairs/terms/{termId}/publish | DRAFT→PUBLISHED；同步排课/课表/工作台/学生端；发 PUBLISHED_NOTICE | 422001 校历或节次未配置完整 | record(publish, term)+audit_trail |
| 冻结 | 教务处 + PUBLISHED | 二次确认 | POST .../terms/{termId}/freeze | PUBLISHED→FROZEN，排课与变更入口关闭 | 409001 version 冲突 | record(freeze, term)+audit_trail |
| 归档 | 教务处 + FROZEN 且学期已结束 | 二次确认 | POST .../terms/{termId}/archive | FROZEN→ARCHIVED 只读可导出 | 422001 学期未结束 | record(archive, term)+ARCHIVE_NOTICE |
| 删除 | 教务处 + DRAFT | 二次确认（软删） | DELETE .../terms/{termId} | 行移除 | 409001 已被排课/任务引用不可删 | record(delete, term) |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「暂无学期」+【新增学期】（教务处）；学院教务 readonly 横幅「只读视图」 |
| DRAFT | 可编辑/删除/配校历节次/发布 |
| PUBLISHED | 只读+可冻结；校历变更需先冻结评估（不支持直接改已发布校历） |
| FROZEN | 只读，仅可归档 |
| ARCHIVED | 只读可导出（P2 归档查看页 /terms/:termId/archive） |

**【治理】** 脱敏=无敏感字段；水印=无；审计=全部写操作 record + audit_trail；进360=否；进统计=注册率/任务完成率均以学期为维度；导入=无；导出=P2 归档页；移动端=学生小程序「我的课表」周次读已发布校历，无独立页；跳转=校历/节次/发布页、任务批次新增（前置校验）。

**【验收】** 1) DRAFT 学期缺节次配置点【发布】→ 422001 且状态不变；发布成功后学生小程序课表周次同步；2) 学院教务调用 POST /terms → 403001，页面无【新增】按钮。

### 2.3 校历编辑（/terms/:termId/calendar）

**【定位】** 一级=教务中心；二级=学年学期与校历；三级=学期下钻编辑页；路由 `/terms/:termId/calendar`；谁能进=教务处（DRAFT 态可编，其余态只读）、学院教务只读；从哪进=学期列表【校历】；用途=维护教学周/考试周/实习周/节假日/调休。

**【展示】** 顶部：学期名+起止+状态徽标。主体：周历视图（周次×周类型着色）+ 节假日/调休清单。筛选：月份/周类型。详情字段：每周（周次、起止、类型=教学/考试/实习/假期）、节假日（日期、名称）、调休（停课日↔补课日配对）。

**【表单】** 周类型标记（周次范围+类型必填）；节假日（日期+名称必填）；调休（原日与补课日必填且配对）。校验：全部日期在学期起止内、考试周/实习周不与教学周重叠冲突、调休必须成对；详见字段文档。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 生成教学周 | 教务处 + DRAFT | 按起止自动切分周次 | GET/本地计算+PUT 保存 | 周历填充教学周 | 422001 学期起止缺失 | 保存时审计 |
| 标记考试/实习周 | 教务处 + DRAFT | 选周次改类型 | PUT /api/v1/academic-affairs/terms/{termId}/calendar | 周类型更新 | 422001 周次越界 | record(update, term_calendar) |
| 添加节假日/调休 | 教务处 + DRAFT | 弹窗录入 | 同上（整体保存） | 清单新增，周历标红 | 422001 调休未配对/日期越界 | 同上 |
| 保存 | 教务处 + DRAFT | 整体保存（带 version） | PUT .../calendar | 草稿更新 | 409001 version 冲突（他人已改，刷新合并） | record(update, term_calendar) |
| 去发布 | 教务处 + DRAFT 且校验通过 | 跳 /terms/:termId/publish | POST .../terms/{termId}/publish | DRAFT→PUBLISHED（学期级） | 422001 存在未闭合调休/空周类型 | record(publish, term)+audit_trail |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=未生成周次时显示【生成教学周】引导 |
| DRAFT | 全量可编辑 |
| PUBLISHED/FROZEN | 整页只读横幅「校历已发布/冻结，调整需冻结评估或下学期处理」 |
| ARCHIVED | 只读可导出 |

**【治理】** 脱敏=无；水印=无；审计=保存/发布 record + audit_trail；进360=否；进统计=教学周供排课/考试/统计口径；导入/导出=无（P2 归档导出）；移动端=学生「我的课表」周次/假期标识同步；跳转=学期列表、节次配置、发布页。

**【验收】** 1) 调休只填停课日未填补课日→保存 422001 逐项红字；2) 发布后再 PUT calendar → 403/409 拒绝且页面只读。

### 2.4 节次配置（/terms/:termId/time-slots）

**【定位】** 一级=教务中心；二级=学年学期与校历；三级=学期下钻配置页；路由 `/terms/:termId/time-slots`；谁能进=教务处（DRAFT 可编）；从哪进=学期列表【节次】；用途=维护作息时间与上下课节次，供排课/课表/考试引用。

**【展示】** 顶部：学期+状态。列表字段：节次号、名称（第1节…晚自习）、开始时间、结束时间、时段（上午/下午/晚上）。筛选：无（单表全量）。

**【表单】** 节次号（唯一）、名称、起止时间必填；校验：结束>开始、节次间不重叠、节次号连续；详见字段文档。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 添加节次 | 教务处 + DRAFT | 行内新增 | PUT /api/v1/academic-affairs/terms/{termId}/time-slots | 节次行新增 | 422001 时间重叠/节次号重复 | 保存时审计 |
| 复制上学期 | 教务处 + DRAFT | 选来源学期整体复制 | GET 来源 + PUT 本学期 | 整表填充 | 404001 来源学期无配置 | record(copy, time_slots) |
| 恢复默认模板 | 教务处 + DRAFT | 载入平台默认作息 | PUT .../time-slots | 整表重置 | — | record(reset, time_slots) |
| 保存 | 教务处 + DRAFT | 整体保存（version） | PUT .../time-slots | 配置生效（学期发布后随之下发） | 409001 version 冲突；422001 | record(update, time_slots) |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「未配置节次」+【恢复默认模板】 |
| DRAFT | 可编辑 |
| PUBLISHED/FROZEN/ARCHIVED | 只读（已被课表引用，调整需下学期） |

**【治理】** 脱敏=无；水印=无；审计=保存类操作 record；进360=否；进统计=否（排课冲突检测引用）；导入/导出=无；移动端=课表格子时间显示来源；跳转=学期列表、校历编辑。

**【验收】** 1) 两节次时间段重叠→保存 422001 定位到行；2) 学期 PUBLISHED 后本页只读，PUT 返回拒绝（demo-school 下所有保存 403 引导沙箱）。

### 2.5 学籍档案列表（/roll/students）

**【定位】** 一级=教务中心；二级=学籍管理；三级=列表页；路由 `/roll/students`；谁能进=教务处（全校）/学院教务（本学院，scope 过滤）；从哪进=菜单「学籍管理」；用途=在籍学生学籍档案总览与检索，学籍业务发起入口。

**【展示】** 顶部：在籍/休学/待注册人数汇总。筛选：学院/专业/班级/年级/学籍状态（PENDING_REGISTER/ACTIVE/REGISTERED/UNREGISTERED/SUSPENDED/RESUMED/TRANSFER_MAJOR/TRANSFER_SCHOOL/WITHDRAWN/RETAINED/GRADUATED/COMPLETED/INCOMPLETE/ARCHIVED）/当前阶段/关键词（学号/姓名）。列表字段：学号、姓名、性别、学院、专业、班级、年级、学籍状态、当前阶段、绑定方案版本。详情字段（/roll/students/:studentId）：基础信息+联系方式（脱敏）+学籍状态历史（StageEvent）+异动记录+成绩单入口。

**【表单】** 本页无直接表单；学籍数据以导入与异动流转维护（禁止直接改学籍状态字段）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 查看详情 | 教务处/学院教务（scope 命中） | 跳 /roll/students/:studentId | 复用 GET /api/v1/academic/students（复用既有 academic 学业过程模块）/{id} | 打开档案详情 | 403002 越权学院 | 查看完整证件号需二次审计 record(view_sensitive, student) |
| 学籍导入 | 教务处/学院教务 | 走导入管线 dry-run→confirm | /api/v1/import/domain/academic-roll/validate\|confirm | 批次入库、行级错误报告 | 422001 行级错误清单（行号/字段/原因）；跨租户批次拒绝 | 导入批次留痕+record(import, roll) |
| 导出名单 | 教务处/学院教务 | 用途弹窗（≥5 字）后导出 | POST /api/v1/export/domain/academic-roll | 生成带水印文件，t_export_task 留痕 | 429001 限流；422001 用途不足 5 字 | t_export_task+下载审计 |
| 代录异动 | 教务处/学院教务 | 跳 /roll/changes/create?studentId= | — | 进异动代录表单 | 403002 | 提交时审计 |
| 成绩单 | scope 命中 | 跳 /grades/transcript/:studentId | — | 打开成绩单 | 403002 | 成绩单页内审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「暂无学生，可通过学籍导入初始化」 |
| 业务态 | 行级学籍状态徽标：SUSPENDED 灰+「休学中」、WITHDRAWN/GRADUATED 等终态行只读；ARCHIVED 只读可导出 |

**【治理】** 脱敏=身份证/电话默认脱敏（MobileSensitiveText 同源规则，`*_encrypted` 出口脱敏），看完整必审计；水印=导出首行水印；审计=敏感查看/导入/导出；进360=档案详情聚合读 StageEvent；进统计=注册率/异动数分母；导入=academic-roll 域；导出=academic-roll 域；移动端=学生小程序「我的学籍」（本人只读）；跳转=异动、成绩单、毕业预审详情、现有学生中心 360。

**【验收】** 1) 学院教务检索他院学号：列表不出现，直连详情 URL → 403002；2) 导出未填用途或不足 5 字 → 422001，成功导出后 t_export_task 有记录且文件含水印。

### 2.6 注册名单核对（/roll/registration/:batchId/check）

**【定位】** 一级=教务中心；二级=学籍管理·入学注册；三级=批次下钻操作页；路由 `/roll/registration/:batchId/check`；谁能进=学院教务（核对，本院）→教务处（确认注册）；从哪进=入学注册批次列表【核对】；用途=迎新完成→待注册学生逐一核身份/专业/班级，系统联查报到缴费材料，教务处确认后转在籍 REGISTERED。

**【展示】** 顶部：批次名/学期/应注册人数/已核对/异常数进度条。筛选：学院/专业/班级/核对状态（待核对/已核对/异常）/缴费状态。列表字段：学号、姓名、专业、班级、报到状态（读迎新）、缴费状态、材料状态、核对状态、异常原因。详情：行展开显示迎新材料明细。

**【表单】** 标记异常：异常原因（必填，枚举=身份不符/未缴费/材料缺失/其他+说明）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 核对通过（单/批量） | 学院教务 + 批次核对中 + 行=待核对 | 勾选提交 | POST /api/v1/academic-affairs/roll-registrations/{batchId}/students/check | 行→已核对 | 422001 存在缴费/材料未通过项需先标异常 | record(check, registration) |
| 标记异常 | 学院教务 + 行=待核对/已核对 | 弹窗填原因 | POST .../students/mark-abnormal | 行→异常，通知辅导员（t_unified_todo） | 422001 原因必填 | record(mark_abnormal, registration) |
| 提交教务处 | 学院教务 + 本院全部行已核对或异常 | 二次确认 | POST .../{batchId}/submit | 批次→待教务处审；教务处收 WORKFLOW_TODO | 422001 存在待核对学生；409001 重复提交 | record(submit, registration)+audit_trail |
| 确认注册 | 教务处 + 批次待审 | 批量确认 | POST .../{batchId}/confirm | 学生 student_status→REGISTERED（在籍）；写 StageEvent 进 360；同步学工/实习/毕设/就业；发 STATUS_CHANGED | 409001 APPROVAL_VERSION_CONFLICT 已被确认 | record(confirm, registration)+audit_trail |
| 导出未注册名单 | 教务处/学院教务 | 用途弹窗后导出 | POST /api/v1/export/domain/academic-registration | 水印文件+留痕 | 429001/422001 | t_export_task |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「该批次暂无待注册学生」 |
| 批次核对中 | 学院教务可核对/标异常/提交 |
| 待教务处审 | 学院教务只读；教务处可确认 |
| 已确认 | 整页只读；异常学生保留 UNREGISTERED 待后续处理 |

**【治理】** 脱敏=证件号脱敏；水印=导出水印；审计=核对/确认/导出；进360=REGISTERED 写 StageEvent；进统计=注册完成率（/stats）；导入=无（名单来自迎新）；导出=未注册名单；移动端=学生「我的学籍」显示注册结果；跳转=迎新模块材料页、学籍档案详情。

**【验收】** 1) 存在待核对学生时点【提交教务处】→ 422001 批次状态不变；2) 教务处确认后学生状态=REGISTERED、360 时间线出现注册事件、/stats 注册率变化；重复确认 → 409001。

### 2.7 异动申请列表（/roll/changes，含详情要点）

**【定位】** 一级=教务中心；二级=学籍管理·学籍异动；三级=列表页（详情 /roll/changes/:changeId 为行下钻）；路由 `/roll/changes`；谁能进=教务处（全校）/学院教务（本院）/辅导员（本班只读）；从哪进=菜单「学籍异动」；用途=转专业/休学/复学/退学/留级/转学六类异动的受理、跟踪与审批入口。

**【展示】** 顶部：tab=全部/待我审/在途/已完结 + 各类型在途计数。筛选：类型/状态/学院/班级/申请时间段/关键词。列表字段：单号、学生（学号/姓名/班级）、类型、发起方式（学生小程序/教务代录）、当前节点、状态、申请时间、操作。详情要点（/roll/changes/:changeId）：申请信息（类型/理由/起止期限/目标专业班级）、材料附件、审批流水（节点/审批人/时间/意见，读 t_workflow_task）、终态后的学籍变化摘要与 360 链接。

**【表单】** 代录新增在 /roll/changes/create：学生（检索必填）、类型（枚举六类必填）、理由（长度规则见字段文档）、材料（按类型必填）、休学需起止期限、转专业需目标专业/班级。校验：学生在籍、申请时间窗内、同类型在途申请拦截。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 代录新增 | 教务处/学院教务 | 跳 /roll/changes/create 提交 | POST /api/v1/academic-affairs/roll-changes | 生成异动单+首节点待办（辅导员）；学生收 STATUS_CHANGED | 409001 同类型在途申请已存在；422001 校验失败（不在籍/超时间窗/材料缺失） | record(create, roll_change) |
| 查看详情 | scope 命中 | 跳 /roll/changes/:changeId | GET .../roll-changes/{id} | 打开详情+流水 | 403002 越权（辅导员非本班） | 敏感材料查看审计 |
| 去审批 | 当前节点审批人 + 状态在途 | 跳 /roll/changes/:changeId/approve | — | 进审批页 | 403002 非当前节点审批人 | 审批页内审计 |
| 统计 | 教务处/学院教务 | 跳 /roll/changes/stats | GET .../roll-changes/stats | 打开异动统计 | 403001 | 不审计 |
| 导出 | 教务处/学院教务 | 用途弹窗后导出 | POST /api/v1/export/domain/academic-roll-change | 水印文件+留痕 | 429001/422001 | t_export_task |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「暂无异动申请」；辅导员 readonly 横幅（只读联动视图） |
| 在途（节点流转中） | 发起人只读+可撤回（首节点未处理时）；当前节点人可审批 |
| 已通过 | 只读；显示学籍变化摘要（新专业/班级或 SUSPENDED 等） |
| 已驳回/已撤回 | 只读；学生可重新发起（小程序） |

**【治理】** 脱敏=学生证件/联系方式脱敏；水印=导出水印；审计=创建/导出/审批；进360=终态写 StudentStageEvent；进统计=异动人数（/roll/changes/stats + /stats）；导入=无；导出=异动记录；移动端=学生小程序「异动申请」（可写发起）、教师移动端审批走 /api/v1/mobile/teacher/approvals；跳转=学籍档案、审批页、统计页。

**【验收】** 1) 学生已有在途休学申请，教务代录同类型 → 409001 仅存一条；2) 辅导员访问非本班异动详情 → 403002。

### 2.8 异动审批（/roll/changes/:changeId/approve）

**【定位】** 一级=教务中心；二级=学籍管理·学籍异动；三级=审批页；路由 `/roll/changes/:changeId/approve`；谁能进=当前节点审批人（辅导员初审→原学院教务转出→目标学院教务接收〔转专业〕→教务处终审；休学/复学为辅导员→学院→教务处）；从哪进=统一待办 / 异动列表「待我审」tab；用途=按 Workflow 节点逐级审批学籍异动。

**【展示】** 顶部：单号/类型/当前节点/剩余节点进度条。详情字段：申请全量信息+材料预览+已有审批流水；转专业节点附目标专业容量/班级余量提示；休学附起止期限与联动影响提示（禁选课/考试/实习/毕设）。

**【表单】** 审批意见（同意可选填、驳回必填）；目标学院接收节点需选定接收班级。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 同意/转出/接收/终审通过 | 当前节点审批人 + 任务 PENDING（按节点变文案） | 提交意见+version | POST /api/v1/approvals/tasks/{taskId}/approve | 节点→下一节点（生成 t_unified_todo+WORKFLOW_TODO）；终审通过：更新 t_student_profile 专业/班级或 student_status=SUSPENDED 等、重绑培养方案（旧成绩保留）、写 StageEvent、学生收 WORKFLOW_RESULT | 409001 APPROVAL_VERSION_CONFLICT 已被处理；422001 接收节点未选班级/容量不足 | record(approve, roll_change)+t_workflow_task 留痕 |
| 驳回 | 同上 | 意见必填提交 | POST .../tasks/{taskId}/reject | 单→已驳回；学生收 RETURNED_NOTICE | 422001 意见必填；409001 同上 | record(reject, roll_change) |
| 转办 | 当前节点审批人 | 选择转办人 | POST .../tasks/{taskId}/transfer | 任务 TRANSFERRED，新审批人收待办 | 403002 转办人无该学生范围 | record(transfer, roll_change) |
| 查看学籍档案 | scope 命中 | 新页跳 /roll/students/:studentId | — | 佐证查看 | 403002 | 档案页内审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；noPermission=非当前节点人进入整页 403 态 |
| 任务 PENDING | 可同意/驳回/转办 |
| 任务已处理（APPROVED/REJECTED/TRANSFERRED） | 只读流水；再次提交 → 409 弹窗刷新（§1.4） |
| 单据终态 | 整页只读，显示学籍变化结果 |

**【治理】** 脱敏=材料中证件脱敏；水印=无；审计=每个动作 record+workflow 留痕（append-only）；进360=终审写 StageEvent；进统计=异动统计实时计数；导入/导出=无；移动端=教师移动端 POST /api/v1/mobile/teacher/approvals/{id}/approve|reject（含范围校验+409）；跳转=学籍档案、异动详情、统一待办。

**【验收】** 1) 双开页面先后点【同意】→ 第二次 409001 APPROVAL_VERSION_CONFLICT 且流转只发生一次；2) 转专业终审通过后学生主档专业/班级更新、360 出现异动事件、小程序「我的学籍」显示新班级。

### 2.9 方案编制（/programs/:programId/edit，含课程配置要点）

**【定位】** 一级=教务中心；二级=培养方案；三级=编辑页（课程配置 /programs/:programId/courses 为其子步骤）；路由 `/programs/:programId/edit`；谁能进=专业负责人（本专业，DRAFT/RETURNED 态）；从哪进=方案详情【编制】/ 退回通知；用途=编制方案基本信息、毕业要求、学分结构与课程配置，提交两级审核。

**【展示】** 顶部：方案名/专业/年级/版本号/状态徽标+学分完成度（已配学分/要求总学分）。分步表单：①基本信息（专业/年级/学制/总学分）②毕业要求 ③学分结构（公共基础/专业基础/专业核心/实践/选修/顶岗实习/毕业设计 分模块学分）④课程配置（子页）。课程配置要点（/courses 子页）：从课程库（仅启用态）选课程 → 配模块归属/学分学时/开课学期/课程性质（必修/选修）；列表字段=课程编号/名称/模块/学分/学时/开课学期/性质。

**【表单】** 关键校验：课程必须来自课程库启用态、同一课程不重复配置、各模块学分合计=学分结构、总学分达标、开课学期在学制范围内合法；其余字段级规则见字段文档。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 保存草稿 | 专业负责人 + DRAFT/RETURNED | 分步保存（version） | PUT /api/v1/academic-affairs/programs/{programId} | 草稿更新，停留当前步 | 409001 version 冲突；422001 | record(update, program) |
| 课程配置 | 同上 | 跳 /programs/:programId/courses | PUT .../programs/{programId}/courses | 课程清单保存 | 422001 课程重复/非启用课程/学期非法 | record(update, program_courses) |
| 学分自检 | 同上 | 前端+后端校验汇总 | POST .../programs/{programId}/validate | 显示逐项校验结果（学分/重复/学期） | 422001 逐项列出 | 不审计 |
| 提交审核 | 同上 + 自检通过 | 二次确认 | POST .../programs/{programId}/submit | DRAFT/RETURNED→COLLEGE_REVIEW；学院教务收 WORKFLOW_TODO；本页转只读 | 422001 校验未通过项清单；409001 重复提交 | record(submit, program)+audit_trail |
| 撤回 | 专业负责人 + COLLEGE_REVIEW 且初审未处理 | 二次确认 | POST .../programs/{programId}/withdraw | 回 DRAFT 可继续编辑 | 409001 初审已处理不可撤回 | record(withdraw, program) |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；noPermission=非本专业负责人 403002 |
| DRAFT/RETURNED | 可编辑可提交；RETURNED 顶部显示退回意见（RETURNED_NOTICE 同步） |
| COLLEGE_REVIEW/ACADEMIC_REVIEW | 只读+撤回（仅初审未处理）；显示审批进度 |
| PUBLISHED/ENABLED | 只读；修改必须走新版本（版本+1 新 DRAFT，历史学生用旧版本） |
| FROZEN/DISABLED | 只读，页顶标注冻结/停用原因 |

**【治理】** 脱敏=无；水印=无；审计=保存/提交/撤回；进360=否（方案经绑定影响学生培养进度）；进统计=方案发布率；导入=P2 方案导入；导出=无（P2 版本对比页）；移动端=无对应页；跳转=课程库列表（选课程）、方案审核页、方案详情。

**【验收】** 1) 配置课程总学分≠学分结构合计时提交 → 422001 且状态仍为 DRAFT；2) ENABLED 方案直接 PUT → 拒绝，只能【新建版本】生成新 DRAFT，旧版本对历史学生继续生效。

### 2.10 方案审核（/programs/:programId/approve）

**【定位】** 一级=教务中心；二级=培养方案；三级=审批页；路由 `/programs/:programId/approve`；谁能进=学院教务（COLLEGE_REVIEW 初审，本院）→教务处（ACADEMIC_REVIEW 终审）；从哪进=统一待办 / 方案列表「待审」tab；用途=两级审核培养方案，终审通过后进入发布。

**【展示】** 顶部：方案名/专业/版本/当前节点。详情字段：基本信息+毕业要求+学分结构汇总表+课程配置清单（按模块分组）+系统自检结果（学分/重复/学期合法性红绿灯）+历史审批流水。

**【表单】** 审批意见（通过可选填、退回必填）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 初审通过 | 学院教务 + COLLEGE_REVIEW + 任务 PENDING | 提交意见+version | POST /api/v1/approvals/tasks/{taskId}/approve | COLLEGE_REVIEW→ACADEMIC_REVIEW；教务处收 WORKFLOW_TODO | 409001 APPROVAL_VERSION_CONFLICT | record(approve, program)+workflow 留痕 |
| 终审通过 | 教务处 + ACADEMIC_REVIEW + PENDING | 提交意见 | 同上 | →待发布（引导跳 /programs/:programId/publish 绑定专业年级后 PUBLISHED） | 409001 同上 | record(approve, program)+audit_trail |
| 退回 | 当前节点人 + PENDING | 意见必填 | POST .../tasks/{taskId}/reject | →RETURNED；负责人收 RETURNED_NOTICE 回编制页 | 422001 意见必填 | record(reject, program) |
| 查看课程明细 | 可进者 | 展开课程配置抽屉 | GET .../programs/{programId} | 只读明细 | 404001 | 不审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；noPermission=他院学院教务 403002 |
| COLLEGE_REVIEW | 学院教务可审；教务处只读 |
| ACADEMIC_REVIEW | 教务处可审；学院只读 |
| RETURNED/PUBLISHED 等 | 整页只读流水；已处理任务再审 409 弹窗（§1.4） |

**【治理】** 脱敏=无；水印=无；审计=通过/退回；进360=否；进统计=方案发布率；导入/导出=无；移动端=无（审批回 PC）；跳转=方案发布页、方案编制页（退回后）、课程详情。

**【验收】** 1) 初审已通过后学院教务重复提交 → 409001，节点不重复流转；2) 退回后方案=RETURNED，负责人编制页可再编辑并显示退回意见。

### 2.11 课程库列表（/courses）

**【定位】** 一级=教务中心；二级=课程库；三级=列表页；路由 `/courses`；谁能进=教务处/学院教务/专业负责人（管理范围内）/教师（只读）；从哪进=菜单「课程库」；用途=全校课程台账，方案编制的课程来源（仅启用态可被引用）。

**【展示】** 顶部：启用/待审/草稿计数。筛选：类别/性质（必修/选修）/状态/学院/关键词（编号/名称）。列表字段：课程编号、名称、类别、性质、学分、学时（理论/实践）、适用专业、先修课程、负责人、状态、版本。详情（/courses/:courseId）：全字段+被引用方案列表+审批流水。

**【表单】** 新增/编辑在 /courses/create、/courses/:courseId/edit（负责人，DRAFT/RETURNED 态）：编号唯一、名称/类别/性质/学分/学时必填、先修课程从课程库选；详见字段文档。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 新增 | 课程负责人/学院教务 | 跳 /courses/create | POST /api/v1/academic-affairs/courses | 新 DRAFT 课程 | 409001 编号重复；422001 | record(create, course) |
| 编辑 | 课程负责人 + DRAFT/RETURNED | 跳 /courses/:courseId/edit | PUT .../courses/{courseId} | 更新草稿 | 409001 version；422001 | record(update, course) |
| 提交审核 | 负责人 + DRAFT/RETURNED | 二次确认 | POST .../courses/{courseId}/submit | →学院初审；学院教务收 WORKFLOW_TODO | 422001 必填缺失 | record(submit, course) |
| 去审核 | 当前节点审批人 | 跳 /courses/:courseId/approve | — | 进审核页 | 403002 | 审核页内审计 |
| 导入 | 教务处/学院教务 | 跳 /courses/import（dry-run→confirm） | /api/v1/import/domain/academic-course/validate\|confirm | 批量入库（DRAFT 或直启用由配置定） | 422001 行级错误清单 | 导入留痕+record(import, course) |
| 查看详情 | 全教务角色 | 跳 /courses/:courseId | GET .../courses/{courseId} | 只读详情 | 404001 | 不审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「课程库为空」+【新增】【导入】；教师 readonly 横幅 |
| DRAFT/RETURNED | 负责人可编辑/提交 |
| 审核中（学院审→教务审） | 只读+撤回（初审未处理） |
| ENABLED（启用） | 只读；可被方案引用；变更走新版本 |

**【治理】** 脱敏=无；水印=无；审计=增改/提交/导入；进360=否；进统计=课程数/启用率；导入=academic-course 域；导出=P2；移动端=无；跳转=课程审核页、方案课程配置（引用侧）。

**【验收】** 1) 新增重复编号 → 409001；2) 未启用课程在方案课程配置中不可选（后端 422001 拦截兜底）。

### 2.12 课程审核（/courses/:courseId/approve）

**【定位】** 一级=教务中心；二级=课程库；三级=审批页；路由 `/courses/:courseId/approve`；谁能进=学院教务（初审，本院）→教务处（终审启用）；从哪进=统一待办 / 课程列表「待审」tab；用途=两级审核课程，终审通过即启用（ENABLED），方案可引用。

**【展示】** 顶部：课程编号/名称/版本/当前节点。详情字段：全量课程信息+先修课程链+同类课程重复度提示+审批流水。

**【表单】** 审批意见（退回必填）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 初审通过 | 学院教务 + 学院审节点 PENDING | 提交意见+version | POST /api/v1/approvals/tasks/{taskId}/approve | →教务审节点，教务处收 WORKFLOW_TODO | 409001 APPROVAL_VERSION_CONFLICT | record(approve, course) |
| 终审通过并启用 | 教务处 + 教务审节点 PENDING | 二次确认 | 同上 | 课程→ENABLED；负责人收 WORKFLOW_RESULT；方案配置可引用 | 409001 同上 | record(approve, course)+audit_trail |
| 退回 | 当前节点人 + PENDING | 意见必填 | POST .../tasks/{taskId}/reject | →RETURNED；负责人收 RETURNED_NOTICE 可再编辑 | 422001 意见必填 | record(reject, course) |
| 查看引用情况 | 可进者 | 展开被引用方案列表 | GET .../courses/{courseId}/references | 只读列表 | — | 不审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；noPermission=他院初审任务 403002 |
| 学院审节点 | 学院教务可审 |
| 教务审节点 | 教务处可审，学院只读 |
| ENABLED/RETURNED | 整页只读流水；已处理再审 409 弹窗 |

**【治理】** 脱敏=无；水印=无；审计=通过/退回；进360=否；进统计=课程启用率；导入/导出=无；移动端=无（回 PC）；跳转=课程详情、课程编辑页（退回后）、方案课程配置。

**【验收】** 1) 终审通过后课程状态=ENABLED 且立即出现在方案课程配置候选；2) 已启用课程的审批任务再次 approve → 409001。

### 2.13 任务批次列表（/teaching-tasks/batches，含新增/生成要点）

**【定位】** 一级=教务中心；二级=教学任务；三级=列表页（新增 /batches/create、生成 /batches/:batchId/generate 为其下钻）；路由 `/teaching-tasks/batches`；谁能进=教务处（管理）/学院教务/专业负责人（本范围查看与核对入口）；从哪进=菜单「教学任务」；用途=按学期建任务批次→按已发布培养方案生成应开课程→跟踪核对/确认/审核进度。

**【展示】** 顶部：当前学期+批次阶段漏斗（待生成→核对中→教师确认中→待教务审→已审核可排课）。筛选：学期/阶段/学院范围。列表字段：批次名、学期、覆盖范围（学院/专业/年级）、应开课程数、已分配教师数、教师确认率、阶段、操作。新增要点（/batches/create）：学期（须校历 PUBLISHED，前置校验）+覆盖范围必填。生成要点（/batches/:batchId/generate）：按已发布/启用培养方案展开应开课程；缺方案专业列入异常清单，不阻断其他专业。

**【表单】** 批次名/学期/范围必填；生成页无表单（确认动作）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 新增批次 | 教务处 | 跳 /batches/create | POST /api/v1/academic-affairs/teaching-tasks/batches | 新批次（待生成） | 422001 学期校历未发布；409001 同学期同范围批次已存在 | record(create, task_batch) |
| 生成任务 | 教务处 + 批次待生成 | 跳 generate 页确认 | POST .../batches/{batchId}/generate | 生成应开课程清单+缺方案异常清单；批次→核对中；学院教务收 WORKFLOW_TODO | 409001 IDEMPOTENCY_CONFLICT 已生成（重新生成需先清空确认）；422001 范围内无已发布方案 | record(generate, task_batch)+audit_trail |
| 核对 | 学院教务/专业负责人 + 核对中 | 跳 /batches/:batchId/check | — | 进核对页 | 403002 他院 | 核对页内审计 |
| 审核 | 教务处 + 待教务审 | 跳 /batches/:batchId/approve | POST /api/v1/approvals/tasks/{taskId}/approve | 批次→已审核（可排课），进入课表导入/排课 | 409001 APPROVAL_VERSION_CONFLICT | record(approve, task_batch)+audit_trail |
| 统计 | 教务处/学院教务（P2，feature flag 隐藏） | 跳 /teaching-tasks/stats | — | — | — | — |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「本学期暂无任务批次」+【新增批次】（教务处） |
| 待生成 | 可生成/可删批次（教务处） |
| 核对中/教师确认中 | 学院侧操作；教务处只读跟踪 |
| 待教务审 | 教务处可审；学院只读 |
| 已审核（可排课） | 只读；作为排课/课表导入前置 |

**【治理】** 脱敏=无；水印=无；审计=建批次/生成/审核；进360=否；进统计=任务完成率（/stats）；导入=P2 教学任务导入；导出=P2；移动端=教师移动端「授课班级」在教师确认后可见；跳转=任务核对、教师确认、课表导入、学期列表（前置）。

**【验收】** 1) 校历未发布的学期建批次 → 422001；2) 重复点【生成任务】→ 409001 IDEMPOTENCY_CONFLICT，应开课程不重复生成。

### 2.14 任务核对（/teaching-tasks/batches/:batchId/check）

**【定位】** 一级=教务中心；二级=教学任务；三级=批次下钻操作页；路由 `/teaching-tasks/batches/:batchId/check`；谁能进=学院教务/专业负责人（本院/本专业，批次核对中）；从哪进=批次列表【核对】/ 统一待办；用途=核对应开课程/班级/人数，分配任课教师，专业负责人确认后提交，推教师确认，最终提交教务处。

**【展示】** 顶部：批次名+本院核对进度（已核对/总数、未分配教师数、异常数）。筛选：专业/课程/是否已分配教师/异常项。列表字段：课程编号/名称、上课班级、学生人数、周学时、教师（可编辑）、教室类型需求、方案外标记、核对状态、异常提示（容量<班级人数、教师工作量超限）。

**【表单】** 分配教师（检索选择必填）；方案外课程需填说明（必填）；合班/拆班备注。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 分配教师 | 学院教务 + 核对中 | 行内选教师保存 | PUT /api/v1/academic-affairs/teaching-tasks/batches/{batchId}/tasks/{taskId} | 行更新；工作量超限黄色提示（不阻断） | 409001 version；422001 | record(assign_teacher, task) |
| 标记方案外课程 | 学院教务 + 核对中 | 弹窗填说明 | PUT 同上 | 行加「方案外」标记 | 422001 说明必填 | record(update, task) |
| 专业负责人确认 | 专业负责人 + 本专业行已核对 | 批量确认 | POST .../batches/{batchId}/major-confirm | 本专业行→已确认 | 422001 存在未分配教师行 | record(confirm, task_batch) |
| 提交学院意见（推教师确认） | 学院教务 + 全部行已确认 | 二次确认 | POST .../batches/{batchId}/submit-college | 批次→教师确认中；任课教师收 WORKFLOW_TODO（PC/移动端） | 422001 无教师不能提交；409001 重复提交 | record(submit, task_batch)+audit_trail |
| 提交教务处 | 学院教务 + 教师确认完成（异议已清） | 二次确认 | POST .../batches/{batchId}/submit | 批次→待教务审；教务处收 WORKFLOW_TODO | 422001 存在未确认/退回任务 | record(submit, task_batch)+audit_trail |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；noPermission=他院批次 403002 |
| 核对中 | 可编辑分配/标记/确认 |
| 教师确认中 | 列表只读+显示逐师确认状态；教师退回行回「核对中」重新处理 |
| 待教务审/已审核 | 整页只读 |

**【治理】** 脱敏=无；水印=无；审计=分配/确认/提交；进360=否；进统计=任务完成率分子；导入/导出=无；移动端=教师确认可在教师移动端「授课班级」查看（确认动作 V1 在 PC /batches/:batchId/teacher-confirm）；跳转=教师确认页、批次列表、课程详情。

**【验收】** 1) 存在未分配教师课程时【提交学院意见】→ 422001 且定位到行；2) 容量<班级人数仅黄色提示可提交，异常项随批次进入教务审核页展示。

### 2.15 班级课表（/schedule/class/:classId）

**【定位】** 一级=教务中心；二级=课表管理；三级=班级维度课表页；路由 `/schedule/class/:classId`；谁能进=教务处/学院教务（scope）/辅导员（本班）；从哪进=总课表下钻 / 班级一览；用途=按周查看班级已发布课表（V1 只读，数据来自课表导入+发布）。

**【展示】** 顶部：班级名/学期/周次切换器（教学周来自已发布校历，当前周高亮）。主体：周视图网格（行=节次〔读节次配置〕，列=周一~周日），格内=课程名/教师/教室/周次范围。筛选：周次、单双周。详情：点击格子弹出课程详情（课程/教师/教室/节次/起止周/来源批次）。

**【表单】** 无（只读页）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 切换周次 | 可进者 | 拉取该周课表 | GET /api/v1/academic-affairs/schedule/class/{classId}?week= | 网格刷新 | 404001 无该周数据（空态） | 不审计 |
| 课程格详情 | 可进者 | 弹出详情 | GET .../schedule/items/{itemId} | 只读弹窗 | 404001 | 不审计 |
| 返回总课表 | 教务处/学院教务 | 跳 /schedule/overview | — | 返回 | 403001 辅导员无总课表 | 不审计 |
| 导出课表 | P2（/schedule/export，V1 置灰隐藏） | — | — | — | — | — |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「该班级本学期暂无已发布课表」；本页天然 readonly（V1 变更只能走课表重新导入发布，调停课 P2） |
| 课表 DRAFT（未发布） | 本页不展示（仅发布后可见）；教务处可在冲突报告/发布页预览 |
| 已发布 | 只读展示；调停课生效后（P2）格子带变更角标 |

**【治理】** 脱敏=无；水印=P2 导出水印；审计=否（只读）；进360=否；进统计=排课完成率消费端；导入=数据源为 /schedule/import；导出=P2；移动端=学生小程序「我的课表」（/api/v1/mobile/academic-affairs/my/schedule 同源）、教师端「今日课程」；跳转=总课表、教师课表、班级一览。

**【验收】** 1) 课表发布后班级课表与学生小程序「我的课表」同周次数据一致；2) 辅导员访问非本班 classId → 403002。

### 2.16 教师课表（/schedule/teacher/:teacherId）

**【定位】** 一级=教务中心；二级=课表管理；三级=教师维度课表页；路由 `/schedule/teacher/:teacherId`；谁能进=教务处/学院教务（scope 内任意教师）；教师仅看本人（teacherId≠本人 → 403002）；从哪进=总课表下钻 / 教务首页教师视图；用途=按周查看教师已发布课表与周学时汇总。

**【展示】** 顶部：教师姓名/所属学院/本学期周学时合计（超限红色提示）。主体：周视图网格同 2.15，格内=课程/班级/教室。筛选：周次、单双周。详情：格子弹窗（课程/班级/人数/教室/起止周）。

**【表单】** 无（只读页）。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 切换周次 | 可进者 | 拉取该周课表 | GET /api/v1/academic-affairs/schedule/teacher/{teacherId}?week= | 网格刷新 | 403002 教师访问他人课表 | 不审计 |
| 课程格详情 | 可进者 | 弹窗 | GET .../schedule/items/{itemId} | 只读弹窗 | 404001 | 不审计 |
| 查看授课班级名单 | 教师本人/教务角色 | 跳班级课表或名单抽屉 | GET .../schedule/class/{classId} | 联动查看 | 403002 | 名单含学生信息时脱敏 |
| 调停课申请 | P2（教师，feature flag 隐藏） | — | — | — | — | — |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「本学期暂无授课安排」；天然 readonly |
| 已发布 | 只读；周学时与任务核对页超限提示同口径 |

**【治理】** 脱敏=班级名单中学生联系方式脱敏；水印=P2 导出；审计=否；进360=否；进统计=教师工作量口径来源；导入/导出=同 2.15；移动端=教师移动端「今日课程」（同源当日切片）；跳转=班级课表、总课表、任务核对（教务角色排障）。

**【验收】** 1) 教师登录访问他人 teacherId → 403002，本人课表正常；2) 课表发布后教师移动端「今日课程」与本页当日数据一致。

### 2.17 冲突报告（/schedule/batches/:batchId/conflicts）

**【定位】** 一级=教务中心；二级=课表管理；三级=批次下钻报告页；路由 `/schedule/batches/:batchId/conflicts`；谁能进=教务处/学院教务（本院冲突）；从哪进=课表导入结果页 / 排课批次（P2）；用途=展示导入/排课后的规则校验冲突，处理清零后才能发布课表。

**【展示】** 顶部：批次名+冲突汇总（阻断级 N / 提示级 M / 已处理 K）。筛选：冲突类型（教师冲突/班级冲突/教室冲突/课程时间冲突/容量不足/教室类型不匹配/校区时间不合理/教师不可用/周学时不满足）/级别/学院/处理状态。列表字段：冲突类型、级别（阻断/提示）、涉及课程、班级、教师、教室、时间描述、处理状态、处理人/意见。

**【表单】** 标记处理：处理方式+说明（必填）；忽略（仅提示级）：理由必填。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 重新检测 | 教务处/学院教务 + 课表未发布 | 全量重跑规则校验 | POST /api/v1/academic-affairs/schedule/batches/{batchId}/conflicts/recheck | 报告刷新（防连点提交锁） | 409001 检测进行中 | record(recheck, schedule_batch) |
| 标记已处理 | 同上 + 行未处理 | 弹窗填处理说明 | POST .../conflicts/{conflictId}/resolve | 行→已处理 | 422001 说明必填；409001 已被他人处理 | record(resolve, schedule_conflict) |
| 忽略（提示级） | 同上 + 行=提示级 | 理由必填 | POST .../conflicts/{conflictId}/ignore | 行→已忽略（留痕） | 403001 阻断级不可忽略 | record(ignore, schedule_conflict) |
| 导出冲突清单 | 教务处/学院教务 | 用途弹窗后导出 | POST /api/v1/export/domain/academic-schedule-conflict | 水印文件+留痕 | 429001/422001 | t_export_task |
| 去发布 | 教务处 + 阻断级冲突=0 | 跳 /schedule/batches/:batchId/publish | POST .../schedule/batches/{batchId}/publish | 课表 DRAFT→PUBLISHED；师生收 PUBLISHED_NOTICE；班级/教师/学生端课表可见 | 422001 仍存在未处理阻断冲突；409001 已发布 | record(publish, schedule_batch)+audit_trail |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「本批次未检出冲突」+【去发布】（教务处） |
| 课表 DRAFT | 可检测/处理/忽略/发布 |
| 课表 PUBLISHED | 报告只读留档；变更走调停课（P2）或新批次导入 |

**【治理】** 脱敏=无；水印=导出水印；审计=处理/忽略/发布全审计；进360=否；进统计=排课冲突数（/stats）；导入=上游 /schedule/import（dry-run 即产生本报告）；导出=冲突清单；移动端=无；跳转=课表导入、课表发布、班级/教师课表（定位冲突现场）。

**【验收】** 1) 存在阻断级冲突时点【去发布】→ 422001 课表仍 DRAFT；2) 冲突清零发布后学生/教师端课表可见，重复发布 → 409001。

### 2.18 学生成绩单（/grades/transcript/:studentId）

**【定位】** 一级=教务中心；二级=成绩管理（V1 仅成绩单查询+统计）；三级=学生维度汇总页；路由 `/grades/transcript/:studentId`；谁能进=教务处（全校）/学院教务（本院）/辅导员（本班），scope 校验（can_teacher_view_student 同源机制）；从哪进=学籍档案详情【成绩单】/ 预警处置页佐证；用途=按学期汇总学生全部成绩（V1 读既有 t_acad_grade），支撑预警处置与毕业预审佐证。

**【展示】** 顶部：学生学号/姓名/班级/GPA/已获学分/挂科门数（汇总条）。筛选：学期/课程性质/通过状态。列表字段（按学期分组）：课程编号、课程名、性质、学分、成绩、绩点、通过状态（pass_status）、考试性质（初修/补考/重修标记，读扩展字段）。详情字段：无更深层（成绩构成明细属 P2 录入侧）。

**【表单】** 无。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 学期筛选 | 可进者 | 过滤成绩 | 复用 GET /api/v1/academic/grades（复用既有 academic 学业过程模块）?studentId=&termId= | 列表刷新 | 403002 越权学生 | 不审计 |
| 导出成绩单 | 教务处/学院教务 | 用途弹窗（≥5 字） | POST /api/v1/export/domain/academic-transcript | 带水印 PDF/Excel+t_export_task | 429001 限流；422001 用途不足 | t_export_task+下载审计 |
| 查看学业预警 | 可进者且该生有预警 | 跳 /warnings?studentId= | 复用 GET /api/v1/academic/warnings（复用既有 academic 学业过程模块） | 预警列表过滤该生 | 403002 | 不审计 |
| 查看学籍档案 | 可进者 | 跳 /roll/students/:studentId | — | 联动查看 | 403002 | 档案页内审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「暂无已发布成绩」；本页天然 readonly（成绩变更走 P2 成绩更正流程，不在本页改） |
| 成绩 PUBLISHED | 正常展示（V1 数据源即既有已发布成绩） |
| 成绩 CHANGE_REVIEW/更正后（P2） | 更正记录保留原值，本页显示最新值+「已更正」角标 |
| ARCHIVED | 只读可导出 |

**【治理】** 脱敏=页面含学生基础信息按规则脱敏；水印=导出水印必带；审计=导出+完整查看；进360=否（成绩事件由发布流程写入）；进统计=挂科率（/grades/stats）；导入=P2 成绩导入；导出=academic-transcript 域；移动端=学生小程序「我的成绩」（只读同源）；跳转=预警列表、学籍档案、毕业预审详情。

**【验收】** 1) 辅导员查看非本班学生成绩单 → 403002 并记审计；2) 导出成绩单必填用途且文件带水印、t_export_task 留痕，1 分钟内第 6 次导出 → 429001。

### 2.19 学业预警列表（/warnings）

**【定位】** 一级=教务中心；二级=学业预警；三级=列表页；路由 `/warnings`；谁能进=教务处（全校）/学院教务（本院）/辅导员（本班，scope 过滤）；从哪进=菜单「学业预警」/ 教务首页卡片；用途=复用既有 t_acad_warning 数据源的预警工作列表（扩展规则来源），分派与处置入口。与现有 /admin/academic 预警页共用数据，不新建预警表。

**【展示】** 顶部：待处理/处理中/已升级计数卡。筛选：状态（PENDING_HANDLE/PROCESSING/ESCALATED/CLOSED）/来源（挂科/低绩点/学分不足/缺考/重修过多/未注册/毕业资格异常等）/等级/学院/班级/学期/关键词。列表字段：学生（学号/姓名/班级）、预警来源、等级、命中规则摘要、生成时间、当前状态、责任人（辅导员）、最近处置时间。

**【表单】** 指派：选择责任人必填；批量提醒：提醒内容模板。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 处置 | 辅导员/学院教务（scope）+ 非 CLOSED | 跳 /warnings/:warningId/handle | — | 进处置页 | 403002 非本班/本院 | 处置页内审计 |
| 指派 | 教务处/学院教务 + PENDING_HANDLE | 弹窗选责任人 | 复用 POST /api/v1/academic/warnings（复用既有 academic 学业过程模块）/{id}/assign | 责任人更新+t_unified_todo+RISK_ALERT | 403002 被指派人无该生范围 | record(assign, warning) |
| 批量提醒 | 教务处/学院教务 | 勾选行发提醒 | 复用 POST .../warnings/{id}/remind | 责任人收 DEADLINE_REMINDER | 429001 频繁提醒限流 | record(remind, warning) |
| 规则配置 | 教务处 | 跳 /warnings/rules（接平台规则中心 safe_rule） | 平台规则中心接口 | 规则更新，下次扫描生效 | 403001 | 平台侧审计 |
| 统计 | 教务处/学院教务 | 跳 /warnings/stats | 复用 warnings 聚合 | 打开统计 | 403001 | 不审计 |
| 导出 | 教务处/学院教务 | 用途弹窗后导出 | POST /api/v1/export/domain/academic-warning | 水印文件+留痕 | 429001/422001 | t_export_task |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「当前范围无学业预警」；辅导员仅见本班（scope 过滤=能看见即能处置） |
| PENDING_HANDLE | 红色徽标，可处置/指派/提醒 |
| PROCESSING | 黄色，可续处置/升级 |
| ESCALATED | 橙色，学院教务/教务处跟进 |
| CLOSED | 灰色只读，可查处置记录 |

**【治理】** 脱敏=学生联系方式脱敏；水印=导出水印；审计=指派/提醒/导出；进360=处置闭环写入（见 2.20）；进统计=预警人数（/warnings/stats、/stats）；导入=无；导出=预警清单；移动端=学生小程序「我的学业预警」（本人只读）、教师移动端「学业预警跟进」（复用既有处理端点）；跳转=预警处置、成绩单、规则配置、现有 /admin/academic 预警页（同数据源互链）。

**【验收】** 1) 辅导员列表仅出现本班预警，直连他班 warningId 处置页 → 403002+审计；2) 成绩发布（或规则扫描）命中后列表新增 PENDING_HANDLE 记录且责任辅导员收到待办。

### 2.20 预警处置（/warnings/:warningId/handle）

**【定位】** 一级=教务中心；二级=学业预警；三级=处置操作页；路由 `/warnings/:warningId/handle`；谁能进=辅导员/学院教务（scope 校验，复用既有处置 API）；从哪进=预警列表【处置】/ 统一待办；用途=记录处置过程，联动谈话/家校，升级或关闭预警并沉淀 360。

**【展示】** 顶部：学生信息卡（脱敏）+预警来源/等级/命中规则明细。主体：处置记录时间线（历次处置人/方式/内容）+关联动作区。详情字段：命中数据快照（如挂科课程清单，链至成绩单）。

**【表单】** 处置记录：处置方式（枚举=电话联系/当面谈话/转谈心谈话/转家校联系/学业帮扶）+内容（长度规则见字段文档）必填；升级：升级原因必填；关闭：处置结论必填。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 提交处置记录 | 责任人/学院教务 + PENDING_HANDLE/PROCESSING | 表单提交（提交锁） | 复用既有预警处置端点（含移动端同源） | 状态→PROCESSING；时间线追加 | 422001 内容必填；403002 越权 | record(handle, warning) |
| 转谈心谈话 | 同上 | 联动学工谈话（带学生与预警上下文） | 学工谈话创建接口（13A） | 生成谈话计划待办；本页留联动记录 | 403002 | record(transfer_talk, warning) |
| 转家校联系 | 同上 | 联动家校模块 | 学工家校接口（13A） | 家校联系待办生成 | 403002 | record(transfer_family, warning) |
| 升级 | 同上 + 非 CLOSED | 原因必填 | 复用 POST /api/v1/academic/warnings（复用既有 academic 学业过程模块）/{id}/escalate | →ESCALATED；学院教务/教务处收 RISK_ALERT | 422001 原因必填；409001 已关闭 | record(escalate, warning) |
| 关闭 | 责任人/学院教务 + PROCESSING/ESCALATED | 结论必填二次确认 | 复用 POST .../warnings/{id}/close | →CLOSED；写学生 360 时间线（StageEvent/域记录）；学生收 STATUS_CHANGED | 409001 已关闭再关 | record(close, warning)+audit_trail |
| 查看成绩单 | scope 命中 | 跳 /grades/transcript/:studentId | — | 佐证查看 | 403002 | 成绩单页内审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；noPermission=非责任范围 403002 |
| PENDING_HANDLE/PROCESSING | 可全量操作 |
| ESCALATED | 升级后原辅导员可续记，关闭权限在学院教务及以上 |
| CLOSED | 整页只读时间线；再次提交处置 → 409001 |

**【治理】** 脱敏=学生联系方式脱敏（家校联动看完整号码走 13A 审计流程）；水印=无；审计=每个动作 record；进360=关闭时写时间线；进统计=处置率/关闭率；导入/导出=无；移动端=教师移动端「学业预警跟进」同端点操作；跳转=成绩单、学工谈话/家校（13A）、预警列表。

**【验收】** 1) 已 CLOSED 预警再次提交处置/关闭 → 409001 页面转只读；2) 关闭后学生 360 时间线出现预警闭环事件，统计计数同步。

### 2.21 毕业资格批次列表（/graduation/batches，含新增要点）

**【定位】** 一级=教务中心；二级=毕业资格审核；三级=列表页（新增 /graduation/batches/create 下钻）；路由 `/graduation/batches`；谁能进=教务处（管理，全校）/学院教务（本院查看与初审入口）；从哪进=菜单「毕业资格」；用途=按届别建审核批次，创建即生成应审学生并触发系统预审，跟踪初审/终审/导出全程。

**【展示】** 顶部：当前届别批次进度（应审人数/系统通过/系统异常/已初审/已终审）。筛选：届别/学期/批次状态。列表字段：批次名、届别、覆盖范围、应审学生数、SYSTEM_PASSED 数、SYSTEM_ABNORMAL 数、初审进度、终审进度、操作。新增要点（/graduation/batches/create）：届别+范围（学院/专业可多选）必填；【创建并生成应审学生】即拉取范围内应届生并逐生系统预审（学籍/方案学分/必修/选修/实习/毕设/处分/材料/就业，各模块供数），学生初始状态 WAIT_PRECHECK→SYSTEM_PASSED/SYSTEM_ABNORMAL。

**【表单】** 批次名/届别/范围必填；重复届别范围拦截。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 新增批次 | 教务处 | 跳 create，创建并生成应审学生 | POST /api/v1/academic-affairs/graduation-audit/batches | 新批次+逐生预审异步执行（进度条） | 409001 同届别同范围批次已存在；422001 范围为空 | record(create, graduation_batch)+audit_trail |
| 预审结果 | 教务处/学院教务（本院） | 跳 /graduation/batches/:batchId/precheck | GET .../batches/{batchId}/precheck-results | 进结果列表 | 403002 他院 | 结果页内审计 |
| 学院初审 | 学院教务 + 批次预审完成 | 跳 /batches/:batchId/college-review | POST /api/v1/approvals/tasks/{taskId}/approve（逐生/批量） | 学生 SYSTEM_*→COLLEGE_REVIEW 流转 | 409001 APPROVAL_VERSION_CONFLICT | record(review, graduation)+workflow |
| 教务终审 | 教务处 + 学院初审完成 | 跳 /batches/:batchId/final-review | 同上 | 逐生定 GRADUATED/COMPLETED/DELAYED；学生端「毕业资格进度」更新；写 360 归档 | 409001 同上 | record(final_review, graduation)+audit_trail |
| 名单导出 | 教务处 + 终审完成 | 跳 /batches/:batchId/export（用途≥5 字） | POST /api/v1/export/domain/graduation-list | 毕业/结业/延毕名单水印文件+t_export_task | 429001；422001 用途不足 | t_export_task+下载审计 |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty=「暂无审核批次」+【新增批次】（教务处） |
| 预审执行中 | 批次行进度条，禁止重复触发（409001 IDEMPOTENCY_CONFLICT） |
| 预审完成 | 学院可初审、可处理异常 |
| 初审/终审中 | 按节点只读/可审 |
| 终审完成/ARCHIVED | 只读可导出；360 归档完成 |

**【治理】** 脱敏=学生信息脱敏；水印=名单导出必带；审计=建批次/初审/终审/导出；进360=终审结论写 StageEvent 归档；进统计=毕业资格通过率（/stats）；导入=无；导出=graduation-list 域；移动端=学生小程序「毕业资格进度」实时读逐条件状态；跳转=预审结果、异常处理、初审/终审页、名单导出。

**【验收】** 1) 同届别重复建批次 → 409001；2) 终审定 DELAYED 后学生小程序「毕业资格进度」显示延毕结论与未达条件，360 出现归档事件。

### 2.22 预审结果列表（/graduation/batches/:batchId/precheck）与异常处理（/graduation/batches/:batchId/exceptions）

**【定位】** 一级=教务中心；二级=毕业资格审核；三级=批次下钻双页（结果列表+异常处理 tab）；路由 `/graduation/batches/:batchId/precheck`、`/graduation/batches/:batchId/exceptions`；谁能进=教务处/学院教务（本院）；异常处理页另含辅导员（本班，补充说明）；从哪进=批次列表【预审结果】/ 预审结果「异常」tab / 统一待办；用途=查看逐生逐条件红绿灯结果，对 SYSTEM_ABNORMAL 学生逐项异常联动责任模块处理并复审。

**【展示】** 顶部：批次名+通过/异常数+条件维度异常分布（学分缺口 N/实习未完成 N/处分未解除 N/就业未填报 N…）。筛选：预审状态（WAIT_PRECHECK/SYSTEM_PASSED/SYSTEM_ABNORMAL）/异常条件类型/学院/专业/班级。列表字段：学生（学号/姓名/班级）、九项条件红绿灯（学籍/学分/必修/选修/实习/毕设/处分/材料/就业）、预审状态、异常项数、处理进度。异常处理页字段：异常项、责任模块、当前值/要求值、联动状态、辅导员补充说明、解决状态。详情下钻：/graduation/batches/:batchId/students/:studentId 逐条件明细。

**【表单】** 辅导员补充说明（必填≥10 字，见字段文档）；标记已解决需附依据说明。

**【按钮动作矩阵】**

| 按钮 | 出现条件(角色+状态) | 动作 | API | 成功变化 | 失败提示 | 审计 |
|---|---|---|---|---|---|---|
| 重新预审（单生/批量） | 教务处/学院教务 + 批次未终审 | 二次确认（提交锁） | POST /api/v1/academic-affairs/graduation-audit/batches/{batchId}/precheck | 重跑各模块供数，红绿灯刷新；SYSTEM_ABNORMAL→SYSTEM_PASSED 自动更新 | 409001 预审执行中；422001 批次已终审 | record(recheck, graduation) |
| 学生预审详情 | scope 命中 | 跳 /students/:studentId 明细 | GET .../batches/{batchId}/students/{studentId} | 逐条件明细+佐证链接 | 403002 他院/他班 | 敏感项查看审计 |
| 异常联动处理 | 学院教务 + 行=SYSTEM_ABNORMAL | 按异常项跳责任模块：学分缺口→补考重修（P2 提示回既有 t_acad_makeup/retake 处理）；实习未完成→实习模块；处分未解除→学工处分页；毕设未通过→毕设模块 | 各模块既有接口 | 责任模块处理后回本页重审 | 403002 目标模块无权限 | 各模块内审计+本页联动留痕 |
| 通知辅导员补充 | 学院教务 + 异常项=就业未填报/材料缺失 | 生成待办 | 写 t_unified_todo + WORKFLOW_TODO | 辅导员收待办，可在异常页补充说明 | 422001 通知对象为空 | record(notify, graduation_exception) |
| 补充说明 | 辅导员（本班）+ 异常未解决 | 表单提交 | POST .../exceptions/{exceptionId}/comment | 说明附于异常项，供初审参考 | 422001 说明长度不足；403002 非本班 | record(comment, graduation_exception) |
| 标记已解决并重审 | 学院教务 + 异常项联动已完成 | 依据必填+触发单生重审 | POST .../exceptions/{exceptionId}/resolve | 异常项闭合；该生重跑预审 | 422001 依据必填；409001 已解决 | record(resolve, graduation_exception)+audit_trail |
| 导出异常清单 | 教务处/学院教务 | 用途弹窗后导出 | POST /api/v1/export/domain/graduation-exception | 水印文件+留痕 | 429001/422001 | t_export_task |

**【状态视图】**

| 状态 | 展示与可操作 |
|---|---|
| 五态 | 按 §1.3；empty（异常 tab）=「无异常学生」+【进入学院初审】引导 |
| WAIT_PRECHECK | 灰色执行中，仅可等待/重跑 |
| SYSTEM_PASSED | 绿色只读，直接进初审队列 |
| SYSTEM_ABNORMAL | 红色，可联动处理/补充/重审 |
| COLLEGE_REVIEW/ACADEMIC_REVIEW 后 | 本页转只读（异常处理冻结，变化走终审暂缓 DELAYED） |
| ARCHIVED | 只读可导出 |

**【治理】** 脱敏=处分/家庭相关佐证仅摘要展示（明细回学工强权限页）；水印=导出水印；审计=重审/解决/补充/导出；进360=否（终审归档统一写）；进统计=毕业资格异常数/通过率；导入=无；导出=异常清单；移动端=学生小程序「毕业资格进度」逐条件同源展示、辅导员待办走教师移动端；跳转=补考重修（P2）、实习、毕设、学工处分、就业、学院初审页。

**【验收】** 1) 学分缺口学生经补考成绩更新后【重新预审】→ 红绿灯转绿、状态自动 SYSTEM_PASSED；2) 辅导员对非本班学生异常提交补充说明 → 403002；已解决异常重复 resolve → 409001。

---

## 三、非核心页精简矩阵（未逐页展开的其余页面）

> 路由省略前缀 `/admin/academic-affairs`；API 省略前缀 `/api/v1`。审批类页一律遵循 §1.4 通用交互；导入导出一律走统一管线（§1.5）。

### 3.1 V1 非核心页（随核心页同期交付）

| 页面 | 路由 | 主要按钮 | 关键 API | 核心状态 | 版本 |
|---|---|---|---|---|---|
| 学年学期新增 | /terms/create | 保存/保存并配校历 | POST /academic-affairs/terms | 新建即 DRAFT | ★V1 |
| 学年学期编辑 | /terms/:termId/edit | 保存/取消 | PUT /academic-affairs/terms/{id} | 仅 DRAFT 可编（409 version） | ★V1 |
| 校历发布 | /terms/:termId/publish | 发布确认/返回修改 | POST /academic-affairs/terms/{id}/publish | DRAFT→PUBLISHED（422 未配全） | ★V1 |
| 学籍档案详情 | /roll/students/:studentId | 成绩单/代录异动/看完整证件(审计) | 复用 GET /api/v1/academic/students/{id}（复用既有 academic 学业过程模块） | 随 student_status 只读展示 | ★V1 |
| 入学注册批次 | /roll/registration | 新增批次/核对/进度 | GET·POST /academic-affairs/roll-registrations | 核对中→待审→已确认 | ★V1 |
| 异动申请详情 | /roll/changes/:changeId | 去审批/撤回(首节点未处理)/查看流水 | GET /academic-affairs/roll-changes/{id} | 在途只读+撤回；终态只读 | ★V1 |
| 异动代录新增 | /roll/changes/create | 提交/存草稿 | POST /academic-affairs/roll-changes | 提交即入首节点（在途 409） | ★V1 |
| 异动统计 | /roll/changes/stats | 维度切换/下钻/导出 | GET /academic-affairs/roll-changes/stats | 只读统计 | ★V1 |
| 专业一览 | /majors | 查看方案绑定/跳班级 | 复用组织表读接口 | 只读复用 t_major | ★V1 |
| 班级一览 | /classes | 跳班级课表/学生列表 | 复用组织表读接口 | 只读复用 t_class | ★V1 |
| 方案列表 | /programs | 新增/编制/待审 tab/发布入口 | GET /academic-affairs/programs | DRAFT…DISABLED 全态徽标 | ★V1 |
| 方案新增 | /programs/create | 创建编制任务 | POST /academic-affairs/programs | 新建即 DRAFT | ★V1 |
| 方案详情 | /programs/:programId | 编制/审核/发布/新建版本 | GET /academic-affairs/programs/{id} | 全角色只读视图 | ★V1 |
| 方案发布 | /programs/:programId/publish | 绑定专业年级+发布 | POST /academic-affairs/programs/{id}/publish | ACADEMIC_REVIEW 通过→PUBLISHED/ENABLED（422 未绑定） | ★V1 |
| 课程新增 | /courses/create | 保存/提交审核 | POST /academic-affairs/courses | 新建 DRAFT（编号 409） | ★V1 |
| 课程详情 | /courses/:courseId | 编辑/提交/引用查看 | GET /academic-affairs/courses/{id} | 只读+状态徽标 | ★V1 |
| 课程编辑 | /courses/:courseId/edit | 保存/提交审核 | PUT /academic-affairs/courses/{id} | 仅 DRAFT/RETURNED | ★V1 |
| 课程库导入 | /courses/import | 下载模板/dry-run 校验/确认导入 | /import/domain/academic-course/validate\|confirm | 行级错误→整批事务 | ★V1 |
| 教师确认 | /teaching-tasks/batches/:batchId/teacher-confirm | 确认任务/有异议退回(理由必填) | POST /academic-affairs/teaching-tasks/.../teacher-confirm\|return | 教师确认中→已确认/退回行 | ★V1 |
| 任务审核 | /teaching-tasks/batches/:batchId/approve | 审核通过/退回学院 | POST /approvals/tasks/{id}/approve\|reject | 待教务审→可排课（409 版本冲突） | ★V1 |
| 课表导入 | /schedule/import | 下载模板/dry-run/确认导入/看冲突 | /import/domain/academic-schedule/validate\|confirm | dry-run 生成冲突报告→DRAFT 课表 | ★V1 |
| 总课表 | /schedule/overview | 维度切换(班级/教师)/下钻/周次 | GET /academic-affairs/schedule/overview | 只读（PUBLISHED 数据） | ★V1 |
| 课表发布 | /schedule/batches/:batchId/publish | 发布确认/返回冲突报告 | POST /academic-affairs/schedule/batches/{id}/publish | DRAFT→PUBLISHED（422 有阻断冲突） | ★V1 |
| 成绩统计 | /grades/stats | 口径切换(挂科率/录入率/发布率)/下钻/导出 | 复用 /api/v1/academic/grades 聚合 + stats 扩展（复用既有 academic 学业过程模块） | 只读统计（V1 基础口径） | ★V1 |
| 预警规则配置 | /warnings/rules | 启停规则/调阈值/保存 | 平台规则中心 safe_rule 接口 | 规则生效于下次扫描（403 非教务处） | ★V1 |
| 预警统计 | /warnings/stats | 维度切换/下钻/导出 | 复用 /academic/warnings 聚合 | 只读统计 | ★V1 |
| 学生预审详情 | /graduation/batches/:batchId/students/:studentId | 逐条件明细/佐证跳转/重审 | GET /academic-affairs/graduation-audit/.../students/{id} | 九条件红绿灯只读+联动 | ★V1 |
| 学院初审 | /graduation/batches/:batchId/college-review | 逐生初审通过/暂缓(理由必填)/批量 | POST /approvals/tasks/{id}/approve\|reject | SYSTEM_*→COLLEGE_REVIEW（409 已审） | ★V1 |
| 教务终审 | /graduation/batches/:batchId/final-review | 定毕业/结业/延毕/批量终审 | POST /approvals/tasks/{id}/approve | →GRADUATED/COMPLETED/DELAYED+360 归档 | ★V1 |
| 名单导出 | /graduation/batches/:batchId/export | 选名单类型/填用途(≥5字)/导出 | POST /export/domain/graduation-list | 水印+t_export_task（429 限流） | ★V1 |
| 教务统计总览 | /stats | 指标卡下钻/学期切换/导出 | /stats/* 扩展（scope 过滤） | 只读统计（注册率/任务/冲突/挂科/预警/毕业） | ★V1 |

### 3.2 P2/P3 后置页

| 页面 | 路由 | 主要按钮 | 关键 API | 核心状态 | 版本 |
|---|---|---|---|---|---|
| 校历归档查看 | /terms/:termId/archive | 导出 | GET /academic-affairs/terms/{id} | ARCHIVED 只读 | ☆P2 |
| 学年注册 | /roll/annual-registration | 生成名单/核对/确认 | /academic/roll-registrations（年度型） | 同入学注册态 | ☆P2 |
| 方案版本对比 | /programs/:programId/versions/compare | 选两版本/差异高亮/导出 | GET /academic-affairs/programs/{id}/versions | 只读对比 | ☆P2 |
| 任务统计 | /teaching-tasks/stats | 维度切换/导出 | GET /academic-affairs/teaching-tasks/stats | 只读统计 | ☆P2 |
| 排课批次 | /schedule/batches | 建批次/载资源/排课/冲突/发布 | /academic/schedule/batches | 草稿→预发布→发布 | ☆P2 |
| 手工排课工作台 | /schedule/batches/:batchId/edit | 拖拽排格/实时冲突/保存 | PUT /academic-affairs/schedule/batches/{id}/items | DRAFT 可编 | ☆P2 |
| 教室课表 | /schedule/room/:roomId | 周次切换/借用入口 | GET /academic-affairs/schedule/room/{id} | 只读 | ☆P2 |
| 课表导出 | /schedule/export | 范围选择/用途/导出 | POST /export/domain/academic-schedule | 水印留痕 | ☆P2 |
| 调停课申请列表 | /course-adjustments | 新增/审批入口/记录 | GET /academic-affairs/course-adjustments | SUBMITTED…APPLIED | ☆P2 |
| 调停课申请新增 | /course-adjustments/create | 选原课/调停补/冲突检查/提交 | POST /academic-affairs/course-adjustments | 提交即 SUBMITTED（冲突 422） | ☆P2 |
| 调停课审批 | /course-adjustments/:adjustId/approve | 学院审/教务审/驳回 | /approvals/tasks/{id}/* | COLLEGE_REVIEW→ACADEMIC_REVIEW→APPROVED→APPLIED | ☆P2 |
| 调停课记录 | /course-adjustments/records | 筛选/导出 | GET .../records | APPLIED 留痕只读 | ☆P2 |
| 选课批次列表 | /enrollment/batches | 建批次/配置/发布/锁定 | /academic/enrollments/batches | 配置→开放→锁定 | ☆P2 |
| 选课批次配置 | /enrollment/batches/:batchId/config | 时间/范围/容量/规则保存 | PUT .../config | 未开放可编 | ☆P2 |
| 选课监控 | /enrollment/batches/:batchId/monitor | 进度刷新/低人数处理/锁定 | GET .../monitor | 开放中实时 | ☆P2 |
| 选课名单导出 | /enrollment/batches/:batchId/export | 用途/导出 | POST /export/domain/enrollment-list | 水印留痕 | ☆P2 |
| 考试批次列表 | /exams/batches | 建批次/生成考试课程/发布 | /academic/exams/batches | 草稿→确认→发布 | ☆P2 |
| 考试课程确认 | /exams/batches/:batchId/courses | 学院确认/剔除说明 | POST .../courses/confirm | 确认前可编 | ☆P2 |
| 考场与座位 | /exams/batches/:batchId/rooms | 排考场/生成座位 | PUT .../rooms | 发布前可编 | ☆P2 |
| 监考安排 | /exams/batches/:batchId/invigilation | 分配监考/冲突检查 | PUT .../invigilation | 发布前可编 | ☆P2 |
| 考试发布 | /exams/batches/:batchId/publish | 发布确认 | POST .../publish | →PUBLISHED 师生可见 | ☆P2 |
| 考试记录 | /exams/records | 标缺考/违纪/缓考 | POST /academic-affairs/exams/records | 考后登记 append | ☆P2 |
| 缓考申请列表 | /exams/deferrals | 查看/审批入口 | GET /academic-affairs/exams/deferrals | 四级审批在途态 | ☆P2 |
| 缓考审批 | /exams/deferrals/:deferralId/approve | 辅导员→教师→学院→教务逐级审 | /approvals/tasks/{id}/* | 终审→标记缓考（409 防重） | ☆P2 |
| 成绩任务列表 | /grades/tasks | 生成任务/录入/审核入口 | GET /academic-affairs/grade-tasks | NOT_STARTED…PUBLISHED | ☆P2 |
| 成绩录入 | /grades/tasks/:taskId/input | 录入/构成配置/暂存/提交 | PUT·POST .../input\|submit | INPUTTING→SUBMITTED（422 超范围） | ☆P2 |
| 成绩审核 | /grades/tasks/:taskId/review | 完整性通过/退回 | /approvals/tasks/{id}/* | SUBMITTED→COLLEGE_REVIEW/RETURNED | ☆P2 |
| 成绩发布 | /grades/publish | 勾选课程/发布 | POST /academic-affairs/grade-tasks/publish | →PUBLISHED 写 t_acad_grade+触发预警扫描 | ☆P2 |
| 成绩更正申请 | /grades/corrections | 发起更正(原/新值+材料) | POST /academic-affairs/grade-change | PUBLISHED→CHANGE_REVIEW | ☆P2 |
| 成绩更正审核 | /grades/corrections/:correctionId/approve | 学院审→教务审 | /approvals/tasks/{id}/* | 通过→更新并保留原值 | ☆P2 |
| 补考名单 | /makeup/list | 生成名单/导出 | 扩展既有 t_acad_makeup 端点 | 发布后识别不及格 | ☆P2 |
| 补考批次安排 | /makeup/batches | 建批次/安排/录成绩 | 同上 | 安排→已考→已更新成绩 | ☆P2 |
| 重修报名列表 | /retake/applications | 查看/审核入口 | 扩展既有 t_acad_retake 端点 | 报名→审核→入选课 | ☆P2 |
| 重修审核 | /retake/applications/:applicationId/approve | 通过/驳回 | /approvals/tasks/{id}/* | 通过入重修选课 | ☆P2 |
| 免修申请列表 | /exemptions | 查看/审批入口 | /academic/exemptions | 教师→学院→教务三级 | ☆P2 |
| 免修审批 | /exemptions/:exemptionId/approve | 逐级审/驳回 | /approvals/tasks/{id}/* | 通过→免修记录+进度标记 | ☆P2 |
| 教材目录 | /textbooks | 维护目录/绑课程 | /academic/textbooks | 台账 | ☆P2/P3 |
| 教材征订 | /textbooks/orders | 征订/汇总 | .../orders | 征订→汇总→到货 | ☆P2/P3 |
| 教材发放 | /textbooks/distribution | 按班发放/登记 | .../distribution | 发放留痕 | ☆P2/P3 |
| 教室列表 | /rooms | 维护教室/容量/设备 | /academic/rooms | 台账 | ☆P2 |
| 教室借用申请 | /rooms/borrow | 发起借用/冲突检查 | POST .../borrow | 申请→审批 | ☆P2 |
| 借用审批 | /rooms/borrow/:borrowId/approve | 通过/驳回 | /approvals/tasks/{id}/* | 通过占用时段 | ☆P2 |
| 评教任务 | /evaluation/tasks | 建任务/发布 | /academic/evaluation/tasks | 草稿→开放→结束 | ☆P2/P3 |
| 问卷配置 | /evaluation/questionnaires | 编题/启用 | .../questionnaires | 启用前可编 | ☆P2/P3 |
| 评价结果统计 | /evaluation/stats | 维度统计/导出 | .../stats | 只读 | ☆P2/P3 |
| 等级考试报名 | /level-exams | 建考次/报名管理 | /academic/level-exams | 报名→考试→出分 | ☆P3 |
| 等级考试成绩登记 | /level-exams/scores | 导入/登记成绩 | .../scores + 导入管线 | 登记留痕 | ☆P3 |
| 学期开课计划 | /teaching-plans | 由方案生成/调整/确认 | /academic/teaching-plans | 草稿→确认（衔接任务） | ☆P2 |
| 归档批次 | /archive | 建归档批次/生成包 | /academic/archive | 生成→确认→归档 | ☆P2 |
| 归档包详情 | /archive/:archiveId | 查看/水印导出 | GET .../archive/{id} | ARCHIVED 只读 | ☆P2 |

---

## 四、本文档验收自查

1. V1 核心 22 页逐页展开，页名/路由与《13B-教务中心页面树与路由设计.md》速览表逐一对齐，未自创路由；含要点合并（异动详情、方案课程配置、任务新增/生成、毕业批次新增、异常处理）共覆盖 V1 页面 28 个，其余 V1 页 31 个入 §3.1 精简矩阵，P2/P3 页 50 个入 §3.2。
2. 每页按钮矩阵列固定为「按钮/出现条件/动作/API/成功变化/失败提示/审计」，失败提示均对应 §1.2 错误码语义；审批页统一遵循 §1.4（version 乐观锁、409 APPROVAL_VERSION_CONFLICT、提交锁）。
3. 状态机与《_13-需求输入-V1.1.md》§3 一致：校历 DRAFT/PUBLISHED/FROZEN/ARCHIVED、方案 DRAFT→…→DISABLED、成绩 NOT_STARTED→…→ARCHIVED、毕业 WAIT_PRECHECK→…→ARCHIVED、预警复用既有 PENDING_HANDLE/PROCESSING/ESCALATED/CLOSED。
4. 集成事实对齐：审批复用 Workflow 双表、待办/消息只写 t_unified_todo/t_unified_message、审计 record()+域级 audit_trail、导入导出复用统一管线与 t_export_task、demo-school 只读 403 引导 sandbox-school、成绩/预警读写既有 t_acad_* 表。
