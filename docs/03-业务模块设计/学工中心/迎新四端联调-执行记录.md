# 迎新与住宿四端联调记录

## 2026-09-22：教师电脑端与学生小程序迎新重构

本节为最新执行事实，下文 2026-09-06 记录保留为历史证据。本轮尚未达到完整业务验收通过；不能以构建或接口测试代替缺失的页面办理。

### 范围与实际环境

- 任务：审计迎新业务、压缩菜单、打通教师电脑端与学生小程序的同一学生办理链。沿现有工程施工，未改认证核心、未放开权限。
- 开始及收尾基线均为 `main` / `b7cfdb74987e19f8dad407ff830622ef5fcd2834`；所有本轮修改仍在工作区，未提交、推送、合并或生产发布。原有 `RELEASE_RUNBOOK.md` 未由本轮修改。
- 日常沙箱已重新核验：`student-lifecycle-v8-mysql`，端口 `3307`，数据库 `student_lifecycle_runtime_20260902`，学校 `sandbox-school`，租户 `1000000000000000007`，迁移 `20260914_aa_opt_candidates`，501 张表。最后启动核验见 `.codex-temp/orientation-runtime-refresh-delivery.log`。
- 正常账号各自登录教师电脑端与学生小程序网页端；学生为虚构的“迎新联调同学甲”。未用超级管理员冒充学生，未直接把数据库业务状态改到终态。
- 虚构批次：`ORI-UX-20260922`，批次编号 `18`，录取编号 `YX-UX-20260922-01`。同一条迎新记录连接正式学生主档、绿色通道、报到、住宿安排与实际入住；保留后续可继续办理的数据。

### 已实施

1. 19 个平铺入口收为 8 个任务工作区：迎新工作台、批次准备、新生办理、审核与缴费、现场报到、住宿安排、异常跟进、统计归档。原办理能力继续通过工作区标签访问，未删除未完成迁移的正式路由。
2. 工作台待办前置；资格、缴费、绿色通道、住宿办理按所选批次及精确学生过滤。详情、审核、现场办理保留批次；从住宿返回迎新也记住批次，按学校、账号、角色及工作上下文隔离。
3. 分开“现场核验条件”“全部手续”“现场报到”三个事实。学生报到成功后仍能看见缺项、补材料入口与学院最终确认状态。
4. 信息被退回后学生补正，环节保持等待复核；老师通过后才完成。绿色通道展示退回原因、原申请并允许重交；审批保留学生说明，审批备注进入审计；通过时同步缴费环节，兼容未配置缴费环节的冻结流程。
5. 绿色通道增加申请说明与既有文件服务的材料查看入口，按正式权限获取文件。已验证空材料状态；实际有附件预览仍待上传链路完成。
6. 新生创建的班级选择改用既有远程搜索；学生网页端省市区选择可用；报到码增加生成失败说明及现场核验凭证复制入口；相关时间以本地日期时间显示。
7. 修复住宿分配发布任务一直排队：日常启动器补起既有住宿分配工作进程，并限定沙箱租户；不另建分配逻辑。隔离库覆盖错误租户不得领取任务。

### 已真实页面跑通的同一学生故事

| 办理段 | 实际动作与写后结果 |
|---|---|
| 学校准备 | 创建虚构批次、录入新生、启用批次、绑定学生主档与账号 |
| 信息补正 | 学生填报 → 老师退回 → 学生修改紧急联系人并重交 → 老师通过；学生刷新回读 |
| 绿色通道 | 学生提交虚构缓缴申请 → 老师退回要求日期 → 学生补充重交 → 老师通过；保留上一份退回历史 |
| 现场报到 | 学生领取并复制报到凭证 → 教师报到点预检 → 确认报到 → 学生重新进入显示已现场报到 |
| 住宿安排 | 生成仅包含本批次一人的分配方案 → 发布任务完成 → 学生看到预留床位 |
| 实际入住 | 宿管在正式床位入口核对预留学生 → 确认入住 → 学生重新加载“我的宿舍”显示当前在住 |
| 跨工作区返回 | 从入住管理返回新生办理，地址仍带 `batchId=18`，入学确认只显示 1 人，没有变成全校名单 |
| 状态对账 | 经正式“刷新资格”命令回读当前事实，工作台缴费/绿色通道进度为 1/1，材料为 0/1；入学确认明确缺少身份证明及录取通知书 |

这不是整条迎新流程已办结。材料未上传，学院确认、关闭及归档尚未在本次正常账号页面完成。

### 验证证据

- 教师前端：`node --test tests/orientation*.test.mjs tests/*workspace*.test.mjs`，568 项通过；受改动目录定向代码规范检查通过。日志 `.codex-temp/orientation-pc-tests-final.log`。
- 学生小程序：`node --test tests/orientation*.test.mjs`，15 项通过；网页端构建、微信构建分别通过。日志 `.codex-temp/orientation-mini-tests-final.log`、`orientation-mini-build-verified.log`、`orientation-weixin-build-verified.log`。
- 教师构建通过，日志：`.codex-temp/orientation-pc-build-verified.log`；导航契约检查通过，472 个正式可见入口路由对应检查通过。构建结果只证明当前代码可构建，不证明完整迎新办结。
- 隔离 MySQL（端口 3306，`student_lifecycle_test`）定向用例：绿色通道退回重交及有/无缴费环节 2 项；信息退回重交 1 项；现场报到/学院确认/关闭归档 3 项；住宿发布任务及沙箱环境约束 5 项。分别见 `orientation-green-payment-regression-final.log`、`orientation-refactor-pytest-5.log`、`orientation-finalize-regression.log`、`orientation-worker-regression.log`。使用测试身份夹具，不能冒充正常账号浏览器验收。
- 队列过滤与既有自助办理回归见 `.codex-temp/orientation-refactor-pytest-2.log`、`orientation-refactor-pytest-4.log`；后者信息用例失败已由 `orientation-refactor-pytest-5.log` 修复验证，不把失败日志写成全部通过。
- 页面证据在 `.codex-artifacts/orientation-audit-20260922/`：01–11 为改前审计；12 现场报到回执；13 资格缺项；14 申请核对弹窗；15 学生窄屏下一步；16 八项菜单与入学缺项；17 重构后工作台。截图已打开检查。浏览器视口工具请求 390 宽时实际页面宽度为 417，无横向溢出；不据此冒称 375/390/430 三档均通过。

### 未完成与继续点

续验进展（同日）：用户已授权并手动开启扩展“允许访问文件网址”，并明确同意本次两个本地学生入口的协议勾选和登录。学生电脑端与学生小程序网页端均已正常登录同一虚构账号，不再等待该授权。助手打开扩展设置页曾被浏览器工具安全策略拒绝，因此由用户完成，未采用命令或其他方式绕过。权限开启后，两个学生入口的正常文件选择仍均被浏览器工具拒绝，尚未到达上传接口；未用接口提交代替页面上传证据。

只读复审后补齐：新生详情绿色通道审核携带原申请版本；服务端拒绝已退回历史单再次审核；切换批次后丢弃菜单缓存中的旧批次；新生名单重置保留原批次、搜索更新地址，迟到响应不覆盖新查询；材料列表回学生详情带回批次；资料补正通过后，仅在没有其他未处理异常时恢复身份异常导致的高风险。未将历史虚构记录直接改成低风险或入学终态。

新增证据：教师端 574 项检查通过、构建通过（`orientation-pc-tests-followup.log`、`orientation-pc-build-followup.log`）；绿色通道旧单拒绝、资格/缴费并发及异常范围、材料精确筛选 6 项真实 MySQL 用例通过（`orientation-followup-permission-regression.log`）；资料补正风险回落及其他异常存在时保持风险 2 项通过（`orientation-info-risk-followup.log`）。上述日志均在 `.codex-temp/`。日常后端已通过统一启动器更新并核验原沙箱（`orientation-runtime-followup.log`）。浏览器验证先切换到批次 17，再进入已打开的审核页签，实际地址及标签均为 17；返回批次 18 后重置筛选仍保留 18。后端重载造成的读取失败经“重试”恢复同一条虚构学生名单，证据 `18-pc-batch-reset-followup.png`。

追加学生电脑端实测：同一学生首页原把现场报到算成“迎新入学已完成”，且空待办显示“一切就绪”。已修复服务端完成口径，只以学院确认为完成；本人迎新未办结时复用已开通的正式入口提示继续办理，保持真实待办数量，不编造待办。首条提示与主按钮须同一业务，其他真实待办保留原列表。两学生端步骤只显示本人冻结批次启用的环节，并带中文名称。学生电脑端增加缺材料直达入口，明确“已现场报到、仍需补办”；小程序首屏有学生待办时不再笼统提示等待学院。

追加验证：`test_orientation_home_progress.py` 的 6 项单元检查最终通过，覆盖现场与最终状态、模块关闭、延期、跨业务提示与按钮匹配（`orientation-home-unit-final.log`）。启用流程步骤的两端接口投影、既有首页读取、消息分区故障隔离共 3 项真实 MySQL 检查通过。组合首次运行总计 8 通过、1 失败：单元夹具缺租户上下文，已补齐并用前述 6 项重验；不把首次失败日志写成全过（`orientation-home-progress.log`）。学生电脑端 5 项定向检查、全目录规范检查与构建通过，命令为 `node --test tests/orientation-journey-behavior.test.mjs tests/o3-orientation-self-service-contract.test.mjs`、`npm run lint`、`npm run build`，证据为工具输出，未虚构落盘日志。

日常后端已通过统一启动器更新，原数据库、学校与迁移再次核验（`orientation-runtime-home-refresh.log`）。正常学生电脑端首页实际显示“迎新手续尚未办齐”和“迎新入学进行中”；点击“查看迎新办理进度”进入同一记录，显示两项材料缺项、已入住与已现场报到，再点“查看与补交材料”到正式表单。证据 `19-student-pc-home-followup.png`、`20-student-pc-orientation-followup.png` 已打开检查。学生小程序同一记录回读 5/7 项完成，仍有材料和学院确认。

最终补验：小程序 16 项定向检查通过，网页与微信构建分别通过（`orientation-mini-tests-delivery.log`、`orientation-mini-h5-delivery.log`、`orientation-mini-weixin-delivery.log`）。浏览器全局视口设置本次未影响目标页，未以此冒认窄屏通过；改用工具公开的目标页面开发调试视口能力，实际页面宽度分别为 375、390、430，文档宽度及滚动宽度一致。三张真实页面截图 `21-student-mini-375.png`、`22-student-mini-390.png`、`23-student-mini-430.png` 已打开检查，首屏主动作可见且未横向溢出，验证后已清除临时视口设置。此证据仅覆盖迎新首页排版，不代表所有表单、键盘和微信宿主通过。

### 2026-09-23 续验：材料审核、办结历史和批次收尾

已实施：

- 材料审核必须携带所见版本，仅当前待审核版本可办理；已退回旧版、已确认入学、关闭批次均拒绝重审。审核动作、附件审核状态、学生资格与审计同事务更新。修复附件存储状态覆盖材料业务状态的问题，避免材料有文件却不显示审核按钮。
- 教师电脑端单条、详情、批量及教师移动端均传原版本。批量中途失败保留未完成目标、原版本及退回原因；切换批次停止后续提交。学生详情和教师移动办理丢弃旧对象迟到响应，提交期间互斥，材料成功后回读同一学生资格；回读失败明确已经处理，不能继续用旧资格确认入学。
- 学生两端在办结、关闭后仍能读取本人信息、到校计划和材料历史。写入继续受原开放窗口与终态规则约束；多个开放批次冲突明确报错，不任取一条。学生电脑端文件选择改为中文按钮与文件名显示。
- 归档列表在数据库中按当前批次筛选、计数及分页，并返回所属批次真实状态。页面创建回填当前批次，未关闭不能执行；从归档直达指定批次一行，结束后可继续同批次归档。关闭仅允许学校范围；新增名单或恢复报到与关闭竞争同一批次锁，避免关闭后遗留新待办。
- 学校管理员、学工管理员移动服务大厅补上既有迎新入口。修复看板“批次总数 1、未报到却为全校 2101”混用口径：统计、未报到名单和后续新生办理共用批次，支持精确学生直达。此轮首次检查时移动绿色通道仍为全批次，后续已补齐，见下方同日续验。

验证：

- 隔离 MySQL 最终 12 项通过，212.18 秒：材料真实并发审批互斥与单条审批审计；正式退回→重交→通过→现场报到→学院确认→关闭→归档；原自助流程；归档批次/租户过滤及分页；学院范围拒绝关闭；新增/恢复分别与关闭先后竞争的 4 种交错；既有批次与归档前置。日志 `.codex-temp/orientation-material-archive-final-20260923.log`。此前两轮各有新测试断言错误（校验状态码、把提交审计计入审批），均修正后由本轮重验，不把失败日志算通过。
- 本人历史读取 6 项和启用步骤投影已在前一隔离库组合运行通过，见 `orientation-material-review-20260923.log`。移动看板 3 项隔离替身单测通过，核对同用户、同批次及无批次不回退全校；这些单测不冒充数据库或浏览器证据。
- 教师电脑端 `node --test frontend/tests/orientation-*.test.mjs` 72 项通过；本轮 24 个已跟踪前端改动文件与新增材料/归档测试规范检查通过；教师构建通过。日志 `orientation-pc-final-20260923.log`、`orientation-pc-lint-20260923.log`、`orientation-pc-build-20260923.log`。
- 移动迎新行为检查 32 项通过（`orientation-mini-final-tests-20260923.log`），包含已现场报到学生提示“仍有入学手续待补办”、手续齐备则提示待学院确认，避免混用未报到资格说明。学生电脑端 5 项、全目录规范检查与构建通过（`orientation-student-pc-delivery.log`、`orientation-student-pc-lint-delivery.log`、`orientation-student-pc-build-delivery.log`）。
- 日常后端通过统一启动器更新；再次确认原 `3307` 沙箱数据库、学校、501 表与迁移，后端健康检查正常；日志 `orientation-runtime-final-20260923.log`。没有改动业务终态、迁移或生产环境。继续保留原 `RELEASE_RUNBOOK.md` 和其他任务新增的 `website/`。

最新收尾：移动网页与微信构建均通过（`orientation-mini-final-h5-20260923.log`、`orientation-mini-final-weixin-20260923.log`），仅证明可构建，未做微信宿主实测。正常教师账号在独立移动网页登录上下文，从服务大厅点击“迎新办理”进入批次 18；实际看板总数 1、未到校 0，进入办理仅有同一虚构学生。详情读取“已现场报到”“已入住”和两项必交材料缺项，学院确认按钮确实禁用。390 像素视口与文档滚动宽度一致，截图 `26-teacher-mini-same-student-390.png` 已打开检查，并已清除临时视口设置。教师移动端尚未完成材料审核写入。

教师电脑端归档页面更新前错误显示其他批次的 2025 级历史归档，更新后当前批次为 0 个任务；创建表单自动填批次与名称，取消未保存；“前往批次管理”精确显示批次 18 一行。截图 `24-archive-before-runtime-refresh.png`、`25-archive-batch-filtered.png` 已打开检查。没有将当前批次实际关闭或归档，也未将现有历史归档算成本轮成功终点。

### 2026-09-23 续验：移动绿色通道按批次办理

已实施：教师看板进入绿色通道时携带当前批次与名称；页面分为“待审核”“全部申请”，显式分页。正式列表服务先按授权范围、批次、待审状态筛选，再计数和分页，修复原先取最近 50 条后才筛待审导致较早待办不可见的问题。旧电脑端查询默认行为保留，旧移动端显式状态筛选保留。页面读取实际审核权限；读取失败不显示旧列表，迟到响应不覆盖新查询；重复点击不重复发起审核。审核冲突保留原版本和意见，禁止继续提交，老师关闭并刷新、重新打开同一申请后才能核对新版，原意见继续保留。未知状态使用中文提示。

验证证据：

- 移动迎新 39 项与中文展示 4 项，共 43 项检查通过：`node --test tests/orientation-*.test.mjs tests/customer-visible-localization.test.mjs`，日志 `.codex-temp/orientation-green-mini-final-20260923.log`。覆盖批次及大整数编号、分页和队列切换、迟到响应、只读和权限失败、重复点击、原版本提交、冲突保留意见与重新核对、空尾页回退及离页后迟到回执。
- 独立 `127.0.0.1:3306/student_lifecycle_test` 回归首次 3 项通过、1 项失败；失败是新增测试批次缺少必需流程版本，被现有数据库约束拒绝，已修正夹具。通过的 3 项为原工作区批次队列与有/无缴费环节的退回重交；日志 `orientation-green-queue-mysql-20260923.log`。等待其他任务释放共享测试库后，仅重跑失败节点 `test_orientation_queue_batches.py::test_mobile_green_queue_filters_pending_and_batch_before_paging`，最终 1 项通过、121.44 秒，日志 `orientation-green-queue-mysql-final-20260923.log`。实际验证 55 条更新的已批准申请不遮住 2 条较早待办，分页不漏项，其他批次隔离，显式状态兼容，参数约束及学生身份拒绝。未将接口夹具结果冒充正常账号页面写入。
- 最终移动网页与微信构建均通过，日志 `orientation-green-mini-h5-final-20260923.log`、`orientation-green-mini-weixin-final-20260923.log`。微信宿主仍未实测。
- 统一启动器更新本地后端成功，日志 `orientation-green-runtime-20260923.log`；原沙箱数据库、学校、501 表与 `20260914_aa_opt_candidates` 迁移版本复核一致，没有迁移或修改业务终态。后端、住宿工作进程及消息调度按原启动器运行。
- 旧验收页控制超时后，在同一浏览器新页使用原正常教师账号重新登录，从“迎新办理”进入批次 18，再进入绿色通道；深层地址刷新仍显示同一批次。待审核为 0，全部申请为同一学生的 2 条历史，分别已退回、已通过；退回意见可见，已办记录没有审核按钮。390 像素视口、文档宽度与滚动宽度均为 390，截图 `27-teacher-mini-green-batch-history-390.png` 已打开检查，临时视口已清除。此轮仅验证移动绿色通道的正常账号读取，没有新增审核写入。

### 2026-09-23 续验：统计、新生查询与批次台账导出

已实施：统计待办进入对应工作区时带上正在查看的批次，包括服务端默认选定的批次。各导出调用明确传当前批次，缺批次不再偷偷选择第一个开放批次。新生查询接通正式文件下载，点击姓名直接继续该生的迎新记录；读取失败清掉旧行并保留重试，迟到响应不覆盖新查询。所有导出明确为当前批次、本人有权查看的完整台账，不冒称按关键词或勾选行导出，不提供后端未实现的自选字段。生成期间互斥并保留失败输入；生成后显示行数、文件和导出记录编号，可重下同一文件。

表格使用中文材料、缴费、绿色通道、异常及来源名称；受阻环节使用该批次冻结流程的业务名称。综合台账把财务记录与绿色通道通过事实分列，缺财务记录明确为“未同步缴费事实”，不改业务审批、财务状态或数据权限。当前虚构学生实际财务记录为“未缴费”，绿色通道为“已通过”，二者分别保留，不能把审批通过当成已到账。

验证证据：

- 教师端最终 `node --test tests/orientation-*.test.mjs tests/stage-b-orientation-capability-contract.test.mjs` 87 项通过，日志 `.codex-temp/orientation-export-pc-regression-20260923.log`。首次为 86 通过、1 失败：旧导航测试仍在旧菜单片段里查权限字符串；现改为验证实际生成的每个工作区权限，并核验正式菜单确实接入生成函数，未删除权限断言。新增行为测试覆盖六个列表消费者带批次、明确批次及大整数编号、统计跳转、读取失败和迟到响应、重复点击、导出回执及重下载、权限拒绝和精确学生详情。
- 本轮改动教师文件定向规范检查通过；导出表单与最后修改的菜单测试再次检查通过。教师端构建通过，日志 `orientation-export-pc-build-final-20260923.log`；既有构建体积和无关页面样式警告仍存在，不将其写成零告警。
- 等待其他任务释放共享测试库后，在独立 `127.0.0.1:3306/student_lifecycle_test` 运行 `tests/test_orientation_export_reports.py tests/test_orientation_import_export_a1.py tests/test_school_xlsx_x1.py::test_x1_orientation_production_report_variants`，5 项通过、207.32 秒，日志 `orientation-export-mysql-20260923.log`。真实创建并下载九类表格，核对当前批次、排除其他批次、中文状态、用途水印、公式注入防护，以及无效/外校批次和无权限拒绝；含既有导入链回归。
- 补充缺财务记录与已批准绿色通道同时存在、学生兼容投影不同步的场景后，仅重跑新增表格节点，最终 1 项通过、142.80 秒，日志 `orientation-export-facts-final-20260923.log`。同节点重新覆盖全部九类表格。该隔离夹具构造状态只用于接口报表回归，不计为正常账号业务办理。
- 日常后端通过统一启动器更新，原沙箱学校、端口 3307、501 表和迁移版本再次确认一致；健康检查通过，日志 `orientation-export-runtime-final-20260923.log`。没有迁移、直接变更学生终态或生产发布。
- 正常学工管理员真实页面：统计批次 18 点击待审材料，修复前丢批次并出现全校 1225 份，修复后地址仍带 18、材料为 0；新生查询只显示 1 人，点击姓名进入 `/admin/orientation/students/70056?batchId=18`。统计页导出绿色通道 2 行，查询页导出综合台账 1 行，均实际落入本机下载目录。绿色通道再下载的两份文件逐字节相同。综合台账只包含同一虚构学生，已现场报到、财务未缴费、绿色通道已通过、已入住均正确。首轮文件内容核对曾误把该生当成“没有财务记录”，断言失败后按实际权威记录修正核对；未为通过测试篡改该生财务数据。缺记录分支由上述隔离数据库场景证明。
- 浏览器的下载事件等待曾超时，但下载文件实际存在；验收依据是页面回执及文件内容，不把事件超时写成事件通过。开发热更新期间空白页经刷新恢复，最终页面回读正常。截图 28 为原统计页，29 为修复后的同批次材料页，30 为绿色通道导出回执，31 为新生查询导出回执，均已打开检查；下载内容及文件一致性核对保存在 `32-export-browser-verification.json`，同在 `.codex-artifacts/orientation-audit-20260922/`。截图中的正常教师使用 1440×900 电脑视口，临时视口验完清除。

本段没有新增学生材料写入、最终入学、批次关闭或归档页面证据。其他导出页面由调用行为测试与真实表格接口覆盖，未宣称九种报表均逐页点过。

### 2026-09-23 续验：未报到跟进交接

对照原方案的收尾条目发现：未报到名单跳“报到安排”只带姓名或录取号搜索，没有批次和精确学生；跳详情也丢批次。页面还显示了正式接口明确尚未支持的单条、批量提醒操作。

已修复：报到安排携带名单行的批次和稳定学生编号，详情携带同一批次；办理入口遵循现行入学管理权限，无权时只提供详情。移除未接通的提醒入口，没有用成功提示冒充送达，也没有删除真实通知服务。列表开始读取即清除旧行与总数，错误有明确提示；迟到结果与离页后的结果丢弃，加载或错误状态不能操作旧对象。未增加后端写入口或改变延期、取消、恢复的规则。

验证：`node --test tests/orientation-batch-roster.test.mjs tests/orientation-route-context.test.mjs` 27 项通过，包含新增的精确学生/批次交接、无权限拒绝、迟到及失败读取、离页失效三个场景；代码规范检查通过。日志 `.codex-temp/orientation-noshow-followup-20260923.log`。教师构建通过，日志 `orientation-noshow-build-20260923.log`。本轮仅改前端，未重复清理数据库运行后端测试。

正常教师浏览器：已有虚构批次 17 的一名未到校学生，从未报到名单点击“报到安排”，到达 `/admin/orientation/qualification?batchId=17&orientationStudentId=70055`，仍只显示原学生；刷新地址及名单保持不变。该学生仅用于读取和跳转检查，没有对其延期、取消、激活或核验写入。回到本次批次 18，未报到为 0，已现场报到的“迎新联调同学甲”未被错误纳入。截图 `33-noshow-exact-student-handoff.png`、`34-noshow-current-batch-empty.png` 已打开检查，临时电脑视口已恢复。

这项修复不等于延期/取消页面写入或提醒能力验收通过；后者仍属明确缺口。材料工具阻断、最终入学与归档的正常账号页面验收继续未完成。

### 2026-09-23 续验：延期、恢复与取消的真实收尾分支

通过正常教师页面在批次 18 新增独立虚构记录“迎新收尾测试同学（虚构）”，录取编号 `YX-CLOSE-20260923-01`，迎新记录 `70057`，软件技术2601班；没有绑定账号、学生主档或分配床位。依次通过正式页面办理“延期报到 → 恢复报到 → 取消入学”，每次填写明确虚构核实原因。延期后刷新仍显示延期；恢复后读到未现场报到；取消后资格页显示已取消入学、未报到队列恢复为 0。详情实际读取到三次操作者、前后状态和原始原因。没有直接写数据库终态，也没有修改主验收学生 `70056` 的报到、绿色通道或入住事实。批次 18 现在保留 2 条记录，其中新增这条已取消；此前单人批次截图与导出为当时事实，不再当作最新总数。

本次发现并修复：取消后资格页仍提示补材料和激活账号，详情仍开放常规审核操作；教师账号激活命令也未校验暂停/停止状态。资格页与详情现明确显示停止/暂停及恢复入口，取消记录不再显示补办催办或激活按钮。后端激活在版本幂等返回之前验证延期、未到校、取消及作废状态，并锁定批次、拒绝已关闭批次；与关闭命令串行，防止关闭后新增账号。沿用既有权限、账号和学生主档服务，没有放开权限、禁用已有账号或修改密码/登录规则。

验证：

- 正常教师页面写入和回读如上；截图 `35-cancelled-student-next-action.png`、`36-disposition-ui-audit-trail.png` 已打开检查，分别展示取消后的下一步和三次办理留痕。新增记录仅覆盖无床位的收尾分支，不代表预留床位释放或学生端取消提示已实测。
- 前端定向方法检查 17 项通过，包含停止状态提示、激活入口拒绝及详情历史只读；代码规范检查、教师构建通过。日志 `.codex-temp/orientation-disposition-pc-20260923.log`、`orientation-disposition-build-20260923.log`；完整迎新前端回归 92 项通过，见 `orientation-disposition-pc-final-20260923.log`。
- 独立端口 3306 测试库运行 `test_orientation_o5_checkin_finalize.py::test_o5_stopped_or_closed_orientation_rejects_activation_without_side_effects` 与 `test_o5_imported_candidate_activates_identity_before_signed_checkin`，2 项通过、144.05 秒，日志 `orientation-disposition-activation-20260923.log`。拒绝场景同时覆盖已绑定与未绑定身份，核对账号/主档/关联数量、版本和学生状态无变化；正常激活、幂等返回及后续签发报到凭证继续通过。未将接口夹具冒称真实学生登录。
- 统一启动器已更新日常后端，原沙箱数据库、学校、501 表及迁移版本复核一致，健康检查通过；日志 `orientation-disposition-runtime-20260923.log`。没有迁移或生产发布。

主验收学生材料上传仍受工具拒绝，最终入学和归档页面闭环未完成；本段收尾分支通过不能替代该主故事。延期、恢复、取消这三项教师页面写入不再列为全未验；未到校登记、带床位释放、学生端收尾结果仍分别保留验收缺口。

### 2026-09-23 主线阻断复核

最新浏览器刷新主验收记录 `70056`，地址仍为批次 18 的精确学生资格页；真实结果为已现场报到、仍需补办，缺身份证明与录取通知书，没有最终确认按钮。前述材料退回→重交→最终确认→关闭归档的 12 项数据库证据，以及最新 92 项前端回归、2 项账号业务状态检查不能代替这条正常账号页面链。

浏览器文件选择拒绝在授权和扩展设置完成后仍持续，已跨越至少三个连续目标续工回合；此期间完成了独立的菜单交接、导出、未报到跟进和延期/恢复/取消修复。现在主线继续所必需的材料页面上传仍无法由现有工具执行，未尝试通过接口上传、直接改状态或其他控制方式绕过拒绝。目标标为受阻而非完成，不缩小原目标；既有代码、虚构材料文件、两条虚构记录和教师续验页保留。

恢复点：工具重新允许正常文件选择，或由用户通过正常学生页面为 `YX2026092201` 上传两份虚构必交材料后，继续同一记录的教师查看与退回、学生补交、教师通过、学院确认、学生回读、批次关闭及归档。其余未验证项仍按下列清单保留，未声称微信宿主或真实师生试用通过。重复授予宽泛权限不能替代文件选择工具能力恢复；本轮没有再次要求用户授权。

基线仍为本地 `main`、`b7cfdb74987e19f8dad407ff830622ef5fcd2834`，本轮改动未提交、未推送、未合并、未发布生产。保留原 `RELEASE_RUNBOOK.md`、其他任务的教务中心文件和 `website/`，未覆盖或清理。完整迎新尚未验收通过，以下阻断与缺口继续有效。

1. 当前首要阻断是浏览器工具文件选择拒绝；两端正常登录已恢复。实际上传与材料页面提交仍未通过。用户开启侧栏和站点权限后，正常文件选择仍被拒绝，未换用其他接口绕过工具拒绝。
2. 继续同一学生：上传两份必交材料 → 教师查看、退回一份 → 学生补交 → 教师通过 → 学院确认入学 → 学生刷新回读 → 批次关闭与归档。不能跳过该链直接修改终态。
3. 微信宿主、真机摄像头扫码与上传、真实师生易用性试用未验证；学生电脑端已验证非空读取和继续办理入口，尚未完成材料写入；教师小程序网页已完成同一学生非空回读与前置拒绝，材料写入仍未验。所涉共享省市区组件的教师其他页面未做浏览器回归。
4. 完整导入部分失败、其余台账入口页面交互、同床并发、所有岗位与跨租户反向、消息送达、断网恢复等整模块验收仍需补齐；已有定向通过不代表覆盖全部。
5. 修复前已批准的虚构申请，其学生说明曾被原实现清空；本轮保留原数据与审计，不编造回填。修复后“批准保留说明”已由新数据库用例验证。

便捷性证据：常驻菜单由 19 项减到 8 项；从住宿返回不再重新选批次；从学生详情跳材料/缴费无需再次搜索学生；学生退回后直接补原申请，报到后继续显示下一件待办。免培训仍是产品目标，尚未由真实师生试用证实。

---

更新：2026-09-06。本记录接续《迎新与住宿统一流程-教师PC规划》，其中上一轮“后端待统一、小程序未修改”的描述为历史状态，以本记录的新证据为准。

## 业务主线

1. 教师 PC 准备迎新批次、报到环节、新生名单和房源。名单必须关联学生主档，未关联者先补关联，不能用迎新记录编号代替学生编号分床。
2. 学生 PC / 小程序激活账号，补充资料、到校信息、提交材料和绿色通道申请。教师在原业务审核入口处理同一申请。
3. 教师在统一分配计划中准备名单、生成方案、处理冲突、人工调整并发布。发布产生床位预留；学生两端查看同一住宿安排。设置报到后公布的床位，在公布条件满足前不显示地址和床位编号。
4. 学生出示报到凭证；教师 PC 或教师小程序预检、核验并确认现场报到。材料、缴费、住宿的后续办理与现场报到分开，不把现场报到直接视为入学完成。
5. 宿管在房态图选床办理实际入住。已有预留须核对预留学生；不能把预留显示成已入住。调宿、退宿走原正式业务，成功后同步迎新住宿展示。
6. 学生报到后仍能补交材料。教师在“资格与入学确认”或教师小程序“迎新办理”查看缺项，条件齐备后确认入学。
7. 学校管理员处理未到校、取消、延期或恢复报到。未到校/取消释放合法预留；延期保留预留；已实际入住须先走正式退宿。
8. 批次内全部学生已确认入学或已完成未到校/取消处置，才可关闭批次；关闭后生成逐生归档快照，重复执行不重复归档。

## 本轮已保存的实现

| 范围 | 实现 |
| --- | --- |
| 统一住宿事实 | 从正式住宿记录、床位及占用人联合投影；重复有效住宿、房床链不一致等显示待核查，不伪装为空床或已入住 |
| 旧入口约束 | 停止迎新手填楼栋房间、直接批量改入住状态；返回明确业务错误，引导正式分配/入住 |
| 教师 PC 分配 | 紧凑计划工作区、候选与方案对照、缺少身份关联提示、冲突处理、人工选床、发布确认 |
| 教师 PC 迎新 | 现场报到工作区复用扫码预检/确认接口；资格队列、报到处置、真实归档和快照查看 |
| 学生 PC / 小程序 | 共享住宿状态；分清预留、入住、退宿；报到后补材料；完成入学后收起已结束的核对事项 |
| 教师小程序 | 迎新办理队列、查询与分页、信息核验、学院确认入学；读取实际权限控制办理按钮，服务端继续校验 |
| 重试与并发 | 入学确认请求标识保持稳定；版本校验、过时查询丢弃、失败保留输入；归档重复执行幂等 |

## 验证证据与边界

- 专用测试库后端回归：10 项通过。覆盖住宿状态统一、旧写入口拒绝、未到校释放床位、过期版本、报到后补材料、现场预检确认、入学确认、批次关闭和逐生归档。日志：`.codex-temp/orientation-final-flow.log`。
- 教师前端定向检查：18 项通过；小程序既有迎新契约检查：7 项通过。它们只证明各自断言覆盖的内容，不替代完整浏览器操作验收。
- 教师 PC、学生 PC、小程序 H5 和微信构建通过；导航契约检查和 490 个可见页面路由对应检查通过。
- 浏览器检查：教师 PC 报到点加载；学生 PC 无迎新记录空态；教师小程序管理员空队列、无迎新权限账号被拒绝。没有向学校现有数据提交测试入住或发布分配。
- 非空主流程写入由隔离 API 集成测试验证；尚未完成四端全部非空页面的浏览器往返验收。微信真机扫码、网络恢复、订阅消息送达不能以 H5 或构建结果代替。

## 尚需完成的整体验收

目标仍是四端迎新与宿舍管理的一体化，不因本批回归通过而缩小范围。

1. 用独立联调批次，完成四端非空记录的实际页面往返：资料/材料、审核、分配、报到、入住、入学确认、关闭归档。
2. 核对原规划中的日常住宿关联：调宿在途、退宿最终确认、检查整改、异常跟进、通知落点及统计一致性，补齐缺证据的分支。
3. 对学校当前配置的实际岗位权限、自动分配不足/冲突、导入部分失败、同床并发与断网重试做场景验收。
4. 继续按同一学生记录串联教师 PC 办理入口，核查返回筛选和所选批次是否保留；不恢复大横幅或装饰统计卡。
5. 微信开发者工具和真机验证摄像头扫码、文件上传及消息跳转。

本轮未宣称整套迎新所有旧页面已经重构，也未宣称微信真机联调完成。

### 接续批次：调宿与退宿工作区

修复：原教师页面先取 100 条退宿单再筛待确认，会被较新的历史记录挤出；服务端新增 PENDING 队列，在分页前筛选待确认/阻断记录。教师页改为调宿申请、退宿待确认、住宿历史三个平面工作区，仅加载当前工作区；退宿与住宿历史均可分页，快速切换丢弃旧响应。住宿来源及状态使用中文业务名称。

验证：新增后端场景在待确认单之后插入 101 条已取消记录，验证待办仍能被分页查到，再完成实际退宿并校验迎新同步，1 项通过。新增前端实际方法测试 2 项通过，覆盖服务端队列参数、历史第 6 页和过时响应。教师构建通过；浏览器已核查紧凑窗口的切换布局和空态，非空渲染仍待独立联调数据验证。

### 接续批次：整改证据与复检反馈

实际缺口：教师 PC 与教师小程序收到接口的整改说明、检查/整改/复检文件，却没有把它们呈现给复查人；学生小程序没有显示复检意见。现已接入两端教师复检区，按原检查、整改反馈、上次复检展示；点击文件通过既有授权 SDK，不能直接信任文件 URL。学生小程序显示上次提交和复检意见，学生 PC 已有复检意见继续保留。教师 PC 整改提交的请求编号在同一弹窗重试中保持稳定。

验证：D5 真实数据库闭环通过，新增教师 PC/小程序读取同一整改说明与文件及跨楼栋拒绝断言。新增前端实际方法 2 项通过，验证失败保留输入、重复提交请求编号一致、授权预览错误反馈。教师 PC 构建及教师小程序微信构建通过；最后学生反馈文字模板编译通过。本轮没有新增微信真机或非空浏览器截图证据。

### 接续批次：宿舍检查平面工作区

教师 PC 检查页撤下三块纵向包裹卡片，改为“检查任务 / 整改与复检”。任务进入独立逐房记录区，可返回原任务列表；整改区默认待复检，支持全部未完成、待整改、整改中、已关闭和升级风险筛选。任务、检查记录及整改均接服务端分页，避免原来 100/200 条上限截断。楼栋/房间选择复用完整分页助手。切换任务/工作区时丢弃过时请求。

定向实际方法测试 4 项通过（含上批证据与重试 2 项）；ESLint 与教师构建通过。浏览器检查了窄窗口任务入口及整改状态筛选。当前联调账号任务为空，未将空态浏览器检查当成非空逐房办理验收。

### 接续批次：非空 PC 页面联调开始

在专用 `codex_leave_ui_test_0ea278f0` 测试数据库新增独立“UI-ORI-DORM-2026”批次、测试学生和 4 张空床。初始化脚本 `.codex-temp/seed-orientation-dorm-ui.py` 可重复执行；不覆盖已有批次或学生。该初始化只准备基础数据，没有预先完成报到、分配、审核或入住。

真实浏览器操作：测试学生通过学生 PC 登录，填写联系信息并保存；教师 PC 查询同一学生，信息核对阻断消失，仍保留材料和住宿阻断；学生报到页同时变为可签发报到码。验证符合“现场报到和入学确认分开”的业务设计。

发现并修正：教师 PC 未识别 PREPARED，现显示“已预报到，待到校”；学生 PC MISSING 缴费状态原被通用标签误译为“缺记申诉”，现显示“尚未登记缴费”，未知金额显示破折号而不是 0 元。两文件 ESLint 通过，并在学生实际页面确认新显示。

当前非空页面联调停点：已完成信息提交，尚待上传材料、正式分配、现场报到、实际入住与入学确认。学生及教师小程序非空页面往返亦待继续，不能将本次两端信息提交扩大为整套四端浏览器验收。

### 接续批次：真实页面分配与入住

同一测试学生在教师 PC 完成创建分配计划、自动生成方案、核对发布；学生 PC 显示“已预留 · 待入住”，同一床位为四端联调宿舍 101 室 1 床。教师从方案查看床位进入房态，再办理预留入住；写入完成后学生 PC 刷新显示“已入住”。没有替换测试学生或预先写入完成状态。

页面联调发现预留床位没有展示预留人，老师必须重复搜索。现于实际生效的床位投影服务加入经正式住宿事实核验的预留学生编号、姓名和学号，入住核对框自动带入该学生；保持 studentId/occupantName 仅代表实际占用，预留信息独立。跨租户房间与楼栋权限校验保留，服务端入住命令仍核验预留归属。

验证：扩展后端住宿统一场景，通过真实发布读取预留人、实际入住、退宿及迎新投影，1 项通过；5 项房态前端测试通过；教师 ESLint/构建通过；浏览器确认自动带人核对框及学生端实际入住显示。当前测试记录已入住，材料尚未提交，现场报到及入学确认未完成，后续联调沿用该记录。

### 接续批次：四端主链路完成至归档（2026-09-06）

沿用同一隔离测试学生 UI-ORI-001、同一批次 UI-ORI-DORM-2026、同一床位 101 室 1 床，完成现场报到、报到后补交 PHOTO、真实 ClamAV 扫描、教师材料通过、学院确认、结束批次和生成 1 条归档快照。文件扫描使用本任务专用 Docker ClamAV 和隔离数据库，没有直接伪造安全扫描结果或学生完成状态。测试图片为合成样本，仅验证上传与流转，不代表真实证件审核。

本批 HTTP 联调使用学生 PC / STUDENT_MINI 和教师 PC / TEACHER_MINI 身份：学生小程序身份提交材料，教师 PC 审核，教师小程序身份执行现场核验与学院确认。两个学生身份最终均返回 ENROLLED、COLLEGE_CONFIRMED、CHECKED_IN、APPROVED。主流程的写入通过真实服务端命令完成，部分操作使用 HTTP 而非页面点击。

实际页面核验：学生 PC 显示“入学手续已完成”、正确床位及材料已通过；学生小程序 H5 显示“报到完成”和“已入住”，首页出现“入学确认已完成”业务消息；教师小程序 H5 的迎新办理列表显示“已确认入学”，详情不再提供重复确认；教师 PC 新壳内显示已归档记录及查看快照入口。微信真机摄像头、微信文件选择和订阅消息仍未做设备验收。

发现并修复两处问题：
- 冻结流程未启用 CHECKIN 时，签发、预检与确认现在统一拒绝，避免预检显示可确认、提交才失败。隔离测试批次补齐 CHECKIN/CONFIRM 配置；没有修改真实学校批次。
- 学生完成后不再显示“未找到开放的本人迎新记录”的过期提醒或失效报到码入口；已完成现场核验的小程序二维码页不再引导重复生成。

验证：O5 报到确认与 O3 自助流程共 3 项测试通过（包含禁用 CHECKIN 后禁止签发/预检/确认、恢复后正常办理）；学生 PC 构建通过；小程序二维码页 Vue 模板编译通过；本批修改文件 diff 空白检查通过。全仓存在其他既有文件的尾部空白，未混入本批处理。

边界：这是一条同一学生从预报到、分配预留、实际入住直到归档的贯通证据，不等同于所有异常分支和微信真机验收完成。绿色通道、收费对账、延期/取消/未到校、调退宿等既有场景仍按各自测试记录核验，后续应集中完成四端页面异常分支验收，而不是再次重跑本条已完成链路。

### 接续批次：待到校队列与异常住宿回归（2026-09-06）

修复未报到跟进默认仅查 NOT_REPORTED 的遗漏：服务端新增 pendingArrival 查询条件，在权限范围内、分页之前包含 NOT_REPORTED/PREPARED/DELAYED/NO_SHOW/ABNORMAL，排除 CANCELLED/ENROLLED。教师 PC 状态筛选默认为全部待到校事项，新增“已预报到，待到校”，清空状态筛选仍限定待到校队列；空结果不再错误宣称所有新生均已报到。快速切筛选时丢弃旧响应。

验证：2 项真实 MySQL 集成测试通过，覆盖延期保留预留、恢复延期保留同床、取消释放床位、恢复取消不重新占床、版本冲突、未办结不得关批，以及待到校筛选发生在分页之前；2 项前端实际方法测试通过，覆盖清空筛选与过时响应。教师 PC ESLint 和构建通过，本批修改 diff 检查通过。

非空页面证据：新增独立测试批次 UI-ORI-FOLLOWUP-2026 和测试学生 UI-FOLLOWUP-001，学生小程序身份通过真实信息提交命令进入 PREPARED。教师 PC 未报到跟进默认列表实际显示该生（共 1 名），上一批已入学且已归档的学生未混入列表。该新记录作为下一轮延期/取消四端页面联调样本，尚未安排床位或执行异常处置。已有完成批次与主链路证据未被重置。

### 接续批次：异常报到的四端反馈（2026-09-06）

学生 PC 与学生小程序分别明确显示延期、未到校、取消入学三类结果及下一步；暂停办理时收起正常报到码与资料补办入口，恢复为 ADMITTED 后恢复正常下一步。延期文案说明已有预留继续保留，取消/未到校说明预留释放，与前批实际床位回归结果一致。教师 PC 与教师小程序优先根据 CANCELLED 阶段显示“已取消入学”，避免旧 reportStatus=NO_SHOW 将取消误称为未到校。教师小程序已确认记录的动作改为“查看”。

同一隔离跟进学生经真实命令依次完成延期、恢复、取消、恢复、未到校、恢复、延期。学生 PC 与小程序接口读取的 stage 一致，暂停时均不可签发报到码且不可提交资料。联调脚本第一次循环重复登录触发正常限流，已改为复用登录凭证后完成剩余场景；没有禁用或绕过限流。

实际学生 PC 页面已验证延期结果、未分配住宿事实、报到码入口收起，不再出现通用“联系学校”的报错堆叠。4 项小程序实际计算/权限方法测试通过，学生 PC 构建通过，各改动 Vue 模板编译及 diff 检查通过；末次标题去重及暂停标签属文案调整。小程序异常态真实设备和完整页面往返仍待后续集中验收，不能把接口身份一致性等同真机完成。

### 接续批次：学生住宿操作可用性与重试（2026-09-06）

审计学生 PC 与学生小程序的选床、调宿和整改入口。保留“学生确认只预留、教师核验后才实际入住”的既有分工，本批没有改变住宿状态机或绕过审批。

修复：小程序住宿页改为 onShow 刷新，返回页面时读取老师最新处理结果；调宿历史请求失败时，两端停止开放新的调宿申请，避免把无法读取的在途申请当作不存在。学生 PC 整改提交按记录和完整提交内容保留请求编号，网络失败后同内容重试复用编号，内容变化才生成新编号，成功后清理草稿请求。

验证：3 项实际前端方法测试通过（异常时禁止调宿、返回页面刷新、整改重复提交使用相同请求编号并保留失败输入）；学生 PC 构建和微信小程序构建通过；修改文件 diff 检查通过。构建不等于微信真机验收，真实设备能力边界保持不变。

### 接续批次：教师工作台批次上下文（2026-09-06）

迎新工作台新增紧凑批次选择和刷新，可查看进行中及已结束批次；复用后端现有 batchId 看板查询，不增加统计口径。批次选项读取完整分页，切批次时忽略过时响应。选择记录在路由查询中，从工作台打开新生名单时携带 batchId，名单初始化及已开页签路由变化均接收该批次，不被默认当前批次覆盖。

实际页面验证：当前跟进批次显示 1 名、0 名已现场报到；切到已完成批次显示 1 名、1 名已现场报到，各必办环节 1/1。点击查看新生列表后仍选中已结束批次，仅展示已入学测试学生，未混入跟进批次学生。2 项实际方法测试通过（完整批次分页、名单携带批次、旧响应不覆盖新批次），ESLint/教师 PC 构建及 diff 检查通过。

尚需统一：工作台其他待办目的页（材料、绿色通道、异常等）的批次筛选仍需逐项接入，不能以此次名单跳转验证宣称所有下钻均已保留批次。

### 接续批次：迎新待办批次衔接（2026-09-06）

材料审核、绿色通道、异常列表三个接口新增可选 batchId，按所属迎新学生过滤再分页。工作台进入对应页面携带当前批次，页面重置普通筛选后仍保留该批次；提供返回原批次工作台入口。三个列表切换批次时丢弃过时响应，避免旧名单覆盖新选择。

验证：1 项真实 MySQL 集成场景分别覆盖三类记录在两个不同批次中的隔离查询、分页总数及不存在批次空结果；5 项前端方法测试通过；教师 PC 构建通过，末次请求顺序调整经实际方法测试和 ESLint 验证。实际隔离服务批次 1 材料数为 1、批次 2 为 0；教师材料页显示批次 1 的真实已通过材料和返回工作台入口。服务重启期间出现的只读体验提示未作为业务验收，重新加载后的真实数据才计入证据。

待继续审计：异常列表现有实现仅显式限制租户，没有像材料和绿色通道列表一样按学生/班级岗位范围过滤；异常详情和后续处置需一并核对，不能仅补列表过滤便宣称整个异常业务授权已完成。另外报到进度等剩余下钻和导出范围仍需继续核对。

### 接续批次：迎新异常完整入口范围校验（2026-09-06）

修复上段记录的异常权限缺口：列表关联学生后按统一学生目录范围过滤，再在 SQL 中计数和分页；保持批次筛选，避免先分页再删除无权记录造成总数不正确。详情、登记、跟进、关闭、升级统一验证稳定学生权限，未关联档案仅允许全校范围处理。写操作取得异常记录行锁；拒绝越权时不写跟进或状态。

验证：隔离 MySQL HTTP 场景使用真实 StudentAffairsSecurityContext 范围实现，由测试注入已解析的学生级授权范围，覆盖可见名单分页、范围外详情/三类处置/登记的 403、未关联记录拒绝、拒绝后无副作用、范围内跟进成功，以及收回学生范围后列表为空且详情禁止。另回归三个迎新待办的批次筛选，合计 2 项通过，80.78 秒，日志 `.codex-temp/orientation-exception-scope-test.log`。本次未修改前端，不重复前端构建。

边界：测试验证接口如何消费已解析的权限范围，不替代真实岗位授权配置全链路与微信真机验证。后台发布任务、批量入住、完整复杂分寝和四端验收仍未完成，继续沿实施方案推进。


### 接续批次：可恢复的批量实际入住（2026-09-06）

教师 PC 入住管理保留按床位办理，新增批量入住工作区：筛选楼栋/姓名学号，分页选择预留学生，明确核验本人到场后提交。业务批次与逐人回执使用现有 AffairsBatchJob/Item，当前单份名单上限按用户最新要求提高到 5000；每个继续请求至多办理 20 人，已成功项不重放，关闭页面暂停后续请求，重进可继续。不是离线后台 worker，不能宣称关页后会自动完成。

单人入住与批量办理复用提取后的 dorm_checkin_command 同一事务命令。预留记录 ID/版本保持固定，冲突拒绝覆盖；每人的入住、迎新同步、审计与成功回执同事务提交，失败回滚该人并保留独立失败回执。查询和继续重新核验创建人、租户及楼栋范围。

验证：真实 MySQL 两个集成场景通过，83.13 秒，覆盖到场确认必填、同请求幂等、名单冲突、过期预留、业务写入后失败回滚、重复继续不变更已入住时间/版本、撤销楼栋权限后拒绝，以及 21 人分为 20+1 两段恢复。三项实际前端方法测试通过：丢失创建响应沿用原请求号、迟到列表不覆盖新楼栋、继续响应丢失只回读不自动重放。教师 PC 构建通过（32.25 秒），随后局部回执提示/读取顺序及样式 import 修正经定向 ESLint 与方法测试复验。

浏览器：在专用隔离学校，使用正常分配命令准备 D3 三名学生预留，教师 PC 实际勾选、核验并入住，三人全部成功，名单清空、回执显示原床位。刷新时本地 Vite 返回 431（请求头过大），已定位为开发环境问题，正在复验重新进入；不可将这一阶段写成刷新验收已过。

新增/改动：后端入住命令、批量办理服务、正式路由及住宿名单过滤；教师 PC 批量工作区和 API client；相关测试。HEAD 仍为 f138667d7ae197b304124a75455c15434b8d8474，未提交、未推送。日志 .codex-temp/dorm-checkin-batch-test.log、dorm-checkin-batch-build.log。本轮尚未完成 5000 人批量实际入住、并发老师竞争及微信真机；5000 人试分/发布负载另行验证中。

### 固定联调环境与容量结果更新（2026-09-06）

用户明确决定：以后四端人工联调与演示都在原有持久化演示沙箱进行，积累可展示的业务数据和办理历史。该约定已写入根 AGENTS.md；自动化清理型测试仍使用独立测试库，不得清空演示沙箱。

只读复核：Docker student-lifecycle-v8-mysql 的 3307 端口对应现有数据库 student_lifecycle_runtime_20260902；sandbox-school 租户 ID 为 1000000000000000007，学生档案实际 20000。确认批量任务、分配批次及分配明细表存在。未执行重置、清表、初始化或数据迁移。旧启动入口为 _local-backup/runtime/start-backend-20k.ps1；使用前须复核运行进程，不能据脚本存在便认定当前服务已连接该库。

当前运行边界：教师 PC 5178 仍是此前连接 8018 的隔离联调服务，本次没有宣称已完成四端切换。下一次人工办理前必须核实各端 API 目标和实际登录租户，切换到上述沙箱后重新验收。此前隔离环境中的 3 人入住结果保留，不能冒充演示沙箱的数据。

容量测试补记：tests/test_dorm_allocation_large_batch.py::test_five_thousand_students_real_dry_run_and_publish 已在独立 MySQL 测试库通过（1 passed，1 deselected，276.12 秒）。5000 名学生、125 个班、25 栋楼、5000 床；试分 16.230 秒、110 次 SELECT，发布 165.320 秒。该理想容量样本验证学生/床位唯一、性别匹配、预留与迎新 ASSIGNED 状态一致，并非 5000 人实际入住或全部复杂冲突验收。发布耗时已超过前端 120 秒超时，持久化后台发布仍为待解决阻断。日志 .codex-temp/dorm-5000-capacity-test.log；该测试不写入演示沙箱。

### 接续批次：独立学工库与 5000 人后台发布（2026-09-06）

用户最新指令覆盖上节的固定沙箱约定：自行选择数据库、停止使用原演示沙箱、加快推进。本批使用 Docker `codex-leave-mysql-0ea278f0` / `3310` / `codex_leave_ui_test_0ea278f0`，租户 `leave-ui-test`；保留此前的三人实际入住结果。原 20K 演示库未重置或清空。清理型自动化另用 `codex_dorm_publish_test_0ea278f0`，不清理人工联调数据。

运行入口：教师 PC `http://127.0.0.1:5178`，学生 PC `http://127.0.0.1:5198/portal/`，教师/学生移动 H5 `http://127.0.0.1:5208`，均指向本任务后端 `8018`。沿用当前 main，不影响其他任务的 8000、5173、localhost:5188 服务。当前 PC 浏览器认证按页签持有会话；已实际复现移动本地 Vite 431（Cookie 请求头过大），本任务启动参数统一为 `--max-http-header-size=65536`。没有修改认证权限或放开生产策略。uni-app 的 manifest 固定端口会覆盖 CLI，使用忽略目录中的专用 Vite 配置覆盖为 5208，未修改共享 manifest。

新增前置联调数据：迎新批次 `ORI-5000-2026`（ID 5），5000 名虚构新生，100 个男女混合班（每班男 30、女 20），25 栋楼（男 15、女 10）、1250 个房间、5000 个固定空床。仅 seed 前置数据与两个正常学生测试账号；分配草稿、自动生成、发布均由教师 PC 正常登录后实际点击完成。未直接写入预留或入住终态。

浏览器实际办理计划 ID 3：自动生成 5000 条提议；确认发布生成持久任务 ID 2；离开页面进入入住管理时后台继续执行；返回页面读取“已发布 / 已预留 5000 / 已入住 0”。任务从 15:22:28 UTC 到 15:27:07 UTC，共 279 秒，在本次同时运行多端及其他任务的本地环境中记录，不作为生产性能承诺。

MySQL 回读：5000 个不同学生、5000 个不同床位，性别错配 0；DormStay 全部 RESERVED，床位全部 LOCKED，迎新全部 ASSIGNED，发布回执 SUCCESS 5000。100 个班按性别形成 200 组，190 组同楼、150 组同层，最多跨两栋；班级相近为软规则，房号顺序不等于真实建筑距离。结果 `.codex-temp/orientation-5000-published-result.json`。实际入住仍为 0，不能把发布当成入住完成。

代码变化：
- 住宿发布新增持久任务与独立 worker，复用正式发布命令。任务与住房结果同事务提交，执行前重新核对身份、授权、范围、计划版本和床位占用。并发 worker 使用数据库行锁，中断后可恢复未提交事务；读取任务不触发写入。教师页面不再等待整批发布完成，回到页面可读取结果，丢失响应不自动重复提交。
- 旧材料提醒的任务列表与详情仅接受 MATERIAL_REMIND，避免跨入住宿发布/批量入住的权限与回执合同；住宿任务继续在对应工作区处理。原同步住宿发布接口仍有既有消费者和测试，本批未未经迁移即删除。
- 分配生成采用明确请求时限；5000 人名单合并改为按 ID 索引；楼栋大整数 ID 按字符串发送。发布状态放在名单上方，首屏可见。
- 学生移动端在到校前显示已有住宿安排和查看入口。同一学生 ORI5000-0001，学生 PC 和学生 H5 均实际读到联调校区01栋 / 101室 / 1床，状态为已预留、待入住。教师 H5 以学校管理员身份登录同库，不能把这项登录/回读证据等同所有教师岗位与微信宿主验收。

集中验证：
- `tests/test_dorm_allocation_publish_job.py` 和 `tests/test_dorm_allocation_publish_revalidation.py`：4 passed，162.74 秒，真实独立 MySQL；覆盖队列幂等、只读查询、崩溃回滚、两个 worker 不重复执行、方案变更、账号停用、性别/资源范围变化，以及材料任务入口不得读取住宿任务。事务单测正常身份解析做隔离替身，5000 人页面发布使用真实账号与实际 worker 授权，未替换身份校验。
- 教师 PC 分配工作区与丢响应恢复实际方法测试 10 项通过；相关 ESLint 通过；本批教师构建通过 37.71 秒，末次局部调整再以方法测试、ESLint 和真实页面验证。移动端相关 7 项检查通过（其中含既有契约断言），微信编译通过；未做微信真机。
- 本批相关 diff 空白检查通过。代码已落盘，未提交、推送、合并或部署；当前 checkout `f138667d7ae197b304124a75455c15434b8d8474`，无 PR 操作。仓库其他大量既有改动保留，不纳入本批通过结论。

当前边界：5000 人分配发布与学生接收已验证，5000 人实际批量入住、混合楼同房性别限制、床位碎片/不足的完整大批次分支、所有四端岗位与微信宿主尚未验完。教师 H5 学工材料队列另出现参数校验失败，本批记录为既有入口缺口，未用空数据掩盖。下一批优先完善按迎新批次/班级核验名单的批量实际入住，并验证异常回执和学生回读。

本机恢复入口（均在已忽略目录，含私有运行前提，不纳入仓库）：`.codex-temp/student-affairs-integration-runtime.py` 启动 API，追加 `--worker` 启动发布 worker；`.codex-temp/start-leave-runtime.ps1` 的 teacher/student/backend 模式已指向保留数据的启动器；`.codex-temp/start-funding-mini.ps1` 使用 5208 专用配置。不要再运行旧的带初始化行为的 leave-ui-server.py 来启动本库。
