# 奖助队列异常恢复复审（2026-09-06）

基线：main / f138667d7，开工时仅聊天附件缓存未跟踪。当前为第二批奖助模块总复审的一部分；不代表其余学工批次完成。

## 代码关联与调用链

| 办理页 | 接口适配与 HTTP | 服务、模型与结果消费者 | 验证 |
|---|---|---|---|
| miniapp/src/pages/teacher/affairs/work-study/index.vue 的 loadRecords/runAction/submitMonthly | teacherApi.js getWorkStudyRecords/actWorkStudy/addWorkStudyMonthly → realApi.js → mobile.py teacher_affairs_work_study_records/action/monthly_add | affairs_funding_ext_service.py list_ws_records/act_work_study/add_monthly → WorkStudyRecord/WorkStudyMonthly/WorkStudyPost；学生 PC WorkStudyStudentView、学生 mini work-study.vue 与教师 PC WorkStudyView 读取同一记录 | test_work_study_four_end_workspace.py；新增真实组件方法异步恢复测试 |
| miniapp/src/pages/teacher/affairs/loan/index.vue 的 loadItems/runAction | teacherApi.getAffairsLoans/actAffairsLoan → realApi → /mobile/teacher/affairs/loans[/id/action] → mobile.py teacher_affairs_loans/loan_action | affairs_funding_ext_service.list_loans/loan_action → StudentLoan；学生 PC LoanStudentView、学生 mini loan.vue、教师 PC StudentLoanView 回读 | test_student_loan_four_end_workspace.py；异步恢复测试 |
| miniapp/src/pages/teacher/affairs/reduction/index.vue 的 loadItems/submitAction | teacherApi.getAffairsFeeReductions/actAffairsFeeReduction → realApi → /mobile/teacher/affairs/fee-reductions[/id/action] → mobile.py teacher_affairs_fee_reductions/fee_reduction_action | affairs_funding_ext_service.list_reductions/fee_action → FeeReduction、FileBinding；学生 PC ReductionStudentView、学生 mini reduction.vue、教师 PC FeeReductionView 回读 | test_fee_reduction_four_end_workspace.py；异步恢复测试 |

列表按租户、StudentProfile 与 _allowed_class_ids 过滤，服务端分页并返回动作及版本；写操作核验权限和版本、写审计再提交事务。此轮不修改状态机、审批范围、文件绑定、统计或消息协议。四端后端闭环继续用上述真实隔离 MySQL 专项覆盖；其他三端保留已有适用正文，本轮修补已确认的教师移动队列缺口。

## 实际流转与问题

| 场景 | 当前行为（代码阅读确认） | 施工决定 |
|---|---|---|
| 待办搜索与切换状态 | 三页没有请求序号；旧列表可以覆盖新查询结果和 loading 标记 | 只允许最新请求写入，卸载使在途请求失效 |
| 加载下一页失败 | Promise 拒绝未统一显示；快速重复调用可能重复追加同一页 | 同时只发一个下一页请求，保留已有行与页码，显示原位重试；按业务 ID 去重 |
| 写成功后刷新断网 | 写与读处于同一 catch，刷新失败被误报为审核/登记失败 | 分别呈现写操作成功和列表读取失败，后续重试只执行读取 |
| 真正的写失败或冲突 | 当前保留表单、版本和原因 | 继续保留；增加方法级忙碌阻断，避免重复事件提交 |
| 勤工月度未填数字 | Number('') 为 0，未填工时/补贴被当成已填零值 | 必填项先验证非空；明确输入 0 仍可按业务规则提交 |

正常、退回、终止沿既有状态：勤工 APPLIED→APPROVED→ONBOARD→TERMINATED，拒绝 REJECTED；贷款 RECEIPT→RETURNED→RECEIPT→VERIFIED→CONFIRMED；减免 SUBMITTED→RETURNED→SUBMITTED→APPROVED→ISSUED 或 REJECTED。版本冲突不自动重放写请求。此轮仅修正客户端网络/并发恢复，不增加新的业务超时状态。

施工顺序：三页统一请求与错误恢复 → 防重复提交与必填修正 → 组件方法异步行为测试 → 受影响四端真实接口闭环与小程序构建批次验收。浏览器首看教师减免页处于加载失败，尚不能证明当前登录/后端可用，须单独核验。
