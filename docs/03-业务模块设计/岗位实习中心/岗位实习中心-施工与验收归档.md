# 岗位实习中心 · 施工与验收归档（合并版）

> **文档性质**：本目录下全部施工记录、长跑任务书、P3 施工记录的**唯一归档**（2026-07-11 合并）。
> **施工总控**：[06-岗位实习中心生产级商业化开发总册.md](./06-岗位实习中心生产级商业化开发总册.md)

## 归档索引

| 原文件 | 章节 |
|---|---|
| 2026-07-07-岗位实习-企业库.md | §2026-07-07-岗位实习-企业库 |
| 2026-07-07-岗位实习-实习学生.md | §2026-07-07-岗位实习-实习学生 |
| 2026-07-07-岗位实习-岗位库.md | §2026-07-07-岗位实习-岗位库 |
| 2026-07-08-岗位实习-岗位匹配.md | §2026-07-08-岗位实习-岗位匹配 |
| 2026-07-09-岗位实习-P3-A-归档中心.md | §2026-07-09-岗位实习-P3-A-归档中心 |
| 2026-07-09-岗位实习-P3-B-统计中心.md | §2026-07-09-岗位实习-P3-B-统计中心 |
| 2026-07-10-岗位实习-商业化升级设计总册.md | §2026-07-10-岗位实习-商业化升级设计总册 |
| 岗位实习中心-生产级全栈施工卡.md | §岗位实习中心-生产级全栈施工卡 |
| 岗位实习中心-长跑TODO清单.md | §岗位实习中心-长跑TODO清单 |
| 岗位实习中心-长跑开发总控任务书.md | §岗位实习中心-长跑开发总控任务书 |
| 岗位实习中心页面重构记录.md | §岗位实习中心页面重构记录 |

---



---

## 归档章节：2026-07-07-岗位实习-企业库

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-07-岗位实习-企业库.md

## 结论
**岗位实习中心「企业库」前后端真闭环，可交付。** 以全系统共享企业主档 `t_emp_company` 为底座（不重复造企业表），叠加实习侧企业库能力：合作状态机 + 资质核验 + 黑名单 + 联系人/企业导师 + 统计 + 真导入导出。后端 28 项测试全绿（12 新增 + 16 回归），前端 lint/build 通过、页面真渲染。

## 架构决策（关键）
- 就业域已有 `t_emp_company`（含统一社会信用代码）。按 CLAUDE.md §1.1「不重复造企业表」+ §8「复用现有」，**企业库复用这张表作共享企业主档**，additive 扩展实习字段，两域不争抢 `status`（企业库合作生命周期走独立列 `coop_status`）。
- 旧就业企业页 `/admin/employment/companies` **保留兼容**（navPlan 里作兼容子项）。

## 做了什么（真实、非 mock）
### 后端（均经 pytest 验证）
- **模型**：`t_emp_company` additive +14 列（region/address/scale/source/coop_status/qualification_status/blacklist/blacklist_reason/review_*/intern_count/remark/archived_*）；新表 `t_internship_enterprise_contact`（联系人/企业导师）。
- **服务** `internship_enterprise_service.py`：列表(筛选+分页+脱敏)、详情(含联系人+审计)、增改(信用码去重)、**审核状态机**(仅待审核可审：通过→合作中+资质通过／驳回)、**合作启停**(暂停/恢复/归档)、**黑名单**(拉黑须原因/移出)、联系人 CRUD(主联系人互斥)、统计、**CSV 真导入**(dry-run 逐行校验+库内/文件内去重 → 整批事务确认)、**CSV 真导出**(脱敏)。
- 横切：租户隔离 + `is_deleted` 软删 + 联系电话 `_mask_phone` 脱敏 + 写审计到 `t_internship_audit_trail`(target_type=ENTERPRISE)。数据范围留了 `_scope_filter` 接入点。
- **API**：`/internship/enterprises` 共 15 个端点（列表/详情/增改/审核/合作/黑名单/联系人CRUD/统计/导入dry-run+确认/导出）；静态子路由排在 `/{id}` 前避免吞路由。
- **状态机**：合作 `PENDING→ACTIVE/REJECTED→SUSPENDED↔ACTIVE→ARCHIVED`、`*→BLACKLIST→ACTIVE`；非法流转（非待审核审核、未合作暂停、无原因拉黑、含错行确认导入）全部被拒并测试覆盖。

### 前端
- `InternshipEnterpriseListView`：筛选 + 工具条 + 表(合作状态/资质标签、电话脱敏) + 新增/编辑抽屉 + 审核/暂停/恢复/拉黑/移出(AppConfirmDialog，拉黑必填原因) + **真导入**(粘贴CSV→预校验→确认) + **真导出**(浏览器下载脱敏CSV)。
- `InternshipEnterpriseDetailView`：主档 / 联系人与导师(CRUD) / 合作与资质 / 审计 四 tab。
- `internship.api.js` +16 企业方法(real+mock 兜底)；mock 补企业列表/联系人/permissionActions/statusOptions；routes +2；按钮按 permissionActions 置灰(审核/拉黑对指导教师角色 disabled)。

## 验证
- 后端：`pytest test_internship_enterprise.py` → **12 passed**；`test_internship + test_employment` → **16 passed**（零回归）；health → 通过。
- 前端：`eslint` 5 文件 → 0 错；`npm run build` → 通过；预览实测：列表 3 家企业真渲染、状态感知动作(待审→审核/合作中→暂停/黑名单→移出)、详情 4 tab、联系人电话 `138****0011` 脱敏。

## 改动文件
后端：`models/employment.py`(EmpCompany扩展+联系人表)、`models/__init__.py`、`services/internship_enterprise_service.py`(新)、`schemas/internship.py`(+9 DTO)、`api/v1/internship.py`(+15端点)、`tests/test_internship_enterprise.py`(新,12测试)
前端：`views/admin/internship/InternshipEnterpriseListView.vue`(新)、`InternshipEnterpriseDetailView.vue`(新)、`modules/internship/api/internship.api.js`、`mocks/internship/internship.mock.js`、`modules/internship/routes.js`、`config/navPlan.js`

## navPlan 状态（诚实标注）
- **implemented**：企业列表 / 企业详情 / 企业联系人 / 企业导师 / 企业资质审核 / 企业黑名单 / 企业归档（+旧就业企业页兼容）
- **partial（待补强）**：企业统计（接口就绪，独立统计页待补）、企业合作记录（现用审计代偿，独立台账待补）
- **planned（待施工）**：企业评价（依赖评价子系统）、企业岗位（依赖「岗位库」in-positions 模块）

## 待办（记账，不阻断交付）
- 企业岗位：接入 `t_emp_job`（岗位库模块单独开发时打通企业↔岗位）。
- 企业评价：随「企业评价」子模块开发补摘要与下钻。
- 数据范围按学院细分：`_scope_filter` 接 `resolve_teacher_scope`（学院实习负责人只看本院合作企业）。
- alembic 迁移把 `t_internship_enterprise_contact` 与 `t_emp_company` 新列纳入迁移史（现靠 `create_all`）。
- 权限 key 统一：现用 `internship.enterprise.*`（沿用实习域旧风格），全局权限迁移时随规范收口。


---

## 归档章节：2026-07-07-岗位实习-实习学生

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-07-岗位实习-实习学生.md

## 结论
**实习学生前后端真闭环，MySQL 真库通过。** 把实习学生 `t_internship_record` 与企业库/岗位库真实打通——**学生-岗位分配闭环让岗位库 `allocated_count` 变为真实值**（关闭历史欠账），并落地满员/黑名单/未上架的分配拦截 + 学生实习状态机 + 实习资格 + 实习去向 + 统计 + 导入导出。前端全部生产级只走真实后端（不回退 mock）。

## 做了什么（真实、非 mock，MySQL 验证）
### 后端
- **模型**：`t_internship_record` additive +5 列（`enterprise_id`/`position_id`/`mentor_contact_id` 关联企业库·岗位库 + `eligibility_status` 实习资格 + `destination_type` 实习去向）。
- **服务** `internship_student_service.py`（新，生产级）：列表(多筛选) / 详情(企业·岗位·导师真实关联 + 电话脱敏) / 建档 / 编辑 / **分配岗位** / 退岗 / 状态机 / 资格认定 / 去向 / 统计 / CSV 导入导出。
- **学生-岗位分配闭环（核心）**：分配校验岗位「已上架」+ 企业非黑名单 + 未满员；回填 `enterprise_id/position_id/mentor` 与冗余名 + `destination=ASSIGNED`；**`position.allocated_count += 1`，满员自动置 FULL**；调岗释放旧岗位名额（旧--、新++，FULL 回落 PUBLISHED）；退岗释放名额。
- **状态机**：`准备中→待上岗(需资格合格)→在岗中(需已分配岗位)→考核中→已归档`；非法流转全拒。
- **反向补岗位库**：岗位详情返回 `assignedStudents` + `assignedCount`，`allocatedCount` 现为真实分配数。
- **API** `internship_student.py`（新，前缀 `/internship/intern-students/*` 避开旧 `/internship/students/*` 兼容路由，零冲突）：11 端点。
- 横切：租户隔离 + 软删 + 手机号脱敏 + 审计 `t_internship_audit_trail(target_type=INTERN_STUDENT)`；数据范围留 `resolve_teacher_scope` 接入点。

### 前端（生产级，仅真实后端）
- `internship-student.api.js`（real-only，call/callList，无 mock 兜底）+ `constants/internship-student.constants.js`。
- 重写 `InternshipStudentListView`（筛选/建档抽屉选学生/分配岗位抽屉选已上架岗位/资格认定/导入导出）与 `InternshipStudentDetailView`（企业·岗位·导师真实关联 + 分配/调岗/退岗/状态机/资格/去向 + 审计；企业/岗位可下钻企业库/岗位库）。路由不变。

## 验证（MySQL 真库）
- 后端：`pytest tests/test_internship_student.py` → **11 passed（MySQL `student_lifecycle_test`，drop/create 每测试，164s）**。覆盖：建档/重复拒绝、**分配→allocated_count 回填+已分配学生列表**、满员拒绝、黑名单拒绝、未上架拒绝、调岗(旧--新++)、退岗释放、状态机(资格门槛+上岗需岗位)、资格、去向(已分配拒改)、统计、导入。
- 前端：`eslint` 0 错、`npm run build` 通过。

## 关闭历史欠账
- ✅ **岗位库「allocated_count（已分配人数）」** —— 由本模块「学生-岗位分配」真实写入并触发 FULL；不再是预留占位。
- 岗位库「岗位专业匹配」等仍为独立模块欠账（本模块只做分配，不做智能匹配）。

## navPlan（诚实）
- **implemented**：实习名单/实习资格认定/学生实习状态/实习去向/学生岗位(分配·调岗·退岗)/学生企业/学生导师
- **planned**：学生打卡/学生周报/学生风险（属打卡/周报/风险独立模块）、学生材料/学生归档

## 改动文件
后端：`models/internship.py`(+5列)、`services/internship_student_service.py`(新)、`services/internship_position_service.py`(反向补已分配学生)、`schemas/internship_student.py`(新)、`api/v1/internship_student.py`(新)、`api/v1/router.py`(+注册)、`tests/test_internship_student.py`(新,11测试)
前端：`views/admin/internship/InternshipStudentListView.vue`(重写)、`InternshipStudentDetailView.vue`(重写)、`modules/internship/api/internship-student.api.js`(新)、`constants/internship-student.constants.js`(新)、`config/navPlan.js`(点亮)

## 待办（记账）
- 数据范围按辅导员/指导教师细分：`resolve_teacher_scope`。
- `t_internship_record` 新列纳入 alembic 迁移史（现靠 create_all；MySQL dev 库需 ALTER 或重建后生效，测试库 drop/create 已含新列）。
- batch_id 与实习批次联调（分配/筛选按批次）。
- 学生打卡/周报/风险/材料/归档：随对应模块开发再补学生视图下钻。


---

## 归档章节：2026-07-07-岗位实习-岗位库

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-07-岗位实习-岗位库.md

## 结论
**岗位库前后端真闭环，可交付。** 独立业务对象 `t_internship_position`，复用企业库共享主档 `t_emp_company`（不重复造企业表），8 态岗位状态机 + 黑名单/停用企业不能上架 + 专业/年级要求 + 容量/已分配(预留) + 企业导师关联 + 风险标记 + 真导入导出。后端 12 测试全绿，前端 lint 0 错 / build 通过 / 预览真渲染。已反向补企业库「企业岗位」入口与摘要。

## 与「实习批次」并行 — 隔离结论
- **是否文件冲突：无实质冲突。** 批次任务并行编辑了共享热文件（`schemas/internship.py`、`api/v1/internship.py`、`internship.api.js`、`internship.mock.js`、`routes.js`、`navPlan.js`、`docs/06-开发施工与质量验收/施工记录/README.md`）。我**全程未改批次代码/表/接口/测试/状态机**；岗位库用**独立新文件**避开批次的 schema/service/api/前端 api/mock：
  - 后端新文件：`models/internship_position.py`、`schemas/internship_position.py`、`services/internship_position_service.py`、`api/v1/internship_position.py`、`tests/test_internship_position.py`。
  - 前端新文件：`views/.../InternshipPositionListView.vue`、`InternshipPositionDetailView.vue`、`modules/internship/api/position.api.js`、`mocks/internship/position.mock.js`。
  - 仅对共享文件做**追加式**编辑（附冲突提示）：`models/__init__.py`(+1 import)、`api/v1/router.py`(+1 import +1 include)、`routes.js`(+2 路由，追加在 enterprises 之后)、`navPlan.js`(in-positions 点亮 + in-enterprises 企业岗位)。均为不同代码块的追加，与批次的追加互不覆盖。
- **README.md**：批次任务已将其重存为 UTF-16 乱码，按「发现批次在改只报告不处理」，本轮**未改 README**；台账更新待批次收口后由人工统一修复编码再补。
- **是否只预留 batch_id：是。** `t_internship_position.batch_id` 为 nullable 预留列，前端批次选择器仅按契约占位（显示"待实习批次模块联调"），不依赖批次已完成。

## 复用企业库（未重复造企业表）
- 岗位 `company_id` → `t_emp_company`；创建/上架时**直接读 EmpCompany** 校验：黑名单企业不能发布岗位、非「合作中」(coop_status≠ACTIVE)企业不能上架。
- 企业导师复用 `t_internship_enterprise_contact`（企业库能力），岗位 `mentor_contact_id` 关联，创建时校验导师属于该企业。
- 前端企业选择器/导师选择器复用企业库接口 `/internship/enterprises`、`/enterprises/:id/contacts`。

## 状态机（8 态，非法流转全拒并测试覆盖）
`DRAFT 草稿 →(提交) PENDING 待审核 →(上架) PUBLISHED 已上架 ↔(下架) OFFLINE ↔(暂停) SUSPENDED`；`PUBLISHED→FULL 满员`(容量预留)；任意非归档 `→ RISK 风险岗位`(标记须说明) / `→ ARCHIVED 已归档`。约束：已归档不能编辑/不能再变更状态；容量不能小于已分配；上架强约束企业黑名单/合作状态。全部写审计到 `t_internship_audit_trail`(target_type=POSITION)。

## 验证
- 后端：`pytest test_internship_position.py` → **12 passed**（CRUD/状态机全链/黑名单不可上架/非合作中不可上架/风险标记/容量/统计/导入dry-run+确认/导出/详情404/反向补企业岗位摘要）；`test_internship_enterprise + health + employment` → **23 passed**（零回归）。
- 前端：`eslint` 7 文件 → 0 错（navPlan 1 处 pre-existing 无害 warning，未动）；`npm run build` → 通过；预览实测：岗位列表 3 岗位真渲染（状态感知动作/风险徽标/容量 2/5）、企业详情「企业岗位」tab 显示 3 个岗位摘要。

## navPlan 状态（诚实标注）
- **implemented**：岗位列表 / 岗位详情 / 岗位要求(专业·年级) / 岗位容量 / 岗位发布(上架) / 岗位下架 / 岗位风险提示 / 岗位归档
- **partial（待补强）**：岗位统计（接口就绪，独立统计页待补）；企业库「企业岗位」「企业统计」「企业合作记录」
- **planned（待施工）**：岗位专业匹配（依赖「岗位匹配」in-match 模块）

## 待办（记账，不阻断交付）
- batch_id 联调：实习批次模块完成后，岗位创建/筛选按批次收口（现 nullable 预留）。
- 已分配人数 `allocated_count`：由后续「岗位分配」模块写入并触发 FULL；本模块已预留列与"满员不可上架"约束。
- 岗位统计独立页、岗位专业匹配：随对应模块补。
- alembic：`t_internship_position` 纳入迁移史（现靠 create_all）。


---

## 归档章节：2026-07-08-岗位实习-岗位匹配

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-08-岗位实习-岗位匹配.md

## 三家成熟系统对标表（CLAUDE.md §0.0）

| 对标对象 | 核心流程 | 角色 | 字段/亮点 | 缺点 |
|---------|---------|------|----------|------|
| A. 锦中/积正等顶岗实习 SaaS | 岗位发布→分配→过程统计 | 管理员/专业负责人/指导教师 | 专业要求、岗位容量、院系统计 | 匹配算法偏运营配置 |
| B. Kinobi 等高校实习平台 | 意向/申请→安排确认→仪表盘 | 学生/企业/校方 | 集中岗位库、确认流程、实时看板 | 中国职教专业目录适配弱 |
| C. 智慧校园/老系统证据（仓库设计） | 专业匹配字段→录用/分配→冲突台账 | 专业负责人审专业匹配 | 专业匹配审核、志愿/分配 | 智能推荐多为演示 |

**三家共性（本轮必须具备）**：岗位专业要求、学生意向/推荐、人工确认落岗、容量约束、统计与导出。  
**本轮吸收**：规则匹配（专业优先）+ 人工确认 + 复用已有 assign + Excel 台账。  
**不做**：假 AI 推荐、学生小程序意向端、完整 in-assign 二级模块。

## 本轮交付

1. **岗位库 · 岗位统计**：增强 `/positions/stats`（byMajor / 空专业上架数 / 容量利用率）+ 前端 stats 面板 KPI，navPlan → implemented。
2. **岗位匹配 in-match**：意向表 + 匹配结果表 + 规则引擎（专业/企业/手动/批量）+ 冲突/确认/驳回 + 统计 + Excel。
3. **岗位库 · 岗位专业匹配**：入口跳转 `/admin/internship/match?panel=major`（同一主链路）。

## 状态机

- 意向：`DRAFT → SUBMITTED ↔ WITHDRAWN`
- 匹配：`RECOMMENDED/PENDING_CONFIRM → CONFIRMED|REJECTED|CONFLICT|CANCELLED`
- 确认落岗：调用既有 `assign_position`（名额/满员/黑名单约束不变）

## 验证

- `pytest tests/test_internship_match.py` → **6 passed**（MySQL `student_lifecycle_test`）
- 与 `test_internship_position` 同跑曾遇 MySQL 并发 DDL（1684）→ setup ERROR，非业务回归；匹配单文件已绿
- `npm run build` → ✓ built in 7.45s
- 模型/API import 自检：`t_internship_intention` / `t_internship_match` OK

## navPlan

- 岗位统计、岗位专业匹配 → **implemented**
- in-match 二级 + 10 个三级 → **implemented**（统一 `/admin/internship/match?panel=*`）

## 历史欠账

- 关闭：岗位统计独立页、in-match PC 主链路待启动
- 未做完已记入 `docs/06-开发施工与质量验收/施工记录/历史欠账.md`「岗位实习中心 / 岗位匹配」：
  1. 学生端/小程序意向入口
  2. AI 智能推荐
  3. 细粒度 `internship.match.*` 后端拦截
  4. 学院/专业/业务关系数据范围
  5. 岗位专业结构化绑定 `t_major` + 专业匹配审核
  6. 未扩 `in-assign` 岗位分配二级模块
  7. 匹配结果 Excel 导入 + 公共 Excel 底座接入
  8. `0025` 正式库迁移 stamp/upgrade
  9. pytest 联合跑并发 DDL 基建


---

## 归档章节：2026-07-09-岗位实习-P3-A-归档中心

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-09-岗位实习-P3-A-归档中心.md

## 结论
新建实习归档中心：7 项材料完整性检查 + 缺失提醒 + 按学生/批次/企业聚合 + 归档动作（完整快照/强制归档/撤销）+ 归档台账导出，owner + 数据范围 + 审计。归档包 zip 打包能力 partial 预留（不伪造）。

## 三家成熟系统对标（无法联网，基于仓库资料 + 同类系统通行做法）
| 对标 | 归档做法 | 亮点 | 本项目取舍 |
|---|---|---|---|
| 工学云类顶岗平台 | 按学生汇总协议/打卡/周报/评价/成绩材料，完整性检查 + 缺件提醒 | 材料清单可视、缺件催办 | 7 项材料清单 + 完整度% + 缺失提醒 |
| 正方/新道类 | 按批次/专业归档统计、归档率 | 批次维度归档率 | 按批次/企业聚合（人数/完整/平均完整度/归档率） |
| 督学/青柠类 | 归档锁定 + 归档包下载 + 撤销归档 | 归档动作 + 撤销留痕 | 归档动作(完整快照)+强制归档(记缺失)+撤销归档+审计；归档包 partial 预留 |

## 已实现
- 表 `t_internship_archive`(alembic 0035) + `internship_archive_service`。
- 材料完整性（7 项）：三方协议(生效)/打卡/周报/企业评价(通过)/学生自评(已提交)/实习成绩(已发布)/指导记录；完整度=已有/7，缺失清单。
- 按学生列表（完整度 + 缺失 + 归档状态，可只看未完整）；按批次/按企业聚合（人数/材料完整数/平均完整度/已归档/归档率）。
- 归档动作：完整→直接归档；不完整→409，需 `force=true` 带缺失强制归档并记 missing；撤销归档需原因≥5字；归档时置记录 status=ARCHIVED，快照 material_snapshot。
- owner + 数据范围（`_current_scope`/`_rec_in_scope`）；审计 target_type=ARCHIVE；导出 xlsx 台账 + 水印 + 审计。
- 前端 `ArchiveView.vue`（按学生/批次/企业三 tab + 完整度条 + 材料清单详情 + 归档/撤销 + 导出）+ 路由 `/admin/internship/archive`。
- navPlan：`in-employment-archive-stats`「实习归档」PA→implemented 指向真实页。

## 验证
- pytest `test_internship_archive` 7 passed（完整度 100/14、缺失提醒、归档完整 vs 强制 409/force、撤销+重复撤销 409、材料清单、按企业聚合、数据范围隔离、学生 403、导出）。真实 MySQL（dev/test 升级至 0035）。
- 前端 `npm run build` 通过、`eslint` 0 error。
- 修复：`_completeness` 变量遮蔽 bug（`for _, _ in MATERIALS` → `for key, _label`）导致完整度恒 0，已修。

## partial / 预留（诚实标注）
| 项 | 状态 | 说明 |
|---|---|---|
| 归档包 zip 打包下载 | partial | `package_file_id` 字段预留；将各材料打包 zip 并落文件中心的能力未实现，不伪造上传成功。 |
| 归档缺失材料一键催办 | 待处理 | 当前缺失以提醒展示 + 强制归档记录；一键催办通知（复用 notify）后续补。 |

## 下一步
P3-C 公共组件全面接入既有 internship 页（AppPermissionButton/AppStatusTag/AppSensitiveText/AppAuditTrail/AppExportButton 等），不改业务流程。


---

## 归档章节：2026-07-09-岗位实习-P3-B-统计中心

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-09-岗位实习-P3-B-统计中心.md

## 结论
新建实习统计中心：15 项过程/结果指标 + 成绩分布，支持学院/专业/班级维度下钻，owner + 数据范围 + 阈值预警 + Excel 导出；实习工作台看板改为真实统计 API（去 mock 兜底）。指标口径对齐《06 页面树·岗位实习业务指标矩阵》。

## 三家成熟系统对标（无法联网，基于仓库资料 + 同类系统通行做法）
| 对标 | 统计做法 | 亮点 | 本项目取舍 |
|---|---|---|---|
| 工学云类顶岗平台 | 落实率/到岗率/打卡/周报/风险按学院专业班级下钻 | 过程监管齐全 + 下钻 | 15 项指标 + 学院/专业/班级筛选 |
| 正方/新道类 | 协议签署率/成绩发布率/评价完成率进统计与驾驶舱 | 合规与成绩闭环 | 签署率/成绩发布率/成绩分布/评价完成率 |
| 督学/青柠类 | 阈值预警标红 + 导出台账 | 预警可视 | 每指标带阈值 + warn；导出 xlsx 水印审计 |

## 已实现
- 后端 `internship_stats_service.py`：
  - 15 项比率指标（实习落实率/岗位匹配率/协议签署率/到岗率/打卡合规率/请假合规率/周报提交覆盖率/周报批阅率/指导覆盖率/巡访覆盖率/风险闭环率/企业评价完成率/学生自评完成率/成绩发布率），每项含分子/分母/达成率/阈值/warn。
  - 计数：实习学生数/在岗学生数/风险学生数。
  - 成绩分布（已发布）：优/良/中/及格/不及格 5 桶。
  - 维度：`overview(college, major, className)` + `dimension_options`（数据范围内可选组织，学生→班级→专业→学院 join，带缓存）。
  - 数据范围：复用 `_current_scope`/`_rec_in_scope`，SCOPED 指导教师只统计本范围，管理员全租户。
- API `GET /internship/stats/overview`、`GET /internship/stats/dimensions`、`POST /internship/stats/export`（require_staff 门禁 + 读审计）。
- 前端 `StatsView.vue`（AppMetricCard 指标网格 + 成绩分布条 + 学院/专业/班级筛选 + 导出）+ 路由 `/admin/internship/stats`。
- 实习工作台看板 `getDashboardSummary` 由 `real(mock 兜底)` 改 `realStrict`（接真实 `/internship/dashboard`，不回退 mock）；移除未用 mock 导入。
- navPlan：`in-employment-archive-stats` 的「实习统计」「学生成绩统计」PA→implemented，指向真实页。

## 验证
- pytest `test_internship_stats` 5 passed（指标口径=100/50、协议签署 1、成绩分布良=1、数据范围隔离 mentor vs admin、维度筛选 班级/学院、学生 403、导出）。真实 MySQL（dev/test）。
- 前端 `npm run build` 通过、`eslint` 0 error（navPlan 既有 line28 warning 非本轮引入）。

## partial / 预留（诚实标注，未虚构）
| 指标 | 状态 | 说明 |
|---|---|---|
| 就业转化率 / 未就业帮扶覆盖率 | partial | 依赖就业模块台账，overview 的 partial 列返回，前端明示「待就业中心对接」，不计算不伪造。 |
| 归档完成率 | partial | 依赖 P3-A 实习归档中心，待归档聚合完成后接入。 |
| 图表可视化 | partial | 当前成绩分布用轻量条形；AppChartCard 图表库未接入，复杂图表能力预留。 |
| 到岗率/周报提交/指导/巡访覆盖 | 口径说明 | 采用「有记录学生/在岗学生」近似覆盖率口径（无逐日应打卡计划），已在指标 note 标注，后续接批次日历可升级为精确「按时率」。 |

## 下一步
P3-A 实习归档中心（归档聚合 + 完整性检查 + 归档包 file_id 预留）；P3-C 公共组件全面接入既有页。


---

## 归档章节：2026-07-10-岗位实习-商业化升级设计总册

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/2026-07-10-岗位实习-商业化升级设计总册.md

## 任务性质

只读诊断 + 升级蓝图设计轮。**未改任何 frontend/backend/miniapp 代码，未改数据库。**

## 实际做了什么

1. 只读盘点五路：设计文档与施工进度、前端实况（navPlan/adminMenu/routes/31 视图/API 封装）、后端实况（27 表/约 195 端点/22 service/17 迁移/174 用例）、小程序与通用底座（文件中心/待办/审计/Excel/权限拦截）、外部对标（三家成熟商业实习平台公开资料 + 教职成〔2021〕4号 + 招标典型需求）。
2. 新建 `docs/03-业务模块设计/岗位实习中心/06-岗位实习中心生产级商业化升级总册.md`（14 章）：差距诊断报告、58 动作端到端业务闭环表、12 二级 96 三级页面树（I=63/P=17/PL=16）、页面体验升级表、22 个状态机、角色权责矩阵、企业侧 H5 MVP 方案、风险中心 18 类规则、24 指标驾驶舱、归档与试点验收 20 项、配置化 15 项、通知待办 15 类、P3-A～P7 分阶段路线、新增欠账登记。
3. `docs/06-开发施工与质量验收/施工记录/历史欠账.md` 追加「商业化差距诊断新增欠账」12 条（与既有欠账去重）。

## 关键诊断结论

- 管理端主链高完成度真闭环；小程序学生端真闭环。
- 五大商业化缺口：企业侧结构性缺位、route 级 permissionCode 未落地、风险自动识别引擎缺失、监管合规校验缺失（8h/44h/知情同意书/报酬台账/备案）、试点验收交付物缺失。
- 两处口径问题：navPlan 三级全标 I 虚高；adminMenu 第 12 二级挂 EMPLOYMENT 授权 key（授权耦合）。
- 五级申请审核状态机为最大设计-施工偏差（P5 反向补全）。

## 检查命令

设计文档轮，无代码改动，未运行 build/pytest（无必要）。

## 下一步

经用户确认后进入 **P3-A 实习工作台**（真实聚合接口 + 三视角 + 15 项验收），见总册第 13 章。


---

## 归档章节：岗位实习中心-生产级全栈施工卡

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/岗位实习中心-生产级全栈施工卡.md

> 创建：2026-07-09 ｜ 依据：只读盘点（3 份施工记录 + navPlan/adminMenu + 设计文档 06 + 后端/前端/小程序三路代码审计 + mobile.py 端点核对）
> 用途：岗位实习中心长跑开发的分阶段施工总控 + 进度记账。本文件只记录，不代表已实现；状态以 implemented/partial/planned/blocked 为准。

---

## 一、当前完成度总评（约 45–55%）
- **已生产级闭环（6，MySQL 真库 + 测试绿）**：实习批次、实习学生、企业库、岗位库、岗位匹配、实习协议模板库。
- **移动端真实自服务**：学生打卡/周报（`/mobile/internship/checkin|weekly` 真实落库）、教师周报批阅+打卡异常（`/mobile/teacher/internship/*`）。
- **薄/挂 mock 的 PC 页**：实习看板、打卡异常台账、周报批阅台账、风险学生看板（real-first + mock fallback）。
- **planned（无页无接口）**：请假异常、指导记录、巡访、企业评价、学生评价/鉴定、实习成绩、实习申请审核、岗位分配（独立）、实习计划、实习归档（独立）、实习统计（独立中心）。
- **归就业域**：就业跟进、未就业帮扶（`/admin/employment/*`）。

## 二、试点结论
- **PC 管理端**：6 个闭环模块可受控试点（strict-real、状态机严谨、xlsx/台账齐、pytest 绿）。
- **学生小程序**：真实页在，`env.js useMock:true` 需切 false + 起后端方可试点（realApi URL 经复核**无反斜杠 bug**，见下）。
- **教师小程序**：真实页在，切 useMock:false + 后端可达即可试点。
- **正式上线**：否（见欠账）。

## 三、各子模块状态表
| 子模块 | 前端 | 后端 | MySQL | 状态机 | 门禁 | 数据范围 | 审计 | Excel | 状态 |
|---|---|---|---|---|---|---|---|---|---|
| 实习看板 | ✓mock兜底 | ✓dashboard | 读多表 | — | ✅(P0已加) | 租户 | 读 | — | partial |
| 实习批次 | ✓strict | ✓ | t_internship_batch | ✓5态 | ✅ | 租户 | ✓ | 导出xlsx | implemented |
| 实习学生 | ✓strict | ✓ | t_internship_record | ✓5态+资格+去向 | ✅ | 租户 | ✓ | **CSV(债)** | implemented |
| 企业与岗位 | ✓strict | ✓ | emp_company+contact+position+match | ✓6/8态 | ✅ | 租户 | ✓ | 全xlsx脱敏 | implemented |
| 实习协议 | ✓模板 | ✓仅模板 | agreement_template | ✓4态 | ✅ | 租户 | ✓ | — | partial(无签署实例) |
| 打卡管理 | ✓仅异常 | 部分 | attendance_exception+checkin(未被PC用) | 异常2态 | ✅ | 租户 | ✓ | — | partial |
| 周报批阅 | ✓mock兜底 | ✓list/detail/review | weekly_report | ✓4态 | ✅ | 租户 | ✓ | — | partial |
| 请假异常 | ✗ | 无请假模型 | — | — | — | — | — | — | planned |
| 风险学生 | ✓工具条占位 | 仅list | risk_record(有全生命周期字段) | 无流转API | ✅ | 租户 | 异常自动生成 | — | partial |
| 指导记录 | ✗ | 完全缺失 | 无 | — | — | — | — | — | planned |
| 就业跟进/未就业帮扶 | ✓就业域 | ✓employment | emp_student/followup/material | ✓ | ✅ | 租户 | ✓EmpAuditTrail | — | implemented(就业域) |
| 实习归档 | ✗独立 | 部分(批次归档+状态) | 归档字段散在 | 归档态 | ✅ | 租户 | ✓ | — | partial |
| 实习统计 | ✗独立中心 | 各模块stats+dashboard | 聚合 | — | ✅ | 租户 | 读 | 各模块导出 | partial |
| 学生小程序入口 | ✓真实 | ✓/mobile/internship/* | 真实落库 | — | 登录 | 本人 | ✓ | — | partial(useMock) |
| 教师小程序入口 | ✓真实 | ✓/mobile/teacher/internship/* | 真实 | — | 登录 | 本人指导 | ✓ | — | partial(useMock) |

## 四、三家成熟系统对标结论（A 锦中/积正顶岗实习 · B Kinobi · C 智慧校园/老系统）
三家共性必备 40 项能力中，本项目已具：实习计划/批次、企业准入、岗位发布、分配、周报批阅、就业衔接、Excel 台账；**尚缺**：岗位申请多级审核、三方协议签署实例、定位打卡防作弊台账+补卡、请假审批、教师巡访/指导、企业评价、学生鉴定、实习成绩五项权重、归档统计、企业导师门户、数据范围细分。
**待复刻老系统精华资产**：① 定位打卡防作弊字段全集（is_mock/device_id/distance/risk）；② 岗位申请五级状态机；③ 三方协议双状态；④ 教师巡访月报；⑤ 实习成绩五项权重（checkin/weekly/monthly/enterprise/school + pass_line）；⑥ 企业导师最小授权门户。

## 五、施工顺序
- **P0 安全底座与可达性收口**（进行中，见 §八）
- **P1 过程监管闭环**：请假异常、指导记录、风险处置全流程、PC 打卡台账+补卡审批
- **P2 协议评价成绩**：三方协议签署实例、企业评价、学生鉴定、实习成绩五项权重
- **P3 归档统计与商业 UI 统一**：实习归档中心、实习统计中心、公共组件全面接入
- **P4 生产级验收报告 + 成熟商业对标达成表**

## 六、公共组件接入（当前几乎为零，最大改造机会）
现状：16 个实习页只用了 `AppConfirmDialog`+`AppTimeline`；权限/状态/脱敏/审计/导出全手写。新开发/改造一律按 CLAUDE.md §40/§41 接入：AppPageShell/AppSectionCard/AppToolbar、AppPermissionButton、AppSensitiveText、AppAuditTrail、AppExportButton/AppExportConfirm、AppFileList/AppFilePreview、AppBatchActionBar、AppApprovalPanel、AppStatusTag/AppRiskTag、AppStudentPicker/AppTeacherPicker/AppCompanyPicker/AppPositionPicker/AppBatchPicker、AppDescriptionList/AppOperationResult/AppMetricCard/AppWorkflowTimeline。

## 七、不能动 / 红线
已闭环 6 模块的表/状态机/服务/测试、共享主档 `t_emp_company`、审计表、真实数据、毕业设计中心成果、公共组件；禁止重写 DataTable/AdvancedFilter/AppDrawer/日期组件/Excel 管道。

---

## 八、P0 执行记录（2026-07-09，本轮只改不提交，提交由用户决定）

> 前置：工作区有 147 项未提交混杂 WIP（岗位实习整套代码本身未提交 + 迎新/学工/教务后端 + 23 个删档）。已向用户报告硬红线，用户选择「我先只改不提交，提交你来定」。本轮全程未碰迎新/学工/教务/删档。

| P0 项 | 结论 | 证据 |
|---|---|---|
| **P0-C require_staff 门禁** | ✅ **已做+已验证** | `router.py` 给 internship/internship_position/internship_agreement_template/internship_student/internship_match/employment 六个 router 加 `_INTERN_DEP=[Depends(require_staff)]`（mobile 不加，学生自服务不受影响）；新增 `tests/test_internship_staff_gate.py` → 学生403/未登录401/教职工200 **3 passed**；回归 `test_internship.py` **9 passed** 无破坏。 |
| **P0-B 路由全局挂载** | ✅ **本已挂载（无需改）** | `router/index.js:18,37` 已 import + 挂 `internshipRoutes`；11 个子页刷新直达可达。routes.js 顶部「未接入全局 router」注释为**过期误导注释**（建议后续删注释）。任务验收表里的 companies/matching/weekly/attendance/risk 实际路径为 enterprises/match/reports/exceptions/risks，均存在。 |
| **P0-F 小程序 URL bug** | ✅ **经复核不存在（无需改）** | `miniapp/src/services/realApi.js` L68/78/88/244/248 **均为正斜杠**（`/mobile/internship/weekly` 等）；盘点报告所述「反斜杠 URL」为子代理误读。`env.js useMock:true` 为 dev 有意开关，非 bug；切真实需运维置 false + 起后端。 |
| **P0-G alembic 评估** | ⚠️ **记账（上线前必补）** | 仅 `0016_internship_agreement_template`、`0025_internship_match` 有迁移；核心表 t_internship_batch/record/attendance_exception/weekly_report/risk_record/position、emp_company 实习附加列、enterprise_contact、intern-student 附加列 **靠 create_all**。本轮不补（entangled WIP + 需对齐模型 autogenerate，风险高）。 |
| **P0-A 文档** | ✅ 本文件 + 历史欠账追加 | — |
| **P0-D 数据范围细分** | ⏭ **降级顺延 P0.5**（记账） | 全域仅租户，无指导教师/辅导员/学院过滤。改动落在 WIP 未提交的 service 上、需大量 MySQL 测试、且无法干净提交；按 §十一降级规则顺延，规格见欠账。 |
| **P0-E 实习学生 CSV→xlsx** | ⏭ **降级顺延 P0.5**（记账） | `internship_student.py` 导出仍 CSV、导入 JSON。需接公共 Excel 底座；同上顺延，规格见欠账。 |

**本轮验证**：前端 `npm run build` ✓（5.41s）；后端 `test_internship_staff_gate.py` 3 passed + `test_internship.py` 9 passed（MySQL 真库）。
**本轮改动文件**：`backend/app/api/v1/router.py`（+门禁）、`backend/tests/test_internship_staff_gate.py`（新）、`docs/03-业务模块设计/岗位实习中心/施工记录/岗位实习中心-生产级全栈施工卡.md`（本文件·新）、`docs/06-开发施工与质量验收/施工记录/历史欠账.md`（追加）。**未提交**（工作区混杂，提交由用户决定）。


---

## 归档章节：岗位实习中心-长跑TODO清单

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/岗位实习中心-长跑TODO清单.md

> 落盘：2026-07-09 ｜ 配套 `岗位实习中心-长跑开发总控任务书.md`。每项含：改哪些文件 / 新增接口 / 新增表字段 / 补哪些测试 / 用哪些公共组件 / 验收命令 / commit 建议。
> 状态标记：`[x]`=已完成 `[~]`=partial `[ ]`=未开工。**进入每阶段前先重读 §2 前置协议并输出「本阶段施工计划」。**
> 通用验收命令（后端·MySQL 测试库）：`cd backend && .venv/Scripts/python.exe -m pytest tests/<file> -q`；前端：`cd frontend && npm run build && npm run lint`。
> 通用公共组件（除特别注明，新页一律用）：AppPageShell/AppSectionCard/AppToolbar、AppPermissionButton、AppStatusTag/AppRiskTag、AppSensitiveText、AppAuditTrail、AppExportButton/AppExportConfirm、AppDescriptionList、AppBatchActionBar、AppOperationResult、AppStudentPicker/AppTeacherPicker/AppCompanyPicker/AppPositionPicker/AppBatchPicker、AppFileList/AppFilePreview（附件预留）。

---

## P0 · 安全底座与可达性收口

### [x] P0-C 管理端 require_staff 门禁 ✅ 已完成（2026-07-09）
- 文件：`backend/app/api/v1/router.py`（+`_INTERN_DEP`，给 internship/internship_position/internship_agreement_template/internship_student/internship_match/employment 加门禁）
- 测试：`backend/tests/test_internship_staff_gate.py`（学生403/未登录401/教职工200）→ **3 passed**；回归 `test_internship.py` **9 passed**
- 验收：`pytest tests/test_internship_staff_gate.py tests/test_internship.py -q`
- commit 建议：`fix(internship): enforce require_staff on management routers`

### [x] P0-B 路由全局挂载 ✅ 已核实（无需改）
- `frontend/src/router/index.js:18,37` 已挂 `internshipRoutes`；11 子页刷新直达可达。建议后续删 `routes.js` 顶部过期注释。

### [x] P0-F 小程序 URL / useMock ✅ 已核实（无 bug）
- `miniapp/src/services/realApi.js` 写接口均正斜杠；`env.js useMock:true` 为 dev 开关。切真实：置 false + 起后端，手动验打卡/周报/批阅/异常真实落库。

### [ ] P0-D 管理端数据范围细分  ← **下一步优先**
- 改文件：`backend/app/services/internship_service.py`、`internship_student_service.py`、`employment_service.py`（在 list/detail/dashboard/weekly/exception/risk/match 的 tenant 过滤后叠加范围过滤）；复用 `resolve_teacher_scope`（毕设/学工同款）。
- 新增接口：无（不改入参/返回结构；仅收窄可见行）。
- 新增表/字段：无（用现有 advisor_name/counselor/college 关系；若缺关联，评估最小加列并记 alembic）。
- 补测试：`backend/tests/test_internship_scope.py`（教师A看不到教师B学生 / 学院负责人看本院 / 管理员看全校 / 学生管理接口403 / 跨租户不可见）。
- 公共组件：无（后端）。
- 验收：`pytest tests/test_internship_scope.py tests/test_internship_student.py -q`
- commit 建议：`feat(internship): apply data scope to management APIs`

### [ ] P0-E 实习学生 CSV→xlsx
- 改文件：`backend/app/api/v1/internship_student.py`（export→xlsx、import→xlsx 上传+dry-run+confirm+errors-xlsx+template）、`internship_student_service.py`；接 `backend/app/services/excel/`（ImportSpec/ExportSpec）；前端 `frontend/src/modules/internship/api/internship-student.api.js`、`InternshipStudentListView.vue`（导入导出按钮改 xlsx）。
- 新增接口：`POST /internship/intern-students/import/xlsx|errors-xlsx`、`GET /internship/intern-students/import/template`（export 已有→改 xlsx 输出）。
- 表/字段：无。
- 模板 15 列：学号/姓名/学院/专业/班级/指导教师/企业名称/岗位名称/实习批次/实习开始/实习结束/联系电话/实习状态/就业去向/备注。
- 补测试：`test_internship_student.py` 增 xlsx 模板+上传+错误行+导出脱敏用例。
- 公共组件：前端用 AppExportButton/AppExportConfirm；导入用公共 Excel 前端底座（`components/common/excel/`）。
- 验收：`pytest tests/test_internship_student.py -q` + 前端 build/lint。
- commit 建议：`feat(internship): replace student CSV with xlsx import/export`

### [~] P0-G alembic 迁移（上线前必补，记账）
- 为 t_internship_batch/record/attendance_exception/weekly_report/risk_record/position、emp_company 实习附加列、t_internship_enterprise_contact、intern-student 附加列补 `backend/alembic/versions/00XX_*.py`；服务器迁移前备份。分类：上线前必补。

---

## P1 · 过程监管闭环（P0 通过后）

### [ ] P1-A 请假异常
- 文件：新 `backend/app/models/internship_leave.py`(t_internship_leave)、`schemas/internship_leave.py`、`services/internship_leave_service.py`、`api/v1/internship_leave.py`（+router.py 注册加 `_INTERN_DEP`）；前端新 `views/admin/internship/InternshipLeaveListView.vue`+detail、`modules/internship/api/leave.api.js`、`routes.js`、`navPlan.js`(in-leave 点亮)；小程序学生请假入口（若接）。
- 接口：list/detail/create/submit/approve/reject/withdraw/cancel(销假)/archive/export(xlsx)。
- 表字段：student_id/batch_id/leave_type/start/end/reason/status/approver/审计字段/file_id 预留；与 t_attendance_exception 联动（请假期间打卡缺卡不计异常）。
- 状态机：待提交→待审核→已通过/已驳回/已撤回→已销假→已归档（非法流转后端拒 + 审计）。
- 测试：`test_internship_leave.py`（状态机全链/越权/数据范围/xlsx/审计/与打卡联动）。
- 公共组件：AppApprovalPanel（审批）、AppStatusTag、通用清单。
- 验收：`pytest tests/test_internship_leave.py -q` + 前端 build/lint。
- commit：`feat(internship): add leave exception workflow`

### [ ] P1-B 指导记录
- 文件：新 model `t_internship_guidance`、schema、service、api（+注册门禁）；前端 `InternshipGuidanceListView.vue`+detail、api、routes、navPlan(in-guidance)；教师小程序入口（若接 `/mobile/teacher/internship/guidance`）。
- 接口：list/detail/create/update/void/export(xlsx)。
- 字段：指导教师/学生/企业/岗位/批次/指导方式(线上·电话·现场·企业导师反馈·视频)/主题/内容/问题类型/处理建议/下次跟进日期/是否形成风险/是否通知辅导员/file_id 预留/审计。
- 测试：`test_internship_guidance.py`（CRUD/数据范围/是否转风险/xlsx/审计）。
- 公共组件：AppFilePreview/AppFileList（附件）、AppTeacherPicker/AppStudentPicker、AppStatusTag。
- 验收：`pytest tests/test_internship_guidance.py -q` + build/lint。
- commit：`feat(internship): add guidance records workflow`

### [ ] P1-C 教师巡访（成熟商业对标项）
- 文件：新 model `t_internship_visit`、schema、service、api（+门禁）；前端页+api+routes+navPlan(in-visit)。
- 接口：list/detail/plan/create/update/rectify-followup/export(xlsx)。
- 字段：巡访计划/对象/企业/时间/方式/企业反馈/学生反馈/安全隐患/整改要求/整改截止/整改状态/巡访月报/file_id 预留/审计。
- 测试：`test_internship_visit.py`（整改状态机/数据范围/xlsx/审计）。
- 公共组件：同上 + AppWorkflowTimeline（整改跟进）。
- commit：`feat(internship): add teacher visit records`

### [ ] P1-D 风险处置全流程（现仅 list）
- 文件：`backend/app/services/internship_service.py`（补 risk create/handle/close/escalate/followup/复盘 + 状态机）、`api/v1/internship.py`（+ risk 动作端点）；前端 `InternshipRiskView.vue`（工具条 toast 占位改真实动作 + 权限按钮 + 数据范围）。
- 接口：`POST /internship/risks`、`/risks/{id}/handle|close|escalate|followup`、`GET /risks/{id}`、`POST /risks/export`(xlsx)。
- 表：复用 t_risk_record（已有 PENDING_HANDLE/PROCESSING/RESOLVED/CLOSED + owner/deadline 字段）。
- 状态机：待处理→处理中→已升级/已关闭→已归档。
- 测试：`test_internship_risk.py`（创建/自动风险/流转/关闭/升级/数据范围/xlsx/审计）。
- 公共组件：AppApprovalPanel/AppBatchActionBar/AppRiskTag/AppPermissionButton/AppAuditTrail。
- commit：`feat(internship): complete risk handling workflow`

### [ ] P1-E PC 打卡台账 + 补卡审批（成熟商业·防作弊字段）
- 文件：`services/internship_service.py`（打卡台账查询 over t_internship_checkin + 补卡状态机）、`api/v1/internship.py`（+ checkin 台账/补卡端点）；新 model 或复用 t_internship_checkin 补 makeup 字段；前端 `InternshipAttendanceLedgerView.vue`（明细/异常筛选/补卡审批）+ 打通移动端真实打卡数据。
- 接口：`GET /internship/checkins`(台账，多筛选)、`POST /internship/checkins/makeup`(补卡申请)、`/checkins/makeup/{id}/approve|reject`、`POST /internship/checkins/export`(xlsx)。
- 表字段（防作弊全集，缺对象存储/地图则字段预留+标 partial）：student/batch/enterprise/position/打卡时间/类型(上班·下班·外勤·补卡)/经纬度/地址/与企业距离/超范围/设备ID/is_mock/异常设备/重复打卡/风险等级/异常原因/处理状态/补卡关联/审计。
- 状态机：补卡 待审→通过/驳回。
- 测试：`test_internship_attendance.py`（台账筛选/补卡审批/防作弊字段/数据范围/xlsx/审计；不伪造定位照片）。
- 公共组件：AppStatusTag/AppRiskTag/AppBatchActionBar/AppExportButton/AppSensitiveText。
- commit：`feat(internship): add attendance ledger and makeup approval`

### [ ] P1-F 岗位申请五级审核 + 换岗/退岗（成熟商业·五级状态机）
- 文件：新 model `t_internship_application`(五级)、schema、service、api（+门禁）；换岗/退岗接 t_internship_record + t_internship_position(名额)；前端 `InternshipApplicationView.vue` + navPlan(in-apply/in-assign)；学生小程序申请入口预留。
- 接口：apply/submit/teacher-review/enterprise-confirm/school-confirm/hire/reject/withdraw、change-post/quit-post。
- 状态机：待学生提交→待指导教师审核→教师同意→待企业确认→企业已确认→学校已确认→已录用/已分配；旁支 已驳回/学生撤回/已换岗/已退岗/已归档。每态明确操作人/按钮/下一态/非法拒绝/审计/pytest。
- 测试：`test_internship_application.py`（五级流转 + 每态操作人 + 非法拒绝 + 换岗名额 + 数据范围）。
- 公共组件：AppApprovalPanel/AppWorkflowTimeline/AppStatusTag/AppPermissionButton。
- commit：`feat(internship): add multi-level job application workflow`

---

## P2 · 协议、评价、成绩闭环（P1 通过后）

### [ ] P2-A 三方协议签署实例（现仅模板库·双状态）
- 文件：新 model `t_internship_agreement`(实例，区别于 template)、schema、service、api（+门禁）；前端协议实例页 + navPlan(in-agreement 补 planned 叶子)。
- 接口：模板选择/实例生成/学生确认/企业确认/学校确认/上传(file_id)/查看/下载/作废/归档/export(xlsx)。
- 字段：template_id/student/enterprise/position/batch/学生确认状态/企业确认状态/学校确认状态/协议总状态/file_id 预留/审计。
- 状态机（总）：草稿→待学生确认→待企业确认→待学校确认→已生效→已驳回/已作废/已归档；无电子签章则「确认流+归档+电子签预留」标 partial。
- 测试：`test_internship_agreement.py`（双状态流转/非法拒绝/数据范围/xlsx/审计）。
- 公共组件：AppApprovalPanel/AppFileList/AppFilePreview/AppStatusTag。
- commit：`feat(internship): add agreement signing workflow`

### [ ] P2-B 企业评价（企业导师最小授权预留）
- 文件：新 model `t_internship_enterprise_eval`、schema、service、api；前端页；**企业导师门户权限预留**（只看授权学生/只填评价/操作审计，不做企业端页面也须权限+文档预留，禁学校老师代填假闭环）。
- 字段：企业导师/学生/岗位/批次/出勤·技能·态度·协作·安全纪律评价/综合评语/是否建议录用/提交状态/学校审核/审计。
- 测试：`test_internship_enterprise_eval.py`（提交/学校审核/授权边界/数据范围/导出）。
- 公共组件：AppApprovalPanel/AppStatusTag/AppSensitiveText。
- commit：`feat(internship): add enterprise evaluation`

### [ ] P2-C 学生鉴定 / 学生评价
- 文件：新 model `t_internship_student_eval`、schema、service、api；前端页。
- 字段：学生自评/实习总结/收获/问题/企业导师意见/指导教师意见/学校审核/审计/归档。
- 测试：`test_internship_student_eval.py`。
- commit：`feat(internship): add student self-appraisal`

### [ ] P2-D 实习成绩（五项权重·成熟商业核心）
- 文件：新 model `t_internship_score_config`(权重) + `t_internship_final_score`、schema、service(核算)、api；前端成绩页。
- 字段：打卡/周报/月报总结/企业评价/学校评价权重 + 指导教师评分 + pass_line + 五项分 + total + incomplete + 状态。
- 状态机：待核算→待复核→已发布→已撤回→已归档。
- 测试：`test_internship_score.py`（**权重和=100 / 缺项不能发布 / 计算正确 / 复核留痕 / 非法发布拒 / 数据范围隔离**）。
- 公共组件：AppDescriptionList/AppMetricCard/AppStatusTag/AppApprovalPanel。
- commit：`feat(internship): add internship grade calculation`

---

## P3 · 归档统计与商业 UI 统一（P2 通过后）

### [ ] P3-A 实习归档中心
- 文件：新 service/api 归档聚合（按学生/批次/企业 + 材料完整性检查 + 缺失提醒 + 归档包 file_id 预留）；前端归档中心页 + navPlan(in-archive)。
- 接口：archive-by-student/batch/enterprise、check-completeness、export(xlsx)。
- 测试：`test_internship_archive.py`（完整性检查/缺失提醒/无 file_id 不宣称完成/审计）。
- 公共组件：AppFileList/AppOperationResult/AppStatusTag。
- commit：`feat(internship): add archive center`

### [ ] P3-B 实习统计中心
- 文件：新 stats service/api（实习覆盖率/岗位匹配率/打卡异常率/周报提交率/风险学生数/协议签署率/成绩分布/就业转化率/企业合作质量 + 学院·专业·班级筛选）；前端统计中心页 + navPlan(in-stats)。
- 测试：`test_internship_stats.py`（各指标口径/维度筛选/数据范围）。
- 公共组件：AppMetricCard/AppChartCard(partial 未接图表库则外壳)/AppDescriptionList。
- commit：`feat(internship): add statistics center`

### [ ] P3-C 公共组件全面接入（低风险改造既有 16 页）
- 文件：`frontend/src/views/admin/internship/*`（手写权限按钮→AppPermissionButton、状态→AppStatusTag/AppRiskTag、脱敏 maskNo()→AppSensitiveText、AuditTrailPanel→AppAuditTrail、导出→AppExportButton/AppExportConfirm、附件→AppFileList、审核区→AppApprovalPanel、批量→AppBatchActionBar、骨架→AppPageShell/AppSectionCard/AppToolbar）。
- 约束：不改业务流程/接口/状态机；不重写 DataTable/AdvancedFilter/AppDrawer。
- 验收：build/lint + `/dev/components` 对照 + 无 console error + 页面 smoke test。
- commit：`refactor(internship): adopt shared SaaS components`

---

## P4 · 最终全量验收

### [ ] P4 生产级长跑报告 + 《成熟商业岗位实习系统对标达成表》
- 输出报告 25 项（阶段/commit hash/文件/接口/表字段/迁移/前端页/小程序/测试/权限结果/数据范围结果/xlsx 结果/审计结果/build/lint/pytest/implemented/partial/blocked/不能上线项/PC 试点/学生小程序/教师小程序/是否打 tag/剩余混杂说明/是否 push=否）。
- 《对标达成表》列：能力项 / 成熟系统应有 / 本项目是否实现 / 状态 / 页面 / 接口 / 表 / 测试 / 是否可试点 / 是否可上线 / 未完成原因。覆盖 28 能力项（实习计划…小程序教师端）。
- 全量验收：`cd backend && .venv/Scripts/python.exe -m pytest tests/test_internship*.py tests/test_employment.py -q`（MySQL 并发 DDL 报错则单文件跑并说明）；`cd frontend && npm run build && npm run lint`。


---

## 归档章节：岗位实习中心-长跑开发总控任务书

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/岗位实习中心-长跑开发总控任务书.md

> 落盘：2026-07-09 ｜ 用途：岗位实习中心生产级长跑开发的**唯一权威总控**，防止长跑遗忘/跑偏/漏项。
> 配套：`岗位实习中心-长跑TODO清单.md`（机器可执行）、`岗位实习中心-生产级全栈施工卡.md`（盘点+P0记录）、`历史欠账.md`。
> 本任务书 = 长跑总提示词 + 成熟商业软件增强规则的合并落盘。两者冲突时，**以"成熟商业闭环/权限安全/真实数据/公共组件/审计/Excel/状态机"为准**。

---

## 0. 最高目标（不是"能打开几个页面"）
1. 学校管理员能管：批次、学生、企业、岗位、匹配、协议、打卡、周报、请假、指导、风险、评价、成绩、归档、统计；
2. 指导教师能看**本人负责学生**，批周报、处理异常、写指导、看风险；
3. 学生小程序能打卡、交周报、看岗位和协议，后续可申请请假/补卡；
4. 企业导师能力**预留清楚**（最小授权，只看授权学生，不做假闭环）；
5. 权限 + 数据范围**真实生效**；Excel + 审计**真实生效**；UI 达商业销售截图标准；
6. **不能正式上线的地方必须诚实标 partial，绝不把半成品混进 implemented。**

## 1. 施工顺序（不跳阶段）
```
P0  安全底座与可达性收口
P1  过程监管闭环
P2  协议、评价、成绩闭环
P3  归档统计与商业 UI 统一
P4  最终全量验收与提交报告
```
先安全/路由/数据范围/真实导入导出/小程序硬 bug；**不要先做花哨 UI、AI 推荐、统计大屏。**

## 2. 每阶段前置协议（防止凭记忆施工）
进入任一阶段**之前必须**：
1. 重新读取：`CLAUDE.md`、本任务书、`岗位实习中心-长跑TODO清单.md`、`docs/02-总体架构与公共底座/公共组件/00-高校SaaS公共组件商业化底座总控表.md`、`历史欠账.md`；
2. 先输出**「本阶段施工计划」**（要改的文件 / 新增接口 / 新增表字段 / 测试 / 公共组件 / 验收命令 / commit 建议 / 本阶段成熟商业对标项）；
3. 再开始改代码。
**不凭记忆施工、不跳阶段、不漏对标项、不把 partial 说成 implemented。**

## 3. 成熟商业岗位实习系统必备 40 项能力（对标基线）
实习计划/批次 · 企业准入/企业库 · 岗位发布/岗位库 · 岗位申请 · 岗位申请多级审核 · 岗位匹配/分配 · 换岗/退岗/调岗 · 三方协议发起 · 学生确认 · 企业确认 · 学校确认 · 协议归档 · 定位打卡 · 打卡防作弊 · 打卡异常 · 补卡申请 · 补卡审批 · 周报提交 · 周报批阅 · 请假申请 · 请假审批 · 指导记录 · 教师巡访 · 企业导师反馈 · 风险学生发现 · 风险处置 · 风险关闭 · 企业评价 · 学生自评/鉴定 · 实习成绩计算 · 实习归档 · 实习统计 · 就业去向衔接 · 企业导师最小授权 · Excel 导入导出 · 审计追踪 · 权限门禁 · 数据范围 · 附件/文件中心预留 · 小程序学生端/教师端真实闭环。
> 某项本轮不能完全实现 → 标 partial/blocked，**不得假装 implemented**。

## 4. 必须复刻的老系统 6 大精华资产（设计文档 06 已证）
1. **定位打卡防作弊字段全集**：student_id/batch_id/enterprise_id/position_id/打卡时间/类型(上班·下班·外勤·补卡)/经纬度/地址/与企业距离/是否超范围/设备ID/疑似模拟定位(is_mock)/异常设备/重复打卡/风险等级/异常原因/处理状态/补卡关联/审计。无真实地图/照片/对象存储时**字段预留 + 标 partial**。
2. **岗位申请五级状态机**：待学生提交→待指导教师审核→教师同意→待企业确认→企业已确认→学校已确认→已录用/已分配；旁支：已驳回/学生撤回/已换岗/已退岗/已归档。每态明确「谁能操作/能点什么/下一态/非法拒绝/写审计/pytest 覆盖」。
3. **三方协议双状态**：协议总状态(草稿/待学生确认/待企业确认/待学校确认/已生效/已驳回/已作废/已归档) + 签署人状态；无电子签章时做「确认流+文件归档+电子签预留」标 partial。
4. **教师巡访月报**：巡访计划/对象/企业/时间/方式/企业反馈/学生反馈/安全隐患/整改要求/整改截止/整改状态/巡访月报/审计/导出。
5. **实习成绩五项权重**：打卡分/周报分/月报总结分/企业评价分/学校评价分(+指导教师评分)/权重配置/及格线/自动核算/人工复核/发布/撤回/归档/审计/导出。pytest：权重和=100、缺项不能发布、计算正确、复核留痕、非法发布拒、数据范围隔离。
6. **企业导师最小授权门户预留**：企业导师身份/只看授权学生/可填评价/可反馈/可确认协议/不能看学校其他数据/操作审计。不做企业端页面也必须在权限与文档预留，**不得把企业评价做成学校老师代填的假闭环**。

## 5. 生产级红线（每条都是 implemented 的前置）
- **权限门禁**：管理端接口无 require_staff 不得标 implemented。测试：未登录401/学生403/教职工放行。
- **数据范围**：指导教师只看本人指导学生、辅导员看本范围、学院负责人看本院、管理员看全校、学生看本人、跨租户隔离、企业导师只看授权学生。优先复用 `resolve_teacher_scope`。
- **状态机**：每业务流程有状态枚举+操作人+允许动作+非法拒绝(后端)+前端按钮只做体验+每次流转写审计+pytest 覆盖。
- **Excel**：正式台账一律 xlsx（禁 CSV 冒充）；模板下载/行级校验/错误行反馈/导入记录/导出脱敏/水印/审计/pytest。逐步覆盖：学生/企业/岗位/匹配/打卡/周报/请假/指导/风险/协议/评价/成绩/归档台账。
- **附件**：无真实对象存储时只做 file_id 预留 + AppFileList/AppFilePreview 展示 + 附件状态 + TODO；**禁止假上传成功、禁止只存文件名就宣称附件中心完成、禁止无 file_id 却说归档包完成**。
- **审计**：复用 `audit_log.record` / `InternshipAuditTrail`（append-only），不伪造。

## 6. 公共组件强制接入（新页面禁止造轮子）
必须优先用：AppPageShell/AppSectionCard/AppToolbar/AppActionBar · AppPermissionButton · AppSensitiveText · AppAuditTrail · AppExportButton/AppExportConfirm · AppFileList/AppFilePreview · AppBatchActionBar · AppApprovalPanel · AppStatusTag/AppRiskTag · AppStudentPicker/AppTeacherPicker/AppCompanyPicker/AppPositionPicker/AppBatchPicker · AppDescriptionList/AppOperationResult/AppMetricCard/AppWorkflowTimeline。
**禁止**：手写权限按钮/状态颜色/脱敏函数/审计表格/导出确认/附件列表/批量操作条/审核区/各页自造 picker。公共组件不够 → 先增强公共组件再接业务页。
**禁止重写**：DataTable/AdvancedFilter/AppDrawer/公共日期组件/Excel 管道。

## 7. 商业 UI 验收（销售截图标准）
清透学院蓝 · 成熟 SaaS 质感 · 密度合理 · 不像 Demo/学生作业/AI 拼接 · 关键数据一眼懂 · 路径清晰 · 风险异常待办突出不刺眼。每个核心页必须有：顶部业务说明/关键指标/筛选区/列表区/批量操作/行内操作/空态/加载态/错误态/权限不可操作说明/审计入口/导出入口。

## 8. 提交纪律（每次 commit 前）
`git status --short` → `git diff --cached --name-only` → `git diff --cached --stat` → `npm run build` → `npm run lint` → 相关 pytest。
**禁止**：git add -A / git push / push --tags / 提交 dist、node_modules、.env、.claude、query、删除的旧设计文档、与本阶段无关的毕设/迎新/学工/教务混杂文件。
工作区有大量混杂改动时**只选择性 add 本阶段相关文件**。

## 9. 硬红线（必须停止并报告）
需删真实数据 · 大改数据库核心结构且无 migration 方案 · 需改毕业设计中心 · 需提交 backend/miniapp/frontend/docs 混杂大杂烩 · npm build 无法通过且原因不明 · pytest 大量失败无法定位 · 涉密钥/短信/对象存储真实配置 · 需 push。

## 10. 做不完的降级规则（不烂尾）
当前阶段做到 build/lint/pytest 通过 → 形成 clean commit（或本轮约定的"只改不提交"）→ 未完成写入 `历史欠账.md` 标 partial/blocked → 不假装上线 → 不把半成品混进 implemented → 再进下一阶段。

## 11. 当前基线现状（务必知悉）
- 已闭环 6 模块（批次/学生/企业库/岗位库/岗位匹配/协议模板库）+ 移动端打卡/周报自服务**代码存在于工作区但整套未提交**，混在 147 项 WIP（含迎新/学工/教务后端 + 23 删档）里。
- **P0-C require_staff 门禁已完成并测试绿（2026-07-09，只改不提交）**；P0-B 路由已挂好（核实）；P0-F 小程序 URL 经复核无 bug。
- P0-D 数据范围 / P0-E 学生 CSV→xlsx / P0-G alembic **顺延，已记账**。
- **长跑分阶段 commit 的前提**：先把岗位实习相关 WIP 理成干净基线（否则每阶段无法干净提交）。


---

## 归档章节：岗位实习中心页面重构记录

> 原路径：docs/03-业务模块设计/岗位实习中心/施工记录/岗位实习中心页面重构记录.md

## 1. 本轮边界

- 目标：在不改后端业务逻辑前提下，完成岗位实习中心菜单/路由/页面归属重构。
- 允许改动：`frontend` 菜单、路由、岗位实习页面归属；岗位实习相关文档。
- 禁止改动：`backend` 业务逻辑、模型、服务、API、迁移、数据库；`miniapp`；其他中心。

## 2. 重构后二级目录（冻结为 12 个）

1. 实习工作台  
2. 批次与规则  
3. 实习学生  
4. 企业与岗位  
5. 匹配与分配  
6. 申请与协议  
7. 打卡与请假  
8. 周报与任务  
9. 指导与巡访  
10. 风险处置  
11. 评价与成绩  
12. 就业转化与归档统计

## 3. 旧菜单到新菜单映射表

| 旧二级菜单 | 新二级目录 | 处理方式 |
|---|---|---|
| 实习看板 | 实习工作台 | 保留主入口 |
| 实习批次 | 批次与规则 | 保留并归并规则配置 |
| 实习学生 | 实习学生 | 保留 |
| 企业库 + 岗位库 | 企业与岗位 | 合并 |
| 岗位匹配 + 岗位分配 | 匹配与分配 | 合并 |
| 实习申请审核 + 实习协议 | 申请与协议 | 合并 |
| 打卡管理 + 请假异常 | 打卡与请假 | 合并 |
| 周报日报 + 周报批阅 | 周报与任务 | 合并 |
| 指导记录 + 巡访记录 | 指导与巡访 | 合并 |
| 风险学生 | 风险处置 | 重命名并扩展 |
| 企业评价 + 学生评价 + 实习成绩 | 评价与成绩 | 合并 |
| 就业跟进 + 未就业帮扶 + 实习归档 + 实习统计 | 就业转化与归档统计 | 合并 |

## 4. 旧路由到新路由兼容表

| 旧路由 | 新结构入口 | 兼容方式 |
|---|---|---|
| `/admin/internship` | 实习工作台 | 原路由保留 |
| `/admin/internship/batches` | 批次与规则 | 原路由保留 |
| `/admin/internship/students` | 实习学生 | 原路由保留 |
| `/admin/internship/enterprises` | 企业与岗位 | 原路由保留 |
| `/admin/internship/positions` | 企业与岗位 | 原路由保留 |
| `/admin/internship/match` | 匹配与分配 | 原路由保留 |
| `/admin/internship/agreements` | 申请与协议 | 原路由保留 |
| `/admin/internship/agreement-templates` | 申请与协议 | 原路由保留 |
| `/admin/internship/attendance` | 打卡与请假 | 原路由保留 |
| `/admin/internship/reports` | 周报与任务 | 原路由保留 |
| `/admin/internship/guidance` | 指导与巡访 | 原路由保留 |
| `/admin/internship/risks` | 风险处置 | 原路由保留 |
| `/admin/internship/risk-disposal` | 风险处置 | 原路由保留 |
| `/admin/internship/scores` | 评价与成绩 | 原路由保留 |
| `/admin/employment` | 就业转化与归档统计 | 原路由保留 |
| `/admin/internship/workbench` 等新分组路由 | 对应新目录 | 新增 redirect 到已实现主路由 |

## 5. 页面处理表（去重/归并）

| 页面 | 处理结果 | 说明 |
|---|---|---|
| `InternshipDashboardView` | 主页面保留 | 承接“实习工作台” |
| `InternshipBatchListView` | 主页面保留 | 承接“批次与规则” |
| `InternshipStudentListView` | 主页面保留 | 承接“实习学生” |
| `InternshipEnterpriseListView` + `InternshipPositionListView` | 菜单归并 | 合并到“企业与岗位” |
| `InternshipMatchListView` | 主页面保留 | 承接“匹配与分配” |
| `AgreementView` + `InternshipAgreementTemplateListView` | 菜单归并 | 合并到“申请与协议” |
| `AttendanceView` + `AttendanceExceptionListView` + `LeaveReviewView` | 菜单归并 | 合并到“打卡与请假” |
| `WeeklyReportListView` | 主页面保留 | 承接“周报与任务” |
| `GuidanceVisitView` | 主页面保留 | 承接“指导与巡访” |
| `InternshipRiskView` + `RiskDisposalView` | 菜单归并 | 合并到“风险处置” |
| `EnterpriseEvalView` + `StudentEvalView` + `ScoreView` | 菜单归并 | 合并到“评价与成绩” |

## 6. implemented / partial / planned 状态表（本轮口径）

| 新二级目录 | 状态 |
|---|---|
| 实习工作台 | partial（总览已实现，部分统计面板待补强） |
| 批次与规则 | implemented（页面与核心能力已实现） |
| 实习学生 | implemented |
| 企业与岗位 | implemented（黑名单跨批次约束保持 partial） |
| 匹配与分配 | implemented（分配规则独立流程保持 partial） |
| 申请与协议 | partial（模板+三方协议已实现，电子签章 planned/partial） |
| 打卡与请假 | implemented |
| 周报与任务 | partial（计划/月报等仍为 partial） |
| 指导与巡访 | implemented |
| 风险处置 | implemented |
| 评价与成绩 | implemented |
| 就业转化与归档统计 | partial（归档统计统一页待 P3 深化） |

## 7. P0/P1/P2 已完成能力落位（关键）

| 能力 | 新目录 |
|---|---|
| 实习学生建档、数据范围、owner 范围、xlsx 导入导出 | 实习学生 |
| 打卡台账、打卡异常、补卡申请/审批、请假通过写 LEAVE 留痕 | 打卡与请假 |
| 周报提交、周报批阅 | 周报与任务 |
| 指导记录、巡访 | 指导与巡访 |
| 请假异常、风险处置/跟进/关闭 | 风险处置 |
| 三方协议、三方确认状态机、协议附件 | 申请与协议 |
| 企业评价、企业五维评分 | 评价与成绩 |
| 学生鉴定/自评、教师意见、学校审核 | 评价与成绩 |
| 实习成绩、五项权重核算、成绩复核/发布 | 评价与成绩 |
| `notify_counselor` 真实站内信 | 风险处置 / 打卡与请假（联动） |

## 8. partial / planned 清单（诚实保留）

- 企业导师门户：partial  
- 企业侧轻入口：planned  
- 电子签章/协议在线签章：planned  
- 旧库正式迁移验证：partial  
- 企业合作记录深度闭环：partial  
- 企业黑名单跨批次约束：partial  
- 就业中心真实联动深化：partial  
- 小程序企业导师端：planned  
- 校领导驾驶舱深度下钻：planned

## 9. 异常分支流程表

| 异常类型 | 发生阶段 | 来源页面 | 触发条件 | 进入风险处置 | 负责人 | 通知对象 | 处理动作 | 关闭条件 | 归档材料 |
|---|---|---|---|---|---|---|---|---|---|
| 未落实岗位 | 匹配分配 | 匹配与分配 | 未匹配/分配失败 | 是 | 指导老师/学院管理员 | 辅导员 | 分配/调岗 | 学生落岗 | 风险记录 |
| 协议未签 | 协议签署 | 申请与协议 | 三方确认超时 | 是 | 协议管理员 | 学生/企业 | 催签/退回 | 协议生效或作废 | 协议台账 |
| 未到岗 | 到岗确认 | 打卡与请假 | 到岗未确认 | 是 | 指导老师 | 学生/辅导员 | 补充说明 | 到岗确认完成 | 到岗说明 |
| 连续未打卡 | 过程监管 | 打卡与请假 | 连续缺卡阈值 | 是 | 指导老师 | 辅导员 | 补卡/核查 | 打卡恢复 | 打卡台账 |
| 请假超期未归 | 过程监管 | 打卡与请假 | 请假超期 | 是 | 指导老师 | 辅导员 | 追踪处理 | 销假完成 | 请假单 |
| 周报逾期 | 周报任务 | 周报与任务 | 周报未按时提交 | 是 | 指导老师 | 学生/辅导员 | 催交/退回 | 周报补交并批阅 | 周报记录 |
| 企业投诉 | 指导巡访 | 指导与巡访 | 企业反馈问题 | 是 | 学院管理员 | 学校管理员 | 处置跟进 | 投诉关闭 | 巡访记录 |
| 巡访发现问题 | 指导巡访 | 指导与巡访 | 巡访问题登记 | 是 | 指导老师 | 学院管理员 | 整改跟进 | 整改完成 | 巡访台账 |
| 实习中断 | 风险阶段 | 风险处置 | 状态中断 | 是 | 学院管理员 | 学校管理员 | 恢复/退实习 | 状态闭环 | 处置记录 |
| 安全风险 | 风险阶段 | 风险处置 | 安全事件 | 是 | 学校管理员 | 校内相关角色 | 升级处置 | 风险关闭 | 安全处置单 |
| 就业风险 | 就业转化 | 就业转化与归档统计 | 未就业预警 | 是 | 就业老师 | 学院管理员 | 帮扶计划 | 就业或阶段关闭 | 帮扶记录 |

## 10. PC 菜单真实渲染源定位结论

1. PC 左侧菜单组件路径：`frontend/src/layouts/BasePortalLayout.vue`。  
2. 菜单最终来源：岗位实习页内侧栏来自 `getVisibleNavPlan()`（`navPlan.js`）；工作台自定义左菜单来自 `getVisibleAdminMenu()`（`adminMenu.js`）。  
3. 是否读取 `adminMenu.js`：是（一级轨道、工作台模块卡、部分侧栏分组）。  
4. 是否读取 `navPlan.js`：是（岗位实习页内真实二级/三级树）。  
5. 是否读取 route meta：是（高亮与权限键辅助），但不是菜单唯一源。  
6. 是否读取 Pinia store：否（菜单渲染不依赖 menuStore/permissionStore）。  
7. 是否读取后端接口：否（菜单结构不走后端菜单接口）。  
8. 是否有 localStorage/sessionStorage 菜单缓存：无菜单缓存；仅主题与工作台收藏项本地存储。  
9. 之前改了 `navPlan/adminMenu/routes` 但 UI 仍可能“看起来没变”的原因：工作台视图使用 `adminMenu.js` 扁平菜单展示，若只改 `navPlan`，工作台入口区不会同步成 12 模块。  
10. 本轮真实修复源头：同步调整 `adminMenu.js` 的岗位实习分组为 12 个二级入口，且保留 `navPlan.js` 的 12 模块三级树作为岗位实习页内真实渲染源。

## 11. P0/P1/P2 能力落位核对表

| 能力 | 新二级模块 | 新三级页面 | 当前状态 | 是否已实现 | partial 原因 | 是否还需 P3 补 | 是否阻断上线 |
|---|---|---|---|---|---|---|---|
| 实习学生建档 | 实习学生 | 实习名单 | implemented | 是 | - | 否 | 否 |
| 管理端数据范围 | 实习学生/风险处置 | 列表页 | implemented | 是 | - | 否 | 否 |
| 指导教师 owner 范围 | 指导与巡访/周报与任务 | 指导记录/周报批阅 | implemented | 是 | - | 否 | 否 |
| xlsx 导入导出 | 实习学生/企业与岗位/匹配与分配 | 导入导出入口 | implemented | 是 | - | 否 | 否 |
| 打卡台账 | 打卡与请假 | 打卡记录 | implemented | 是 | - | 否 | 否 |
| 打卡异常 | 打卡与请假 | 打卡异常 | implemented | 是 | - | 否 | 否 |
| 补卡申请 | 打卡与请假 | 补卡申请 | implemented | 是 | - | 否 | 否 |
| 补卡审批 | 打卡与请假 | 补卡审批 | implemented | 是 | - | 否 | 否 |
| 周报提交 | 周报与任务 | 周报提交 | implemented | 是 | - | 否 | 否 |
| 周报批阅 | 周报与任务 | 周报批阅 | implemented | 是 | - | 否 | 否 |
| 指导记录 | 指导与巡访 | 指导记录 | implemented | 是 | - | 否 | 否 |
| 巡访 | 指导与巡访 | 巡访记录 | implemented | 是 | - | 否 | 否 |
| 请假异常 | 打卡与请假 | 请假审批/超期未归 | implemented | 是 | - | 否 | 否 |
| 风险处置 | 风险处置 | 风险处置 | implemented | 是 | - | 否 | 否 |
| 风险跟进 | 风险处置 | 风险跟进 | implemented | 是 | - | 否 | 否 |
| 风险关闭 | 风险处置 | 风险关闭 | implemented | 是 | - | 否 | 否 |
| 三方协议 | 申请与协议 | 协议发起 | implemented | 是 | - | 否 | 否 |
| 三方确认状态机 | 申请与协议 | 三方确认 | implemented | 是 | - | 否 | 否 |
| 协议附件 | 申请与协议 | 协议上传 | implemented | 是 | - | 否 | 否 |
| 企业评价 | 评价与成绩 | 企业评价 | implemented | 是 | - | 否 | 否 |
| 企业五维评分 | 评价与成绩 | 企业五维评分 | implemented | 是 | - | 否 | 否 |
| 学生鉴定/自评 | 评价与成绩 | 学生自评 | implemented | 是 | - | 否 | 否 |
| 教师意见 | 评价与成绩 | 教师意见 | implemented | 是 | - | 否 | 否 |
| 学校审核 | 评价与成绩 | 学校审核 | implemented | 是 | - | 否 | 否 |
| 实习成绩 | 评价与成绩 | 综合成绩 | implemented | 是 | - | 否 | 否 |
| 五项权重核算 | 评价与成绩 | 成绩规则 | implemented | 是 | - | 否 | 否 |
| 成绩复核 | 评价与成绩 | 成绩复核 | implemented | 是 | - | 否 | 否 |
| 成绩发布 | 评价与成绩 | 成绩发布 | implemented | 是 | - | 否 | 否 |
| notify_counselor 站内信 | 风险处置/打卡与请假 | 风险处置/请假审批 | implemented | 是 | - | 否 | 否 |
| 请假通过写 LEAVE 打卡留痕 | 打卡与请假 | 请假审批 | implemented | 是 | - | 否 | 否 |
