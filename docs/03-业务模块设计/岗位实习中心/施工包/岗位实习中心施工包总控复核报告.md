# 岗位实习中心施工包 · 总控复核报告

| 项 | 内容 |
|---|---|
| 工作分支 | `cursor/internship-construction-package` |
| 基线 commit | `22bfff59934cc4bf91610315d7049984c4904e83` |
| Worktree | `…/职校学生全生命周期系统-internship-wt` |
| 复核日期 | 2026-07-12 |
| 执行方式 | 只文档；差量补强；禁止业务代码修改 |

## 1. 数量门禁

| 检查项 | 结果 |
|---|---|
| 二级模块 = 12 | PASS |
| 三级施工卡 = 99 | PASS |
| 分模块证据包 = 12 | PASS |
| 共享证据库 = 1 | PASS |
| 空文件 | PASS（无） |
| 禁止短语「见总册」「开发时再确认」 | PASS（抽检+批量扫描 bad=0） |
| 每卡含 AS-IS + designSource | PASS |

## 2. 外部证据

| 通道 | 结果 |
|---|---|
| 共享库实际打开原文 | PASS（校友邦高校通知/操作指南、习讯云培训/学生端 PDF、超星 CCGP 中标、教职成〔2021〕4号转载全文） |
| 12 个分模块证据包 | PASS（复用共享库 + 模块映射；状态 verified_external_evidence_v1） |
| 搜索摘要冒充正文 | 未发现 |

**证据综合：PASS（支撑文档 design_ready）**  
说明：分模块包以总控共享库为原文缓存；施工卡均要求引用 evidenceId+原始 URL+locator+localEvidencePath。

## 3. 代码事实

| 结论 | 说明 |
|---|---|
| 非从零 | 矩阵：KEEP 52 / STRENGTHEN 45 / REBUILD 0 / MISSING 2 |
| 严重缺口已登记 | PC router 未接 `/archive` `/stats` `/plans` `/guidance-plans`；advisor_name 脆弱；实习销假/超期 MISSING |
| 企业库 | 复用 `t_emp_company`，禁止平行库 — 已写入相关包 |
| 测试 | 仅确认测试文件存在；**本轮未执行 pytest/build**（不得写测试通过） |

## 4. 权限与安全

| 检查 | 结果 |
|---|---|
| 十类角色矩阵写入总包/卡 | PASS（模板级实例化） |
| 企业导师禁全校 | PASS（边界写明） |
| 不扩大现有权限；新增 key 标建议新增 | PASS |
| 实习请假 ≠ 学工请假 | PASS（销假/超期卡强制独立） |

## 5. 跨模块冲突

| 项 | 结论 |
|---|---|
| 重复企业库 | 无（共享就业企业表） |
| 请假状态机混用 | 禁止；MISSING 卡已隔离 |
| 成绩回写教务 | 标用户决定，未擅自设计为强制同步 |
| 风险 vs 打卡/周报 | 风险包明确事件源边界，不重建对方状态机 |
| 权限 key 冲突 | 文档阶段沿用 internship.* 前缀；未见互相覆盖声明 |

## 6. design_ready / draft / blocked

| 状态 | 模块 |
|---|---|
| `design_ready`（文档差量） | 全部 12 个二级总包；97 张 KEEP/STRENGTHEN 卡 |
| `design_ready` 但代码 MISSING | 销假管理、超期未归（设计可开工，实现从零） |
| `blocked` | 无（证据与矩阵已过门禁） |
| 代码 `implemented` | 不在本轮宣称；以矩阵与后续代码任务为准 |

## 7. 误改检查

| 项 | 结果 |
|---|---|
| frontend/backend/miniapp/Alembic/测试 | **未修改** |
| 学工中心施工包 | **未修改** |
| CLAUDE.md / 根提示词 / 施工图 / 公共导航 | **未修改** |
| 主工作区用户改动 | **未覆盖** |

## 8. 学校决定项（仅政策）

1. 企业资质考察报告附件是否强制  
2. 成绩权威源：批次 `rules_config.score` vs `t_internship_score_config`  
3. 电子签深度  
4. 实习成绩是否回写教务  
5. 「学生对企/岗评价」独立实体还是合并鉴定口径  
6. 就业跟进：本中心嵌入 vs 只读跳转就业中心  
7. 隐藏菜单「实习保险核验」是否升为正式三级（见目录差异建议）

## 9. 下一步（代码施工，另开任务）

1. 接线：`/internship/archive*` `/stats*` `/plans*` `/guidance-plans*`  
2. 新建：实习销假、超期未归（独立状态机）  
3. 强化：advisor_user_id、规则版本冻结、黑名单 nav 改 I  
4. 跑相关 pytest + 前端 build 后再改矩阵/施工图状态  

## 10. 提交记录（模块）

见总索引表格；矩阵提交 `9745d72`；本报告与总索引单独提交。
