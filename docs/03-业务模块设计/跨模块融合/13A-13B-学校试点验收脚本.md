# 13A/13B 学校试点验收脚本

> 版本：V1.0（2026-07-05）
> 依据：《13A-学工中心开发阶段与验收用例.md》《13B-教务中心开发阶段与验收用例.md》（自动化用例基线）、《13A-13B-跨端一致性与数据联动矩阵.md》、`_13-现有系统集成事实速查.md`、docs/07-部署运维交付与商业化/delivery/试点学校验收清单.md。
> 定位：**现场手工验收脚本**（学校方与交付工程师面对面逐条执行），是 docs/07-部署运维交付与商业化/delivery/试点学校验收清单.md 的 13A/13B 增补件（见 §4.4）。
> 命名口径：以《13A-13B-现有代码库融合证据报告.md》2.4 为准（student-affairs / academic-affairs、t_affairs_\* / t_aa_\*、学生移动端 /api/v1/mobile/student-affairs|academic-affairs/\*）。

## 0. 账号与环境约定

| 账号 | 租户 | 密码 | 用途 |
|---|---|---|---|
| admin / teacher / student | demo-school（…003，**只读锁**：写 403） | 123456 | 只读演示与只读锁验证 |
| admin2 / teacher2 / student2 | sandbox-school（…007，**每晚 0 点重置**） | 123456 | 全部写操作脚本 |

- 全部写操作脚本在 **sandbox-school** 执行；当天执行完毕（隔夜数据会被重置）。
- teacher2 已配置 t_teacher_student_scope（CLASS scope，仅带"班级 A"）；student2 属班级 A；越权用例使用班级 B 学生 S2（种子内置，见 §4.2）。
- 端代号：PC=学校 PC 管理端（/admin/*）、MP-S=学生小程序、MP-T=教师小程序端。
- 每条脚本含"负向补充"：全脚本合计覆盖重复提交 409、越权 403/404 各 ≥8 处。

---

## 1. 学工中心验收脚本（SA-01 ～ SA-10）

### SA-01 学工首页角色视图与指标下钻

- **前置账号**：admin2（PC）；对照 admin（demo-school，PC）。
- **操作步骤**：① admin2 登录 PC → 左侧菜单点「学工中心」→「学工首页」；② 核对 summaryCards（全校学生数/风险数/今日待办/请假待审/困难学生/宿舍异常等卡）；③ 点击「风险学生」卡 → 下钻列表 → 点任一学生 → 进入学生 360；④ 返回首页记录「请假待审」数字 N；⑤ 换 teacher2 登录 → 打开同页 → 确认只显示所带班级口径。
- **预期结果**：三角色（管理员/辅导员）视图区块不同；卡片数字与下钻列表条数一致；360 页正常打开且时间线可见。
- **涉及端**：PC。
- **涉及接口**：GET /api/v1/student-affairs/dashboard、GET /api/v1/stats/*、GET /api/v1/authz/tenant/brand。
- **涉及数据变化（表级）**：无（纯读）。
- **验收通过标准**：卡片-列表逐卡对账一致；辅导员视图无全校数据；页头校名来自品牌配置（非硬编码）。
- **负向补充（403）**：demo-school 的 admin 打开学工首页可正常浏览（读），但点击任一写按钮（如新建批次）→ 提示 403 与"请前往沙箱体验"引导；界面无白屏。

### SA-02 辅导员工作台数据范围（看得见=能处理）

- **前置账号**：teacher2（PC + MP-T）。
- **操作步骤**：① teacher2 登录 PC → 「学工中心」→「辅导员工作台」；② 核对「我的学生」列表仅含班级 A；③ 记录「今日待办」数；④ 打开 MP-T 工作台页 → 对比同卡片数字；⑤ 在 PC 学生快速搜索框输入班级 B 学生 S2 姓名。
- **预期结果**：PC 与 MP-T 数字同源一致；搜索 S2 无结果或结果不可点入。
- **涉及端**：PC、MP-T。
- **涉及接口**：GET /api/v1/student-affairs/counselor/workbench|students|todos|risk-students、GET /api/v1/mobile/teacher/todos。
- **涉及数据变化**：无。
- **验收通过标准**：列表零越权数据；两端计数一致（onShow 重拉后）。
- **负向补充（403/404）**：直接以 URL 访问 S2 的学生 360（/admin/student-affairs/students/{S2 id}）→ 403/404，且 t_security_audit_log 出现 TEACHER_SCOPE_DENIED 记录（管理员在 /admin/system/logs 可查）。

### SA-03 学生请假提交 → 辅导员移动审批（核心闭环）

- **前置账号**：student2（MP-S）、teacher2（MP-T）。
- **操作步骤**：① student2 登录小程序 → 首页「学工服务」宫格 → 「请假」→「新增请假」；② 选类型=病假、起止 2 天、事由 ≥10 字、上传 1 张 JPG → 点「提交」；③ 看到"待辅导员审批"回执；④ teacher2 打开 MP-T「今日待办」→ 出现该请假待办 → 点入审批页 → 核对摘要 → 点「同意」填意见；⑤ student2 下拉刷新详情 → 状态"已通过"，消息中心收到审批结果；⑥ teacher2 打开该生 360 → 时间线出现"请假获批"。
- **预期结果**：全链 ≤5 分钟走通；状态、待办、消息、360 四要素齐。
- **涉及端**：MP-S、MP-T（PC 可旁证列表）。
- **涉及接口**：POST /api/v1/mobile/student-affairs/leave、GET /api/v1/mobile/teacher/approvals、POST /api/v1/mobile/teacher/approvals/{id}/approve。
- **涉及数据变化**：t_cs_leave 新增/更新 1 行（affairs_status SUBMITTED→APPROVED）；t_workflow_instance/task 各 1 行；t_unified_todo 1 行（DONE）；t_unified_message 2 条；t_student_stage_event 1 行。
- **验收通过标准**：库表增量与上行一致（工程师后台核对）；无重复通知。
- **负向补充（409 双处）**：② 后立即对同时段再点提交 → 409"存在时间重叠的进行中请假"，列表仍 1 条；⑤ 前由 admin2 在 PC 对同一任务再点「同意」→ 409"已被他人处理"并自动刷新。

### SA-04 销假闭环与逾期风险

- **前置账号**：student2（MP-S）、teacher2（MP-T/PC）。
- **操作步骤**：① 用 SA-03 已通过的请假单：student2 详情页点「销假」→ 上传返校证明 → 提交；② teacher2 待办出现"销假确认" → 点「确认」；③ student2 刷新 → 状态 CLOSED；④ 工程师触发逾期扫描（对种子内一条到期未销单，后台任务或管理端按钮）；⑤ teacher2 待办出现 RISK_ALERT 风险待办，PC 风险列表出现 source=LEAVE_OVERDUE 记录。
- **预期结果**：销假后 360 出"销假完成"；逾期单变 OVERDUE 红标并生成 1 条风险。
- **涉及端**：MP-S、MP-T、PC。
- **涉及接口**：POST /mobile/student-affairs/leave/{id}/cancel-leave、POST /mobile/teacher/approvals/{id}/approve、风险列表 GET /api/v1/student-affairs/risk。
- **涉及数据变化**：t_affairs_leave_cancel_record 1 行；请假单状态 CLOSED/OVERDUE；t_affairs_risk_record 1 行；todo/message 对应增量。
- **验收通过标准**：逾期扫描重跑一次不重复生成风险（幂等）。
- **负向补充（409）**：② teacher2 确认成功后再次点「确认」（回退页面重进）→ 409，无第二条 StageEvent。

### SA-05 困难认定：申请 → 三级评审 → 公示 → 困难库

- **前置账号**：admin2（PC，兼学院/学校审核角色演示）、teacher2、student2。
- **操作步骤**：① admin2 PC「困难认定」→「新建批次」（公示天数用测试档 0 天）→ 发布；② student2 小程序宫格「困难认定」→ 填家庭经济信息+材料 → 提交；③ teacher2 待办初审通过；④ admin2 依次完成学院复审、学校终审 → 状态进入公示；⑤ 公示期满（0 天档）刷新 → APPROVED；⑥ PC「困难学生库」出现该生等级；⑦ 打开公示名单页 → 核对全脱敏。
- **预期结果**：12 态状态机逐级推进；困难库、360、统计"困难学生数"同步 +1。
- **涉及端**：PC、MP-S、MP-T。
- **涉及接口**：POST /api/v1/student-affairs/aid/batches、POST /mobile/student-affairs/aid/apply、POST /api/v1/student-affairs/aid/{id}/review。
- **涉及数据变化**：t_affairs_aid_identification_batch/apply、aid_family_economy（隔离表）、aid_review_record、aid_level_history 各新增；todo/message/StageEvent 增量。
- **验收通过标准**：公示名单接口无任何家庭经济字段；等级进 level_history。
- **负向补充（409 + 敏感审计）**：② 后 student2 同批次再提交 → 409 仅一条；teacher2 在申请详情点「查看完整家庭经济」→ 必须填原因 ≥5 字才返回明文，管理员审计日志出现 VIEW_SENSITIVE 记录。

### SA-06 奖学金：校验拦截 → 申请 → 评审公示确认

- **前置账号**：admin2、teacher2、student2；种子学生 S3（有未解除处分）。
- **操作步骤**：① admin2 建奖学金批次并开放；② 用 S3 账号（种子提供）提交申请 → 观察被拦截提示"存在未解除处分"；③ student2 提交申请成功；④ teacher2 初审通过 → admin2 学院评审、学校复核、公示、确认；⑤ PC 结果页导出获奖名单（填用途 ≥5 字）→ 打开文件核对首行水印。
- **预期结果**：校验链拦截明确（422 文案说明原因，不产生半提交）；确认后 360 出"获奖记录"；导出登记可在导出记录页看到。
- **涉及端**：PC、MP-S、MP-T。
- **涉及接口**：POST /mobile/student-affairs/funding/apply、POST /api/v1/student-affairs/funding/{id}/review、POST /api/v1/export/domain/affairs_funding。
- **涉及数据变化**：t_affairs_funding_application/review/publicity/result；t_export_task 1 行（sha256/purpose）；StageEvent 1 行。
- **验收通过标准**：S3 拦截无残留数据；水印含校名/导出人/用途。
- **负向补充（409 + 403）**：③ 后 student2 同批次重复提交 → 409；范围外教师（种子 T2）在 MP-T 审批该申请 → 403 且审计留痕。

### SA-07 处分登记生效与解除

- **前置账号**：admin2（登记+终审）、teacher2、student2。
- **操作步骤**：① admin2 PC「处分管理」→「登记」：选 student2、警告级、违纪事实+材料 → 提交；② 学院初审、学工处复核逐级通过 → EFFECTIVE → 点「送达通知」；③ student2 小程序「我的处分」可见生效记录；④ admin2 发起解除（考察期规则临时调 0）→ 三级审通过 → REMOVED；⑤ 核对 360：处分历史保留且新增"解除"事件；⑥ 回到 SA-06 批次：student2 申请奖学金不再被拦。
- **预期结果**：EFFECTIVE 时学生端可见（审批中不可见）；解除后奖助校验放行。
- **涉及端**：PC、MP-S。
- **涉及接口**：POST /api/v1/student-affairs/discipline、/{id}/review、/remove-applies。
- **涉及数据变化**：t_affairs_discipline_case/decision/material/remove_apply；t_cs_discipline 投影行同步（学生端数量接口 +1/-1）；StageEvent 2 行。
- **验收通过标准**：投影一致（学生端数量与 case 状态吻合）；打印决定书仅 EFFECTIVE 后可用。
- **负向补充（403/404）**：student2 修改 URL/请求参数尝试读 S2 的处分明细 → 403/404，不泄露存在性；审计留痕。

### SA-08 风险预警：多来源生成 → 处置 → 关闭

- **前置账号**：teacher2（MP-T/PC）、admin2。
- **操作步骤**：① 确认 SA-04 已生成 LEAVE_OVERDUE 风险；② admin2 PC 将一条种子学业预警"转风险"；③ teacher2 MP-T「风险学生」列表出现 2 条 → 点入其一 → 填处置记录 → 点「关闭」填结论 ≥5 字；④ PC 风险列表核对状态 CLOSED；⑤ 首页风险数对应 -1。
- **预期结果**：来源/责任人分派正确；关闭后学生相关提醒终止；360 出"风险关闭"摘要。
- **涉及端**：PC、MP-T。
- **涉及接口**：GET/POST /api/v1/student-affairs/risk/{id}/handle|close、MP-T 走 /api/v1/mobile/teacher/student-affairs/risk/*。
- **涉及数据变化**：t_affairs_risk_record/handle_record/close_record；todo 闭合；StageEvent 1 行。
- **验收通过标准**：PC 与 MP-T 同一记录状态同步（重拉后一致）。
- **负向补充（409 + 403）**：③ 关闭成功后另一会话再点关闭 → 409；普通教师 T2 读心理来源风险明细 → 403，仅见"需关注"标记。

### SA-09 谈心谈话与心理权限隔离

- **前置账号**：teacher2、心理教师 P1（种子账号，PSY_STUDENT scope 含 student2）。
- **操作步骤**：① teacher2 MP-T「谈话速记」：选 student2、类型=学业 → 填纪要提交；② 记录标记"需跟进"→ 转风险 → 确认生成关联风险；③ P1 登录建一条类型=心理的谈话记录；④ teacher2 尝试打开该心理记录；⑤ admin2 PC 谈话统计页核对完成率 +1。
- **预期结果**：学业谈话进 360 时间线；心理谈话对普通教师不可见明细。
- **涉及端**：MP-T、PC。
- **涉及接口**：POST /api/v1/mobile/teacher/student-affairs/talk、GET /api/v1/student-affairs/talk/*。
- **涉及数据变化**：t_affairs_talk_record 2 行；t_affairs_risk_record 1 行（talk 来源）；StageEvent 1 行（心理类不写通用时间线明细）。
- **验收通过标准**：转风险互链可跳；心理记录读取有 scope 判定。
- **负向补充（403 + 409）**：④ → 403 且审计；① 提交时连点 → 仅 1 条记录（提交锁+409）。

### SA-10 学工归档包与只读锁

- **前置账号**：admin2、teacher2。
- **操作步骤**：① admin2 PC「学工归档」→ 新建批次（选本学年、勾选请假/困难/谈话）→ 生成档案包；② 查看补缺清单 → teacher2 处理补缺待办；③ admin2 完整性确认 → 点「生成水印归档包」→ 下载 zip；④ 解包核对：manifest 清单、各单据 PDF 首页水印、audit_sheet、checksum；⑤ 回到 SA-03 的请假单详情 → 确认按钮区只剩「导出」。
- **预期结果**：zip 结构完整、sha256 与导出记录一致；归档后业务单 ARCHIVED 只读。
- **涉及端**：PC、MP-T。
- **涉及接口**：POST /api/v1/student-affairs/archive/batches、POST /api/v1/export/domain/affairs_archive。
- **涉及数据变化**：t_affairs_archive_batch/package；t_export_task 1 行（export_mode=ARCHIVE）；相关业务单状态→ARCHIVED。
- **验收通过标准**：下载动作在审计日志有 ARCHIVE_DOWNLOAD；包内无心理明细。
- **负向补充（409/422 + 429）**：对 ARCHIVED 请假单调用编辑/审批接口 → 拒绝且无副作用；1 分钟内连续第 6 次导出 → 429 限流提示。

---

## 2. 教务中心验收脚本（AA-01 ～ AA-10）

### AA-01 学年学期与校历发布

- **前置账号**：admin2（PC）。
- **操作步骤**：① PC「教务中心」→「学年学期」→「新建」2026-2027-1（起止日期）；② 进入校历编辑：设教学周 1-18、考试周 19-20、国庆节假日+调休；③ 配置作息节次（上午 4 节/下午 4 节）；④ 打开「发布确认」页 → 点「发布」；⑤ 核对教务首页"当前学期"卡切换。
- **预期结果**：DRAFT→PUBLISHED；管理员收发布通知；后续排课/注册窗口按校历生效。
- **涉及端**：PC。
- **涉及接口**：POST /api/v1/academic-affairs/terms、/{id}/calendar、/{id}/publish。
- **涉及数据变化**：t_aa_term/calendar/timeslot 新增；t_unified_message 通知。
- **验收通过标准**：教学周缺失时发布被拒（先删教学周试一次 → 422 携缺失项清单）。
- **负向补充（409 + 403）**：发布成功后双击重放「发布」→ 409 或幂等（按契约，仅一条发布审计、通知不重发）；用 teacher2 直调 publish 接口 → 403 且审计。

### AA-02 入学注册（迎新联动）

- **前置账号**：admin2（教务处+学院教务员双角色演示）。
- **操作步骤**：① PC「学籍管理」→「注册批次」→ 开启批次；② 打开「待注册名单」→ 核对预检列（报到/缴费/材料三状态，读迎新数据）；③ 勾选预检通过的 student2 → 点「确认注册」；④ 批次复核确认；⑤ 打开 student2 学籍详情 → 状态 REGISTERED；⑥ 名单中欠费学生 S4（种子）→ 点「确认注册」观察拦截；⑦ 为 S4 启用绿色通道标记后重试 → 成功且带"欠费跟踪"。
- **预期结果**：student_status 经单一入口变更；360 出"入学注册完成"；注册完成率 +。
- **涉及端**：PC（学生可在 MP-S「我的学籍」旁证）。
- **涉及接口**：GET /api/v1/academic-affairs/registration/pending、POST /registration/confirm。
- **涉及数据变化**：t_aa_registration 新增；t_student_profile.student_status 更新；StageEvent 1 行。
- **验收通过标准**：欠费拦截 422 文案明确；绿色通道路径生成跟踪待办。
- **负向补充（409）**：③ 后对 student2 重放确认请求 → 409，注册记录/StageEvent/消息各仅一条。

### AA-03 学籍异动：转专业全链

- **前置账号**：student2（MP-S）、teacher2、admin2（原学院/目标学院/教务处三节点演示）。
- **操作步骤**：① 确认规则中心转专业窗口开启；② student2 小程序「我的学籍」→「异动申请」→ 类型=转专业 → 目标专业 → 原因 ≥10 字+材料 → 提交；③ teacher2 待办初审通过；④ admin2 依次完成原学院转出、目标学院接收、教务处终审；⑤ student2 刷新「我的学籍」→ 专业/班级已切换；⑥ 360 时间线出"转专业生效"。
- **预期结果**：五节点逐级推进；主档 major_id/class_id 更新、方案绑定切换；异动统计 +1。
- **涉及端**：MP-S、MP-T、PC。
- **涉及接口**：POST /api/v1/mobile/academic-affairs/status-changes、审批走 /api/v1/approvals/* 与移动包装。
- **涉及数据变化**：t_aa_status_change 1 行（append-only 流水）；t_student_profile 更新；workflow/todo/message/StageEvent 增量。
- **验收通过标准**：每节点审批人待办准确；退回分支可演示（任一节点退回→学生收原因→修改重提）。
- **负向补充（409 + 403）**：② 后重放提交 → 409"已有在途申请"；非该生辅导员 T2 审批初审任务 → 403 + 审计，随后 teacher2 正常通过，另一管理员对已处理任务再审 → 409 版本冲突。

### AA-04 休学与复学（业务冻结验证）

- **前置账号**：student2（MP-S）、teacher2、admin2。
- **操作步骤**：① student2 发起休学（原因+材料）→ 辅导员/学院/教务处逐级通过 → 状态 SUSPENDED；② student2 再尝试发起转专业 → 观察拦截；③ PC 异动列表查看该单：原因列显示脱敏（reason_masked）；④ admin2 进详情点「查看完整原因」；⑤ 发起复学 → 辅导员确认 → 学院定班（新班级）→ 教务处通过 → NORMAL；⑥ 核对 class_id 更新与方案重绑。
- **预期结果**：休学期间转专业/选课入口冻结（422/置灰）；复学重绑正确。
- **涉及端**：MP-S、PC。
- **涉及接口**：同 AA-03 族（change_type=SUSPEND/RESUME）。
- **涉及数据变化**：t_aa_status_change 2 行；student_status SUSPENDED→NORMAL；StageEvent 2 行。
- **验收通过标准**：② 为 422"休学期间不可办理"，不产生新单。
- **负向补充（403 + 审计）**：无权限角色（teacher2 若无 roll.viewSensitive）点「查看完整原因」→ 403；admin2 查看成功后审计日志出现 VIEW_SENSITIVE。

### AA-05 课程库与培养方案：编制 → 两审 → 发布 → 绑定

- **前置账号**：admin2（教务处）、专业负责人 PL1（种子账号）、学院教务员角色由 admin2 演示。
- **操作步骤**：① PL1 PC「课程库」→ 新增课程（编号/名称/学分/学时/类别）→ 提交 → 学院审 → 教务审 → ENABLED；② admin2 建方案编制任务 → PL1 编制方案：拖入课程模块 → 核对学分汇总条；③ 故意加一门重复课程点「提交」→ 观察校验；④ 修正后提交 → 学院审核 → 教务审核 → 发布 → 绑定 2026 级；⑤ 点「修订」→ 确认生成 V2 草稿且 V1 只读、原绑定不变。
- **预期结果**：校验器给字段级明细（重复课程清单/学分差额）；版本规则生效。
- **涉及端**：PC。
- **涉及接口**：POST /api/v1/academic-affairs/courses、/programs、/programs/{id}/submit|publish|bind。
- **涉及数据变化**：t_aa_course、t_aa_program/program_course；workflow/todo 增量。
- **验收通过标准**：③ 为 422 携明细、状态留 DRAFT；发布后 2026 级学生方案版本正确。
- **负向补充（409 + 403）**：新增与已有课程同编号 → 409（唯一键 tenant+code）；B 学院负责人（种子 PL2）编辑该方案 → 403 + 审计。

### AA-06 教学任务：生成 → 分配 → 教师确认 → 审核

- **前置账号**：admin2、teacher2（任课教师身份）。
- **操作步骤**：① admin2 建任务批次 → 点「按方案生成应开课程」；② 再点一次「生成」→ 确认条数不变（幂等提示）；③ 学院核对页为各课程分配 teacher2（其中一门造容量 40<班级 45 → 观察黄色提示仍可保存）；④ teacher2 登录 PC「任务确认」→ 逐门点「确认」；⑤ 学院提交批次（先留一门未分配试提交 → 观察拦截 → 补分配后提交）；⑥ admin2 教务审核通过 → 批次可排课。
- **预期结果**：generate 幂等；warning 非阻断、422 阻断分明。
- **涉及端**：PC。
- **涉及接口**：POST /api/v1/academic-affairs/teaching-tasks/batches/{id}/generate|assign|confirm|submit|review。
- **涉及数据变化**：t_aa_teaching_task 批次与明细；todo/message 增量。
- **验收通过标准**：⑤ 未分配提交 → 422 携课程清单。
- **负向补充（403 + 409）**：另一教师 T2 调 teacher2 课程的确认接口 → 403 + 审计；teacher2 确认后重放确认 → 409，确认记录仅一条。

### AA-07 课表：冲突检测 → 发布 → 学生小程序查看

- **前置账号**：admin2、student2（MP-S）、teacher2。
- **操作步骤**：① admin2 建排课批次 → 周历网格中为班级 A 添加课表项（课程/教师/教室/节次/周次）；② 故意再添加同教师同节次第二项 → 观察冲突拦截（红标+冲突对方信息）；③ 修正后点「预发布」→ teacher2 PC 核对本人课表 → admin2 点「正式发布」；④ student2 小程序「我的课表」→ 核对本周课程、切换周次、空周 empty 态；⑤ teacher2 MP-T「今日课程」核对。
- **预期结果**：发布后师生同收 PUBLISHED_NOTICE；三端（PC/MP-S/MP-T）同源一致。
- **涉及端**：PC、MP-S、MP-T。
- **涉及接口**：POST /api/v1/academic-affairs/schedule/items、/batches/{id}/publish、GET /api/v1/mobile/academic-affairs/schedule、GET /api/v1/mobile/teacher/academic-affairs-affairs/today-courses。
- **涉及数据变化**：t_aa_schedule_batch/item/conflict；message 增量（教师+班级学生）。
- **验收通过标准**：② 422 冲突明细；课表导出（xlsx）首行水印正确。
- **负向补充（409 + 参数注入）**：发布后重放 publish → 409、通知不重发；student2 的课表请求手工携带 S2 班级参数重放 → 服务端按 token 忽略参数仍返回本班（或 403/404），不泄露他班数据。

### AA-08 成绩查看一致性与成绩单导出

- **前置账号**：admin2、teacher2、student2。
- **操作步骤**：① admin2 PC「成绩总览」按学期→课程→班级→学生四级下钻；② 同屏对照既有「学业过程」模块成绩页（/admin/academic/grades）→ 数字一致（同读 t_acad_grade）；③ teacher2 查看所带班成绩 → 按可见性矩阵仅见等级（默认档）；④ student2 小程序「我的成绩」→ 仅本人、与 /academic/my 汇总一致；⑤ admin2 导出班级成绩单：填用途 → 敏感二次确认弹窗 → 下载 → 核对首行水印与导出记录（行数/sha256）。
- **预期结果**：两模块同源零偏差；可见性矩阵生效；导出登记完整。
- **涉及端**：PC、MP-S。
- **涉及接口**：GET /api/v1/academic-affairs/grades/*、GET /api/v1/mobile/academic-affairs/grades、POST /api/v1/export/domain/aa_grades。
- **涉及数据变化**：t_export_task 1 行；业务表无变化（只读）。
- **验收通过标准**：挂科红标与挂科率卡一致。
- **负向补充（429 + 403）**：1 分钟内连续发起第 6 次导出 → 429；teacher2 查询非授课程成绩 → 403 + 审计。

### AA-09 学业预警：扫描生成 → 移动处置闭环

- **前置账号**：admin2、teacher2（MP-T）、student2。
- **操作步骤**：① admin2 PC「预警规则配置」核对阈值（挂 2 门=中级）；② 打开「扫描执行」→ 点「预览」核对命中名单（种子学生两门 FAILED）→ 点「确认落库」；③ 再连点两次「确认落库」→ 核对条数不变；④ teacher2 MP-T 收到预警待办 → 处理 → 新增干预记录 → 关闭（说明 ≥5 字）；⑤ student2 小程序「我的学业预警」状态更新；⑥ 首页预警数 -1。
- **预期结果**：写入既有 t_acad_warning（不建新表）；既有处置端点响应结构不变。
- **涉及端**：PC、MP-T、MP-S。
- **涉及接口**：POST /api/v1/academic-affairs/warning-scan（预览/确认）、既有 /api/v1/academic/warnings/{id}/close（复用既有 academic 学业过程模块）与移动处理端点。
- **涉及数据变化**：t_acad_warning 1 行（source_code/rule_code 落值）；todo+message 双落点；干预记录 1 行。
- **验收通过标准**：扫描幂等（键 student+source+term）；360 可见预警与处置记录。
- **负向补充（403 + 409）**：非责任辅导员 T2 调 close → 403 + 审计；teacher2 关闭后重放 close → 409/422 终态保护。

### AA-10 毕业资格预审与终审

- **前置账号**：admin2（教务处+学院初审演示）、student2（MP-S）。
- **操作步骤**：① admin2 建毕业审核批次 → 圈定应审学生（种子毕业班）→ 点「执行系统预审」；② 打开预审结果页：核对逐项三态矩阵（种子含全通过 1 人、实习未归档 1 人、有 ACTIVE 处分 1 人、老生无实习记录 UNKNOWN 1 人）；③ 异常清单页点「一键推待办」→ 核对责任人路由（实习→指导教师、处分→辅导员）；④ 学生端：对应学生小程序「毕业进度」红绿灯与 PC 同口径；⑤ 13A 侧完成该处分解除后 → 点「重跑预审」→ 该生转 PASS 且结果仍一条（逐项更新留历史）；⑥ 对全通过学生走学院初审 → 教务终审（名单预览+二次确认）→ 学生状态 GRADUATED；⑦ 导出毕业/结业/延毕三名单核对水印。
- **预期结果**：三态判定不误判（UNKNOWN≠FAIL）；终审经单一写入口改状态并写 StageEvent。
- **涉及端**：PC、MP-S。
- **涉及接口**：POST /api/v1/academic-affairs/graduation-audit/batches/{id}/precheck|review、GET /api/v1/mobile/academic-affairs/graduation-progress、POST /api/v1/export/domain/aa_graduation_lists。
- **涉及数据变化**：t_aa_graduation_audit(+batch)；t_student_profile.student_status；StageEvent；t_export_task。
- **验收通过标准**：重跑幂等（结果条数不变）；异常项待办按责任人精确投递。
- **负向补充（403 + 409）**：B 学院教务员（种子）初审 A 学院学生 → 403 + 审计；两个管理员会话并发终审同一任务 → 后者 409 版本冲突。

---

## 3. 汇总

### 3.1 验收执行顺序（半天节奏，约 4 小时）

1. **环境准备（30 分钟）**：§3.2 种子命令 + 冒烟检查。
2. **教务基础链**：AA-01 → AA-02 → AA-05 → AA-06 → AA-07（学期/校历是全局前置，先行）。
3. **学工主链**：SA-01 → SA-02 → SA-03 → SA-04 → SA-05 → SA-06 → SA-07。
4. **交叉联动**：SA-08（用到 AA 侧预警）→ SA-09 → AA-08 → AA-09。
5. **收口**：AA-03 → AA-04 → AA-10（依赖 SA-07 的处分解除）→ SA-10（归档最后做，避免提前锁只读）。
6. 全部负向补充随所在脚本就地执行，不集中补测。

### 3.2 环境准备（种子命令）

```
# 1) 数据库就绪检查
cd backend && python scripts/check_mysql_connection.py

# 2) 沙箱租户重置并灌 13A/13B 验收种子（dry-run 先看统计，--confirm 落库）
python scripts/reset_sandbox_school.py --dry-run
python scripts/reset_sandbox_school.py --confirm
#   （13A-P8-BE-01 / 13B-B8-2 已将学工/教务种子并入该脚本：
#    班级A/B、S2/S3/S4、T2、P1、PL1/PL2、开放批次、到期未销请假、
#    两门 FAILED 成绩、毕业班四类预审样例、空闲床位等）

# 3) 试点预检与冒烟（PowerShell，服务器侧）
scripts/check/preflight-school-trial.ps1
scripts/check/smoke-school-trial.ps1
```

注意：sandbox-school 每晚 0 点自动重置——验收须当天完成；跨天则重新执行第 2 步。demo-school 不执行任何写脚本。

### 3.3 验收记录表模板

| 编号 | 脚本名 | 执行人（校方/交付） | 日期 | 结果（通过/不通过/整改后通过） | 负向补充结果 | 证据（截图/导出文件号） | 备注 |
|---|---|---|---|---|---|---|---|
| SA-01 | 学工首页 | | | ☐/☐/☐ | ☐ | | |
| …（SA-02～SA-10、AA-01～AA-10 同表逐行） | | | | | | | |

结论页：通过 __ 条 / 整改 __ 条 / 不通过 __ 条；校方验收人签字：____；交付负责人签字：____；日期：____。整改项须注明复验日期，复验通过后本表重签。

### 3.3.1 每条脚本默认附加的通用断言（执行人无需重复记录，抽查即可）

1. 全部接口响应为统一响应体 `{code,bizCode,message,data,traceId,timestamp}`；业务失败绝不出现 500 白页。
2. 写操作后待办/消息增量与脚本预期一一对应，无多发漏发（工程师后台核 t_unified_todo / t_unified_message）。
3. 越权尝试均在 t_security_audit_log 留痕；敏感读取有 VIEW_SENSITIVE 记录。
4. 页面四态（loading/empty/error/data）无白屏；小程序弱网重试不产生重复数据。
5. 页头/水印中的校名一律来自租户品牌配置（t_tenant_brand_config），非硬编码。

### 3.3.2 执行中断与恢复约定

- 单条脚本失败：记录"不通过"+截图+traceId，**继续执行后续脚本**（除非是 AA-01/AA-02 等前置链，此时跳过其依赖项并标注"阻塞"）。
- 环境被写脏（如误在 demo-school 操作）：demo 有只读锁通常无实害，核对 403 记录即可；sandbox 脏数据影响后续脚本时重跑 §3.2 第 2 步重置，从中断处的分组重新开始（分组间数据独立，组内不独立）。
- 跨天恢复：sandbox 0 点已重置，必须整组重跑，不允许引用隔夜单据号。
- 版本更新后复验：仅重跑受影响脚本 + 其负向补充；验收记录表新开一列"复验结果"。

### 3.3.3 现场常见问答（给交付工程师）

| 校方问 | 答复口径 |
|---|---|
| "为什么故意让系统报错（409/403）？" | 负向补充是验收核心：证明重复提交不产生双单、越权访问被拦并留痕，这是数据安全承诺的可见证据。 |
| "能用我们学校真实学生数据验收吗？" | 验收用沙箱种子数据；真实数据在开局初始化阶段导入并按 delivery/试点部署信息采集表 流程处理，不混入验收环境。 |
| "验收通过是不是等于正式商用没风险？" | 否。按 delivery/试点学校验收清单.md 第七节知情确认执行：等保/渗透/备份演练等事项另行承诺。 |
| "小程序上能不能全部办完？" | V1 口径：请假/困难/异动等学生侧全流程在小程序；长假部分节点、成绩录入、终审、归档在 PC（脚本中已体现）。 |

### 3.4 与 docs/07-部署运维交付与商业化/delivery/试点学校验收清单.md 的关系

- 既有《试点学校验收清单》是**平台底座级**验收（开局初始化/学生端基础/教师端范围/管理端/安全合规/通知/知情确认），继续作为第一层，先行执行且必须全部通过。
- 本文是其 **13A/13B 模块增补件**：底座清单通过后追加执行本文 20 条；两者共用同一验收记录归档（建议在既有清单末尾追加一行"☐ 13A/13B 增补脚本 20 条已执行，记录见附件"）。
- 重叠项处理：底座清单的越权/脱敏/审计条目不因本文重复而豁免——本文的 403/404/409 负向补充是对 13A/13B 新端点的**增量覆盖**，不替代底座抽查。
- 本文不改动 delivery/ 目录既有文档；正式交付时可将 §3.3 记录表导出为附件归入交付包。

### 3.5 通过判定总口径

- **模块通过**：SA 组 10 条全部"通过/整改后通过"→ 13A 验收通过；AA 组同理 → 13B 验收通过；任一"不通过"未闭环则对应模块不出验收结论。
- **负向补充一票项**：任何一处 409 未拦截（产生双单）、403/404 未拦截（看到越权数据）为**一票不通过**，修复后整组复验。
- 本手工脚本与《13A/13B-开发阶段与验收用例》的自动化用例（42+8 条 / 11 流程）互为表里：自动化全绿是执行本文的前置条件，本文面向校方做"眼见为实"的抽样复证。
