# 07 学籍、注册、异动、专业分流、预警、毕业——代码对齐与页面设计


> 文档版本：V2·代码对齐与页面施工版  
> 审计基线：`penghaibin9/saas`，默认分支 `main`，审计时HEAD=`62c8b82c0f180bffc91f1721070aec7e2a6c576b`  
> 原10份文档提交：`089e3f59276154820556805393d53fce7100d934`  
> 适用对象：Cursor、Claude Code、Codex及普通代码模型  
> 决策规则：**现有代码事实 > 本V2裁决 > 原V1文档。发生冲突时不得自行猜测。**  
> 行号规则：文中的行号是本次审计快照；施工时必须先按“文件路径 + 类/函数/组件名”重新定位，禁止只按旧行号盲改。


## 一、冲突复审

原文档低估了部分现有能力：

- 注册已有`eligibility_status/note`、异常表和暂缓注册；
- 专业分流已有批次、容量、志愿、GPA快照、分配、调剂和确认；
- 学籍异动已有多节点、范围、待办、通知和唯一状态写入口；
- 预警已有多规则、干预、提醒、统一待办；
- 毕业已有十项三态和跨域联动。

因此不得重复建完整流程。真正补强点是页面组织、规则证据、影响预览和历史快照。

## 二、页面01：学籍名册与档案

### 名册首屏

```text
在籍8234 / 休学126 / 保留学籍18 / 退学42 / 待注册87
信息异常：证件缺失12、无班级6、重复学号0
[处理信息异常] [导入导出]
```

### 学籍档案

`AaRosterDetailView.vue`目标：

- 基本身份（敏感字段按授权查看）；
- 当前学籍；
- 组织归属；
- 注册历史；
- 异动时间线；
- 培养方案；
- 选课/成绩/预警/毕业摘要；
- 信息更正记录；
- 审计。

学籍档案是只读聚合，不复制各域数据。

## 三、页面02：注册工作台

`AaRegistrationWorkbenchView.vue`应从Tab列表升级为异常处置：

```text
批次：2026秋学期注册
应注册8234 / 已注册8102 / 未注册132 / 暂缓28 / 异常41
最近截止：9月15日

异常类型：材料缺失18 / 身份不符2 / 缴费未知16 / 其他5
责任：学院教务、辅导员、学生
```

现有`eligibility_status`先作为总体结论；若学校需要多条资格规则，再增加资格明细子表，不直接重复建新主表。

## 四、页面03：学籍异动

### 申请页

现有独立`AaStatusChangeFormView.vue`方向正确，补：

- 当前学籍事实；
- 申请材料清单；
- 规则解释；
- 目标组织选择；
- 生效影响预览；
- 审批路径；
- 暂存/提交。

### 审批页

`AaStatusChangeApprovalView.vue`从队列进入独立详情：

```text
申请人/类型/原因/材料
当前状态 → 目标状态
课表、选课、考试、成绩、宿舍、资助、实习、毕设影响
当前节点与后续节点
同意/退回/驳回
```

终审前必须展示“生效后系统将修改哪些字段/关闭哪些任务”。

## 五、页面04：专业分流

`AaMajorSplitView.vue`当前流程已较完整，页面需显式阶段化：

```text
配置专业容量 → 开放志愿 → 关闭 → 预分配 → 人工调剂 → 公示 → 确认生效
```

首屏显示：

- 应参与人数；
- 已填志愿；
- 未填；
- 容量；
- 未分配；
- 调剂；
- 可确认。

新增“分配结果解释”：GPA快照、志愿顺序、容量命中、调剂原因。

## 六、页面05：学业预警

`AaWarningConsoleView.vue`不再让用户在多个分类Tab间来回找同一学生。目标：

### 总览

- 新增预警；
- 高风险；
- 逾期未跟进；
- 已升级；
- 可自动恢复；
- 责任人缺失。

### 处置队列

按学生聚合：

```text
学生 | 预警组合 | 等级 | 责任人 | 首次响应期限 | 最近干预 | 下一动作
```

### 详情

- 触发证据；
- 规则版本；
- 干预计划；
- 谈话/联系记录；
- 学工联动；
- 恢复条件；
- 关闭证据。

规则配置继续复用配置中心，增加恢复规则和SLA字段，不另建平行预警表。

## 七、页面06：毕业审核

`AaGraduationAuditConsoleView.vue`已有大量Tab和十项Drawer，信息很全但认知负担大。

### 目标首屏

```text
批次：2026届毕业审核
应审2134 / 系统通过1980 / 有阻断154 / 待学院审80 / 待教务终审46
阻断归属：教务92 / 学工31 / 实习18 / 毕设13
[进入问题处置] [学院审核] [教务终审]
```

### 问题处置页

按“问题”而不是按十个Tab：

- 必修未通过；
- 选修学分不足；
- 实践不足；
- 实习未知；
- 毕设未知；
- 处分未解除；
- 方案无法解析；
- 其他。

每条显示责任模块、责任人、证据、跳转和关闭条件。

### 学生审核详情

新增独立路由：

```text
/graduation/:batchId/students/:studentId
```

不再把十项详情放Drawer。详情显示：

- 学生绑定方案及解析依据；
- 十项逐项结论；
- 原始证据；
- 人工复核；
- 历次复跑；
- 最终结论影响。

## 八、毕业逐项表的安全实施

当前JSON继续作为API摘要。新item表采用：

1. 预审时同时写JSON和item；
2. 对账item聚合结果与JSON完全一致；
3. 页面逐步读item；
4. 旧移动端仍读JSON；
5. 稳定后JSON只保存摘要，不删除。

## 九、学生方案解析

优先级：

```text
班级绑定
→ 专业+入学年级绑定
→ 生效日期内历史绑定
→ 无法解析则UNKNOWN
```

不得按专业取第一条ACTIVE方案。

## 十、首轮施工卡

```text
只做毕业审核学生详情独立页和方案解析修正。
不建graduation_item表。
新增/student详情路由，复用现有十项JSON数据。
后端封装resolve_student_program_binding并替换first ACTIVE逻辑。
测试班级特例、多年级、多版本、无绑定UNKNOWN、跨租户。
旧Drawer入口改跳新页并保留兼容，不commit/push。
```


## AI施工共同约束

1. 开工前必须同时读取：本专题文档、`academic-affairs.routes.js`、对应Vue页面、对应API文件、后端model/service/router/test。
2. 先输出“事实核对表”，明确哪些能力已经存在；**不得因为文档写了“新增”就重复建表、重复建service或重复建页面。**
3. 一轮只改一个页面主任务或一条业务闭环，不允许一次重写整个教务中心。
4. 旧路由保留redirect/alias；旧接口保持兼容；旧数据采用新增列/表、回填、双读、双写、切换的安全顺序。
5. 复杂业务使用独立页面。抽屉只允许：少量字段的新建、快速确认、简单原因填写；名单、编排、规则、审核详情、影响分析不得放抽屉。
6. 页面首屏必须回答：当前阶段、是否正常、阻断数量、责任人、截止时间、下一动作。
7. 后端必须覆盖：租户隔离、数据范围、权限、状态机、幂等、并发、审计、通知、归档写保护。
8. 测试只跑本轮相关文件及必要构建，不无理由跑全仓数据库测试。
9. 禁止 `git add -A`；禁止自动commit/push；完成后只提交修改清单、测试证据和剩余风险。
