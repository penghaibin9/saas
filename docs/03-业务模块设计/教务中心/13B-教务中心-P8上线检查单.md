# 13B 教务中心 · P8 上线检查单（验收唯一依据）

> 生成于 2026-07-06（P1–P7 全部端到端做通后）。按成熟商业教务软件(正方/强智)标准建，目标可卖。
> 验收命令：`cd backend && python -m pytest tests -q`（全量）。当前基线随各阶段递增，全程零回归。

## 一、六大业务域交付状态

| 阶段 | commit | 交付（商业软件标准） |
|---|---|---|
| P1 首页+学年学期+学籍注册 | 09cbf1c | 时间轴基座 + **change_student_status 单一写入口** + 入学/学年注册 |
| P2 学籍异动+方案骨架 | b6fec72 | 五类异动全链(休学/复学/退学/留级/转专业)+休学期限+转专业迁院系班 |
| P3 课程库+方案发布+教学任务 | 389b313 | 课程库全字段(学时构成/性质/类别/考核)两级审+版本化;方案两审发布;教学任务生成幂等 |
| P4 课表 | 505b66b | **教师/班级/教室三重冲突检测**+单双周+三视图+发布通知+作废重发 |
| P5 成绩+预警 | d5ca20b | **成绩平时+期末按比例合成**+发布回写 t_acad_grade;挂科预警规则引擎幂等 |
| P6 毕业预审 | (本轮) | **七项跨域供数三态判定**+学分按方案审+终审二次确认经单一入口写毕业状态 |
| P7 多端收口·后端 | (本轮) | 学生自视图(课表/成绩单/学籍异动申请/毕业进度)+教师课表 |

## 二、贯穿性红线

| 红线 | 落地 |
|---|---|
| **change_student_status 单一写入口** | 注册(P1)/异动(P2)/毕业终审(P6) 三处学籍变更全经此函数；合法转移白名单校验、乐观锁、异动流水、进360、审计五件事一事务 |
| 在籍判定 is_enrolled | 供全域入口(休学生禁请假/选课)；毕业预审 STATUS 项复用 |
| 复用不新建 | 成绩复用 t_acad_grade(投影回写)；预警复用 t_acad_warning(加2列)；毕业预审只读六域不复制 |
| 幂等 | 注册/教学任务生成/课表导入/预警扫描/毕业预审 均幂等，各有 409/count=0 用例 |
| 加列不动旧列 | t_acad_warning 加 source_code/rule_code(nullable)；t_aa_status_change 加 current_node/expire_date |

## 三、退出自检

1. **审批四件套**：异动(workflow多节点)/课程/方案/教学任务/毕业预审 各有审批链 + 待办 + 通知 + 审计；终态写 StageEvent 进360。✅
2. **错误码**：仅 400/403/404/409/422；业务无 500。✅
3. **scope/租户**：新端点经 require_staff + 租户过滤；_load 校验 tenant。✅
4. **成绩/学籍敏感**：学籍名册脱敏；成绩读侧零写入；异动原因脱敏位。✅
5. **回归**：全量 pytest 全绿（各阶段递增，零回归）。✅
6. **单一写入口红线**：全库 student_status 直写清单——建档初值(NORMAL)非转移属正常；`db_service.py` RECYCLED 直写为回收操作(不在注册/异动/毕业范围)，**待后续统一收编 + CI grep 门禁**（诚实）。

## 四、上线前待办（诚实清单）

1. **前端 academicAffairs 模块 + 小程序 .vue 页**：后端优先，学生端(课表/成绩/异动/毕业进度)与教师端页面待前端实现（接口已备齐，pytest 测后端）。
2. **组织三表加列维护页**（P2 范围，优先级低于异动/方案，未做）。
3. **迎新归档供数接入**（毕业预审 ARCHIVE 项当前 UNKNOWN 占位）。
4. **RECYCLED 直写收编 + CI 禁直写 grep 门禁**。
5. **双租户演示种子 + sandbox reset 覆盖 t_aa_* 表**。
6. **导出水印**（三名单/成绩单导出走 domain 导出管线，登记 t_export_task，待接）。

## 五、结论
13B 教务中心六大业务域后端 + 移动自视图端到端做通，按成熟商业教务软件标准建，可进入前端联调与演示。
