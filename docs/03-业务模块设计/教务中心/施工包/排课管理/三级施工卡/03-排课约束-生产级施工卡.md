# 03-排课约束 · 三级模块生产级施工卡

## 0. 卡片身份与状态

| 项 | 内容 |
|---|---|
| 业务中心 / 二级模块 / 三级模块 | 教务中心 / 排课管理 / 排课约束 |
| moduleKey / menuKey | `aa-scheduling` / `aa-scheduling.constraint` |
| 所属二级总包 | `排课管理-生产级施工包.md` |
| 当前代码基线 | commit `d2be0bf` |
| 施工卡状态 | `design_ready` |
| 当前菜单状态 | `navPlan.js:232`"排课约束"叶子 `planned` |
| 当前真实实现状态 | `missing`，与"排课规则"（02卡）共用底座 |

## 1. 证据与设计来源

| 设计项 | designSource | 依据 | 能证明 | 不能证明 |
|---|---|---|---|---|
| "排课约束"是"排课规则"的明细/子集，非独立业务对象 | `ai_proposal` | navPlan 将二者列为并列叶子（第232行），但手册R4指令原文只提"约束项：教师可用时间/教室类型要求/周学时分布，存规则中心"（下册:167），未区分"规则"与"约束"两个独立数据对象 | navPlan已有两个叶子需要都落地 | 不证明产品原意是否要拆两张表 |
| 具体约束类型示例（体育课后限制/课程间隔） | `project_rule` | `13B-...总册.md:429` E12（"教师每日上限、课程间隔、体育课后限制等，接平台规则中心"） | 已冻结的约束示例清单 | 不证明是否需要独立于"排课规则"页面 |

## 2. 当前真实代码审计

同 02 卡（无独立代码资产，全部 missing）。本卡与 02 卡共用后端表 `t_aa_schedule_rule`，前端在同一页面内以"约束"分组子表单呈现，不新建独立表/独立路由，避免拆分出两套几乎相同的CRUD代码。

## 3. 真实业务结果与边界

- **本卡与02卡关系裁定（`ai_proposal`，需二级总控确认）**：不新建独立三级页面/独立后端表，"排课约束"作为"排课规则"页面内的一个分组（"硬性/软性约束"tab），复用 02 卡的 `t_aa_schedule_rule` 表与 API，仅在 `ruleKey` 枚举中区分"规则类"（教师可用时间/教室类型/周学时分布）与"约束类"（课程间隔/每日上限/体育课后限制），navPlan 两个叶子指向同一路由的不同锚点/tab。
- 若总控认为业务上确需独立页面/独立表，需重新拆卡，本卡当前按最小可用、避免重复造轮子的原则设计。
- 进入/退出条件、上下游关系同 02 卡。
- 明确不做：不做约束驱动的自动排课引擎（P4冻结）。
- 仅学校能决定：具体约束参数默认值。

## 4. 页面/工作区与交互规格

复用 02 卡页面 `/admin/academic-affairs/scheduling/:batchId/rules?tab=constraint`，新增"约束"tab；不新建页面文件。

## 5. 字段、按钮与校验

约束类 `ruleKey` 枚举扩展：`teacherDailyMax`(教师每日上限节数)、`courseIntervalMin`(同课程间隔最小天数)、`peAfterBanCulture`(体育课后N节禁排文化课)。其余字段结构与 02 卡一致（`ruleKey`/`ruleValue`/`enabled`）。

## 6. 主流程、异常流程与状态机

同 02 卡，无独立状态机。

## 7. API 可编码契约

复用 02 卡 `GET/POST/DELETE /schedule-batches/{id}/rules[/{key}]`，约束类记录与规则类记录共用同一组端点，前端按 `ruleKey` 归类展示。不新增独立端点。

## 8. MySQL 数据与迁移

复用 02 卡 `t_aa_schedule_rule` 表，不新建表。

## 9. 权限总控逐动作实例化

复用 02 卡 `academicAffairs.schedule.rule.manage`/`academicAffairs.schedule.view`，不新增权限码。

## 10. 敏感数据、安全与审计

同 02 卡。

## 11. Excel、附件、通知、待办、统计、打印和归档

全部**不需要**。

## 12. 自动化测试与非技术验收

复用 02 卡测试用例，追加约束类 `ruleKey` 的枚举校验用例（T-13c：非法 `ruleKey` 值→422）。非技术验收：教务处在规则页切到"约束"tab→设置"体育课后2节禁排文化课"→保存成功。

## 13. 部署、初始化、升级与回滚

无独立迁移（复用02卡表结构）。

## 14. 精确施工文件与顺序

| 顺序 | 层 | 文件 | 目的 |
|---|---|---|---|
| 1 | 前端 | 02卡新页面内新增"约束"tab | UI分组 |
| 2 | 后端 | 扩展 `ruleKey` 枚举校验列表 | 支持约束类key |

## 15. 三级施工卡设计就绪门禁

- [x] 已明确与02卡的复用关系（避免重复对象，符合CLAUDE.md §6"不重复造轮子"）；
- 其余项同02卡，全部通过。

状态：`design_ready`（**特别标注**：本卡"不新建独立表/独立页面"的裁定为 `ai_proposal`，若总控/甲方认为"排课规则"与"排课约束"必须是完全独立的两套功能，需在总控复核阶段改判并重新分卡）。
