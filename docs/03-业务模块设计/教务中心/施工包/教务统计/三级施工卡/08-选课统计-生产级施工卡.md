# 08-选课统计-生产级施工卡

## 0. 卡片身份与状态

| 项 | 内容 |
|---|---|
| 业务中心 / 二级模块 / 三级模块 | 教务中心 / 教务统计 / 选课统计 |
| moduleKey / menuKey / route | `aa-stats` / `aa-stats-course-selection` / `/admin/academic-affairs/stats?tab=course-selection` |
| 所属二级总包 | `教务统计-生产级施工包.md`（`partial`） |
| 施工卡状态 | `blocked`（UNKNOWN 占位，与任务要求一致） |
| 当前菜单状态 | `navPlan.js:271` planned；依赖的「选课管理」二级模块 `navPlan.js:238 mod('aa-course-selection', '选课管理', null, P(...))` 全部 planned |
| 当前真实实现状态 | `missing`（无任何真实数据来源） |

## 1. 证据与设计来源

| 设计项 | `designSource` | 证据ID/代码位置 | 能证明什么 | 不能证明什么 |
|---|---|---|---|---|
| 选课模块完全未建 | `existing_code` | `grep "course_selection\|选课" backend/app/models/*.py` 全仓无命中；`frontend/src/config/navPlan.js:238` 全部 `P()` | 无任何选课业务表、无前端路由规划实现 | — |

## 2. 当前真实代码审计

| 层 | 已有资产 | 实现程度 | 缺口 | 复用/兼容决定 |
|---|---|---|---|---|
| model/Alembic/MySQL | 无（0 张选课相关表） | missing | 全部 | 不新建（属「选课管理」二级模块未来职责） |
| 后端 router/schema/service | 无 | missing | 全部 | 不新建聚合逻辑 |
| 前端 | 无 | missing | 全部 | 占位空态 |

## 3. 真实业务结果与边界

- 该三级模块让哪类学校用户完成什么工作：**当前无法完成任何真实统计工作**——学校尚无法通过本系统开展学生选课，无选课数据可统计。
- 本卡必须完成：诚实空态页"选课管理模块尚未启用，暂无统计数据"。
- 明确不做：不实现任何选课统计真实计算逻辑（无数据源，强行实现即造假）。
- 仅学校能够决定的业务政策：是否优先建设「选课管理」二级模块，决定本卡何时可施工——列入二级包 D-07 用户决定项。

## 4. 页面/工作区与交互规格

同「07 调停课统计」占位空态模式：空状态插画+文案"选课管理模块尚未启用，暂无统计数据"，复用项目"未开通"空态组件。

## 5. 字段、按钮与校验

不适用。

## 6. 主流程、异常流程与状态机

不适用。

## 7. API 可编码契约

| 方法与路径 | 用途 | 响应字段 | permission key | 错误码 | 测试ID |
|---|---|---|---|---|---|
| `GET /academic-affairs/stats/course-selection` | 返回模块未开通标记 | `{status:"MODULE_NOT_ENABLED", message:"选课管理模块未启用"}` | `academicAffairs.stats.view` | 200（业务空态） | T-EMPTY |

## 8. MySQL 数据与迁移

不适用。

## 9. 权限总控逐动作实例化

| 动作 | 角色 | 权限码 | 拒绝码 |
|---|---|---|---|
| 查看占位页 | 任意已登录教务角色 | `academicAffairs.stats.view` | 401/403（无权限） |

## 10. 敏感数据、安全与审计

不适用。

## 11. Excel、附件、通知、待办、统计、打印和归档

全部不需要（无数据）。

## 12. 自动化测试与非技术验收

| 测试ID | 操作 | 预期 |
|---|---|---|
| T-EMPTY | 打开选课统计 | 明确显示"模块未启用"，非报错非假数据 |

甲方验收：打开「选课统计」，确认页面诚实说明模块未启用，无虚假 0 值统计冒充已实现。

## 13. 部署、初始化、升级与回滚

不适用。

## 14. 精确施工文件与顺序

| 顺序 | 层 | 文件 | 目的 |
|---|---|---|---|
| 1 | 后端 | `academic_affairs_stats_service.py::course_selection_stats()` | 固定返回未开通标记 |
| 2 | 前端 | `stats/CourseSelection.vue` | 占位 UI |

## 15. 三级施工卡设计就绪门禁

全部适用项已满足（占位设计简单明确，无外部证据冒充，无技术事实甩锅用户）。

**状态判定：`blocked`**（诚实反映选课管理模块未建的事实，非缺陷）。
