# 前四阶段总体验收与服务器部署准备记录

> 验收日期：2026-07-18  
> 数据库：本地真实 MySQL 8  
> Alembic：`0109_implementation_permissions (head)`  
> 服务器状态：按用户要求未连接、未上传、未迁移、未重启

## 1. 总结论

国家标准全文库、业务关系自动安装、流程/工作台/通知真实主表、多实例共享导入批次四阶段均已通过对应数据库与端到端验收。第五阶段已完成 2U4G Linux 非容器部署准备包，但没有对真实服务器执行任何操作。

## 2. 第一阶段：国家标准全文库

真实开发库对账：

| 项目 | 数量 | 说明 |
|---|---:|---|
| 专业目录 | 1349 | 2021 版中职、高职专科、职业本科目录 |
| 文档 | 2098 | 教学标准 752 + 专业简介 1346 |
| 教学标准全文 | 752 | 教育部公告为 758 项；公开附件实际可取得 752 项，差异 6 项已保留来源对账，不伪造 PDF |
| 专业简介全文 | 1332 | 另有 14 项元数据级简介 |
| 可检索章节 | 20274 | 教学标准及专业简介结构化章节合计 |

验收覆盖全文关键词搜索、详情章节、官方来源标识、学校专业绑定和学生越权拒绝。核心用例通过。

## 3. 第二阶段：业务关系自动安装

已验证开户 XLSX 的辅导员—班级关系，在账号、组织和角色安装后写入真实 `t_class.counselor_id`；安装账本记录目标表、目标行和前后值；回滚恢复原值。重复账号确认不会重复写入或再次显示初始密码。

六类关系仍坚持“只绑定已有真实对象”：辅导员/班主任—班级、教师—教学任务、毕设导师—学生、实习导师—学生、宿管—楼栋。不生成虚假教学班、毕设批次、实习记录或楼栋。

## 4. 第三阶段：运行预设真实落库

干净租户应用高职标准版后，自动化验收确认真实生成：

- 24 个流程定义及责任节点；
- 12 套角色工作台；
- 16 个站内通知模板。

流程首次为待政策确认；缺少责任角色成员时阻断启用；确认后业务可发起；修改时限/责任角色后重新变为待确认。上线检查读取真实主表，不以配置快照冒充已安装。

开发库当前这三类租户运行数据为 0，是因为没有擅自给真实开发租户应用实施方案；自动化用例在隔离租户中完成安装与清理。这不是缺失数据。

## 5. 第四阶段：多实例共享导入批次

后端全量检索已不存在 `_MEM_BATCHES`、`_BATCHES: dict` 或 `_REQUESTS: dict` 导入批次实现。身份开户使用 `t_identity_import_batch`；学生主档、21 域历史迁移和旧契约入口使用 `t_shared_import_batch`。

已验证：模块重新加载后批次仍在、租户/操作者隔离、数据库行锁租约、同批并发拒绝、失败释放、重复确认幂等、日期类型跨 JSON 恢复、到期清空个人数据和保留期物理清理。

## 6. 自动化结果

| 验收 | 结果 |
|---|---|
| 国标搜索/详情/绑定 | 通过 |
| 预设快照、24/12/16 落库、流程门禁 | 通过 |
| 业务关系真实写入、重复确认、回滚 | 通过 |
| 身份批次跨模块重载、隔离、租约 | 通过 |
| 学生导入跨租户拒绝 | 通过 |
| 学期历史迁移日期持久化与覆盖 | 通过 |
| 健康/就绪探针 | 4 条通过；真实开发库返回 `READY` |
| 前端全量 ESLint | 通过（0 errors / 0 warnings；仅 Node 旧 `eslint-env` 提示） |
| PC 前端生产构建 | 通过 |
| miniapp H5 生产构建 | 通过，资源为相对路径 |

全前端 32 条测试现为 32 通过。此前失败的“保留学籍深链”是测试仍使用 `RETAINED`（留级）而菜单实现使用 `PRESERVED`（保留学籍）；已按后端状态机和国家标准语义修正测试。实施与预设中心新增菜单测试均通过。

后端全量 `pytest -q` 曾尝试执行，但本机其他长期运行的开发/测试进程同时占用同一 MySQL 测试库，导致全库 `drop/create` 在元数据锁处等待并最终超时；清理旧进程后，连续大批量重建/清理 283 张表又触发本机 Windows MySQL 原生崩溃。上述环境异常均不作为通过证据，前四阶段核心回归仍以明确的模块结果、健康探针 4/4 及各批次隔离/租约测试为准；后续应在独占的 Linux/MySQL 测试实例中再跑全量集。

身份导入单元回归现为 36/36 通过：新版模板包含业务关系工作表，解析器同时兼容旧 9 列行布局，并继续拒绝学生填写管理员角色。

后续回归还校正了开户向导测试契约：开局向导只写学校基础资料、院系、专业和班级；师生账号统一由“系统管理 → 身份导入”唯一入口创建，直接把学生或教师数组提交给开户向导会返回 `VALIDATION_ERROR`。本地 FAST 测试模式改为关闭外键后的数据 DELETE 清理，避免 Windows MySQL 对大批 TRUNCATE 的元数据提交等待。

2026-07-19 又补齐了运行预设的后期维护闭环：运行预设状态 API 返回工作台和通知完整配置，实施中心页面可直接修改工作台标题/副标题/启停状态及通知标题、正文、跳转地址和启用状态；工作流、工作台、通知保存均支持 `projectVersion` 乐观并发校验，过期版本拒绝覆盖；对应实施主流程测试在独占测试库中通过（1/1）。

业务关系页面也已提供安装后的安全回滚入口，回滚前执行版本校验和安装后漂移检查，只有目标未被人工修改时才恢复安装前值。

### 6.1 数据库迁移一致性补充检查

- 本地真实数据库的迁移链新增 `0109_implementation_permissions`，部署前需执行 `alembic upgrade head` 使 `current` 与 `heads` 一致。
- 对前四阶段涉及的 17 张表执行限定范围的模型—数据库结构比对：字段、类型、表、索引及唯一约束结构差异为 **0**。
- 全库 `alembic check` 仍会报告旧模块长期积累的注释、默认值和索引命名基线差异；这不是本轮 0103—0109 迁移缺失，也不应在本次部署前生成一个覆盖约 250 张历史表的大迁移。后续应另立“全库迁移基线治理”任务，先评审再处理。

## 7. 2U4G 非容器部署准备

已准备：

- `deploy/systemd/school-lifecycle-backend.service`：FastAPI 2 workers，仅监听本机；
- `deploy/systemd/school-lifecycle-scheduler.service`：单独执行逾期扫描、沙箱重置和导入批次清理；
- `deploy/env/backend.systemd.env.example`：生产安全配置及 2U4G 连接池参数；
- `deploy/nginx/school-lifecycle.systemd.conf.example`：HTTPS、PC/H5、API 反代和静态直链阻断；
- `scripts/deploy/preflight-linux.sh`：只读环境、密钥、资源和迁移链预检；
- `scripts/deploy/install-systemd-release.sh`：必须显式 `--apply`，先备份，再建版本、迁移、原子切换；失败自动退回上一代码版本，不自动倒退数据库；
- `scripts/deploy/verify-systemd-release.sh`：服务、Nginx、健康、迁移、生产文档关闭和鉴权冒烟；
- MySQL 备份/恢复脚本与共享批次清理脚本。

发布就绪探针现同时核验数据库、连接池、Redis（配置时）、审计写入状态、上传/导出目录和 Alembic 版本；任一关键项失败返回 HTTP 503。

## 8. 暂不执行项

根据用户最新指令，本次不登录云控制台、不读取服务器、不修改安全组、不安装软件、不上传代码、不执行生产备份/迁移、不重启 Nginx/MySQL/Redis/后端。未来开始部署时，必须先跑只读预检并确认现有服务和备份位置，再执行 `--apply`。

## 9. 2026-07-19 本地运行态复核

开发服务 `/health` 返回 `UP`，`/health/ready` 返回 `READY`；本轮已在本机开发数据库执行 `alembic upgrade head`，当前为 `0109_implementation_permissions`，权限表中 15 个实施中心权限点已落库。该复核只针对本机开发服务，不代表真实服务器已部署。

## 10. 2026-07-19 后续回归补充

- 共享导入回归：`test_migration_import.py` 10/10、`test_migration_import_p2.py` 5/5、`test_import_tenant_isolation.py` 2/2 通过。
- `tests/test_system_implementation.py`：6/6 通过，覆盖预设安装、工作台/通知编辑、版本冲突、变更项目继承、业务关系真实写入与安全回滚。
- `tests/test_national_standards.py`：3/3 通过，覆盖全文检索、详情章节、专业绑定和学生越权拒绝。
- `test_migration_import_p2.py::test_academic_chain_course_program_task_schedule`：通过；修复共享批次 JSON 反序列化后 `programKey` 数组不能作为分组键的问题。
- 教育部标准同步脚本改为懒加载 PDF 解析器，目录/元数据测试不再因本地 PDF 原生依赖导入而崩溃。
- 上线检查新增 12 类真实配置/数据证据、关系安装批次、责任角色和阻断状态；前端生产构建、回归 32/32、Lint 均通过。
- WARNING 检查项新增责任确认 API 与前端入口；阻断项明确禁止人工绕过，确认记录包含版本校验、说明、时间和审计事件。
- 本机开发库仍为 `0109`，真实服务器仍未连接。

## 11. 2026-07-19 本轮独立复核

- 本地 `GET /health/ready` 返回 HTTP 200、`status=READY`，数据库、连接池、审计、上传目录、导出目录和迁移版本检查均为 `ok=true`；当前/期望版本均为 `0109_implementation_permissions`。
- 本地 MySQL 复核数量：专业目录 1349、标准文档 2098、结构化章节 20274；实施中心权限码 15 个。
- 使用项目虚拟环境独立重跑 `tests/test_system_implementation.py`：6/6 通过；前端 `npm test -- --run`：32/32 通过；`py_compile` 与 `git diff --check` 通过。
- 服务器边界再次确认：本轮没有 SSH、SCP、云控制台、生产数据库、Nginx、Redis 或 systemd 操作；部署脚本仅完成审阅，必须由后续明确授权并在目标机执行 `preflight` 后才允许 `--apply`。
- 迁移补充：本轮新增 `0110_change_impact_analysis`（记录变更项目继承的安装版本）；本机已执行 `alembic upgrade head`，健康探针当前/期望均为 `0110_change_impact_analysis`。此前 section 1/9/10 中的 `0109` 表述是上一轮记录，以上述最新探针结果为准。
- 迁移补充（本轮继续）：新增 `0111_immutable_acceptance_summary`，验收封板现在持久化检查摘要、配置段哈希和摘要指纹；本机已再次执行 `alembic upgrade head`，健康探针当前/期望均为 `0111_immutable_acceptance_summary`。
- 跨阶段回归：`test_national_standards.py` 3/3、`test_migration_import.py` 10/10、`test_migration_import_p2.py` 5/5、`test_import_tenant_isolation.py` 2/2 均通过；说明 0111 迁移没有破坏国标检索、共享批次和租户隔离。
- 封板门禁复核：`ACCEPTED` 项目对重复验收和责任确认写入口均返回业务冲突，摘要指纹不会被后续检查确认覆盖；实施测试 7/7 通过。
- 运行预设封板复核：`ACCEPTED` 项目对流程策略、工作台和通知模板更新统一返回业务冲突；新增专项用例已验证流程更新入口不会绕过验收摘要冻结。
- 三入口专项回归：流程、工作台、通知三个更新 API 在封板项目上均返回冲突；专项测试通过。
