entries:
  - module: file-center
    client: shared
    route: document-preview-contract/v1
    page: docs/architecture/document-preview-contract.md
    action: preview
    fileCategory: ALL_PREVIEWABLE
    api: EXISTING_DOMAIN_PROVIDER_OR_FILE_CENTER_AUTHORIZED_BYTES
    backendService: backend/app/api/v1/file_contract.py
    storageMode: existing-authorized-bytes-only
    authMode: file-center-object+domain-provider
    dataScope: tenant+object-relation+domain-scope+batch+scan
    versioned: true
    scanGated: true
    preview: true
    download: false
    import: false
    export: false
    archive: false
    status: needs-verification
    risk: P0
    targetPhase: '10'
    notes: W0 freezes document-preview-contract/v1. W1 delivers Admin/Teacher PC AppDocumentViewer with PDF/Image adapters, lazy rendering, bounded ticket refresh, generation cancellation and resource cleanup. W2 delivers Graduation Teacher PC Gold integrations for final review, proposal review and the same-page Material Center fullscreen Reader through the existing graduation business-ticket boundary, canonical FileVersion lock, exact historical FileVersion reading, version-conflict fail-closed and per-version comment drafts. The shared four-client platform remains needs-verification until Student PC W3 and miniapp reader-return W4 are completed; later Office/DOCX waves also remain outside this status.
