entries:
  - module: platform-control
    client: backend
    route: /tenants/{tenant_id}/offboarding/preview
    page: backend/app/api/v1/control_plane_offboarding.py
    action: tenant-offboarding-business-preview-no-file-io
    fileCategory: BUSINESS_PREVIEW_NO_FILE_IO
    api: GET /api/v1/platform/tenants/{tenant_id}/offboarding/preview
    backendService: backend/app/services/tenant_offboarding_service.py::preview_offboarding
    storageMode: DATABASE_ONLY_NO_FILE_IO
    authMode: platform-super-admin
    dataScope: platform+target-tenant-impact-summary
    versioned: false
    scanGated: true
    preview: false
    download: false
    import: false
    export: false
    archive: false
    status: active
    risk: P1
    targetPhase: '10'
    notes: P0 退租影响预演仅返回租户状态、计数、Legal Hold、Purge Registry 与阻断项；不读取或生成文件字节，不创建 FileObject，不提供文件预览、下载、导入、导出或归档能力。路由因包含 preview 被文件能力扫描器高置信命中，故显式登记为 BUSINESS_PREVIEW_NO_FILE_IO，而不是绕过扫描器。
