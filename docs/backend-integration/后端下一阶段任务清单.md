# 后端下一阶段任务清单（P3 起）

1. **P3-DB**：本地/服务器 PostgreSQL 建库 student_lifecycle → `.env` DB_ENABLED=true → `alembic upgrade head` → 种子数据脚本（租户/组织/演示学生）。
2. **P3-学生主档**：student_service 真实 CRUD + 分页查询（keyword/college/major/class/status/riskLevel 全部落 SQL）；void 写 t_student_recycle_bin 冷冻期；字段级审计 t_student_field_audit。
3. **P4-审批**：t_workflow_def/node/instance/task/action_log 全量建模；approve/reject/transfer 走状态机 + 乐观锁 version；reject ≥5 字校验保留。
4. **P4-RBAC**：权限点表初始化（17 动作枚举）；数据范围规则 t_data_scope_rule 生效；接口级 permission 依赖注入。
5. **P5-文件/导入导出**：MinIO + 短期签名 URL；openpyxl；导出三档模式 + SoD。
6. **P5-审计落库**：audit_log → t_security_audit_log；登录/导出/敏感查看全埋点。
7. **P6-真实认证**：t_user + bcrypt/argon2；HttpOnly Cookie 会话（见 docs/security/05）；mock-login 仅保留 dev 环境。
