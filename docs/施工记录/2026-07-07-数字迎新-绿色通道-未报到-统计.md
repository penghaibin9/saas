# 2026-07-07 数字迎新 › 绿色通道 / 未报到学生 / 迎新统计（前端补页点亮）

## 结论
3 个 partial 页 → **implemented**。数字迎新 **8/19 → 11/19** 已真闭环。
这 3 页的后端接口上一轮 pytest 已验证过，本轮只补真实前端页（build 验证），未改后端。

## 做了什么（都接真实后端接口）
- **绿色通道**（`/admin/orientation/green-channels`）：审批闭环页——列表 + 筛选 + 通过 / 退回(原因≥5) / 驳回(原因≥5)，二次确认，接 `/orientation/green-channels`（approve/reject/return，后端已测 `test_green_channel_closed_loop`）。
- **未报到学生**（`/admin/orientation/no-show`）：未报到 / 延迟 / 未到校名单 + 单个/批量报到提醒 + 进学生详情，接 `/orientation/students?reportStatus=...` 与批量提醒接口。
- **迎新统计**（`/admin/orientation/statistics`）：KPI 卡（6）+ 报到环节漏斗（7 环节）+ 待办概览，接 `/orientation/dashboard`（后端已测 `test_dashboard_and_audit`）。

## 验证
- 前端：`eslint` 3 页 + 路由 + navPlan → **0 错**；`npm run build` → **通过**。
- 预览：3 页真实渲染，侧栏「绿色通道 / 未报到学生 / 迎新统计」全部点亮可点、零报错；统计页 6 KPI + 7 漏斗 + 待办正常。

## 改动文件（全前端，未改后端/数据库）
新增页：`OrientationGreenChannelView.vue`、`OrientationNoShowView.vue`、`OrientationStatsView.vue`
改：`modules/orientation/orientation.routes.js`（+3 路由）、`config/navPlan.js`（3 页点亮）

## 数字迎新剩余（8 个 planned，下一批做）
新生数据、新生信息核验、报到资格、报到流程配置、宿舍预分配、现场报到点、迎新通知、迎新归档。
其中多数需**新表 + 新接口 + 新页**（像上一轮迎新批次那样一条龙），按批扎实做，每批 pytest + build 验证。

## 待办（记账，不阻断）
- 权限 key 仍用 `orientation.student.view` 兜底，后续统一迁到 `studentAffairs.orientation.*`（旧 key 保留）。
- 迎新通知页要接真实通知任务表（渠道显示"未配置"，不接真短信）。
