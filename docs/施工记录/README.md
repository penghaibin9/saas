# 模块施工记录（施工地图 + 一次性完工台账）

> 本文件夹记录每个业务模块的**一次性全栈施工**（前后端闭环）。
> 规则：一次给一个模块，做完前后端闭环 + 测试通过，才算完工。

---

## 〇、数据库标准（MySQL-only，2026-07-07 收口）

1. **本项目正式数据库标准 = MySQL**（utf8mb4 / utf8mb4_unicode_ci）；开发库 `student_lifecycle_dev`、测试库 `student_lifecycle_test`。开发 / 测试 / 部署统一 MySQL。
2. **SQLite 仅作历史临时演示环境**（legacy，保留不删）；不得当正式验收。
3. **PostgreSQL 仅作历史兼容配置**（legacy，保留不删）。
4. **所有模块收口报告必须写明数据库类型**（例如"经 MySQL `student_lifecycle_test` 真库验证"）。
5. **模块标 `implemented` 前必须说明是否经过 MySQL 真库验证**；仅经 SQLite 通过的，只能记 `partial` 并在历史欠账登记"待 MySQL 真库复验"。
6. **历史欠账统一记录在** `docs/施工记录/历史欠账.md`。
7. **本目录所有文件必须 UTF-8**，禁止用 Word 另存为 `.docx`（会变二进制乱码）。

> 当前状态：config / alembic / conftest / .env.example 默认均已切 MySQL；**本机暂无 MySQL 服务器（3306 超时）**，企业库/实习批次/岗位库的 MySQL 真库复验待 MySQL 就绪后进行（见历史欠账"数据库·MySQL-only 收口"）。

---

## 一、后端开发环境状态

| 项 | 状态 | 说明 |
|---|---|---|
| 虚拟环境 `.venv` | ✅ 已配置 | 项目根 `.venv`（Python 3.12.5） |
| 后端依赖 | ✅ 已安装 | `backend/requirements.txt` 13 个包 + 传递依赖 |
| pytest 可运行 | ✅ 已验证 | `test_health` + `test_orientation` → 13 passed（30.73s） |

**跑后端测试的命令（每次开发后必跑）：**
```powershell
cd 职校学生全生命周期系统\backend
..\.venv\Scripts\python.exe -m pytest tests\test_xxx.py -q
```

环境配置详情见 `2026-07-07-后端环境配置.md`。

---

## 二、一次性完工标准（每个模块/页面点亮的验收线）

一个页面从 `planned` 点亮为 `implemented`，必须同时满足：

1. **前端页面接真实接口**（`request('/...')`，不是纯 mock 展示）；
2. **后端齐全**：model + service + api（+ 需要时 alembic 迁移）；
3. **状态机**：状态流转有校验，非法流转被拒；
4. **权限**：用 `studentAffairs.xxx.*` 新 key（旧 key 保留兼容，不删）；
5. **数据范围**：后端按当前角色 `resolve_teacher_scope` 过滤；
6. **审计**：关键操作写审计；
7. **脱敏**：敏感字段（身份证/手机号等）默认脱敏；
8. **导入导出**：需要的页至少 CSV/XLSX 之一，真实导出；
9. **pytest 通过**（自修复 ≤ 3 轮，修不好输出剩余错误 + 续接提示词）；
10. **前端 lint + build 通过**；
11. **navPlan.js** 对应节点 `planned/partial` → 更新为真实状态；
12. **不删旧路由/表/接口，不清数据，不改生产库，不提交 git（除非明确要求）**。

只前端完成、后端未闭环 → 标 `partial`（待补强）。
只有规划 → `planned`（待施工）。

---

## 三、模块进度表

| 一级中心 | 模块 | 状态 | 记录文件 |
|---|---|---|---|
| 学工中心 | 数字迎新 | ✅ **19/19 全部真闭环（模块完成，可交付）** | `2026-07-07-数字迎新-*.md`、`../modules/02A-…审计与续接说明.md` |
| 学工中心 | 学生画像 | ✅ **7 页 + 学生360（含跨域学工摘要）真闭环** | `2026-07-07-学生画像-学生360学工摘要.md` |
| 学工中心 | 其余（请假/奖助/宿舍/心理/家校…） | ⏳ 待启动 | — |
| 教务中心 | 全部 | ⏳ 待启动 | — |
| 毕业设计中心 | 全部 | ⏳ 待启动 | — |
| 岗位实习中心 | 企业库 | ✅ 前后端真闭环（共享企业主档 + 状态机 + 导入导出）· **经 SQLite 验证，待 MySQL 真库复验** | `2026-07-07-岗位实习-企业库.md` |
| 岗位实习中心 | 实习批次 | ✅ 前后端真闭环（状态机/时间轴/规则/台账导出；JSON 文本框待优化）· **经 SQLite 验证，待 MySQL 真库复验** | 记录文件待批次任务补 |
| 岗位实习中心 | 岗位库 | ✅ 前后端真闭环（复用企业库 + 8 态状态机 + batch_id 预留）· **经 SQLite 验证，待 MySQL 真库复验** | `2026-07-07-岗位实习-岗位库.md` |
| 岗位实习中心 | 其余（实习学生/协议/打卡/周报/成绩…） | ⏳ 待启动 | — |
| 系统管理 | 全部 | ⏳ 待启动 | — |

> 每完成一个模块，在此表更新状态并新增/更新对应记录文件。

---

## 四、"以后做"清单（本阶段先挂起，按需求方确认再接）

这些是**真实外部服务对接**，当前一律显示"未配置/待接入"，不阻断系统：

1. 真实短信通知接口（迎新通知、退回提醒、未报到提醒）；
2. 真实支付/财务系统对接（缴费状态、欠费金额）；
3. 真实身份证核验接口（新生信息核验）；
4. 真实小程序/移动端学生入口联调。

对接前：通知渠道状态 = `disabled/未配置`；缴费金额 = 状态字段建模；身份核验 = 人工/规则匹配。
**严禁**：点击后假装发送成功、假装支付成功、假装核验通过。

---

## 五、记录规则

- 每个模块一个记录文件：`docs/施工记录/<日期>-<模块名>.md`；
- 记录内容：本次做了什么、改了哪些文件、新增表/接口、pytest 结果、build 结果、点亮了哪些页、剩余 planned、续接提示词；
- 环境或全局性变更记在本 README + 单独的日期文件。

---

## 六、目录总则

1. 本目录用于保存每个模块的施工记录、验收报告和历史欠账。
2. 每个模块完成后必须在这里留痕。
3. `docs/施工记录/历史欠账.md` 是上线前总收口清单。
4. 开发新模块前必须先读取历史欠账，判断是否需要反向补全。

> 编码约定：本目录所有 `.md` 一律 **UTF-8 纯文本**，禁止用 Word 另存为 `.docx`（会变二进制乱码）。
> 2026-07-07 已将本 README 由 UTF-16 修复为 UTF-8（乱码备份见 `README.md.utf16.bak`）、将 `历史欠账.md` 由 `.docx` 二进制转为 UTF-8。

