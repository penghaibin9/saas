# 毕业设计中心 · 开题/成果连续双栏批阅工作区（2026-07-11）

> 依据 `docs/毕业设计中心/05-毕业设计中心生产级商业化开发总册.md` §17 事实矩阵（开题批阅/成果批阅 = partial）与 §19-P3
> 连续批阅要求施工；后端契约逐行核对 `backend/app/api/v1/graduation.py` + `services/graduation_service.py`。

## 提交

| commit | 内容 |
|---|---|
| `9504495` refactor(graduation-review): add continuous proposal review workspace | 开题审核双栏 + ProposalReviewCard 抽取 + 详情页瘦身为深链壳 |
| `8de1004` refactor(graduation-review): add continuous final submission review workspace | 成果检查双栏（行摘要+查重摘要+批阅/催交） |

## 一、开题审核（/admin/graduation/proposals）

- 新组件 `_shared/ProposalReviewCard.vue`：材料正文（背景/方案/成果）、附件（AppFileList）、历史版本、批阅（通过/驳回≥5字，AppPermissionButton 按 reviewProposal 权限）、开题答辩录入、审批留痕（AppAuditTrail）；双栏右栏与独立详情页共用同一张卡（一套真实闭环，无平行实现）。
- `ProposalListView.vue` 重写为连续双栏：左队列（AppSearchBox 关键词=后端 list_proposals keyword 真实参数、状态页签带 /proposals/stats 真实计数、AppPagination、行内当前选中高亮、逾期未交派生行）；右批阅卡 + 队列条（本页第 X/Y·共 N、上一条/下一条、"批阅后自动进入下一条待审"默认开）。
- 连续处理：批阅成功→行状态就地更新→自动选中下一条 PENDING_REVIEW（本页无→翻页续查→全部处理完提示）；↑↓ 方向键切换（输入框内不劫持）。
- 并发（后端 DATA_CONFLICT"该开题已批阅"）：显示原因 + 刷新队列与计数。
- 选中项写入 `?sel=`，刷新/分享不丢；筛选、页码、滚动因不离开页面天然保留。
- 窄屏（<1100px）：右栏隐藏，点击行进入原独立批阅详情页（整屏详情，不用窄抽屉）；`/proposals/:id` 路由保留（深链兼容），返回按钮带 `?sel=` 回到双栏。

## 二、成果检查（/admin/graduation/finals）

- 双栏同模式；成果无独立详情接口，右栏= 行摘要卡（学生/课题/导师/提交时间/状态/查重率+状态）+ 批阅表单（通过/退回≥5字，reviewFinal 权限）/催交卡（NOT_SUBMITTED）/结果卡 + 「查看学生完整档案」跳 `/students/:projectId`。
- 查重超标（>30%）：前端提示 + 后端拦截「通过」（真实 GD-R09 规则），不伪造查重结果。
- 页签计数接 /finals/stats（待审阅、查重超标）；keyword=后端 list_finals 真实参数。
- 窄屏：列表 ⇄ 整屏详情切换（带"返回列表"），不用窄抽屉。

## 三、验证

- `npm run build` ✅（11-12s，两轮）；ESLint 三个新/改文件 0 error 0 warning；文件完整性宿主侧核验（无 NUL/截断）。
- **浏览器走查未执行**（Chrome 扩展未连接）——两工作区状态为「代码完成，待浏览器复验」。走查清单：
  1. /admin/graduation/proposals：默认落待审阅队列、首条自动选中、通过/驳回并自动下一条、↑↓ 键、翻页续审、催交、刷新保留 sel、窄屏点击进详情页；
  2. /admin/graduation/proposals/:id 深链 + 返回；
  3. /admin/graduation/finals：同上 + 查重超标行「通过」被后端拦截的报错展示、档案跳转。

## 四、本轮裁决记录

- 批次条（GraduationBatchStrip）不再堆参与数/归档率等跨域指标：与毕设工作台重复、每页多 3 个请求，违背"高价值指标少而精"；批次条保持批次名/状态/届次/无批次引导/失败重试。跨域指标看毕设工作台与统计页。
- 双栏 CSS 在两视图中有约 40 行重复（pr-*/fr-*），未抽公共样式——避免再触发挂载连续写损（本轮曾有前科）；记为模块内小欠账。
- navPlan 39 叶 implemented 与总册 §17（9 implemented/6 partial）口径冲突：本轮不改共享 navPlan（红线），回真作为后续集成待办。

## 五、未做（进入下轮队列）

学生完整档案 16-Tab（F）、答辩全屏编排（G）、成绩批量核对（H）、风险处置双栏（I）、归档完整性/统计图表（J）、移动端。下一轮最高优先级：**F 学生完整档案**（成果双栏的"查看档案"已指向它，是当前动线的下一站）。
