# 下一步施工总控

> 更新：2026-07-09
> 用途：给 Cursor / 人工排期的**单一排期事实源**（与模块状态表、历史欠账一致）。

---

## 1. 当前不应该做什么

1. ❌ **不要**把全仓当成 Demo 继续堆 planned 页面的空壳。
2. ❌ **不要**并行大乱改：学工 B 包 + 教务大范围 + 毕设新二级 + 实习菜单回退。
3. ❌ **不要**恢复「学生中心 / 教学实践 / 数据中心 / 权限与流程」为一级导航。
4. ❌ **不要**毕业设计中心再做大规模导航/二级拆分（2026-07-09 已对标合并）。
5. ❌ **不要**数字迎新从零重写（只做强底座补强：Excel/日期/公共组件）。
6. ❌ **不要**读 `ai-handoff/`、`archive/` 当施工依据。
7. ❌ **不要**在本轮文档任务外改 `frontend/`、`backend/`、`miniapp/`（除非用户新任务）。
8. ❌ **不要** `git add -A`；不要提交 `dist/`、`.env`、`.claude/`、`query`。
9. ❌ **不要**把 PostgreSQL/SQLite 当正式验收数据库。
10. ❌ **不要**新增 CSV 正式导入导出。

---

## 2. 当前最应该做什么（优先级排序）

| 优先级 | 任务 | 说明 |
|---|---|---|
| **P0** | **学工中心 B 包** | 看板、班级管理、请假销假、宿舍管理、风险预警 — PC+后端+小程序+权限 |
| **P1** | **岗位实习 P3 补强** | 实习工作台聚合、周报 partial、实习学生 Excel、协议申请 partial |
| **P1** | **历史欠账 RBAC** | 细粒度 permission 后端拦截（迎新/实习/毕设等高风险操作） |
| **P2** | **数字迎新 D 包欠账** | 进度/异常 Excel、公共组件替换、日期底座 |
| **P2** | **Alembic 全链 stamp** | 上线前 B 类欠账；不阻断当前模块局部交付 |
| **P3** | **教务中心 V1 首模块** | 学工 B 有进展后再开；按 `13B-V1范围与开发顺序` |
| **冻结** | **毕业设计中心** | 仅修阻断试点/上线的 bug 与欠账 |

---

## 3. 关键决策问答

### 3.1 岗位实习下一步是否进入 P3？

**是。** 12 二级结构已 refactor 完成（commit a77d99c/e26fed1）。下一步是 **P3 体验与闭环补强**：

- 实习工作台（`in-workbench` partial → 真实聚合指标）
- 周报与任务（partial → 批阅闭环补强）
- 实习学生 CSV → Excel + 公共底座
- 申请与协议（partial → 审核台账）

**不是**回到 25 个旧二级菜单。

### 3.2 学工中心下一步是否进入 B 包？

**是。** A 包（施工地图 + 欠账冻结）已完成，见 `13A-学工中心四大任务包施工总图.md`。

B 包范围：学工看板、学生画像（收编）、班级管理、请假销假、宿舍管理、风险预警。

施工卡：`docs/modules/13A-学工中心B包-PC高频主线生产级施工卡.md`

### 3.3 毕业设计中心是否暂时冻结？

**是（大功能冻结）。** 核心链路已真闭环（批次/题目/选题/过程/答辩/归档等）。

允许：修 bug、欠账（alembic、RBAC）、小程序局部增强。

禁止：再拆新二级、大改导航、无验收标准的新三级。

### 3.4 数字迎新是否需要公共底座补强？

**需要，但归入学工 D 包，不阻塞 B 包。**

迎新主链路已 implemented；欠账：进度/异常导出、ImportDialog/ExportDialog 公共化、AppDate* 组件统一。

---

## 4. 不可并行开工组合

| 组合 | 原因 |
|---|---|
| 学工 B 包 ∥ 教务大范围 | 共享学生/班级/权限/审批底座，易冲突 |
| 毕设导航重构 ∥ 实习菜单重构 | 已完成；禁止再来一轮 |
| 文档体系梳理 ∥ 业务代码大改 | 避免 git 混杂无法审查 |
| 多 worktree 同时改 navPlan.js | 菜单事实源唯一 |

---

## 5. worktree 与分支注意

- 当前分支：`master`
- 近期实习 refactor 已在 master；新任务建议 **单模块单分支** 或明确 worktree 目录。
- 合并前必须：`npm run build` + 相关 `pytest` + 检查 `navPlan.js` 冲突。

---

## 6. 历史脏文件 — 禁止误提交

```
frontend/dist/**
backend/.env
.claude/**
query
*.zip in docs/ui/
```

工作区若混有 backend/frontend 未提交改动，**文档任务只提交 docs/**。

---

## 7. 下一次推荐给 Cursor 的任务（一句话）

> **执行学工中心 B 包：按 `13A-学工中心B包-PC高频主线生产级施工卡.md`，先做「班级管理」或「请假销假」任一完整闭环（PC 页 + 后端 API + MySQL + 权限 + 审计 + pytest），并更新 navPlan / 历史欠账。**

备选（若用户指定实习）：

> **岗位实习 P3：实习工作台真实指标聚合 + 实习学生导入导出升级 Excel 公共底座。**

---

## 8. 验收闸门（进入下一包前）

| 闸门 | B 包 | 实习 P3 |
|---|---|---|
| MySQL 真库 pytest | 必须 | 必须 |
| navPlan 更新 | 必须 | 必须 |
| 历史欠账更新 | 必须 | 必须 |
| 公共组件 checklist | 新页必须 | 导入导出必须 |
| 细粒度 RBAC | 可记 B 类欠账 | 可记 B 类欠账 |
