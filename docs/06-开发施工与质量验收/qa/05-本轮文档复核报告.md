# 05-本轮文档复核报告（PROJECT-CONTRACT-QA-DEMO-DOCS 自查）

- **文档性质**：对本轮新增 56 份交付文档的一致性复核报告（发现问题 + 修正记录 + 遗留说明）
- **适用端**：全端文档质量自查
- **依据**：`docs/api/00-API契约冻结总册.md`（错误码/响应权威）、`docs/database/00-数据库设计冻结总册.md`（数据范围权威）、真实代码 `frontend/src/`、`miniapp/src/`
- **复核方法**：客观交叉核对——把文档里的接口/错误码/角色码/数据范围/端口逐一与冻结文档和真实代码比对，而非主观通读
- **当前阶段声明**：复核只改本轮的 docs/ 文件，未碰冻结册、代码与其它任务包文件
- **生成日期**：2026-07-04

---

## 一、复核范围

本轮生成的 56 份文档（docs/api 9 + rbac 8 + testing 10 + demo 7 + backend-integration 10 + sales 7 + qa 5，本报告为 qa 第 6 份）。重点核对：统一响应结构、错误码命名、数据范围枚举、角色码、权限点、模块路由、端口、前后矛盾、sed 批量修正引入的副作用。

## 二、发现并已修正的问题

| 编号 | 严重度 | 问题 | 根因 | 修正 |
|---|---|---|---|---|
| DOC-FIX-01 | 高（一致性） | 错误码命名与冻结册不一致：文档用了 `NOT_LOGGED_IN / DATA_SCOPE_DENIED / NOT_FOUND / INVALID_PARAM / IMPORT_VALIDATE_FAILED / REJECT_REASON_TOO_SHORT / MODULE_NO_LICENSE / MODULE_READONLY / CONFLICT_VERSION` | 生成时的共享底稿自造了一套错误码名，未对齐冻结总册 `00` §三的权威命名 | 全库统一替换为冻结册权威名：`UNAUTHORIZED / NO_DATA_SCOPE / DATA_NOT_FOUND / VALIDATION_ERROR / REJECT_REASON_REQUIRED / MODULE_NOT_AUTHORIZED / MODULE_EXPIRED_READONLY / APPROVAL_VERSION_CONFLICT`；导入校验失败归 `VALIDATION_ERROR`（域码 `IMPORT_VALIDATION_FAILED`）。残留计数复查为 0 |
| DOC-FIX-02 | 高（一致性） | 数据范围枚举理解偏差：`docs/rbac/04` 称系统只有 6 类 `DATA_SCOPE`，并把"本专业/指定组织/自定义范围"标为"系统尚未实现/待后端确认" | 底稿只取了前端 `workflow.constants.js` 的 6 类简化枚举，漏看数据库冻结册 §10.2 已冻结的 **12 类** | 重写 `docs/rbac/04` 与 `docs/rbac/README`：以冻结册 12 类（含 `MAJOR`/`DEPARTMENT`/`TEMP_AUTH`/`CUSTOM` 等）为权威，前端 6 类标为"简化聚合"；题面 9 类全部映射到 12 类，不再有"系统未定义"的范围。与 `docs/backend-integration/04` 口径一致 |
| DOC-FIX-03 | 高（权威表） | `docs/api/04` 错误码表缺 `ROLE_NOT_FOUND/DATA_CONFLICT/FILE_TOO_LARGE/FILE_TYPE_NOT_ALLOWED/WECHAT_AUTH_REQUIRED/LOCATION_PERMISSION_REQUIRED/IDEMPOTENCY_CONFLICT/RATE_LIMITED`；且有重复行 | 底稿错误码表不全 | 重写 `docs/api/04` §四，与冻结册 `00` §三 21 条逐条对齐；补 §4.1 域码、§4.2 前端 419 说明 |
| DOC-FIX-04 | 中（批量副作用） | 批量改名把 `docs/api/02` 里"workflow mock 真实枚举表"的**原始名**也改了（`NOT_FOUND=40401` 被误改），破坏了"mock→统一码"映射价值 | 全局替换未区分"契约目标名"与"如实记录 mock 现状名" | 重写该映射表为"mock 真实枚举（数字）→ 统一字符串码"两列对照，恢复真实 mock 名 |
| DOC-FIX-05 | 中（批量副作用） | 批量改名后出现自我比较 gibberish（如"`VALIDATION_ERROR` vs `VALIDATION_ERROR`"）与同行重复列举 | 原句在比较"底稿名 vs 冻结名"，两边改同名后失去意义 | 清理 `docs/api/04`、`docs/backend-integration/01`、`docs/api/08` 相关句；改为"错误码已与冻结册统一，无差异" |

## 三、复核通过项（核对一致，无需修改）

| 项 | 核对结论 |
|---|---|
| 统一响应结构 | `code/message/data/traceId/timestamp` 五字段，全库一致，与冻结册 `00` §二一致 |
| 端口 | PC=5173、miniapp H5=5188，与 `frontend/vite.config.js`、miniapp 实际一致 |
| `requestId` 用法 | 三处含义未混淆：响应链路 `traceId`（≠requestId）、写操作幂等 `requestId`（请求体）、workflow mock 历史响应字段 `requestId`（如实标注） |
| 角色码 | 7 类 `ROLE_CODE` 与 `workflow.mock.js` 一致；题面 11 类角色的映射与"待后端确认"标注准确 |
| 权限点 | 抽样核对 `docs/rbac/06` 权限点均来自 `workflow.mock.js` / `student.mock.js` / miniapp `roles.config.js`，未见杜撰 |
| 模块与路由 | 12 个 PC 模块的 `moduleCode`/路由/首页权限点与 `adminMenu.js` 一致 |
| 红线 | 冻结册 `docs/api/00~05`、`docs/database/00` 全程未改（git 复查无改动）；未改任何代码；未碰其它任务包文件（`docs/security`、`docs/compliance`、`docs/demo-data`、`docs/demo/一套完整演示脚本.md`）；未 git add/commit/push |

## 四、遗留"待后端确认"清单（非缺陷，如实标注供后端拍板）

1. 前端 6 类简化数据范围 → 冻结册 12 类的**细分落地**，需后端在切真实 API 时补齐（`MAJOR`/`DEPARTMENT`/`TEMP_AUTH`/`CUSTOM` 前端暂未实现）。
2. 题面角色中"学工老师/系统管理员/校领导-院系领导"在系统中**暂无独立角色码**，文档给了建议映射，最终角色码与数据范围以后端为准。
3. 列表字段名 `list` 与历史冻结册个别接口的 `items` 差异，需后端 DTO 层归一（本轮不改冻结册，建议后端下一版同步）。
4. workflow 模块前端 mock 为**数字 code + `requestId`**，后端真实接口须转为字符串 code + `traceId`；前端切换时把 `code===0` 改为 `code==='SUCCESS'`。
5. `licenseContext`（模块授权/到期只读）为预留，`MODULE_NOT_AUTHORIZED`/`MODULE_EXPIRED_READONLY` 的计费判定是否启用待后端确认。

## 五、结论

本轮 56 份文档经交叉复核，发现 5 类一致性问题（含 2 个批量修正引入的副作用），**已全部修正并复验通过**；其余核对项与冻结文档、真实代码一致。遗留项均为"待后端确认"的设计选择，已如实登记，非文档错误。文档可交付。
