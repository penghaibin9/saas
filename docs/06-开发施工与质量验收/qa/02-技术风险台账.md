# 技术风险台账

- **文档性质**：技术与工程风险台账
- **适用端**：PC 管理端（`frontend/`）+ miniapp（`miniapp/`）+ 后端（`backend/`）+ 数据库 + 基础设施（跨端结构性风险）
- **依据**：
  - 共享事实底稿 `_shared_brief.md`
  - `docs/database/00-数据库设计冻结总册.md`（数据库设计，只引用不重写）
  - `docs/api/00-API契约冻结总册.md` 及分域文档（接口契约，只引用不重写）
  - `docs/security/00-等保三级安全基线总册.md`（安全基线，只引用不重写）
  - `docs/backend-integration/`（后端接入顺序与联调任务分工，由另一 agent 生成，本表风险的解决路径以其为准）
- **当前阶段声明**：本表登记的是项目当前所处 mock 阶段的**结构性/工程性风险**，如实反映现状：PC 管理端 UI 母版对接中、模块齐全、当前用 mock 数据；miniapp 纯 mock，可运行/可构建/可演示；`backend/` 仅有 config/middlewares/utils，真实接口**尚未接入**；数据库设计已冻结但**未真实联调**；前端权限判断（`permission.context.js` 等）**不可信**，一切以服务端二次校验为准；导入导出**未接真实文件服务**；当前**无真实登录鉴权**；等保三级**未测评**，仅有前端安全基线预留。这些风险不是"bug"，而是项目所处阶段的客观事实，登记目的是让管理层/客户方对"演示可用"与"生产可用"之间的差距有清晰认知。
- **生成日期**：2026-07-04

---

## 一、说明

本表所列风险均为**结构性、跨模块、影响上线判断**的技术与工程风险，区别于 `01-当前已知问题台账.md` 登记的具体功能点问题。每条风险附带缓解建议，缓解路径的具体任务分工与顺序以 `docs/backend-integration/` 为准，本表只做风险登记与阻断判定，不重复制定详细实施计划。

---

## 二、技术与工程风险清单

| 编号 | 风险或问题描述 | 影响 | 当前状态 | 建议处理时间 | 是否阻断演示 | 是否阻断上线 | 缓解建议 |
|---|---|---|---|---|---|---|---|
| RISK-001 | 后端未接入：`backend/` 目录当前仅有 config/middlewares/utils，业务真实接口尚未开发/接入；技术栈已拍板为 PostgreSQL + Python FastAPI + SQLAlchemy + Alembic，但尚无可运行的业务服务 | 系统当前不具备任何真实的服务端业务能力，所有前端展示均为静态/mock 驱动，不具备生产可用性 | 尚未接入（仅框架代码） | 后端接入阶段（按 `docs/backend-integration/` 计划推进） | 否（不影响当前 mock 演示） | 是 | 按 `docs/backend-integration/` 的任务清单与优先级顺序，分域（认证/学生主档/迎新/待办审批/岗位实习等，参照 `docs/api/00-API契约冻结总册.md` 已冻结的 5 个分域契约）逐步实现真实接口，并在每个域完成后对照 `docs/testing/` 对应清单回归验证 |
| RISK-002 | 数据库未真实联调：`docs/database/00-数据库设计冻结总册.md` 已完成设计冻结，但当前**未接入真实数据库实例**，前端/miniapp 均未与真实库产生任何读写交互 | 数据模型的实际可用性、约束完整性、性能表现均未经真实验证；当前所有"数据"均为前端 mock 静态数据，不具备真实业务数据能力 | 设计已冻结，未真实联调 | 后端接入阶段（与 RISK-001 同步推进，需先完成 Alembic 迁移脚本落地与库实例部署） | 否 | 是 | 按冻结的数据库设计执行 Alembic 迁移，搭建真实开发/测试库实例，逐域完成 ORM 模型（SQLAlchemy）与真实接口的联调，并执行数据完整性、约束、索引性能的基础验证 |
| RISK-003 | mock 数据与真实数据存在结构性差异风险：当前 PC 端（`frontend/src/mocks/`）与 miniapp（`miniapp/src/mock/`）的 mock 数据是按"演示剧本"手工构造的（如基准日 `TODAY=2026-07-02`、当前实习周 `CURRENT_WEEK=4`），字段覆盖度、边界情况（空值、异常状态、超大数据量）与真实生产数据可能存在差异 | 后端真实接入后，前端页面在真实数据的边界情况下（如超长文本、空数组、大分页）可能出现当前 mock 阶段未暴露的展示异常 | 大量使用 mock 数据（当前阶段普遍现象） | 后端接入阶段，每域接入后需专项验证边界情况 | 否 | 是（数据真实性是生产可用的前提） | 后端接入时优先用真实结构但小规模的种子数据做联调，逐步扩大到接近生产规模的数据量，并针对空值、异常状态、超大分页等边界情况专项测试（参见 `docs/testing/07-异常状态测试清单.md`） |
| RISK-004 | 权限判断仅在前端实现，不可作为安全边界：当前权限判断入口 `frontend/src/modules/workflow/context/permission.context.js`（`canAccessPage`/`canClickButton`/`canViewMenu`/`getDataScope`/`hasRole`/`hasAnyPermission` 等）仅在前端执行，属于 UI 层面的"该不该显示"判断，**前端判断结果不可信**，任何前端隐藏的按钮/菜单，只要接口可被直接调用，理论上均可绕过前端限制 | 若后端接口未做二次权限校验就直接暴露，将导致越权访问、越权操作等严重安全风险，属于生产环境不可接受的安全漏洞 | 前端已实现 UI 级权限控制，服务端校验**尚未接入**（后端未接入，见 RISK-001） | 后端接入阶段，需与后端接口开发同步完成，不能滞后 | 否 | 是 | 后端每个接口（`/api/v1/admin/*`、`/api/v1/student-pc/*`、`/api/v1/student-mini/*`、`/api/v1/teacher-mobile/*` 等）均需在服务端根据登录上下文解析的 `tenant_id`、当前角色 `currentRoleCode`、数据范围 `dataScope` 做二次校验，权限点命名与颗粒度参照共享事实底稿第 3 节与 `docs/rbac/` 矩阵，校验失败按统一错误码 `NO_PERMISSION`（403）/`NO_DATA_SCOPE`（403）返回 |
| RISK-005 | 导入导出未接真实文件服务：学生批量导入（`student.import`）、导出（`student.export`）、导出档案（`exportArchive`）等功能当前仅有前端交互与 mock 响应，`/api/v1/files/*` 文件中心尚未真实实现，文件上传/下载/解析校验均无真实后端支撑 | 导入导出在演示环境下看到的"成功"是模拟效果，不代表真实文件能被正确解析、校验、存储；上线后若直接沿用当前逻辑，文件相关功能完全不可用 | 未接真实文件服务（`backend/` 未接入的一部分） | 后端接入阶段，建议作为文件中心域优先实现（多个业务模块依赖） | 否 | 是 | 优先实现 `/api/v1/files/*` 文件中心统一服务，遵循共享事实底稿"文件上传只返回 `file_id`，业务只提交 `file_id`"的契约原则；落地上传白名单（pdf/doc/docx/jpg/jpeg/png/zip/rar/xlsx/pptx）、单文件 ≤50MB、黑名单（exe/js/bat/sh/php/jsp/html/svg）等安全基线要求（参见 `docs/security/00-等保三级安全基线总册.md`）；导入功能需实现真实的模板下载、逐行校验、错误行清单返回（对应错误码 `VALIDATION_ERROR`） |
| RISK-006 | 无真实登录鉴权：当前系统不存在真实的用户名密码/短信验证码等登录鉴权机制，PC 端与 miniapp 的"登录"均为 mock 模拟登录（如 miniapp 直接使用预置账号"学生 林可欣 学号 2024010612"“教师 张明远 工号 T20190087”） | 系统当前不具备任何身份鉴别能力，任何人在当前环境下均可"扮演"任意预置角色，不具备生产环境的基本安全前提 | 无真实鉴权（mock 登录） | 后端接入阶段，需优先实现（属于 `/api/v1/authz/*` 权限与身份底座核心能力） | 否（演示环境刻意如此简化） | 是 | 优先实现 `/api/v1/authz/*` 认证与当前上下文接口（参见已冻结的 `docs/api/01-认证租户与当前上下文API.md`），落地真实的登录鉴权（含首登强制改密、会话管理：PC 30 分钟无操作超时/移动端 7 天/并发 3/超时前 5 分钟预警等安全基线要求），`tenant_id` 由后端从登录上下文解析，前端不传 |
| RISK-007 | 性能与并发未验证：当前系统无真实后端与数据库，未做过任何真实场景下的性能压测、并发访问验证；共享事实底稿中提及的用户规模（如学生角色 `userCount 2140`、指导教师 `46`、辅导员 `12`、企业导师 `31` 等 mock 角色统计）仅为演示数据规模，不代表真实并发承载能力 | 后端接入后，若未做性能与并发验证，可能在真实多租户、多并发场景下出现响应慢、超时、数据库连接耗尽等生产问题 | 未验证（当前无可压测的真实环境） | 后端接入完成后、上线前 | 否 | 是（大规模并发访问下的稳定性是生产可用的基本要求，具体是否作为强阻断项视客户方规模与 SLA 要求而定） | 后端接入完成后，针对核心高频接口（登录、学生列表查询、待办审批、数据驾驶舱统计等）制定性能基准测试计划，覆盖单租户与多租户并发场景，明确响应时间与并发用户数的验收指标 |
| RISK-008 | 多租户隔离未真实验证：产品定位为多租户 SaaS（适用本科院校、高职院校、职业本科院校、应用型高校），共享事实底稿提及演示租户"演示职业技术学院 / 软件学院"，但当前**无真实多租户运行环境**，租户间数据隔离（`tenant_id` 由后端解析）、数据范围（`SELF/ASSIGNED/CLASS/COLLEGE/SCHOOL/ALL`）的越权隔离均未经真实验证 | 若多租户隔离在后端实现时出现疏漏，可能导致跨校/跨学院的数据越权访问，属于 SaaS 产品的核心安全红线，一旦发生是重大生产事故 | 未真实验证（当前仅前端 mock 单租户演示数据） | 后端接入阶段核心任务，需在每个接口开发时同步落地，上线前需专项验证 | 否 | 是 | 后端所有数据查询均需强制带 `tenant_id` 过滤（从登录上下文解析，不接受前端传参覆盖），数据范围 `dataScope` 的过滤逻辑必须在服务端实现并覆盖所有列表/详情接口；上线前需设计并执行专项的多租户越权测试用例（不同租户账号互相尝试访问、不同数据范围角色互相尝试访问），参见 `docs/testing/05-数据范围测试清单.md` 及 `docs/rbac/` |
| RISK-009 | 等保三级测评未做：共享事实底稿明确声明"当前为前端 mock / 预留实现，不代表系统已通过等保三级"；`docs/security/00-等保三级安全基线总册.md` 是安全基线设计文档，不等于已完成的测评认证 | 若客户方（尤其是公立院校）有等保三级合规硬性要求，当前系统不具备可用于生产环境的合规资质证明；未测评状态下上线可能面临监管合规风险 | 未测评（仅前端安全基线预留：会话管理、安全 HTTP 状态码、敏感分级、上传策略、导出策略、审计事件、CSRF 防护等均为前端预留设计，后端未落地） | 视客户要求，建议在后端接入完成、具备可测评的真实系统后启动测评流程 | 否 | 是（视客户合规要求而定；若客户为强制要求等保三级的公立院校，则为硬性上线前置条件） | 后端接入完成、真实鉴权与审计落地后，评估是否需要正式启动等保三级测评（需选择有资质的测评机构，测评范围覆盖网络安全、主机安全、应用安全、数据安全等维度），测评前需对照 `docs/security/00-等保三级安全基线总册.md` 逐项自查落地情况 |
| RISK-010 | 小程序真机与审核风险：miniapp 当前 `build:mp-weixin` 构建通过，但尚未在微信开发者工具/真机上做过完整联调验证，也**尚未提交微信小程序审核**；小程序上架涉及微信平台的类目资质、隐私协议、内容合规等审核要求，具体审核周期与驳回风险不可控 | 若小程序在真机环境暴露构建阶段未发现的兼容性问题，或审核被驳回（如资质材料不全、隐私协议不合规），会影响小程序端上线时间表 | 已构建但未真机联调、未提审 | 上线前，建议提前规划审核所需资质材料与提审时间 | 否 | 是（若产品上线范围包含微信小程序渠道） | 后端接口对接完成后，尽早在微信开发者工具中做真机预览与体验测试，提前准备小程序主体资质、隐私协议、用户协议等提审材料，预留至少 1-2 周的审核与整改缓冲时间 |

---

## 三、风险等级与关注优先级说明

以上 10 项风险均为"是否阻断上线=是"的结构性风险（RISK-007、RISK-009、RISK-010 视客户具体要求可能有条件放宽），说明当前项目所处阶段是**功能与交互设计已基本完成、可用于演示，但距离生产可用仍有完整的后端接入、数据联调、安全鉴权、合规测评等核心工作量**。建议管理层与客户方沟通时，明确区分"演示效果"与"生产可用性"两个维度，避免因演示效果良好而产生"即将可以上线"的误判。

风险解决的整体推进顺序建议：RISK-006（真实鉴权）与 RISK-001（后端接入）→ RISK-002（数据库联调）→ RISK-004（服务端权限校验）与 RISK-008（多租户隔离）→ RISK-005（文件服务）→ RISK-003（mock 与真实数据差异专项验证）→ RISK-007（性能并发）→ RISK-009（等保测评，视客户要求）/ RISK-010（小程序审核，视渠道规划）。具体任务拆解与时间排期以 `docs/backend-integration/` 为准。
