# 上线前必做清单 · 总闸门（真上线前必须逐条打勾，漏一条不上线）

> 面向甲方（非技术）：**你不用记这些，真要上线时打开这一页,从上往下逐条问"这条做了没"。全打勾才上线。**
> 维护规则：任何模块交付时,凡标「B·上线前」的欠账,必须同步登记到本页。本页是 `历史欠账.md` 里所有「上线前必须」项的**汇总视图**。
> 最后更新：2026-07-16（学工「风险分派责任人后端零校验」「学生选择器数据范围提示不实」登记入 B）

---

## 🔴 P0 安全红线（不做完绝不能上线，会出事）

- [ ] **真实权限拦截（RBAC 后端）** —— 现在后端权限拦截"近空",等于门没锁死。所有业务端点接入统一鉴权 + 数据范围 + 越权 403 + 审计。分支 `system-admin-role-bootstrap`（已发提示词③）。
    - 2026-07-12 进度①：`studentAffairs.risk.*`（风险）、`studentAffairs.risk.psyDetail.view`（心理 `/mental/*` + 旧 `/campus-service/mental-records`）**已接入细粒度权限**（`require_permission`）。
    - 2026-07-12 进度②（SEC-5 收口）：**新学工面 `/api/v1/student-affairs/*` 全部端点已接入** `studentAffairs.<域>.<动作>` 细粒度权限（`backend/app/api/v1/student_affairs.py`：dashboard/班级/辅导员考评/请假/困难/资助/违纪/画像/谈话/家校/归档 共 79 端点 `require_staff`→`require_permission`；仅 `/aid/.../reveal` 保留 `require_staff` 因授权+越权审计由服务层 `reveal_family_economy` 负责，网关不得短路吞审计）。`COUNSELOR` 角色已按「本班范围广读 + 班级/请假/风险/谈话/家校可操作、困难/资助/违纪只读、无 sensitiveView」实例化（`app/core/permissions.py`）。证据：8 套 affairs 测试真 MySQL **61 passed**（含各域 `counselor01` 跨范围 → `NO_DATA_SCOPE`、`aid/reveal` 越权 → 403 且落 `SENSITIVE_VIEW` DENY 审计）。
    - 2026-07-12 进度③（SEC-6 收口）：**旧「在校服务」面 `/api/v1/campus-service/*` 全部端点已接入** `campusService.<域>.<动作>` 细粒度权限（`backend/app/api/v1/campus_service.py`：dashboard/学生台账/请假/资助/宿舍异常/违纪/工单/审计 共 31 端点 `get_current_user`→`require_permission`；`mental-records` 早前已按 `studentAffairs.risk.psyDetail.view` 加固）。角色矩阵：学工处/院/`STUDENT_AFFAIRS`=`campusService.*`；宿管=`campusService.dorm.*`；辅导员=本班广读+请假审批（不含资助/违纪写）；领导只读；审计员见域审计；**学生/无授权教职工=拒**。证据：`tests/test_campus_service.py` 真 MySQL **10 passed**，含 SEC-6 回归（`student01`/`employment01` 读 `/disciplines`·`/grants` → 403 `NO_PERMISSION`，`sa_admin01` → 200 未误伤）。**旧实证「`employment01` 越权可读」已消除。**
    - 2026-07-12 进度④（统一安全底座收口）：新建 `backend/app/core/affairs_security.py` · `StudentAffairsSecurityContext`——一处解析 登录→角色能力→数据范围，供全部 student_affairs 服务与 legacy campus_service 共用。scope_type 由角色定（TENANT_ALL/COLLEGE/CLASS/STUDENT/DORM_BUILDING/SELF/**NONE fail-closed**），**彻底删除 `_allowed_class_ids` 的 `TENANT_FALLBACK=看全租户`**：未配范围的非管理角色返回空集合（列表空 / 详情·改·批·导出 403002）。同时修复 COLLEGE_ADMIN 跨院越权、心理摘要越权、async 中间件绑定当前用户以修 contextvar 传播缺口、请假台账导出落 `SENSITIVE_EXPORT`。`campus_service` 学生台账/请假/资助/违纪 的 list+detail+审批/作废已按班级范围收敛（与新学工面同口径）。证据：`tests/test_affairs_security.py`（真 MySQL，两院四班+跨租户+PSY 授权，先复现3洞后修复）**12 passed** + 9 套 affairs 回归 **88 passed** + campus 10 + teacher_scope/affairs_mobile 16。
- [x] **辅导员数据范围兜底红线** —— ✅ 2026-07-12 已解除：`resolve/_allowed_class_ids` 的 `TENANT_FALLBACK=看全租户` 已删，未配 `t_teacher_student_scope` 的辅导员现 fail-closed（列表空、详情/审批/导出 403002 NO_DATA_SCOPE）。回归证据见进度④ `test_affairs_security::test_scope_counselor_unconfigured_fail_closed`。（注：`resolve_teacher_scope` 在 mobile/internship/graduation 域仍保留旧 TENANT_FALLBACK，属各自域后续收敛，不影响学工面。）
- [ ] **文件中心底座**（病毒扫描 / 对象存储 / 签名下载）—— 现在附件走本地磁盘,上线前必须接真实对象存储 + 病毒扫描。
- [ ] **服务器数据库从 SQLite 迁到 MySQL**（若服务器仍是 SQLite）—— 先备份、出迁移方案再动,禁止直接覆盖生产数据（CLAUDE.md §36）。

---

## 🟡 B 级 · 上线前必须补（不补功能不完整，但不是安全问题）

### 学工中心
- [ ] **风险分派责任人后端零校验**（2026-07-16 登记）—— `affairs_risk_service.py` 的 `assign()`/`transfer()` 对 `owner_id` 是 `int(owner_id or 0)`，**不校验账号存不存在、是否同租户、是否持处置角色**。分派给虚构 id → 风险单显示"已分派"实则无人收到、无人处理（学生风险漏掉）；填非数字 → 500；分派给无 `studentAffairs.risk.*` 的教师 → 对方待办点开即 403 变死信。
    - 已做：新增只读端点 `GET /student-affairs/risk/owner-candidates`（按 6 个持处置权限的角色码收敛），前端责任人已改为只能从候选集下拉选，**界面路径已踩不到**；故降为 B 而非 P0。但后端仍可绕过（CLAUDE.md §6.1「前端隐藏不等于权限」）。
    - 未做原因：启用校验必然改红 `tests/test_affairs_risk.py` 的 r1/r2/r3——它们断言 `ownerId="486"` 能成功，而 `conftest` 不建任何 `User`，即测试把该缺陷固化成了预期行为。当晚共享 MySQL 测试库被并行会话持续 DDL（`1684 concurrent DDL statement`），不具备"改完能证明绿"的条件，未在无人值守时段落地。
    - 收账三步（需独占测试库窗口）：① `_seed()` 播种持 `OWNER_ROLE_CODES` 的在职 `User`，r1/r2/r3 改用真实 id；② 启用 `affairs_risk_service.py` 中已写好并注释保留的 `_validate_owner()`；③ 补负向用例（不存在 id / 非数字 / 无处置角色 → 均 `VALIDATION_ERROR` 非 500）。详见 `施工记录/历史欠账.md`。
- [ ] **学生选择器的数据范围提示与后端不符**（2026-07-16 登记）—— 前端多处 `dataScopeHint` 写「仅显示你数据范围内的学生」，但后端 `/students` 只有 `require_staff` + 租户过滤，**没有按辅导员班级范围收敛**。提示语是假的：辅导员搜得到全校学生。需二选一：后端补数据范围收敛（推荐），或先删掉这句不实提示。
- [ ] **3 套 studentAffairs 前端收敛成 1 套**（选 feat 最完整版为基线,接进菜单,删冗余）—— 欠账 SV-5。
- [ ] 辅导员/班主任绑定调整（#12，需院/处角色门禁 + scope 同步）—— 欠账 CL-1。
- [ ] 请假审批页从 legacy campus-service 切到 13A 端点 `/student-affairs/leave/*` —— 欠账 L-2。
- [ ] 班级材料 / 请假证明附件接文件中心（随 P0 文件中心底座）—— 欠账 CL-5 / L-3。
- [ ] 请假逾期升级(≥48h) / 到期前提醒 定时任务接调度 —— 欠账 L-5。
- [ ] 捞回的看板/画像/宿舍/风险/困难/奖助/违纪/谈话/家校/归档/统计页,逐页对齐真实后端字段 —— 欠账 SV-1 / SV-6。
- [ ] **活动二课与社团整域缺失**（无路由/无页面/无后端端点）—— 承诺的二级模块,需从零建后端+前端 —— 验收 2026-07-12。
- [ ] **宿舍与公寓前端管理界面**：现只有「刷新/宿舍风险」,缺房源/入住退宿/调宿/查寝/异常上报/统计操作页（后端 `/dorm/*` 已具备）—— 验收 2026-07-12。
- [ ] **学工归档缺批次列表端点** `GET /archive/batches`(现 405),前端刷新丢批次 —— 验收 2026-07-12。
- [ ] 空库**初始化种子**（班级/宿舍楼/示例数据）,否则学校首次进来处处空 —— 验收 2026-07-12。

### 毕业设计中心（移动端本轮欠账）
- [x] 教师端「成果审核」移动端（`GET /mobile/teacher/graduation/final/{id}` + 论文初稿/定稿单页队列批阅）。✅ 2026-07-11 闭环（真实前后端+范围校验+审计+pytest，查重超标 GD-R09 后端拒绝）。
- [x] 教师端 中期检查 / 评阅 / 答辩安排 / 成绩录入核对 的移动端视图。✅ 2026-07-11 闭环（中期 check/rectify-review；评阅 SoD 回避+评分0-100；答辩只读；成绩三段构成+复核）。
- [x] 毕设移动端 pytest 在 **MySQL 测试库**跑通补 §36。✅ 2026-07-11 毕设移动端全量+核心 **MySQL 50 passed**（不再是 SQLite）。

### 教务中心
- [ ] 教务中心前端 3 条分支（academic-affairs-ui-refactor 等）审查 + 捞回 master + 点亮菜单（同学工打法）。
- [ ] **教学质量-教学事故（04）具体认定标准与处理规则** —— 现仅提供上报/认定/关闭的技术容器（自由文本字段），事故等级划分与处理决定需学校提供教学事故认定管理办法原文，否则该功能只能"能记录"不能"按学校真实制度判定"。登记于 `docs/03-业务模块设计/教务中心/施工包/教学质量-生产级施工包.md` §17。
- [ ] **教学质量-质量整改/整改跟进（05/06）复核关闭权限口径确认** —— 现收紧为仅全校范围教务管理角色可复核关闭，学院范围角色只能发起/跟进；如学校要求学院自行闭环需另行放开并重新评估越权风险。
- [ ] **教学质量-质量归档（09）为读侧聚合轻量版** —— 未实现学期冻结封存/完整性检查清单，如需与「教务归档」（`aa-archive`）统一学期封存口径需另立任务。

### 岗位实习中心
- [ ] 实习小程序 + PC 页改造分支并回 master（已发提示词②）。

#### 07 整改 P5/P6 收口（2026-07-13，未提交未 push）
- [x] **P5.1 PC 操作按钮/标签角色化**：getContext 改由真实 permissionPatterns 驱动 permissionActions（弃 userType 猜角色）；18 个视图审核/发布/核验/分配/落岗按钮按后端真实权限码启停；文案改台账；路由 permissionKey 纠正到真实码。✅ build 绿 + 单测绿（含"禁装饰性假码"回归闸门）。后端 require_permission 仍是最终边界。
- [x] **P5.1 残留·路由守卫消费 meta.permissionKey**：新增 `frontend/src/security/permissionGate.js`（getContext 落库 patterns，router.beforeEach 调 canEnterRoute）。**仅拦岗位实习路由**（避免跨模块误伤）、**fail-open**（未登录/冷加载放行，后端兜底）、不匹配 → `/security/403`。✅ 7 单测绿。⚠️ 残留：**直链冷加载首个实习页仍先进后由后端 403**（守卫时 patterns 未加载），如需干净跳转需在 app 引导期全局预载 current-context（后续可做）。
- [x] **P5.2 教师移动端只见本人任务**：已核验——`test_teacher_scope.py` 覆盖 SCOPED 本班可见/非本班 403、跨租户 404、周报批阅越班 403、预警处理越班 403；`TENANT_FALLBACK`（空 `t_teacher_student_scope`→仅租户隔离）是**文档化试点兜底并有专测**。⚠️ 运营残留：标准版上线前须**播种该表让 scope 收敛到本人**（数据/部署任务，非代码缺陷）。
- [x] **P5.3 学生小程序自助入口打通**：请假/协议/意向/保险/计划/变更/自评/过程报告 8 页——补 `studentApi` 21 个 facade 方法（包已存在的 realApi 函数）+ 注册 8 条 `pages.json` 路由 + index 页加「自助服务」入口网格。✅ **mp-weixin build 绿**，21 个 facade 目标全部核验存在。
- [ ] **P5.4 企业联系人/导师门户或安全 H5**：🔴 **零基建 + 需学校政策**。EnterprisePortalLayout 未接线空壳；student-portal 是 STUDENT-only 脚手架；无外部身份基建（一次性令牌/魔法链接/企业 RBAC 供给全缺）；企业评价 100% 教师代录。按收口总控 §131 须先取学校对外部身份的政策与对接规范，**甲方 2026-07-13 拍板挂起等政策**。
- [x] **P5.5 跨端状态文案统一**：单一来源=后端 `statusLabel`。realApi 教师批阅映射透传 `statusLabel`，miniapp `MobileStatusTag` 加 `:label` 消费之——周报现两端一致显示「待批阅/逾期未交」。共享 map 未动（毕设/审批/请假等模块不受影响）。✅ mp-weixin build 绿。
- [x] **P6 越权/缓存测试**：菜单投影/搜索过滤/角色快照/按钮角色化/路由守卫 fail-open 与拦截/多身份缓存隔离（ctxKey 含 permissionVersion，升权换 ctxKey 投影失效）——共 25 前端单测绿。
- [ ] **P6 性能压测 / 审计监控面 / 回滚演练**：审计**写**已做（每域 `t_internship_audit_trail`）；统一审计监控面、容量压测、回滚演练需 **staging 环境 + 工具选型**（本地静态跑不算通过）。

### 小程序端 · 微信登录 + 生产配置（2026-07-14 登记；代码已就绪，缺你/平台侧配置）
> 微信一键登录前后端已实现并通过测试（后端 5 个 MySQL 单测绿；两端 build 绿），但**不配以下项就跑不起来**。逐条打勾：
- [ ] **后端 `.env` 填 `WX_APPID` + `WX_SECRET`**（你小程序的 AppID/AppSecret）——不填微信登录端点返回"未配置"，只能用账号密码登录。AppSecret 禁止写进仓库，仅经 .env/环境变量注入。
- [ ] **`miniapp/src/manifest.json` 填真实小程序 AppID**（顶层 + `mp-weixin.appid`，现为空）——不填无法真机预览/发布。
- [ ] **生产库跑 `alembic upgrade head`** 应用迁移 `0058_user_wx_openid`（给 `t_user` 加 `wx_openid` 列）——不升级则微信登录/绑定报列不存在。⚠️ 若并行分支另加了 alembic head，先 `alembic merge heads` 或改 `0058` 的 `down_revision` 指向最终 head 再升级。
- [ ] **微信公众平台配合法域名**：把 `https://api.hnyueke.com` 加入 **request / uploadFile / downloadFile** 三处服务器域名——域名须 ICP 备案 + 有效 HTTPS（微信不接受 IP/端口/自签）。
- [ ] **建 `miniapp/.env.production`** 填 `VITE_API_BASE_URL=https://api.hnyueke.com` + `VITE_USE_MOCK=false`（本地已由 AI 预建，`.gitignore` 忽略不入库；换构建机需重建）——不建则生产构建 baseURL 回退 `localhost:8000`，真机全部请求失败。
- [ ] （可选，产品决策）真实校名品牌：现小程序全程显示写死的"示范高校"，`/tenant/brand` 接口存在但未接——单校部署改 `miniapp/src/config/brand.config.js` 真实校名，或登录后动态下发（见任务卡「小程序品牌按租户下发」）。
---

## 🟢 C 级 · 增强（上线后迭代也行，不挡上线）

- [x] 毕设移动端附件真实预览/下载（接文件中心 file_id）。✅ 2026-07-11 主体闭环：开题/成果附件经 `attachment_view` 解析 file_id → 真实审计下载 `GET /files/download/{id}`；学生端 `uni.chooseFile/chooseMessageFile` 真实上传；`GraduationFinal.attachments_json` 列 + Alembic 0043。⚠️ 残留：文件中心对象存储 + 病毒扫描 + 真正签名/过期 URL 仍属 P0（见上方「文件中心底座」条），毕设附件随平台底座升级自动受益。
- [ ] 各模块 CSV → Excel 升级、体验打磨、性能优化等（见 `历史欠账.md` C 级条目）。

---

## 怎么用这张清单（给甲方）

1. **平时**：每次有人交付模块,让他把「上线前必须」的欠账登记到这里(P0/B 分级)。
2. **真要上线时**：打开这一页,从 🔴 到 🟡 逐条打勾。**🔴 一条没打勾 = 绝不上线**;🟡 没打勾 = 功能不全,评估能不能先上试点。
3. **找细节**：每条后面括号里是对应的欠账编号,去 `docs/06-开发施工与质量验收/施工记录/历史欠账.md` 搜那个编号看详情。

> 这张清单 + 历史欠账.md = 你的"防遗忘双保险"。你不用记,机器替你记;上线前过一遍这张表,漏不了。
