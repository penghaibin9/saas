# 上线前必做清单 · 总闸门（真上线前必须逐条打勾，漏一条不上线）

> 面向甲方（非技术）：**你不用记这些，真要上线时打开这一页,从上往下逐条问"这条做了没"。全打勾才上线。**
> 维护规则：任何模块交付时,凡标「B·上线前」的欠账,必须同步登记到本页。本页是 `历史欠账.md` 里所有「上线前必须」项的**汇总视图**。
> 最后更新：2026-07-20（补登「系统管理中心 · 国标预设体系数据灌库」——甲方 Codex 并行开发的系统管理中心批量代码此前一直未提交，本轮已核实并入 master/codex/mock/main 三分支且已推送 GitHub；顺带修复正式部署脚本会漏掉国标源文件的 bug）
> 历史：2026-07-17 深夜（三域只读销账核验：学工 11 条核验 7 销 4 留、教务 10 条核验 1 全销 6 改口径、实习+平台 11 条核验 3 销 2 半销——甲方并行施工已解决的记录按"解决即删"清除，剩余条目口径已对齐代码现状；另迁移体系全线收口=单头 a1198e75cb72 + dev 全链升级 + 全新库 266/266 表验证；学工请假逾期扫描已接启动定时 L-5 收口）

---

## 🔴 P0 安全红线（不做完绝不能上线，会出事）

- [ ] **真实权限拦截（RBAC 后端）** —— 现在后端权限拦截"近空",等于门没锁死。所有业务端点接入统一鉴权 + 数据范围 + 越权 403 + 审计。分支 `system-admin-role-bootstrap`（已发提示词③）。
    - 2026-07-12 进度①：`studentAffairs.risk.*`（风险）、`studentAffairs.risk.psyDetail.view`（心理 `/mental/*` + 旧 `/campus-service/mental-records`）**已接入细粒度权限**（`require_permission`）。
    - 2026-07-12 进度②（SEC-5 收口）：**新学工面 `/api/v1/student-affairs/*` 全部端点已接入** `studentAffairs.<域>.<动作>` 细粒度权限（`backend/app/api/v1/student_affairs.py`：dashboard/班级/辅导员考评/请假/困难/资助/违纪/画像/谈话/家校/归档 共 79 端点 `require_staff`→`require_permission`；仅 `/aid/.../reveal` 保留 `require_staff` 因授权+越权审计由服务层 `reveal_family_economy` 负责，网关不得短路吞审计）。`COUNSELOR` 角色已按「本班范围广读 + 班级/请假/风险/谈话/家校可操作、困难/资助/违纪只读、无 sensitiveView」实例化（`app/core/permissions.py`）。证据：8 套 affairs 测试真 MySQL **61 passed**（含各域 `counselor01` 跨范围 → `NO_DATA_SCOPE`、`aid/reveal` 越权 → 403 且落 `SENSITIVE_VIEW` DENY 审计）。
    - 2026-07-12 进度③（SEC-6 收口）：**旧「在校服务」面 `/api/v1/campus-service/*` 全部端点已接入** `campusService.<域>.<动作>` 细粒度权限（`backend/app/api/v1/campus_service.py`：dashboard/学生台账/请假/资助/宿舍异常/违纪/工单/审计 共 31 端点 `get_current_user`→`require_permission`；`mental-records` 早前已按 `studentAffairs.risk.psyDetail.view` 加固）。角色矩阵：学工处/院/`STUDENT_AFFAIRS`=`campusService.*`；宿管=`campusService.dorm.*`；辅导员=本班广读+请假审批（不含资助/违纪写）；领导只读；审计员见域审计；**学生/无授权教职工=拒**。证据：`tests/test_campus_service.py` 真 MySQL **10 passed**，含 SEC-6 回归（`student01`/`employment01` 读 `/disciplines`·`/grants` → 403 `NO_PERMISSION`，`sa_admin01` → 200 未误伤）。**旧实证「`employment01` 越权可读」已消除。**
    - 2026-07-12 进度④（统一安全底座收口）：新建 `backend/app/core/affairs_security.py` · `StudentAffairsSecurityContext`——一处解析 登录→角色能力→数据范围，供全部 student_affairs 服务与 legacy campus_service 共用。scope_type 由角色定（TENANT_ALL/COLLEGE/CLASS/STUDENT/DORM_BUILDING/SELF/**NONE fail-closed**），**彻底删除 `_allowed_class_ids` 的 `TENANT_FALLBACK=看全租户`**：未配范围的非管理角色返回空集合（列表空 / 详情·改·批·导出 403002）。同时修复 COLLEGE_ADMIN 跨院越权、心理摘要越权、async 中间件绑定当前用户以修 contextvar 传播缺口、请假台账导出落 `SENSITIVE_EXPORT`。`campus_service` 学生台账/请假/资助/违纪 的 list+detail+审批/作废已按班级范围收敛（与新学工面同口径）。证据：`tests/test_affairs_security.py`（真 MySQL，两院四班+跨租户+PSY 授权，先复现3洞后修复）**12 passed** + 9 套 affairs 回归 **88 passed** + campus 10 + teacher_scope/affairs_mobile 16。
- [x] **辅导员数据范围兜底红线** —— ✅ 2026-07-12 已解除：`resolve/_allowed_class_ids` 的 `TENANT_FALLBACK=看全租户` 已删，未配 `t_teacher_student_scope` 的辅导员现 fail-closed（列表空、详情/审批/导出 403002 NO_DATA_SCOPE）。回归证据见进度④ `test_affairs_security::test_scope_counselor_unconfigured_fail_closed`。（注：`resolve_teacher_scope` 在 mobile/internship/graduation 域仍保留旧 TENANT_FALLBACK，属各自域后续收敛，不影响学工面。）
- [ ] **文件中心底座**（病毒扫描 / 对象存储 / 签名下载）—— 现在附件走本地磁盘,上线前必须接真实对象存储 + 病毒扫描。
- [ ] **服务器数据库从 SQLite 迁到 MySQL**（若服务器仍是 SQLite）—— 先备份、出迁移方案再动,禁止直接覆盖生产数据（CLAUDE.md §36）。
    - 2026-07-17 进度：**本地 dev 库迁移链已全链收口**——分叉的 3 个 alembic head（0054 实习基线 / 4c722c7c33fa 帮助工单表 / portal_r3 学生门户）经 `alembic merge heads` 收敛为单头 `a1198e75cb72`，dev 库 `alembic upgrade head` 22 个待应用版本零报错跑通（256→267 表，t_feedback/t_user_preference/教务0085-0098/门户3表实测已建）。生产上线时在服务器执行同一条 `alembic upgrade head` 即可，无需再处理多头。

---

## 🟡 B 级 · 上线前必须补（不补功能不完整，但不是安全问题）

### 学工中心
> 2026-07-17 只读核验一轮：原 10 条中 6 条已被并行施工解决（三套前端已收敛为 modules 一套 / 捞回页对齐随之过时 / 活动二课与社团 25 端点+6 视图+4 测试文件全建成 / 宿舍前端 6 页齐全 / 宿舍写操作范围校验已铺全部写点 / 开局向导已覆盖组织+账号+范围九类实体），已按"解决即删记录"清除；同日再收口 2 条：**L-5 学工请假逾期扫描已接启动定时**（`AFFAIRS_LEAVE_OVERDUE_AUTO_SCAN` 开关+逐租户幂等循环）、**CL-1 班级绑定 scope 同步已补齐**（改绑辅导员撤旧立新 scope、班级改名 ref_value 跟随，`test_aa_orgs.py` 8/8 真 MySQL 绿）。下面是核验后**仍真实存在**的：

- [ ] **学生选择器的数据范围提示与后端不符**（2026-07-16 登记；2026-07-17 复核仍在）—— 前端 `entityPickers.js:44` 提示「仅显示你数据范围内的学生」，但后端 `/students`（`student.py:18` → `db_service.list_students`）仍只有 `require_staff`+租户过滤，未接任何 scope 收敛。需二选一：后端补数据范围收敛（推荐），或删掉不实提示。
- [ ] 请假审批页从 legacy campus-service 切到 13A 端点 `/student-affairs/leave/*` —— 欠账 L-2（2026-07-17 观察：另一并行会话已在暂存区做此项，勿重复）。
- [ ] **请假销假证明附件未接文件中心**（L-3 残余；班级材料 CL-5 已闭环 file_id）—— `affairs_leave_service.py` 销假仍只有 `proof_note` 文本，无 file_id 字段消费。
- [ ] 学工**请假逾期升级(≥48h)/到期提醒仍未接调度**（L-5）—— `scan_overdue` 只有手动 POST 端点；`main.py` 启动定时里只有沙箱重置+实习请假两条循环，学工请假不在内。补一条同款 asyncio 循环即可。
- [ ] 空库初始化种子·残余：**宿舍楼/示例业务数据未进开局向导**（组织/账号/范围九类已进 `onboarding.py` 向导，宿舍等仍靠散装脚本）。

### 平台运营端（2026-07-17 登记；本轮已修 5 项见 commit d0cd3c6，下面是修不了/不该现在修的遗留）
- [ ] **审计本体仍是"占位实现"**（P1，2026-07-17 部分收口，未全部解决）——`audit_log.py` 内存队列(2000条)为主；落库失败此前 `except: pass` **静默吞掉**，**已改为**记 `logging.error` + 暴露到 `/health/ready`（`checks.auditLog`，持续失败时整体 `status` 降为 `DEGRADED`），回归见 `test_health.py::test_health_ready_audit_log_degrades_on_db_write_failure`（3 passed）。**仍未做**：内存条目字段与 `SecurityAuditLog` 表口径统一、真正的 append-only 落库重做（与冻结册 §16「留存 ≥3 年」的根本对齐）——这两项工作量大、278 处调用点风险高，仍留专项，不在本轮范围。详见 `施工记录/历史欠账.md`「平台运营端审计整改遗留」。
- [ ] **平台端 4 块 mock 页面挂在正式路由**（落地看板/集成开放/API 访问/同步任务）——2026-07-17 复核仍全 mock（`platform.api.js:36` 静态 import mock，四视图均消费）。**新发现**：落地看板其实有真后端可接——`backend/app/api/v1/platform.py` 已有 `/overview` 等约 50 个真实端点，只是前端没接线；integrations/api-access/sync-tasks 三块后端确实没有。拍板建议更新为：落地看板直接接 `/overview`（纯接线），其余三块再二选一（下线标"建设中" or 排期做真后端）。
- [ ] **（运维必读）COS 密钥加密绑定 JWT 密钥**——平台端保存的腾讯云 COS SecretKey 用 JWT_SECRET_KEY 派生的密钥加密；**上线后如果轮换 JWT 密钥，文件存储配置会静默失效**（上传下载悄悄回退本地盘）。运维手册必须写：换 JWT 密钥后,到平台端「文件存储」重新保存一次配置。

### 毕业设计中心（移动端本轮欠账）
- [x] 教师端「成果审核」移动端（`GET /mobile/teacher/graduation/final/{id}` + 论文初稿/定稿单页队列批阅）。✅ 2026-07-11 闭环（真实前后端+范围校验+审计+pytest，查重超标 GD-R09 后端拒绝）。
- [x] 教师端 中期检查 / 评阅 / 答辩安排 / 成绩录入核对 的移动端视图。✅ 2026-07-11 闭环（中期 check/rectify-review；评阅 SoD 回避+评分0-100；答辩只读；成绩三段构成+复核）。
- [x] 毕设移动端 pytest 在 **MySQL 测试库**跑通补 §36。✅ 2026-07-11 毕设移动端全量+核心 **MySQL 50 passed**（不再是 SQLite）。

### 教务中心
> 2026-07-17 只读核验：毕业资格 9 个 tab 学号列已修（`list_results` 已 join 返回 studentNo/realName，销账）；排考的**排考场/监考**阶段已统一 `_time_overlap` 时段重叠判定；选课先到先得/退课/摇号写容量已条件更新原子化；分流/等级考/成绩认定 3 域小程序学生端全链路已通。以下为核验后仍真实在账（口径已按代码现状改准）：

- [ ] **前端分支捞回·仅剩尾巴**：`feat/aa-qz-scheduling` 还有 2 个前端提交未并入 codex/mock——990a2d8（5 个教务视图手写 table/select 改 DataTable/AppSelect）+ 206aea8（DataTable 增 rowClass）。其余分支（quick-phrases、merge/aa-p0）已确认并入。⚠️ 并入前勿动 AaEvaluationConsole 等 5 个视图，防冲突。
- [ ] **三孤儿端点接线剩视图层**：`rule-catalog`/`miss-report` API 函数已定义但无视图调用；`auto-times` 前端零命中。
- [ ] **排考时间编排阶段仍漏时段重叠**：`auto_assign_times` 仍按 `(class_id, exam_date, start_time)` 精确相等判撞（autoexam_service.py:450-486），与已修好的排考场阶段不一致。
- [x] ~~**TOCTOU 无锁**~~ **✅ 2026-07-17 三处全收口**：①摇号 `draw_round` CLOSED→DRAWN 改**条件更新抢占**（行锁下并发第二个 draw 匹配 0 行→409）；②自动排课容量 `capacityWarnings` 告警（未填人数任务不再静默盲排）；③**排考 `auto_arrange` 对批次行加 `FOR UPDATE` 行锁**（同批次并发排考串行化，第二个阻塞至第一个提交后读到已排 `arranged_ids` 增量跳过，杜绝重复建考场/座位/准考证号；dry_run 走普通读不占锁）。回归：`test_aa_autoschedule`+`test_aa_selection_rounds` 真 MySQL **27/27**；`test_aa_autoexam` 隔离 SQLite **11/11**（FOR UPDATE 单跑逻辑不破坏；并发锁属 MySQL 运行时行为，共享测试库当时被并行会话密集 DDL 无法直跑 MySQL）。
- [ ] **window.prompt 残余 2 处**：`AaSchedulingConsoleView.vue:196` + `AaEvaluationConsoleView.vue:184`（"原生弹窗清零"漏网；后者在未并提交 990a2d8 改动的文件里，等分支并入后一起清）。
- [ ] **纵深硬化残余**：认定附件仍只校验 tenant 无上传者归属（8a）；schedule move 仍 `require_staff` 粗粒度（8c）；移动端路由层未统一 `_require_student`（服务层已按 studentNo 收口、横向越权已封死，仅规范性残余 8b）。
- [ ] **毕业预审 REJECTED 死胡同**：学院退回后无 resubmit/重入路径（docstring 写"待重新初审"但代码无出口），唯一绕法另建批次——需产品定流程后补。
- [ ] **教学质量-教学事故（04）具体认定标准与处理规则** —— 现仅提供上报/认定/关闭的技术容器（自由文本字段），事故等级划分与处理决定需学校提供教学事故认定管理办法原文，否则该功能只能"能记录"不能"按学校真实制度判定"。登记于 `docs/03-业务模块设计/教务中心/施工包/教学质量-生产级施工包.md` §17。
- [ ] **教学质量-质量整改/整改跟进（05/06）复核关闭权限口径确认** —— 现收紧为仅全校范围教务管理角色可复核关闭，学院范围角色只能发起/跟进；如学校要求学院自行闭环需另行放开并重新评估越权风险。
- [ ] **教学质量-质量归档（09）为读侧聚合轻量版** —— 未实现学期冻结封存/完整性检查清单，如需与「教务归档」（`aa-archive`）统一学期封存口径需另立任务。

### 岗位实习中心
> 2026-07-17 只读核验：`fix/internship-audit-0717`（8表迁移基线+16处跨学院越权写收口）**已完整并入 codex/mock**（合并提交 ee3aef8）；沙箱已种 RUNNING 实习批次（下游页面不再空列表）；教师数据范围已改按本人账号 ID 主匹配（姓名仅历史兜底）；实习学生/匹配意向导入导出 **CSV 已消灭、全线 xlsx**（模板/dry-run/错误行齐全）——以上四项欠账已销。残余：
- [ ] 实习小程序 + PC 页改造分支并回 master（已发提示词②）。
- [ ] （口径统一，C级）实习学生/匹配意向的 xlsx 走 `xlsx_util` 直连，未接 `app/services/excel` 的 ImportSpec/ExportSpec 规格底座（现仅企业库在用底座）——功能完整，仅底座口径不一。
- [x] ~~岗位 companyId/batchId 非数字 → int() 500~~ **✅ 2026-07-18 已修**：`internship_position_service` 加 `_opt_int` 校验（空→None、非数字→400 VALIDATION_ERROR），create/update 两处收口。回归 `test_internship_position.py`（新增 `test_create_non_numeric_ids_400_not_500`）隔离 SQLite 全文件 14 passed。
- [ ] （C级，非阻断）岗位 `batch_id` 只存不校验批次存在/同租户——模块设计上刻意与批次表松耦合（文件头注明"不依赖批次模块"），如需强 FK 校验须先确认耦合取舍，非明确 bug。

### 就业中心
- [ ] **就业中心数据范围＝全校口径？需甲方确认**（2026-07-18 核验发现）——`employment_service` 全部 list/detail（学生/材料/未就业/跟进/企业/岗位/看板）**只按租户过滤、无学院/教师数据范围收敛**。当前默认角色矩阵下 `COLLEGE_ADMIN` 不含 `employment.*`，故非活跃泄露；**唯一风险**：`LEADER` 角色注释含"院领导"，若分配给院级领导，因 service 忽略 dataScope 会看到全校就业明细（含全量 `salary_range`，`_salary_mask` 只做空值兜底不脱敏）。二选一由甲方定：① 就业＝全校统一口径（现状即正确，销账）；② 就业须按学院收敛（则需照 affairs `_scope_or_403` 补数据范围 + 建院级就业角色）。
- [x] ~~新建成绩任务引用他租户 teachingTaskId~~ **✅ 2026-07-18 已修（教务/租户隔离）**：`academic_affairs_grade_service.create_grade_task` 的 `db.get(AaTeachingTask)` 补 `tenant_id != _tid()` 校验，跨租户引用 → 404；此前会把他租户 `teacher_key` 带进本租户新任务。回归 `test_aa_grade.py::test_g10_cross_tenant_teaching_task_rejected` 真 MySQL passed。

#### 07 整改 P5/P6 收口（2026-07-13，未提交未 push）
- [x] **P5.1 PC 操作按钮/标签角色化**：getContext 改由真实 permissionPatterns 驱动 permissionActions（弃 userType 猜角色）；18 个视图审核/发布/核验/分配/落岗按钮按后端真实权限码启停；文案改台账；路由 permissionKey 纠正到真实码。✅ build 绿 + 单测绿（含"禁装饰性假码"回归闸门）。后端 require_permission 仍是最终边界。
- [x] **P5.1 残留·路由守卫消费 meta.permissionKey**：新增 `frontend/src/security/permissionGate.js`（getContext 落库 patterns，router.beforeEach 调 canEnterRoute）。**仅拦岗位实习路由**（避免跨模块误伤）、**fail-open**（未登录/冷加载放行，后端兜底）、不匹配 → `/security/403`。✅ 7 单测绿。⚠️ 残留：**直链冷加载首个实习页仍先进后由后端 403**（守卫时 patterns 未加载），如需干净跳转需在 app 引导期全局预载 current-context（后续可做）。
- [x] **P5.2 教师移动端只见本人任务**：已核验——`test_teacher_scope.py` 覆盖 SCOPED 本班可见/非本班 403、跨租户 404、周报批阅越班 403、预警处理越班 403；`TENANT_FALLBACK`（空 `t_teacher_student_scope`→仅租户隔离）是**文档化试点兜底并有专测**。⚠️ 运营残留：标准版上线前须**播种该表让 scope 收敛到本人**（数据/部署任务，非代码缺陷）。
- [x] **P5.3 学生小程序自助入口打通**：请假/协议/意向/保险/计划/变更/自评/过程报告 8 页——补 `studentApi` 21 个 facade 方法（包已存在的 realApi 函数）+ 注册 8 条 `pages.json` 路由 + index 页加「自助服务」入口网格。✅ **mp-weixin build 绿**，21 个 facade 目标全部核验存在。
- [ ] **P5.4 企业联系人/导师门户或安全 H5**：🔴 **零基建 + 需学校政策**。EnterprisePortalLayout 未接线空壳；student-portal 是 STUDENT-only 脚手架；无外部身份基建（一次性令牌/魔法链接/企业 RBAC 供给全缺）；企业评价 100% 教师代录。按收口总控 §131 须先取学校对外部身份的政策与对接规范，**甲方 2026-07-13 拍板挂起等政策**。
- [x] **P5.5 跨端状态文案统一**：单一来源=后端 `statusLabel`。realApi 教师批阅映射透传 `statusLabel`，miniapp `MobileStatusTag` 加 `:label` 消费之——周报现两端一致显示「待批阅/逾期未交」。共享 map 未动（毕设/审批/请假等模块不受影响）。✅ mp-weixin build 绿。
- [x] **P6 越权/缓存测试**：菜单投影/搜索过滤/角色快照/按钮角色化/路由守卫 fail-open 与拦截/多身份缓存隔离（ctxKey 含 permissionVersion，升权换 ctxKey 投影失效）——共 25 前端单测绿。
- [ ] **P6 性能压测 / 审计监控面 / 回滚演练**：审计**写**已做（每域 `t_internship_audit_trail`）；统一审计监控面、容量压测、回滚演练需 **staging 环境 + 工具选型**（本地静态跑不算通过）。

### 系统管理中心 · 国标预设体系数据灌库（2026-07-20 登记；代码已就绪，缺一次性操作）
> 给非技术使用者：这一步不用你懂技术，只要在真正部署到服务器那天，让负责部署的人（或找 AI）照着下面命令跑一遍。**每套全新的服务器/数据库只需要做一次**，不是每个学校、每次更新都要重做——本地这台电脑上的开发数据库已经跑过了，不用管。
- [ ] **专业目录/教学标准/专业简介三份国标数据灌入生产数据库** —— 页面「系统管理 → 实施与预设中心 → 职业教育国家标准库」依赖数据库里的真实数据；服务器首次部署完（代码到位 + `alembic upgrade head` 建好空表后），必须手动跑一次同步命令，否则这个页面会一直空着，教务的「培养方案」也少了官方专业依据可绑定。命令、原理见 `docs/03-业务模块设计/系统管理中心/10-国家标准全文库实施记录与运维手册.md` §4；SSH 登录服务器后依次执行：
  ```bash
  cd /opt/school-lifecycle/current/backend
  .venv/bin/python scripts/sync_moe_vocational_catalog.py --input ../tmp/moe-vocational-catalog-2021.docx --apply
  .venv/bin/python scripts/sync_moe_professional_profiles.py --manifest-input ../tmp/moe-professional-profiles.json --apply
  .venv/bin/python scripts/sync_moe_professional_standards.py --manifest-input ../tmp/moe-standards-manifest.json --apply --offset 0 --limit 0 --workers 1
  ```
  跑完用 `GET /api/v1/national-standards/stats` 或直接看页面有没有数据来确认（应显示专业目录 1349、教学标准 752、专业简介 1332+14）。
  - 2026-07-20 进度：源文件（`tmp/moe-*.json`/`.docx`/PDF，共 12MB）此前误当「下载缓存」漏提交，已补提交进仓库；同时发现正式部署脚本 `scripts/deploy/install-systemd-release.sh` 的 rsync **原本会把 `tmp/` 整个排除**，导致即使代码到位也拿不到这几个文件——已修复（去掉 `--exclude 'tmp'`）。本地开发库（`student_lifecycle_dev`）已核实灌库完成：来源 3、目录 1349、正文 2098、章节 20274。

### 小程序端 · 微信登录 + 生产配置（2026-07-14 登记；代码已就绪，缺你/平台侧配置）
> 微信一键登录前后端已实现并通过测试（后端 5 个 MySQL 单测绿；两端 build 绿），但**不配以下项就跑不起来**。逐条打勾：
- [ ] **后端 `.env` 填 `WX_APPID` + `WX_SECRET`**（你小程序的 AppID/AppSecret）——不填微信登录端点返回"未配置"，只能用账号密码登录。AppSecret 禁止写进仓库，仅经 .env/环境变量注入。
- [ ] **`miniapp/src/manifest.json` 填真实小程序 AppID**（顶层 + `mp-weixin.appid`，现为空）——不填无法真机预览/发布。
- [ ] **生产库跑 `alembic upgrade head`** 应用迁移 `0058_user_wx_openid`（给 `t_user` 加 `wx_openid` 列）——不升级则微信登录/绑定报列不存在。⚠️ 若并行分支另加了 alembic head，先 `alembic merge heads` 或改 `0058` 的 `down_revision` 指向最终 head 再升级。
- [ ] **微信公众平台配合法域名**：把 `https://api.hnyueke.com` 加入 **request / uploadFile / downloadFile** 三处服务器域名——域名须 ICP 备案 + 有效 HTTPS（微信不接受 IP/端口/自签）。
- [ ] **建 `miniapp/.env.production`** 填 `VITE_API_BASE_URL=https://api.hnyueke.com` + `VITE_USE_MOCK=false`（本地已由 AI 预建，`.gitignore` 忽略不入库；换构建机需重建）——不建则生产构建 baseURL 回退 `localhost:8000`，真机全部请求失败。
- [ ] （可选，产品决策）真实校名品牌：现小程序全程显示写死的"示范高校"，`/tenant/brand` 接口存在但未接——单校部署改 `miniapp/src/config/brand.config.js` 真实校名，或登录后动态下发（见任务卡「小程序品牌按租户下发」）。
---

## 🟢 C 级 · 增强（上线后迭代也行，不挡上线）

- [x] 毕设移动端附件真实预览/下载（接文件中心 file_id）。✅ 2026-07-11 主体闭环：开题/成果附件经 `attachment_view` 解析 file_id → 真实审计下载 `GET /files/download/{id}`；学生端 `uni.chooseFile/chooseMessageFile` 真实上传；`GraduationFinal.attachments_json` 列 + Alembic 0043。⚠️ 残留：文件中心对象存储 + 病毒扫描 + 真正签名/过期 URL 仍属 P0（见上方「文件中心底座」条），毕设附件随平台底座升级自动受益。
- [x] ~~各模块 CSV → Excel 升级~~ **✅ 2026-07-18 核验已销**：全仓 `backend/app` grep `text/csv`/`csv.writer`/`writerow`/`DictWriter` **0 命中**，唯一 CSV 出现在 `import_export_service.py` 的**导入**兼容（允许 .csv 上传，非正式导出）；正式导出全线 xlsx。CLAUDE.md §6.1「CSV 不得作为正式交付主方案」红线未被违反。
- [ ] 体验打磨、性能优化等（见 `历史欠账.md` C 级条目；N+1 已增量收口 risk/funding 列表，余按页面频率逐模块收敛）。

---

## 怎么用这张清单（给甲方）

1. **平时**：每次有人交付模块,让他把「上线前必须」的欠账登记到这里(P0/B 分级)。
2. **真要上线时**：打开这一页,从 🔴 到 🟡 逐条打勾。**🔴 一条没打勾 = 绝不上线**;🟡 没打勾 = 功能不全,评估能不能先上试点。
3. **找细节**：每条后面括号里是对应的欠账编号,去 `docs/06-开发施工与质量验收/施工记录/历史欠账.md` 搜那个编号看详情。

> 这张清单 + 历史欠账.md = 你的"防遗忘双保险"。你不用记,机器替你记;上线前过一遍这张表,漏不了。
