# 施工记录 · 风险预警与处置 生产级闭环（2026-07-12 夜）

> 模块：学工中心 / 风险预警与处置（sa-risk）。代码事实基线 `c1cd650`（其上用户文档提交 `326a475` 未改代码）。
> 本次为**代码施工**（甲方明确授权进入编码 + 独立提交，禁 push）。仅改本模块相关代码/测试/前端 + 本施工记录。

## 1. 改动内容

| 文件 | 变更 |
|---|---|
| `backend/app/services/affairs_risk_service.py` | ① **SEC-2**：`_MENTAL_ROLES` 移除 `STUDENT_AFFAIRS_ADMIN`（学工处管理员默认不得见心理原始明细，权限总控 §7.3/13A §13）；仅保留心理老师 + 校/平台超管。② **敏感查看**：`get_risk(reason)` —— 心理来源明细仅授权角色 + 原因(≥5字)方 reveal，并写 `SENSITIVE_VIEW` 至 `t_security_audit_log`；无原因返回遮蔽摘要。③ **列表恒遮蔽**心理明细（`_row(reveal=False)`，仅摘要）。④ **sourceRefId 合法性**：非数字 → 400（修复旧 `int('manual-<ts>')` 抛 500）；`MANUAL` 来源忽略 refId、不去重。|
| `backend/app/api/v1/student_affairs.py` | 12 个 `/risk/*` 端点由 `require_staff` 改为 `Depends(require_permission("studentAffairs.risk.<action>"))`（view/create/assign/handle/transfer/escalate/close/reopen，对齐 13A §12）；详情端点增 `reason` 查询参。|
| `backend/app/core/permissions.py` | 补 `STUDENT_AFFAIRS_ADMIN`、`PSYCHOLOGY_TEACHER` 角色→权限码授予集（§12 对齐，启用细粒度权限所需底座）。|
| `backend/app/services/mock_auth_service.py` | 补测试身份 `sa_admin01`(STUDENT_AFFAIRS_ADMIN)、`psych01`(PSYCHOLOGY_TEACHER)。|
| `backend/tests/test_affairs_risk.py` | 重写 R6（心理遮蔽/reveal/原因/SENSITIVE_VIEW 审计）；新增 R8 401、R9 403001 无权限、R10 SEC-2 学工处遮蔽、R11 sourceRefId 400 非 500 + MANUAL 正常、R12 跨租户隔离 404。|
| `frontend/src/views/admin/student-affairs/StudentAffairsRiskListView.vue`、`StudentAffairsRiskDetailView.vue` | **前后端枚举统一**：`DORM_ABNORMAL` → `DORM`（对齐后端 `SOURCES`）。|

## 2. API / 权限码

- 权限码（全部 `studentAffairs.risk.<action>`）：view / create / assign / handle（process·follow·takeover·scan-timeout 共用）/ transfer / escalate / close / reopen；心理明细 = `studentAffairs.risk.psyDetail.view` 概念下由角色遮蔽 + 原因 + 审计实现。
- 拒绝码：401 `UNAUTHORIZED` / 403 `NO_PERMISSION`(403001) / 403 `NO_DATA_SCOPE`(403002) / 409 `APPROVAL_VERSION_CONFLICT`(非法状态) / 409 `DATA_CONFLICT`(来源去重) / 400 `VALIDATION_ERROR`(sourceRefId/结论字数)。

## 3. 验证（实际执行）

- **后端测试**：`python -m pytest tests/test_affairs_risk.py -q` → **12 passed**（0:03:48，**真实 MySQL** `student_lifecycle_test`，非 SQLite）。覆盖：状态机(R1/R2/R3/R5)、来源去重幂等(R4)、心理遮蔽/reveal/原因/SENSITIVE_VIEW 审计(R6)、跨班 403002(R7)、未登录 401(R8)、无权限 403001(R9)、SEC-2 学工处遮蔽(R10)、sourceRefId 400 非 500(R11)、跨租户隔离 404(R12)。
- **前端 build**：`npm run build`（vite）→ **✓ built in 11.37s**（exit 0；仅既有 chunk>500kB 提示，非错误）。
- **迁移**：本模块无表结构变更，`alembic heads` = 单一 `0043_gd_final_attachments`（未新增迁移）。
- **API smoke**：经 FastAPI TestClient 端到端覆盖（见测试）。

## 4. 数据库 / 迁移 / 回滚

- 无 schema 变更、无新迁移、无数据变更。
- **回滚**：`git revert <本提交hash>` 即可完全回退（无迁移需回滚）。

## 5. 遗留与后续

- **SEC-5**（全域 `require_staff` 无细粒度码）：本模块 `/risk/*` 已接入 `require_permission`；其余域仍待各自模块接入（非本模块职责）。
- **PSY_STUDENT 逐生授权**：心理来源当前按角色遮蔽（心理老师/超管可 reveal）；对"授权辅导员逐生可见"的 PSY_STUDENT 数据范围在**心理关注模块**落地。
- **并发**：状态机以 status 前置校验 + version 自增实现；非法状态并发 → 409（R3 覆盖）。显式双写并发压测留后续。
- 既有 `datetime.utcnow()` DeprecationWarning 为历史存量，非本次引入，不阻断。
