# 学工核心业务四端落地进度

固定入口：[学工核心业务四端分批落地固定提示词](../ai-prompts/03-学工核心业务四端分批落地固定提示词.md)。

## 总目标与续接位置

2026-09-05 用户把目标扩展为五批、四端全部落地。当前续工依据用户最新授权、适用项目规则、固定提示词、当前代码及实际验证推进，不将阶段检查等同于整个目标完成。下表于 2026-09-07 根据文末记录与关联执行记录校正，历史证据不自动代表当前全部回归通过。

| 批次 | 顺序 | 当前状态 |
|---|---|---|
| 一 | 请假返校 | 核心代码与真实四端交接已闭环，原申请最终 `CLOSED`；微信原生及发布门禁仍未验证 |
| 二 | 困难认定 → 奖助服务 | 困难认定、材料补交、公示申诉、奖助发放，以及勤工、贷款、减免和临补的核心代码与独立 MySQL 流程已闭环；保留联调样例仍在公示期，微信原生、银行外部回执及发布门禁未验证 |
| 三 | 住宿管理 | 5000 人自动分配和批量入住、调宿驳回重申、退宿、检查整改及高风险关闭的核心代码与 PC/H5 流程已闭环；微信原生及发布门禁未验证 |
| 四 | 风险处置 → 谈话家校 → 心理关怀 → 违纪处分 | 宿舍高风险、谈心转家校、心理自评转人工关注、处分申诉与解除均有同一对象跨端闭环；微信订阅消息、原生宿主及真实师生试用未验证 |
| 五 | 学生档案 → 班级工作 → 迎新报到 → 活动成长 → 统计归档 → 今日工作汇总 | 档案材料、主辅导员交接、迎新材料与既有报到主链、活动现场流转已闭环；统计、归档和统一工作台交接代码已收口，原生宿主及发布门禁未验证 |

五批核心代码施工已进入收口，不再保留“继续开发下一批”的错误口径；当前剩余工作属于候选提交、发布交接与宿主验收。第二批保留样例的公示期限及其他未验项仍保留。住宿/迎新的容量与主流程结果参见 [迎新四端联调执行记录](../../03-业务模块设计/学工中心/迎新四端联调-执行记录.md)。各批微信宿主和发布验收缺口持续保留，不用 H5、构建或接口回读结果代替。

## 第一批：请假返校

开始日期：2026-09-05。分支 main，基线 `6810bd6284b4d5c69a78947560f808cd7738327e`；以下结果来自未提交工作区，尚无候选 commit 验证或部署证据。

审计与设计：[请假返校施工包](../../03-业务模块设计/学工中心/施工包/请假返校-四端施工包.md)。包含三个学校实际采用系统的来源、真实状态流转、权限范围及四张三级施工卡。现有 `_thresholds` 为 3/7 天，未擅自改变学校审批政策或声称支持配置。

### 关联问题及已落地修复

| 问题与影响 | 修复及真实落点 | 验证 |
|---|---|---|
| 旧本人列表按姓名兼容，可能匹配同名学生 | `mobile_affairs_service.py:_self_leave_condition` 使用可信学生关联或租户内唯一学号；新增本人详情，内部审核字段与管理动作不进入学生详情 | 同名、他人、跨租户、64 位 ID 和允许/拒绝回归 |
| 日期解析混用带时区与无时区值，结束自然日可能漏查 | `affairs_leave_date_contract.py` 复用 `timeutil`；日期范围包含当地结束当天，UTC 入库；台账/统计日期使用相同边界 | 起止日期、偏移量、纯日期、统计过滤回归 |
| 教师手机列表固定 50 条，详情只有提示框 | 后端真实分页/关键词；教师 uni-app 列表、独立详情、材料、当前处理人、时间线及续返校动作 | 131 条分页搜索回归；手机 H5 实际审批 |
| 学生不知道当前处理人、补正入口与下一步 | 学生 PC 提取 `LeaveWorkspace.vue`，学生小程序接入 `MobileLeaveDetail.vue`；按实际状态显示重提/续假/返校入口 | PC 发起→教师退回→学生手机修改重提；意见、版本和时间线真实更新 |
| 提交已成功但后续读取失败可能重做写入 | 教师手机区分写入失败与读取失败；冲突停止连续办理，刷新真值后才能继续；保留失败意见 | `leave-workspace-actions.test.mjs` 行为回归 |
| 手机列表底部“新建”栏覆盖详情操作 | 详情打开时隐藏列表安全区操作栏，保留详情自己的固定底部动作 | 浏览器复现并修复，修改退回申请不再误开新表单 |
| 手机填写意见后下一确认框被前一弹窗关闭干扰 | 编辑意见弹窗关闭后再进入确认；不提前提交业务 | 实际执行通过、续假通过、返校确认弹窗 |
| 状态、审批节点、异常可能透传英文内部值 | `shared/contracts/leave-presentation.json` + 三端接口展示适配；未知值中性中文，用户事由原样保留；教师工作台角色/来源同步修复 | 每端 4 项展示行为测试，移动入口及材料状态合同 |
| 材料入口丢失请假上下文 | 真实 `LEAVE/bizId` 深链，材料查询在权限范围内先过滤再分页；复用 File Center 和版本补交 | 真实 MySQL 补交、版本、清单及敏感性验收 |
| 教师内容区被重复标题与时限卡占用 | 本批四页标题压缩，时限折叠，审批/跟进展示真实处理上下文；统计下钻台账并携带筛选 | 前端测试、构建、浏览器检查 |
| 教师正式壳仍是旧菜单，学生已经新壳 | `BasePortalLayout` 增加可选工作区呈现，本批请假页启用；复用原身份/搜索/数据范围，双窄栏、页签、四主题、可编辑常用栏 | 权限投影与恢复测试；浏览器最后验收进行中 |

### 四端页面和三级覆盖

| 业务页面 | 教师 PC | 学生 PC | 教师小程序 | 学生小程序 |
|---|---|---|---|---|
| 申请、补正、审批 | `/admin/student-affairs/leave`，队列与详情、理由及材料、通过/退回/驳回 | `/portal/campus-service?tab=leave`，本人列表、按需展开申请/修改、详情及进度 | `/pages/teacher/affairs-leave/index`，待审列表→详情→办理；支持 `recordId` | `/pages/student/affairs/leave`，本人申请与详情、退回重提；支持 `recordId` |
| 续假、返校、逾期 | `/admin/student-affairs/leave/followup`，续假/返校/逾期队列 | 从同一申请详情续假、提交销假，明确教师确认前未办结 | 同页“续假与返校”，阅读原/新截止时间、返校信息后办理 | 同一申请详情发起续假/返校，查看结果 |
| 台账与历史详情 | `/admin/student-affairs/leave/ledger`，过滤、真分页、详情抽屉、异步 xlsx | 同页本人记录；不提供全校管理台账或导出 | 授权跟进列表及详情深链；不复制 PC 全校导出 | 同页本人历史记录 |
| 请假统计 | `/admin/student-affairs/leave/stats`，真实统计并下钻带筛选台账 | 不适用：学生本人无管理统计权限 | 真实队列总数；管理大报表保留 PC | 不适用：本人状态不冒充全校统计 |
| 材料 | `/admin/student-affairs/material-operations?bizType=LEAVE&bizId=…` | `/portal/materials?bizType=LEAVE&bizId=…` | 学工材料中心保留同一业务上下文 | 学工材料中心保留同一业务上下文 |

列表、表单、详情均使用现有业务接口。材料绑定、工作流、统一待办、站内消息、审计、成长事件及后台导出仍由现行服务产生。未新建平行审批、上传或通知体系。

### 实际办事检查（智能体走查，非真人试用）

隔离验收学校的合成记录，请假 ID 为 1。PC 使用真实密码登录；学生/教师身份、已发布岗位模板及班级关系按现有初始化服务建立在测试库内，没有绕过权限或使用生产账号。

1. 新学生：学生 PC“请假与返校”→申请请假，填写假种、日期、事由；身份从本人关联带入，不重复填写学号。提交后显示当前审批人和办理时间线。
2. 经办教师：教师 PC“请假审批”找到同一申请，退回并写明返校安排需要补充。学生手机查看原日期与退回原因，修改后重提；新内容、版本和记录进入原单据。
3. 跨端交接：教师手机实际通过；学生 PC 刷新后申请续假，教师手机核对原/新日期和理由后通过，学生 PC 截止时间同步延长。
4. 返校：学生 PC 提交销假后显示“返校待确认”；教师手机核实并提交销假。最后状态需在末轮浏览器检查中复核，不把点击动作本身当作成功证据。
5. 业务管理员：本批新增的统计下钻、台账和材料入口沿用学校授权范围；学校管理员不因此获得学生本人申请或所有审批节点的办理权。审批阈值、调班及转派政策未改变。

已确认并修复的便捷性问题：手机详情误触新建、嵌套确认框、分页上限、提交后刷新失败重试、学生后台刷新丢工作区、重复顶部大块内容。未实现跨设备草稿，不将页签恢复说成表单恢复。

### 实际检查与环境

自建 MySQL 8 容器 `codex-leave-mysql-0ea278f0`，仅监听本机 3310。自动化库 `codex_leave_test_fba1c4667b28` 与 UI 库 `codex_leave_ui_test_0ea278f0` 分开。测试密码/连接串只在忽略目录，文档不保存凭据。模型建表用于当前链路测试，不作为 Alembic 升级通过证明。

| 范围 | 本轮实际命令 | 结果 / 忽略目录日志 |
|---|---|---|
| 后端核心 | `backend/.venv/Scripts/python.exe .codex-temp/leave-test-runner.py tests/test_leave_workspace_flow.py tests/test_affairs_leave.py tests/test_affairs_leave_pending_search_contract.py tests/test_affairs_four_end_ui_contract.py -q --tb=short --disable-warnings` | 40 passed，退出 0，491.61 秒；`leave-backend-verification.log` |
| 后端身份与材料边界 | 同 runner 执行 `test_leave_material_display_contract.py`、`test_affairs_leave_identity_cutover_contract.py`、`test_affairs_material_center_read_truth_contract.py`、`test_affairs_material_business_context_contract.py`、`test_affairs_material_biz_context_entry_contract.py` | 22 passed，退出 0，282.55 秒；`leave-boundary-tests.log` |
| MySQL 材料 | `backend/.venv/Scripts/python.exe .codex-temp/leave-material-acceptance.py` | 补交/版本/清单/敏感性验收退出 0；`leave-material-acceptance.log` |
| 教师 PC | `npm test`、`npm run lint`、`npm run build`（frontend） | 业务页轮 764 项通过及构建/lint 退出 0；同壳增量轮 768 项通过、lint 退出 0，构建待收齐；`leave-teacher-*-workspace.log` |
| 学生 PC | `npm test`、`npm run lint`、`npm run build`（student-portal） | 135 项通过，lint/build 退出 0；`leave-student-*-final.log` |
| 小程序 | `npm test`、`npm run build:h5`、`npm run build:mp-weixin`（miniapp） | 272 项通过；最后弹窗修复后相关 11 项再次通过，H5/微信构建退出 0；`leave-mini-*-final.log` |

后端测试存在现有 `datetime.utcnow()` 弃用警告；教师构建提示部分包较大。没有通过删测试或抹掉警告形成结论。没有运行整个项目上线门禁、生产压测或对现有学校库写入验收数据。

### 剩余验收与后续入口

- 教师新壳：四主题/两栏独立固定/页签关闭恢复/快捷编辑及台账统计浏览器验收，检查构建结果。
- 复核已提交的教师销假结果，核对台账与学生详情的同一最终状态。
- 微信开发者工具/真机、实际拍照与原生文件预览、真实微信订阅通知未验证；H5 和 mp-weixin 构建不能替代。
- 本次没有表结构变化。迁移 fresh/history upgrade、全量发布门禁、真实学校试点和正式部署尚无本候选证据。
- 本批收口后按顺序审计困难认定→奖助服务；追踪认定结果到奖助资格及发放台账的真实关联，不能只改两端列表。

### Git 与恢复影响

本轮未提交、推送或部署。保留历史路由，不删除旧数据或迁移；版本冲突、租户与节点裁决仍在后端。恢复范围是本次文件差异，测试运行产物不进入 Git。

原有改动：学生 PC 外壳、首页、浏览器验收脚本、外壳测试、固定提示词及学工索引。本轮还在 `AffairsFourEndView.vue` 中增量修改请假部分，保留已有页签同步。途中出现教务提示词/教务索引、AI 规划与审计文件等其他工作，未覆盖。

本轮业务改动覆盖后端 8 个入口/服务及相关测试，教师请假四页/API/展示工具/布局、学生请假与材料页面、学生/教师 uni-app 请假及材料入口、稳定中文契约；另新增教师工作区呈现和测试。具体工作区状态以最终 `git status --short` 为准，不把所有未提交文件归为本轮成果。

### 第一批补充复核（本轮）

之前三页提示修改属于代码进展，不能据此将第二批记作完成。本轮以工作区、实际服务与日志重新确认续接位置；没有重建丢弃旧验收记录。

- 隔离 MySQL 容器为 Exited，8018/5178/5198 无监听，已恢复原容器与原数据。5188 是另一个现有 H5 进程，未关闭或冒充隔离环境。
- 教师 PC 台账 ID=1 读取为已销假，详情有退回、修改、重提、批准、续假、返校确认全程；学生 PC 同一记录显示“本次请假已办结”，续假和返校信息一致。
- `.codex-temp/verify-leave-final-four-clients.py` 使用原隔离账号按 PC / TEACHER_MINI / STUDENT_MINI 正常密码登录，读取四个实际详情接口：`/student-affairs/leave/1`、`/mobile/teacher/affairs/leaves/1`、`/portal/affairs/leave/1`、`/mobile/affairs/leave/1/detail`，均为 CLOSED，退出0。教师原接口真值字段为 affairsStatus，学生接口为 status，不改接口伪造一致。日志 `leave-four-client-final.log`。
- 浏览器验证护眼绿、石墨紫、雾蓝、曜石黑选择；两栏取消固定、三级独立固定不改变二级；关闭台账→恢复；快捷名称改为“待我审批”、排序、刷新保留。检查实际护眼绿/石墨紫/曜石黑页面，窄窗口目录可展开。测试用视口已恢复默认。
- 已补读 `leave-teacher-build-workspace-final.log` 的完整结束标记：构建成功；768项教师新壳测试的历史结果与当前文件同壳部分一致。本轮教师全套又新增至770项，结果见下一节。
- 上述两条原“教师新壳/最终销假结果”剩余事项已复核；微信原生、真实学校试点、迁移及发布级门禁仍未验证，第一批不标 deployed/deliverable。

## 第二批：困难认定（施工中）

审计入口：[困难认定四端施工包](../../03-业务模块设计/学工中心/施工包/困难认定-四端施工包.md)。七个教师三级页、学生PC/手机、教师手机、原子申请替换、退回、异议、困难库、奖助资格、待办与敏感查询均已按实际符号追踪；三张审计清单和四端设计清单在施工包内。公开学校来源只核验场景，不照搬他校政策。

用户本轮补充“批量同步”，落实为当前模块四端一起同步，并未据此提交/推送Git或改学校真实数据。

### 首轮修复及四端同步

1. 公示确认传递列表所见 version，阻止重复点击；网络不确定时提示刷新核对，不自动换新版本重试。异议 API 适配器补 version 参数；现有异议页面的 versioned 调用保留。
2. 认定新申请统一检查已发布状态与已配置的起止时间，教师代受理、学生 PC 与学生手机原子提交共用裁决；退回补正保持既有在途规则。新增窗口时间输入接入两个原有批次新建入口；新时间按统一 timeutil 存 UTC，已有数据不迁移。
3. 学生开放批次先在SQL按窗口过滤；当前仍取第一页最多200条，超大开放批次量的翻页选择仍需完善，不能声称无限量覆盖。
4. 同批次已有终态或软删申请时，管理受理也返回业务冲突并引导原申请，避免重新INSERT撞唯一键。
5. 核心 `affairs_aid_service.presentation` 提供统一中文状态与进度提示；两学生端和教师PC/手机同步展示。学生手机等级显示中文、公示标“拟认定等级”，四端不再按含“通过”二字推断流程已完成。学生承诺文案去掉哈希等技术说明，实际确认留痕未改。

### 本轮验证

所有命令在当前未提交工作区执行。测试与UI库分离，未写生产或现有学校数据库；无表结构变化。

| 范围 | 实际执行与结果 | 日志（忽略目录 `.codex-temp/`） |
|---|---|---|
| MySQL窗口、评审、公示、异议 | `backend/.venv/Scripts/python.exe .codex-temp/leave-test-runner.py tests/test_aid_workspace_window.py tests/test_affairs_aid.py tests/test_affairs_aid_objection.py -q --tb=short --disable-warnings`；16 passed，退出0，260.27秒。包含新建窗口拒绝/允许、终态重复、多级评审到困难库、公示异议。此轮启动在随后纯展示增量前 | `aid-window-backend.log` |
| 展示与退回契约增量 | 同 runner 执行 `test_aid_workspace_window.py::test_four_client_progress_never_confuses_review_publicity_with_completion`、`test_affairs_aid_detail_projection_contract.py`、`test_affairs_aid_draft_resubmit_contract.py`；3 passed，退出0 | `aid-presentation-backend.log` |
| 教师PC | npm test：770 passed；npm run lint/build 退出0。新增真实组件方法行为回归：原版本、重复点击、409、不确定请求；测试正则lint问题修正后相关2项及lint/build重跑通过 | `aid-teacher-test.log`、`aid-teacher-lint.log`、`aid-teacher-build.log` |
| 学生PC | npm test：135 passed；npm run lint/build 退出0 | `aid-student-test.log`、`aid-student-lint.log`、`aid-student-build.log` |
| 两小程序工程 | npm test：272 passed；build:h5、build:mp-weixin 退出0 | `aid-mini-test.log`、`aid-mini-build-h5.log`、`aid-mini-build-mp-weixin.log` |

教师构建仍有现有大包提示，后端有既有弃用警告。测试覆盖已修复断点，不证明本批七页完整设计、四端浏览器业务串联或微信真机已经完成。

### 下一施工位置（不得跳过）

- 完成教师手机待审先按权限与责任人过滤再分页，修复100条截断；真实第101条与异议/调整分别验收。
- 补批次草稿发布真实入口、版本裁决及完整配置页；统一批次新建入口。学生可申请批次的真实分页选择。
- 按施工包完成学生PC/手机的列表→详情→按需申请/补正重构，教师PC七页同壳紧凑工作区和教师手机完整办理详情；本轮仅同步进度，不作为“顶级UI”验收。
- 公示统计前200条冒充全量问题、到期时间/可执行动作、敏感材料入口、完整时间线与下游奖助资格衔接继续施工。
- 同一组隔离记录验证四端正常/退回/重提/异议/认定/调整及错误恢复，然后进入奖助服务。全五批目标保持 active。

### 2026-09-06：困难认定批量同步（本轮续接）

用户确认好用页面可保留。本轮沿既有审计清单修复困难认定链路，复用七页表格、筛选和既有敏感授权，不重写审批政策。

- 教师手机：131条真实待办可完整分页；SQL先按节点权限/组织范围/最新具体受理人过滤。返回allowedActions，恢复原先因为缺动作而隐藏的按钮；搜索按姓名/学号，字面百分号不作通配符。分页失败保留记录、旧请求不覆盖新搜索。
- 待办直达：浏览器发现原页没有消费recordId，已补精确详情读取，终态可查看、拒绝时不替换其他学生；审批后刷新同一事项。调整通过仍须选择目标等级并确认，按钮改四字以避免手机换行，点击区至少44px。
- 教师PC：七个aid三级页接入已认可外壳；简称为“批次/评审/公示/台账/名册/统计/异议”；真实ctx角色与范围替换静态“资助老师/全校”。指标卡与标题压缩；成熟表格继续保留。
- 批次草稿新增真实发布动作，权限、租户、软删、DRAFT、期限与原版本检查及审计在同一事务；重复发布拒绝。按ID获取批次，历史单据所属批次即使不在首100条也能精确进入。
- 公示卡片改用后端total，不再reduce前200条；批次按状态读取total，“已发布”与是否处于申请窗口分开。失败统计显示未知。
- 学生PC：去掉技术化说明；已有申请时收起新表单，先看记录；新增页内刷新进度，保留表单输入。学生小程序正确分别传入原始status和中文label，修复“辅导员初审”被组件当枚举而显示“状态待确认”。

#### 本轮实际验证

- 隔离MySQL `test_aid_mobile_queue.py` + 原核心/异议套件通过13项；`test_aid_batch_publish.py` + 窗口/详情投影通过7项，共20项。日志 `aid-sync-backend.log`保留首次发布跨租户用例失败（测试helper固定了原租户）；改为显式设置第二租户后 `aid-sync-batch-backend.log` 7 passed，186.85秒。分页首次使用mock用户ID无法对应真实任务，已换正常密码登录；请求校验按项目统一400校正断言，未修改生产校验规则。
- 教师PC测试新增至773项；小程序新增分页/通知直达/调整选择行为测试；学生PC135项通过。浏览器发现按钮换行，改文案后原按固定文案断言失败，已更新断言并新增真实选择等级与取消不写入行为验证。最终命令结果以本节后续日志记录为准。
- 教师、学生PC lint/build及小程序H5、mp-weixin构建运行通过，已有大包和datetime弃用提示保留；不是发布级全项验收。
- 隔离UI仍使用原8018后端、5178教师PC、5198学生PC；仅重启确认归属的leave-ui-server进程。5188用户已有服务未动。另用临时配置在5208启动指向8018的双小程序H5；没有更改正式manifest端口。测试学校、账号、批次及申请均为合成数据。
- 浏览器实际：教师PC发布批次1 DRAFT→OPEN，计数与“查看申请”同步；学生PC看到该批次、提交申请1；教师手机首页出现真实待办，从待办进入申请1，班级评议通过后为COUNSELOR_REVIEW；学生PC页内刷新显示“辅导员初审”，学生小程序同一记录的进度同样为辅导员初审。手机视口验证后已恢复默认。当前UI单据留在初审，未用修改时钟等手段伪造整段公示完结。

#### 下一步继续，不得标全模块完成

最终复核：`aid-sync-teacher-test.log` 773 passed、`aid-sync-student-test.log` 135 passed、`aid-sync-mini-test.log` 279 passed；共1187项。PC两端lint/build退出0，小程序H5与最后一次mp-weixin构建退出0。`git diff --check`当前相关文件通过。学生PC点击“刷新进度”和学生小程序标签已实际观察为“辅导员初审”。构建日志分别为 `aid-sync-{teacher,student}-build.log`、`aid-sync-mini-{h5,weixin}.log`；UI库申请1仍为辅导员初审，后续沿用它继续验证。

1. 在上述隔离申请上继续退回/补正/重提、学院/学校终审、公示异议与认定/调整四端视觉和实际业务联动；MySQL核心闭环已测不替代四端页面全链验收。
2. 学生手机目前仍默认显示长申请表，需要按记录/申请/补正任务重排；学生PC详情材料/完整时间线、成功后编辑提示语义继续打磨。
3. 教师PC评审页仍重复角色上下文、两个建批次入口；材料查看、到期信息、批次选择分页、动态调整权限与资格衔接继续完善。名册/台账/统计保留好用结构，逐页验证后再决定局部调整。
4. 七页深色与不同尺寸、真正微信工具/真机及订阅通知仍未完成。无Git提交、推送、部署；全五批目标继续active。

### 2026-09-06：困难认定学生详情、原文补正与四端同步

本轮先复核 `aid_my`、学生门户适配、`affairs_student_returned`、两端 API 与工作区，再实施本人详情。既有教师队列和审批政策继续复用。

- 学生 PC / 小程序新增同一真实单据详情：批次学年、状态提示、申请/认定等级、家庭成员/收入/债务/标签/说明、真实创建/公示截止/结果时间，以及按权限出现的补正入口。家庭详情只在当前详情内存中保留，关闭清空；列表不携带敏感明细。未实现完整审批时间线，不伪造节点时间。
- 手机默认先看记录，长申请表与公示异议按需展开；修复导航 `back` 无效属性为 `show-back`。刷新保留输入和批次选择，失败保留旧记录、标明未更新；过时响应不覆盖当前详情。补正关闭确认保留取消退出的选择。
- 浏览器实测发现并修复两个具体问题：学生单笔编辑投影未回传 statement，导致补正说明为空；手机自定义补正层层级过高，遮挡系统确认框。PC 新详情初次使用了不属于学生壳的颜色变量，截图发现透明后已改为学生壳的 surface/line/t1/t3，再截图确认不透明底色与独立滚动。
- 原版本机制保留；重提失败不丢已经保存的版本与输入。新学生详情在服务端再次校验本人、租户、软删，并记录本人查看审计。

#### 实际浏览器流转

沿用隔离 UI 库批次1、申请1，未操作用户正式数据：教师小程序（counselor01）在辅导员初审退回，填写“隔离验收：请补充家庭收入减少的具体原因。”→学生 PC 与学生小程序详情显示已退回和相同意见→修复原文后学生手机重新打开补正，保留32字原说明与3人/24000元/0元→修改说明并保存重提→申请1回到CLASS_REVIEW。学生手机、学生PC详情、教师手机待办、教师PC工作台全部实际显示班级评议；教师手机和学生PC看到同一份新说明。申请编号仍为1，没有创建替代单据。

后端只重启确认路径匹配 `.codex-temp/leave-ui-server.py` 的本任务8018进程。重启后的首次详情请求约9秒，前端出现可重试错误；再次刷新正常显示。保留此冷启动现象待性能复核，没有扩大超时掩盖问题。5188用户服务未动。

#### 验证结果与边界

- 隔离 MySQL 最终 `test_aid_student_detail.py`、`test_affairs_aid_detail_projection_contract.py`、`test_affairs_aid_draft_resubmit_contract.py`：3 passed，142.67秒，日志 `aid-detail-backend-final.log`。涵盖非退回/退回/已认定本人详情、其他学生与删除记录拒绝、单笔查看审计、列表不出家庭明细、补正原文一致。无数据库结构变更。
- 学生PC测试、lint/build，小程序测试和H5/mp-weixin构建均执行；最终结果见本节后续复核。微信构建不等于微信开发者工具或真机验收。
- 当前 UI 单据停在重提后的班级评议。下一步继续学院/学校评审、公示异议、认定与动态调整，补完整材料/时间线和多批次入口后再进入奖助批次。全五批未完成；没有Git提交、推送或部署。

最终复核：学生PC 135 passed、lint与build退出0；小程序285 passed（新增6项真实组件方法行为测试），H5/mp-weixin构建均DONE。日志 `aid-detail-student-{test,lint,build}.log` 与 `aid-detail-mini-{test,h5,weixin}.log`。相关改动 `git diff --check`通过，仅有工作区LF/CRLF提示。本轮合计423项通过，不包含未修改教师PC工程的重复全量测试，也不表示整个困难认定批次已交付。

### 2026-09-06：困难公示异议四端批量同步

先完成施工包§九调用链审计，继续沿用main当前工作区，保持确认过的壳。没有Git提交、推送、部署或学校正式数据操作。

- 教师PC：公示登记收起为按需区；公示申请与异议列表独立分页，指标读取服务端total，支持失败重试、旧响应隔离、原版本复核和重复点击保护。评审后重新取单笔详情，修复列表投影覆盖详情导致家庭说明消失。
- 教师手机：异议队列在数据库按具体责任人过滤后分页；只显示服务端允许的动作；通知recordId精确打开原异议，已办结仍能回看结论。复核后的按钮收起，展开详情时去掉重复理由。
- 学生PC与手机：本人详情展示公示异议的复核结论、意见、时间，不返回匿名异议人和原始举报理由。复核不成立仍保持公示中，明确等待公示截止和确认认定。
- 手机身份：补齐学校管理员/学工管理登录映射、工作台中文身份和办理入口；“我的待办”直达统一待办；徽标使用真实pending指标。学工写权限继续由后端判定。
- 手机尺寸检查发现详情打开时背景也有滚动条，本轮限制背景工作区滚动，保留正文独立滚动和固定返回/刷新栏；DOM复测根高度与滚动高度均844，结果卡可滚动查看。临时390×844视口验收后恢复默认。

#### 同一单据真实流转

隔离库 `codex_leave_ui_test_0ea278f0` 的批次1、申请1：教师PC依次完成班级、辅导员、学院、学校审核→PUBLICITY；学生PC提交异议1；教师手机学校管理员查看原单并提交“不成立”及复核意见→CLOSED。随后教师PC实际显示公示申请1、待复核0、已复核1；学生PC与学生手机本人详情显示完全相同的复核意见和时间，状态仍为公示中。学生手机首页也出现“困难认定异议结果”消息，消息跳转和微信订阅送达另需继续验收。

学校管理员“待我处理”为0不是漏单：隔离数据库只读核对具体责任人为leave_sa01，其统一待办在管理复核后为DONE；不将全校可管理事项计入个人待办。公示截止2026/9/7 01:53:31未到，未篡改UI库日期伪造公示期满。

教师PC联调曾出现Vite页面空白，页面无异常日志；仅重启确认属于本任务5178的Vite后恢复，未改登录业务代码。用户其他项目与端口未动。

#### 本轮验证

- 后端隔离MySQL：`test_aid_objection_workspace.py`、`test_affairs_aid.py`、`test_affairs_aid_objection.py`，13 passed / 309.27秒。覆盖完整评审公示认定、异议成立与不成立、未结异议阻断确认、51条本人待办逐页无遗漏、他人责任事项拒写、单笔已办结回看、过期版本拒绝、学生结果隐私边界。
- 教师PC：775 passed，lint/build通过。学生PC：135 passed，lint/build通过。
- 小程序最终：290 passed，H5和mp-weixin构建DONE。新增真实组件/安装器行为测试覆盖分页、精确原单、角色映射、服务端计数与动作保留、待办徽标。
- 日志：`.codex-temp/aid-objection-{backend,teacher-test,teacher-lint,teacher-build,student-test,student-lint,student-build,mini-test,mini-h5,mini-weixin}.log`。相关已跟踪代码 `git diff --check` 通过，仅换行提示。

本轮合计1213项测试通过。该数字只覆盖本轮命令集合，不代表全五批交付。下一施工位置仍是困难认定材料/完整流转时间线、多批次入口、公示期满确认、动态调整与资助资格衔接；七页完整视觉及微信开发者工具/真机验收仍待完成，随后进入奖助模块。

### 2026-09-06：困难认定材料与办理记录四端批量同步

沿用 main / 6810bd6284b4d5c69a78947560f808cd7738327e 及现有未提交改动；先完成施工包§十审计再实施。没有Git提交、推送、部署或正式学校数据操作。

- 统一详情增加真实办理记录：申请、退回、补正保存/重提、逐级评审、公示、异议及认定结果。服务端仅解释白名单事件，按真实时间排序，排除批次、查看审计及原始技术详情。学生本人投影不含操作人和匿名异议信息，列表不携带家庭说明与历史。
- 四端详情接通原申请材料入口与返回；学生PC修复带重复portal前缀导致回首页的问题；教师PC材料页保留紧凑壳、身份与范围，材料表单、指标、背景与提示随四主题适配。
- 教师材料中心补齐业务过滤，列表、统计和分页结果一致；学生PC及教师PC加入跨申请旧响应隔离。教师登记材料后保留当前申请，材料等级改为中文。
- 浏览器隔离账号：申请1共11条办理记录在四端一致；教师PC登记测试缺项1后，学生PC和两手机H5均看到相同名称/说明/责任人。学生PC返回自动打开原申请详情；教师PC、教师手机也返回申请1。学生手机返回原记录，并可再次打开详情。观察学生手机390×844详情，固定返回栏及正文滚动正常；视口已恢复。教师PC曜石黑材料页实看并修正提示条对比。

#### 验证证据

- 最终专项：`test_aid_material_context.py`、`test_aid_history_workspace.py`、`test_affairs_aid_detail_projection_contract.py`，隔离MySQL **4 passed / 156.56秒**。覆盖25条目标材料跨页、其他业务同ID和其他学生排除、服务端统计、真实退回重提评审认定历史、学生隐私投影及等级显示。日志 `.codex-temp/aid-material-history-final.log`。
- 关联材料回归：`test_aid_material_context.py`（新增等级测试前）、`test_affairs_material_center.py`、`test_affairs_material_biz_context_entry_contract.py`，**17 passed / 1 skipped**。跳过项为历史独立MySQL验收脚本的 DATABASE_URL/DB_ENABLED 环境门禁，不能计为上传/重交验收通过。日志 `.codex-temp/aid-material-context-backend.log`。
- 历史详情首轮3 passed / 1 failed，失败为原投影单测用object替代db，新历史依赖无法查询；补齐该单测隔离后重跑通过，真实历史由新增MySQL测试覆盖。学生详情与审计命名空间测试已通过。
- 最终教师PC **777 passed**、学生PC **137 passed**，lint/build通过；新增4项实际组件行为测试验证乱序响应、同ID跨业务切换、加载更多返回和登记对象保留。日志 `.codex-temp/aid-material-{frontend,student-portal}-{test,lint,build}.log`。
- 两手机沿用本轮历史/材料改动后的 **290 passed**，H5、mp-weixin构建DONE，日志 `.codex-temp/aid-history-mini-{test,h5,weixin}.log`。本轮未在构建后继续修改手机代码。
- `git diff --check`通过，仅工作区LF/CRLF提示；PC构建保留已有大包提示。

仍未完成：材料文件实际上传、重交及验收全链路；教师手机材料页的宽屏/原生完整视觉；材料工作区独立页签与所属导航；公示到期确认及动态调整责任人/权限闭环；通知安全直达、多批次入口、资助资格衔接。UI申请1仍为公示中、材料1仍待补交。继续困难认定这些缺口后再进入奖助服务，不标记整个第二批或五批交付。

### 2026-09-06：材料提交—退回—重交—验收四端闭环与待办同步

延续main及原有未提交施工，先读施工包§十一与关联待办审计。以下为本轮更新，替代上一轮“材料仍待补交”的运行状态；认定申请仍处于公示中，材料验收不代表认定完成。

- 学生PC采用主题内确认弹窗，教师PC退回/验收采用主题内审核弹窗；防连击，失败保留文件、说明及退回原因，提交中禁止误关。四端材料时间统一按本地时间显示。
- 隔离UI库材料1：学生PC实际上传PDF第一版→教师PC退回并说明原因→学生手机H5实际上传第二版重交→教师手机H5验收。学生PC及学生手机均显示已验收、第二版文件、第一版历史及原退回意见；教师材料页显示已验收，待办恢复0。
- 修复材料通知被AID消息包装器错误改写的问题。实际点击学生首页新“材料已验收”通知，跳到`/pages/student/affairs/index?materialRequirementId=1`并展示正确历史。修复作用于新生成通知，未批量改写历史消息。
- 登记MATERIAL_REVIEW教师PC/手机安全路由，PC接受recordId别名、手机在SQL分页前按材料编号过滤；浏览器验证两端`recordId=1`直达已验收材料。手机验收成功刷新完整工作区，防止材料已验收但待办数保留旧值。
- 教师手机首页待办统计改用同可见范围UnifiedTodo全量统计，保留教师PC审批统计口径；“即将超时”消费服务端24小时指标，不再把五条预览中的普通材料待办算成临期。今日完成采用真实completed_at，避免历史完成记录仅修改备注后计入今日。

#### 本轮验证

- 完整实际HTTP上传/越权/退回/重交/验收：`test_aid_material_flow.py`，真实四类client登录、真实POST文件字节，检查其他学生文件不可绑定/下载、版本冲突409、两版独立公共版本、验收公共版本APPROVED、原认定状态不变、统一待办DONE与材料通知动作。前轮最终与通知投影合跑2 passed，日志`.codex-temp/aid-material-notice-final.log`。新增工作台计数断言后的结果另记在下。
- 安全消息、移动动作、材料业务与精确编号过滤：45 passed，177.13秒，日志`.codex-temp/aid-material-routing-final.log`。
- 教师PC778 passed及lint/build通过；学生PC139 passed及lint/build通过，日志`.codex-temp/aid-upload-{frontend,student-portal}-{test,lint,build}.log`。
- 小程序294 passed；H5和mp-weixin构建DONE，日志`.codex-temp/aid-workbench-mini-{test,h5,weixin}.log`。包含材料审核后全工作区刷新、精确材料分页、服务端临期总数与普通待办文案的行为测试。
- 最终关联后端回归：`test_workbench_snapshot.py`、`test_teacher_mobile_v3_t8_workbench_contract.py`、`test_aid_material_flow.py`，隔离MySQL **8 passed / 186.35秒**，日志`.codex-temp/aid-workbench-final.log`。覆盖PC审批统计仍与Approval一致、手机10条本人待办只返回2条预览仍统计7条临期、其他责任人排除、历史已完成后编辑不计今日，以及实际材料提交后首页待办+1、验收后恢复原数。
- 重启本任务8018隔离API后，教师手机首页实际显示待我处理0、已逾期0、24h到期0、今日完成1，与已验收材料一致。最终`git diff --check`通过。警告为既有datetime.utcnow弃用及PC构建体积提示，不计为失败。

限制与续接：本轮手机运行证据为H5，微信原生选文件/预览与真机仍待验收；扫描服务未就绪时的提交重试体验仍需专项验证，未放宽扫描门禁。历史错误消息不作自动迁移。材料工作区独立页签、认定公示期满确认、动态调整责任人及权限、多批次入口、资助资格衔接仍未结束；下一步继续这些困难认定缺口，再进入奖助服务。本轮没有提交、推送、部署或正式学校数据操作。

### 2026-09-06：困难等级调整四端同步与生效等级保持

先完成施工包§十二调用链审计，保留main及所有既有施工改动。代码修复：

- 调整申请分派到现有校级认定岗位，避免辅导员收到自身无权限办理的待办；PC审核路由和动作使用aid.approve，与领域服务/手机统一，并消费单据allowedActions。未修改学校角色授权。
- 同等级调整不再提交；未知审核动作明确拒绝；手机传入页面版本，错误目标和旧版本拒绝，保留原单并提示刷新。教师手机确认的是已提交目标，删除与后端实际执行不一致的再次选择等级步骤。
- 四端详情展示当前→目标等级；教师额外显示申请原因，两学生端说明审核完成前原等级仍有效；教师PC驳回意见进入真实历史并向学生本人展示。拒绝后维持原等级，通过后更新并写等级历史，材料版本保持。
- 运行发现调整期间学生头部变成“尚未认定”。同步修复学生概览、困难库、认定统计与助学金资格读取：ADJUST_REVIEW继续读取已生效final_level，不能提前读取目标suggest_level或临时取消原资格。
- 工作台初次批次加载失败时，重试重新初始化批次；教师手机详情提交时间使用本地时间。

运行证据（继续补全）：隔离UI申请1通过测试专用脚本将公示时间置为已到期，脚本限制固定隔离库、测试租户、学生1和验收批次；这属于时间边界测试，不是实际等待学校公示到期。随后教师PC点击正式“确认公示通过”，状态变已认定/一般困难，再提交“特别困难”动态调整。教师PC、教师手机、学生PC均读到同一原等级和目标；学生不显示内部调整原因。小程序运行验收使用H5，原生/真机尚未验证。

首轮专项 `test_aid_adjustment_flow.py`、`test_aid_mobile_queue.py`、`test_aid_history_workspace.py`：隔离MySQL **3 passed / 212.33秒**，日志`.codex-temp/aid-adjust-backend.log`。覆盖真实四种client登录、校级分派、PC驳回/手机通过、越权403、旧版本/目标不一致409、非法动作400、两学生端状态和记录、仅成功调整写一次等级历史并结束待办。生效等级保持新增断言后的最终回归另记在下。

#### 等级调整最终运行与回归结果

- 隔离申请1：PC提交一般困难→特别困难，教师手机弹窗确认原申请目标后通过；四端读到特别困难及实际调整历史。随后PC发起特别困难→困难，学生手机详情显示目标并明确原等级仍有效；PC填写“现有依据不足以支持下调”后驳回，两学生端和教师手机均显示原等级特别困难及相同驳回意见，不再提供旧审批按钮。
- 只读隔离库核验：申请1 `APPROVED / SPECIAL`；MATERIAL_REVIEW和AID_ADJUST均DONE；等级调整生效历史只有1条。调整待办实际分派leave_sa01（既有校级岗位），school_admin01按已有全校管理权限完成审核，所以后者首页“待我处理0”不是漏计。
- 修复学生手机详情刷新后列表更新而页头保持旧等级：详情后重新读取权威概览，不用历史申请详情猜测最新认定等级。行为测试覆盖历史申请为一般困难、当前认定来自另一单为特别困难的情况。两学生端均不展示教师内部调整申请原因。
- 最终MySQL：`test_aid_adjustment_flow.py`、`test_aid_student_detail.py`、`test_affairs_aid.py` **11 passed / 246.18秒**，`.codex-temp/aid-adjust-final.log`。追加困难库/统计/两学生概览/助学金资格在ADJUST_REVIEW继续保持原等级的断言；包括原多级评审、公示、范围和敏感数据回归。
- 教师PC778 passed、学生PC139 passed，均lint/build通过；小程序296 passed，H5及mp-weixin构建DONE。日志`.codex-temp/aid-adjust-{frontend,student-portal}-{test,lint,build}.log`、`.codex-temp/aid-adjust-miniapp-test.log`、`.codex-temp/aid-adjust-mini-{h5,weixin}.log`。旧手机测试原先断言可再次选等级，已替换为确认持久化目标与旧版本停止的行为测试，非放宽断言绕过失败。
- `git diff --check`通过。隔离API重启冷查询期间学生手机一次刷新超时，实际接口日志GET200/9.36秒；重试后读取成功，未通过改权限或扫描规则绕过。原生微信及生产负载网络仍未完成验收。

仍需继续：公示名单的到期时间与可确认状态展示、多批次选择、四端全部页面视觉/原生验收、奖助业务资格与申请发放完整闭环。当前只是关闭困难认定的具体断点，不标记第二批或全五批完成；没有Git提交、推送或部署。

### 2026-09-06 续接：公示批量扫描与四端状态同步

本轮保持main与原有修改，先沿施工包第十三节审计后施工。

- 统一服务端公示截止、是否到期、说明，列表与详情使用同一计算。教师PC工作台和公示名单未到期不开放确认；教师手机、学生PC与学生手机读取同一期限与说明。缺失有效批次时不默认猜测期限。
- 批量扫描改为先在SQL中过滤有效批次、已到期、无待复核异议，再锁定并限量200条。修复前200条未到期挡住后面到期申请的问题；结果显示本次确认、有异议、批次异常数量，可继续扫描剩余到期事项。
- 教师批次搜索接入现有AppAidBatchPicker中央适配器，关键词按名称/学年在服务端分页前过滤，精确ID单独解析，保留原始批次状态。工作台增加现有“全部批次”分页入口，历史批次不再依赖前100条缓存。未新建平行选择器。
- 实页发现旧golden样式覆盖新壳：公示页在黑色主题下出现白色渐变和不可读表头。将公示及误匹配该页面的旧风险装饰限定在旧壳，保留新壳主题组件；补齐pri-bg令牌，刷新与扫描恢复同一行。已实看曜石黑、护眼绿，安全水印保留。

真实运行证据（固定隔离UI数据库，不是正式学校记录）：

1. 经正式接口建立批次2“公示期限验收 · 2026”与学生1申请2，完成四级评审进入PUBLICITY。四端读取同一截止2026/9/9 03:57:43，教师PC为“暂不可确认”，手机教师无审批动作。
2. 专用测试时间脚本仅允许固定测试库、租户、学生1、批次2及上述精确批次名，将申请2设为已到期；未修改真实学校批次天数。这是时间边界夹具，不代表实际经过学校公示期。
3. 四端刷新均显示“公示期已满”；教师PC出现确认入口。通过页面“公示期满扫描”实际确认1条、异议0、异常批次0，公示队列归零。学生PC、学生手机H5、教师手机H5刷新后均为已认定并展示“完成困难认定”历史，教师手机无旧审批动作。
4. 教师PC批次选择器输入“公示期限”只返回对应批次，能够选择；旧批次超过100条的情况由下述HTTP数据库测试覆盖。

验证：

- `.codex-temp/aid-publicity-batch-final.log`：`test_aid_publicity_window.py`、`test_aid_batch_search.py` **3 passed / 144.36秒**。覆盖截止前后边界、203条队列中等待/异议不阻塞到期单、重复扫描不重复生效、单条提前确认被拒、列表/详情期限一致；106个批次中查历史名称、学年、字面百分号及租户隔离。
- 首轮`.codex-temp/aid-publicity-final.log`为1 failed / 11 passed，失败是新增测试夹具违反学生/批次唯一约束，尚未执行扫描；修正为每条申请对应不同批次后，最终上述3项通过。未删除或放宽业务唯一约束。
- 教师PC **781 passed**，lint通过；学生PC **139 passed**，lint/build通过；小程序 **296 passed**，H5及mp-weixin构建DONE。原日志`.codex-temp/aid-publicity-*`；主题精修后教师PC重复回归仍781 passed，lint通过，build通过（56.72秒），日志`.codex-temp/aid-publicity-theme-build.log`。
- `git diff --check`通过。手机运行证据为H5，微信开发者工具/真机未验收，不能宣称原生端已验收完成。

后续缺口：学生开放批次接口仍固定取前200条（`aid_batches_open`），待补实际分页可达性；困难认定其余全部页面视觉与原生验收、材料上传扫描等待与脏标记等已登记问题继续推进，然后进入奖助服务。第二批与全五批仍未完成，未提交、推送、部署。

### 2026-09-06 续接：学生批次查找与认定结果页

前轮为有效进展。本轮在main/6810bd6、134项既有修改基础上，按施工包第十四节审计后施工。

已实施：

- 两学生批次接口增加page/pageSize/keyword，继续使用真实OPEN/申请起止窗口/租户/删除过滤，服务端分页前搜索。两学生页面均有名称/学年搜索、数量、加载更多和局部重试；移除自动换成首个批次的行为。候选结果和已选批次分别保留，搜索/翻页/失败不改申请对象、不清空家庭情况输入；服务端提交仍重新校验批次有效性。
- 教师认定台账补齐待补正、辅导员初审、调整中筛选，显示批次/学年/申请时间，点击查看申请沿recordId进入现有鉴权工作台。
- 困难学生库等级人数改为读取各等级同范围SQL total，避免从前200条推算全量；名单补学号、来源批次/学年和查看认定依据。来源信息按本页批次批量读取，未增加逐条请求；不增加家庭经济明细。
- 认定统计明确是申请“份数”，同一学生多批次分别计数，区别于困难学生库当前去重人数；调整中的已生效认定仍按原等级计入。三个结果页增加请求序号及异常finally，避免旧筛选覆盖和加载状态卡住。
- 台账/名册链接使用主题文字与键盘焦点，选中筛选使用主题色；学年独立一行，避免与长批次名称挤在一起。保留已确认外壳。

运行验证：

- 学生PC选择“四端联动验收”批次并填写临时验收说明，再搜索“公示期限”，匹配结果变为1条，原批次仍选中、说明保留。学生手机H5经同样操作，结果也保留，未提交测试填写内容。
- 教师PC台账显示隔离学生甲一的两份已认定申请；困难学生库只显示该学生1人、当前等级一般困难及最新申请2来源。两个“查看”入口均实际跳到申请2及其批次与办理记录。
- 统计页显示申请2份、已生效2份，等级分布各1份；与名册1人的区别在页面说明中明确。护眼绿下台账/名册/统计均实际截图检查，黑色下统计与台账继续复核；不以编译成功冒充视觉验收。
- API重启期间遇到代理错误/手机会话回到入口，检查实际服务启动后页面重试恢复，手机用隔离学生重新登录；未绕过权限或修改正式账号。

测试与构建：

- `.codex-temp/aid-student-batches-backend.log`：`test_aid_batch_search.py`、`test_aid_batch_publish.py`、`test_aid_workspace_window.py` **8 passed / 171.99秒**。新增真实PC/STUDENT_MINI登录测试：210个开放批次的默认页、末页、名称/学年搜索；草稿、关闭、未开始、过期、其他租户、删除不可见；教师不能访问学生列表；pageSize上限生效。
- `.codex-temp/aid-result-workspaces-backend.log`：`test_aid_adjustment_flow.py`、`test_affairs_aid.py` **10 passed / 230.19秒**。追加名册applyId/批次名/学年/学号与台账一致、敏感字段不扩散断言，原评审/公示/调整/资格保持回归通过。
- 学生PC **141 passed**、lint/build通过；小程序 **298 passed**、H5/mp-weixin构建DONE。新增行为测试覆盖选择保持、翻页使用已执行的关键词、请求乱序、失败不清空；日志`.codex-temp/aid-student-batches-{portal,mini}-*`。
- 教师PC **784 passed**、lint/build通过（34.34秒），日志`.codex-temp/aid-result-workspaces-frontend-*`。新增行为测试验证680人的全量等级人数不依赖单页样本、三个结果页失败可重试、旧台账响应不能覆盖新筛选。最后的链接样式精修另构建，日志`.codex-temp/aid-result-workspaces-final-build.log`。

最后链接/筛选样式构建通过（26.33秒），`git diff --check`通过；黑色台账截图已复核。两学生页面临时验收填写已清空，未提交额外申请。

下一步精确审计线索：`PortalLayout.markEdited`捕获全部业务输入，仅在路由变化时清零；`MaterialSupplementView.submit`成功后虽清空selectedFiles/notes，却没有通知外壳清除edited，造成已提交仍提示未保存。应由当前业务表单向外壳报告真实未提交状态，不能全局在任意请求成功时清零。另两学生材料提交当前每次重试都会再次上传；材料中心`_require_file_ready`对等待扫描、扫描失败、隔离共用409，details含scanStatus/fileStatus。后续需沿已有文件状态查询能力区分等待/拒绝和可重试阶段，不绕过文件安全门禁，也不能把整个材料命令自动重放。

仍需推进：困难认定全页面的手机尺寸/微信原生与真机验收、材料扫描等待提示及提交后脏标记、原有入口/通知的整体复审；下一业务模块奖助服务尚未进入完整施工。没有把第二批或全五批标为完成，没有提交、推送、部署。

### 2026-09-06 续接：材料补交批量同步与扫描等待收口

用户本轮指令“批量同步”按当前学工四端施工上下文执行，本轮范围为材料补交共用流程及直接关联的学生PC表单状态。main/6810bd6，保留现有未提交施工；没有进行Git提交、远程推送或部署。

本轮正式代码：

- 学生PC与学生小程序补交页面复用已有文件元数据接口，上传成功暂存fileId，手动检查扫描状态复用原文件；未就绪、扫描失败、隔离和读取失败不提交业务审核命令。提交沿原业务版本，不自动刷新版本或重放审核命令。更换文件才清除上传缓存，失败保留文件与说明。
- 学生PC外壳接收材料页真实未提交状态：提交成功仅清理该项，其他未提交项继续保护；提交过程中禁止路由离开。两学生端提交过程中锁定附件、说明及重复提交，手机文件选择回调也检查忙碌状态。
- 教师PC及教师小程序继续使用已有材料审核与元数据权限链，审核结果来自同一版本和服务端状态；未增加绕过扫描入口。

真实运行（固定隔离学校/合成材料）：

1. 本轮新增测试要求2“扫描与提交体验验收”，关联认定申请2。普通ZIP按原策略拒绝，未改上传策略。改用合法合成DOCX，在仅隔离运行启用FILE_SCAN_REQUIRED情况下显示等待扫描；手动再次检查后只读数据库证实只有一份上传文件3，QUARANTINED/PENDING，材料尚未进入审核。已截图检查确认弹窗；这证明等待与重试，不证明真实扫描进程处理完成。
2. 通过页面替换为已有测试PDF，正常上传并提交；材料2为PENDING_REVIEW/第1版，学生PC编辑圆点消失，返回申请2详情不再误报未保存。没有把DOCX安全状态强制置为通过。
3. 教师PC正式确认验收，材料2变ACCEPTED/第1版，待审核0；学生PC“已完成”及学生手机H5都读到相同文件、时间和“隔离测试材料已核对，验证四端结果一致。”审核意见。学生手机待补材料变0。
4. 教师手机旧测试会话打开加载失败；重新通过正式教师登录页登录隔离school_admin01后，材料recordId=2深链读到已验收/第1版，无旧审核按钮，待办0；首页今日完成2。未绕过角色或扩展权限。故不把首次失败隐去，也不把会话重建当生产接口修复。

验证结果：

- 后端实际上传/等待扫描禁止提交/他人元数据不可读/退回重交/验收/版本冲突与材料上下文：`test_aid_material_flow.py`、`test_aid_material_context.py`，**3 passed / 208.54秒**，`.codex-temp/aid-material-scan-backend.log`。
- 学生PC **147 passed**，lint/build通过（3.78秒）；`.codex-temp/aid-material-sync-portal-{test,lint,build}.log`。行为测试包括等待扫描两次仅上传一次、成功后清理、其他材料保留、未知就绪不提交、提交中不能离开、已保存直接返回、确认切换上下文后旧暂存清除。
- 小程序 **301 passed**，H5及mp-weixin构建DONE；`.codex-temp/aid-material-sync-mini-{test,h5,weixin}.log`。覆盖扫描等待、版本冲突说明保留、仅就绪后提交、忙碌时不能更换文件。本轮教师PC代码未修改，沿用前轮784项通过结果，本轮已做实际页面验收。
- `git diff --check`通过；换行提示为既有CRLF配置警告，无空白错误。

剩余：手机材料离开时的未提交保护、微信原生/真机选择文件与返回行为；教师材料独立页签接入；困难认定其余页面完整视觉复审；下一模块奖助服务的实际资格、申请、评审与发放链仍需先审计再施工。第二批及全五批均未标记完成。

### 2026-09-06 续接：手机材料离开保护、教师材料页签

第十五节上一轮有代码变更和四端运行证据，本轮分类为进展。先完成困难认定施工包第十六节关联审计，再修改共用材料导航。

- 学生小程序材料页的返回、返回原申请、业务入口和底部导航统一检查未提交文件/说明；忙碌时阻止离开，取消保留内容，确认放弃才清除本页暂存；已提交材料不受影响。底部导航增加可选beforeNavigate，其他页面默认行为保持。材料页本身不再把首页错误标为当前页，首页现在能作为真实返回入口。
- H5增加beforeunload与同步onBackPress；微信按已安装官方类型声明接入enable/disableAlertBeforeUnload，限当前可见且有未提交内容页面。平台能力检测保留；原生手势滑回与退出不保证拦截，不能声称原生全覆盖。
- 教师“学工工作台”增加“材料与档案”真实三级入口，沿用dashboard.view权限；由既有workspacePages接入页签、关闭恢复和收藏。路由从router/index.js归回studentAffairs.routes.js，地址与名称兼容，参与现有权限投影。新叶子追加在原有项之后，旧surfaceKey不变；按现有生成器更新navigation-surface-contract.json。

验收：

1. 教师隔离页面带AID申请2/材料2深链，出现选中的“材料与档案”页签；关闭后恢复，仍回到同一申请材料及版本记录。曜石黑截图复核，沿用现有窄栏与工作区。
2. 学生手机H5新增固定隔离要求3“离开保护体验验收”，仅测试用。选测试文件并填写说明，点底部服务显示确认，点继续填写保留内容。初次热更新会话取消后再次导航表现异常；清空测试输入、重新加载当前编译版本后复验：取消回调返回false，再点返回原申请仍能弹窗，确认放弃进入aid?recordId=2。临时诊断日志已删除。没有提交要求3，也没有改真实学生数据。
3. 手机尺寸390×844检查过原生风格确认弹窗，随后已恢复浏览器默认尺寸。运行证据仍为H5，微信开发者工具和真机未验收。

最终验证：

- 教师PC **785 passed**、lint/build通过（最终24.61秒）；日志`.codex-temp/aid-material-nav-route-final-{test,lint,build}.log`。首次导航清单漂移1 failed，生成权威契约后最终通过，未删掉契约一致性断言。
- **503个正式可见导航路由**对照通过；`.codex-temp/aid-material-nav-route-parity.log`，生成器--check也通过。
- 小程序 **305 passed**；H5/mp-weixin构建DONE；`.codex-temp/aid-material-leave-mini-final-{test,h5,weixin}.log`。包括取消保留、确认清理、忙碌阻断、防重复弹窗、可见页面原生提醒生命周期与底部默认导航行为。
- 后端材料静态合同 **13 passed / 3.05秒**；`.codex-temp/aid-material-nav-backend-contract-final.log`。原文案断言匹配旧技术提示失败，改为当前已在页面使用的隐私提示；路由断言改读权威学工路由模块。业务审核、文件、权限服务本轮未改，原实际HTTP上传/退回重交/验收3项数据库回归仍有效。
- 学生PC本轮未修改；沿用上轮147项及真实提交后不误报未保存证据。

续接：当前材料导航缺口已处理；困难认定完整原生/真机及其余页面视觉复审仍在清单中，不把它们算完成。接着进入奖助勤贷补现有模块的四端关联审计，保留现有好用页面。旧2026-07-12奖助施工包基线已过时，不按其“只有3表/7接口”结论重新造系统。全五批目标继续有效，未提交/推送/部署。

### 2026-09-06 续接：奖助批次查找与新申请窗口同步

本轮用户指令“批量同步”沿既定四端施工执行。main/6810bd6284b4d5c69a78947560f808cd7738327e，保留此前未提交改动。先在《奖助服务-四端施工包》记录运行时安装链、模型与批次入口审计，再处理该子链；不是整个奖助模块或第二批完成。

正式修改：

- 两学生入口共用开放批次查询：按当前学校、未删除且启用项目、匹配奖/助类型、OPEN和UTC起止时间过滤，项目名称/学年搜索在分页之前执行。默认20条，单页最大200，返回完整total；手机不再取前50条后过滤并把单页数当总数。
- 模型没有独立批次名称；列表显示“项目名称 · 学年 · 批次编号”，区分同年多个批次。PC与小程序增加搜索、加载更多、空结果、失败重试，按ID显式选择；搜索/翻页/刷新/失败保留原选择和填写内容，较旧响应不能覆盖新结果。切换奖学金/助学金时清空旧批次与确认，不自动替用户选第一项。
- 新建申请共用require_application_batch，教师代办、教师资格预检与两学生原子提交均检查时间窗口和有效项目。不会因新申请截止而自动阻止在途评审或退回重提；未修改这些状态机。教师手机既有受理队列沿现有流程，本轮未改其页面或新增发布入口。
- 小程序申请/退回修改的理由统一5–1000字，申诉前端与PC一致；提交忙碌期间锁定批次、类型、说明及确认，方法层拦截重复提交。失败保留说明、文件和目标批次。PC申请同样增加方法层重复提交保护。
- PC奖助页接入现有registerWorkspaceForm：搜索/类型浏览不再误报未保存，实际说明、本人确认、未提交申诉和退回编辑仍保护，忙碌时阻止离开；其他模块维持原注册方式。未把全局任意请求成功当作保存成功。

实际运行证据：

1. 仅在leave-ui-test隔离学校，经现有教师API创建2个合成项目、23个开放批次（1个奖学金、22个助学金），无新增学生奖助申请、评审或资金发放。
2. 小程序H5选择助学金，20/22加载至22/22；明确选中批次23，填写测试理由，再搜索2025-2026仅命中批次2，批次23和原理由仍保留。学生PC同样经过22条加载、选择23、搜索2025-2026，保留原申请对象及内容。两端截图检查过现有布局；本轮没有做微信原生或手机尺寸完整视觉验收。
3. 走查初次手机查询失败，实际日志定位到新参数Optional[str]未在模块定义；两路统一使用str | None后重启本任务8018隔离API，重试正常。首轮数据库用例同样捕获此问题，保留失败日志，不以页面重试代替代码修复。
4. 表单状态修复后，PC实际输入搜索条件，页签不亮编辑圆点；填写申请理由后出现圆点，清空后消失。已清空两端测试说明/搜索条件，没有提交临时填写内容。保留学生PC和手机预览标签。

验证：

- `test_affairs_funding.py`与`test_affairs_funding_preflight_amount_contract.py`共11项通过，包含教师申请、资格拦截、节点流转、公示授予和原金额预检；日志`.codex-temp/funding-batch-backend.log`。该轮另1项因上述Optional声明失败。
- 修复后重跑`test_funding_student_batches.py`：**1 passed / 148.85秒**，`.codex-temp/funding-batch-backend-final.log`。实际PC/STUDENT_MINI登录，210条分页与末页、类型/名称/学年/%字面搜索、跨学校与停用/删除项目、未来/过期/关闭/错误类型均核验；两学生提交、教师代办与预检都拒绝不可用批次，断言未生成申请。合计相关12项已通过，不把初次失败算通过。
- 学生PC最终**151 passed**、lint/build通过（2.28秒）；`.codex-temp/funding-batch-portal-final-{test,lint,build}.log`。中间因加载watch的静态形状断言失败，恢复独立加载watch、把表单注册分开后全量通过，未修改原回归断言。
- 小程序**309 passed**，H5及mp-weixin构建DONE；`.codex-temp/funding-batch-mini-{test,h5,weixin}.log`。新增选择保持/乱序/失败/类型变更/1000字/重复提交/目标锁定行为测试。
- 教师PC源代码本轮未改，沿用上一轮785项及构建证据；教师实际创建项目/批次接口和新建/预检后端规则本轮已验证。没有宣称教师手机整页视觉已验收。

下一步继续奖助申请、材料、资格和金额快照子链：学生PC尚缺手机已有的申请附件入口；学生原子申请的项目资格与金额冻结仍需和教师authority实现对齐；本人完整详情、教师手机受理/退回、申诉与发放结果仍需逐项施工和四端验证。困难认定原生/真机与其他页面复审继续保留。没有提交Git、推送或部署，没有结束五批目标。

### 2026-09-06 续接：奖助申请金额、四端材料与待办业务类型同步

本轮沿既定四端施工继续，保留 main 上此前所有未提交改动。先读《奖助服务-四端施工包》中的实际安装链与申请、退回、授予、发放、文件服务关联审计，再处理申请材料子链。不是全奖助模块或第二批完成，也未提交、推送或部署。

正式改动：

- 教师受理与学生原子申请在创建申请的同一事务内冻结项目标准金额和 amountAuthority，再生成本人确认快照；教师不再首次提交后另开事务补写。学生资格校验传入真实项目规则。退回修改不接受客户端改写正式金额，重提保留标准金额来源和项目资格规则；旧记录缺失金额来源时按既有项目规则补齐。数据库原本已有标准金额触发器，本次修的是事务和快照一致性，不描述成客户端可决定实际发款。
- 学生 PC 增加佐证材料上传、扫描就绪、失败重试、移除、数量/大小限制和成功后清空；附件草稿接入现有未保存保护。四端详情使用原文件中心读取当前正式绑定的申请材料，复用原预览组件，没有另造附件存储或审核平台。
- 手机共用附件组件在选择、上传和元数据加载期间就绪为 false；父表单清空后同步清除附件，过期上传和扫描响应不能把已移除文件恢复。最初添加的额外 busy 事件引起封存契约检查失败；评估后移除不必要的新事件，使用既有 ready 通道，公开 props/emits 和版本 1.0.0 保持，未篡改封存契约。
- 实际 HTTP 发现学生文件正式绑定后被通用教师权限 resolver 拒绝。FUNDING 专用读取核对真实学生账号绑定、申请归属、上传人和当前有效 BUSINESS_EVIDENCE；教师仍走原业务权限与数据范围。本人申请关联的教师内部上传不自动开放给学生。
- 页面实看发现手机把中文状态当状态码，显示“状态待确认”；改为 status 与 label 分传，提示改为奖助评审语境。获资助不等于已发款，已驳回/已取消/已归档不再提示继续等审核。
- 教师手机首页实际点击“资助申请待审：甲一”曾进入困难认定申请1。沿工作台 action → todo_route_registry._mini_target → 共用审批页 onLoad 查明：后端仅下发 recordId，页面缺 type 时默认 AID_APPROVAL。统一路由对共用审批页补业务 type，覆盖奖助、困难认定/调整和处分两类；保留原授权查询，不按标题猜业务。奖助当前仍是待审队列落点，未谎标 exact 详情。

自动验证与失败记录：

- 新实际数据库 HTTP 闭环 test_funding_application_workspace.py：2 passed / 127.25 秒。覆盖四客户端登录、项目资格覆盖、上传和他人禁止读取、标准金额确认哈希、教师手机退回、学生修改/重提、新申请窗口关闭后在途重提、逐级评审、公示确认、登记发放结果，以及金额冻结失败零落单。日志 .codex-temp/funding-application-backend-final.log；登记发放不是调用银行付款。
- 同轮原有奖助、额度、授予资格复查与手机附件绑定 32 项通过；初轮另2项失败分别为上述本人文件404和测试 studentId 字段类型，修正后新闭环2项通过。中间重复 resolver 注册导致导入失败，删除旧通用 FUNDING 注册后恢复，失败日志保留。
- 文件与绑定回归最终 16 passed / 147.70 秒，.codex-temp/funding-file-access-final.log。首次旧断言要求原 resolver，更新为专用注册并新增当前绑定、失效/历史/其他业务/其他学生/教师内部上传拒绝回归；未放宽授权断言。
- 学生 PC 153 项通过，lint/build 通过；教师 PC 785 项通过，lint/build 通过。见 .codex-temp/funding-application-{portal,teacher}-test.log 与对应 check0/check1 日志。
- 小程序附件修复后312项通过；状态文案修复后最终313项全部通过，H5/mp-weixin均 Build complete，.codex-temp/funding-sync-mini-{test,h5,weixin}.log。
- 共用待办业务类型、移动动作、待办路由及材料通知回归51项通过 / 3.10秒，.codex-temp/funding-sync-todo-route.log。覆盖同一recordId下不同业务保持各自type，学生奖助路由不受影响。

实际 UI 证据：仅 leave-ui-test 隔离学校，学生 PC 提交带 aid-material-v1.pdf 的奖学金申请。提交成功后说明、本人确认、附件0/5及页签编辑标记均清除；记录进入辅导员初审，原材料可读取。学生手机刷新出现同一申请和材料，已核验修正后的中文状态与提示。教师 PC 正式登录后选择对应奖学金项目/批次，读取甲一、标准金额3000.00、相同说明和PDF，出现原审核按钮。教师手机旧会话加载失败，正式登录辅导员后首页读到该待办；点击时发现并修复上述业务类型遗漏，复验另记。未在 UI 中实际重复批准或登记付款，完整流转的证据来自隔离HTTP测试。

后续仍需：奖助待办首100条截断与完整对象深链；本人完整结果/发放明细；统一材料聚合投影；教师奖助各三级页的新壳适配与视觉复审（本轮实看教师奖助仍走旧布局）；项目批次、公示申诉、勤工/贷款/减免各分支审计施工；微信原生/真机验收及全五批最终复审。不得把新壳原型或附件子链通过当作全系统完成。

最终补充验收：

- 统一路由修复后，教师手机正式登录，从首页“资助申请待审：甲一”点击去处理，实到奖助待审；展开可读同一申请说明和 aid-material-v1.pdf。复验时发现没有 allowedActions 的第二个真实缺口，已按上述正式审批校验补齐手机列表/详情投影。再次正式登录与点击后，页面显示“通过 / 退回 / 驳回”及“审核通过”按钮，附件仍可读取。未在UI额外审核该单。
- 增补权限投影断言后的实际HTTP闭环最终 **2 passed / 145.49 秒**，.codex-temp/funding-sync-actions-backend.log。当前受理辅导员列表与详情均有动作；非当前受理节点的管理员、已退回申请均无动作；原退回重提、公示授予、登记发放及事务回滚仍通过。不是靠前端默认按钮放开操作。
- 接口重启期间旧手机会话曾显示示例首页；正式登录后恢复真实1条奖助待办。此异常未作为成功证据，首页失效会话/演示回退需后续专项修复，不能让其在正式使用中冒充真实业务。
- 最终 git diff --check 通过；保留学生PC、学生手机H5、教师PC和教师手机H5验收标签。未做微信原生真机验收，未完成教师奖助同壳与其他三级页面施工。当前是第二批奖助申请材料子链进展，全五批继续，下一步先补奖助待办完整查询与结果详情并接入已确认新壳。

### 2026-09-06 续接：奖助待办完整分页、通知原单直达与教师新壳

上一轮完成金额/材料、待办业务类型和动作投影，有实际代码与验收证据，本轮分类为进展。基线main/6810bd628，开工167项未提交/未跟踪变更，保留其他工作。按《奖助服务-四端施工包》“教师待办完整查询、对象直达与新壳”先完成调用链审计，再改代码。

- 教师手机待办从“取前100条再过滤”改为正式SQL：本校有效申请/学生、班级读取范围、当前评审节点对应身份范围、最新PENDING流程任务的真实受理人、审批权限全部进入COUNT/LIMIT之前；姓名/学号字面搜索支持%，返回完整total和分页参数。旧Todo不会覆盖转派后的最新任务；未创建正式任务的单据不会伪装成可处理待办。PC普通列表保持默认读取语义。
- 手机API适配传递page/pageSize/keyword，共用审批页对奖助开放搜索、加载更多、失败重试；沿已有请求序号和已提交搜索条件处理乱序及翻页。带type=FUNDING_APPROVAL和recordId时先读原申请详情，已处理结果仍能查看，错误/无权限目标不替换成队列中另一个学生。统一路由的exact标志仍保守保持false，本轮没有扩大共用路径所有业务的精确落点声明。
- 正式funding.get_application统一投影allowedActions，复用最终安装的实际受理校验；移除上轮仅手机的重复动作查询辅助函数。手机队列的SQL已按同一授权约束筛选，不再逐行另开读取。教师PC初审按钮改依赖服务端动作，学校管理员可查看初审申请但不能越节点处理；公示确认仍沿原权限与命令，不在本轮重设学校政策。
- 奖助工作台和funding子路由接入现有TeacherWorkspaceFrame，沿同一navPlan、权限、页签、快捷栏和四种主题；不新增另一套壳。窄栏明确显示项目/批次/评审/申诉/统计等，展开保留原名和路由。主工作台删除重复任务上下文条，压缩项目批次操作区，名单变为真实可键盘操作的按钮，突出姓名、学号、金额和当前选中项。资格摘要使用主题色变量，非受理人显示准确只读说明，不误称“终态”。批次选择器的原英文类型改用现有中文词表。
- 教师手机详情补资助类型、标准金额和批准金额；保持服务端脱敏字符串，未知批准金额不填0。学生PC与学生手机本轮未修改；原申请材料与金额闭环仍在后端真实四客户端回归中验证。

验证：

1. 后端test_funding_mobile_queue、test_funding_application_workspace及test_affairs_funding共 **11 passed / 249.68秒**，.codex-temp/funding-queue-backend.log。122条有效待办分7页完整到达且无重复，最新任务转派、错误节点/班级、删除申请/学生/任务和缺失流程均排除；%字面搜索、分页参数校验、非受理人无动作、真实手机退回后队列减少与原详情保留均验证。原四客户端上传、金额快照、退回重提、逐级评审、公示授予、登记发放及失败回滚仍通过。合成数据仅在隔离测试库；没有银行付款。
2. 小程序最终 **317 passed**，H5/mp-weixin构建均DONE；.codex-temp/funding-queue-mini-final-{test,h5,weixin}.log。新增奖助分页查询、原单终态直达、返回错误ID拒绝展示、脱敏/未知金额显示测试；原认定队列、版本冲突等回归保持。
3. 教师PC **786 passed**，最终lint通过，最终build通过24.29秒并生成21条既有官网预渲染路由；.codex-temp/funding-workspace-teacher-final-{test,lint,build}.log。初轮新增测试正则空格写法触发1项lint错误，改为明确量词后通过，未改动或放宽规则。
4. 真实运行的教师PC recordId=1深链打开隔离甲一奖学金申请，曜石黑新壳、窄栏全名/简称、项目批次中文、名单选中、标准金额3000.00与原PDF可见。管理员不出现初审动作，提示由对应节点受理人处理。教师手机同一通知自动展开原单和材料；返回全部待办，搜索不存在学生为0，搜索A001为1，仍有当前辅导员的通过/退回/驳回动作。未在UI重复办理此单，状态修改的闭环来自上述正式HTTP测试。
5. git diff --check通过。保留两教师验收标签；这次H5截图为桌面浏览器宽度，不声称手机尺寸、微信原生或真机已验收。

未完成项继续有效：两学生完整申请结果/发放明细与统一材料聚合投影；教师项目/批次/公示申诉/发放及勤工贷款减免各页的完整流程和视觉复审；工作台失效会话示例回退问题；原生/真机与全五批复审。新壳接入是基础适配，不等于所有奖助三级页面已重新设计并验收。下一步继续本人完整结果与公示/发放链，不跳到下一批，不标记整个奖助或第二批完成。未提交、推送或部署。

### 2026-09-06 续接：两学生端奖助结果详情同步

沿“批量同步”继续四端业务施工，main 上原有改动保留。本子链先完成奖助施工包中的本人结果详情关联审计，再实现 API、两个学生端内容页及闭环验证；不代表第二批或五批整体完成。

- 新增 affairs_funding_student_service.detail，由学生 PC 和学生手机两个正式入口共用。真实账号与学生档案绑定、本校本人申请和未删除状态先校验；项目、批次、申诉与发放再按租户和申请关系查询。只投影本人需要的说明、标准/批准金额、公示与结果时间、发放登记和申请级历史。不返回教师资格快照、银行账号、申诉者身份/原始内容或操作人；同编号项目/批次审计不混入申请历史。
- 申请标准金额、批准金额、获资助资格和实际发放登记分别展示。未批准不填0；无台账明确尚未登记；失败提示后，重新登记成功不残留旧失败原因。发放登记不声称银行到账。本人复核只展示已形成的结论，不开放第三方申诉内容。
- 学生 PC 使用已确认工作区壳内的原生 dialog 详情，手机使用独立全屏详情层；支持 recordId 直达、加载失败重试、返回与退回后修改入口，复用现有材料组件。关闭详情不卸载申请表单。请求序号、卸载失效与返回对象ID核验防止串单。手机实看修复根页面和详情双滚动、金额字体被标签样式覆盖两处问题。

本次验证：

1. 后端 test_funding_application_workspace 最终 **2 passed / 188.82秒**，日志 `.codex-temp/funding-student-detail-backend-final.log`。真实四客户端闭环覆盖申请/材料、退回补正重提、逐级评审、公示确认、发放失败→重新登记成功；两学生详情逐字段一致，非本人/教师账号/删除申请拒绝，删除后列表排除。申诉者与项目审计隐私标记未泄漏。中间一轮因测试先软删除再要求列表找到记录而失败，修正测试顺序并补删除后列表排除断言，未修改业务逻辑去迎合错误断言。
2. 学生 PC **159 passed**、lint/build通过（4.83秒），日志 `.codex-temp/funding-student-detail-portal-{test,lint,build}.log`。新增两客户端实际组件脚本的乱序、错误ID、关闭后响应、未知金额行为用例。
3. 小程序 **317 passed**，初次H5/mp-weixin构建通过；两处纯布局修正后再次构建均DONE，日志 `.codex-temp/funding-student-detail-mini-final-{h5,weixin}.log`。教师PC本轮无新改动，上一子链786项与构建证据仍有效，不冒充本次重新执行。
4. 隔离运行API8018已更新，正式账号重新登录。学生PC与学生手机H5真实打开甲一申请1，项目/学年/初审状态、3000.00标准金额、批准尚未确定、无发放登记、原PDF和提交记录一致。PC窄窗口详情只有一个可滚动容器；手机H5文档高度与视口均720，额外外层滚动已消除。两端都实际填写临时说明→打开详情→关闭后原说明保留，随后清空临时文字，未提交测试草稿。保留两端结果预览标签。手机截图是桌面H5宽度，尚未验证微信原生真机。
5. git diff --check通过。没有Git提交、推送、部署或银行转账。

后续继续公示申诉和教师发放工作区的完整页面/流程审计与施工。待完成项保留：学生列表项目识别、材料聚合投影；教师项目/批次/公示申诉/发放及勤工贷款减免子页；失效会话/演示回退专项；原生真机与五批整体复审。

### 2026-09-06 续接：奖助申诉复核四端同步与文件保存核验

教师 PC 申诉工作区已经写入当前 main：待复核优先、独立分页、登记区折叠、原申请与理由上下文、主动选择复核结论、失败保留意见与版本。教师手机增加服务端 REVIEW 动作、完整分页、按申诉 ID 读取原单，已关闭记录只读。两学生继续复用本人申请结果详情查看复核结论。

已核验的证据：后端申诉与版本闭环 6 passed（240.44 秒，funding-appeal-backend-final.log）；教师 PC 789 passed，lint 与最终构建通过（33.30 秒）；小程序 319 passed，H5 与 mp-weixin 构建通过。后端覆盖两学生分别提交、两教师端分别复核、错误版本及重复复核拒绝、208 条记录完整分页和终态只读。中间出现过新接口权限注册遗漏、手机入口分支遗漏与测试语法错误，均修正后重新通过，未降低权限或放宽规则。

继续发现申诉结果消息将申诉 ID 当成申请 ID 的问题，已保存新消息显式申请 ID、历史消息按真实关联转换与失效关联禁用的修复。消息回归首轮 10 passed / 1 failed，发现共享参数校验会丢弃 bizType，导致历史转换未生效；已将 bizType 登记为仅奖助消息的可选参数，正在重新验证，尚不记为通过。

本地文件保存与 Git 提交分开记录：当前修改和新增文件均在磁盘，仍未 Git 提交、推送或部署。教师 PC 5178、学生 PC 5198、小程序 H5 5188 均实际请求 HTTP 200；后端 8018 已监听。小程序启动日志显示实际端口为 5188，不能沿用先前 5208 链接。预览服务重启不代表代码曾丢失。

保留未完成项：本轮教师手机界面最终实看、消息回归、公示扫描与分页、发放及其余奖助子页、微信原生真机、全五批复审。隔离视觉样例由直接写入测试库生成，不能作为完整业务流转证据。

### 2026-09-06 续接：公示工作区、批次范围与移动分页复审

本轮先在奖助施工包补公示与下游发放的调用链和缺口，再按已接受的教师 PC 壳改公示内容区；没有切换 main、覆盖旧改动或扩大学生权限。

- 公示页去除三张占高指标卡，改为紧凑总数、完整分页、公示截止、办理条件、同申请申诉入口。状态、总数和可确认动作取自真实后端；页面空了不再宣称整个批次完成。有已获资助申请才显示本批次发放入口，同时交代仍在评审/补正的人数。
- 批量确认新增可选 batchId，手动调用传当前用户，查询与逐条提交均约束数据范围；无请求体兼容旧调用，服务层无参调用兼容后台扫描。候选不再限前 200 条，保留逐条事务、申诉重验、当前资格重验和额度冲突隔离；返回成功、未到期、申诉阻断、额度/资格/批次/公示时间及并发状态等结果。单条确认也拒绝失效批次，窗口冻结显示的版本与范围，失败保留核对上下文。
- 申诉历史消息跳转专项最终 11 passed / 165.41 秒（funding-appeal-notice-backend-final.log），新旧消息都指向对应申请，失效关联禁用跳转。首次参数白名单丢失 bizType 的失败已经修正，未移除断言。
- 手机实看后继续追踪适配器，确认原分页只在组件层成立，affairsAppealApi 的 query 被 realRequest 忽略。统一使用正式 GET data，修复奖助申诉、认定异议及同文件积分申诉列表。新测试跨真实适配器、realRequest 和 uni.request，修复前实际三次页码均 undefined，修复后分别为 2/3/4。初次测试装载器未处理 export default 的语法问题单独修正后，才复现真实请求缺参。

验证证据：

1. 公示初轮后端 5 passed / 180.64 秒（funding-publicity-backend.log），含原四客户端申请→补正→评审→公示→申诉→授予→发放登记闭环及 MySQL 单条额度冲突隔离。补未到期/缺失时间诊断后专项 3 passed / 134.71 秒（funding-publicity-backend-final.log）。超过 200 条、指定批次、模拟范围解析器限制本班、学生权限拒绝、原版本、未结申诉、幂等、无请求体兼容、失效批次拒绝均覆盖；其中范围测试注入的是既有范围解析器结果，不声称该用例覆盖全部角色配置。
2. 教师 PC 793 passed，lint 与 build 通过（32.09 秒；funding-publicity-teacher-complete-*）。初次测试装载的 CRLF 处理失败已修正；旧契约曾要求空列表显示“本批次完成”，按真实新行为改为仅已获资助对象提供发放入口，同时保留批次传递和不强制跳页要求。末次办理结果显示再精简为仅展示非零原因，相关 10 项与构建另记 funding-publicity-feedback-*。
3. 小程序 320 passed，H5 / mp-weixin 均 DONE（funding-appeal-transport-mini-*）。真实浏览器 Network 请求证据：/mobile/teacher/affairs/appeals/FUNDING_APPEAL?page=1&pageSize=20，参数确实抵达请求层。此前仅组件 mock 的分页证据不能替代该链路验证。
4. 教师 PC 实际展示隔离甲一申请2，截止日、申诉阻断、批次24与金额一致；点击“查看申诉”只读取原申请2的复核记录。批量确认窗口显示批次24，实际提交返回确认0、申诉待复核1，原申请仍公示，没有授予或银行转账。教师手机同一会话登录后可读取申诉1及原申请2、原始理由和复核动作，打开意见窗口后取消，未在UI复核此样例。390×844的单页设备模拟中 clientWidth/scrollWidth 都是390，刷新后仍读原单；模拟已清除。这是H5设备模拟，不是微信原生真机验收。
5. 教师/学生 PC 预览与隔离后端持续运行；教师小程序使用127.0.0.1:5188（不改动另一个::1监听进程）。两个验收标签已保留。git diff --check通过，代码和进度文件已保存；未Git提交、推送或部署。

仍未完成：奖助项目/批次、学生列表项目识别与材料聚合、发放全内容页、勤工贷款减免等其余三级流程；移动会话失败反馈与材料队列参数专项；微信原生真机；住宿等后续批次及最终全五批复审。公示与申诉子链推进不等于奖助模块或第二批全部完成。下一步继续发放工作区及其四端结果衔接的完整审计与施工。

### 2026-09-06 续接：发放登记、教师手机结果与四端实看

上一轮状态询问仅核验保存位置，没有业务施工；本轮已重新核验磁盘、main和测试终态后继续，无重复启动旧数据库测试。奖助施工包已记录本轮真实调用链，保留原权限、事务、版本锁和异步Excel导出。

- 教师PC发放页将五张大指标卡合并成紧凑汇总，列表、统计和导出按同批次。批次列表读取完整分页，显示项目、学年、中文状态；仅使用服务端允许动作。生成和导出窗口冻结打开时的范围，成功/失败窗口冻结申请、金额与版本，快速重复点击仅提交一次，失败保留输入，旧列表/统计/导出轮询不得覆盖新上下文。补项目参数单独变化时的重验，避免旧批次项目关系漏检。
- 最终stats_integrity_guard按可选batchId聚合；API拒绝非正批次。batch_job_guard动作投影核验发放权限。教师手机真实原单get_application新增同租户/同申请/同学生/未删除的只读发放投影，金额继续按权限脱敏，不返回银行后四位和回单号，不添加移动发放命令。
- 后端最终3 passed / 175.81秒，日志funding-disbursement-backend-final.log，覆盖真实四客户端申请、材料、退回重提、逐级评审、公示/申诉、授予、发放失败→重新登记成功，以及多批次统计、异步XLSX真实文件行数。教师PC最终801 passed、lint/build通过（29.96秒），日志funding-disbursement-teacher-final-{test,lint,build}.log；新增行为测试覆盖上下文/版本/重复点击/分页/乱序。小程序320 passed，H5/mp-weixin均DONE，日志funding-disbursement-mini-{test,h5,weixin}.log。日志位于.codex-temp。
- 隔离UI库批次25/申请3由直接种子生成已获资助状态，浏览器随后真实执行生成台账→登记失败→重新登记成功，观察统计1笔已发放/金额3000.00、回单DEMO-2026-09-06-25、旧失败原因消失；四端真实页面均读取同一金额与08:31:44登记时间。该种子缺批准金额/完整前置审计，学生端正确显示“尚未确定”和无历史，不能把这份视觉样例算作申请审批闭环证据；完整闭环以独立MySQL HTTP测试为证。没有银行付款。
- 教师手机按390×844 H5检查，文档宽度=滚动宽度=390，无横向溢出，已清除设备覆盖。手机为H5模拟，尚非微信原生/真机。学生端登录使用真实隔离账号；新标签未登录先显示失败，不冒充后端数据不存在。会话失效提示仍为后续已登记缺口。

继续推进本人列表识别：两个学生列表均只显示类型，实际三份不同申请无法辨认。已先审计共用funding_my与详情的批次/项目关系，再保存批量同租户关联查询及两端项目名、学年、批次/申请编号展示；不改变申请动作，不增加逐行查询。其追加回归与UI结果将在下一段记录。剩余全范围仍有效：材料聚合、教师项目/批次/统计及勤工贷款减免等三级页，原生真机与五批整体复审。未Git提交、推送或部署。

本人列表追加验证：test_funding_application_workspace最终2 passed / 167.88秒（funding-list-identity-backend.log），在完整四客户端流程末尾比较两个学生列表的项目名、学年、batchId与本人详情，保留删除排除/隐私等原有断言。学生PC159 passed、lint/build通过（2.76秒），funding-list-identity-portal-{test,lint,build}.log；小程序320 passed、H5和mp-weixin均DONE，funding-list-identity-mini-{test,h5,weixin}.log。两个学生端实看均能区分项目“隔离验收·发放回执”、励学奖学金2026-2027/2025-2026与批次25/24/1；手机H5刷新一度返回身份选择，经正式登录恢复，失效会话/跨标签身份专项仍未完成，不把恢复登录算作该专项修复。教师PC宽屏DOM1440无横向溢出、1个表格1条记录；CDP截图出现合成重影，不能以该截图作为精细视觉验收，设备覆盖已经清除。

当前办理检查：学校经办从公示的本批次入口或发放台账选择项目/学年→生成→核对申请与金额→登记银行结果，错误时保留意见并按最新版本重新办理；教师手机原申请可查看结果；学生从本人列表按项目学年识别原单并打开详情看登记进度。真实款项以实际收款核对。剩余施工顺序维持奖助模块：学生材料聚合、教师项目/批次配置及统计、助学金/勤工/贷款/减免等完整页面流转，再模块复审；没有跳过五批剩余工作。所有本轮测试句柄已取得退出码0，git diff --check通过；文件已保存，未提交推送部署。

### 2026-09-06 续接：奖助原申请内的材料聚合与四端范围修复

上一目标轮是有效进展；本轮重新核验main/6810bd628和191项原有条目，先在奖助施工包记录附件、补交、版本、责任人与通知/待办的真实调用链，再施工。

- 四端材料工作区原来只识别LEAVE/AID，奖助传入FUNDING时丢失申请范围。本轮补教师PC、学生PC、教师手机和学生手机的FUNDING范围、单项材料直达与返回原申请。前端筛选继续调用后端本人/权限/学生范围过滤，不增加写权限。
- 三份FundingEvidence供四端使用，保留原文件SDK与预览；原附件下新增紧凑“补交与验收”，展示材料项、状态、截止、当前提交版本数与文件名、退回/验收意见，以及现有材料工作区入口。按10项数据库分页，不以第一页或加载失败宣称已交齐；关联对象核验、请求序号、卸载失效、失败页重试和刷新保护。教师手机显式使用teacher接口，学生使用本人接口。详情原重复材料按钮已合并。
- 修正学生PC“奖助申请申请”重复文案，材料业务副标题将SCHOLARSHIP等真实类型转为中文；未臆造材料验收自动通过奖助审批的规则。

验证证据：

1. 新增test_funding_material_flow与原test_funding_application_workspace，共3 passed / 197.66秒，funding-material-backend.log。真实隔离MySQL与四客户端登录：申请原附件、要求补交、学生PC第一版、教师手机退回、学生手机第二版、教师PC验收；重复提交与错误版本409、他人材料不可读、不同申请不混入、两版本历史保留、验收后奖助申请仍保持原审批状态。原全链路测试继续覆盖评审/公示/申诉/发放。最后仅汉化副标题，py_compile通过，不将该文案修正描述为新一轮数据库全链路重跑。
2. 教师PC814 passed、lint/build通过31.94秒；学生PC160 passed、lint/build通过3.27秒，最后重复文案修正再次build通过2.32秒；小程序322 passed、H5/mp-weixin均DONE。日志funding-material-{teacher,portal,mini}-final-*与funding-material-portal-label-build.log。12项新增组件行为测试实际执行三份组件脚本，覆盖四种角色端的分页参数/范围、乱序、错误ID、卸载和错误反馈；另补四端材料工作区FUNDING上下文及返回路由测试。27项定向上下文组合通过，funding-material-context-final.log。
3. 实际UI：教师PC在奖助申请1创建隔离材料要求4“隔离验收·获奖证明补充件”，学生/教师两个PC与两手机原申请均可见同一项和原PDF；学生PC、教师手机通过单项入口保持bizType=FUNDING/bizId=1/materialRequirementId=4，教师返回定位原申请1。学生手机390px DOM宽度=滚动宽度，无横向溢出；CDP截图存在桌面合成缩放/重影，不能作为精细像素验收，已清除设备覆盖。仍是H5，未验收微信原生真机。
4. 浏览器真实上传补交样例，通过FileChooser选择.codex-temp/funding-supplement-ui.txt，确认后界面保留输入并显示“文件已上传，正在等待安全扫描；稍后点击检查并提交审核，无需重复上传”，要求仍MISSING/版本0，未假成功。已关闭确认窗，保留tab6的已上传待扫描草稿便于续接，不能刷新丢弃此窗口状态。

尚未完成的真实联调与下一步（仍属于奖助材料，不跳模块）：

- 隔离UI扫描环境尚未完成：HTTP辅助脚本funding-material-ui-flow.py第一阶段上传文件6 funding-supplement-v1.txt后提交409；只读数据库确认PENDING/QUARANTINED，未篡改扫描状态。脚本已改为复用已有文件6并在readyForBusiness为真后才提交，accept阶段也缓存上传ID，禁止重复上传。另有浏览器上传的funding-supplement-ui.txt等待扫描。docker ps没有ClamAV容器，3310明确由本任务MySQL占用；尚未确认有效ClamAV配置/宿主进程。下一步应核验配置并启动隔离真实扫描服务/worker，完成上述UI文件安全扫描，再继续实际退回重交验收，不能绕过扫描或将DB测试默认准备好的文件等同于该环境已扫描。
- 继续审消息发现student.affairs.material注册只保留materialRequirementId；原生产者bizType/recordId会被参数校验移除。现有消息仍能定位到唯一材料要求，故没有串单，但历史消息及教师待办只带材料ID时缺“返回原申请”上下文。需从已授权加载的材料要求恢复真实所属申请，避免把材料ID误作奖助申请ID；本轮未修改消息协议或完成这个补项。
- 材料登记仍要求经办填写材料项编码，业务副标题暂只有中文类型、未聚合项目学年；记录为便捷性复审项，不能标成全部材料页已达到最终效果。

所有本轮测试进程均取得退出码0；没有Git提交、推送、部署，没有银行操作或生产数据修改。五批完整目标、奖助其他项目/批次/统计及勤工贷款减免子页、后续所有批次与原生/全局复审均继续保留。

### 2026-09-06 续接：真实扫描、两版补交验收与历史入口归位

上一段登记的两个缺口已经实际闭环。隔离 UI 环境使用官方 `clamav/clamav:stable` 镜像，固定镜像摘要 `sha256:f0954d679017eb6d48221e2b2be3ac5457bf278a844f39b672376f55a085f591`；独立容器 `codex-funding-clamav-0ea278f0` 绑定本机回环 13310，状态为 running/healthy。扫描 worker 只连接 `codex_leave_ui_test_0ea278f0` 与本任务文件目录，进程持续运行；实际引擎返回 `ClamAV 1.5.4/28108/Sun Aug 30 06:27:10 2026`。文件 6、7、8 均经既有 FileScanJob → ClamAV INSTREAM → FileScanRecord → 安全存储 finalize 链处理为 CLEAN/readyForBusiness，未直接改数据库状态、未关闭 FILE_SCAN_REQUIRED、未增加跳过扫描入口。

真实隔离业务流：要求 4 属于奖助申请 1。学生 PC 复用已扫描文件 6 提交第 1 版，教师小程序正式退回并填写“请补全证书获奖日期与盖章说明。”；随后学生小程序上传文件 8 `funding-supplement-v2.txt`，worker 实际扫描就绪后提交第 2 版，教师 PC 正式验收并填写“补交版本完整，材料验收通过。”。最终要求为 ACCEPTED、版本数 2；第 1 版保留为历史版本，第 2 版为当前版本。奖助申请仍处于辅导员初审，材料验收没有越权自动批准资助申请。

历史入口修复按审计决定落地：教师 PC、学生 PC、教师小程序、学生小程序都优先使用显式合法申请上下文；旧通知/待办只携带材料编号时，仅从当前账号已经授权加载且 requirementId 匹配的材料行取得真实 bizType/bizId。查询过滤、学生范围、责任人与审核权限没有改变；无匹配行不显示返回动作。实际用旧入口 `recordId=4`/`materialRequirementId=4` 打开四端，均显示奖助申请 1 的已验收材料和两版记录；点击“返回原申请”分别到学生 PC `campus-service?tab=funding&recordId=1`、教师 PC `funding?recordId=1`、学生小程序 `funding?recordId=1`、教师小程序 `affairs-review?type=FUNDING_APPROVAL&recordId=1`，没有把材料 4 当成申请 4。

本轮完整验证：教师 PC 全量 815 passed，修正测试正则规范后定向 5 passed、lint 与生产构建通过；学生 PC 160 passed、lint 与生产构建通过；小程序 324 passed，H5 与 mp-weixin 均 Build complete。小程序首轮 323/324 捕获旧导航测试没有装入新增返回上下文，测试改为真实可见返回动作后全量通过；没有删除忙碌阻断断言。四端历史上下文定向 19 项通过，覆盖材料 ID 与申请 ID 不相等、无匹配行、显式上下文优先与不修改查询过滤。`git diff --check` 通过，仅有仓库既有 CRLF 提示。

以上代码、测试与记录均已保存到当前 main 工作区，仍未 Git 提交、推送或部署。当前浏览器验证为小程序 H5；微信开发者工具与真机验收仍保留。奖助模块后续继续项目/批次配置、统计、助学金/勤工助学/贷款/减免分支及模块复审；全五批范围不缩减。

### 2026-09-06 续接：奖学金/助学金项目与批次配置闭环

本轮继续在当前 main 与原未提交施工上推进。先审计 FundingProject、FundingBatch、两学生开放批次、申请金额冻结、额度触发器及在途评审关系，再改后端与教师 PC 内容区；没有改动用户已经确认的 UI 外壳，也没有把勤工、贷款、减免等独立模型强并入奖助申请状态机。

- 项目页改为紧凑摘要、服务端搜索/类型/状态筛选、真实分页和表格主工作区；可创建当前闭环的奖学金/助学金项目，并按服务端允许动作启停。标准金额强制为正数，名额有限值；停用说明明确已有申请继续办理。
- 批次页改为紧凑摘要、完整项目选择、服务端搜索/项目/状态筛选和真实分页；草稿默认、行内发布、人工关闭均带确认与可见版本。列表显示申请窗口、公示、申请数、名额占用、学生端状态和额度事实。主奖助工作台删除重复新建抽屉，统一跳项目/批次设置页，且完整读取项目和批次分页。
- 后端列表补真实 summary 与逐行投影，项目状态和批次 PUBLISH/CLOSE 使用租户、权限、版本与审计约束。发布重验项目启用、类型、金额和申请窗口；关闭或项目停用只阻止新申请，不能删除、拒绝或回滚既有申请。
- 教师 PC 真实浏览器完成项目创建→草稿→发布→关闭。学生 PC 同一搜索在草稿时 0、发布后 1、关闭后 0；教师曜石黑页面在 1264×710 工作区首屏直接看到表格，项目/批次抽屉及高风险确认可用。验证数据均位于隔离学校，不含真实付款。
- 新专项 `test_funding_catalog_workspace.py` 首次仅因测试时间字符串包含微秒不符合现有秒级 API 契约失败，修正测试输入后 **1 passed / 94.62 秒**；生产接口未为测试放宽。前端项目/批次专项 **6 passed**，教师 PC 全量 **821 passed**，小程序全量 **324 passed**。完整关联后端套件和最终构建结果将在本段续写，不把仍运行中的命令记为通过。

代码、测试和记录均已写入磁盘，未提交、推送或部署。剩余顺序：收口本子链完整回归与两小程序构建，再审计资助统计、助学金管理、勤工助学、贷款、减免与临时补助；微信原生真机及五批总复审继续保留。

项目/批次子链最终验收：四组资金后端测试共 **12 passed / 1038.89 秒**，使用隔离 Docker/MySQL 串行执行，覆盖项目启停、批次草稿/发布/关闭、学生 PC 与学生小程序接口可见性、在途申请保留、材料、审核、公示、申诉与发放衔接。教师 PC 全量 **821 passed**、ESLint 通过、生产构建 **33.51 秒**并完成 21 条官网路由预渲染；小程序全量 **324 passed**，H5 与 mp-weixin 构建完成。专项 6 项与先前单链 1 项继续保留。`git diff --check` 通过，仅有既有 CRLF 提示。

教师 PC 与学生 PC 已实际完成“项目创建→批次草稿不可见→发布可见→关闭后停止新申请”的同数据验证，教师页面在 1264×710 首屏保留紧凑统计与表格主工作区。小程序 H5 追加登录检查被浏览器保存的旧密码和验证码状态干扰，未将这次登录页面操作登记为移动视觉验收；正式移动接口闭环、324 项页面测试及 H5/mp-weixin 构建均为退出码 0。代码、测试和记录保存在当前 main 工作区，仍未提交、推送或部署。下一项转入资助统计与台账内容区的调用链审计和施工，随后再进入助学金、勤工助学、贷款、减免及临时补助分支。

### 2026-09-06 续接：资助成效、困难覆盖与到账对账

审计确认旧统计页只汇总 FundingApplication，却把文案扩展到勤工、贷款与减免；五张大卡占高，并把拒绝和取消也算入“办理中”。本轮改用真实模型分别统计奖助获批、发放台账、最新有效困难认定、在岗勤工、已确认贷款和已出具减免，受助学生跨分支去重。到账登记与获批结果分开，获批但未建台账、待登记、失败和退回均进入核对范围。

- 后端保留原 `total/granted/byStatus/byType` 兼容字段，新增覆盖率、困难生覆盖、分支人数、年度趋势、发放状态和金额摘要。精确金额只向学校管理员、学工管理员和资助经办角色投影。
- 新增七类只读下钻，按账号数据范围返回脱敏姓名、学号与来源；发放异常同时包含显式异常台账和“获批但尚未建台账”。短历史学号也强制脱敏。
- 教师 PC 改为紧凑总览与主要工作入口，首屏突出覆盖和到账问题；分类与年度明细下移。下钻抽屉具备分页、错误重试与请求序号保护。公共 AppDrawer 补齐 small/medium/large/xlarge 实际宽度映射，680px 名单无横向滚动。

验证证据：真实 Docker/MySQL 专项 **1 passed / 88.17 秒**，既有统计接口与服务回归 **5 passed / 401.59 秒**；教师 PC 定向 **3 passed**、全量 **824 passed**、ESLint 和生产构建通过，Vite 构建 **33.16 秒**并完成 21 条官网路由预渲染。实际浏览器在曜石黑新壳读取隔离库数据，受助 1/2、困难生受助 1/1、奖助获批 1、发放已登记 1，脱敏名单显示“甲* / A**1”。初次页面出现新字段为零是隔离 8018 后端仍加载旧进程；确认同项目进程后重启，真实接口与页面恢复。`git diff --check` 通过，仅有仓库既有 CRLF 提示。

本子链已保存到当前 main 工作区，未 Git 提交、推送或部署。四端总目标仍未完成；下一步进入勤工助学，先审计岗位发布、学生申请、教师审核、在岗记录与学生两端结果，再按同一 UI 壳施工。

### 2026-09-06 续接：勤工助学申请、上岗与月度补贴四端闭环

本轮先审现有模型、接口、权限和四端页面，再施工。旧系统只有教师 PC 代报名和简化状态按钮；学生两端没有岗位申请，教师小程序没有勤工队列，岗位也缺地点、排班、截止、月工时与协议字段。按照教育部勤工助学办法，将“学生自愿申请、学校审核、协议确认、上岗、月度考核和补贴、结束岗位”落到同一 `WorkStudyRecord`，并保留仓库现有单级经办权限，未虚构学校尚未实现的多级审批。

- 后端与数据库：增加岗位用工类型、地点、排班、截止、月上限、协议要求，以及学生申请说明、可工作时段和协议确认时间；新增学生本人申请/撤回、教师和移动四端接口。录用复核名额，上岗前强制协议确认，月度工时按学生跨岗位汇总且不超过 40 小时，重复月份和错误版本拒绝。本人只收到本人动作和精确补贴，教师数据继续按原范围过滤。迁移 `20260906_work_study_four_end` 已成为唯一 head。
- 教师 PC：勤工内容区改为紧凑统计、申请记录/岗位目录切换、数据库搜索分页、岗位创建启停、录用/拒绝、协议上岗、月度登记和结束岗位。保留已确认的顶部、窄菜单、页签、目录与快捷栏。
- 学生 PC：新增同壳岗位与本人申请工作区，支持搜索、申请、待审撤回、状态与月度补贴回读，草稿接入现有未保存编辑保护。
- 两套小程序：学生端新增岗位申请/本人结果页；教师端新增待审核优先队列、状态筛选、协议上岗、月度考核和结束岗位。教师登录页移除学生专属新生激活入口。
- 联调基础设施：实看初期小程序请求仍命中旧 `8000`，导致隔离账号看似失效；修正 Vite 配置按 mode 读取 `.env` 后，同源代理正确连接 Docker 隔离 API `8018`。该问题属于本地开发目标读取缺口，业务代码和先前修改始终在磁盘。

真实浏览器与隔离 MySQL 使用同一条“图书馆阅览服务岗”记录：学生提交申请，教师 PC 录用并确认协议上岗，登记 2026-09 月 20 小时、合格、360 元；学生 PC 和学生小程序回读在岗与 360 元，教师小程序“在岗 1”显示本人申请信息、累计补贴、月度登记与结束岗位。页面读写均经过正式 API，没有手改业务状态。

验收结果：扩展原回归与四端闭环合并 **4 passed / 324.63 秒**；修改后最终四端专项 **1 passed / 89.28 秒**；权限注册 **12 passed / 462.31 秒**。学生 PC **161 passed**且生产构建通过；小程序 **324 passed**，H5 与 mp-weixin 两种构建均完成；教师 PC 定向 ESLint、全量测试和生产构建已通过。`git diff --check` 与 Alembic head 检查通过。现有警告为 `datetime.utcnow()`、Starlette 兼容提示和 CRLF 转换提示，未掩盖失败。

全部改动已保存于当前 main 工作区，仍未 Git 提交、推送或部署。小程序本轮是 H5 浏览器加 mp-weixin 产物验证，微信开发者工具与真机继续列为上线前检查。下一步按奖助分支继续审计并施工助学贷款、学费减免和临时补助，随后做奖助模块四端总复审；全学工五批目标保持不变。

### 2026-09-06 续接：助学贷款四端回执闭环

本轮先从 `StudentLoan`、资金扩展服务、教师 PC 旧推进按钮、两学生端、教师小程序、文件中心和权限注册表反向审计。结合教育部 2024 助学贷款额度政策、2025 学生资助手册和国家开发银行地方回执办理指南，明确系统只承担校内回执登记、核验和确认，不冒充银行审批或放款。高职年度金额由服务端约束为 1000—20000 元，同一学生同一学年只允许一笔非撤回记录。

- 后端新增回执加密/哈希、退回与重提、语义化核验/确认动作、各节点时间、本人接口、教师移动接口和 `LOAN` 文件访问解析。写动作继续使用真实贷款经办权限、学生本人关系、数据范围、租户、版本和审计约束。
- 教师 PC 以紧凑任务台账替代旧“下一步”机械推进；学生 PC 使用已确认同壳新增本人回执页；学生与教师小程序分别增加本人办理、待核验队列。四端按钮只消费服务端 `allowedActions`。
- 实际隔离库完成学生提交 8000 元回执 → 教师退回并填写明确原因 → 学生修改重提 → 教师核验 → 教师确认。学生 PC、学生手机与教师手机均读取同一终态，完整编号未返回客户端。

后端贷款合并回归 **4 passed / 182.79 秒**；教师 PC 全量 **827 passed**、ESLint 和生产构建通过；学生 PC 全量测试、lint、构建通过；小程序全量 **325 passed**，H5 与 mp-weixin 构建通过，handoff 已按当前源码重建。Alembic 保持单头。代码、测试与迁移都已保存到当前 main 工作区，未提交、推送或部署。

### 2026-09-06 续接：教师工作区去卡片化精修

按“老师聚焦业务本身”的要求复查曜石黑教师 PC。勤工助学原页同一组待审核、在岗和岗位数量在概览卡与台账重复，且说明卡把表格整体下推。现已删除概览卡和只读统计行，改为一处“申请与在岗 / 岗位目录”切换、一排可点击状态筛选和紧随其后的搜索表格；默认打开待审核，表格末列改为“下一步”。切到在岗后，真实学生记录及“月度考核 / 结束岗位”动作均正常显示。

申请评审入口同时去掉无批次时的重复左右空面板、无效状态条和无监听价值的返回按钮；当前没有批次时只保留单一短空态。存在真实申请时自动选中第一条并加载详情，老师进入页面即可处理，仍可手动切换其他申请。项目、批次、公示、受理、材料与审批能力没有删除。

新增教师聚焦专项 2 项，连同贷款与动作契约定向共 **5 passed**；相关 Vue 与测试 ESLint 通过，教师 PC 全量 **827 passed**并完成生产构建。改前/改后同视口截图已对照，首屏减少一层重复信息。下一施工项仍是学费减免和临时补助，不以本次视觉精修宣称全部教师页面已经完成。

### 2026-09-06 续接：减免与临时补助施工前审计

调用链已从 `FeeReduction` 模型、资金扩展服务、教师接口与旧页面反向核验到学生门户、小程序、公共文件绑定和访问解析。确认旧系统只有教师 PC 代录、批准/驳回和统一“发放”，其余三端没有入口；模型缺少学年、原因类别、退回补正、本人撤回、材料与结果落实方式；教师页面的摘要条、流程条、三张大指标卡和说明性副标题重复占用工作区。

本批施工范围已经冻结为：学生 PC/小程序本人申请、材料、撤回和补正；教师 PC/小程序待审优先队列、退回、批准、驳回和结果落实；学费减免显示“确认减免”，临时补助显示“登记发放”，但保留原 `ISSUED` 数据终态兼容；文件访问补本人范围解析。仓库没有真实多级审批权限，本批不伪造角色链，待学校制度确认后再把审核节点做成配置。实现和实数验收尚未完成，本段只记录已经完成的审计结论。

### 2026-09-06 续接：减免与临时补助四端闭环及教师内容区收口

减免与临时补助已按审计结果落地。后端补齐学年、原因类别、退回意见、本人撤回、补正重提、材料绑定、类型化落实动作与各节点时间，并新增学生 PC、学生移动端和教师移动端正式接口；`REDUCTION` 文件解析同时覆盖本人和原教师权限。迁移 `20260906_fee_reduction_four_end` 保持为唯一 Alembic head。

- 学生 PC 与学生小程序：本人申请、材料、撤回、退回补正、进度与结果回读。
- 教师 PC 与教师小程序：待审优先、退回、批准、驳回，以及学费“确认减免”或临补“登记发放”。
- 数据边界：四端只消费服务端 `allowedActions`；重复在途、跨本人、跨范围、跨租户、旧版本和未完成扫描的材料均由服务端阻断。
- 页面收口：另对教师 PC 18 个办理页集中删除重复摘要、流程说明和装饰指标卡，把主要动作放到页头，正文直接进入筛选、台账与下一步；统计、驾驶舱和正式详情继续保留必要汇总。

真实隔离库已完成学生提交 1800 元临时困难补助 → 教师批准 → 教师小程序登记发放 → 学生 PC 回读“补助已发放”。附件经过真实 ClamAV worker 扫描为 CLEAN 后才绑定。统一验收结果：后端四端专项 **1 passed / 130.44 秒**；教师 PC **829 passed**与生产构建通过；学生 PC 定向 **3 passed**与生产构建通过；小程序定向 **21 passed**，微信生产构建、WXSS 兼容检查、包体预算与 handoff 生成通过，主包 **0.49 MiB**、总包 **2.15 MiB**。

当前阶段代码已保存到 `main` 本地提交 `bbdcf78f9`，随后补入小程序共享详情组件的微信选择器兼容修正与本段验收记录。没有推送、部署或操作生产数据。奖助服务的勤工、贷款、减免与临补四端子链均已有可运行闭环，下一批应做奖助模块跨流程总复审和异常恢复，再进入其他学工核心模块。

## 2026-09-07：材料工作区聚焦与四端原单回读

- 基线：main / f138667d7ae197b304124a75455c15434b8d8474。共享工作区原有改动保留；本批仅前端、移动适配、定向测试与展示文档，无后端/数据库修改，无提交、推送、合并或部署。
- 教师 PC：登记缺项表单改为按需窗口，保留原申请对象回填；关闭再打开保留本页未提交内容。当前申请入口收起全局统计与无记录的批次区，材料清单、版本、当前状态优先展示；上下文提示与敏感材料说明改为紧凑文字，移除重复刷新入口。已验收记录和必要操作、版本授权读取均保留。
- 教师/学生移动端：从材料深链进入时直达这份申请的材料，收起无关业务入口、统计和空批次区；普通学工首页仍保留这些功能。
- 真实阻断：教师移动端未筛选时，uni.request 把 undefined 的 requirementId 序列化为空字符串，接口返回 400。affairsContractApi 现在只发送非空参数；不放宽后端整数校验。真实浏览器修复后材料查询为 page=1&pageSize=20，返回 200，显示授权材料。
- PC 登记命令按字符串传递大整数业务 ID，防止 Number 精度损失；失败保留输入，提交成功后的列表刷新失败单独提示，避免误认为写入失败。隔离测试覆盖大 ID、重复点击、失败保留与写后读取失败。
- 本次实际浏览器回读：FUNDING 申请 1 / requirement 4，四端都读取 V2 已验收；学生端保留 V1 退回意见及 V2 验收意见。教师 PC 使用现有验收管理员上下文，教师 H5 使用 counselor01，学生 PC/H5 使用 leave_student。四端分别点击返回原申请，均正确定位申请 1；申请仍是辅导员初审，没有将材料验收冒充奖助审批办结。
- 本次未重做材料写操作。V1 退回、V2 上传与验收的真实 UI 历史证据见此前记录。本次新增证据是当前四端结果回读、原单导航、登记窗口交互与接口修复。PC 登记窗口草稿测试内容已清空，未提交新缺项。
- 请假复核：独立联调运行时的四种正常身份客户端均 GET 到请假 1 的 CLOSED；只是结果复核，未新增审批动作。
- 布局检查：教师 PC 当前窗口 993×596，窄窗材料表使用内部横向滚动；手机以 390px CDP 渲染核对材料和返回入口。截图采集使用 Page.captureScreenshot，避免工具缩放合成图重复；所有临时视口设置已恢复。未据此声明 375/430px 或微信宿主全部通过。
- 展示稿：[学工核心流程-实景展示.html](../../04-UI与全端交互/ui/学工四端原型/学工核心流程-实景展示.html)，单文件内嵌四端真实截图，可切换角色并下载原图。仅材料示例，虚构身份与材料，明确 H5/微信宿主边界与奖助待评审状态，未发布。JS 语法检查通过；本地 file URL 被浏览器策略拒绝，未绕过策略，展示稿浏览器交互未实测。
- 定向测试：frontend 四个材料相关文件 24 项通过；miniapp 五个材料/奖助异常相关文件 33 项通过。新增 transport 用例先复现 1 失败，再随修复通过。定向 ESLint 通过；这些隔离测试不等同于完整四端/MySQL 闭环。
- 本批最终构建：npm --prefix frontend run build 通过（37.82 秒、21 条官网预渲染）；npm --prefix miniapp run build:h5 与 build:mp-weixin 均通过。保留既有 CSS @import 顺序/包大小警告，未顺手改动其他模块。微信宿主仍未实测。

## 2026-09-07：奖助原单退回、重提与三级评审交接

- 同一对象：FUNDING applicationId=1，batchId=1，补交 requirementId=4；保留原有材料 V1/V2、审计和申请历史。本轮实际写操作均通过正常 UI + 正式 API，没有直接修改业务状态、回拨公示时间或重新造单拼接。
- 真实顺序：08:38 辅导员 H5 退回并填写具体补正意见 → 08:39 学生 PC 原单修改说明、保存重提 → 08:40 辅导员 H5 通过 → 08:44 学院受理人 PC 通过 → 08:49 学工处受理人 PC 通过，进入 PUBLICITY。学生 PC/学生 H5/教师 H5/学工处 PC 均回读到公示状态。
- 身份证据：辅导员 counselor01、学生 leave_student、学院 leave_college01、学工处 leave_sa01，各正常角色/范围未变。学院和学工处在 localhost PC 先退出再分别登录；辅导员 H5 与学生 localhost H5 使用不同 origin 的正常会话。两个既有虚构 reviewer 账号仅有 test-hash 占位密码，初始化为本地测试可登录凭据；未改角色、权限、租户或业务数据，凭据不写本文。
- MySQL 只读回查：已核验的独立持久化联调库中，申请 1 为 PUBLICITY / version 6，无待处理评审 WorkflowTask。学院节点期间曾实查 PENDING 的受理人确为 leave_college01。此次不是使用超级管理员代替各角色。独立库以当前运行时只读核验为准，原两万人演示库未操作。
- 当前边界：公示截止 2026-09-12 08:49:12（页面本地时间）；公示工作区显示“公示期尚未结束 / 等待办理条件”，两学生端显示尚未登记发放。不能称本单奖助办结。下一步先验证公示申诉与复核，公示到期后的确认/发放保持待验，不为赶验收篡改时间。
- PC 工作区真实缺口：学院审批响应仅是摘要，原代码直接替换完整详情，申请说明变成“—”。现改为合并摘要后重新读取完整详情，学校终审后实际仍展示原补正说明。增加在途阻断、finally 解锁及已写成功/读取失败分离，避免重复提交。
- 公示入口：评审页原来仅根据 PUBLICITY 状态显示“确认公示通过”，没有展示实际期限。改为“查看公示进度”，带原批次/项目进入已有权威公示页；到期判断、申诉和确认继续走已有服务端合同，本轮不变更后端。
- 学生易用性：PC 与手机补正成功后自动打开原单详情读取实际节点，失败仍保留修改内容。新入口行为通过组件方法测试；本轮真实补正动作发生在这项自动打开改动前，不能宣称已用真实 UI 验收新回执自动弹出。教师手机精确对象入口增加“刷新进度”，实际点击后正确由学院旧快照刷新至 PUBLICITY。
- 截图：.codex-artifacts/verified-flows/funding-20260907/ 四张；移动端按 390px 并重新载入后核对，避免只改视口却保留旧 rpx 度量造成误判。临时 CDP 尺寸已恢复。图片已并入学工核心流程-实景展示.html 第二场景，共两场景八张内嵌实景图；保留材料历史采集状态说明与当前公示未结束边界。HTML JS 语法检查通过，浏览器预览仍受前次 file URL 策略阻止，未绕过。
- 定向验证：教师 PC 评审回读+公示工作区 8 passed；学生 PC/移动补正回执+详情+附件 12 passed；教师移动相关奖助/队列 11 passed，共 31 项。PC 两工程修改页定向 ESLint 通过。附件测试起初悬挂：手写组件挂载器没消费 defineProps 的 maxCount 默认值，超量上传进入未完成 Promise；补齐挂载器默认值后原断言全部通过，业务上传限额未放宽。
- 交付基线：main / f138667d7ae197b304124a75455c15434b8d8474，已有共享工作区修改保留；本轮增加 4 个业务组件的局部修复、2 个行为测试文件、附件测试挂载器修正与既有进度/展示稿更新。未提交、推送、合并或部署。后端、权限及状态机代码未修改。
- 最终构建：教师 PC npm run build 59.27 秒通过，学生 PC npm run build 3.92 秒通过，miniapp H5 与 mp-weixin 构建均通过；git diff --check 无空白错误。仍有既存 CSS 导入顺序/包大小警告，微信宿主和学生重提自动打开回执的新增 UI 动作未实测。

### 2026-09-07：谈心家校工作区精简续工

- 本轮仅修改教师 PC 前端：重点学生跟进、谈话工作台的指定记录定位、家校回执。延续已认可 UI 壳与主题，未变更后台、权限或业务状态。
- 重点学生跟进由三块数字加两组纵向清单改为“待跟进谈话 / 高风险事项”切换，同屏只展示当前工作。删除以单页数据计算的“重点学生合计”；调用已有服务端分页和过滤，谈话包含 PLANNED/SCHEDULED/FOLLOW_UP，风险使用 OPEN + HIGH_CRITICAL。页面不会再把前 200 条当完整名单；拒绝/网络错误可重试，迟到响应不能覆盖新队列。
- 跟进清单跳转携带 studentId、talkId；工作台读取指定谈话，包括不在第一页的记录，拒绝读取时清除旧选中对象。大整数 ID 保持字符串。队列和页码存入 URL，刷新/返回可恢复。
- 家校回执移除重复数字条和二次标题，保留已有真实数量筛选与登记动作，选中色改用主题变量。当前原型前后截图已在应用工具中查看：重点跟进不再上下堆叠两组空清单；家校回执首屏直接进入状态筛选。
- 验证：node --test tests/key-student-follow.test.mjs（7 项）、student-affairs-cross-domain-links.contract.test.mjs（4 项）、global-state-visibility-contract.test.mjs（3 项），共 14 项通过；后两组包含既有源码合同检查，不能当真实流程验收。3 个修改组件定向 ESLint 通过；npm run build 35.48 秒通过，21 个官网路由预渲染完成；仍有已有 CSS import 顺序及包体积警告。
- 浏览器：教师 PC 5178 现有验收管理员会话中查看两个工作区空态、点击高风险队列与家校导航；未新增或修改业务记录。有数据的谈话深链由方法测试覆盖，未进行本轮四端真实办理，不能报完整业务闭环。
- 基线 main / f138667d7ae197b304124a75455c15434b8d8474。保留原有工作区改动；本轮代码已保存，未提交、推送、合并或部署。

### 2026-09-07：公示申诉复核四端回读

- 延续原申请 1 / 申诉 2 的真实办理。开工只读 MySQL 核验申诉 SUBMITTED/version 0，UnifiedTodo 18 PENDING，受理人 leave_sa01；未重建或清空现有隔离联调库。
- 教师 H5 正常登录学工处复核角色，在 FUNDING_APPEAL_REVIEW / recordId=2 点击“不成立 · 维持公示”，录入隔离联调复核意见并提交；回读显示已复核、申诉不成立、完整复核意见，不再显示复核动作。
- 同单数据库只读复核：申诉 2 CLOSED/version 1，待办 18 DONE。学生 PC 与学生 H5 均在申请 1 的“公示申诉与复核”展示相同意见与 2026-09-07 09:14:51 时间；教师 PC 正常学工处账号在“已复核”清单显示同一结论。四端刷新/重新进入后的结果一致。
- 原申请仍 PUBLICITY，公示截止 2026-09-12 08:49:12，未登记发放；材料 V2 保持已验收。本轮证明申诉复核分支办结，不证明原奖助已获资助或已发放，不修改公示期限绕过前置条件。
- 已保存四张真实合成数据截图：.codex-artifacts/verified-flows/funding-appeal-20260907/。教师移动和学生移动为 H5 390px 稳定布局，截图前确认内容；学生 PC 截图滚动到复核意见。采集后清除移动视口覆盖。微信宿主未实测。
- 已并入既有学工核心流程-实景展示.html，新增“公示申诉与复核回执”，累计 3 个场景、12 张内嵌截图；保留测试数据和未发放边界。node --check 对实际内嵌脚本通过；HTML 浏览器预览仍受既有 file URL 策略限制，未绕过、未发布。
- 本轮无业务代码变更，未重复执行全量构建；证据是正常登录/UI 提交、四端回读、MySQL 只读核验、实际截图及展示脚本语法检查。main/f138667d7ae197b304124a75455c15434b8d8474，未提交、推送、合并或部署。
- 下一步继续第二批尚未覆盖的勤贷减补完整角色流转与异常恢复；公示未到期期间不暂停其他可独立业务。五批整体目标仍未完成。

### 2026-09-07：从断点恢复服务并补齐减免临补回执

- 用户要求从断链处续接，本轮不重走已验的公示申诉。实际核验 API 8018、教师 PC 5178、学生 PC 5198、H5 5208 均无监听，MySQL 3310 仍在；通过既有无 seed 运行脚本恢复四个服务，实际端口监听及两学生页面登录读取确认恢复。原数据不重建。旧浏览器断网页的 data URL 受浏览器策略限制，未操作该错误页；在同一 in-app 浏览器打开正式 HTTP 业务入口继续检查。
- 明确的代码断点：_fee_row 已返回 issuedAt/fulfillmentChannel/fulfillmentReference，但 ReductionStudentView.vue 与学生 reduction.vue 未展示，学生只有“办理完成”。两端新增同单结果回执，展示申请编号、当地时间、办理方式、凭证摘要；缺失信息明确未记录，临补登记发放不冒充实际到账。PC 处理意见取消单行截断。
- 同数据只读核验：独立学工库 student 1 的 FeeReduction 1 为 TEMP_AID/ISSUED，2026-2027，issued_at=2026-09-06 06:38:21 UTC，BANK_TRANSFER，保留凭证摘要。正常学生账号在 PC/H5 回读 ¥1800、当地落实时间 2026/9/6 14:38:21 和相同凭证摘要。本轮没有重放审核/发放命令或变更数据终态；不能把历史闭环当新一次四端验收。
- 截图已保存 .codex-artifacts/verified-flows/reduction-receipt-20260907/ 两张，手机390px已检查并恢复视口；并入既有实景 HTML，新增“临补落实回执（学生两端）”，累计4场景14图。明确本场景只展示学生结果修复，不伪造教师截图。HTML脚本 node --check 通过，浏览器预览仍未验。
- 修改仅学生PC与学生移动2个组件；学生PC定向 ESLint、最终构建2.77秒通过；最终 H5、mp-weixin构建均 DONE；git diff --check 无错误。微信宿主未实测。服务当前已启动，续工仍需检查进程而非凭文档认定在线。
- main/f138667d7ae197b304124a75455c15434b8d8474，文件已保存，未提交/推送/合并/部署。下一断点待查：减免/临补提交后的统一教师待办及其精确业务深链，尚不能证明该入口接通；继续沿现有权威待办体系核验，不新增平行审批。

### 2026-09-07：减免临补教师原单深链接补接

- main / checkout f138667d7ae197b304124a75455c15434b8d8474，延续已有改动。本轮修复教师 PC、教师 H5/小程序代码的原单定位；没有重建申请或变更任何申请终态。
- 查到两个实际断点：FeeReductionView 与教师 reduction 页面忽略 recordId，默认 SUBMITTED 会挡住已批准/已落实的原单；PC getFeeReductions 请求适配层也会丢弃编号。已贯通页面→API client→PC/mobile router→list_reductions 的 recordId，服务继续沿用租户、未删除和班级范围条件。编号在 JS 中保持字符串。精确定位不叠加默认待审筛选；无记录明确提示，不回落展示其他申请。
- PC 同路由切换编号重新查询，迟到响应不覆盖新对象；退出定位只移除 recordId，保留其他路由上下文。移动端返回队列使用正式 redirectTo 清除编号，刷新不会回到旧原单；处理成功后的回读仍按原申请编号。
- 实际浏览器用正常学工处账号读取保留的申请1（TEMP_AID / ISSUED / 1800），PC 与教师 H5 都显示同一条记录。再用不存在的编号999999999反查，两端均提示不存在或不在当前权限范围；这一步发现并修正了 PC 适配层漏传参数，未把早期正常单展示当作完整证据。教师 H5 返回队列后 URL 无 recordId，重新进入申请1并刷新可回读。H5 390px 检查 document scrollWidth/clientWidth 均390，采集后恢复视口。截图：.codex-artifacts/verified-flows/reduction-focus-20260907/（teacher-pc、teacher-mobile、teacher-mobile-not-found）。
- 最终 node --test frontend/tests/fee-reduction-focus.test.mjs miniapp/tests/funding-queue-recovery.test.mjs：30/30通过，含请求适配层传参、大整数、失效链接、迟到响应、重复提交和写成功后读失败恢复。PC 两个受影响文件 ESLint 通过，构建40.81秒通过；H5与mp-weixin构建DONE；三处后端修改 py_compile 通过，git diff --check 无错误。没有执行全仓测试或微信宿主实测；跨租户/班级负向回归本轮未重新执行，后端范围条件保持原有实现。
- 仅修改上述查询与两教师页面/测试，并补充本记录。既有 HTML 展示未新增场景，因为本次只证明教师原单定位，不证明新一次完整四端办理。文件已保存，未提交/推送/合并/部署。
- 下一实际缺口：submit_reduction、resubmit_reduction_self、fee_action、withdraw_reduction_self 尚未生产/同步统一待办；affairs_teacher_workbench_service 只读取 UnifiedTodo，现有 todo_route_registry 也未注册减免待办类型。不能宣布“学生提交→教师工作台自动接收”已通。后续需沿既有受理人/权限合同设计并验证待办生命周期，禁止直接套用申诉受理节点或添加零受理人池来冒充接通。

### 2026-09-07：减免临补四端待办断链接通（申请 2）

- 延续 main / checkout f138667d7ae197b304124a75455c15434b8d8474 与已有未提交改动。复用独立学工联调库 codex_leave_ui_test_0ea278f0、租户 leave-ui-test，未清理保留数据；自动化使用另一个 codex_reduction_todo_test_0ea278f0 库。
- 修复提交、退回、重提、批准、落实、撤回时的统一待办投影：与持锁申请同事务提交。受理人来自有效资助/学工岗位、真实功能权限及学生班级范围；无有效受理人整笔回滚，不创建零受理人待办。退回使用学生正式账号绑定生成补正待办；重提关闭学生待办并恢复原教师审核待办，批准转换为落实待办，终态关闭待办。通用“完成待办”不能替代业务命令。
- 教师 PC 工作台原先把业务待办折叠在审批统计之下，首屏现可直接进入真实业务对象；完整队列支持分页续载、错误重试、去重。仅学工业务显示此入口。教师 PC/移动端两种待办、学生 PC/移动端补正待办均传递字符串 recordId；学生两端只展示指定原单，失效编号不回落到其他单据。
- 真实浏览器故事：学生 PC 正常提交申请 2（TEMP_AID、2026-2027、1200），证明 File13 经实际上传与 ClamAV CLEAN 检查。学工处账号从 PC 和 H5 工作台点击待办进入原单；教师 PC 退回，学生 H5 保留原材料补正；新增学生待办后再次正常退回验证费用构成补正，学生 PC 与 H5 都从首页待办进入原单 2，学生 H5 再次重提；教师 PC 批准，教师 H5 从落实待办进入原单并登记。四端刷新后均读取同一结果。
- 最终 MySQL 回读：FeeReduction 2 = ISSUED/version6；UnifiedTodo19审核、20学生补正、21落实均 DONE；申请1原 ISSUED/version2 未变。结果登记时间 2026-09-07 10:34:18（本地），摘要明确为隔离联调登记，无真实资金划转。此轮证明临补申请的待办交接与结果登记，不证明银行实际转账。
- 便捷性：学生不再靠菜单寻找退回原单，教师不再展开折叠区寻找业务待办；补正保留已有材料，只修改需要补正的事实。补齐教师结果时间/凭证摘要、学生上传必填提示；修复学生首页空截止时间误报“逾期20703天”。
- 验证：独立 MySQL 定向 pytest 4/4（156.93秒），覆盖正常流程、退回重提、阶段待办唯一、旧待办复用、角色深链、通用完成拒绝、学生访问教师接口拒绝、无受理人回滚；前端与移动定向 node tests 43/43；受影响 PC 文件 ESLint 通过。最终教师 PC build 32.22秒、学生 PC build 3.23秒，H5/mp-weixin 均 DONE。未运行全仓测试、微信宿主或真实师生试用；未重做全范围跨租户测试。
- 截图保存在 .codex-artifacts/verified-flows/reduction-todo-core-20260907/，四端各一张；手机 H5 390px 两端均无横向溢出，临时视口已恢复。教师 PC 当前窄窗口表格需横向滚动，截图为滚动后的结果列，不能冒充宽屏布局验收通过；该紧凑窗口布局仍待精修。已有实景 HTML 新增完整临补场景，共5场景18张内嵌图，脚本 node --check 通过；HTML 浏览器预览此前受工具策略限制，本轮未绕过、未宣称已预览。
- 修改范围：减免权威命令/待办服务与路由注册、教师工作台/减免页、学生减免页/首页日期展示、对应测试和既有展示/进度记录。文件已保存，未提交、推送、合并或部署。其余原有改动保留。本故事的真实资金支付、微信宿主验收仍不在已通过证据中，原五批目标尚未全部完成。
- 下一衔接点：沿原固定提示词核验助学贷款、勤工助学提交后的教师待办与结果回读；先审计是否已接通，不重做已经完成的减免临补申请 2。

### 2026-09-07：贷款回执四端交接与原单工作区（记录 2）

- 本轮沿固定提示词第二批奖助服务继续；上一轮属于真实进展，未重做减免临补记录 2。main / checkout f138667d7ae197b304124a75455c15434b8d8474，已有共享改动保留。
- 实际断点：register_loan、loan_action、resubmit_loan_self、withdraw_loan_self 未生成统一待办；教师两端贷款列表忽略 recordId。已接通上述命令的同事务投影、统一工作台入口及四端字符串编号定位。教师查询新增 recordId，继续保留原租户、学生班级范围与软删除限制，不叠加默认待核验筛选；非法编号/不存在编号不回落到其他记录。
- 待办语义：REGISTERED/RETURNED → 学生补回执或补正；RECEIPT → 有贷款办理权限且覆盖学生班级的资助/学工受理人核验；VERIFIED → 台账确认；CONFIRMED/WITHDRAWN → 关闭。通用完成入口不能替代贷款业务命令。原减免待办服务移至 affairs_funding_todo_service.py，贷款与减免复用投影内部逻辑，各自传入权限与阶段类型；四个原减免调用点同步更新，无旧代码引用，不创建平行工作流或权限旁路。
- 教师 PC 待办直达贷款记录时使用平铺原单工作区：学生/状态、金额、银行尾号、脱敏回执、材料、意见、业务动作与最终回执；台账队列仍用表格。窄窗口原单不再横向拖动表格找动作。学生两端补正保留旧编号和材料，不将脱敏编号重新提交为明文。四端新增校内台账确认时间与“银行放款以银行结果为准”说明，移动端调整标题分行与普通/退回意见颜色。
- 保留联调库 codex_leave_ui_test_0ea278f0、租户 leave-ui-test 的真实浏览器故事：旧贷款 1（2026-2027 / CONFIRMED / v4）不改。学生 PC 提交另一学年的虚构回执，生成贷款 2（2027-2028 / 12000），附件 File14 经正式文件选择上传及真实 ClamAV CLEAN 检查。教师 PC 从首页待办22打开原单并退回；学生 H5 从首页待办23进入原单，修改测试经办行，保留回执编号与原附件重提；学生 PC 的首页待办也指向同一记录。教师 PC 核验，教师 H5 从待办24进入记录并确认台账。四端刷新读同一确认时间 2026-09-07 11:01:16（本地）。无真实贷款合同、借贷或资金划转。
- 最终 MySQL 回读：StudentLoan2 CONFIRMED/version4；UnifiedTodo22核验、23补正、24台账确认均 DONE；StudentLoan1 CONFIRMED/version4 保持。没有直接修改数据库到终态。
- 自动化在单独 codex_loan_todo_test_0ea278f0 库执行：test_student_loan_four_end_workspace、test_fee_reduction_four_end_workspace、test_affairs_funding_ext.py::test_fee_reduction_flow、test_funding_todo_route，共5项通过（190.10秒）。覆盖跨端读写、唯一回执/同学年冲突、其他学生不可见、文件权限、退回重提、待办阶段唯一/原待办恢复、旧版本409、终态拒绝撤回、教师代登记后学生补录、撤回清理，以及减免共用逻辑回归。
- 最终前端/移动定向 node tests 42/42通过：loan-focus、fee-reduction-focus、workbench-focus、funding-queue-recovery、loan-student-focus。包括大整数定位、旧响应丢弃、精确查询传参、失败保留与重试、工作区只展示服务端允许动作。受影响PC文件 ESLint通过；教师PC最终构建29.21秒、学生PC构建8.04秒，最终H5与mp-weixin构建DONE。未执行全仓测试、微信宿主实测、银行外部联调或真实师生试用。
- 四张结果实景保存在 .codex-artifacts/verified-flows/loan-todo-core-20260907/；移动H5以390px检查，无横向溢出，截图后清除视口覆盖。教师PC图为窄窗口滚动后的结果区。并入既有“学工核心流程-实景展示.html”，现6场景22张内嵌截图；node --check通过，浏览器预览先前受工具策略限制，未绕过、未声称已预览。
- 文件已保存，未commit/push/merge/deploy。全五批目标仍未完成。下一处已查明的交接缺口是勤工助学：现有申请、录用、月度考核和补贴服务尚未注册对应统一待办类型；下轮从其真实岗位负责人/办理权限、月度记录及学生结果链路开始，先查已有记录，避免重做已办结数据。

### 2026-09-07：勤工申请接续录用、协议上岗与当月考核（记录 2）

- 延续 main / checkout f138667d7ae197b304124a75455c15434b8d8474，保留已有未提交修改。继续使用独立学工联调库 codex_leave_ui_test_0ea278f0、租户 leave-ui-test；自动化另用 codex_workstudy_todo_test_0ea278f0，不清理人工办理记录。
- 真实断点：申请、录用、上岗接口已有，但不生成统一待办；教师 PC/H5 默认待审核过滤使录用后的原单从当前队列消失。申请新增 WORK_STUDY_REVIEW，录用转换 WORK_STUDY_ONBOARD，上岗/拒绝关闭、撤回取消；与原业务行锁、版本和审计同事务提交。接收人沿既有资助/学工岗位、workstudy.manage 权限和学生班级范围选取，不新增角色权限或用人部门权限政策；无有效接收人回滚，不落入零受理人池。通用完成待办不可代替录用和协议核验。当前模型无独立月度任务状态，本轮没有凭空生成周期考核任务或宣称存在银行支付能力。
- 教师 PC 与 H5 待办均定位字符串 recordId，接口保留租户、学生范围校验；聚焦记录不附加默认待审核筛选。PC 原单平铺学生、岗位、排班、申请说明、协议、动作与月度明细；上岗后继续原单登记考核。考核明细失败单独显示错误和重试，不把失败当作尚无考核。修复未录用原因原先未持久化、学生两端读取 reason 而接口返回 remark、教师 H5 读取 studentName 而接口返回 realName 的契约断点；学生结果文案不再将台账登记表述为银行到账。
- 实际 UI：普通学工处账号从教师 PC 发布虚构岗位 2（资料整理助理·待办联调）；学生 PC 自主提交申请 2。教师 PC 首页收到待办25，点击去办理准确进入申请2并正常录用；教师 H5 工作台刷新收到协议上岗待办26，点击进入同一申请并核验上岗，然后正常登记2026-09月8小时、合格、160元。教师PC刷新和教师H5回读当月明细，学生PC展开本人原申请月度明细、学生H5切换我的申请均读到相同结果。只用虚构联调材料与记录，未发生真实资金划转。
- 最终 MySQL 只读核验：WorkStudyRecord2 ONBOARD/version3/post2/subsidy160.00；UnifiedTodo25 REVIEW、26 ONBOARD 均 DONE，接收账号 leave_sa01；WorkStudyMonthly2 2026-09/8.00/PASS/160.00。旧记录1保留ONBOARD/version3、20小时/360元。办理使用正式页面与接口，没有直接改库到终态。
- 验证：独立MySQL六项用例通过（215.69秒），包含 work_study_four_end_workspace、loan/reduction 四端用例与资金待办路由回归；覆盖重复申请、其他学生不可见、学生拒绝访问教师查询、精确/不存在编号、待办阶段唯一、缺协议409、月工时冲突、考核回读、拒绝原因与撤回清理。47项前端定向测试通过（work-study-focus、loan-focus、fee-reduction-focus、funding-queue-recovery），覆盖大整数、无效编号、迟到请求、写后原单保留、月度明细失败与重试等。受影响PC ESLint通过，教师PC最终构建52.96秒、学生PC5.01秒，最终H5和mp-weixin构建DONE。后端测试随后仅删除不可达的贷款测试分支并通过语法检查，未因此重复全批MySQL测试。未跑全仓测试，未做微信宿主、银行外部服务和真实师生试用。
- 四张截图已查看并保存于 .codex-artifacts/verified-flows/workstudy-todo-core-20260907/，移动H5按390px检查后恢复视口。教师PC截图为窄窗口滚动后的结果区。并入既有“学工核心流程-实景展示.html”，现7场景26张内嵌截图，脚本 node --check 通过；HTML浏览器预览仍未验证，未绕过前轮工具策略限制。
- 文件已保存，未commit/push/merge/deploy。该轮证明申请→录用→上岗→当月考核与四端PC/H5回读；不代表全部五批已完成。后续先核对住宿状态与迎新安排之间的真实断点，复用旧联调记录，不重建已跑通的勤工、贷款和减免流程；奖助申请1仍处公示期，不能提前改成发放终态。

### 2026-09-07：迎新批次名单到5000人实际入住，修复默认迎新队列超时

- 延续 main / checkout f138667d7ae197b304124a75455c15434b8d8474。保留所有已有改动与联调记录，复用独立学工库 codex_leave_ui_test_0ea278f0、租户 leave-ui-test。自动化单独使用 codex_dorm_context_test_0ea278f0；未清理人工联调库、未提交/推送/合并/部署。
- 住宿列表新增迎新批次、行政班过滤，先在SQL中过滤和计数，再分页；批次与班级选项同样沿用住宿数据范围。迎新住宿核对传递当前批次到批量入住工作区。按当前筛选分段读取，每次200、最多5000，界面只渲染50行；名单变动、迟到响应、取消读取不会留下部分选择。选中批次完成后选项不再有待入住人时仍保留当前过滤标识，避免误显示全部批次。
- 正常教师PC在已有迎新批次5、分配计划3上选择5000人，核验合成数据到场后提交正式批量入住任务3。中途在400人后暂停，优化同一人25栋楼反复解析权限为一次解析后逐栋核对；每个人仍重新核验任务归属、楼栋范围、床位版本及权限并独立提交，未跨请求缓存授权。重启自有API后从原任务继续，没有重建或重放已成功记录。
- 实际UI最终显示已入住5000、待核对0、未处理0；刷新后任务仍SUCCESS。MySQL只读回读：任务3共5000条SUCCESS回执、uniqueItems5000、每条attempt_count最大1，完成时间2026-09-07 12:08:18本地。5000学生、5000唯一床位，DormStay ACTIVE5000、DormBed OCCUPIED5000、迎新dorm_status CHECKED_IN5000，性别冲突0。原分配200个班级/性别组合中190组同栋、150组同层、最多跨2栋，未将就近偏好误报为全部同层硬约束。
- 同批学生ORI5000-0001：教师PC回执、教师H5迎新详情、学生PC迎新页、学生H5均读取01栋101室1床已入住。H5重登使用正常学工岗位；未用超级管理员替代。该5000人数据是住宿专项虚构联调批次，仅配置住宿环节，不能当作全批现场报到/入学确认通过；原迎新report_status仍未报到，未直接改库到业务终态。
- 现场发现教师H5默认迎新队列读取5000人后逐人核验再分页，导致超时。改为默认队列先SQL计数和分页，仅核验当页；主档班级与学号一次按当前页回读。真实页面刷新后可打开30条/页、167页并进入同一学生住宿详情。派生资格筛选仍需筛选后计数，待补办大队列性能尚未收口；未声称所有筛选都已解决。学生PC将必办事项齐备和现场报到完成分开表达，避免仅完成住宿就显示整个报到已完成。
- 定向验证：独立MySQL住宿8项通过212.09秒（批次班级过滤、批量入住、楼栋范围、迎新住宿状态）；迎新默认分页与O4资格3项最终通过129.31秒。首次O4失败原因是测试流程未启用CHECKIN却申请报到码，补齐测试流程前置环节后通过，未移除校验。默认分页用例实际确认第2页只核验该页2人，越界页不触发逐人核验，总数保持5；返回真实班级。前端18项定向测试通过；受影响PC组件ESLint通过；教师PC最终构建29.07秒，学生PC5.27秒。未跑全仓测试；本轮未改小程序源码，未重复微信构建，微信宿主未实测。
- 四张结果图已查看并保存 .codex-artifacts/verified-flows/dorm-checkin-5000-20260907/。教师PC为窄窗口滚动结果区，教师H5为原生浏览器窗口（设备缩放截图偏差，未把错误缩放图收录），学生H5为390px，采集后恢复视口。并入既有学工核心流程-实景展示.html，累计8场景30图，实际内嵌脚本node --check通过。HTML浏览器预览仍未验，未绕过先前工具策略，未发布。
- 下一实际断点：迎新派生资格/待补办筛选在大批量时仍需全量核验；学生迎新摘要的班级/年级仍读取旧迎新字段，主档已有班级但页面空白。继续按现有正式服务修复，不重做5000人已入住任务。五批整体目标保持未完成。

### 2026-09-07：迎新待补办大队列与学生主档班级回填

- 本轮是有效进展：继续main/f138667d7ae197b304124a75455c15434b8d8474，修复上一轮明确的派生资格筛选超时和班级空白。保留5000条已入住回执；没有重新分配或入住，没有提交/推送/合并/生产部署。
- 资格筛选仍调用同一evaluate规则，未改成先分页后筛选。按主键每段最多200人，一次读取该段关联的冻结流程、环节状态、账号绑定、材料/文件版本/绑定证据、缴费、绿色通道、住宿和未结异常。事实仅保留在当前请求，未跨请求缓存，持久化重算拒绝使用预读事实；材料文件查询限定当前租户和有效记录。只保留目标页结果并累计真实匹配数，避免先把全校所有结果装入列表；默认队列仍先SQL分页。
- 学生迎新摘要通过稳定student_id读取同租户主档行政班及年级，同时使用实际班级查询辅导员；保留无主档时的原迎新字段。学生PC与H5正常回读均显示2026级分宿联调001班，PC年级为2026，同一01栋101室1床已入住不变。
- 实际教师H5点击待补办后正常返回名单，服务端GET /orientation/qualifications为200，耗时4430.7ms（保留5000人专项批次及少量旧记录）；不是全系统性能承诺。学生PC /portal/orientation/my为200，1022.5ms。重启期间曾出现代理500，恢复后重试成功，未把该错误视为业务空数据。
- 独立MySQL验证：默认/派生分页与O4资格最终3项通过151.41秒，205人跨200人分段，逐人比较预读与直接核验输出一致、预读evaluate不再产生逐人SQL、匹配205无重复遗漏、空页总数正确；材料事实、阻断原因、inputHash、资格结论和到校条件一致。学生主档班级投影单项通过121.61秒，明确覆盖旧迎新班级/年级不覆盖实际主档行政班。未修改前端源码，不重复跑无变化的前端全构建；后端py_compile及diff检查通过。
- 更新学生PC/H5实景图，实际查看后并入既有HTML，仍8场景30图，内嵌脚本node --check通过。本次更新的学生H5图为浏览器原生窗口，设备缩放有偏差的截图未收录，未冒充390px截图或微信宿主验收。未发布HTML，浏览器HTML预览仍未验。
- 下一条住宿故事为调宿申请→辅导员/宿管办理→原床释放与目标床占用→学生结果。初步定位服务有node_guard/reliability层覆盖，后续必须确认真实运行注册与路由，不能只按affairs_dorm_service中原始函数判断流程。第三批其他分支及其余批次、微信宿主等全目标缺口仍保留。

### 2026-09-07：调宿教师PC与移动待办原单定位

- main/f138667d7ae197b304124a75455c15434b8d8474，继续已授权住宿故事，保留全部既有修改。初步核验正式调宿读取经scope_guard与projection包装，不能仅以原始service函数判断范围。联调库只读查询尚无DormTransfer申请，本轮没有伪造一条已执行申请作为浏览器成功证据。
- 真实断点：教师PC忽略待办recordId，移动工作台仅取前100条待审并混合请求巡检/整改/门禁数据，完成后原申请会从待审队列消失。新增GET /student-affairs/dorm/transfers正整数recordId，沿原租户、宿管楼栋或辅导员班级范围在SQL分页前过滤，经所有当前包装层传递；不扩大角色权限。JS保持字符串ID，不附加待审筛选。不存在/超范围记录明确错误，不回落另一队列。
- 教师PC原单显示申请编号及返回队列，聚焦时隐藏无关工作区导航；移动深链接显示调宿原床、目标床、事由、当前服务端允许动作，跳过无关汇总与巡检请求。已执行原单仍能回读；invalid编号不发请求；迟到响应不覆盖新对象。移除移动端在allowedActions缺失时自行按状态猜审批权限的回退。两教师待办路由均注册LIST_FOCUS并保留recordId。
- 验证：test_m4_transfer_executes在独立MySQL通过，正常辅导员初审→宿管终审，原床VACANT/目标床OCCUPIED、旧Stay ENDED/新Stay ACTIVE；同一申请在处理前以辅导员精确查询、终态精确回读，失效编号返回0。该次联合4项既有scope/projection检查共5项174.78秒。补充路由聚焦合同后5项合同检查通过1.00秒；5项前端行为测试通过（大整数、终态、空结果、invalid编号、迟到响应）。PC两文件ESLint及构建通过1m2s，H5与mp-weixin构建DONE；最后仅增加PC聚焦时隐藏无关导航并通过ESLint，未因此重复全构建。微信宿主未实测。
- 浏览器普通学工岗位在PC/H5访问不存在编号999999999，两端均提示不存在或不在当前权限范围，返回调宿队列按钮可用；无伪造记录、无假接口。此负向入口验证不等于学生→辅导员→宿管的真实四端调宿全流程已完成。本轮未新增宣传场景，既有HTML保持8场景30图。
- 代码与记录已保存，未commit/push/merge/deploy。自有API当前运行PID16752（续工仍须核验），5178/5198/5208沿现有配置。下一步准备保留的普通学生、负责辅导员、目标楼栋宿管与空床前置事实，通过真实UI跑同一笔调宿，再回读四端、截图；不使用超级管理员替代审批角色、不改库到终态。完整五批目标仍未完成。

### 2026-09-07：调宿申请1真实四端办理及本人记录回读修复

- 继续main，checkout/HEAD f138667d7ae197b304124a75455c15434b8d8474，保留既有未提交修改；未提交、推送、合并或部署。无PR远端基线核验，本轮仅本地施工。
- 恢复已停止的8018/5178/5198/5208服务，仍为3310独立学工联调库codex_leave_ui_test_0ea278f0及leave-ui-test租户。补齐测试楼栋1的标准DORM_MANAGER宿管账号与负责关系，不改床位占用或流程终态。旧5000人住宿记录保留。
- 正式UI：学工处为学生1甲一办理101室2床入住；学生PC选择3床提交调宿申请1；负责辅导员counselor01在PC初审；普通宿管移动工作台收到同一待办，点击去处理直达申请1，终审后EXECUTED/version2。宿管工作台待办由1变0、今日完成1，重新登录后仍能从原单深链接回读。学生PC与学生H5读取同一2床→3床记录及住宿历史，MySQL只读核验学生1唯一ACTIVE床位3、原床2 VACANT。没有直接改库到终态或用超级管理员代替审批。
- 实际发现并修复：学生提交后显示暂无调宿申请，根因为scope_guard未识别SELF。现按可信self_student_id与租户/未删除主档共同限制SQL，保留教师班级和宿管楼栋范围。补createdAt、returnReason真实投影；学生PC显示已完成调宿和申请日期，退回原因使用真实字段；教师PC终态节点不再露出EXECUTED代码；学生H5已入住提示不再误用新生等待分配通知。
- 定向验证：扩展test_m4_transfer_executes，真实MySQL中本人PC与学生Mini身份在待审/终态均能回读，另一学生即使带studentId参数仍为空，学生无审批动作、创建时间与退回原因合同正确，原床释放及新Stay创建；联合范围合同最终6项通过136.89秒。前端原单聚焦与学生申请保护7项通过。学生PC ESLint及build通过2.54秒；教师PC、H5、mp-weixin集中构建通过。git diff --check通过。旧datetime.utcnow弃用警告仍存在，未扩成本轮后端治理。
- 四张截图均实际查看，加入既有学工核心流程-实景展示.html，现9场景34图，脚本node --check通过；未发布，HTML浏览器预览未验。两张H5图为原生浏览器窗口，不冒充375/390/430px或微信宿主实测。
- 未完成：本笔未做退回重交UI分支；移动教师端服务重启/刷新后曾返回身份入口，重新登录后业务结果保留，会话恢复原因仍待核验。学生PC提交后仍可能保留壳的已编辑标记；在真实已落库后确认离开，再刷新可正确回读。这些易用性问题未标完成。下一步优先接续移动会话恢复与学生已提交标记，再做调宿退回/冲突及正式退宿故事。五批整体目标仍未完成。

### 2026-09-07：住宿草稿保护与调宿驳回申请2

- 延续main/f138667d7ae197b304124a75455c15434b8d8474，上一轮为有效进展。本轮未改认证核心或数据库业务状态，未commit/push/merge/deploy。
- 学生PC住宿页接入既有registerWorkspaceForm：空表单不误报，选床/事由/未提交整改说明与附件保护离开；提交成功或取消清空调宿草稿后解除标记，提交中阻止离开。住宿页重新进入强制回读，避免沿用教师处理前的loadedTabs缓存。实际UI验证编辑→关闭提醒→继续编辑保留→取消后直接关闭，正式申请2提交后已编辑标记消失，关闭无重复提醒。
- 联调申请2：学生PC从当前3床申请4床，负责辅导员PC驳回，原因明确“补充调整需求后重新申请，保留原3号床”。MySQL回读申请2 REJECTED/version1，唯一ACTIVE仍3床，4床VACANT，已执行申请1不变。学生PC与H5可见未通过结果，PC显示真实returnReason；H5补上原先漏掉的未通过原因。教师调宿弹窗移除错误关联的违纪快捷用语，改为填写本次调宿原因。
- 验证：学生草稿/整改重试3项通过，教师原单5项通过；学生PC ESLint及最终build通过，教师修改文件ESLint通过，移动H5及mp-weixin构建DONE；本轮没有后端变更，不重复上轮MySQL自动化全套。真实UI及只读MySQL为本次驳回故事证据，未把驳回等同于原单退回重交。
- 四端截图保存在.codex-artifacts/verified-flows/dorm-rejected-20260907；教师移动截图首次误读上一单，已刷新核对申请2后重拍覆盖。尚未逐图视觉验收和并入官网HTML，现有展示仍9场景34图。
- 移动会话本轮刷新未复现掉登录，保留调查，不基于推测改认证。新发现：H5同一页面仅改变recordId（1→2）时地址变而页面仍显示1，刷新后正确读取2；此深链接参数切换是下一步具体修复断点。微信宿主、375/390/430px、床位竞争与正式退宿故事仍待验。全五批目标保持未完成。

### 2026-09-07：调宿链接切换与冲突集中回归

- 延续 main/f138667d7ae197b304124a75455c15434b8d8474，在原有未提交改动上续工；未提交、推送、合并或部署。
- 教师 H5 调宿办理监听同页 hash 中的 recordId 变化，清除旧记录与确认弹窗，重新加载精确原单；保留大整数字符串和迟到响应隔离，卸载时移除监听。终态显示完成结果或驳回原因与再申请提示。
- 实际浏览器验证申请 #2→#1→#2，编号、床位、结果和驳回原因同步切换，无须刷新。6 项定向前端测试通过；H5、mp-weixin 本批各构建一次通过，微信宿主未实测。
- 新增真实 MySQL 用例：驳回后建立新申请、重复申请拒绝、目标床位被另一学生正式入住后最终审批返回冲突，原床与原住宿记录保持，旧驳回单及新单版本不被错误覆盖。与正式退宿阻断/确认、占用/性别冲突用例集中运行；结果待本批日志回收后补充。
- 本次未把 API 自动化当作四端 UI 闭环；再申请与正式退宿的跨端 UI 验收、官网截图收口仍待续工。采用整批定向测试与构建，不重跑无关业务的已通过检查。
- 本批最终结果：独立 MySQL 库 codex_dorm_context_test_0ea278f0 的上述 3 项全部通过（173.08 秒），未操作保留联调库的住宿终态。运行仍有既有弃用警告，未扩成无关治理。前端 6 项及双目标构建均通过；本批相关 diff 空白检查通过。

### 2026-09-07：驳回后重新申请3四端实办

- 延续 main/f138667d7ae197b304124a75455c15434b8d8474。上一轮属于有效进展（断链修复、3项MySQL及6项前端测试）。本轮使用保留联调库 codex_leave_ui_test_0ea278f0、leave-ui-test 租户；所有办理均为正式页面动作，无数据库终态写入。
- 学生PC leave_student 查看申请2驳回原因后重新选择4床并提交申请3；负责辅导员 counselor01 在PC初审；普通宿管 transfer_dorm01 的H5工作台收到1项对应待办，经“去处理”直达申请3并终审。学生PC刷新、学生H5重进、教师PC刷新与教师H5原单均回读结果。
- 只读MySQL验证申请3 EXECUTED/version2，申请2仍REJECTED/version1，申请1仍EXECUTED/version2；学生1唯一当前床4，原3床VACANT。未将新申请等同原单退回重交，未以超级管理员替代上述办理角色。
- 四端截图均查看，学生H5定位到新旧申请与住宿历史；并入学工核心流程-实景展示.html 的“调宿驳回后重新申请”，累计10场景38图，内嵌脚本node --check通过。HTML浏览器渲染未验证、未发布；H5为浏览器原生宽度截图，不冒充微信宿主或移动尺寸验收。
- 本轮无业务代码修改，不重复上轮已通过构建与数据库自动化。联调窗口重新创建后需正常登录；未登录直接进入移动业务深链时只显示加载失败，缺少登录并返回原单的引导，作为已观察断点待修，未据此修改认证核心。
- 下一故事：正式退宿，从教师PC现住4床发起正式退宿单→负责宿管确认→学生两端与教师PC台账回读，不影响5000人迎新批次。整套住宿及五批目标仍未完成。

### 2026-09-07：正式退宿单1与移动办理入口

- 延续 main/f138667d7ae197b304124a75455c15434b8d8474，上一轮为再申请3真实四端实办进展。核验教师移动页没有正式退宿入口，新增既有宿舍工作区“退宿办理”，接入现有GET/confirm正式接口；后端状态机和权限不变。每页20条、待确认/已退宿切换、阻断原因、二次确认、版本提交、防重复点击、成功回执与重新回读。既有调宿聚焦保持可用。
- 真实办理：leave_sa01在教师PC定位4床甲一，选择转走读、填写原因并创建退宿单1；普通楼栋宿管transfer_dorm01在H5查看并确认。PC回读4床空置，学生PC与H5均回读无在住床位及住宿历史。全程使用正常UI与独立身份，未直接写库终态。
- 只读MySQL：codex_leave_ui_test_0ea278f0/leave-ui-test，退宿1 CONFIRMED/version1，关联Stay ENDED，checkoutAt 2026-09-07 05:57:14 UTC，4床VACANT且student_id为空，学生1 ACTIVE住宿数0。5000人迎新批次未作变更。
- 学生两端修复退宿后误提示等待学校分配：已有结束住宿历史、无在住/预留且未开放选床时，显示住宿已结束与历史查询/联系宿管提示；新分配和选床合同不变。
- 验证：教师移动与PC原单定向测试8项通过，学生PC最终build 2.36秒、H5与mp-weixin最终构建通过，相关diff空白检查通过。前一批独立MySQL的正式退宿阻断/确认、重复提交与床位冲突检查已通过，本轮无后端改动未重复运行。微信宿主、消息实际送达与移动375/390/430px仍未验。
- 四端截图已查看并加入学工核心流程-实景展示.html，累计11场景42图，脚本node --check通过；HTML浏览器渲染未验、未发布。当前学生PC/教师H5两张为用户窗口变化后的窄窗口截图，宣传前仍需统一尺寸与版面精修。
- 剩余：退宿从移动待办/消息精确进入、未登录深链恢复、角色动作投影与相关拒绝分支继续核验；住宿巡检整改/异常故事及后续两大批尚未全部完成。未commit/push/merge/deploy。

### 2026-09-07：退宿原单聚焦与完成后回查

- 延续main/f138667d7ae197b304124a75455c15434b8d8474；上轮为正式退宿1真实办理及四端回读进展，本轮继续补直接消费者。未提交、推送、合并或部署。
- GET退宿列表新增可选正整数recordId，权威service在原租户/楼栋/班级范围SQL内叠加ID过滤。未新增写入口或修改审批状态机。扩展已有MySQL正式退宿测试：CONFIRMED原单可回查、终态无动作、缺失ID为空、其他楼栋宿管即使带精确ID也为空。
- 教师PC支持tab=checkout&recordId=原单编号，忽略队列PENDING/学生筛选，缺失与越权范围明确报错；创建退宿后使用返回requestId直达刚创建单。bedId按字符串传递。移动同一路径按tab区分调宿/退宿，切换相同编号不同业务会清旧确认弹窗和记录，已确认原单仍能显示；返回保持退宿队列。
- 验证：12项前端定向测试通过（含真实组件路由初始化、同号跨业务切换、原单已办读取），独立MySQL正式退宿节点1项通过143.37秒，包含阻断/确认/重复提交与新增范围回查；既有弃用警告未扩治理。PC、H5、mp-weixin构建通过，相关diff空白检查通过。独立学工API8018已核验进程后重启加载本轮代码，其他中心运行环境未动。
- 本轮新原单链接浏览器交互尚未补验；不得以定向测试替代实测。上轮退宿四端实办及11场景42图保留，不重拍相同结果、不重复无关构建。待办/消息自动派发与未登录深链恢复仍待核查，不因有原单路由就标交接全通。
- 下一业务定位：教师PC DormCheckView.vue发布/登记查寝，教师H5 dorm-review巡检与复检，学生PC/H5整改；现有test_dorm_inspection_d5.py::test_d5_four_end_inspection_rectification_recheck_and_negative_gates为关联自动化入口。下一轮从真实页面查寝任务开始，沿用现有模块，不新建一套流程。

### 2026-09-07：卫生整改单1跨端退回补交与复检关闭

- 延续main/f138667d7ae197b304124a75455c15434b8d8474。上轮原单链接修复与定向验证为有效进展。本轮正式UI重新为学生1办理4床入住，建立新Stay；原退宿1及原住宿历史保留，未改5000批次。
- 学工PC leave_sa01新建日常卫生任务1、登记101室地面异常并关联甲一，生成整改1。学生PC leave_student开始整改、上传合成联调截图并提交；普通宿管H5 transfer_dorm01读取说明/附件后退回；同一学生H5查看上次提交和复检意见，重新上传并在同一单补交；学工PC查看最新说明及两次附件、保存复检通过关闭。学生PC/H5刷新均显示已关闭及最终意见，宿管H5待复检和关联异常清零。
- 只读MySQL（codex_leave_ui_test_0ea278f0、leave-ui-test）回读整改1/task1/record1/student1：CLOSED、LOW、version5、closedAt 2026-09-07 06:18:34 UTC，最新补充说明和最终复检意见均保留。全程正常UI写入，未直接置终态。联调附件为已注明用途的环境截图，不冒充学校现场照片。
- 修复PC整改工作区刷新丢位置：workspace、rectStatus、rectPage保存在路由query，回读时校验取值，保留其他上下文。实际验证已关闭筛选刷新仍在整改页。6项证据重试/路由恢复相关前端测试通过、PC构建通过、diff空白检查通过。本轮无后端变更，没有重复重建MySQL测试库或移动构建。
- 四端截图已逐张查看、加入学工核心流程-实景展示.html，累计12场景46图，脚本node --check通过，未发布。教师H5截图为关闭后的空待复检队列，不假称已办原单详情。附件预览按钮已点击，但新预览窗口内容未核验，下载/预览全链路仍待补证。微信宿主、375/390/430px、高风险升级分支未验。
- 当前窗口已markHandoff保留：PC21、学生PC22、宿管H523、学生H524，减少下轮重复开窗登录。已有学校和角色上下文仍需轻量核验。
- 观察缺口：PC已关闭整改无详情入口；宿管H5只有待复检，缺已办查询；检查模板/要求还显示技术用语，末尾联调文本存在z，保留原始事实未改历史；截止时间显示UTC片段需统一。下一步先收口已办证据回查，再推进高风险异常转风险与谈心家校故事。整个五批目标仍未完成，未commit/push/merge/deploy。

### 2026-09-07：整改终态证据回查与移动优先

- 延续 main/f138667d7ae197b304124a75455c15434b8d8474，保留共享工作区既有改动，未提交/推送/合并/部署。
- PC整改列表新增查看记录，复用原检查、整改材料、复检意见；VIEW模式隐藏提交入口并阻止写动作。教师移动端新增已关闭查询及分页，终态无复检按钮；整改页取消无关统计和误导队列数量。
- 真实页面使用原整改1（CLOSED/version5）回查，PC和普通宿管H5均看到最新补交说明、两份附件与最终意见，无办理输入。截图已查看并替换实景展示既有场景，仍为12场景46图；HTML脚本检查通过，未发布、HTML整体渲染未验。
- 本批17项定向测试通过；PC构建及最终H5、mp-weixin构建通过，相关diff空白检查通过。无后端写入或数据变更，未重复重建MySQL库。微信宿主与375/390/430px仍未实测。
- 用户最新优先级：老师、学生大部分工作在微信小程序，后续学工以移动发起/补材料→教师待办处理→学生结果回读为主线，PC承担批量与配置。H5联调、微信构建、微信宿主实测分开报告。
- 下一断点已定位：教师dorm-review的recordId分支目前除checkout外按调宿读取，尚不能将整改编号作为复检原单深链；DORM_RECTIFICATION/RECHECK待办已由服务创建，但移动路由注册与接收方聚焦还需连同学生消息action一起核验修复。未将菜单可打开等同待办交接跑通。五批总体目标未完成。

### 2026-09-07：学生整改消息与教师复检原单深链

- 延续main/f138667d7ae197b304124a75455c15434b8d8474；上一轮已完成终态回查，本轮继续修移动交接断点。未提交/推送/合并/部署。
- 教师tab=recheck&recordId通过既有受控整改详情接口读取原单，不再按同号调宿查询；终态回查、返回整改队列、切原单清旧附件/意见、迟到响应隔离。学生rectificationId原单模式直接查询本人详情，隐藏选床/调宿/归寝等无关区，保留原单提交动作及结果；无效编号不发请求，缺失或越权失败明确显示。
- 登记已有DORM_RECTIFICATION学生待办、DORM_RECTIFICATION_RECHECK教师待办，以及服务已在发送的STUDENT_AFFAIRS_DORM_RECTIFICATION消息动作键。消息适配仅对声明聚焦的路由转换参数，避免普通AFFAIRS_DORM消息被误解为整改编号；其他角色无新权限，未改状态机或详情服务范围。
- 验证：16项前端原单/切换/迟到响应/选床回归通过；后端test_funding_todo_route.py及test_mobile_action_v3_contract.py合计39项通过2.52秒，在独立测试配置运行，此处是合同测试，不冒充完整MySQL业务闭环。最终H5与mp-weixin构建通过，相关diff空白检查通过。历史注释所指action-focus-contract.test.mjs实际不存在，未记为通过；真实action-router-contract.test.mjs另行执行。
- API8018核验目标进程后重启；保留学工联调库数据。普通宿管H5与学生H5实际分别打开整改1原单，读回CLOSED与最新补交/最终复检意见；学生不再先看选床。两张实景已检查并替换HTML，仍12场景46图，脚本检查通过。截图为H5实际窗口，不是微信375/390/430px证据。
- 待补：本批实际从新待办点击、消息worker真实送达与从消息进入仍未验；未登录深链仍显示未登录，登录后原位置恢复需沿现有登录组件补通。房间级整改分派宿管的START/SUBMIT移动操作尚待补齐。微信宿主未验、全五批目标仍未完成。

### 2026-09-07：移动整改单首次未登录续办

- 延续main/f138667d7ae197b304124a75455c15434b8d8474。上轮原单聚焦为有效进展，本轮补首次未登录恢复，不改认证裁决、角色匹配和业务权限。
- 学生/教师整改原单在auth错误时显示登录后继续整改；各自登录包装页传递dormRectificationId，公共登录组件在既有角色校验、会话建立后返回对应整改入口。只接受正整数编号，拒绝外部URL、额外参数、无效编号和未知身份；没有回跳参数的登录仍进入原首页。
- 16项原单/登录路由定向测试通过；H5及mp-weixin构建通过，相关diff空白检查通过。实际新建未登录学生H5窗口：整改1→登录后继续整改→正常学生账号密码登录→自动返回整改1，回读CLOSED与补交/最终复检意见；没有手工导航替代登录回跳。终态画面与既有HTML相同，未重复增加截图。
- 教师同一回跳代码已构建及角色路由测试，教师完整未登录UI、微信登录/绑定回跳、微信宿主、会话过期触发现有全局强制退出分支尚未验；本轮不声称全部登录恢复完成。下一步仍为新整改待办及消息真实交接，房间级宿管整改操作，高风险升级链路。未提交/推送/合并/部署，五批目标未完成。

### 2026-09-07：宿管房间级整改移动办理补齐

- 延续main/f138667d7ae197b304124a75455c15434b8d8474。教师移动整改工作区补START、上传TEMP_PRIVATE证据、SUBMIT，调用原移动接口并传expectedVersion与clientRequestId；只按allowedActions显示/执行，不修改权限或状态机。失败保留材料，相同内容重试复用请求号；切对象清旧草稿，上传迟到不绑定新上下文。
- 增加未完成查询与真实状态标签；教师DORM_RECTIFICATION待办登记到tab=recheck原单，供房间级整改责任人办理。复检与整改为同页不同后端动作，不再把所有未关闭记录标待复检。
- 16项前端定向测试通过，40项后端路由/消息合同测试通过2.53秒，H5/mp-weixin构建及相关diff空白检查通过。没有重跑无关PC构建，也未直接改数据终态。
- PC正式UI以学工处受理人新建“宿管公共区域整改联调”（日常卫生，四端联调宿舍测试楼栋），列表显示进行中、0间。尚未登记问题/生成新整改，因此不得把本批记为宿管整改真实闭环。现存原整改1与5000批次未改。
- 旧PC21与宿管H523窗口已不存在，未声称仍在运行。当前PC27新任务现场已markHandoff；下一轮恢复普通宿管身份，从该新任务检查→房间级整改→上传→提交→复检继续真实UI验收，再核对待办及消息。教师新待办路由还需在下一次受控API重启加载；接口本身未改。
- 五批目标未完成；微信宿主未验；未提交/推送/合并/部署。

### 2026-09-07：宿管公共区域检查与整改2真实办结

- 延续main/f138667d7ae197b304124a75455c15434b8d8474；上轮新增移动办理能力，本轮执行真实UI。核验后重启独立API8018加载待办路由，其他中心未动。
- 普通宿管transfer_dorm01在教师H5选任务2、101室地面异常，不关联学生，提交生成房间级整改2；返回工作台看到“宿舍整改待完成：101”1项，点击去处理真实跳tab=recheck&recordId=2。开始整改→上传合成联调PNG→提交复检，页面回读WAITING_RECHECK及说明/附件。
- 学工PC leave_sa01待复检列表读取同一房间原单，核对说明/附件，保存复检通过。宿管H5刷新原单显示CLOSED与最终意见且无写动作；回工作台待我处理0、暂无待办，今日完成从3到5。
- 只读MySQL codex_leave_ui_test_0ea278f0/leave-ui-test确认task2/record2/rectification2，student_id为空、LOW、CLOSED、version3、submittedAt 2026-09-07 06:55:03 UTC、closedAt 06:55:49 UTC，整改说明及最终意见均存在。没有直接置终态、没有改变5000批次。学生PC/学生Mini在此房间级责任故事无办理动作，不能拿这条当四端学生整改验收。
- 两端截图已逐张查看并加入既有实景HTML，累计13场景48图，node --check通过，未发布、整体HTML浏览器渲染仍未验。图片为合成联调环境，非微信宿主或实际学校现场证据。
- 本轮无业务源码变更，复用上一轮16项前端、40项后端合同测试及H5/mp构建结果，不重复无关全量检查。留存PC27、宿管H528并markHandoff。
- 仍待：学生整改消息送达与消息点击原单、高风险升级到风险/谈心家校、微信宿主及移动目标尺寸验证；全五批未完成，未提交/推送/合并/部署。

### 2026-09-07：学生整改站内通知真实送达与点击原单

- 续工先只读核验独立学工库：整改1的CREATED/RETURNED/CLOSED消息Outbox31/32/33均PENDING无错误；原因是本联调环境SCHEDULER_MODE=external但未运行消息调度进程，不是学生端深链本身再次断开。
- 使用原process_pending_outbox限定上述3个ID、租户leave-ui-test，正式服务投递3条；回读全部SUCCEEDED。没有插入假消息、没有直接改消息状态、没有向真实学校或其他租户发送。
- 学生H5 leave_student首页实际出现3条校园通知；点击“宿舍整改已通过复检”直接进入整改1，显示CLOSED、最后补交说明和最终复检意见，无提交动作。本次验证的是已登录学生站内消息点击，微信订阅推送、在未完成状态从退回通知补交尚待补验。
- 为避免后续联调反复手动派发，启动本地隐藏affairs-message-worker.py，启动核验固定独立库与租户，仅领取该租户source_module=student-affairs的待发/到期重试/过期租约事件，每批20条，复用原消息服务的锁、重试、去重与投递；不改生产调度配置、不处理教务/实习其他环境。运行路径在.codex-temp，日志不输出payload/凭据。
- 本轮没有业务源码改动，没有重复构建。学生终态画面与13场景48图中的既有记录相同，未为凑数重复加截图。学生H529继续保留。五批未完成，未提交/推送/合并/部署。

### 2026-09-07：高风险移动处置接口版本断点修复

- 继续同一main基线。审计发现小程序affairsContractApi.processRisk/closeRisk已发送version，但mobile.py未传给_mobile_teacher_service_impl，后者调用权威风险process/close时也未带expected_version；缺少版本的真实写入会被现有乐观锁阻断。
- 仅补充移动router→教师适配服务→权威风险服务的版本参数传递。缺省None、0及有效版本原样透传，不自行读取最新版本覆盖、不改变权限/状态机/锁规则。
- 新test_mobile_risk_version_transport.py六项参数化合同测试通过1.17秒，覆盖处置/关闭及None/0/7，验证实际router函数经适配层到权威调用的参数；权威服务在此测试中隔离替身，不能冒充MySQL实际风险写入。相关diff空白检查通过，无前端变化未跑无关构建。
- 下一缺口：teacher/risk-students当前是风险学生聚合列表，处理按钮只去学生详情，RISK_HANDLE也未原单聚焦；需要复用真正风险详情与动作合同建立移动处置工作区，再用宿舍高风险检查关联单实办。宿舍高风险当前规则分派楼栋宿管，不擅自改成辅导员政策。五批未完成。
- 本轮修改已保存，尚未重启API加载本次版本透传；未提交/推送/合并/部署。

### 2026-09-07：教师风险待办原单工作区

- 风险学生页保留无recordId时的既有范围分页；带recordId则请求既有移动风险详情，展示学生、问题、当前状态、责任人、办理期限及真实handles历史，RISK_HANDLE路由声明精确对象聚焦。已关闭也能回查，不按学生编号误取风险。
- PROCESS/CLOSE按钮按后端allowedActions，传当前版本，关闭前明确确认；失败保留说明，不自动拉新版本覆盖冲突。成功通过详情回读状态与历史。角色切对象清输入，晚响应不覆盖新原单。未接通的转办/升级/接管明确提示PC，不冒充移动完整流程。
- 4项组件行为测试通过123ms，含精确读取、权限动作、冲突保留、关闭回读、非法编号；7项后端版本/路由合同测试通过1.09秒；H5/mp-weixin构建与相关diff空白检查通过。测试不替代真实风险MySQL写入/移动UI办理。
- 下一步在真实宿舍高风险检查中生成关联风险、从普通宿管待办办理，再核验升级与谈心家校交接。当前新增原单尚未浏览器实测，API本轮版本/路由变更仍需受控重启后联调；不标此故事完成。main原基线，代码已保存，未提交/推送/合并/部署，五批未完成。

### 2026-09-07：宿舍高风险检查、学生整改、复检与风险结案四端闭环

- 延续 main/f138667d7ae197b304124a75455c15434b8d8474，保留共享工作区已有修改；复用独立学工联调库与测试租户，未直接修改业务表到终态，未提交、推送、合并或部署。
- 教师PC新建高风险用电检查任务3；普通楼栋宿管在教师H5对101室逐项检查，关联学生甲一并上传合成联调图片，生成检查记录3、整改单3及DORM高风险1。宿管从移动工作台的RISK_HANDLE待办进入风险1，先登记现场断电处置；学生H5从整改单3开始整改、上传材料并提交；宿管H5核对材料、上传复检图片并通过复检；随后回到同一风险1填写结论并关闭。
- 四端最终回读同一业务对象：教师PC风险详情显示风险1已关闭、version2及两条处置时间线；教师H5显示处置和关闭记录；学生H5显示整改单3已关闭、本人说明和复检意见；学生PC“我的检查整改”显示相同任务、问题、截止时间与已关闭结果。附件均为合成联调材料，不冒充学校现场照片。
- 修复真实阻断：移动端版本原先未传到权威风险服务；RISK_HANDLE未定位原单；宿管虽是DORM风险明确责任人，仍被班级范围和通用风险权限两层拒绝。现仅允许持宿舍检查权限的DORM_MANAGER读取、处置和关闭明确指派给本人的DORM单条风险；其他宿管、其他来源风险和通用风险列表权限均不放开。服务端allowedActions与写状态机共用规则，关闭前仍要求已有处置记录和正确version。
- 集中验证：移动端原单、失败保留和登录回跳8项通过；真实MySQL D5检查→学生整改→宿管复检→风险处置/关闭及另一宿管403，加角色权限矩阵在联合运行中通过，唯一失败是新辅助测试使用不完整假DB；移除该重复负向断言后版本/权限单元测试9项通过。H5与mp-weixin最终构建DONE，教师PC新增窗口重新获得焦点自动回读移动办理结果并通过定向ESLint。未重复运行无变化的学生PC或教师PC全构建。
- 四张结果图已逐张查看，保存在 `.codex-artifacts/verified-flows/dorm-high-risk-20260907/`。移动截图来自H5实际页面；微信宿主、微信订阅推送和375/390/430px尺寸仍未实测。宣传HTML暂不为单一故事重复改写6MB内嵌文件，待下一组故事完成后批量并入并统一预览。
- 宿舍高风险业务故事已闭环；全部学工五批仍未完成。下一轮进入心理关注、谈心家校与违纪处置，先审计现有状态机、消息/待办与四端正式入口，再选择能由移动端发起和接续的真实故事连续施工。

### 2026-09-07：谈心谈话转家校联系四端闭环

- 延续 main/f138667d7ae197b304124a75455c15434b8d8474，复用独立学工联调库与测试租户。教师 PC 为学生1（A001）创建并记录谈话1，填写真实谈话内容、结果与待回访状态，再以不少于5字的业务说明转为家校联系1；全部办理经正式页面、接口和服务写入，没有直接修改数据库终态。
- 教师 H5 通过 talkId=1 精确回读原谈话内容与结果，从“继续家校联系”直达 contactId=1 并看到对应联系记录；教师 PC 也能从谈话原单进入同一家校联系。学生 H5 与学生 PC 均通过 recordId=1 聚焦同一谈话，只展示本人可见的安全摘要、结果和“待回访”状态，不返回教师内部备注或家校联系隐私。
- 修复联调发现的真实阻断：教师 PC 转家校原先没有原因输入而被后端拒绝；移动教师适配层丢失 expectedVersion；学生移动状态组件把中文标签误当状态枚举；四端缺少谈话及家校联系原单深链。教师移动动作现只依据服务端 allowedActions，不再用前端兜底动作扩展状态机。
- 集中验证：独立 MySQL 中谈话与家校联系11项后端测试通过；教师 PC 12项定向测试、ESLint和构建通过；学生 PC 2项定向测试、ESLint和构建通过；移动端4项谈心家校测试及关联合同测试通过，H5与mp-weixin构建通过。浏览器已按谈话1、家校联系1在四端逐一回读。构建仍有既有CSS导入顺序和大分块警告，未扩成无关治理。
- 为真实角色验收，仅在该独立联调库补充了具有A班范围的合成辅导员账号；未记录凭据、未修改生产身份。移动浏览器验证属于H5，微信开发者工具宿主、微信通知投递及375/390/430px专项尺寸仍未实测。
- 本批相关差异空白检查通过，仅有仓库换行提示。代码与联调数据已保存，未提交、推送、合并或部署。宿舍高风险和谈心转家校两条故事已闭环；心理关注、违纪处置及其余学工流程继续按完整批次施工，全部五批仍未完成。

### 2026-09-07：学生心理自评到人工关注四端闭环

- 学生1（A001）分别从学生PC和学生H5读取同一份自评问卷，在学生H5提交自评并主动选择老师联系；服务端形成自评1和人工关注转介1。重复触发时复用本人未关闭转介，不生成并行重复关注单。
- 实际承办心理老师在教师H5从本人待办进入转介1，先登记跟进再关闭；教师PC心理关注原单、学生PC历史和学生H5历史均回读同一关闭结果。学生端仅展示本人自评和人工关注状态，不暴露教师内部处置内容或诊断结论。
- 修复了心理教师角色在移动身份映射中缺失、跟进/升级/关闭版本号未传入权威服务、页面重复卡片和安全结果表达等断点。定向后端24项、教师PC11项、学生PC4项、移动14项通过；两套PC构建及H5/mp-weixin构建通过。四端截图位于 `.codex-artifacts/verified-flows/mental-self-help-20260907/`。
- 本次移动验收仍为H5，微信开发者工具宿主和真实师生试用未验。联调账号与数据只存在独立学工库；未提交、推送、合并或部署。

### 2026-09-07：处分决定、申诉与解除四端闭环

- 教师PC登记处分1并提交，学院与学工处实际责任人分别完成审核，警告处分生效并登记直接送达；学生H5对同一案件提交申诉1，学工处实际承办人在教师H5复核为维持。教师PC、学生PC与学生H5均回读同一申诉理由、复核意见和结果，统一待办由PENDING转DONE。
- 维持结果后，教师PC发起解除申请1；负责辅导员、学院受理人、学工处受理人依次在教师H5完成三级审批。最终处分1为REMOVED/version6，解除申请APPROVED，三个工作流任务均APPROVED；学生PC和学生H5显示生效日、解除日及“处分已解除”，教师PC原单进入只读终态。
- 实际联调发现并修复两个阻断：移动解除页未识别服务端REMOVE_APPROVE/REMOVE_REJECT动作；移动处分审批虽收到version却未传给权威服务。另修复学生端只查询EFFECTIVE导致解除后历史消失，现保留REMOVED结果且不再提供申诉动作。
- 集中验证：版本传递与学生已解除结果3项后端定向测试通过，学生PC目标文件ESLint和生产构建通过，移动H5与mp-weixin构建通过；移动工程当前没有可用eslint.config，故没有把其定向ESLint失败冒充代码失败。处分申诉和处分解除四端截图分别位于 `.codex-artifacts/verified-flows/discipline-appeal-20260907/` 与 `.codex-artifacts/verified-flows/discipline-remove-20260907/`。
- 第四批已有宿舍高风险处置、谈心转家校、心理自评转人工关注、处分决定申诉解除四条真实闭环。微信宿主、订阅消息和目标尺寸专项仍待最终验收；全五批未完成，下一步进入第五批学生档案与班级关系的首条真实流程。未提交、推送、合并或部署。

### 2026-09-07：学生档案材料缺项、退回重交与验收四端闭环

- 延续 `main` / `f138667d7ae197b304124a75455c15434b8d8474`，在学生主档仍由学校统一维护的边界内，将既有权威材料中心扩展到 `PROFILE` 业务；没有开放学生修改姓名、学号、班级或学籍状态，也没有另建平行材料表。
- 教师 PC 从学生1（A001）的学生360直接登记“个人档案信息页”缺项；学生 PC 从“我的档案”进入同一材料记录并上传第一版；教师 H5 从真实待办进入记录5并退回；学生 H5 查看退回原因、上传第二版；教师 PC 核对后验收。学生 PC 与学生 H5 刷新均显示已验收、两版历史和两次审核意见。
- 联调修复了档案入口仍要求老师手填业务编号、四端不识别 `PROFILE` 语义、移动页把档案材料误写成请假申请的问题。教师/学生移动页现显示“学生个人档案材料”，并返回各自学生档案；教师 PC 精确上下文仍由服务端解析学生与业务记录。
- 真实 MySQL 只读回读：材料记录5为 `PROFILE/#1`、`ACCEPTED/version4`，V1为历史退回版本、V2为验收版本，关联待办全部 `DONE`，同一动作键产生3条站内消息。全部业务写入均来自正式页面和服务，数据库只用于前置账号准备与最终只读核对。
- 验证：后端真实 MySQL 流程测试1项通过；教师 PC 相关节点测试9项、移动相关测试最终8项通过；教师 PC、学生 PC、H5、mp-weixin构建通过，目标文件 ESLint 与差异空白检查通过。构建仍有既有 CSS 导入顺序及大分块警告，本轮没有扩成无关治理。
- 四张终态截图保存在 `.codex-artifacts/verified-flows/profile-material-20260907/`。移动浏览器验证属于 H5，微信开发者工具宿主与真实订阅通知仍未验。下一条进入班级—主辅导员责任关系、移动“我的班级/我的学生”及学生本人班级信息回读。第五批及五批总目标仍未全部完成；未提交、推送、合并或部署。

### 2026-09-07：班级主辅导员交接与四端责任范围闭环

- 延续 `main` / `f138667d7ae197b304124a75455c15434b8d8474`，复用独立学工联调库与测试租户。教师 PC 在责任台账将 A 班主辅导员由王莉交接给李老师，保留原主责结束记录及现有协同辅导员；成功回执明确新辅导员已接管学生、待办与未关闭风险。
- 教师 H5 使用交接后的真实辅导员身份读取“我的班级”，同时看到 A、B 两班；从 A 班下钻“我的学生”只返回甲一（A001）。学生 PC 与学生 H5 的本人档案均从同一班级责任关系回读“辅导员 李老师”，没有在四端分别维护姓名。
- 修复了生产登录令牌使用 `db-<id>` 时教师移动端无法识别本人、协同/临时代班误显示为未知类型且可错误发起主责交接、学生两端档案缺责任辅导员、H5 中文班级名双重编码及学籍正常状态显示“状态待确认”等阻断。交接接口现只允许活动主辅导员作为来源，CO/TEMP 不能替换主责。
- 真实 MySQL 四端投影测试1项通过；CO 越权交接与生产教师编号解析2项通过；教师 PC、学生 PC、移动端定向测试共4项通过，相关差异空白检查通过。测试库使用独立 `codex_counselor_flow_test`，浏览器联调仍使用保留数据的独立学工库。
- 四端截图位于 `.codex-artifacts/verified-flows/counselor-handover-20260907/`。为实际角色浏览器验收临时重置的合成联调账号口令均已恢复，临时哈希备份和辅助脚本已删除。移动端证据为 H5；微信宿主与真实消息接管仍待最终验收。
- 班级主责交接故事已闭环，第五批继续处理迎新、活动成长、档案归档和工作台交接；五批总目标仍未完成。未提交、推送、合并或部署。

### 2026-09-07：迎新材料核验与活动成长教师移动闭环

- 迎新主故事不重复重跑，继续复用既有同一新生完成身份激活、到校核验、材料审核、住宿入住、学院确认与批次关闭的四端记录。本轮修复教师 PC 与教师 H5 材料审核仍缺真实文件内容的问题：迎新材料列表按迎新学生精确过滤，批量关联文件中心权威视图，展示真实文件、版本、扫描状态、预览/下载权限及退回原因；教师 H5 在学生办理抽屉内直接审核当前材料版本。
- 活动成长审计确认学生 PC、学生 H5 已有报名、动态码签到、成绩单和积分申诉，教师 PC 已有活动流转、名单确认及积分台账；缺口是教师 H5 只能发签到码，无法完成现场业务。新增“活动现场”独立工作区，按待处理/进行中/已完成查看活动，读取参与名单与签到人数，并依序办理报名截止、开始、生成动态码、结束、确认名单与生成积分。通用学工待办页仅保留入口，移除重复的签到区块。
- 新移动接口全部复用 `affairs_activity_service`，按 `activity.view/publish/confirm` 分权并进入教师移动权限注册表；写操作携带页面当前 `version`，服务端继续校验租户、数据范围、状态机和乐观锁。确认只为已签到学生生成正式积分，零签到时页面不允许误确认；撤销确认等低频高风险动作继续保留教师 PC。
- 集中验证：迎新材料后端真实 MySQL 文件权限节点1项通过，教师 PC 与移动端定向测试10项通过；活动模块独立 MySQL 7项通过（含移动流转、名单、积分、撤销重算、统计与台账），移动页面定向2项通过；H5 与 mp-weixin 构建均完成，相关差异空白检查通过。活动页浏览器已实际渲染到登录失效态，因合成教师账号此前已触发临时锁定，本轮没有绕过认证或修改口令，故不把登录后的页面画面计作实测证据。
- 本轮没有改变活动积分政策、迎新状态机或数据库结构；没有直接修改业务数据到终态。移动证据仍以 H5/构建为主，微信宿主、真机文件预览与真实教师试用未验。代码已保存于共享 `main` 工作区，未提交、推送、合并或部署；第五批下一步进入统计归档与今日工作汇总的跨模块交接。

### 2026-09-07：五批核心代码收口、统计归档与统一工作台交接

- 学工工作台的常用入口拆为“学工统计”和“学工归档”，分别使用 `studentAffairs.stats.view`、`studentAffairs.archive.view`，不再把统计归档错误指向材料运营。归档批次和档案包台账用同一 `batchId` 深链联动，刷新、切换批次和返回时保留业务上下文。
- 统一收口宿舍分配与退宿查询：发布前明确提示学生范围和精确床位池冻结；后台发布任务继续使用权威任务状态轮询；无原单编号时不再发送空 `recordId`。三个教师 PC 页面未知枚举改为中性中文，不把后端英文值直接展示给老师。学生 PC 将离校保持为独立正式入口，迎新页不再保留不可见的伪兼容按钮。
- 学工表面审计脚本按运行时权限构造方式识别迎新权限，并按贷款、勤工、减免的服务端语义动作检查失败关闭；直接执行审计退出 0。后端选定套件首次 51 项中仅 2 条旧静态断言失败，修正断言后两条复跑通过；归档真实 MySQL 流程、路由挂载、四端终态守卫、权限运行时和资金待办均在该轮通过。
- 最终集中检查：教师 PC 全量 **943/943**、ESLint、生产构建和导航权威清单检查通过；学生 PC 全量 **176/176**、ESLint和生产构建通过；小程序 H5 与 mp-weixin 构建均完成。小程序测试 **387/388**，唯一失败是落盘发布交接文件仍固定 149 条旧路由，而当前源码已有 151 条路由（新增教师迎新待办与活动现场）；工作区尚未形成真实候选提交，因此没有手工改发布指纹冒充交接通过。
- 当前结论是“五批核心代码施工可以收口”，不是“五批已经发布验收完成”。尚缺：将共享 `main` 的大批未提交改动安全整理为候选提交；在候选提交上重建并验证小程序 handoff；微信开发者工具与真机、375/390/430px、订阅消息和真实师生试用；第二批真实资金外部回执。教师 PC 构建仍提示已有大分块，未扩成本轮无关性能重构。
- 收口基线仍为 `main` / `f138667d7ae197b304124a75455c15434b8d8474`；本轮未提交、推送、合并、部署，也未修改数据库业务终态。当前本地 `main` 与 `origin/main` 存在双向差异，进入提交前必须先识别并保护同工作区其他中心的并行改动。

### 2026-09-08：近期学工四端代码收口复审与本地保存

- 范围：近期学工四端及迎新住宿、工作台、大屏的现有变更。沿用共享 `main`，没有重置、清理或覆盖其他中心改动，没有修改生产数据、提交、推送、合并或部署。当前 checkout/head 仍为 `f138667d7ae197b304124a75455c15434b8d8474`，以下是未提交工作区证据，不是该 commit 的发布证明。
- 修复两块大屏共用加载逻辑：切身份/范围时及时清空旧统计并启动新请求；忽略迟到响应；权限信息缺失不发统计请求；部分接口失败清除对应旧数字；下钻前重新核验身份与范围，网络失败可恢复。修正“逾期未销假”指标错误的路由参数，浏览器实际从大屏指标进入带 `status=OVERDUE` 的请假台账，筛选显示“已逾期”。
- 大屏视觉收尾：区块标题改为独立 Vue 组件，修复原动态组件子节点未命中 scoped 样式造成的标题挤压；零值柱条不再人为显示长度，无效比例不再画成完整橙色环；当前预警读取未关闭风险，零待办标“暂无”；指标弹窗随最新快照更新。保留已确认的蓝色大屏及现有 UI 壳，没有把示意校园当作真实位置数据。
- 后端发现并修复同一段错位筛选代码：住宿列表引用未定义的 `orientation_student_id` 导致实际 500；对应新生过滤本应放在材料列表，遗漏会使审核工作区出现同批其他学生材料。移除住宿中的错位代码，在材料查询的分页和计数之前按迎新学生 ID 过滤，保留批次、租户和范围限制。
- 回归中的两项旧测试问题：统计聚合从 `cockpit_view` 提取到三个辅助函数后，原静态断言仍检查旧位置，现逐个覆盖实际聚合函数；辅导员真实 `db-` 账号测试缺少有效岗位，现补齐测试库角色关系，不放宽生产认证。原有交接前后班级范围、学生 PC/移动责任人回读断言全部保留。
- 验证：教师 PC 最终全量 **955/955**、ESLint、生产构建通过（37.20 秒，21 条预渲染路由）；学生 PC **176/176**、ESLint、构建通过；H5 与 mp-weixin 构建通过。小程序仍 **387/388**，唯一失败为发布 handoff 路由指纹与新增页面不一致，未更改冻结文件或弱化断言。
- 后端首轮独立 MySQL **85/88**，其中 2000 人和 5000 人分配发布均通过：学生与床位不重复、按性别分楼、同班同层、发布只预留不冒充入住。修复后相关三个测试文件 **34/34** 通过，包含辅导员交接与迎新预留→入住→退宿回读。新增材料按学生隔离回归结果另见本节补记。仅新建 `codex_affairs_closeout_test_*` 隔离测试库，未清理 `codex_leave_ui_test_0ea278f0` 人工联调库。此轮使用快速模型建表，不冒充 Alembic 全迁移验收。
- 入口与结构核对：导航合同清单检查通过，**489** 个生产可见入口与路由对应；学工表面审计退出 0，缺失权限/角色清单为空。`git diff --check` 无空白错误；原有换行提示、CSS 导入顺序、大分块和 Python 弃用警告没有包装成业务通过证据。
- 保存：源码继续保留在当前工作区，另生成桌面 `Codex代码备份/学工收口-*` 本地 ZIP，包含相关四端、后端、共享契约、测试、脚本与文档的当前文件（含新增未提交源码），逐文件 SHA-256 与 ZIP CRC 复核；不含环境凭据、数据库、依赖及构建产物。准确快照位置和校验结果写入 `.codex-artifacts/closeout-20260908/backup-result.json`，检查日志位于同目录。恢复应先解压到独立目录比对，不能直接覆盖正在施工的工程。
- 边界：本轮是集中复审修复与可恢复保存，不是全系统发布验收。仍需候选提交上的小程序 handoff 重建，以及微信宿主/真机、订阅消息、真实外部资金回执和师生试用；本轮未重新从头操作所有四端 UI 故事。
- 材料隔离补记：新增真实 MySQL 回归 **1/1** 通过，两名新生分别精确返回自身材料，分页总数为 1，冲突批次返回 0；完整初轮日志为 `backend-tests.log`，34 项复跑退出码记录为 `backend-recheck-34-result.json`，最终新增回归日志为 `backend-recheck.log`。
