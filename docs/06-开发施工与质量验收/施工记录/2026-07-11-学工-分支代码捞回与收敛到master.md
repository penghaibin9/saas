# 学工中心 · 两条 WIP 分支代码捞回 + 收敛到 master + 分支归档

> 日期：2026-07-11 ｜ 分支：master ｜ 背景：甲方拍板「都捞过来、删掉没用的、分支不要了、学工在 master 继续」

## 1. 背景

存在两条并行学工 WIP 分支（各自 git worktree 文件夹），与 master 割裂：
- `student-affairs-ui-refactor`（9 提交，领先 merge-base；落后 master 47）：干净菜单标签 + §42 规划占位页 + campus-service 页面双栏改造。
- `student-affairs-b`（3 提交；落后 master 64）：一整套 studentAffairs 前端（看板/画像/班级/宿舍/请假/风险）。

甲方只能看 PC 前端判断进度，要求把两条分支有价值的真实代码全部捞到 master，删掉被取代的，分支归档不再保留。

## 2. 捞回三阶段（每阶段 build/lint + 浏览器真机验证 + 独立提交）

| 阶段 | 内容 | 来源 | commit | 验证 |
|---|---|---|---|---|
| A | **§42 规划占位页**：灰色 planned 叶可点进公共占位页(navPlan+施工图信息，无假数据) | ui-refactor | `d??`(feat nav §42) | 真机点困难认定·认定批次灰叶→占位页正确渲染(清单+施工卡+推进指令) |
| B | **campus-service 双栏页改造**：请假审批/奖助/宿舍/违纪/工单 + 违纪详情/住宿详情独立页 + 学生身份核验/信息更正/列表/详情/编辑/学籍状态 + SplitWorkspace/routeState 组件 | ui-refactor | `feat campus-service 双栏` | 真机请假审批双栏版渲染11条真实数据 |
| C | **学工看板/学生画像/宿舍/风险页**（master 缺的4块）+ studentAffairsApi + xlsxDownload；接 /admin/student-affairs/* | student-affairs-b | `feat 捞回看板/画像/宿舍/风险` | 真机风险预警页渲染真实风险数据+来源/等级/状态筛选+详情/分派/处置动作 |

**捞回同时对齐菜单**：master navPlan 学工中心「班级与辅导员」(班级列表/画像/材料/辅导员考评) + 「请假销假」(请假审批/销假与续假/请假台账/请假统计) 8 个三级全部点亮跑通(commit 2b73a6d，前一轮)。

## 3. 跳过（被 master 取代，不捞，避免重复建设）

- student-affairs-b 的 班级(ClassList/Detail)/请假(Leave) 页：master 已有更新、对齐真实端点的正式模块(views/admin/student-affairs/class|leave/* + campus-service 路由)。
- ui-refactor 的 adminMenu.js 二级标签微调：核心菜单已由 master navPlan 手工对齐。
- ui-refactor 改的 2 个旧路径 docs：已被 master 文档九大类重排取代。

## 4. 分支收敛

1. **归档为 tag**（提交永久保留、可随时找回）：`archive/student-affairs-b`、`archive/student-affairs-ui-refactor`。
2. 两个 worktree 文件夹（桌面 student-affairs-b / student-affairs-ui-refactor）均 CLEAN(无未提交)，已 `git worktree remove --force`。
3. 删除分支 `student-affairs-b`(d6b60c7) / `student-affairs-ui-refactor`(159cb5f)。
4. **master 从此为学工唯一主线**，后续学工模块均在 master 继续。

## 5. 检查结果

- 每阶段 `npm run build` ✓ / `npm run lint` 0 error(2 既有 warning)。
- 浏览器真机(localhost:5188 master 版 + admin 登录)：§42 占位页、请假审批双栏、风险预警页、8 个点亮三级——全部验证渲染/可点/接真实后端。

## 6. 风险 / 欠账

见 `历史欠账.md` 同日条目：student-affairs-b 捞回页(看板/画像/宿舍/风险)落后 master 64 提交，个别字段需与现行后端对齐（B包·第3步 宿舍/风险 正式开发时收口，届时以捞回页为起点）；studentAffairs 模块与 campus-service 存在过渡期并存，后续统一。
