# 教务核心业务四端落地进度

## 本次范围与基线

- 用户最新决定：复用同一套公共 UI 壳；教务菜单可归并、更名。按固定提示词执行八批四端 UI 重构与核心业务、前后端代码修复，一次只开一批；每批先出完整调用链，业务闭环并测试后再开下一批。此决定取代下文历史记录中的菜单冻结、前端限定与旧后续顺序。
- 工作区：`C:/Users/10850/Desktop/职校学生全生命周期系统-academic-affairs-ui-20260905`。
- 分支：`codex/academic-affairs-ui-20260905`；基线：`6810bd6284b4d5c69a78947560f808cd7738327e`。
- 教师直接沿用 `frontend/src/layouts/BasePortalLayout.vue`；学生沿用源学工工作区的 `student-portal/src/layouts/PortalLayout.vue` 及主题/导航组件原样快照。复用清单见 [2026-09-05-教务工作区公共壳复用清单.json](./2026-09-05-教务工作区公共壳复用清单.json)。公共壳不是本分支另行设计的教务壳。
- 只转入公共壳及教务资料，未转入原工作区正在进行的学工请假、后端、小程序等业务改动。原工作区未切分支、未暂存、未提交、未重置。
- 两套 PC 的依赖锁文件与来源一致，当前通过本地目录联接复用已安装依赖；未安装或升级依赖。合并前须核对公共壳主维护分支的后续改动。
- GPT 网页版项目库尚未读取，本记录不声称与其中全部设计对齐。依据当前源码与已确认 UI 决定，保留学校政策、正式状态语义和兼容入口，修复已核实的权限、流程与数据一致性缺口；必要增量接口与迁移分别记录验证。

## 八批总进度（2026-09-06 更新）

| 批次 | 范围 | 当前状态 |
|---|---|---|
| 1 开学条件 | 学年学期、校历节次、学院专业班级、学籍、注册 | 进行中：学期/校历/节次已验证多项真实流程；组织模块推进至转班、状态、删除保护、专业方向、教学秘书绑定及学院/专业维护事务与学院更名范围闭环，尚有组织、学籍、注册和四端运行欠账；本批整体未验收 |
| 2 培养与计划 | 课程库、培养方案、教学计划 | 待开启 |
| 3 开课与课表 | 教学任务、教学资源、排课、课表 | 待开启；已有课表 UI 改动及历史验证，不能抵作本批验收 |
| 4 选课与变更 | 选课、调停课 | 待开启 |
| 5 日常教学 | 考勤、教材、教学评价、教学质量 | 待开启 |
| 6 考试成绩 | 考务、成绩、审核发布更正、补缓重免 | 待开启 |
| 7 学业变化 | 分流、学籍异动、预警 | 待开启 |
| 8 毕业收口 | 毕业审核、归档、统计、看板 | 待开启 |

菜单最新决定：用户澄清原有 29 项是二级业务模块。撤回 29 → 9 的整组压缩，不预设 14 等目标数量；当前保留 29 个模块身份、次序与入口，先精简冗长三级名称并保留旧名搜索。263 是既有菜单入口数，含同页页签和重复目的地，不能称为 263 个独立页面。是否合并模块须先证明业务职责重叠，不能只按数量归并。

导航审计：发现 14 个旧入口漏配权限；其中「发起异动」路由使用目录中不存在的 statusChange.manage，已核对正式 status_change_router.py::status_change_submit 和 legacy._SC_APPLY，统一为已有 statusChange.apply。其余按现有路由补齐。搜索补验 permissionAny；全部入口、状态和能力标识保留。最新 29 模块方案导航回归 29 项、502 个入口路由一致性、ESLint 和教师构建均通过。学期相关真实 MySQL 基线 21 项通过；第一批完整业务闭环尚未验收。详细入口、调用链和问题见 [第一批开学条件-调用链审计.md](./第一批开学条件-调用链审计.md)。

## 第一批最新续工：组织引用核对（2026-09-06）

核实09卡的真实职责是组织变更前引用检查，现已在原组织树入口接入学院、专业、行政班的分页核对及详情。覆盖在籍与未分班学生、未归档任务、PUBLISHED/ENABLED/FROZEN培养方案、有效下级和学院任务批次；历史引用单独提示，不宣称可以直接删除。复用org.view与现有数据范围；班级授权不能读取整个上级学院/专业计数。每次核对按实体类型记录审计，不修改组织、学籍或任职。

本轮 **19项后端、52项前端通过**；502个导航入口路由一致性与共享导航契约通过，ESLint通过；最终教师构建 **36.27秒、21条预渲染路由**。普通管理员真实登录后，总览显示6个组织，班级20名在籍学生/2条未归档任务，学院与专业各21名/2条；详情及跳回对应行政班查询通过。只读回查既有学院版本5、名称范围版本2以及学生、学籍事实、任职和任务快照保持不变。详细证据见调用链审计末尾。

保留29个二级模块；仅“组织结构同步”显示名改为“组织核对”，旧名搜索与原URL兼容。本轮完成独立核对能力，学院/专业停用写入口的统一治理、组织统计、学籍/注册和四端剩余验证仍待继续，第一批未整体验收，后七批未开启。

## 第一批续工记录：学院基础维护（2026-09-06）

学院新建、编辑和软删除的主数据、版本及两类审计同事务；学院更名时历史名称授权保持原学院范围，重名歧义拒绝保存，稳定ID授权、学籍和任职保持原值。表单字段长度对齐数据库，并区分可修改的名称歧义与需重开的版本冲突。

最终学院/范围/Tier1组合 **30项后端通过**；组织前端现42项，关联组合 **62项通过**；教师构建 **29.04秒、21条预渲染路由**。普通校级/院级双账号实测更名拒绝→纠正保存→院级仍可见原4班且不见其他学院→原学院删除保护→恢复名称→空学院软删除。两阶段只读回查均PASS，学生与已有任职/任务快照不变。详细反例、中间测试修正和最终证据见调用链审计末尾。

第一批继续组织同步/统计和状态治理，再接学籍/注册；已发起已有学生跨学院/整班专业变更办理方式的业务选择问题，但它不阻碍其他已授权工作。当前“组织结构同步”仅定位到只读组织树，需补查正式同步链；不以更名代替能力交付。第一批尚未整体验收，后七批未开启。

## 第一批最新续工：专业基础维护（2026-09-06）

专业新建、编辑、软删除的主数据和两类审计已接入同事务；编辑/删除按锁定后的实际学院归属授权，保留部分更新和既有版本契约，调整原因进入审计。前端请求异常解除保存状态并保留输入，版本冲突要求重开最新资料，错误提示在抽屉顶部可见。

新增 9 项与既有组织/范围组合共 **26 项后端通过**；组织前端现 41 项，关联组合 **61 项通过**；最终教师构建 **41.09 秒、21 条预渲染路由**。正常管理员双页面实测覆盖新建、并发旧版本拒绝、重开复核、保存、删除确认与软删除；只读回查合成专业版本3、业务/系统审计各4条，原学院/班级/学生/学籍事实不变。详见调用链审计末尾及 `.codex-temp/batch1-org-major-edit-*` 日志。

父组织跨学院变更与自动学生学籍调整存在施工卡/现行代码边界冲突，本轮不擅自新增自动转专业，也未消除该欠账。第一批继续处理学院维护、组织同步等不依赖学校政策决定的缺口，再接学籍和注册；本批未整体验收，第二至八批未开启。

## 第一批实施：学年学期（2026-09-06）

范围：原有 8 个三级菜单及新建、详情两个业务页面。保留现有表格、公共表单、学期选择器和详情结构，统一正文主题与层次，未改教师公共壳。学生 PC 与小程序共用的校历读取已改用当前学期解析服务，不复制管理功能到学生端。

已落地：

1. 新增 `terms/:termId` 正式路由；列表和学年明细可进入详情，新建成功直达已创建的学期。新建/详情菜单归属修回「学年学期」。
2. 当前学期加载中、失败、无管理权限或非校级范围时，不提供直接发布/切换；统一治理学校可发布定义，但在统一治理中启用当前学期。
3. 保存前预览影响；旧版本冲突保留用户输入，要求重新核对；切换页面或筛选后的过期响应不能覆盖当前内容。
4. 可选教学周、考试周和名称的清空语义与实际保存一致。教学周快捷保存复用关联业务检查并递增版本，阻止截断既有课表或旧详情覆盖。
5. 解冻使用公共确认框收集真实原因；公开状态提示改为业务语言。新建字段校验同步补至后端，追加 13 项新建及便利性回归通过（`.codex-temp/batch1-term-create-regression.log`）。
6. 切换记录接入 `CalendarTransitionEvent` 的全校激活事件；新的统一治理发布使用 `PUBLISH_DEFINITION` 留痕，不伪装成切换。历史记录缺少切出快照，页面明确显示按已有流水顺序还原，未改写旧审计事实。

验证记录：

- 专用 MySQL：本机 `127.0.0.1:3308`；回归库 `academic_ui_batch1_test`，真实浏览器验收库 `academic_ui_batch1_e2e`。正式迁移运行到本分支 head。测试与浏览器使用不同数据库，未写原项目业务库。
- 真后端 `127.0.0.1:8096`，本分支教师 PC `http://127.0.0.1:5290`；专用测试学校 `academic-w1-school`。普通账号密码登录、正式权限目录及学校角色，关闭 mock-login；浏览器不是 mock 页面。
- 浏览器已实际完成：新建 → 进入详情 → 修改名称 → 影响预览 → 保存 → 发布为当前 → 冻结 → 空解冻原因拦截 → 填写原因解冻 → 返回已发布状态。详情可查到相应操作人与原因。学期周次已读取真实 20 周数据。
- 学生 PC / 小程序校历权威解析：4 项真实数据库接口测试通过，日志 `.codex-temp/batch1-student-calendar-authority.log`。
- 详情更新、冻结/解冻、关联课表检查、考试周清空、切换记录与原学期三级能力：20 项 MySQL 回归通过，日志 `.codex-temp/batch1-term-full-regression.log`。此轮在新建 DTO 校验追加之前执行，追加验证另记，不复用该结果冒充最新字段校验通过。
- 最新前端行为与学期契约 19 项通过，日志 `.codex-temp/batch1-term-frontend-final.log`。另有导航组合 24 项及 502 个正式入口路由一致性检查通过。
- 学期页最终教师构建通过（37.31 秒），日志 `.codex-temp/batch1-term-build-final.log`；构建含仓库既有 CSS import 顺序、第三方注释和分包警告，不声称零警告。后续名册与校历改动的构建另记。

本批未关闭：校历节次、组织、学籍、注册仍需继续 UI 与真实流转验证；学生端完整场景和小程序运行时验收未完成。后续批次保持待开启。

## 第一批续工：名册与校历（2026-09-06）

名册保留既有列表与档案结构，使用公共工作区正文样式。学生姓名接入已存在的档案路由；`PRESERVED` 显示保留学籍，`RETAINED` 显示留级；同组件切换分类菜单会重新查询，尚未提交的筛选不改写结果标题。休学、保留学籍学生沿现有异动表单办理复学，不提前开启第七批重构。敏感证件查看按权限显示，切换学生会清理上一个学生的内容并丢弃过期响应。

已在隔离数据库建立四名模拟学生（仅验收数据，建档钩子同步产生学籍事实），浏览器完成名册 → 保留学籍学生档案 → 复学表单，确认正确预选学生与 `RESUME`；从分类菜单进入保留学籍列表只返回一名对应学生。该记录不代表正式学生导入、组织分班或复学审批已验收。

校历继续保留原有六个业务页签及三级菜单，正文统一公共颜色与卡片。补齐真实维护/发布权限、明确已发布只读状态、学期目录失败重试、菜单页签同步及过期请求保护。日期按自然日显示；编辑可清空旧备注、旧调休目标。复制预览在换来源/目标时失效，复制中固定目标学期，部分成功后要求重新核对，避免重复写入。

后端以正式 HTTP 测试复现后修复：非法校历事件类型、倒置/无效日期、修改事件类型遗留调休字段、校历发布未进入学期切换记录、两个教学周接口对考试周解释不一致。周次与校历共用计算，考试开始周之后保持考试周，显式考试事件生效，当前周使用学校本地日期，跨周调休在原日期和补课日期所在周均可查看。

治理保护的证据修正：旧 `publish_calendar` 服务函数未直接读取 Resolver，但 `academic_calendar.py` 的模型事件已经调用 `academic_term_authority_lock`，真实请求切换到其他 ACTIVE 学期原本会返回 409；不能写作系统允许绕过治理。本次在服务层前置权限/范围和 Resolver 检查，保持模型防线，并避免治理模式下发布同一学期时改写兼容标记。

- 修复前数据库回归：3 失败、1 通过，`.codex-temp/batch1-calendar-before.log`；通过项正是底层治理拦截。
- 修复后校历、学期三级能力及复制便利性：16 项通过，`.codex-temp/batch1-calendar-after.log`。
- 校历/复制新增 7 项、名册 4 项，加学期/权限/导航共 32 项前端测试通过，`.codex-temp/batch1-calendar-frontend.log`；变更页面 ESLint 通过。
- 追加凌晨跨日、跨周调休、同治理学期发布、学期命令及学生两端校历读取：14 项数据库回归通过，`.codex-temp/batch1-calendar-authority-final.log`；与前一轮 16 项存在重叠，不相加计算独立用例。
- 最新教师前端构建通过（24.47 秒，21 个公开页面预渲染），`.codex-temp/batch1-calendar-roster-build-final.log`。导航契约一致，502 个正式入口路由检查通过。
- 浏览器使用隔离库正常登录完成：创建春季草稿学期（termId=2）→ 从详情进入本学期校历 → 添加节假日 → 编辑并清空备注 → 三级菜单进入补课日并配对日期 → 无节次时发布返回明确提示 → 新建第一节 08:00–08:45 → 发布校历成功，当前学期读取为 termId=2，重复发布按钮禁用。请求日志 `.codex-temp/batch1-backend-8096-v3.log`。此处作息仅验证创建前置条件，时间段与各角色仍需另行验收。

页面换分类菜单会将选中学期保留到 URL，刷新也读取同一学期；保存时阻止同组件菜单切换。校历复制为按需展开的辅助区域，主要空间用于当前学期安排。

## 第一批续工：作息节次与时间段（2026-09-06）

保留「作息时间／节次管理／上课时间段」三个原菜单、同一个工作区及公共 UI 壳。模板辅助区默认收起，节次行可直达该节次的时间段；选择保留在 URL。按真实权限展示读写动作，保存时禁用相关操作并保护菜单切换，旧请求不覆盖新选择，编辑日期按自然日回显，清空字段真实落库。表格操作按钮已做实际截图检查并补齐间距。

后端已修正钟点、序号和生效日期校验，支持显式清空；同节次同校区的启用时间段不得日期重叠。已有课表、预约、调课、教师排课设置或考勤引用的节次不可改号、停用、删除；无引用节次删除时，其时间段同步软删除。写命令以本租户目录锁串行检查冲突。模板部分成功会保留失败原因，要求重新检查剩余节次。

新增回归先复现 4 项失败、1 项通过，修复后作息/校历/便利性 20 项真实 MySQL 回归通过；另 22 项课表消费与学生接口回归通过。前端最终组合 28 项通过；ESLint、导航契约、502 个正式入口路由检查通过；最终教师构建 36.27 秒通过，21 个公开页面预渲染。日志与完整调用链见同目录审计记录的“作息节次与上课时间段”章节。

浏览器在专用学校正常登录完成第二节新增、行内进入时间段、夏季作息新增、重复生效日期冲突、编辑回显、清空字段、停用及重新启用。最终保留 MAIN 校区 2027-05-01 至 2027-09-30、09:10–09:55 的启用记录。验收数据仍保留在独立 3308 实例的 E2E 库，未写原项目业务库。当前验收入口为 `http://127.0.0.1:5290/admin/academic-affairs/time-slots?tab=bands&slotId=2`。

**下一步仍在第一批：学院专业班级 → 学籍其余页面 → 注册流转与适用四端验收。** 作息下游已有数据库消费验证，不能替代学生/教师 PC 与小程序的完整运行时验收；批次总状态保持进行中。

发布后返回校历管理已实际核验：节假日与补课日两条记录均为只读，不再展示新增/编辑/删除按钮；补课日同时显示原停课日与目标日期（`2027-05-03 → 2027-05-08`）。本次验收未改原项目运行数据，教师公共壳继续复用。

第一批仍处于施工与验收中。作息节次、组织、学籍其他页面、注册和适用移动端尚未全部收口；第二至第八批保持待开启。

## 第一批续工：组织基础维护与读侧（2026-09-06）

教师 PC 继续使用同一公共工作区壳，保留学院专业班级模块及既有三级入口。学院、专业、行政班表单接入正式版本校验；普通编辑保留未修改字段，真实改归属才填写原因。新增班级时的辅导员范围建立、改绑与解绑已补齐。浏览器真实完成学院 → 专业 → 行政班创建和改名，班级编号、编制人数及应毕业年份保留。

组织列表、年级、树、统计和班级成员按精确授权班级读取，只展示必要的上级组织；一个班的授权不再扩展到同学院其他班级。学院级写操作检查直接学院授权，班级范围只读。组织审计限制本模块类型并按对象范围过滤，已删除的本院组织仍有历史记录；调整申请的部分越界、无法解析归属及空范围不再放行，重复执行的已完成申请也先验证范围。

教学班台账优先读取已存在的正式教学班投影，兼容尚未投影的历史任务；按学期、批次和实例区分，并排除已被合并的旧任务。新增学期和关键字筛选、学期与课程列，按权限进入既有教学任务详情。正式记录显示班额，历史记录显示预计人数；未在查询中补建教学班或名单版本。

班级成员页和行政班成员抽屉改用分页；失败可重试，旧请求不覆盖新班级，翻页后搜索回到第一页。筛选区调整为紧凑横向布局。审计对象和动作使用业务名称，UTC 操作时间交给公共日期工具显示本地时间。统计原查询实际计数全部未删除学生档案，界面名称改为“学生档案数”，不把该数冒称为当前在校人数。

验证证据：

- 组织读侧新增 4 个 MySQL/HTTP 场景全部先复现失败，`batch1-org-scope-before.log`；修复后与基础维护、既有组织和主数据治理一起回归 **39 项通过**，196.89 秒，`batch1-org-scope-final.log`。测试仅使用独立 3308 测试库。
- 前端当前组合 **32 项通过**（组织 12、导航 6、校历 7、作息 7），`batch1-org-scope-frontend-final.log`；变更页面及测试 ESLint 通过。导航契约一致，502 个正式入口路由检查通过，git diff 空白检查通过。最新构建结果见审计记录。
- 正常登录独立 E2E 库，真实查看两个学期的同代码教学班；筛选第一学期只返回对应班级与课程，显示正式名称和班额 35。21 个模拟班级成员第一页 20 条、第二页 1 条，第二页搜索“学生01”回到第一页且只显示匹配学生。
- 组织树已验证信息工程学院 → 软件技术 → 软件技术2026级1班；统计为学院 1、专业 1、行政班 1、学生档案 25；变更审计显示真实创建与改名记录。日志 `batch1-backend-8096-org-scope.log`。教学班和分页数据由独立 E2E 脚本建立，仅用于组织读侧验收，不代表教学任务生成或注册流程已验收。

下一步仍在本组织模块：专业方向工作区、组织调整核对与执行的一致性、单个学生调整的业务限制、跨组织移动的下游一致性，以及适用师生端消费。之后继续第一批学籍与注册；组织模块、第一批及八批总目标均未标记完成。未暂存、提交、推送或合并。

## 第一批续工：班级调整申请与结果（2026-09-06）

继续学院专业班级模块，完成批量调整申请的发起 → 核对 → 确认执行/撤销 → 结果查看。原菜单及公共壳保持，页面改用分页和明确的核对清单；发起成功自动展示核对，冲突保留说明，核对过期或存在待处理事项时不能执行。已执行显示实际变更班级数及完成时间，已撤销只保留历史核对。

后端执行前重新锁定并核验班级、学生、组织归属与教学任务，核对后任何相关变化要求重新核对；申请版本与逐班版本更新，同事务保存结果和审计，重复执行只返回已有回执。合班登记保留现有停用空来源班的行为，因此先处理来源学生与教学任务；拆班登记继续只登记，不迁移学生或新建班级。新增长文本迁移支持正常批量记录，并验证超长历史时拒绝危险缩字段回退。

本次验证：后端新增 8 项先复现失败，修复后组织组合 **47 项通过**；前端 **39 项通过**，ESLint、导航契约与 502 个正式入口检查通过；最终教师构建 **30.41 秒通过**并完成 21 个公开路由预渲染。独立 3308 E2E 库完成迁移往返及超长回退保护验证。具体文件、调用链及日志见[第一批调用链审计](./第一批开学条件-调用链审计.md)本次结果段。

浏览器正常登录验证：有 21 名学生、2 条未归档教学任务的班级被阻断，撤销后可查历史；新建独立空班完成停用，回执与行政班列表显示已解散。DB 复核原班 21 名学生不变、空班版本增至 1、执行和班级变更审计各一次。全部在本工作区私有验收服务与模拟记录中完成，未迁移来源或生产数据库。

下一段仍属本组织模块：单人转班及行政班直接状态编辑的限制一致性、专业方向、教学秘书、跨组织移动下游一致性和适用师生端消费；之后继续第一批学籍与注册。不能将本次申请流程通过视为组织模块、第一批或四端完整完成。八批目标继续保持进行中，第二至八批未开启；未暂存、提交、推送或合并。

## 第一批续工：单人转班与师生端资料刷新（2026-09-06）

按真实正式入口补齐“行政班 → 学生 → 调整班级 → 核对影响 → 确认 → 回执 → 目标名册”。执行沿 C1 学籍事实命令，核对与执行均检查来源/目标权限、班级与父组织状态、当前学籍及事实一致性；学生/目标版本或目标人数变化要求重新核对。编制超限按现行规则提醒，保留年级与学籍状态，记录操作人和原因。页面复用公共抽屉，失败保留输入，防止旧响应和重复提交。

学生 PC 资料页与师生小程序现有资料/名册页补上刷新和页面返回时读取最新归属，离开后旧请求失效。教师小程序实际使用 V3 名册接口，已纠正审计记录，未用旧接口的存在代替当前消费链。学生学籍状态显示明确，浅色、深色正文沿公共主题；29 个二级模块及导航层级继续保留。

真实隔离库、普通登录验证：模拟学生原班 21 → 20 人，接收班 0 → 1 人；停用目标被拒绝，超编警示后按现有规则可确认；学生 PC 刷新后两个资料区域都显示新班级，“已注册”及 2026 年级保持。只读 DB 复核两条连续学籍事实、唯一当前投影、正常操作人和一条原因审计通过。没有重置历史调整申请或改动来源数据库。

最终后端组合 **34 项通过**；教师前端组合 **43 项**、学生 PC **3 项**、小程序 **7 项**通过。三个前端构建通过，导航契约及 502 个正式入口通过；保留既有构建/ORM 警告，学生资料页 lint 有一条既有未用 catch 变量警告。证据与日志路径见[第一批调用链审计](./第一批开学条件-调用链审计.md)“单人转班与师生消费续工结果”。本次无需新数据库迁移。

组织模块仍待行政班直接状态变更限制一致性、专业方向、教学秘书与跨父节点移动核验；学生注册和其余学籍闭环待继续。小程序本次完成真实消费接口、页面行为和构建验证，微信运行验收未完成。第一批未关闭，第二至八批未开启；未暂存、提交、推送或合并。

## 第一批续工：行政班状态核对与删除保护（2026-09-06）

已修复“班级调整申请会检查引用，编辑班级状态却可直接停用”的旁路。原编辑抽屉在状态改变后显示原/目标状态、原因、在籍学生和未归档教学任务，先核对再确认保存；普通名称、编制等编辑保持直接保存。正式保存即时重检，学生或任务变化使整个编辑失败并保留输入。结班不代替学生毕业或注册办理。

班级主数据、班名/辅导员范围和两类审计现同事务保存，实际并发只能有一个版本胜出。班级删除也使用调用方事务，审计失败时软删与系统审计一起回滚。批量核对、统一班级写入及删除复用当前学生/任务引用查询，系统共享 CLASS 停用分支补齐任务保护；保留该入口原有更严格的历史学生关联规则。锁定父组织后重检学院范围，避免核对中组织移动造成越权。

普通登录隔离验收已完成：有 20 名学生、2 条未归档任务的班级不能停用；空班核对 0/0 后同时更名并设已毕业，版本增一，其他资料保留；独立模拟空班软删成功，原班删除被拒绝。删除确认原来错用了专业名，现显示准确班名，失败原因直接留在确认框。DB 复核状态、人数、业务与主数据审计通过，历史转班与批量调整记录保留。

最终后端组合 **34 项通过**（含本轮状态/删除 10 项、原组织/主数据/批量调整回归）；上一步另有并发、开校向导与系统实施组合 34 项通过，组合重叠不累加。最终教师前端 **47 项通过**，构建 **24.42 秒通过**及 21 个公开页预渲染，ESLint、导航契约和 502 个入口检查通过。具体调用链、分阶段日志及限制见[第一批调用链审计](./第一批开学条件-调用链审计.md)本轮结果。

下一项仍在学院专业班级：专业方向、教学秘书和跨父节点移动的主档、范围与下游一致性。随后继续第一批学籍与注册；微信运行验收等缺口继续保留。公共壳与 29 个二级模块未重排，第二至八批未开启；未暂存、提交、推送或合并。

## 第一批续工：专业方向维护（2026-09-06）

仍在既有「学院专业班级 → 专业方向」页签内施工，未调整二级菜单身份或公共壳。补齐专业切换与分页的过期响应处理、开关加载失败、固定表单归属、保存错误保留、停用确认与历史只读；复用公共抽屉、提示和分页。后端在现有表上补齐配置/父组织/方向的事务锁定、可选版本校验、方向停用幂等、父组织有效性、编码清空与历史编码冲突，无新增迁移。

最终方向 7 项加班级状态/删除 10 项后端回归 **17 通过**；既有 Tier1 R2 14 项此前全通过。前端组织、导航、校历、节次 **53 通过**；ESLint、最终构建（54.93 秒、21 条预渲染）、导航契约和 502 个入口路由一致性通过。日志与详细验收见调用链审计中的“专业方向实施与验收”。

专属验收学校普通管理员通过页面完成启用→新建→更名/清空编码→重复编码被拒后修正→停用→关闭再开启。数据库回读：全栈方向停用且版本 3，云应用方向启用且版本 0；开关版本 3，两条记录完整保留，8 条操作审计匹配普通管理员，原行政班 20 名学生未变。该目录尚未与培养方案等方向关系连接，师生自助端不新增内部目录管理页。下一步仍在第一批内处理教学秘书绑定、父组织调整及学籍/注册剩余闭环；整体及微信实际运行未验收。

## 第一批续工：教学秘书绑定与受理关系（2026-09-06）

学院列表显示具体绑定人员及失效状态；绑定抽屉复用公共组件，按本校有效教职工选人，核对变更后确认，保存失败保留选择，成功显示真实回执。目标学院、原版本及人员选择固定，旧请求和重复提交受保护。后端补齐同租户校内职工校验、学院/账号锁定、可选版本、无变化幂等、同事务姓名审计；原 POST 与 null 解绑兼容，无新增迁移。

这不是自动授权或人员任职操作。已沿真实代码核对成绩、调停课、成绩更正和学籍异动的受理关系；测试验证新的持权绑定影响未来解析，已有任务保留原受理人，清空绑定仍保留有效任职，绑定未授权教师不会给他审核权。相关模块完整流程仍按八批顺序验收。

后端新增秘书 6 项加原组织/范围共 **18 通过（133.99 秒）**，前端组织/导航/校历/节次 **58 通过**，ESLint、教师构建（29.49 秒/21 条预渲染）、导航契约及 502 个入口路由一致性通过。普通管理员在专属库实际完成王老师绑定、失效提示、换李老师、解除绑定；只读回查证实学院版本 0→3、三条人员前后审计、角色/任职/待办归属及原班 20 名学生保持预期。查看过桌面确认页及窄窗结果抽屉；没有将内部人员配置复制到师生自助端，也未宣称微信端实际验收完成。

下一步继续本批父组织变更一致性，再衔接学籍、注册。证据详见调用链审计的“教学秘书绑定实施与验证”；日志均在专属 `.codex-temp/batch1-org-secretary-*`。无提交、推送或合并。

## 历史阶段：课表管理前端重构（真实数据验收另列）

选择依据：已存在完整的查询、批次、发布、导出和师生课表入口，适合先完成高频工作区的一致适配；不依赖调整导航或重建业务。按原三级菜单逐页核对，不将排课管理、调停课独立模块整体纳入本次。

### 关联审计与四端范围

代码定位相对于上述工作区，代码阅读证据对应当前未提交工作区，运行结果另记。

| 三级内容与动作 | 教师 PC 真实页面 | 学生 PC / 小程序 | 现有接口及关联 |
|---|---|---|---|
| 课表批次 / 排课 | `AaScheduleBatchListView.vue`；批次内 `AaScheduleMaintainView.vue`、`AaScheduleViewsView.vue` | 学生不适用批次维护；两小程序不适用大型编排工作区 | `academic-affairs.api.js` 的批次/课位/预检/三视图接口；统一 `academic_affairs.py:1256` 起；教学任务与资源只引用 |
| 班级、教师、学生、教室、教学班课表 | 同名 `Aa*ScheduleView.vue` 共五页 | 学生 PC `/academic/schedule` → `StudentScheduleView.vue`；学生小程序 `pages/student/academic-affairs/schedule`；教师小程序实际为 `pages/teacher/my-schedule/index`，不是猜测的 academic-affairs/schedule | 后端 `academic_affairs_schedule_service.py::class_schedule/teacher_schedule/room_schedule/student_schedule/teaching_class_schedule`；`academic_affairs.py:1339` 起；学生仅本人，教师查询依现有范围裁决 |
| 周课表、学期课表 | `AaWeekScheduleView.vue`、`AaSemesterScheduleView.vue` | 师生自助课表内现有周次筛选；不向手机引入管理查询权限 | 复用各维度查询、学期/校历与节次；不另算学校规则 |
| 课表发布 | `AaSchedulePublishView.vue` | 师生只消费已发布结果，无发布权限 | 现有完整性检查、预发布/正式发布、发布记录；通知由后端原链路处理 |
| 课表调整记录 | `AaScheduleAdjustmentLogView.vue` | 不提供内部操作台账；师生查看本人课表及现有通知 | `list_schedule_adjustments`；调停课链接保留原目的地，不改其业务 |
| 课表导出及打印 | `AaScheduleExportView.vue`，批次打印 `AaPrintScheduleView.vue` | 学生 PC 保留现有本人打印，学生小程序保留摘要复制；教师小程序保留现有授课入口 | 现有导出任务/用途与水印；不新增外传能力或修改导出范围 |

教师前端真实目录为 `frontend/src/modules/academicAffairs/`，旧中心索引中的 `frontend/src/views/admin/academic-affairs/` 在当前基线不存在。教师路由位于 `academic-affairs.routes.js:94` 起；学生路由 `academicRoutes.js` 已挂公共 `PortalLayout`。小程序页面路径从 `pages.json` 核验，不改页面注册。

### 当前实际流程

1. 授权教务人员选择批次、教学任务、班级和资源，预检后使用原接口排课；冲突裁决仍由服务端完成。
2. 通过原检查进入预发布或正式发布；已发布后原直改限制与调停课入口保留。
3. 教师、学生在本端读取有权查看的已发布课表；小程序教师当天课程保留服务端提供的现场办理目标。
4. 查询失败应显示失败与重试，不能显示无课；未选查询对象、无记录、未配置节次分别说明。
5. 导出、打印、发布记录沿原接口与权限执行。本次未运行这些写业务，不将源码阅读写成真实流转已通过。

### 问题与施工顺序

| 证据 | 类型与影响 | 本次前端处理 |
|---|---|---|
| 多个 ModulePageShell 的长 subtitle 含数据库表名、权限实现和开发说明 | 正文层次冗长，用户需要额外理解技术背景 | 精简为当前任务与结果说明，保留必要资格和范围 |
| 教师今日课程、学生 PC 和手机课表多处白色/渐变写死 | 深色主题正文底色与文字不协调 | 使用现有公共语义颜色和紧凑区块，样式仅限课表正文 |
| `StudentScheduleView` 的 todayHeading/todayNote 不判断 error | 请求失败仍可能称今天无课、已核对 | 失败状态优先，隐藏错误统计并保留重试 |
| `AaScheduleGrid` 的课程点击元素缺键盘操作，空格新增仅鼠标 | 键盘无法完成已有操作 | 使用有名称的可聚焦操作，不增加接口或动作 |
| 教学班未选择时缺说明、部分记录查询无错误正文 | 初次进入或失败时没有下一步 | 复用 EmptyState/ErrorState 补齐明确反馈 |

顺序：核验公共壳与三级入口 → 13 个教师正文及共用课表网格 → 学生 PC 课表 → 两套小程序课表 → 相关测试、构建与可执行视觉检查 → 记录缺口。

### 验证状态

- 公共壳导入后、课表改动前：学生 PC `npm test` 退出码 0，124 通过、0 失败。
- 原有本地教师入口实际打开后跳转登录页，尚未取得本任务安全测试登录态；未绕过认证、未使用保存的账号信息提交登录。
- 正式课表浏览器检查、四端真实数据交接和微信开发者工具运行验证未执行；隔离样例只用于正文视觉和组件行为检查，不作为业务验收。

### 本轮实现（2026-09-05）

- 教师 PC：11 个既有三级页面、批次内维护/三视图 2 个页面使用同一公共 ModulePageShell、AppSectionCard；新增课表域内部样式 `frontend/src/modules/academicAffairs/styles/schedule-workspace.css`，统一正文标题、筛选区、卡片密度与颜色引用。公共组件本体、教师布局和所有菜单未改。
- 课表网格：七列等宽，窄屏在课表内部滚动；节次列固定。具有原点击动作的课程卡片支持 Enter / Space；空课位新增改为有名称的按钮，仍发送原 cell-click；教师课位详情接收焦点并保留三种调停课申请入口。打印时取消节次列固定定位。
- 学生 PC：同一公共壳内使用精简页头、今日课程卡片、周次选择框、回到本周、按天排列的课程；保留作息多校区差异、单双周、选课标识及原有打印留痕。加载失败时隐藏今日结论和课程统计，禁用打印并提供重试。
- 教师今日课程、发布记录：失败与空数据分别呈现，独立读取今日课程失败不伪装成无课，也不遮蔽正常的周课表。
- 教学班初次进入和批次三视图未选择时使用原 EmptyState；三视图切换对象后清除旧结果，过期响应不能覆盖新对象。
- 附带修复现有打印错误：三视图中的学生打印此前错误地使用班级类型；本轮保留 student 查询类型，既有打印页面复用已存在的学生课表查询 API。打印页加载失败时提供重试并禁止打印。未新增菜单、路由注册或接口。
- 学生与教师小程序：仅修改原「我的课表」页面正文。保留 MobileNavBar / MobileTabBar、原接口和权限；卡片使用公共颜色，增大说明文字和周次选择触点；学生周课表增加既有作息钟点显示。

### 已执行验证与证据

| 检查 | 结果 | 范围与实际限制 |
|---|---|---|
| 学生公共壳快照核验 | 10 / 10 字节哈希一致 | 对照导入清单；App、PortalLayout、两级菜单轨道、导航配置和主题均未经本轮改写 |
| 教师公共壳、菜单、四端路由及公共 ModulePageShell | 8 / 8 与基线一致 | 加上上述 10 项共 18 项通过；复核结果 `.codex-temp/frozen-shell-check.json` |
| 教师课表相关自动化 | 22 通过，0 失败 | 5 个测试文件，其中新增 4 个运行实际页面方法的测试：发布记录失败/恢复、今日请求失败、过期查询响应、学生打印维度；`.codex-temp/academic-tests.log` |
| 学生 PC 自动化 | 124 通过，0 失败 | `student-portal/npm test`，含菜单契约和今日课程服务端投影契约 |
| 小程序相关自动化 | 9 通过，0 失败 | `academic-teacher-today-contract.test.mjs`、`academic-v81-teacher-schedule-change-receipt.test.mjs`；早先一次从学生工程目录调用未找到文件，已在 miniapp 正确目录重跑并取得本结果 |
| 变更前端 ESLint | 退出码 0 | 教师课表 Vue、网格与新增行为测试；学生 StudentScheduleView；未宣称全库 lint |
| 教师前端正式构建 | 退出码 0 | 最后一次构建 34.23 秒，含现有公开页面 prerender；仅有现有大 chunk 提示；`.codex-temp/frontend-build-final.log` |
| 学生前端正式构建 | 退出码 0 | `.codex-temp/student-build.log` |
| 微信小程序构建 | 退出码 0 | `npm run build:mp-weixin`；`.codex-temp/miniapp-build.log`，不等同于微信工具或真机验收 |
| 浏览器：学生真实公共壳 + 课表组件 | 已查看 | 独立样例端口 5286；浅蓝/曜石黑视觉、周 2 → 周 1 后 11 → 12 个安排并出现单周课程、回到本周、失败时无「无课/已核对」结论且打印禁用 |
| 浏览器：教师真实课表正文组件 | 已查看 | 常规宽度与实测 650 × 900 CSS 视口；筛选换行、内部课表滚动、键盘 Enter 打开详情，焦点落入详情并保留申请按钮；临时视口已恢复 |

本轮只验证上述 UI 与契约。未执行真实批次创建、发布、作废、导出、学生打印留痕、跨租户/角色业务流转；这些行为仍沿现有服务端裁决，不写作已验收。GPT 网页版项目文档未读取。

依赖方面，三个工程的锁文件与来源工作区一致；仅建立本工作区 node_modules 本地 Junction 复用已安装依赖，未升级依赖。浏览器样例入口、样例数据与构建日志全部位于被忽略的 `.codex-temp/`，不加入正式业务代码。未进行 git add、commit、push 或合并。

视觉样例启动：在本工作区运行 `node .codex-temp/schedule-review/server.mjs`，访问 `http://127.0.0.1:5286/.codex-temp/schedule-review/student.html`。页面有样例标识，加载现有公共壳及真实课表组件；不提供完整业务导航和写操作，只供本地 UI 复核。

### 后续

旧阶段下一模块候选曾为选课管理。现按最新八批顺序执行：先收口第一批开学条件，课表真实数据和微信验证欠账保留到第三批；公共壳继续复用，教务菜单可调整。

## 2026-09-06 公共壳替换与教务菜单迁移

按用户本轮要求，在独立工作区 `职校学生全生命周期系统-academic-affairs-ui-20260905`、原分支 `codex/academic-affairs-ui-20260905` 继续施工。checkout/base 均为 `6810bd6284b4d5c69a78947560f808cd7738327e`；工作区原有业务改动保留，未提交、推送、合并或部署。

- 从学工工作区读取当时的公共壳实现（源 HEAD `f138667d7ae197b304124a75455c15434b8d8474`，含未提交公共壳文件）；10 个文件的实际来源哈希保存在 `.codex-temp/academic-shell-source-snapshot.json`。本节更新早期复用清单所描述的旧壳状态，不将旧哈希当作当前验证结果。
- 接入 BasePortalLayout、TeacherWorkspaceFrame、账号入口、页面标签、常用入口、快捷入口编辑器和公共导航辅助。既有教务业务页面继续在新壳的公共内容区渲染；精简页头仅在教务布局注入，保留正文标题和操作。
- 原有 29 个二级模块全部保留；263 个菜单配置项中 262 个可见三级入口迁入新壳，原先隐藏的学籍档案重复项继续隐藏。菜单仍按真实权限过滤。
- 对共用相同路由的菜单增加 `_workspace` 导航上下文，修复跨模块误高亮、页签混淆；不改变原业务路径和业务查询参数。保存的页签及常用入口按当前可见权限回收，教务页面偏好与学工页面列表分开保存。
- 本轮只替换壳和接入既有内容，不扩展后端业务、不写入业务终态，也不将全部三级业务流程标为验收通过。

### 运行目标与已验证内容

用户明确要求“自己选择库吧。不要沙箱这个库了”，本轮后续人工检查改用既有独立 MySQL `academic_ui_batch1_e2e`（本机 3308），学校 `academic-w1-school`；未重置该库，未清理或迁移原持久化沙箱。前端 `http://127.0.0.1:5290` 代理到独立后端 8096；原 5173 页面不属于本次新工作区预览。

- 正常学校管理员登录后，新壳读取原有两个学期、信息工程学院、软件技术专业。实际点击学院管理 → 专业管理后，URL 的 tab、页面标签与正文列表对应一致。
- 已检查教学计划下重复路由的菜单上下文、刷新保持、关闭页签后恢复；1440 CSS 宽度检查菜单选中及正文单实例。内嵌浏览器的大视口截图有拼接伪影，不能作为桌面视觉无缺陷的证据；默认约 500px 截图已查看，375/390/430px 本节未完成逐宽验证。
- `node --test tests/academic-shell-navigation.test.mjs tests/academic-navigation-workspaces.test.mjs tests/academic-org-workspace-behavior.test.mjs tests/academic-term-workspace-behavior.test.mjs`：64 通过，0 失败。菜单测试遍历所有可操作三级入口的目标与选中结果；日志 `.codex-temp/academic-new-shell-regression.log`。
- 改动壳组件、教务布局与新增导航测试 ESLint 通过。`node scripts/check-navigation-route-parity.mjs`：502 个正式可见入口校验通过。
- 教师前端最终 Vite 构建通过，24.69 秒，21 个公开页面预渲染通过；有现有大 chunk 提示。日志 `.codex-temp/academic-new-shell-build-final.log`。
- 未执行本轮后端业务写操作、四端交接、微信宿主实测及全部 262 个三级页面逐一浏览器验收；导航覆盖测试与真实业务闭环分别记录。

## 2026-09-07 教务三级页面与四级深链视觉优化（仅前端）

使用用户指定的 Product Design 截图审查流程，在同一独立工作区和分支继续施工；checkout/base 仍为 `6810bd6284b4d5c69a78947560f808cd7738327e`。本节仅记录本轮增量，之前的后端、菜单、公共壳和其他端未提交改动继续保留。

- 29 个二级模块、262 个可见三级入口保持完整；代码核对覆盖 127 个叶子路由、119 个页面组件，其中 22 个路由含动态参数。通过教务正文作用域统一标题、操作区、筛选、表格、卡片、表单、空态与焦点样式；没有全局修改公共壳或共享组件默认样式。
- 对 42 个 Vue 文件做定向呈现调整，新增 `business-workspace.css` 与 `print-preview.css`。课程表单按基本信息、学分学时与考核、责任与适用范围分组；课程、学籍、异动详情突出当前对象；四类筛选页将标签与控件保持成组；13 处原生表格增加键盘可达的横向滚动容器；6 类打印页面仅改善屏幕预览，保留打印字段和打印媒体规则。
- 归档检查工具折叠为按需展开；移除部分面向开发者的正文术语；正文旧色值与小字号收敛到公共壳主题。实测修复任务详情深色模式下的浅底白字按钮、深色指标文字和范围说明块。
- 本轮没有改变业务脚本、事件及双向绑定、API、权限、状态机或数据库；未发起业务写操作。页面分类和信息分组改善已检查，未通过真实师生试用量化点击效率。

### 本轮验证与限制

- 浏览器使用真实管理员会话、既有独立库 `academic_ui_batch1_e2e`、学校 `academic-w1-school`；前端 5290 / 后端 8096。262 个菜单入口逐项导航并检查正文、查询参数与页面溢出；排课规则等待加载完成后复核通过，考务打印按独立打印布局复核。管理员进入“本人选课”收到原有权限拒绝，未扩权绕过。
- 真实对象深链已查看学期 `/terms/2` 与教学任务 `/teaching-tasks/2`；学期详情刷新后保持对象。并非 22 个动态路由的所有对象、角色和状态都已实测；打印仅检查无考场上下文的入口，未生成正式打印结果。
- 默认约 994px 窗口已检查浅色、深色及课程表单前后截图；学期详情、课堂考勤在 375/390/430/1440 CSS 宽度的 DOM 几何检查均无正文或文档横向溢出。内嵌浏览器模拟宽度截图有拼接伪影，不能据此声称完成窄屏像素级视觉验收。临时视口已清除，主题已恢复雾蓝。
- `node .codex-temp/check-academic-visual.mjs`：29 模块、262 入口、127 路由、42 个改动 Vue，模板/样式编译及业务脚本、事件、双向绑定对比无错误。此检查是范围与静态合同证据，不代表业务闭环。
- `node --test` 执行 `frontend/tests/academic*.test.mjs`：166 通过，0 失败。42 个改动 Vue 在 frontend 目录执行 ESLint 通过；首次从根目录未找到 ESLint 配置，已修正工作目录后重跑。
- `node scripts/check-navigation-route-parity.mjs`：502 个正式可见入口通过。最终 Vite 构建 25.04 秒通过，21 个公开页面预渲染通过；保留既有大 chunk 提示。
- 本轮截图、262 项检查明细、响应式几何结果保存在忽略目录 `.codex-temp/academic-visual-review/`；构建与测试日志在 `.codex-temp/academic-visual-*.log`。课程表单 `02-course-form-before/after.png`、考勤 `10-attendance-final.png`、任务深色 `08-task-detail-dark.png` 为已查看证据；`06-term-detail-390.png` 有拼接伪影，不作为视觉验收证据。

本轮完成教务页面统一视觉层与定向页面优化，不宣称四端业务闭环、全量动态对象状态、微信宿主或真实师生易用性验收通过。没有提交、推送、合并、发布；原 5173 页面不是本工作区交付预览。

### 2026-09-07 续工：二、三、四级模块

用户补充要求继续美化“234级模块”。沿原工作区、原分支与上述 HEAD 继续，仅修改教务前端。

- 新增 `module-navigation.css`，通过教务布局根类 `aa-workspace-ui` 限定消费者；共享壳文件不改。二、三级菜单提升至 13px，展开时长名称允许换行，强化选中条与键盘焦点，恢复细滚动条提示；移动菜单触点至少 44px。页签同步优化文字与选中层级。29 个模块及其全部入口、权限和导航目标保留。
- 正文详情键值行统一间距与分隔，窄内容区切换单列；对象摘要与审核按钮组完善视觉分组。课程详情原审核区前移到对象摘要之后、课程材料之前；动作、显示条件与事件保持原实现。课程列表导入标题改用业务用语。
- 改动 Vue 的 ESLint 通过；166 项 academic 测试通过，502 个正式入口路由检查通过；最终构建 26.51 秒通过，21 个公开页面预渲染通过，保留大 chunk 提示。日志 `.codex-temp/academic-module-navigation-tests.log` 与 `.codex-temp/academic-module-navigation-build.log`。
- 原 5290/8096 服务停止后已恢复独立服务。浏览器会话失效，登录页要求接受学校用户协议与隐私政策；已按浏览器工具确认规则向用户发起本次同意请求，尚未勾选。此续工的登录后视觉与响应式复核待完成，不能沿用上一轮截图声明已验收。

本次增量为 3 个 Vue、1 个既有正文样式与1 个新菜单样式；未提交、推送、合并或修改后端。后续继续完成登录后截图复核，再沿二级模块逐项深化其三级和四级内容。

## 2026-09-07 教务领导大屏资源包安全接入（仅前端）

按用户要求先读资源包 AI_START.md，再读 CODEX_PROMPT.md、安装器与数据接线；直接复用 `Downloads/跃科-教务领导大屏-精修完整版-Codex资源包-20260907/academic-wall` 内的界面、配置、素材和运行模块，没有从截图重写。当前分支 `codex/academic-affairs-ui-20260905`，checkout/base 仍为 `6810bd6284b4d5c69a78947560f808cd7738327e`。

- 新增“教务大屏”二级菜单，位于“教务看板”前；正式入口 `/admin/academic-affairs?wall=1`，沿用 `academicAffairs.dashboard.view` 权限。现在 30 个二级模块、263 个可见三级入口。原工作台、路由和权限保留；原工作台提供“打开领导大屏”，大屏退出移除 wall 参数并保留原 panel。
- 安装器的源码基线门禁发现 4 个差异文件，只复审 AaDashboardView.vue、academic-affairs.routes.js、academic_affairs_dashboard_scope_facade.py、academic_affairs_service.py。包基线 `bafc9db2f48e98a1ac906a4f88cf37fce39915ce`；差异为已有页面样式、学期路由/权限及学期周次、校历节次业务，相关看板接线合同保持一致。本次没有修改后端或降低安装器校验。
- 通过独立适配脚本调用原安装器的文件清单和 patch_source，逐文件检查目标边界和覆盖风险；13 个包内文件原样复制，哈希及差异复审记录位于 `.codex-temp/wall-install-review.json`。安装后原安装器再次 dry-run 返回 already_installed、changed=[]。接入 AaDashboardView，更新菜单、导航测试与生成的导航合同。此前临时新建且未发布的 AaBigScreenView 和 big-screen 路由已由本资源包入口替代。
- 本次未增加依赖、未改锁文件，未改业务权限或数据库。保留工作区先前已有的后端、其他端和公共壳改动。没有暂存、提交、推送、合并或发布。

### 验证证据与边界

- 包完整性校验：40 文件、0 错误；Node 资源包测试 64 通过。Windows Python 安装器测试受 Git 只读文件清理和符号链接权限限制，改在已安装的 WSL Ubuntu 执行原测试，14 通过；未删除或跳过断言。
- 导航相关测试 12 通过；正式可见路由合同 503 个入口通过，生成合同 --check 通过；改动 Vue/菜单 ESLint 通过。前端正式构建 22.91 秒通过、21 个公开页面预渲染通过；仍有既有大 chunk 提示。日志 `.codex-temp/leadership-wall-build.log`。
- 浏览器使用独立教务库 academic_ui_batch1_e2e、学校 academic-w1-school 的真实管理员会话，前端 5290、后端 8096。大屏读取真实学期/准备项；无成绩任务显示“—%”，真实零值保持 0，校园空间保留“待绑定真实楼宇”提示，没有填充演示统计。
- 实测指标解释弹窗、成绩工作区下钻、浏览器返回、大屏返回原工作台。另从 `?panel=warningReminders` 打开领导大屏并返回，URL 仍为 `?panel=warningReminders`，原菜单位置保留。
- 1920×1080、1366×768、3840×2160 的 DOM 几何检查均无横向溢出或面板越界（亚像素容差 0.5px），校园素材加载成功。结果 `.codex-temp/leadership-wall-review/viewport-check.json`。
- 有效实际页面截图 `.codex-temp/leadership-wall-review/actual-native.png` 已查看。内嵌浏览器在大尺寸模拟下产生拼接伪影，actual-1920.png 被排除，不作为完整视觉验收证据。全屏按钮未进入 document.fullscreenElement 状态，备用 Edge 也因工具 Emulation.setFocusEmulationEnabled 超时无法完成验证；全尺寸像素级验收和宿主全屏仍未完成。
- 权限拒绝、身份切换、迟到响应、缺失数据和错误态有包内自动化覆盖，但本轮未用另一个受限账号逐项浏览器复验；不将其记为真实跨角色验收。所有临时视口设置已恢复。

### Product Design 工具复验补充（2026-09-07）

本次重新实际操作并截图，纠正上节“全屏未进入”的结论：普通浏览器 playwright.evaluate 使用的页面读取接口不完整暴露 fullscreen 属性，不能据其 undefined 判定宿主不支持。改用同一浏览器 CDP Runtime.evaluate 只读原生 DOM 后，确认 fullscreenEnabled=true、点击进入 fullscreen=true、再次点击退出 fullscreen=false；全屏弹窗下钻到 grade-overview 后 fullscreen=false 且大屏宿主已卸载。通过现有“教务运行大屏”页签返回，再进入全屏成功。

当前实际全屏视口为 1360×729，完整截图及指标弹窗截图均无拼接伪影，已保存并逐张打开复核。证据位于 `.codex-temp/leadership-wall-review/product-design-qa/`：01-wall-full.png（完整画面）、04-fullscreen-dialog.png（全屏口径弹窗）、05-grade-workspace.png（恢复公共壳后的成绩分析）、verification.json（原生状态）。03-metric-dialog.png 为窄侧栏弹窗，支持内部滚动；主要操作需滚动才能看见。1920 模拟截图 02-wall-1920.png 仍有重复拼接，被排除；不能宣称 1920/4K 像素级验收通过。临时视口覆盖已重置。

本次截图可见的产品风险：低高度画面中部分面板底部辅助文字紧贴边缘；指标弹窗直接展示英文状态、接口字段和公式，对学校领导不够友好；全屏按钮进入后仍叫“全屏”，没有“退出全屏”状态提示。按用户“直接复用，不重新设计”的要求，本次只记录，不改变包内界面。页面仍明确提示部分统计不可用，未冒充全部数据就绪。此次为只读验收，无业务代码、权限、后端变更，无提交推送。

## 2026-09-07 大屏空数据、可读性与布局问题修复

用户授权处理验收发现的问题，并明确“现在没数据，以后有数据了显示出来就行”。本轮只修改教务前端，延续 `codex/academic-affairs-ui-20260905`；HEAD/base 仍为 `6810bd6284b4d5c69a78947560f808cd7738327e`。保留原资源包整体设计、素材、公共壳和接口权限；原下载包未改，已安装组件在用户授权下进行定向优化，不再以安装时逐字节一致的旧哈希证明当前版本。

- 修复正常空结果被误标为“统计不可用”：核对现有后端 _today_courses、_resource_occupancy 和考试提醒返回合同，前端分别处理暂无课程、暂无近期考试、教室资料待配置、暂无使用记录。缺失/错误/无权限/学期冲突仍独立显示，不补假零。零分母保持“—”，整体状态不再因正常暂无成绩任务而报接口异常。
- 新增 aa-wall-presentation.mjs 集中处理中文说明；英文状态改为中文，公式改为业务描述，接口字段折叠到管理员技术信息。更新时间按学校配置时区展示。录入并完成对应业务后，继续由原连接器自动刷新读取；未创建或填充虚构业务数据。
- 全屏按钮通过 fullscreenchange 同步“全屏/退出全屏”、标题和按下状态，卸载清理监听。全屏失败有明确反馈。
- 提示条独占一行，不再覆盖底部图表；修复 SVG 行内基线、图例行高和面板间距引起的内容截断。弹窗内容可滚动，操作按钮固定在底部；弹窗打开时大屏背景 inert，技术说明可用键盘展开。
- 修改已安装组件 aa-wall-data.mjs、aa-wall-view.mjs、aa-wall.css，新增 aa-wall-presentation.mjs；新增两组前端测试及测试专用 fixture。资源包原 64 项断言保留，仅调整项目内导入位置与 Node 测试引用；测试 fixture 不进入生产组件。

验证：当前代码 ESLint 通过；`node --test tests/academic-leadership-wall.test.mjs tests/academic-leadership-wall-empty.test.mjs` 75 通过，0 失败。新增覆盖正常空学校、教室未配置与已配置但无占用、缺失/错误/无权限/冲突、质量异常，以及连接器从空响应刷新为有课程/教室/考试明细和完整总数。这个有数据转换属于隔离自动化测试，不冒充真实库新增业务闭环。最终 Vite 构建 45.95 秒通过，21 个公开页面预渲染通过，保留既有大 chunk 提示；日志 `.codex-temp/leadership-wall-refined-build.log`。

真实浏览器连接原独立教务库和管理员账号：统计来源均显示已更新，页面显示真实已有准备事项及正常空态；全屏进入/退出和标签、指标弹窗、数据说明、成绩分析下钻、原页签返回通过。窄侧栏视口 478×596 下，两个操作按钮底边约 512px，可直接操作；全屏截图已逐张保存并打开复核。通过原生 CDP 在 1920×1080、1366×768、3840×2160 核验，7 个面板正文滚动溢出均为 0，面板/页脚/提示条越界数均为 0。临时设备覆盖已清除。

证据目录 `.codex-temp/leadership-wall-review/refined/`：01-wall-final.png、02-metric.png、03-narrow-dialog.png、05-data-status.png、native-layout-checks.json。原生 1920 截图 04-native-1920.png 仍受到宿主截图拼接问题影响，已排除；三档几何检查不冒充 1920/4K 像素级截图验收。学校楼层实时使用率没有现有数据接线，继续明确显示校园示意，不承诺仅录入普通课表即可形成楼层实时监控。未提交、推送、合并或发布。

## 2026-09-07 对齐成熟教务业务组织：菜单与统计工作区

用户批准按湖南强智、杭州正方的业务流程组织教务中心。本轮为已批准公共壳内的前端菜单整理及教务统计重构，未扩为全系统能力对等或四端流程验收。沿用独立工作区和分支 codex/academic-affairs-ui-20260905，checkout/base 为 6810bd6284b4d5c69a78947560f808cd7738327e；原有未提交改动全部保留。

### 设计依据与具体取舍

查阅日期 2026-09-07，以下页面已实际打开。厂商资料用于能力域和流程归属，学校资料用于真实使用流程；它们不证明厂商全部客户采用相同菜单，也不证明本项目已达到厂商完整能力。

- QZ-01：强智官网智慧教务产品 https://www.qzdatasoft.com/list-15-1.html ，产品功能段落列出培养、开课、排课、选课、考试、成绩、质量与分析等能力域（external_evidence）。
- QZ-02：强智教学管理培训材料 https://www.qzdatasoft.com/uploadfile/2021/0831/20210831030034558.pdf ，第 6–7 页文本，培养方案、执行计划与教学进程的衔接；PDF 文本已读取，截图工具超时（external_evidence）。
- QZ-03：江西职业技术大学外语教学部操作说明 https://wyjxb.jxpu.edu.cn/info/1100/2010.htm ，培养方案与执行计划操作章节。2019 年学校使用材料，仅证明该校当时流程，不冒充当前全局菜单模板（external_evidence）。
- ZF-01：正方教学管理系统 https://www.zfsoft.com/product/jxglxt.html ，产品介绍与功能组件章节，业务组件和角色服务组织（external_evidence）。
- ZF-02：正方本研一体化 https://www.zfsoft.com/product/byyth.html ，教学任务、排课、选课、考试、成绩业务链（external_evidence）。
- ZF-03：上海大学本科生院 2026 年注册说明 https://bksy.shu.edu.cn/info/1445/319644.htm ，正文“学籍管理→报到注册”；附件有验证码，未读取附件（external_evidence）。

17 个二级入口是结合本项目现有能力形成并由用户批准的方案（ai_proposal + user_decision），并非照抄厂商菜单：教务大屏、教务总览、教务基础、学籍管理、培养与课程、开课管理、排课与课表、选课管理、日常教学、考务管理、成绩管理、毕业管理、教材管理、教学资源、教学质量、教务统计、教务归档。

### 本轮落地

- 29 个原业务二级模块加已接入大屏，原合计 30 个，整理为 17 个。开课、排课、选课、考务、成绩保留独立工作区；注册、专业分流、异动、预警归入学籍；调停课、考勤、预约归入日常教学；补缓清考归考务，重修成绩归成绩；工作量办理归开课。
- 常驻三级入口为 108 项，按模块显示；“全部业务与设置”展开完整分组目录并可按旧模块名、旧名称检索。原 263 个可见来源入口中，15 个同地址、同权限、同状态的重复入口合并显示；另补登记原统计页面已有的快照深链，共 249 个可见唯一目录入口。来源记录仍保留，路径、权限及非重复能力不删除。逐项映射在 .codex-temp/academic-navigation-review/migration.json。
- 统计页把原 15 个并排维度整理为 7 个专题，每个专题只显示相关维度。总览按开学与教学准备、教学运行、学业与毕业分区；筛选标签不再挤成竖排，保留真实零值和无分母“—”，请求失败明确报错。
- 统计维度与筛选同步 URL；刷新、历史返回保留位置；统计专题高亮父入口。修复迟到响应覆盖新专题、工作量明细翻页丢教师条件、导出失败未释放提交态等前端问题。大整数标识按字符串保留。
- 浏览器发现原快照组件错误读取 shared request 已解包的数据，正常 code=0 接口却报加载失败。本轮修正列表、创建、详情、校验的消费方式；不修改请求底座、接口或后端。真实空学校现在显示“暂无统计快照”。
- 公共壳沿用 TeacherWorkspaceFrame，仅带菜单分层元数据的教务入口启用完整目录控制；其他中心原菜单不受影响。统计导出、快照维持独立权限，无权限不展示相应动作。

本轮代码集中于 academicNavigation.js、navPlan.js、TeacherWorkspaceFrame.vue、teacherWorkspace.js、AaStatsOverviewView.vue、AaStatsSnapshotWorkspace.vue、相关前端测试和生成的 navigation-surface-contract.json。未新增依赖、改锁文件、改后端或数据库；未暂存、提交、推送、合并或部署。

### 验证证据

- node --test tests/academic-navigation-workspaces.test.mjs tests/academic-shell-navigation.test.mjs tests/academic-stats-workspace-behavior.test.mjs tests/navPlan.studentStatus.test.mjs：30 通过。覆盖完整菜单路径/权限保留、重复项核对、旧词搜索、深链归属、其他中心壳行为、统计权限/失败/迟到响应/刷新/分页。
- node --test tests/academic-stats-snapshot-behavior.test.mjs tests/academic-stats-snapshot-w3-contract.test.mjs：8 通过。快照创建、详情、校验为隔离行为测试；不冒充真实库冻结操作。
- 所有本轮生产文件和行为测试定向 ESLint 通过。导航生成合同 --check 通过；route parity 覆盖 489 个正式可见入口通过。git diff --check 无新增空白错误。
- 最终 Vite 构建 46.35 秒通过，21 个公开页面预渲染通过；保留既有大 chunk 提示。日志 .codex-temp/academic-navigation-build.log。
- 浏览器使用 5290 前端、8096 后端、独立 academic_ui_batch1_e2e 教务联调库与真实学校管理员会话。选课、注册、考务、预警、工作量统计真实接口均返回 HTTP 200；报表导出返回 HTTP 200 和 xlsx MIME。未以模拟接口替代真实请求；未打开核对下载工作簿全部内容。
- 真实点击七个专题、选课维度刷新、快照列表空态、完整目录展开、按“上课时间段”搜索并进入作息安排深链通过。无业务数据增删，未执行快照冻结。权限拒绝和上下文迟到响应为自动化行为覆盖，本轮未用另一受限账号作跨角色浏览器验收。
- 1366、375、390、430 宽度原生 DOM 检查，页面、筛选栏和指标网格无横向溢出；临时视口覆盖已恢复。这不等于小程序或四端业务验收。
- 证据 .codex-temp/academic-navigation-review/browser-checks.json；有效截图 01-before.png、03-overview-native.png、06-final-native.png 已查看。06 为恢复宿主尺寸后的最终完整画面。02、04、05 的模拟截图有缩放/拼接伪影，排除不用。

交付边界：本轮完成菜单组织及统计工作区，不宣称全部约 250 张业务页面均完成重新设计，不宣称达到两家厂商所有业务能力。下一轮应沿已明确的培养→开课→排课→选课→考务→成绩→毕业业务链逐模块深化办理页面，继续保留角色、数据和权限真值。

## 2026-09-07 教务总览入口重复与学业过程空链修复

用户截图指出“教务待办”和“今日教学运行”显示同一个页面，以及“学业过程”为空入口，并要求不保留旧地址。本轮沿用独立教务工作区及 codex/academic-affairs-ui-20260905；checkout/base 6810bd6284b4d5c69a78947560f808cd7738327e。范围为教务总览子工作区前端修复，后端只读追踪，未修改后端、数据库、公共壳或权限体系。

根因：AaDashboardView 没有消费菜单已有的 panel 查询参数；所有提醒深链都渲染整张 readiness 总览。学业过程菜单和 adminMenu 搜索仍指向 /admin/academic，其根路由只会重定向回总览。读取 academic_affairs_service.py 的 _todos、_today_teaching、_today_courses、各类提醒函数及 academic_affairs_dashboard_scope_facade.py，确认既有 GET /academic-affairs/dashboard/reminders 已提供所需真实只读数据。

- 新增 AaDashboardPanelWorkspace.vue 与 dashboardPanels.js，按 panel 呈现 12 个专属工作区；无 panel 的“运行总览”保留阶段准备、阻断与风险，领导大屏分支仍优先且不改变资源包。
- 教务待办按成绩审核、成绩催交、学籍异动、预警处置和毕业复核分类；保留服务端业务对象 ID、责任角色、截止、变化、下一步与 exactRoute。分类存入 URL，刷新保留；不再把返回的明细统一截成 15 条，也不冒充服务端未返回的全量数据。
- 今日教学展示当日课表指标、课程安排、实际空态原因及正式课表入口；“进行中”明确是按课表时段判断，不冒充考勤。课程、调停课、资源占用各深链显示独立内容。
- 学业过程使用正式 /admin/academic-affairs?panel=academicProgress，关联成绩提交、异动、预警、毕业关注项及待办摘要；不新建一套学业数据真值。其余成绩、考务、预警、毕业、趋势深链均有独立标题和内容。
- 菜单与 adminMenu 搜索直接使用新地址；退役 /admin/academic 根地址，不增加 redirect/alias。旧根地址实测触发系统通用未知路径回退到 /workbench，不再回到教务总览。历史 /admin/academic/warnings、makeup-retake 等子地址仍有 message_action_registry.py、stats_service.py、academic_service.py 消费者，本轮不改后端，故这些其他业务地址暂保留，不能宣称全部旧教务地址已清空。
- 使用原 canEnterRoute 门禁决定下钻动作，真实后端仍独立校验。学院/个人 scopeRestricted 单独显示受限说明，不伪装零数据；错误清除旧事实，上下文切换/卸载忽略迟到响应。更新时间复用既有 UTC 转学校时间格式化方法。
- 重生成 navigation-surface-contract、route-index、capability-registry。能力注册表较旧基线变化较大，包含前轮已授权 17 组菜单的生成同步；未手工放宽权限或修改生成器。

验证：7 项 dashboard-panels 行为测试、17 项原导航/壳/待办合同测试通过，共 24 项。定向 ESLint 通过，导航合同 --check 与 489 个正式入口 route parity 通过，diff --check 无错误。最终 Vite 构建 31.44 秒通过，21 个公开页面预渲染通过；保留既有大 chunk 提示，日志 .codex-temp/academic-dashboard-panels-build.log。

浏览器 Product Design 工具验收使用现有真实学校管理员和独立教务库：四个主入口分别显示正确内容；另 9 个提醒深链通过正式三级菜单逐项打开并核对标题与数据加载。待办预警分类刷新保持，今日教学跳到正式课表管理；旧根地址不再注册。初始批量整页刷新受宿主加载时机影响，标题尚未加载，不计通过；后改用正式菜单完成验收。未注入假接口或新增业务数据，当前无待办/课程，仅实测真实空态及导航；有数据、权限拒绝、上下文迟到响应属于隔离行为测试，未作四端办理验收。

1366、375、390、430 宽度学业过程页面及指标网格 DOM 检查无横向溢出，已恢复原视口。证据目录 .codex-temp/dashboard-panels-review：02-academic-progress-native.png、03-today-native.png、04-todos-native.png 为已保存并查看的有效原生窄窗口截图；01 模拟桌面截图右侧截断，排除，不宣称完整桌面像素验收。verification.json 记录菜单、刷新和几何检查。未暂存、提交、推送或部署。


### 2026-09-07 二级菜单业务图标精修

本轮仅修改 academicNavigation.js 的 17 组图标配置与 TeacherWorkspaceFrame.vue 的显式图标渲染：沿用 Element Plus 图标，浅蓝圆角底座，选中项蓝底白图标。保留其他中心原有图标回退；菜单文字、顺序、地址、权限不变。

定向 ESLint 通过，academic-navigation-workspaces 与 academic-shell-navigation 共 15 项测试通过；Vite 构建 33.45 秒通过，21 个公开页面预渲染通过（既有 CSS import 顺序与大 chunk 警告仍在）。真实浏览器确认 17 个不同图标全部渲染、菜单按钮无内容溢出、教务总览切换与选中高亮正常。截图：.codex-temp/academic-navigation-review/business-menu-icons.png。本轮为局部视觉验收，不涉及业务写入或四端办理。工作分支 codex/academic-affairs-ui-20260905，HEAD 6810bd6284b4d5c69a78947560f808cd7738327e；已保存文件，未提交、推送。


## 2026-09-07 教室资源重构与整批建库

用户先要求 HTML 交互稿，再按前后端事实重构教室资源；随后明确授权“补齐必要后端，完成整批建库”。本轮故事为教务资源管理员在 Staff PC 建立教学楼、整批初始化教室、核对例外并回读结果。复用独立工作区与 codex/academic-affairs-ui-20260905，checkout/base HEAD 均为 6810bd6284b4d5c69a78947560f808cd7738327e。原组织架构、菜单、大屏等未提交改动保留；未暂存、提交、推送、合并或生产部署。

设计与实现：
- HTML：docs/03-业务模块设计/教务中心/教室资源交互稿.html。参考本项目学工 DormResourceView.vue 与《迎新2000人自动分宿与批量入住-实施方案》的楼栋→楼层→房间、批量生成、预检入库交互，未修改学工工作区。
- 外部已打开依据保留在 HTML：河北北方学院正方排课说明 PDF（教学/考试座位及场地用途）、西南交通大学教室目录（校区、楼层、类型、双容量）、强智教学运行与教室管理介绍。是能力参考，不宣称复制厂商全部能力。
- 正式入口仍为 /admin/academic-affairs/classrooms。增加教学楼目录、明确楼层筛选、教室网格/台账切换；复杂批量处理使用独立页内工作区。为适应原公共壳宽度，单间详细资料使用现有详情面板。公共壳、其他菜单和原权限码保持。
- 新建教学楼后直接进入按楼层生成；统一设置编号前缀、流水号位数、每层数量、跳过编号、类型与双容量，预检时可取消行或逐行修正。单批上限 1000 间，前端分页展示，不逐间发创建请求。
- xlsx：空模板下载→文件上传预检→错误行及核对清单下载→页内修正/重新预检→确认入库→结果下载。每批最多 1000 行、5MB，解压限制 30MB，拒绝公式；教室编号按文本保留前导零。
- 后端新增 AaTeachingBuilding、AaClassroom.building_id/floor_no 及 Alembic 20260907_aa_classroom_buildings。仅迁移独立教务联调库。沿用现有 exam_seats/is_exclusive，未增加未接线的“允许排课/排考/借用”开关。
- 教学楼编码全校唯一；与已有教室的编码、名称、校区完全一致时关联旧 ID，楼层仍为空待核对，不猜测房号。已有教学楼本轮维护楼层数，名称/编码/校区作为稳定归属保持不变。已有教室（包括历史已删除编号）在批量预检中保留，不覆盖或复活。
- 预检复用 SharedImportBatch，限定当前租户、操作人，24小时有效；确认锁定批次、核对楼栋版本，教室+审计+成功回执同一事务。重复确认返回同一结果；预检后编号冲突使整批回滚。成功回执保留7天，batchNo深链支持刷新恢复。
- 单间维护接回已有查询、创建、更新、状态、删除；补充楼层/考试座位/专用属性回填及 expectedVersion 冲突保护。失败保留输入；列表和楼栋加载丢弃迟到响应，查询失败明确报错，不冒充空数据。

验证证据：
- 独立自动化 MySQL academic_ui_batch1_test：通过 .codex-temp/run-academic-pytest.py 运行 test_aa_classroom_bulk.py、test_aa_resource.py、test_aa_teaching_resource_refactor_contracts.py，21 passed（128.90秒）。覆盖200间往返、重复确认、编号竞争整批回滚、Excel错误/前导零/公式/空文件、楼栋版本、旧ID与单间版本、不同租户与不同操作人拒绝、学生权限拒绝、原资源接口及路由权限合同。
- 前端 node --test tests/academic-classroom-workspace.test.mjs：5 passed，覆盖筛选刷新/重置、迟到请求、错误态、冲突留输入及大整数ID、楼栋汇总容量不误填为单间容量。修改文件 ESLint 通过。
- npm --prefix frontend run build 对应正式 build 脚本通过，Vite 35.28秒，21个公开页面预渲染完成；既有其他页面CSS导入顺序与大chunk警告仍在。日志 .codex-temp/classroom-review/frontend-build.log。
- Product Design/CUA 浏览器通过正常账号登录独立教务联调库 academic_ui_batch1_e2e：创建“整批建库验收教学楼”10层，在UI预检200间，单独将101改为80教学/40考试座位后整批确认；回读200间。重复生成显示0待建/200保留，确认按钮不可用，原101配置保持。
- 通过真实文件选择器上传2行Excel，其中99层报错；在清单改为2层、重新预检、确认2间。0101前导零保留，0201专用教室为真；结果文件下载按钮正常，刷新仍读取同一成功批次。另经UI将0101考试座位改为24并保存回读。
- 只读MySQL核对：本楼202间、202条BATCH_CREATE审计，成功批次分别200/2；异常房型和考试容量与UI操作一致。证据 .codex-temp/classroom-review/database-readback.json。业务终态均由正式UI/API命令产生，未用SQL改终态。
- 刷新后教室列表和教学楼请求均HTTP200，无采集到的Runtime异常。证据 browser-responses.json；截图 official-floor-grid.png、official-batch-rules.png、official-duplicate-preview.png。原生桌面与窄窗口已目视检查。375/390/430宽度DOM无页面横向溢出；模拟截图受宿主缩放截取影响，不作为移动设备像素验收，已恢复临时视口覆盖。

便捷性：200间从逐间填写/提交改为一次规则预检与一次整批确认；例外只改对应行。Excel错误可在清单修正；已有对象不重复建。HTML示例数据与正式资源独立。

边界：本轮是教务管理员教室建库故事，未重构实训室/设备/预约审批模块；学生PC与教师/学生小程序不承担教学楼建库动作，沿用原消费接口，不计作四端新增流程验收。未开展真实师生试用、微信宿主实测或生产学校导入。已有教学楼的重命名/跨校区搬迁及既有教室批量改写不在本轮新增操作范围。

### 考务打印三级菜单跳出公共壳修复（2026-09-07）
- 浏览器复现：点击“考务打印”后导航全部消失；根因为该路由注册在公共布局之外。
- 将同名、同路径考务打印路由移入教务布局子路由，保留权限与roomId深链；新增页面标题及仅作用于本页的打印媒体样式，打印时隐藏公共导航、解除容器滚动限制。正式数据核验及打印审计门禁保持原实现。
- 验证：真实菜单进入、考务安排往返、刷新、二三级菜单与页签高亮通过；打印媒体下导航隐藏且内容可见。9项相关Node测试、定向ESLint通过；前端构建38.37秒成功，预渲染21条路由。未执行正式考场数据的纸张打印，不将本次导航验收算作考务全流程验收。
- 证据：.codex-temp/exam-print-shell-after.png、.codex-temp/exam-print-shell-build.log。分支codex/academic-affairs-ui-20260905，基线6810bd6284b4d5c69a78947560f808cd7738327e；保存于工作区，未提交、推送或合并。

### 教务三级页面首屏密度重构（2026-09-07）
- Product Design 浏览器审查确认：共用标题、说明、上下文、摘要与筛选连续占高，导致主台账或待办在596px高的实际工作窗口中落到首屏以下。运行总览的首个责任区原位于 y=653，需滚动后才能开始办理。
- 统一压缩教务业务骨架、基础与排课工作区的标题间距、内容栈、卡片标题/正文、筛选和业务页签；专项压缩运行总览的操作区、阶段结论和指标，学业过程/今日教学/教务待办的说明与指标，学期管理的当前学期摘要。按钮仍保留至少34px高度和键盘焦点样式，未删除业务说明、状态、权限、筛选或动作。
- 影响范围：17个二级业务组、265个三级菜单项（244个唯一地址）；路由登记的120个视图中，113个业务视图使用同一紧凑骨架，6个纸张打印视图不受屏幕密度规则影响。
- 浏览器验收：运行总览标题区由141px降至80px，阶段结论由299px降至145px，首个责任区上移至 y=411；学期台账、学业过程、统计总览均在首屏露出主要业务。逐组打开教务总览、教务基础、学籍、培养与课程、开课、排课、选课、日常教学、考务、成绩、毕业、教材、教学资源、教学质量、统计和归档16个默认三级页，标题区均为38—80px，页面均无横向溢出。
- 验证：31项相关Node测试通过，定向ESLint与diff检查通过；前端构建32.93秒成功并预渲染21条路由。证据保存在 .codex-temp/academic-density-audit/，构建日志为 .codex-temp/academic-density-build.log。未提交、推送或合并。
- 续改“当前最需要处理”阻断/风险项：将大详情卡收敛为89px高的紧凑任务条，标题、规则类型、数量、原因、责任/截止和操作同屏排列；窄屏恢复单列换行。浏览器确认3条均为89px且无横向溢出，截图为07-issue-list-after.png；9项相关测试、定向ESLint与29.31秒构建通过。
- 续改教务台账密度：统一收紧DataTable和原生业务表格的表头、单元格、副标题及分页留白。学籍名册实测每行由82px降至66px，同一视口从约4行提升到约6行，状态标签和“发起异动”操作保持可见，页面无横向溢出。截图为08-roster-table-after.png；9项相关测试、diff检查与33.35秒构建通过。
- 续改页面内大块统计/结论区：教学任务工作台与教师任务确认的指标卡压至约87px，教师提示条约40px、筛选条约55px；成绩分析6项指标改为同一行约86px；归档预检指标条约78px，并同步收紧结论区和业务分区间距。真实浏览器在1600×900视口逐页核对，四页均无横向溢出，列表、空态或处理区均提前进入首屏；截图为09-task-batch-after.png至12-archive-precheck-after.png。19项相关Node测试、4个页面定向ESLint、diff检查与41.24秒正式构建通过；另行运行的电脑端枚举全局测试暴露AaOrgConsole.vue既有英文兜底问题，与本轮4个样式文件无关，未混入本次视觉修改。
