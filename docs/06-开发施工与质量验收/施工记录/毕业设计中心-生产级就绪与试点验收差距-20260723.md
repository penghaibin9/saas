# 毕业设计中心 · 生产级就绪与试点验收差距（2026-07-23）

> 口径短文：对齐「主链路代码就绪」与「试点服 UAT 签字前不称已可验收上线」。  
> 不替代总册；状态以 [05-毕业设计中心生产级商业化开发总册.md](../../03-业务模块设计/毕业设计中心/05-毕业设计中心生产级商业化开发总册.md) 文首「2026-07-23 状态回真」为准。

---

## 1. 达标项（主链路生产级代码就绪）

| 项 | 证据 |
|---|---|
| 批次→选题→任务书/开题→指导/中期→成果/查重/评阅→答辩→成绩→归档 | [全角色E2E业务验收报告-20260722.md](./毕业设计中心-全角色E2E业务验收报告-20260722.md) |
| 门禁与回归 | `backend/tests/test_graduation*.py`（含 e2e/reaudit/permission 等） |
| 活体多角色主线 | `backend/scripts/e2e_graduation_live_flow.py`（报告记载 pass=65） |
| 生产 Mock 闸门 | `scripts/check/check-graduation-production-gates.mjs` |
| 开题批阅 / 成果批阅 / 答辩安排 | 总册第 17 章已回真为 **implemented**（同 E2E + pytest） |

---

## 2. 差距项（仍 open，不阻断主链路试用）

| 差距 | 说明 |
|---|---|
| PDF 套打 | **partial / MVP（2026-07-23）** | 已交付最小可用路径：任务书 `POST /api/v1/graduation/gd-taskbooks/{gdStudentId}/export-pdf`（reportlab + 内置版式 / 可选启用中 TASKBOOK 模板变量填入），权限 `graduationDesign.export`，平台审计 + 域审计留痕；过程指导页「下载任务书 PDF」。**未做**：开题封面等多文种全量套打、可视化排版编辑器、校章页眉页脚正式版式套件、批量套打。台账仍以 xlsx + 附件为主。 |
| 导师评价 / 指导计划签到 | **implemented-MVP（2026-07-23）** | 导师→学生过程评价 `t_gd_student_eval` + 指导计划签到 `t_gd_guidance_plan`；PC 过程指导 Tab + `graduationDesign.guide.manage` + `assert_student_access`；学生本人签到走 `/mobile/graduation/guidance-plans*`。学院评导师（既有 `t_gd_mentor_eval`）仍独立。欠：门户深页、批量评价、小程序深度。 |
| 教师小程序深度 | **partial / MVP（2026-07-23）** | 指导教师工作台已可深链到毕设指导页分 tab：开题/成果批阅队列+逐条通过/退回、中期检查/整改复核、记录指导、答辩安排只读+待评分入口、评阅/成绩复核；另有选题审核、任务书下达/变更、答辩评分独立页。后端沿用既有 `/mobile/teacher/graduation/*`。**未完**：`build:mp-weixin` 未单独验收；门户级完整档案/批量催办未搬小程序。 |
| 学院 claim 开箱配置 | **已收口（代码+种子）** | JWT 透传 `collegeId`/`majorId`；`TeacherStudentScope` COLLEGE/MAJOR → 登录注入；`_seed_graduation_org_scope` + bootstrap A8；缺 claim 中文提示。试点仍须跑迁移 `0116_gd_student_org_scope` 并重新登录。 |
| 学生详情 Tab、AppPermissionButton 等 | **partial**，体验/组件回收债，**不阻断主链路** |

---

## 3. P3 卫生项（2026-07-23 续）

| 项 | 状态 | 说明 |
|---|---|---|
| PC `graduation.api.js` 死 mock | **已收口** | 删除未调用的 `_mockStudents` 与 `real`/`realList` mock 兜底辅助；业务方法仅 `callStrict`/`listStrict`；文件头不再把 mock fallback 写成生产路径。`getContext` 静态壳 + `scopeHint`/组织范围字段保留。 |
| 多批次学生档案边界 | **已收口（代码侧）** | `_resolve_gd_student` → `_pick_latest_non_archived_gd`：优先最近未归档档；student_id 未命中时按学号有序挑选，避免无序 `.first()`。纯函数单测 + 既有 e2e `test_mobile_resolve_prefers_latest_non_archived_gd_student`。 |
| 多批次脏数据产品化 | **仍 partial** | 同生多档的归档/唯一约束清理、管理端多批次切换 UI **未做**（本轮不发明大 UI）；门户脏数据下字段聚合偶发不一致仍见 E2E 报告 BUG-9 附注。 |

---

## 4. 对外口径（统一说法）

1. **可以说**：毕业设计主链路已达「生产级代码就绪」，可在试点服做升级初始化与 UAT。  
2. **不可以说**（在试点 UAT 签字前）：「已可验收上线」「学校正式验收通过」「生产已部署」。  
3. **不要再说**：「多子模块 implemented/partial 并存」导致主链路状态含糊——主工作区已回真；仅增强项与体验债保留 partial。

试点升级与签字步骤见：[毕业设计中心-试点服升级与UAT验收清单-20260723.md](./毕业设计中心-试点服升级与UAT验收清单-20260723.md)。
