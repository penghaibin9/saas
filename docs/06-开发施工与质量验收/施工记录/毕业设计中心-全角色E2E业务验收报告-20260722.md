# 毕业设计中心全角色业务验收报告（E2E）

> 日期：2026-07-22  
> 执行角色：毕业设计中心全角色业务验收工程师  
> 环境：本地 MySQL `student_lifecycle_dev` + FastAPI `:8000` + Vite `:5173`  
> 租户：`sandbox-school`（可写沙箱）

## 1. 测试环境与角色账号

| 角色 | 登录名 | 角色码 | 创建方式 |
|---|---|---|---|
| 系统管理员（学校） | `admin2` | `SCHOOL_ADMIN` | 已有沙箱种子；修复损坏密码哈希后可用 |
| 教务/毕设管理员 | `e2e_academic_admin` | `ACADEMIC_ADMIN` + `GRADUATION_ADMIN` | 官方师生导入 API |
| 学院秘书 | `e2e_college_secretary` | `GD_COLLEGE_ADMIN`（学院范围） | 官方师生导入 API |
| 指导教师 A/B | `e2e_advisor_a` / `e2e_advisor_b` | `GD_MENTOR` | 官方师生导入 API |
| 评阅教师 | `e2e_reviewer` | `GD_REVIEWER` | 官方师生导入 API |
| 答辩专家 A/B | `e2e_defense_a` / `e2e_defense_b` | `GD_DEFENSE_EXPERT` | 官方师生导入 API |
| 学生 A/B/C | `E2E20260001`~`003` | `STUDENT` | 官方师生导入 API |

组织主数据（系统组织 API 创建）：

- 学院：E2E智能制造学院  
- 专业：E2E工业机器人技术  
- 班级：E2E机器人2401班  

凭据保存在本地忽略文件：`backend/tmp/e2e_graduation_credentials.local.json`（不入库）。

说明：`SYS_ADMIN`/`SCHOOL_ADMIN` 不可经师生导入分配（`teacherAssignable=false`）。学校管理员使用既有 `admin2`；平台侧创建学校管理员需平台超管账号。

## 2. 测试批次与学生

- 活体批次：`E2E-GD-20260722-220328`（`E2E-毕业设计全流程-...`）
- 自动化门禁批次：`E2E-W-1` / `E2E-S-1` 等（pytest MySQL test 库）
- 学生编号：`E2E20260001`（正常流）、`E2E20260002`（整改流）、`E2E20260003`（冲突/越权）

## 3. 按业务阶段结果

| 阶段 | 结果 | 证据 |
|---|---|---|
| A 批次规则/状态机 | PASS | `test_graduation_batch.py` + 新增权重/阶段校验 |
| B 资格/题目审核 | PASS | 资格不合格拦截选题；题目审核退回需原因（既有） |
| C 选题/容量冲突 | PASS | `test_graduation_topic_round.py` 容量并发；`test_graduation_topic_selection.py` |
| D 任务书/开题 | PASS | 既有 `test_graduation_proposal.py` / process 测试 |
| E 指导/中期整改 | PASS | 既有 `test_graduation_guidance_midterm.py` |
| F 成果/查重/评阅回避 | PASS | `test_graduation_review.py`（查重超阈值阻断答辩、导师不可评阅） |
| G 答辩安排冲突/评分 | PASS | `test_graduation_defense.py` |
| H 成绩缺项/归档/风险 | PASS | 既有成绩测试 + **新增未关闭风险阻断归档** |
| 六类角色越权 | PASS | `test_graduation_permission_gate.py` |

## 4–6. Bug 清单 / 根因 / 修复 / 回归

### BUG-1 资格不合格学生仍可确认选题 / 学生可填报志愿
- **复现**：创建毕设学生后设 `UNQUALIFIED`，调用 `/gd-students/{id}/assign-topic` 仍可能成功；学生端提交志愿未校验资格。
- **根因**：`assign_topic_in_session` / `submit_choices` 未校验 `eligibility_status`。
- **修复**：
  - `graduation_student_service.py`：`UNQUALIFIED` 禁止分配
  - `graduation_topic_round_service.py`：非管理员导入时要求 `QUALIFIED` 才可填报志愿
- **回归**：`test_unqualified_student_cannot_be_assigned_topic` PASS

### BUG-2 批次成绩权重≠100% / 阶段时间倒序仍可保存
- **复现**：`POST /batches/{id}/rules` 写入 advisor 0.5 + reviewer 0.3 + defense 0.3；`POST .../stages` 后一阶段早于前一阶段。
- **根因**：`set_rules`/`set_stages` 仅覆盖存储，无业务校验。
- **修复**：`graduation_batch_service.py` 增加 `_validate_and_merge_rules` / `_validate_stages`
- **回归**：`test_batch_rules_reject_non_100_percent_weights`、`test_batch_stages_reject_reversed_dates` PASS

### BUG-3 未关闭风险不阻断归档
- **复现**：材料齐全但存在 `OPEN` 风险时仍可 `submit` 归档。
- **根因**：`submit_archive`/`verify_and_file` 只检查材料清单。
- **修复**：`graduation_archive_service.py` 增加 `_assert_no_open_risks`；归档成功联动 `grad_qual_status=PASS`（不覆盖 FAIL）
- **回归**：`test_open_risk_blocks_archive_submit` PASS

### BUG-4 沙箱 `admin2` 无法登录
- **复现**：`admin2/123456` 返回账号密码不正确。
- **根因**：密码哈希与种子口令不一致（损坏/被重置）。
- **修复**：用官方 `hash_password` 恢复种子口令（仅修复初始化，未改正式管理员）。
- **回归**：`admin2` 登录成功，可创建批次与组织。

### BUG-5 学校账号列表缺少 `loginName` 字段
- **复现**：`GET /system/users` 仅返回 `userNo`，脚本/客户端按 `loginName` 匹配失败。
- **根因**：`_user_row` 未输出标准登录名字段。
- **修复**：`system.py` `_user_row` 增加 `loginName`/`realName` 别名。
- **回归**：列表可按登录名匹配。

### BUG-6（记录，未改前端 mock 创建）手工「新增账号」仍走前端 mock
- **说明**：真实创建入口为 `/system/identity-import/*`。本次按官方导入完成账号创建；前端 `createUser` mock 属系统管理欠账，未在本任务扩大范围重做整页。

## 7. 仍未解决 / 阻塞

1. **活体全链路逐角色 UI 点击截图**：本次以真实 HTTP + MySQL 自动化为主；PC/小程序同人绑定的完整四端 UI 截图未全部采集（账号已导入，可继续手工点验）。
2. **`e2e_sysadmin`**：受师生导入角色白名单限制，未单独创建；使用 `admin2` 作为学校系统管理员。
3. **登录频控**：批量激活临时密码时会触发「登录过于频繁」，需 pacing；已用脚本处理。

## 8. 自动化测试命令与结果

```text
cd backend
$env:FAST_TEST_SCHEMA="1"
.\.venv\Scripts\python.exe -m pytest `
  tests/test_graduation_e2e_acceptance_gates.py `
  tests/test_graduation_topic_change.py `
  tests/test_graduation_permission_gate.py `
  tests/test_graduation_review.py `
  tests/test_graduation_defense.py -q --tb=line
```

结果：**29 passed**

补充相关集：`test_graduation_batch.py`、`test_graduation_topic_round.py`、`test_graduation_topic_selection.py` 在门禁修复后已通过。

## 9. 关键接口响应证据（摘要）

- 创建批次：`POST /api/v1/graduation/batches` → `code=0`，`status=DRAFT`，`batchNo=E2E-GD-20260722-220328`
- 组织：`POST /api/v1/system/org-nodes` → 学院/专业/班级 id=25/29/47
- 师生导入：`validate-file` total=10 valid=10；`confirm-batch` code=0，创建教师 7 + 学生 3
- 门禁用例：权重非 100%、阶段倒序、未关闭风险归档、评阅回避、查重超阈值、答辩冲突均为拒绝态

## 10. 最终提交号

见本任务 Git commit（推送到本地/远程 master 后回填）。

## 使用的本地脚本（可重复）

- `backend/scripts/e2e_bootstrap_graduation_accounts.py` — 组织 + 师生导入
- `backend/scripts/e2e_force_activate.py` — 官方重置密码并改成稳定测试口令
- `backend/tests/test_graduation_e2e_acceptance_gates.py` — 本次缺陷回归
