# 2026-07-17 教务中心 · 正方(ZFSOFT)对标 7 功能补强 + 电脑端组件复用收口

> 状态：功能与电脑端 **implemented + 已并入 master 并推送**；小程序端构建验证与「多角色评教评价者填报页」为在建/欠账（见文末）。
> 事实源：commit + 迁移 + MySQL 测试 + vite build，均可核验。

## 一、对标来源

正方软件《教学管理信息服务平台》**学生用户操作手册(74页)** + **教师用户操作手册(61页)**（甲方 2026-07-16 提供，桌面 `正方教务系统学生端.pdf` / `正方教务系统老师操作手册.pdf`）。
注：桌面另一份 `正方操作手册.pdf` 经 MD5 证实是**强智**手册重名，非正方。

审计 8 个候选功能后甲方选「能补的都补上」（调停课审计确认已全有，不计）。

## 二、7 个功能补强（feat/aa-qz-scheduling → 已并入 master）

| # | 功能 | 对标正方 | 迁移 | commit | 测试 |
|---|---|---|---|---|---|
| 1 | 成绩分析统计表(按课程/班级分组+优秀率/平均分/最高最低+Excel导出) | 教师端4.10 | 无(读侧) | 78663d9 | MySQL g8/g9 绿 |
| 2 | 成绩分项扩展(平时+期中+期末按比例) | 教师端3.1/3.3 | 0094 | 6ff53a6 | MySQL g10/g11 绿 |
| 3 | 课堂考勤跨堂次统计+点名类别+PC查询页 | 教师端2.4/4.19 | 0095 | 98384ea | MySQL 5 绿 |
| 4 | 学生成绩复查申请全链路(学生发起→教务复审维持/调整回写+通知) | 学生端3.12/教师端3.11 | 0096 | 4ef7548 | MySQL 绿 |
| 5 | 多来源教学评价(督导/同行/自评+加权综合分) | 教师端5.x | 0097 | 662c415 | MySQL 绿 |
| 6 | 教师工作量申报-审核(教师申报→教务审核→并入统计) | 教师端1.18/1.19 | 0098 | 3788ec2 | MySQL 绿 |
| 7 | 教材学生自助(我的教材/费用查询+本人签收) | 学生端6.13/6.14 | 无(复用) | c9cf7bd | MySQL 绿 |

迁移验证：清空测试库跑完整链 0001→0098 rc=0；单列/单表迁移用「删列/表→stamp 上一版→upgrade→验→downgrade→再 upgrade」约 15 秒法，均 rc=0。

## 三、电脑端公共组件复用收口（甲方反馈「合理运用公共组件、别自己写」）

本轮新建/改动的 5 个电脑端页原图快用了手写 `<table>/<select>`，返工为公共组件（照抄同模块已上线页 AaEvaluationConsoleView 的组件契约）：
- 成绩分析/考勤统计/成绩复查/工作量审核/教学评价 5 页 → DataTable / AppSelect / AppTextInput / AppTextarea / AppButton(从 @/components/ui)；行内动作用 `mp-link`；数字列 `align:'center'`。
- **增强公共组件而非退回手写**：给公共 `DataTable` 新增可选 `rowClass`(向后兼容)，还原考勤「旷课≥3 整行标红」（换组件时丢的唯一功能）。
- **与 master 既有改动调和(不盖别人的活)**：master 已给成绩分析页分数段分布加了真图表 `AppG2Chart`、并把 DataTable 字号改为 CSS 变量；本轮把这两处并进来做成无损超集。

commit：990a2d8(手写→组件) + 206aea8(DataTable rowClass+标红还原+对齐) + bc8671f(调和 AppG2Chart+字号)。
应用到 master：`git checkout feat/aa-qz-scheduling -- <6文件>` → master commit **db54107** → `git push origin master`（快进、非强推，`1be80a7..db54107`）。

## 四、验证证据

- **后端**：各功能 MySQL 测试全绿（见上表；测试从 worktree/backend 用主 venv 跑，`import app` 正确解析到 worktree）。
- **前端(电脑端) vite build**：教务副本 17.59s、master 副本 50.69s，均零编译错误（仅既有 chunk 体积警告）。
- **迁移**：清库全链 0001→0098 rc=0。
- **Git**：7 功能 + 组件改造均已在 origin/master。

## 五、欠账 / 未验证 / 待决（如实登记）

1. **小程序(uni-app)前端构建未跑** —— 功能 #3/#4/#7 改动了小程序页（教师考勤点名类别、学生复查/教材自助等），全程只跑了电脑端 vite build，小程序端 `uni build` 一次未跑。→ 本轮正在补（装 miniapp 独立依赖后 `npm run build:mp-weixin`）。
2. **多角色评教「评价者填报页」缺失** —— 后端多来源提交端点 `/evaluation/submit` 与管理端任务生成(genType)已具备，但**全仓无任何评价者(学生/教师)填报页真正调用提交**，评教是「只有管理端编排、无填报入口」。要真「启用上线」需补评价者填报页 + 任务归属 scope。→ 甲方 2026-07-17 已明确要做，在建。
3. **正式工作量核算(绑职称/薪酬)** 未做，仅教务侧申报-审核+基础统计（正式核算属人事系统，甲方确认边界到此）。
4. **feat/aa-qz-scheduling 工作线** 内容已全部并入 master；工作线本身是否推送/清理见收尾。

关联：[[project-aa-zf-benchmark-7features-2026-07-16]]、[[feedback-reasonable-component-adoption]]。
