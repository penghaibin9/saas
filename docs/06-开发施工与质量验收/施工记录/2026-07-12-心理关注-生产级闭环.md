# 施工记录 · 心理关注 生产级闭环（2026-07-12 夜）

> 模块：学工中心 / 心理关注（sa-mental，强敏感红线）。代码事实基线 `c1cd650`（其上仅文档提交 `326a475` + 风险模块提交 `b8ffb5b`，均不影响心理域代码事实）。
> 本次为**代码施工**（甲方明确授权：按国家制度/权限顶层设计/真实代码/现有施工卡编码，外部厂商证据欠账不阻断今晚施工）。仅改心理域相关代码/测试/迁移 + 本施工记录，独立提交，禁 push。

## 1. 改动内容

| 文件 | 变更 |
|---|---|
| `backend/app/models/affairs_mental.py` **(新)** | `PsyReferral`(`t_affairs_psy_referral`)：心理转介与回访状态机（REFERRED/FOLLOWING/ESCALATED/CLOSED）；note 涉密明细；risk_id 回链危机风险；无诊断字段。|
| `backend/alembic/versions/0044_13a_psy_referral.py` **(新)** | 建表迁移，`down_revision=0043_gd_final_attachments`，幂等（表存在则跳过），downgrade 直接 drop。|
| `backend/app/services/affairs_mental_service.py` **(新)** | 心理服务：`psy_scope_ids`(PSY_STUDENT 逐生授权，键派生同 resolve_teacher_scope)、`_can_view_detail`(SEC-2 排除学工处管理员)、`list_attention`(名单·恒摘要)、`get_referral`(明细须原因+SENSITIVE_VIEW)、`student_summary`(仅需关注标记无明细)、`create/follow/escalate_crisis/close`(状态闭环)、`stats`(仅聚合)。危机升级建 source=MENTAL 风险记录接风险中枢、幂等。|
| `backend/app/api/v1/student_affairs.py` | 新增 `/mental/*` 8 端点（list/referrals CRUD/summary/stats），全部 `require_permission("studentAffairs.risk.psyDetail.view")`（统计=`stats.view`、摘要=`risk.view`）。|
| `backend/app/api/v1/campus_service.py` | **SEC-1**：`/mental-records` 由零门禁 `get_current_user` 改为 `require_permission("studentAffairs.risk.psyDetail.view")` + `reason` 参。|
| `backend/app/services/campus_service_service.py` | **SEC-1/SEC-2**：`list_mental` 增 `user/reason`；counselorNote 明细默认遮蔽，仅授权角色(心理老师/校·平台超管，**不含学工处管理员**)+原因(≥5字)方明文并写 SENSITIVE_VIEW。|
| `backend/app/models/__init__.py` | 注册 `PsyReferral`。|
| `backend/tests/test_affairs_mental.py` **(新)** | M1-M8 端到端。|

> 复用（未重建）：`STUDENT_AFFAIRS_ADMIN`/`PSYCHOLOGY_TEACHER` 权限授予集、`sa_admin01`/`psych01` mock 身份、风险中枢 `t_affairs_risk_record` —— 均在风险模块提交 `b8ffb5b` 已落地，本次直接复用。

## 2. 安全红线落地（可核验）

- **SEC-1**：`/mental-records` 不再零门禁；无 `psyDetail.view` → 403，未登录 → 401（test M5）。
- **SEC-2**：`STUDENT_AFFAIRS_ADMIN` 无 PSY_STUDENT 专项授权 → 心理明细数据范围拒绝 403 NO_DATA_SCOPE（test M6）；`_can_view_detail` 不含学工处管理员。
- **PSY_STUDENT 逐生授权**：心理老师仅见授权学生（test M7：见 P001 不见 P002）。
- **明细最小暴露**：列表恒仅摘要 noteMasked=true（M3）；明细须原因(≥5字)方明文 + 写 `t_security_audit_log(SENSITIVE_VIEW)`（M4）。
- **危机接风险中枢**：升级建 source=MENTAL·CRITICAL 风险记录、回链 risk_id、幂等（M2）。
- **无自动诊断**：等级/事由人工登记，事由摘要必填≥5字，无算法诊断（M8）。
- **转介闭环**：REFERRED→FOLLOWING→CLOSED；或 →ESCALATED（M1）。业务流转→`t_affairs_audit_trail`，敏感查看→`t_security_audit_log`。

## 3. 验证（实际执行）

- **后端测试**：`pytest tests/test_affairs_mental.py` → **8 passed**（0:02:25，真实 MySQL）。回归 `pytest tests/test_campus_service.py tests/test_affairs_risk.py` → **21 passed**（0:06:07，SEC-1 改动未破坏既有心理审计用例，风险模块无回归）。覆盖：401/403001/403002(数据范围)/敏感字段/状态机/幂等/跨授权学生。
- **前端 build**：`npm run build` → **✓ built in 10.08s**（exit 0）。
- **迁移**：`alembic heads` = 单一 `0044_13a_psy_referral`（链在 0043 之后）。测试用 `metadata.create_all` 建表（含新表）。
- **API smoke**：FastAPI TestClient 端到端覆盖。

## 4. 数据库 / 迁移 / 回滚

- 新增表 `t_affairs_psy_referral`（迁移 0044，幂等）。无数据变更、无破坏性改动。
- **回滚**：`git revert <本提交hash>`；数据库 `alembic downgrade 0043_gd_final_attachments`（drop t_affairs_psy_referral）。

## 5. 遗留与后续（诚实登记）

- **前端页面**：本轮为**后端安全闭环**（心理域后端此前整域缺失，本次从零建成 API + 表 + 测试）。心理关注 5 个 PC 页面（名单/预警摘要/转介回访/危机升级/统计）**尚未建 Vue 页面**——后端 `/mental/*` 契约已就绪可直接对接，页面为后续独立前端工作（非本次已完成项，不得记为已实现）。
- **PSY_STUDENT 授权维护界面**：逐生授权当前经 `t_teacher_student_scope`(scope_type=PSY_STUDENT) 数据行；授权维护 UI 为后续项。
- **旧 `t_cs_mental_record` 与新 `t_affairs_psy_referral`**：本次 SEC-1 加固旧表读取门禁；两表长期归并（cs_student_id 与真实 student_id 对齐）留后续数据治理。
- 既有 `datetime.utcnow()` DeprecationWarning 为历史存量，非本次引入。
