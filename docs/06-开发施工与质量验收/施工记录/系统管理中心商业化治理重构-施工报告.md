# 系统管理中心商业化治理重构 — 施工报告

> 最近更新：2026-09-13。当前施工结果见末尾“首批实施”节；2026-07-25内容保留为历史记录，其完成表述不代表本轮学校自治改造已经完成。

## 一句话结论

商业化治理控制面主干已落地并可提交验收：模块清单/能力注册表/合同检查、模块门禁、有效权限、结构化数据范围（读写对齐）、组织主数据统一写入、敏感字段加密写入收口、假同步 SUCCESS 已杀、系统管理九工作区、组织编码租户唯一索引 Expand。仍有登记豁免与空适配器能力缺口，不得对外宣称“全仓敏感字段历史明文已回填”或“外部同步已可真跑通”。

## 审计问题核验

| 问题 | 状态 |
|---|---|
| employment 复用 internship 门禁 | 已修 |
| orientation / 旧 academic 缺门禁 | 已修 |
| 自定义角色前后端权限不一致 | 已修（effective permissions） |
| Role.remark 数据范围主写入 | 已改结构化写入；**登录读路径已对齐 resolve_role_scope_code** |
| 手机号明文写入 encrypted | **系统/迎新/就业/学业/实习企业/毕设导师/家长授权写入口已 encrypt_field**；历史明文仅扫描不自动回填 |
| 同步任务假 SUCCESS | 已改为 PENDING（KNOWN_SYNC_ADAPTERS 仍为空） |
| 审计固定 SYSTEM | 已按 action/path/moduleCode 分类 |
| 模块开关未接入门禁 | 已接入 module_access_service |
| permissionGate 未覆盖 SYSTEM/EMPLOYMENT | 已扩 |
| auth 读范围与写脱节 | **2026-07-25 已修** |
| 0132 空串唯一索引隐患 | **0132 建索前规范化 + 0134 补丁** |
| 组织旁路 | delete/开局/实施已接入；sandbox/分流写死豁免清单 |

## 组织主数据

### 已统一

- `POST/PUT /api/v1/system/org-nodes`、状态停用、重复编码只读
- 教务 `orgs/colleges|majors|classes` 的 create/update/**delete**
- 学校开局 `school_onboarding_service`、实施安装 `system_implementation_service`（会话内 `apply_org_node_in_session`）

### 正式豁免（`ORG_WRITE_BYPASS_ALLOWLIST`）

- `sandbox_service`：演示沙箱种子
- `academic_affairs_major_split_service`：专业分流业务内建班

## 数据库

- `0132_org_code_unique_expand`：查重 → 空串改 NULL → 租户+编码唯一索引
- `0133_merge_counselor_org_uk`：合并 counselor 与 0132 双 head
- `0134_org_code_empty_to_null`：已执行旧版 0132 的环境补丁（空串→NULL）
- 生产未在本任务中连接或迁移

## 能力注册 / 路由索引

- `generate-capability-registry.mjs` 落盘**完整** `shared/generated/route-index.json`（含 exact/patterns/redirects/aliases）
- `routeExists` 基于真实路由索引（含可选参数、嵌套、redirect），非字符串前缀猜测
- 合同检查：正式侧栏节点不得 UNRESOLVED；豁免须带合法原因

## 未完成 / unresolved（诚实口径）

- `KNOWN_SYNC_ADAPTERS` 为空：无外部适配器时同步只能 PENDING/FAILED
- 历史手机号明文不自动回填（`scan_sensitive_fields.py` 只读扫描）
- 临时授权依赖 loginName 匹配的边界加强仍待后续
- 豁免清单外禁止新增组织旁路

## 验证证据（本修复轮）

以实际命令结果为准（见同轮测试输出）：治理专项 pytest、合同检查、Alembic heads、route-index 全量落盘。

## 2026-09-13：学校自治与入口收拢规划记录

- 用户决定：新校交付时预配角色、菜单、动作和默认范围；客户可自行修改本校角色；普通角色升级不覆盖学校配置；P4使用三类代表身份，不细拆多套管理员流程。
- 本轮专题：[04-学校级系统管理-8组26项能力目录与实施规范](../../03-业务模块设计/系统管理中心/04-学校级系统管理-8组26项能力目录与实施规范.md)。补齐26项施工卡摘要、52项逐项去向、角色预配、权限修改、管理员接管、模板更新、一校一库归属、四端消费及P0—P4退出条件。
- 基线：`main` / `bc596b6bf24037e9035e3ec606777001cec3208b` 加原有工作区改动；云端一校一库版本未核对。本轮没有改应用代码、迁移或运行权限，未提交、推送、合并或部署。
- 纠正：8组26项属于当前产品整理方案而非行业强制标准；现有52项不能直接当冗余删除；已安装角色模板409的具体业务原因仍待核；主管理员授权能力不等于敏感业务全权；日历、业务表单、国标库、存储及通用交换能力保留可达入口。
- 证据边界：沿用前轮页面/日志与当前代码事实，未重新做写业务验收。本次只校验文档完整性、52项覆盖、26项编号与本地链接；不以文档检查冒充pytest/E2E通过。
- 状态：`draft / 施工规划已记录`。当前文档足以明确施工目标与顺序；完整三级权限/API实例表、云端合同及适用外部证据须在相应阶段补齐后才能标design_ready。旧节的历史实现/验收结论不代表本轮完成。

## 2026-09-13：首批实施——权限总览恢复、八组入口与初始化保留

### 本轮业务结果

学校管理员可以重新查看权限目录和已发布学校角色模板；系统管理导航收为八组，原52个页面入口保留。数据字典仍在“学校配置 → 数据字典”。概览、权限目录、模板独立加载，一项服务失败时可单独重试，不再把整个工作区变成错误页。IAM页去除重复身份与数据范围条，标题和快捷操作压缩。

导入师生、重复实施、迎新账号开通会共同调用角色初始化。该函数现在只补缺失角色，不覆盖现有名称、备注、版本和启停状态，也不复活软删除角色。新校缺失角色仍可创建；角色不可用时成员分配继续失败，不绕过学校停用决定。这是初始化重放保护，不等于已完成模板升级保护或普通预设角色转本校角色。

### 根因与修改范围

| 修改 | 证据与边界 |
|---|---|
| 学校模板目录409 | 沙箱存在错误标记为TENANT的PLATFORM_COMMERCIAL模板，目录逐项严格检查时报ROLE_TEMPLATE_PLANE_VIOLATION。`backend/app/modules/system_admin/services/school_iam_authority_projection_service.py:template_catalog`在目录发现阶段排除非学校角色；指定访问平台/企业模板仍拒绝，学校模板规范化权限与摘要检查仍严格执行 |
| IAM局部恢复与上下文 | `frontend/src/modules/system/components/workspace/SystemIamGovernancePanel.vue`及`SystemIamWorkspaceView.vue`：三个区块独立状态/重试；请求栅栏隔离切换上下文和卸载后的迟到响应；查询失败计数显示未取得，不伪造零；成员大整数编号以字符串提交 |
| 八组导航与读取权限 | `systemManagementCatalog.js`合并实施容器到系统概览，保留原页面key/path；模板/权限页读取用role.view，品牌读取用config.view，操作/登录审计用audit.view；`system.routes.js`对齐数据交换viewOwn与文件治理view。没有增加写权限或给管理员配置通配符 |
| 导航合同 | `shared/contracts/navigation-surface-contract.json`通过既有生成器更新；584条记录，学校556/平台28，摘要38bed6dda2dee0d89b74b2286ef56990593633c046ada840e813387b72b26bc6。包含工作区原有的字典入口和实习大屏顺序改动，不把它们误报成本轮新实现 |
| 初始化不覆盖 | `backend/app/services/saas_role_service.py:ensure_builtin_roles`只创建缺失行，保留已有角色，CUSTOM占用系统编码仍报冲突。旧report.restored字段保留且为0，调用方事务提交职责不变；未改成员授权命令或权限解析核心 |
| 启动脚本 | 既有未跟踪`scripts/dev/start-sandbox.ps1`读取进程状态JSON显式用UTF8，解决Windows PowerShell读取中文目录造成的JSON解析失败；仅按脚本既有进程归属检查重启后端 |

### 基线与环境

- 当前分支、HEAD与实际checkout：`main` / `bc596b6bf24037e9035e3ec606777001cec3208b`，加本轮及原有未提交改动；无新提交，未推送、合并或部署。云端PR/head/base未核对。
- 经`check-sandbox-runtime.py`核对：`student_lifecycle_runtime_20260902`，`sandbox-school`，tenantId `1000000000000000007`，499表，迁移head `20260910_phone_recovery_freeze`。本轮无数据迁移、清库、模板重发或直接授权补写。
- 实库只读回查：学校角色27、成员21302、可分配权限目录656、学校模板27，角色来源缺失计数0。目录656不代表管理员已具备656项操作权限。

### 已执行验证

| 检查 | 结果 |
|---|---|
| `pytest tests/test_school_iam_catalog_recovery.py tests/test_control_plane_role_template_plane.py -q`（backend，ENV与ENVIRONMENT均为test） | 10通过；包括误分类模板隔离、显式平台/企业访问拒绝、学校模板规范化权限与摘要漂移拒绝 |
| `pytest tests/test_saas_role_templates_unit.py -k 'initialization or unavailable_preset' -q`（相同test环境） | 6通过、35未选择；包括首次安装幂等、名称/备注/状态/删除标记保留、CUSTOM编码冲突、不分配不可用角色 |
| frontend所有`tests/system*.test.mjs`及`navigation-surface-contract.test.mjs` | 173通过、0失败、0跳过；日志在忽略目录`.codex-artifacts/system-management-audit-20260913/implementation-frontend-tests.log` |
| 本轮前端文件定向ESLint | 通过 |
| `npm --prefix frontend run build` | 通过；保留既有CSS @import顺序与chunk体积等警告，没有宣称无警告 |
| `node scripts/generate-navigation-surface-contract.mjs --check` | 通过 |
| 当前沙箱浏览器回查（学校管理员已有会话） | IAM目录恢复；模板页加载学校角色；学校配置下数据字典加载12类目录；五大中心及八组导航可见。已用1440×900检查并恢复原视口 |

上述pytest是纯单元/合同验证，不运行会清理数据库的db_mode fixture；不能当作MySQL角色写入闭环验收。浏览器未进行角色授权写入，不代表撤权、跨校、模板升级及四端闭环通过。H5与微信宿主本轮均未重验。

### 尚未完成与下一施工顺序

1. **P0仍有关键合同缺口**：学校管理员可授予集合当前仍受本人基础操作权限限制。实库SCHOOL_ADMIN模板646项、目录656项，差异为10个`systemAdmin.phoneBinding.*`权限；不得自动补齐敏感权限。须核对手机号治理发布合同及云端一校一库权限来源。
2. **P1学校自治未完成**：已有SYSTEM角色仍依赖已发布标准模板；已有CUSTOM复制/权限保存链路有来源版本与规范化权限，但普通预设角色不能仅改role_type就视作可编辑。须先核对领域身份guard、业务关系及云端模板分发合同，完成预检和受控迁移，再验证学校编辑、刷新回读、升级不覆盖、另一学校不变与最后管理员保护。
3. **P2部分完成**：八组与52入口保留已落地；26项能力工作区的内部合并、编号规则真实能力、字典全集/所有消费者、其余菜单与接口读取权限差异仍需逐项收口，不删除成熟能力凑数量。
4. **P3部分完成**：IAM标题及错误恢复已改；其他系统管理工作区仍按施工卡逐个压缩，不能将单页处理算全模块完成。
5. **P4未验收**：沿用学校主管理员、受限管理员、普通教师三类代表身份；学生仅验证接收侧。保存回读、撤权旧会话、跨学校、并发冲突和升级不覆盖需要真实隔离MySQL与同一业务对象证明。

状态：`in_progress / 首批修复验证通过 / 整体未验收`。下一步先补P0授予边界与云端合同，再推进P1本校角色实例化；不以八组菜单已显示代替整套学校自治交付。

## 2026-09-13：继续施工——从模板创建并维护本校角色

### 已完成的业务故事

学校管理员在“学校角色与成员 → 新增角色”选择已发布模板，查看将带入的权限数量，创建本校角色；进入权限工作区调整权限、填写原因并保存，刷新后读到同一角色的新版本。成员不自动加入，原有教师业务身份不替换。

- `SystemRoleListView.vue`默认选择“带入模板权限”，仍提供“空角色，稍后配置”。提交模板版本和摘要；模板信息不完整时不可当作就绪模板，冲突时保留表单并防连续重复提交。
- `system_router.py:create_system_role`复用原创建入口和事务，增加`initialPermissions=TEMPLATE`显式模式。旧调用未传此字段仍创建空角色。读取规范化模板权限并检查操作者永久授予上限、Custom Role可分配目录和Permission行；前端传入的权限数组不作为模板来源。
- 模板版本/摘要必须和操作者读取的一致；不匹配返回409。Role、DataScopeRule、CustomRoleSource、RolePermission及关键审计同事务提交。角色固定来源版本，`automaticUpgrade=false`，后续权限由本校规范化RolePermission读取；不自动绑定成员。
- 角色创建、复制、权限修改的审计显示具体业务名称，不再统一显示“业务操作”。

### 实测发现及沙箱补齐

第一次浏览器创建返回409，回查没有残留角色。原因是当前沙箱的全局权限基础表只具备活动目录中的58项，而任课教师模板引用的34项均缺少对应Permission行。模板存在不等于学校自定义角色可写入。

核对仍为约定499表沙箱后，使用既有`permission_catalog_reconciliation_service.reconcile_permission_catalog`执行一次本地维护对账，新增缺失基础定义659项，活动目录共717项，缺失归零。717包括平台/企业目录，学校仍按656项可分配集合及后端授予上限过滤；这不是给学校增加717项权限。现有Permission行ID和内容保留，没有重发模板或修改师生授权。

对账前后RolePermission/UserRole总数均为68/22320。之后浏览器创建的测试角色单独新增规范化权限记录。对账前备份与结果位于忽略目录：

- `.codex-artifacts/system-management-audit-20260913/permission-catalog-before.json`
- `.codex-artifacts/system-management-audit-20260913/permission-catalog-reconcile.json`

### 验证结果

| 验证 | 实际结果 |
|---|---|
| 后端定向`pytest tests/test_school_role_template_creation.py tests/test_school_iam_catalog_recovery.py -q` | 16通过；涵盖带入、空角色兼容、模板冲突、授权拒绝、目录缺失、审计失败回滚及非学校模板边界。使用纯单元替身，未调用清库fixture |
| 前端系统系列及导航合同 | 178通过；本次角色表单功能变化经diff复审后更新该文件的两项旧视觉冻结锚点，原鉴权/忙碌/上下文守卫仍在，新增行为测试覆盖写入合同 |
| 后续角色行为与公共文案检查 | 12通过（含新增审计名称检查）；定向ESLint通过 |
| 当前沙箱真实UI与MySQL | 创建“沙箱联调角色0913”，角色ID108，CUSTOM_6C4E81F1；任课教师模板第3版带入34项。通过UI移除消息中心权限并保存，刷新后为33项、角色版本1、来源版本仍3，模板仍34项；成员0人；创建与修改两条留痕均可查询 |
| 前端构建 | `npm --prefix frontend run build`通过；有既有chunk体积等警告。输出日志`.codex-artifacts/system-management-audit-20260913/role-template-build.log` |

日常沙箱保留这条未分配成员的联调角色和留痕，便于复核。未修改其他角色或任何成员绑定；未提交、推送、合并或部署。基线仍为main / bc596b6bf24037e9035e3ec606777001cec3208b加既有和本轮改动。

### 整体边界与接续

这次完成的是“从模板建立独立本校角色 → 调整权限 → 保存回读”。**尚未完成**旧SYSTEM角色原地转为学校可编辑角色、新学校整套自动实例化、受限管理员和教师/学生接收侧闭环、跨校验证与实际发布新版模板后的升级验证。现有来源固定机制和只读代码检查不能冒充已执行升级验收。

已只读核对任务《一校一库改造施工》的最新记录：最新交付内容仍是复审规划，没有可核验的分库源码/真实MySQL验收结论。本轮没有把该规划当成已上线架构，也没有切换数据库或执行3,000人缩量。下一施工仍需以实际本地分库合同为依据，明确学校角色实例化与主管理员授予边界；继续保留三类代表身份的P4口径。

## 2026-09-13：继续施工——旧业务角色转为本校维护（代码完成，实库验收阻断）

### 本轮故事与实现

学校管理员在原预设业务角色的权限页选择“转为本校维护”，核对当前角色、成员数和权限数，填写原因后转换，随后仍在同一角色页面调整权限。目标是保留原角色编号、编码、成员和教师业务关系，模板升级不覆盖本校设置。

- 新增`school_role_adoption_service.py`，沿用现有Role、CustomRoleSource和规范化RolePermission。转换读取同一学校角色并加行锁，核对角色版本、已发布模板版本及权限摘要；权限只取服务器规范化模板。超出操作者永久可授予集合、来源冲突、目录缺失均拒绝。
- 切换为CUSTOM前，把原先不作为SYSTEM权威的旧RolePermission残留对齐到完整模板快照，避免隐藏残留变成额外授权。没有新增权限；不改UserRole、默认范围或教师关联表。来源固定版本，记录`SCHOOL_ADOPTED_SYSTEM_ROLE`与`automaticUpgrade=false`；角色版本递增，关键审计与业务同事务。
- `system_router.py`显式注册`POST /system/roles/{role_id}/adopt`，沿用`systemAdmin.role.config`与永久授权检查。角色详情带转换预检字段。学校主管理员、系统管理员、安全审计员、学生及非学校角色不能走普通业务角色转换。
- 初始化与沙箱角色整理识别有真实来源绑定的已转换角色并保留；伪装CUSTOM占用预设编码仍拒绝。没有批量转换现有角色，也未改动全局鉴权解析核心。
- `RolePermissionPanel.vue`增加确认入口，提交读取到的版本和摘要，不提交自选权限。写入中禁重复，冲突/未知结果要求重新读取，切换上下文丢弃迟到响应；缓存失效失败报告“已提交但需核对”，不重新执行转换。
- 角色转换审计显示“转为本校维护”。原有自定义角色编辑及受保护角色只读行为保留。

### 验证与边界

| 检查 | 结果 |
|---|---|
| `pytest tests/test_school_role_adoption.py tests/test_school_role_template_creation.py tests/test_school_iam_catalog_recovery.py -q` | 35通过；纯单元替身。覆盖受保护身份、快照冲突、临时授权拒绝、目录/来源/审计失败回滚、残留权限移除、重复转换拒绝、缓存部分失败与初始化保留 |
| `pytest tests/test_saas_role_templates_unit.py -k 'initialization or builtin_role_initialization or binding_unavailable' -q` | 6通过、35未选择；未使用清库fixture |
| frontend所有`tests/system*.test.mjs`及导航合同 | 185通过；包含新增6项转换行为测试。原视觉冻结锚点仅在复审新业务行为后更新RolePermissionPanel两项，未删除鉴权断言；日志`role-adoption-tests.log`位于本任务既有忽略产物目录 |
| 定向ESLint、前端build | 均通过；build 34.08秒，有既有chunk体积警告。日志`role-adoption-build.log` |
| 本地启动防护 | `python -m unittest tests.test_sandbox_runtime_profile -q`两项通过；PowerShell AST语法检查通过 |
| 真实MySQL与浏览器 | **未通过验收**：连接预检已失败，尚未进行任何角色转换写入。页面观察为登录请求卡住，后端记录登录503及Redis超时 |

### 环境阻断与恢复记录

Docker引擎首先返回HTTP 500；使用官方`docker desktop restart --detach`尝试恢复后仍启动失败。Docker日志明确指向`C:/Users/10850/AppData/Local/Docker/wsl/disk/docker_data.vhdx`的虚拟磁盘元数据打开失败，错误为挂载点与设备所属卷不一致；只读检查文件仍存在，约67.8GB。仅文件存在不能证明磁盘内部数据完整，未对此作保证。

未重置Docker、删除/重建容器、迁移磁盘或重建数据库。已在原一键启动脚本补上PowerShell 5原生命令stderr捕获，数据库预检增加连接/读写超时，避免依赖故障无限挂住。启动仍明确报错，不会静默换库或开始清库。

当前基线仍为`main` / `bc596b6bf24037e9035e3ec606777001cec3208b`加既有及本轮未提交改动；没有commit、push、merge或生产部署。旧角色转换尚未在日常沙箱生效，上一轮角色ID108的验收不得挪作本轮证据。

下一步先恢复Docker虚拟磁盘可用性，再在原沙箱核对无成员预设业务角色，完成“转换 → 权限一致 → 本校编辑 → 刷新回读 → 初始化不覆盖”的真实验收；实际模板发布升级、跨校与教师接收侧仍属P1/P4待验。状态维持`in_progress / 代码已保存 / 实库验收阻断`。

## 2026-09-13：桌面一键启动恢复

用户恢复Docker后，实测桌面快捷方式指向正确的`launch-local.ps1`。启动失败发生在数据库预检：MySQL日志显示02:55:53 UTC开始启动、02:59:30 UTC才就绪，超过原来60秒等待；快捷方式直接运行PowerShell，未处理的错误会随窗口关闭而难以读取。

- `check-sandbox-runtime.py`对可重试的连接故障最多等待300秒，每15秒报告等待状态，超时给出容器名称及重试提示；错误学校、错误数据库、迁移不一致仍直接拒绝。沿用原连接/读写超时，不新建数据库。
- `start-sandbox.ps1`增加Docker、原容器、数据库核验、各应用启动四阶段提示。
- `launch-local.ps1`捕获失败并返回退出码1，增加仅供桌面入口使用的`PauseOnError`。桌面“一键启动职校学生全生命周期系统.lnk”增加该参数，失败后显示原因并等待回车；原快捷方式备份在既有忽略运行目录。成功仍正常打开登录入口。
- 实际运行与桌面相同的启动脚本（验证时传`-NoBrowser`）：后台8000、管理/教师PC5173、学生PC5199、H5 5188、企业端5202全部返回就绪。再次运行复用全部5个原进程，未重复启动；并发锁持有时明确拒绝并返回1。浏览器登录表单可正常显示和操作。
- 沙箱只读核验仍为`student_lifecycle_runtime_20260902` / `sandbox-school` / 499表 / `20260910_phone_recovery_freeze`。没有清库、重置、迁移或角色转换写入。两项现有启动环境单测及PowerShell语法检查通过。

环境启动阻断已解除；角色转换、跨校和P4业务验收仍待继续，不能用服务启动成功替代业务闭环。文件已保存，未提交、推送或部署。

## 2026-09-13：预设业务角色转为本校维护实库验收

本轮故事：学校管理员登录原沙箱，在学校角色工作区将无成员的普通教职工角色转为本校维护，修改菜单权限，保存后刷新查看结果与留痕。仍使用`student_lifecycle_runtime_20260902`、`sandbox-school`、499表和原迁移头；没有创建另一套联调库。

- 真实页面第一次转换失败，定位到`ROLE_ADOPT`没有进入关键审计动作集合；事务完整回滚。补齐现有审计门面注册，并增加使用真实关键审计写入函数的测试，验证存储失败继续向上传播，不能吞掉必需审计。
- 转换成功后，仅修改权限仍被旧空CUSTOM范围挡住。修复权限保存对“未改变的数据范围”保持原样，不建立范围记录、不扩大范围；明确修改范围仍校验目标，空CUSTOM在运行时仍拒绝数据访问，版本冲突仍先拒绝。
- UI完成STAFF（角色107）转换：SYSTEM版本0→CUSTOM版本1，原编号、编码和0位成员保持不变。随后勾选消息中心权限并保存，版本2；重载页面仍显示自定义角色、已选1项和消息中心菜单。失败后的选择与原因可保留并重新读取，未要求重新填写。
- 留痕页真实显示“转为本校维护”“修改角色权限”两条成功记录及填写原因。数据库回读唯一权限为`workbench.message.view`，来源模板固定版本1、自动升级关闭、范围表仍无新增记录、成员仍为0。
- 在MySQL显式只读事务中调用实际`ensure_builtin_roles`：created=0、restored=0、unchanged=27，前后角色及权限快照一致。此证据证明当前初始化不会覆盖该角色，不代表已经实测发布新模板版本的完整升级流程。

验证：在backend目录以ENV/ENVIRONMENT=test运行`pytest tests/test_school_role_adoption.py tests/test_role_permission_scope_preservation.py tests/test_school_role_template_creation.py -q`，36通过（含7项范围保持测试），有既有Starlette弃用提示。测试使用单元隔离，未执行清库fixture；MySQL与浏览器证据如上独立记录。本轮无前端代码修改，未重复把前轮前端185项与构建记为本轮新结果。

基线仍为`main` / `bc596b6bf24037e9035e3ec606777001cec3208b`加原有及本轮未提交改动。只转换了无成员STAFF角色，未批量改变在用教师角色或师生授权；教师接收端、跨校写入、新校完整初始化、实际模板升级及P4整体未验。施工文件已保存，未提交、推送、合并或部署。

## 2026-09-13：学校复制角色与留痕对象边界

从既有沙箱联调角色108的“详情与维护 → 复制为自定义角色”正常确认，创建角色109。页面展示复制的33项权限和0位成员；在109勾选消息中心，填写调整原因后保存，刷新后版本1、34项权限。MySQL显式只读回读确认108仍为版本1、33项权限，109仅多出`workbench.message.view`；两者0成员、SELF范围及目标一致，来源均固定ACADEMIC_TEACHER版本3，副本自动升级关闭。保留该无成员联调副本供后续验收。

发现并修复同一故事的留痕缺陷：`system_i4_router.role_audit`原条件为资源`role:<id>`或任意相同`resource_id`，使编号109的角色混入编号109用户的PASSWORD_CHANGE、RESET_PASSWORD审计。正式查询改为匹配`role:<id>`或`role:<id>:`下的成员等子资源；租户过滤、分页及角色存在性校验保持。没有修改或删除任何历史审计。

修复前真实页面显示4条（含2条无关记录）；重启本轮后端后“刷新记录”及整页重载均只显示复制与权限保存2条。实际MySQL只读会话调用同一查询，pageSize=1的两页total均为2且动作集合正确，另一学校角色60返回404；原无关审计仍保存在库中。此直接调用只验证查询边界，不能替代HTTP鉴权或跨校写入验收。证据脚本位于既有忽略目录`.codex-temp/verify-role-copy.py`，无seed、commit或清库。

定向路由装配回归`pytest tests/test_control_plane_move_contract.py::test_system_facade_exports_final_composed_router_and_preserves_frozen_surface -q`：1通过，有既有Starlette弃用提示；定向`git diff --check`通过。本轮业务代码只改角色审计查询（3行新增、1行删除），无前端改动，未重跑不相关全仓构建。HEAD仍为`bc596b6bf24037e9035e3ec606777001cec3208b`，文件已保存，未提交、推送、合并或部署。其他在用角色的同期变化不是本轮操作，不纳入本轮证明。

下一故事仍为学校角色成员授权、接收方回读及撤权；P1/P4整体保持未完成。

## 2026-09-13：测试老师接收角色、回收及授权台账分页

本轮仍使用原499表沙箱，通过学校管理员真实PC页面，在STAFF角色107成员页搜索专用联调账号`sbx_t0001`（用户234202），选择并填写原因，添加1位老师。该角色仅有消息中心权限，原5个教务/实习/毕设业务角色未替换。页面成功回读1位成员。

教师侧使用正常`/auth/login`、`/auth/me`、`/auth/switch-role`接口与独立令牌验证，不伪造身份、不直接改授权表：新增授权后旧令牌401，重新登录的身份集合增加STAFF，切换到`role:107`后me返回200。随后学校PC通过授权台账的该记录“回收”，填写原因确认；已预热的同一个教师令牌再次请求立即401。重新登录身份集合恰好恢复原5个角色，没有STAFF。MySQL只读回读授权记录11为REVOKED、version=1，原5个有效角色保留。本轮临时令牌文件已清理，凭据未进入代码和报告。

此证据覆盖正常缓存环境下真实授权/接收接口/撤权，学校管理操作为真实浏览器；教师PC页面、微信宿主、受限管理员、缓存故障下撤权仍未验，不冒充四端闭环。

发现授权台账只请求第一页50条，页面没有分页，后续记录无法通过列表定位。`SystemRoleAssignmentView.vue`沿现有接口补充页码/总数、上一页/下一页和直接跳页，切换分类回到第一页；读取序号丢弃迟到响应，读取失败清除旧可操作行且可重试当前页，提交期间禁止翻页/切分类。真实页面总数22241，直接跳到445页成功，末页下一页禁用；切换“即将到期”回到1/1页、0条，恢复全部分类成功。

验证：新增`system-role-assignment-pagination.behavior.test.mjs`5项通过，涵盖真实页码参数、分类保留、失败恢复、迟到响应、无效分页回执及提交期间阻止切换；定向ESLint与diff检查通过。最终前端build 45.34秒成功，21条官网路由预渲染成功，保留既有chunk体积等警告；日志在`.codex-temp/daily-sandbox/role-assignment-build.log`。本轮未改变后端授权/撤权核心。

剩余直接缺口：`role_assignment_service.list_assignments`仍全量读入后汇总切片，前端可翻页不能证明数据库容量已收敛；历史授权补登记、多人高权分类返回形状及真实教师页面仍需接续。本轮代码、测试及记录已保存；main / `bc596b6bf24037e9035e3ec606777001cec3208b`加未提交改动，未提交、推送、合并或部署，P1/P4整体未完成。

## 2026-09-13：授权台账数据库分页与历史补登记

本轮延续上节的展示查询与历史记录维护缺口。新增`role_assignment_query_service.list_page`，将登记记录与未登记UserRole组成数据库查询，分别按租户限定，先筛选、汇总和排序、LIMIT/OFFSET，再按当页主键装载最多pageSize条明细。`list_assignments`保留既有到期回收及必需审计入口，将展示查询委派给该服务。列表顺序固定为登记记录优先、各类编号倒序；历史记录不再依赖数据库未指定的返回顺序。

同时修复两处合同错误：role_code筛选现在同时限制登记与历史记录，登记存在性在筛选前判定，避免别的角色被当成历史授权；HIGH_PRIV_MULTI分类现在返回具有userId、userRoleId等字段的具体成员，统计卡仍统计多人持有的高权角色种类。原页面把聚合角色对象当成员显示的缺口解除。

原499表沙箱MySQL显式只读验证：总数22241，未复核/来源不明均22239，前两页各50且成员关系不重叠，末页41；STAFF和ACADEMIC_TEACHER筛选没有混入其他角色；多人高权1类返回2名具体成员；不存在的学校结果与统计均为空。8次查询组合约1.1秒，仅为当前样本观察，不是容量承诺。真实页面也显示该分类的2名成员。证据脚本`.codex-temp/verify-assignment-db-page.py`不seed、不commit。

新增正式“补登记”命令与页面按钮：同租户角色/账号/成员关系按既有锁顺序核验，检查可转授边界、有效成员关系、expectedVersion与已登记冲突；只创建RoleAssignmentValidity，原生效时间/长期有效状态保持，source_type仍UNKNOWN，不把当前登记人冒充原授权人。事务内必需ROLE_ASSIGNMENT_REGISTER审计失败即回滚，不修改UserRole或权限。登记后沿已有复核、转交、回收入口维护。

学校PC真实对专用沙箱账号`sbx_t0269`既有GD_MENTOR关系263107填写原因并补登记，生成记录12。刷新后显示原因及复核/转交/回收按钮。MySQL逐字段对照登记前后的UserRole完全相同，来源UNKNOWN、期限未改、原始生效时间保留、审计accessChanged=false。普通教师正常登录后请求该新接口实际403，未创建任何额外记录；临时令牌已清理。没有批量补登记或扩大师生权限。

验证：`test_role_assignment_legacy_registration.py`8项（正常保留、缺失/版本/已失效/已登记/授权/审计拒绝及关键审计注册）、`test_role_assignment_query_page.py`2项查询形状保护、正式路由装配1项，共11通过。查询形状单测不冒充MySQL执行，实际SQL证据另列如上。前端分页/补登记6项行为测试、定向ESLint及diff检查通过；前端最终build54.99秒成功、21条官网路由预渲染通过，既有chunk等警告保留，日志`.codex-temp/daily-sandbox/role-ledger-db-build.log`。未运行清库fixture。

本轮改动：查询服务、原列表服务、历史登记守卫、正式router、审计动作标签、PC API及成员页面、对应测试；复用原施工记录。基线仍main / `bc596b6bf24037e9035e3ec606777001cec3208b`加未提交改动，未提交/推送/合并/部署。到期回收批处理仍沿旧实现，本轮不能证明大量同时到期容量；教师PC/微信宿主、受限管理员和模板升级全故事仍未验，P1/P4整体保持未完成。


### 2026-09-13：添加角色成员必填校验可见性

- 用户确认：无法添加是未填写授权原因，填写后可添加，并非数据范围阻断。原表单把原因放在长候选列表之后，悬浮确认按钮提前可见，校验失败没有定位。
- 将原因、可选到期日期、错误提示及确认按钮合并到候选列表上方；明确原因必填且至少 5 字，移除本面板悬浮保存条。漏填或日期失效时聚焦并滚动到对应字段，保留成员选择与输入；不改变后端授权合同。
- 验证：新增 4 项行为测试通过（模板编译、原因漏填、过期日期、正常提交合同）；定向 ESLint 及 diff check 通过。实际学校 PC 页面选择一位候选并空原因点击确认，出现原因提示、输入高亮与定位，选择保留；随后清空本轮选择并返回清单。未执行真实新增授权，成员仍为 1 位。
- 基线沿用 main / bc596b6bf24037e9035e3ec606777001cec3208b；仅保存工作区，未提交、推送、合并或部署。本次为局部表单修复，未重跑全量构建与四端业务验收。
- 补充回归：原纯视觉脚本快照因本次获授权的校验交互调整发生差异；逐项复审 diff，确认授权门禁和请求载荷不变后更新对应锚点，并保留独立行为测试。两组共 15 项通过。


### 2026-09-13：P3 授权复核、转交与回收失败恢复

- 故事：学校管理员办理一条既有授权，发生校验失败、请求中断或版本冲突时仍能看到原因和所填信息，先核对结果再继续。范围限 Staff PC 授权台账确认弹窗；后端授权、审计、版本合同未改。
- `SystemRoleAssignmentView.vue`：失败提示进入弹窗；冲突不再自动关闭和丢弃草稿；结果不明或冲突时阻止原请求重复提交，提供显式返回清单回读。提交期间阻止关闭弹窗，finally 释放提交状态；校验失败保留可编辑输入。
- 验证：`node --test frontend/tests/system-role-assignment-pagination.behavior.test.mjs` 9 项通过，含新增冲突保留原版本、异常释放提交锁、重复提交和关闭防护；定向 ESLint 与 diff check 通过；前端完整构建 59.49 秒通过（原有分块体积警告）。真实页面转交弹窗漏填接收人时错误就地显示，未发起真实转交，随后取消并恢复原页面。
- 证据边界：网络异常与版本冲突为行为单元测试，未冒充真实 MySQL 并发验收；未新增或撤销用户授权；微信及其他端无本轮系统管理编辑职责，不计四端全通过。main / bc596b6bf24037e9035e3ec606777001cec3208b，保存工作区，未提交/推送/合并/部署。
- 后续已观察缺口：自定义角色在授权台账显示“角色名称待维护”，转交仍要求填写用户编号；另按原规划继续学校自治与受限身份验收。本段不将 P1/P4 整体标为完成。


### 2026-09-13：P3 转交授权改为搜索接手老师

- Staff PC 授权台账的转交弹窗不再要求手填用户编号：复用角色成员候选接口，按姓名/工号分页查询，每页 10 位；明确显示选中姓名和工号。新查询清空原选择，旧查询迟到结果不得覆盖新结果，接口失败独立显示且阻止提交。
- 台账分页 DTO 增补字符串 roleId，从同租户 UserRole 关联回读，供候选查询；候选接口仍排除停用账号、学生与既有成员，转交请求保持原 expectedVersion、toUserId、原因和后台权限审计校验。未改授权规则及数据库结构。
- 真实候选响应为 items，与台账 list 不同；浏览器联调发现并修正，测试同步使用实际契约。真实页面按 e2e_aa_teacher_b 搜索得到 1 位并选中，随后取消恢复原页面，未实际转交权限。
- 验证：12 项前端相关行为测试、定向 ESLint、Python 编译和 diff check 通过；原 499 表沙箱只读 MySQL 验证 roleId 对应真实 UserRole，22243 条台账分页/过滤/学校隔离检查通过；最终前端构建 28.95 秒通过，保留原分块体积警告。日志 `.codex-temp/daily-sandbox/iam-recipient-search-build.log`。
- 边界：人员搜索和选择已真实页面验证，写请求参数由单元测试验证；本轮未重复执行真实权限转交及微信验收。保存本地工作区，未提交、推送、合并或部署。


### 2026-09-13：学校授权边界与数据字典首个真实消费者

- 授权台账同时返回角色编号和同租户角色名称，自定义角色显示学校维护的真实名称。原页面中的“角色名称待维护”不再掩盖实际角色；真实沙箱页面已显示“沙箱联调角色0913（自定义）”。
- 最后一名有效学校管理员保护补齐4类回归：唯一有效管理员拒绝移除、存在第二位有效管理员时允许、停用账号不能充当接班人、普通角色不误触主管理员查询。保护逻辑仍由后端在锁内裁决，前端按钮状态不作为安全边界。
- 永久授权上限补齐3类回归：受限管理员只能授予本人基础权限、通配权限先展开再比较、临时授权即使让本人暂时可操作也不能被再次固化给角色。没有给任何管理员增加`*`或敏感权限。
- 数据字典审计确认原页面虽可保存12类配置，多数业务页面仍使用各自静态选项，不能把保存成功记作业务生效。本轮新增只读有效字典合同，普通已登录学校身份可读取非敏感显示词；状态编码仍由业务域固定，学校新增陌生编码、停用必需状态均不能改变业务状态机。
- 首批接入学工中心的“学生状态、风险等级”。学校覆盖只改变相同规范编码的显示名称；未知编码忽略，缺失项使用业务默认值；读取失败回落原选项且不阻断权限上下文。字典页逐类显示“已用于：学工中心”或“尚未接入业务页面”，不再把预留配置冒充已经生效。
- 验证：授权/管理员/字典后端定向测试16项通过；新增授权上限与字典组合9项通过；前端授权及字典合同15项通过；定向ESLint通过；完整前端构建43.55秒通过并生成21条官网预渲染路由，保留既有CSS导入顺序、分块体积等警告。真实字典正式入口加载12类并显示业务接入状态，未修改沙箱字典内容。
- 边界：本轮只完成数据字典第一个真实消费者。审批、实习、毕设、就业、文件与服务类型仍须逐域核对规范状态后接入；未接入项在页面明确标记。未执行跨校写入、实际模板升级或微信宿主验收。基线仍为main / `bc596b6bf24037e9035e3ec606777001cec3208b`加未提交改动，未提交、推送、合并或部署。

### 2026-09-13：沙箱模板收敛与 main 阶段保存

- 随后在已核验的日常沙箱运行既有模板发布命令：LEADER v4（209项）、SCHOOL_ADMIN v5（656项）、SYS_ADMIN v4（82项），补齐目录内已有的手机号治理权限；24个模板未变，再次预览无待收敛差异。旧发布版本保留。
- 回读已有自定义角色108、109：权限分别33、34项，来源仍为ACADEMIC_TEACHER v3，禁止自动升级；成员分别0、1人。本次模板发布没有修改成员关系。此证据不能代替同源模板升级、跨校隔离或新校初始化的完整验收。
- 用户要求将本任务成果保存到本地main。本次按明确文件清单保存系统管理、数据字典、公共壳及菜单修复和相关测试文档；其他任务的后端业务、移动端、启动脚本及本地附件不混入此提交。
- 提交前前端定向检查31项通过，暂存差异格式检查通过。此处为可追溯施工检查点，学校自治与管理员边界尚未全部收尾，新校默认角色初始化及受限管理员、跨校、并发等完整验收仍待完成。未推送、部署；数据库内容不包含在Git代码提交中。
