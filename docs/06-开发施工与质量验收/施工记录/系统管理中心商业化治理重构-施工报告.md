# 系统管理中心商业化治理重构 — 施工报告

> 生成时间：2026-07-24。本文件为任务唯一交付文档，不另开总册。

## 一句话结论

已完成模块清单/能力注册表/合同检查、模块门禁、有效权限、数据范围主链路、组织主数据校验、敏感字段写入、假功能去伪、审计分类、系统管理九工作区与上线检查的主干改造；部分业务写入口未全部切到统一主数据、历史明文回填与真实同步适配器仍待后续。

## 审计问题核验（开工时）

| 问题 | 状态 |
|---|---|
| employment 复用 internship 门禁 | 仍存在 → 本轮已修 |
| orientation / 旧 academic 缺门禁 | 仍存在 → 本轮已修 |
| 自定义角色前后端权限不一致 | 仍存在 → 本轮统一 get_effective_permission_patterns |
| Role.remark 数据范围主链路 | 仍存在 → 本轮改为结构化写入 |
| 手机号明文写入 encrypted | 仍存在 → 本轮已修 |
| 同步任务假 SUCCESS | 仍存在 → 本轮改为 PENDING |
| 审计固定 SYSTEM | 仍存在 → 本轮按 action/path/moduleCode 分类 |
| 模块开关未接入门禁 | 仍存在 → 本轮接入 module_access_service |
| permissionGate 未覆盖 SYSTEM/EMPLOYMENT | 仍存在 → 本轮已扩 |

## 已完成阶段

1–6, 7（主链路）, 8（系统写入+教务学院创建接入）, 9（写入加密+只读扫描）, 10（去伪）, 11（分类筛选）, 12（九工作区）, 13（上线检查）, 14（乐观锁/幂等主干）, 15（相关测试）

## 未完成 / unresolved

- 教务专业/班级等其余写入口未全部改为 org_master_service（仅学院创建已切）
- 历史手机号明文不自动回填（仅扫描工具）
- KNOWN_SYNC_ADAPTERS 为空：无外部适配器时任务只能 PENDING/FAILED
- 临时授权依赖 loginName 匹配，跨租户同工号边界需后续加强
- Expand-only：未做组织编码 DB 唯一索引 Contract 阶段
- 能力注册表 routeExists 为启发式，极少数相对子路由可能误报
