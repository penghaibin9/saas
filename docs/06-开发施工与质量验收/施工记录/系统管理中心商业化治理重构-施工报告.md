# 系统管理中心商业化治理重构 — 施工报告

> 最近更新：2026-07-25（审计修复轮）。基线 commit 含 `7e88391`，本文件同步当前代码事实。

## 一句话结论

商业化治理控制面主干已落地并可提交验收：模块清单/能力注册表/合同检查、模块门禁、有效权限、结构化数据范围（读写对齐）、组织主数据统一写入、敏感字段加密写入收口、假同步 SUCCESS 已杀、系统管理九工作区、组织编码租户唯一索引 Expand。仍有登记豁免与空适配器能力缺口，不得对外宣称“全仓敏感字段历史明文已回填”或“外部同步已可真跑通”。

## 审计问题核验

| 问题 | 状态 |
|---|---|
| employment 复用 internship 门禁 | 已修 |
| orientation / 旧 academic 缺门禁 | 已修 |
| 自定义角色前后端权限不一致 | 已修（effective permissions） |
| Role.remark 数据范围主写入 | 已改结构化写入；**登录读路径已对齐 resolve_role_scope_code** |
| 手机号明文写入 encrypted | **系统/迎新/就业/学业/实习企业/毕设导师/家长授权写入口已 encrypt_field**；历史明文仅扫描不自动回填 |
| 同步任务假 SUCCESS | 已改为 PENDING（KNOWN_SYNC_ADAPTERS 仍为空） |
| 审计固定 SYSTEM | 已按 action/path/moduleCode 分类 |
| 模块开关未接入门禁 | 已接入 module_access_service |
| permissionGate 未覆盖 SYSTEM/EMPLOYMENT | 已扩 |
| auth 读范围与写脱节 | **2026-07-25 已修** |
| 0132 空串唯一索引隐患 | **0132 建索前规范化 + 0134 补丁** |
| 组织旁路 | delete/开局/实施已接入；sandbox/分流写死豁免清单 |

## 组织主数据

### 已统一

- `POST/PUT /api/v1/system/org-nodes`、状态停用、重复编码只读
- 教务 `orgs/colleges|majors|classes` 的 create/update/**delete**
- 学校开局 `school_onboarding_service`、实施安装 `system_implementation_service`（会话内 `apply_org_node_in_session`）

### 正式豁免（`ORG_WRITE_BYPASS_ALLOWLIST`）

- `sandbox_service`：演示沙箱种子
- `academic_affairs_major_split_service`：专业分流业务内建班

## 数据库

- `0132_org_code_unique_expand`：查重 → 空串改 NULL → 租户+编码唯一索引
- `0133_merge_counselor_org_uk`：合并 counselor 与 0132 双 head
- `0134_org_code_empty_to_null`：已执行旧版 0132 的环境补丁（空串→NULL）
- 生产未在本任务中连接或迁移

## 能力注册 / 路由索引

- `generate-capability-registry.mjs` 落盘**完整** `shared/generated/route-index.json`（含 exact/patterns/redirects/aliases）
- `routeExists` 基于真实路由索引（含可选参数、嵌套、redirect），非字符串前缀猜测
- 合同检查：正式侧栏节点不得 UNRESOLVED；豁免须带合法原因

## 未完成 / unresolved（诚实口径）

- `KNOWN_SYNC_ADAPTERS` 为空：无外部适配器时同步只能 PENDING/FAILED
- 历史手机号明文不自动回填（`scan_sensitive_fields.py` 只读扫描）
- 临时授权依赖 loginName 匹配的边界加强仍待后续
- 豁免清单外禁止新增组织旁路

## 验证证据（本修复轮）

以实际命令结果为准（见同轮测试输出）：治理专项 pytest、合同检查、Alembic heads、route-index 全量落盘。
