# 2026-07-07 数字迎新 › 新生信息核验 / 报到资格（生产级全栈闭环）

## 结论
2 个 planned 页 → **implemented**。数字迎新 **11/19 → 13/19** 已真闭环。
打通了新生主线闸口：导入 →（信息核验）→ 已核验 →（报到资格判定/人工放行）→ 可报到。

## 做了什么（真实、非 mock）
- **新生信息核验**（`/admin/orientation/verify`）
  - 后端新增接口 `POST /orientation/students/{sid}/verify`（通过/不通过）+ 服务 `verify_student`：
    - 通过 → `stage=PRE_STUDENT_VERIFIED`、环节 `INFO=DONE`、写审计「信息核验通过」；
    - 不通过 → 原因≥5 字校验、记录 `exception_note`、标高风险、写审计「信息核验不通过」；
    - 已入学/已取消不可再核验（非法流转拒绝）。
  - 前端页：待核验/已核验筛选 + 身份证脱敏展示 + 核验通过/不通过(原因) + 二次确认，接真实接口。
- **报到资格**（`/admin/orientation/qualification`）
  - 资格闸口视角：按「卡点 blockedStep + 核验状态」综合判定 **合格 / 待核验 / 受阻**；
  - 受阻可**人工放行**（复用已验证的 `/orientation/progress/{id}/resolve`，放行需理由、留痕）。
  - 复用现有新生表与接口，未新建表。

## 验证
- 后端：`pytest tests/test_orientation.py` → **13 passed**（新增 `test_verify_closed_loop`：短原因拒绝→通过→INFO=DONE→不通过记录→审计≥2）。
- 前端：`eslint` 改动文件 → **0 错**；`npm run build` → **通过**。
- 预览：2 页真实渲染（核验页 4 行 + 核验按钮）、侧栏「新生信息核验 / 报到资格」点亮可点、零报错。

## 改动文件
后端：`schemas/orientation.py`（VerifyBody）、`services/orientation_service.py`（verify_student）、`api/v1/orientation.py`（verify 端点）、`tests/test_orientation.py`（verify 测试）
前端：`modules/orientation/api/orientation.api.js`（verify 方法）、新增 `OrientationVerifyView.vue`、`OrientationQualificationView.vue`、`orientation.routes.js`（+2 路由）、`config/navPlan.js`（2 页点亮）

## 数字迎新剩余（6 个 planned）
新生数据、报到流程配置、宿舍预分配、现场报到点、迎新通知、迎新归档。
- 新生数据：给新生表加 `batch_id` 关联批次 + 按批次筛选（下一批做，含表结构小改）。
- 报到流程配置 / 宿舍预分配 / 现场报到点 / 迎新通知 / 迎新归档：需新表 + 接口 + 页，按批一条龙。

## 待办（记账，不阻断）
- 权限 key 仍用 `orientation.student.view` 兜底，后续统一迁 `studentAffairs.orientation.*`（旧 key 保留）。
