# 2026-07-07 后端开发环境配置记录

## 目的
解除"沙箱无法运行后端验证闭环"的阻断项，使后续可做**可验证的真实后端开发**（CLAUDE.md §14 要求 pytest + 自修复）。

## 环境事实
- Python：3.12.5
- 项目根：`职校学生全生命周期系统`
- 后端：`backend/`（FastAPI + SQLAlchemy 2.x + Alembic）
- 依赖清单：`backend/requirements.txt`（13 个直接依赖）

## 执行步骤（可复现）
```powershell
# 1) 在项目根创建虚拟环境
cd 职校学生全生命周期系统
python -m venv .venv

# 2) 安装后端依赖
.\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt

# 3) 验证 pytest 能跑
cd backend
..\.venv\Scripts\python.exe -m pytest tests\test_health.py tests\test_orientation.py -q
```

## 结果
- venv：✅ 创建成功（`.venv\Scripts\python.exe`，pip 24.2）
- 依赖安装：✅ `PIP_EXIT=0`，13 直接依赖 + 传递依赖全部成功
- pytest 验证：✅ **13 passed（30.73s）**，`PYTEST_EXIT=0`
  - 覆盖：`test_health` + `test_orientation`（含 green_channel / material / dashboard / exception 等闭环）
  - 仅有 `DeprecationWarning: datetime.utcnow()`（无害，后续可统一改 `datetime.now(datetime.UTC)`）

## 已安装关键版本（落在 requirements 允许区间内）
fastapi 0.139.0 · uvicorn 0.50.2 · pydantic 2.13.4 · sqlalchemy 2.0.51 · alembic 1.18.5 ·
pytest 8.4.2 · httpx 0.28.1 · openpyxl 3.1.5 · PyMySQL 1.2.0 · psycopg 3.3.4 · PyJWT 2.13.0

## 说明
- 数据库建表方式：`app/db/init_db.py` 的 `metadata.create_all()`（DB_ENABLED=true 时）；测试用 SQLite/内存（conftest）。
- 迁移史 alembic 到 `0014`，**orientation 表未纳入迁移史**（靠 create_all 建表）→ 后续做数字迎新时补 `0015_p7_orientation`。
- 本次仅配环境 + 建记录，**未改任何业务代码，未提交 git**。
