# 上线前必做清单 · 总闸门

> 用途：教务中心（13B）商业化上线前，本文件汇总所有**已知但本轮未做完**的欠账，防遗忘。
> 甲方是非技术产品负责人，AI 替其把关：每条欠账写清「是什么 / 影响 / 阻断上线否 / 建议何时做」。
> 状态口径：🔴 阻断上线（必须做）｜🟡 上线前应做（B 级欠账）｜🟢 可上线后补（增强）。
> 最后更新：2026-07-15（选课/考务/补考/教材/并行整合 B 级欠账集中消化 + 旧路由 redirect 收敛后登记）。

---

## 一、全局未做（跨模块）

| # | 项 | 状态 | 影响 | 建议 |
|---|---|---|---|---|
| G1 | **未 push / 未并 master** | 🔴 | 6 个 commit 仅在本地分支 `feat/13b-parallel-mods`，远程/CI/其他环境拿不到 | 甲方拍板后 push + 并 master |
| G2 | **未部署到真实服务器** | 🔴 | 目前只是"生产级代码就绪 + 本地 MySQL 验收"，没有任何线上环境 | 上线前做部署（含 alembic upgrade head 到生产库、初始化 seed、回滚预案） |
| G3 | **移动端小程序页未做** | 🟡 | 学生端选课/缓考申请/重修免修申请/教材签收等本轮只做了 PC + 学生 PC 自助页，`miniapp/` 对应页缺 | 小程序另排期（甲方 2026-07-14 明确"今天不做小程序"） |
| ~~G4~~ | ~~**旧路由未清理收敛**~~ | ✅ 已消化 | 2026-07-15：`academic.routes.js` 全量改为 redirect——旧 `/admin/academic/*`（总览/学生/成绩/学分/补考重修/预警）→ 新 `/admin/academic-affairs/*` 等价页；navPlan 移除 5 个「（现有）」冗余叶子。旧组件文件保留（可逆），刷新不 404。vite build 通过 | 已完成，无需上线前再做 |
| G5 | **教务处岗位角色码未定** | 🟡 | 当前"教务处"落在 `SCHOOL_ADMIN`(全权过粗)；是否专设 `ACADEMIC_AFFAIRS_OFFICE` 角色码待学校定 | 上线前与学校确认组织架构后定角色 |
| G6 | **规则中心键默认值待学校确认** | 🟡 | retake.maxCount=2 / makeup.scoreRule=CAP60 / exemption.maxCount=2 等为默认值，改配置不改代码 | 上线前与学校教学管理规定核对，配置生效 |

---

## 二、已实现模块的三级欠账（planned 叶子 + 联动欠账）

各模块**核心闭环真实可用、已 MySQL 验收**。下列标 ✅ 项为 2026-07-15 集中消化的 B 级欠账（均已 MySQL 验收），🟢/🟡 为仍留的增强项。

### 选课管理（50d6a2f + 2026-07-15 消化，MySQL 12/12）
- ✅ 选课八条校验**全部落地**：④先修课程校验（`prerequisite_codes_json` × 已通过课名）、⑤课表时间冲突校验（`_task_slots`+`_weeks_overlap`）、⑧重修规则（已通过不可重选）已补齐 `_validate_enroll`。
- ✅ `run_time_tick(user)` 端点已接：PUBLISHED→OPEN（到 select_start_at）、OPEN→CLOSED（过 select_end_at）状态迁移逻辑落地。
- 🟢 真·定时 cron/scheduler 调度（无人值守自动触发 tick）属基础设施，仍排期；当前经端点触发。
- 🟢 选课规则专页、补选指引专页、冲突报表专页为 planned（控制台已含统计与名单）。

### 考务管理（05bdde7 + 2026-07-15 消化，MySQL 10/10）
- ✅ 缺考**真联动 13A 学工风险**：`record_incident` 对 ABSENT 写 `AffairsRiskRecord(source="EXAM_ABSENT")`，进入辅导员风险台账（非仅标记位）。
- ✅ 巡考冲突检测（`assign_patrol`/`list_patrols` + `_time_overlap` 同人时段冲突 409）已做。
- ✅ 准考证/门贴打印页 + 座位表专页 `AaExamSeatingPrintView.vue`（room_seats 端点）已做。
- ✅ 考务通知真实推送：`publish_batch` 对每学生/教师写 `UnifiedMessage`。
- ✅ 发布前编排完整性校验：`_check_arrangement_complete`（每 CONFIRMED 课程需考场+座位+监考，缺则 409）。
- 🟢 违纪 `discipline_case_ref` 仍为字段位供学工消费，未实现反向联动（增强）。

### 补考重修缓考免修（d0ab568 + 2026-07-15 消化，MySQL 7/7）
- ✅ 补考成绩**加学院审核门**：`college_review_scores`（SCORING→REVIEWED）→ `finish_makeup_batch` 强制要求 REVIEWED 才可回写；未审核直接收口 409。前端 `AaMakeupConsoleView` 生命周期按钮已改（学院审核 / 教务发布回写）。
- ✅ 免修材料附件接 `t_file_object`：`exemption_apply` 校验 `materialFileIds` 存在于 FileObject（非纯文本占位）。
- ✅ 补考挂考务批次：`link_exam_batch` 校验 `AaExamBatch` 存在后关联（非仅字段位）。
- 🟢 完整委托 R1 grade-task 三级模块的审核链（跨模块编排）仍为增强；当前审核门在补考模块内闭环。
- 🟢 统计分析专页、材料归档、补考报名(学生自助确认)为 planned。

### 教材管理（06792a8 + 2026-07-15 消化，MySQL 6/6）
- ✅ 教材库存：`textbook_stock`（到货 arrived − 已发 distributed RECEIVED，按教材统计）已做。
- ✅ 费用分档/部分收款：`mark_fee(..., amount)` 支持 PARTIAL（`paid_amount` 累加，满额转 PAID，超额 400）已做。
- 🟡 表名 `t_aa_textbook_*` 为约定名（施工卡 D-01 标"待用户确认"），全新表零数据，如需改名易迁移。
- 🟢 教材归档为 planned；发放签收学生小程序自助签收未做（今日不做小程序）。
- 🟢 教材负责人业务关系取 _derive_keys，未接权限总控 §8A 专属范围收敛。

### 并行整合那批（7bd62b6：orgs/resource/scheduleChange/stats + 2026-07-15 消化，MySQL 15/15）
- ✅ 调停课通知推送：`_apply_schedule` 改为逐学生通知循环（遍历班级学生逐条 `UnifiedMessage`），非单条广播。
- ✅ 教室预约：`book_classroom`/`list_bookings`/`review_booking`（同教室同时段占用 409 冲突检测 + 审核），新表 `t_aa_classroom_booking`（迁移 0080），前端 `AaClassroomBookingView.vue` + 路由 + navPlan「教室预约」叶子已接。
- 🟢 实训室/设备预约、资源占用/维修、组织变更审计下钻等 planned 叶子未做（增强）。
- 🟡 选课/考务/补考等模块的 `list_*` 列表端点数据范围未逐一收敛到 COLLEGE（具体操作时有 scope 拦截，列表层可能跨院可见）——已在各 service docstring 标注。

---

## 三、二级模块开发进度（截至 2026-07-14 晚）

**26/27 二级模块已实现**（含本轮新增 5 个）。逐个独立 commit + 真实 MySQL 验收：

| 模块 | 状态 | 备注 |
|---|---|---|
| 排课管理 aa-scheduling | ✅ 已实现 | 增强层：规则中心+教师可用时间+全量冲突报告(HARD/SOFT) |
| 教学评价 aa-evaluation | ✅ 已实现 | 评教批次6态+匿名评教+均分分级+申诉 |
| 教学质量 aa-quality | ✅ 已实现 | 零新表：运行质量看板+报告导出 |
| 教务归档 aa-archive | ✅ 已实现 | 按学期归档+9数据域完整性+学期封存 |
| 教学计划 aa-teaching-plan | ✅ 已收编 | 用户 2026-07-14 拍板「收编」：navPlan 叶子指向 培养方案/教学任务/教务归档，零新表 |

**✅ 27/27 二级模块全部有可用入口——教务中心总体收工。**

> 说明：课表管理(aa-schedule 已实现)是**手工课表批次**；排课管理是**规则/冲突/可用时间增强层**，是另一层，故单列。

### 教学计划裁决（已闭合）
- **U-01 已裁决：用户选「收编」**（2026-07-14）。navPlan `aa-teaching-plan` 叶子指向培养方案/教学任务/教务归档既有页，零新表。建独立域设计作备选存档于施工卡。

---

## 四、上线闸门判定

- 🔴 阻断项（G1 push、G2 部署）必须在真实上线前完成。
- 🟡 项建议上线前与学校确认/补做，但不阻断内部验收演示。
- 🟢 项可上线后按迭代补。
- 本文件随后续模块完成与欠账消化持续更新。
