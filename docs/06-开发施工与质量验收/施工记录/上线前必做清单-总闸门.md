# 上线前必做清单 · 总闸门

> 用途：教务中心（13B）商业化上线前，本文件汇总所有**已知但本轮未做完**的欠账，防遗忘。
> 甲方是非技术产品负责人，AI 替其把关：每条欠账写清「是什么 / 影响 / 阻断上线否 / 建议何时做」。
> 状态口径：🔴 阻断上线（必须做）｜🟡 上线前应做（B 级欠账）｜🟢 可上线后补（增强）。
> 最后更新：2026-07-14（教务中心 R1 + 8 二级模块全栈完成后登记）。

---

## 一、全局未做（跨模块）

| # | 项 | 状态 | 影响 | 建议 |
|---|---|---|---|---|
| G1 | **未 push / 未并 master** | 🔴 | 6 个 commit 仅在本地分支 `feat/13b-parallel-mods`，远程/CI/其他环境拿不到 | 甲方拍板后 push + 并 master |
| G2 | **未部署到真实服务器** | 🔴 | 目前只是"生产级代码就绪 + 本地 MySQL 验收"，没有任何线上环境 | 上线前做部署（含 alembic upgrade head 到生产库、初始化 seed、回滚预案） |
| G3 | **移动端小程序页未做** | 🟡 | 学生端选课/缓考申请/重修免修申请/教材签收等本轮只做了 PC + 学生 PC 自助页，`miniapp/` 对应页缺 | 小程序另排期（甲方 2026-07-14 明确"今天不做小程序"） |
| G4 | **旧路由未清理收敛** | 🟡 | 旧 `/admin/academic/*`（6 个 partial 页）与新 `/admin/academic-affairs/*` 并存；navPlan 保留"补考重修成绩(现有)"兼容链接 | 上线前把旧页 redirect/alias 到新页，避免双入口混淆；刷新不能 404 |
| G5 | **教务处岗位角色码未定** | 🟡 | 当前"教务处"落在 `SCHOOL_ADMIN`(全权过粗)；是否专设 `ACADEMIC_AFFAIRS_OFFICE` 角色码待学校定 | 上线前与学校确认组织架构后定角色 |
| G6 | **规则中心键默认值待学校确认** | 🟡 | retake.maxCount=2 / makeup.scoreRule=CAP60 / exemption.maxCount=2 等为默认值，改配置不改代码 | 上线前与学校教学管理规定核对，配置生效 |

---

## 二、已实现模块的三级欠账（planned 叶子 + 联动欠账）

各模块**核心闭环真实可用、已 MySQL 验收**，以下为施工卡里标 planned 的三级页/联动，均非阻断。

### 选课管理（50d6a2f）
- 🟡 选课八条校验只落地 5 条；未做 ④先修课程校验、⑤课表时间冲突校验、⑧重修规则校验。
- 🟢 定时任务自动开选/截止（PUBLISHED→OPEN、OPEN→CLOSED）未接，本期教务处手动端点触发。
- 🟢 选课规则专页、补选指引专页、冲突报表专页为 planned（控制台已含统计与名单）。

### 考务管理（05bdde7）
- 🟡 缺考"写 RISK_ALERT 给辅导员"仅落 `risk_alert_sent` 标记位 + 审计，未与 13A 学工风险预警真联动。
- 🟢 巡考冲突检测、准考证/门贴打印页、座位表专页、考务通知真实推送、归档完整性校验为 planned。
- 🟢 违纪 `discipline_case_ref` 仅存字段位供学工消费，未实现联动。
- 🟢 发布前"编排完整性校验"（每课程有考场+座位+监考且无冲突）简化为确认即可发布。

### 补考重修缓考免修（d0ab568）
- 🟡 补考成绩录入未接 R1 grade-task 三级审核链（本期教务处直录+按 CAP60 回写 t_acad_grade source=MAKEUP）。
- 🟢 免修材料附件（证书/先修证明）以理由文本占位，未接 t_file_object。
- 🟢 补考挂考务批次编排（考场/监考）以 exam_batch_ref 字段位关联，未联动。
- 🟢 统计分析专页、材料归档、补考报名(学生自助确认)为 planned。

### 教材管理（06792a8）
- 🟡 表名 `t_aa_textbook_*` 为约定名（施工卡 D-01 标"待用户确认"），全新表零数据，如需改名易迁移。
- 🟢 教材库存、教材归档为 planned。
- 🟢 发放签收本期教务处代签，学生小程序自助签收未做。
- 🟢 费用=定价×数量，分档/部分收款 PARTIAL/减免比例未细化。
- 🟢 教材负责人业务关系取 _derive_keys，未接权限总控 §8A 专属范围收敛。

### 并行整合那批（7bd62b6：orgs/resource/scheduleChange/stats）
- 🟢 调停课通知推送、教室预约/占用/维修、组织变更审计下钻等 planned 叶子未做（核心 CRUD/审批已做）。
- 🟡 选课/考务/补考等模块的 `list_*` 列表端点数据范围未逐一收敛到 COLLEGE（具体操作时有 scope 拦截，列表层可能跨院可见）——已在各 service docstring 标注。

---

## 三、二级模块开发进度（截至 2026-07-14 晚）

**26/27 二级模块已实现**（含本轮新增 5 个）。逐个独立 commit + 真实 MySQL 验收：

| 模块 | 状态 | 备注 |
|---|---|---|
| 排课管理 aa-scheduling | ✅ 已实现 | 增强层：规则中心+教师可用时间+全量冲突报告(HARD/SOFT) |
| 教学评价 aa-evaluation | ✅ 已实现 | 评教批次6态+匿名评教+均分分级+申诉 |
| 教学质量 aa-quality | ✅ 已实现 | 零新表：运行质量看板+报告导出 |
| 教务归档 aa-archive | ✅ 已实现 | 按学期归档+9数据域完整性+学期封存 |
| **教学计划 aa-teaching-plan** | ⏸ **待用户裁决** | 施工卡已撰写但标 blocked：手册 P6 冻结"收编不建域"，与"建独立域"冲突，需用户二选一（见下）|

> 说明：课表管理(aa-schedule 已实现)是**手工课表批次**；排课管理是**规则/冲突/可用时间增强层**，是另一层，故单列。

### 教学计划待裁决项（🟡 阻断该模块，不阻断其余 26 个）
- **U-01 建独立域 vs 收编**：教务开工手册-下册 P6 已冻结"教学计划不新建表/端点/页面，收编到既有 `AaProgramBinding`(年级教学计划) + `AaTeachingTaskBatch`(学期开课计划)"，理由是功能重叠。用户"做完教学计划"指令与该冻结决议冲突。按 CLAUDE.md §2 用户指令优先，但因这是会显著改变产品的架构选择，暂停等用户明确二选一：① 建独立域(3新表,真实工作台) ② 收编(navPlan 叶子指向既有页,零新表,即达 27 模块)。
- U-02 外部证据 PARTIAL（同其余触发式模块，可接受 project_rule 继续）。

---

## 四、上线闸门判定

- 🔴 阻断项（G1 push、G2 部署）必须在真实上线前完成。
- 🟡 项建议上线前与学校确认/补做，但不阻断内部验收演示。
- 🟢 项可上线后按迭代补。
- 本文件随后续模块完成与欠账消化持续更新。
