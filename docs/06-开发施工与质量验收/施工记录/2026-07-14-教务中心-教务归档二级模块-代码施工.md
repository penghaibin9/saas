# 教务中心 · 教务归档二级模块 · 代码施工记录

- 日期：2026-07-14
- 分支：`feat/13b-parallel-mods`（基线：教学质量提交 `d5613ce` 之后）
- 依据：`docs/03-业务模块设计/教务中心/施工包/教务归档-生产级施工包.md`（design_ready，外部证据 PARTIAL）
- 模式：CLAUDE.md B 模式，照卡施工全栈连续。

## 一、数据库（迁移 0078_13b_archive，链在 0077 后）
2 张新表：
- `t_aa_archive_batch` 归档批次（一学期一批次 D-01，唯一 term_id；DRAFT/CHECKING/READY/MISSING_ITEMS/ARCHIVED/CANCELLED）
- `t_aa_archive_item` 归档物料（9 数据域完整性：学籍/注册/异动/培养方案/教学任务/课表/考务/成绩/毕业资格，D-02）

真实 MySQL `alembic upgrade head` 建 2 表通过，链 0077→0078 单链。审计复用共享 `AffairsAuditTrail`(biz_type=AA_ARCHIVE)，不建独立审计表（施工卡 §1 勘误：手册所称 t_aa_audit_trail 代码实为共享 AffairsAuditTrail）。

## 二、后端（academic_affairs_archive_service.py）
- 建批次：按学期(一学期一批次,重复409)。
- **完整性检查**：聚合 9 数据域记录数(有 term_id 的按学期计:REGISTRATION/TEACHING_TASK/SCHEDULE/EXAM;有 term_code 的:STATUS_CHANGE/GRADE;无清晰学期关联的按租户全量:STUDENT_STATUS/PROGRAM/GRADUATION)→ present/missing → missing_count → READY/MISSING_ITEMS。
- **确认归档**（D-04）：批次→ARCHIVED + **学期 status→ARCHIVED（学期封存）**；MISSING_ITEMS 需 force 强制归档。
- **特批解冻**（D-06）：仅 SCHOOL_ADMIN，批次 ARCHIVED→DRAFT + 学期恢复 PUBLISHED，原因≥5字。
- 取消批次（已归档不可取消，走解冻）。
- router：8 个 `/archive/*` 端点。管理用 `academicAffairs.archive.manage`；查看 `.view`；学生越权 403。

## 三、前端
- `AaArchiveConsoleView.vue`（控制台）：批次列表 + 详情（9 数据域完整性表 + 生命周期动作：检查/确认/强制/解冻/取消）。
- api：`academicAffairsArchiveApi` 命名导出。routes：`archive` 子路由。navPlan：`aa-archive` 从 planned 翻 implemented（归档控制台叶子 + 保留缺失提醒/批量/导出 P()）。

## 四、验证证据（真实 MySQL）
- 迁移：MySQL 建 2 表通过。
- **pytest `test_aa_archive.py` 4 passed / 0 failed（123s）**——建批次+同学期重复409、完整性检查(9域,学籍/方案present)、确认归档(学期status→ARCHIVED校验)、MISSING强制归档、特批解冻(原因过短400+学期恢复PUBLISHED校验)、学生越权403。SQLite 快跑同样 4/4。
- 前端 `vite build` exit 0（16.9s）。

## 五、已知欠账（非阻断）
- **D-04 学期封存后的全域写保护横切（该学期所有教务写端点 409）本期未落地**——本期只把学期 status 置 ARCHIVED，未在 R1-R6 各写端点插入"学期是否 ARCHIVED"拦截（横切改动跨全部教务写端点，工程量大且触及既有已验证代码，登记为闸门清单欠账，上线前统一加拦截中间件/依赖）。
- 完整性检查的学期口径：部分数据域（学籍/培养方案/毕业资格）无清晰 term_id 关联，本期按租户全量计数作近似"是否有数据"指标，非严格按学期切分——真实按学期归档需各域补 term 维度，留后续。
- 归档缺失提醒（主动推教务处待办）、批量归档（多学期一次归档）、归档导出（打包 xlsx/水印封存包）为 planned。
- 毕业资格归档接 t_orientation_material(material_type=ARCHIVE) 的真实判定（施工卡 D-05）本期未接，归档物料 GRADUATION 域按 AaGraduationAuditBatch 计数。
