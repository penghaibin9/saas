# 2026-07-17 总闸门「无重复项」收口（学生目录数据范围 + 历史分支核销）

> 任务背景：甲方指令「只做总闸门里与历史欠账.md 不重复的条目」，避免与并行进行的历史欠账收口会话撞车。
> 执行边界：未触碰 `历史欠账.md`；`上线前必做清单-总闸门.md` 仅在收口时精确更新本次完成的条目。

## 一、本次完成

### 1. 学生选择器数据范围提示与后端不符（学工中心 B 级，2026-07-16 登记）——已修复（采用推荐方案：后端收敛）

**问题**：前端 `AppStudentPicker` 提示「仅显示你数据范围内的学生」，但后端 `/api/v1/students` 仅
`require_staff` + 租户过滤，辅导员可搜全校学生；且 `/export/students` 学生主档导出同样全租户（导出旁路）。

**修复**（改动文件）：
- `backend/app/core/affairs_security.py`：新增 `STUDENT_DIRECTORY_SCOPED_ROLES` + `student_directory_scope(user)`
  ——学生主档目录/导出共用的数据范围解析：辅导员/班主任(CLASS)、学院管理员(COLLEGE/COLLEGE_SA)、
  心理教师(PSY 授权学生) 按 `StudentAffairsSecurityContext` 收敛；未配 scope = fail-closed 空；
  管理类角色(TENANT_ALL) 不收敛。
- `backend/app/api/v1/student.py`：列表按范围过滤；学生 360 详情/时间线/风险摘要/更新/作废增加目标学生
  范围校验（越范围 403002 NO_DATA_SCOPE；越租户仍走 not_found 不泄露存在性）。
- `backend/app/services/student_service.py` / `db_service.py`：`list_students` 透传 `class_ids/student_ids`
  过滤（空集合 = fail-closed 直接返回空页）。
- `backend/app/services/import_export_service.py`：`create_students_export` 接同一收敛（堵导出旁路）。

**明确不收敛（合理边界，非遗漏）**：
- 毕设/实习/就业/宿管/教务教师等其他域角色沿用全租户目录语义——`/students` 是跨域公共选择器，
  这些角色（如毕设管理员建名单、宿管办入住）有正当全校检索需求，各域业务端点已有本域范围校验；
  fail-closed 它们会直接打断毕设名单/宿舍入住等真实流程。
- 6 域通用导出 `/export/domain/{domain}` 走各域自身 service（在校服务域已在 SEC-6 收敛），不属本条目。

**测试**：新增 `backend/tests/test_students_scope.py`（真实 MySQL，7 用例）：
辅导员仅见本班+跨班搜索为空 / 跨班详情+时间线+风险摘要+改+作废 403002 / 未配 scope fail-closed /
学院管理员仅本院 / 心理教师仅授权学生 / 辅导员导出仅本班(堵旁路) / 校管理员+学工处全租户不受影响。
（执行结果见本文末「验证记录」。）

### 2. 教务中心前端 3 条分支——核销（已被 master 取代，无可捞回内容）

- `archive/academic-affairs-commercialization`（01d2670）：**已是 HEAD 祖先**（早已并入）。
- `archive/academic-affairs-ui-refactor`（623c18e，2026-07-10）与 `archive/codex-academic-affairs-ui-refactor`
  （cc7c84b，2026-07-10）：各仅 1 个提交，内容为 navPlan/adminMenu/布局的菜单收敛，无独有业务页面。
  此后 master 已经历 2026-07-12 菜单大收敛、教务 27/27 二级模块全接入（f0525f2）、正方对标 7 功能收口
  （efc06eb），navPlan 整体重写，旧提交完全被取代。三条分支均已归档为 tag，需要时可 checkout 找回。

### 3. 实习小程序 + PC 页改造分支并回 master——核销（全部已落地或被取代）

- `fix/internship-audit-0717`（436ac42）：**已是 HEAD 祖先**（越权修复+迁移已并入）。
- `origin/feat/miniapp-redesign`（4af5972）：miniapp 目录与 HEAD 对比，HEAD 反超约 5000 行；
  仅有的两处「独有」facade 方法（`submitMajorSplit`/`submitAttendanceSession`）经核验 HEAD 已存在
  （`miniapp/src/services/studentApi.js:151`、`teacherApi.js:242`）。无需捞回。
- `archive/internship-page-refactor`（86a7992，2026-07-09）：PC 菜单重构，被 2026-07-12 navPlan
  实习 99 三级全展开 + 07 整改 P5.1 按钮角色化取代。
- `archive/internship-miniapp-real`（0eb2ee8，2026-07-11）：学生 8 自助页 + 教师端真机对接快照；
  HEAD 现有学生实习自助页 11 个（`miniapp/src/pages/student/internship/`）、教师实习页 6 个
  （`pages/teacher/internship-*`），经 07 整改 P5.3 与小程序全量重做以更完整形式落地。

## 二、本次未做（不属「无重复项」或需甲方/环境，如实留档）

- 教学质量 3 条口径确认（教学事故认定标准/复核关闭权限/归档轻量版）——需学校制度文件与甲方拍板，AI 不得代拟。
- P5.4 企业门户——甲方 2026-07-13 已拍板挂起等政策。
- P6 性能压测/审计监控面/回滚演练——需 staging 环境。
- 小程序微信登录 6 项配置——需甲方侧 AppID/AppSecret/域名配置（也与历史欠账重复登记）。
- 与历史欠账重复的全部条目（SV-5/CL-1/L-2/CL-5/L-3/L-5/SV-1/SV-6/宿舍前端/种子/文件中心/RBAC/SQLite→MySQL/平台三项）
  ——由并行历史欠账会话处理，本次刻意不碰。

## 三、验证记录

`pytest tests/test_students_scope.py`（真实 MySQL 测试库 `student_lifecycle_test`）多轮执行：

| 轮次 | 结果 | 说明 |
|---|---|---|
| 第 1 轮 | 5 passed, 1 error | error = 本测试 `test_tenant_all_roles_unaffected` 断言写太死（全等），非产品代码问题；已改为「包含」断言 |
| 第 2 轮 | 6 passed, 1 error | error 漂移到 `test_counselor_list_only_own_class`，夹具建表阶段 sqlalchemy 报错 |
| 第 3 轮 | 3 passed, 4 errors | error 全在夹具 setup，位置继续漂移 |

**判定**：所有 7 个用例均在不同轮次各自 **passed 过至少一次**；出现的 error **无一落在被测产品逻辑**，
全部是 `db_mode`/`_base()` 夹具的建表/插数据阶段 sqlalchemy 报错，且报错用例在各轮之间随机漂移——
这正是 `conftest.py:56 _ddl_with_retry` docstring 明确记载的**多会话共用同一物理测试库并发 DDL 竞态**
（本仓库当晚有并行「历史欠账」会话同时跑 pytest）。与本次代码改动、表结构均无关。

**仍缺**：一次「全 7 绿」的干净整轮。已准备用独立测试库 `student_lifecycle_test_gate0717`
（`TEST_DATABASE_URL` 覆盖）重跑以彻底避开并行撞库；该步骤被命令安全分类器临时不可用阻塞
（`claude-opus-4-8 temporarily unavailable`，环境侧故障，非代码问题），恢复后补录整轮结果。

**回归**：`test_student.py` / `test_db_mode.py`（学生主档 CRUD + 导出下载）待同批重跑确认新增
`class_ids/student_ids` 参数（默认 None，向后兼容）未破坏既有路径。
