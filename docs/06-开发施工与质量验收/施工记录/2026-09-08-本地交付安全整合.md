# 本地学工与岗位实习安全整合

- PR： https://github.com/penghaibin9/saas/pull/266 （Draft，尚未合并）。
- 整合分支：`codex/integrate-local-deliveries-20260908`，独立目录 `职校学生全生命周期系统-integration-20260908`。
- 远端 main 基线：`a309a84d60f81e418ee1675a6df023a63fefac41`。
- 本地学工源：`main` / `a0c7c5761563d5c27f161cb4fa348e1fe41ff566`；岗位实习源：`codex/internship-ui-refactor-20260905` / `605d1ef91c0c06ff73d3f4a48afffca54e6ba77c`。
- 用户原 main 工作区未提交的说明文档、AGENTS 和附件保持原位；岗位实习源已独立提交并备份。整合不直接覆盖原工作区。
- 本地已认可公共 UI 壳为统一基准。未来分支保留业务服务、权限和页面能力，并接入当前壳；不能整文件恢复旧导航、旧布局、共享路由或锁文件。
- 远端 Draft PR #263/#264/#265 是后续独立批次，本次未纳入。

## 2026-09-08 收口修复（基于 4d5145efc 的 CI）

1. 修复新旧壳整合后浏览器用例的真实入口：账号菜单、中心导航、企业准入详情、邀请激活、协议生成、批次与学生菜单；保留实际写请求、令牌轮换、版本和刷新回读断言。
2. 企业招聘访问结束单独说明，避免误报招聘季冲突；正常实习协同继续可用。
3. 对新增及同调用链直接主键查询使用既有 `tenant_get`；销假办结额外拒绝跨学校或学生关联不一致。没有扩大权限。
4. 修复宿舍房态覆盖投影漏掉 `studentName/studentNo`，同一入住对象由正式 API 回读验证。
5. 路由索引按实际对象嵌套解析，绝对子页不再误作后续兄弟页面的父路由；不存在的重定向目标不冒充注册路由。
6. 更新过期静态合同与测试前置受理岗位，不恢复旧卡片、不放宽并发/租户/业务校验。大文件登记仅保留用户认可的独立离线原型，重复登记仅限两个独立构建端的请假展示合同。

本批验证：PC lint 与 1997 项单测通过；企业端 lint、138 项单测、构建及 6 项浏览器交互测试通过（后者是现有 API 替身 UI 测试，不冒充真实学校闭环）。路由索引 641 个导航入口 missing=0；控制面合同、租户查询棘轮、仓库卫生通过。独立 MySQL 专项首次 7 项通过含宿舍回读与跨租户 helper；学工合同批次 57 通过、1 失败、1 跳过，失败为过期 FULFILL 动作断言，修正后该文件 3 项通过；销假跨租户/错误学生关联负向测试 1 项通过。跳过项未计通过。

自动化检查使用独立 `codex_merge_checks_20260908_test`，不会清理人工联调库。测试连接凭据不入库。

## 2026-09-08 角色权限与业务工作区衔接（基于 492017369）

- 工作台补齐 `WORKBENCH` 模块标识，使已有 `workbench.home.view` 权限实际进入路由守卫；自定义角色验证改用其真实可访问入口，不再默认所有学校岗位都能打开工作台。
- 学校能力投影一次读取商业授权，避免逐模块重复查询；不跨调用缓存，未知、缺失及读取失败的商业决定仍拒绝授权。32 项能力/权限 MySQL 专项通过，补充缺失授权负向测试后快照文件 3 项通过。小型单岗位权限解释实测 28 SELECT；这不是 20K 规模证据，远端原 60 次预算保持不变。
- W12 真实浏览器矩阵 1 项通过，覆盖全部 14 个学校/平台岗位、菜单投影、直链拒绝、会话轮换与平台办理。本地环境按现行 CI 配置部署 SHA，使用独立签名会话；未修改业务安全门禁。
- 学工紧凑工作区 5 项真实浏览器检查通过：楼栋/房间/床位联动并携带床位进入入住，1024px 无横向溢出，活动列表、心理隐私提示及资助批次选择。移除的旧卡片断言由真实业务交互和有效工作区尺寸检查替代。
- 自主实习申请 7 项真实浏览器步骤通过：学生上传 PDF、跨租户拒绝、教师下载/驳回、学生重交、新附件审核通过、学生回读真实去向、管理员及 MySQL 审计。办理定位跟随当前独立工作区，保留文件绑定、业务版本和写后回读断言。
- 实习批次 3 项通过：规则错误不发请求、创建及编号唯一性、编辑/冻结名单/启用/只读/结束/审计；阶段编号按页面自动生成的 `STAGE1` 核对，同时新增实际日期的 MySQL 核对。
- 修复学生 PC 实习请假记录漏显示 `reviewComment`，学生可看见驳回原因后重交；页面沿用既有风格。学生端 lint、222 项单测、构建通过；教师 PC 路由定向 12 项通过。
- 请假、退回重交、销假、真实 xlsx 导出、统计与 MySQL 对账 5 项最终复测通过；统计口径详情深链刷新也通过。统计核验脚本所需独立 DB 环境已配置，未复用其他窗口的库。
- 独立真实浏览器库为 `codex_merge_browser_20260908_e2e`，完成从空库迁移至当前 Alembic head；教师 PC 5276、学生 PC 5296、API 8176 仅用于本次整合验证。人工学工联调库及原 main 工作区未改动。

## 2026-09-08 当前壳回归与旧测试前置收敛（实现基于 3bc221a01）

- 实习测试按现行学校职责补齐围栏、租户上下文、保险保障期及实习期内打卡，不修改生产权限、合规门禁或状态机。企业提交岗位后由学校通过正式接口配置围栏，使用返回的新版本上架；保留末席竞争、满员回滚、准入与黑名单的拒绝断言。
- 成绩 11 项 MySQL 回归通过。随后一组 40 项中 31 通过、9 失败；定位并修正日期窗口、学校围栏配置及遗漏的发布前置后，合批复测 57 项中 56 通过，剩余为保险 fixture 的 DateTime 转日期格式，修正后定向 1 项通过。不同轮次有重叠，不能累加为独立通过数量。
- 迎新旧状态直改测试改为验证拒绝与写后不变；住宿异常进入异常台账，不能覆盖床位真值。O5 正式签名报到、学院确认、学生 PC/移动读同一事实、关闭批次、归档与重复回读已在上述合批回归通过。
- 打卡测试实际调用短时凭证预检，再提交定位并验证幂等及异常申诉；不以 mock 凭证绕过正式服务。缺少模块授权的外校教师先返回 403 且无数据，同校越范围仍验证 404。
- 旧壳浏览器用例跟随真实新入口：创建企业进入详情后返回列表、协议材料安全可用后登记、学生请假从原记录打开进度与办理。企业创建/审核/暂停/恢复和列表电话脱敏已真实浏览器通过 1 项；其余新增调整待候选 CI 完整复核。
- 学生门户首次完整浏览器复审：35/36 路由通过，32 个 tab、5 项功能、96 个分辨率场景通过。就业页首次出现 portal-config 尚未完成即截空屏，复审脚本改为等待真实工作区出现；缺失仍失败，不把空页算无溢出。完整复审尚待新候选 CI。
- 四套主题技术切换已通过，复审额外发现办事大厅黑色主题分类按钮继承旧浅底；公共 `.sp-tab` 改用现有主题变量（影响学生门户所有使用该类的业务分类按钮，保留布局与选中规则）。修复后四主题可见点击、对比度、分类操作、刷新保留专项全部通过；学生 lint/build 通过。枚举扫描 36 路由、67 个 tab 无裸枚举，当前双栏黑色主题通过。
- 本地 pytest 仍使用独立自动化库；通过该本地 MySQL 公共 CA 校验连接，避免重复扫描 Windows 系统证书，不降低 TLS 检验，也未改变项目数据库配置。临时日志、诊断工具和凭据环境未加入 Git。

## 2026-09-08 第二轮候选收口（基于 b8c7c0dbc）

- 远端已通过 W13 完整 20K IAM 拓扑、学校 IAM 浏览器、管理 PC lint/test/build、移动 H5/微信构建、备份恢复演练、学生门户完整复审等检查；以各 workflow 实际 SHA 为证，不能替代下一候选结果。
- 学生主题专项的源码保护测试仍查找已移动到 review-lib 的真实主题点击方法。改为同时检查调用方与共享方法，222 项学生测试重新通过。
- 教务未授权拒绝用例替身仍挂在旧 feature_enabled；改为对当前权威 effective_features 返回撤权结果，并增加撤权前可访问断言。真实 MySQL 的该文件 5 项通过，权限服务未放松。
- 岗位详情测试改为独立编辑页与发布管理；通过学校表单保存围栏，保留 expectedVersion。黑名单从企业详情办理，仍验证学生目录不可见、界面禁上架及同一认证上下文的正式命令拒绝。
- 独立 MySQL 浏览器库中企业/岗位完整 7 项通过，审计回读覆盖创建、审核、暂停恢复、邀请、企业报送、学校修改、上下架、黑名单进出、风险标记解除。日志：`.codex-temp/integration-position-current.log`；未修改人工联调库。
- 协议 PDF/归档、生产运行请假标题、学生消息直达详情、毕设材料菜单与中文教师身份等旧验收定位已同步当前壳。保留真实上传、写后回读、归档和跨端合同；这些调整尚待下一候选 CI 完整复核。
- 原 main 尚未提交的 6 个文件已另外按原路径备份并逐个 SHA256 校验，补充 patch 与基线清单。备份位于 `Codex代码备份/学工收口-20260908-063534/合并候选-b8c7c0dbc-原工作区增量`，原文件保持原位。

## 待验与阻断

- 新版壳入口更新后的真实浏览器矩阵尚待最新提交 CI 验证，不能使用历史 SHA 的结果声称修复已在远端通过。
- 多个旧 Golden 视觉测试仍要求圆角卡片、旧字号/留白、旧组件结构，需按已认可紧凑工作区更新有意义的可见交互与布局检查，不能删断言或跳过。
- W13 20K IAM 原查询预算超限已在 b8c7c0dbc 的完整拓扑 workflow 通过，未提高预算；下一候选仍需复核。
- 岗位实习专项旧运行触发 25 分钟超时；V8 seed 与部分深链流程待继续诊断。
- Native S3 固定检验历史产品 SHA `f34b800249252e84f9ab5f34b801742b76c0e68c`，其公网健康检查返回 DEPLOYMENT_PENDING。这不是当前整合提交部署证据；本次未发布生产环境，也未绕过门禁。
- `492017369` 的 Main 全量后端分片还发现旧 fixture 缺少租户上下文、岗位上架缺电子围栏等前置问题；只修对应 fixture，禁止通过放松租户或发布规则解决。
- 只在当前候选的相关安全、契约和流程验证完成后合入 main；原始源分支、提交与备份继续保留，不强推、不删除远端业务分支。

## PR #266 Actions 修复（基线 b5c4abd8e）

- 远端 main 仍为 a309a84d60f81e418ee1675a6df023a63fefac41；修复只在 codex/integrate-local-deliveries-20260908，未合并或部署。
- Main 分片 1 的减免待办缺失：MySQL DATETIME 会将微秒进位到下一秒，新待办落在 asOf 读取边界之外。写入侧截断到秒，保留读取边界；新增定时钟 MySQL 回归先复现失败，再验证修复。
- Main 分片 2 的辅导员交接用例：学生创建时间也可能进位。此故事交接已有学生，因此准备明确的历史创建时间；不改变权限、范围或分页服务。
- Main 分片 6、CI 岗位实习闸门及变更 pytest 的共同失败：旧授权替身未作用于 effective_features。改挂当前权威入口，并保留撤权前 200 / 撤权后 403 断言。
- IX009：学生壳正文 stacking context 导致快捷栏拦截岗位选择弹层；解除正文独立层叠并补滚动底部留白。保留已认可的浮动快捷栏和布局。
- Golden 旧卡片、旧标题与隐藏窄栏文字定位同步新壳；补实际对话框打开/取消、选中对象保留、紧凑行和窗口边界验证，不恢复旧装饰卡片。
- 移动资助测试通过搜索选择明确批次、真实上传、同一申请 ID 回读验证附件归属；回读轮询复用当前学生会话，避免每次轮询重复登录。
- V8 实习 seed 显式设置并恢复租户上下文，历史保险前置数据补齐覆盖日期；不放松租户、保险、成绩及归档服务。
- 周报退回重交使用原记录“按意见修改”，核验版本递增；归档入口按当前学生标题定位。
- 本地独立 MySQL：减免/贷款/岗位授权 7 项、辅导员交接 3 项通过。学生 PC lint、222 项测试及生产构建通过；IX009 六项跨端匹配通过；移动 Real Task 四项分别复核通过；16 项对应 Golden 页面检查通过；GDJ05/GDJ08 两项深链通过。
- 本轮日志保存在忽略目录 .codex-temp/pr266-*.log；浏览器数据位于 codex_merge_browser_20260908_e2e，后端测试使用 codex_merge_checks_20260908_test，未修改人工联调数据库。
- S1 的 HTTPS/Nginx 完整运行留给候选 CI；本地仅同步其参与名单入口，未用 HTTP 结果冒充 HTTPS 验收。Native S3 的公网历史 SHA 部署前置仍为 DEPLOYMENT_PENDING，未发布生产或绕过检查。
- 以上是本地未提交修复树证据，远端通过与否必须以随后生成的 exact-head 候选 Actions 为准。
- V8 GJ05–GJ09 五项分段验证全部通过，包括成绩发布与申诉重算、归档预检、冻结版本、包恢复校验及就业交接；未重跑无改动的已通过前三项。
