# 2026-08-18 教务 B 线 PR #146 剩余任务收口

> 固定 PR：`#146`  
> 固定分支：`agent/academic-b-schedule-selection`  
> 本轮父 HEAD：`799a82e61c24a52724dbb071e751850450be2e68`  
> 唯一施工总册：`B_教务Schedule_Selection_当前代码精确施工总册_V2.1_20260816.md`

## 1. 本轮纠偏

B-P1-09 不是生产实现仍缺失。现有 `academic_affairs_production_audit_guard` 已把 Selection 的 staff scope、SQL count/offset/limit、冲突报表 SQL group/count/paging 正式安装回 public Selection owner；`test_aa_selection_read_production_contract.py` 已锁定这些合同。

因此 B-P1-09 的真实剩余项是**施工记录未回写 P1=0 证据**，不是再造第三套 read service。

## 2. 真正剩余的 B-W4 cross-line activation

旧 B Gold 的边界是：B 只接受显式 provenance snapshot，但当时上游 shared runtime 尚不存在，因此不在 B 内猜 formation policy。

现在 INT 已有 sealed runtime：

- A-C4 canonical formation policy；
- `TeachingTask.source_program_course_id`；
- `TeachingTask.formation_mode`；
- canonical Task generation 写入 exact ProgramCourse provenance；
- `academic_affairs_task_formation_provenance_service` 只按 direct persisted source link 解析，旧脏数据保持 UNKNOWN/CONFLICT。

本轮 B 只补**consumer seam**：正式 SelectionCourse writer 在 shared runtime 存在时读取 canonical provenance snapshot，经现有 B dependency normalizer 要求 `PROVEN + sourceProgramCourseId + mode`，再调用 A 的 canonical `selection_eligible`。B 不复制 A enum、不从 course/class/major/grade/current activation 猜 formation，也不修改 shared model/Alembic。

Standalone B checkout 仅在整个 upstream provenance module 尚不存在时保留历史 independent subseal；一旦 shared runtime 出现在 integration tree，缺失/部分 provenance 必须 fail-closed，非 selectable mode 必须拒绝。

## 3. Gate

新增 `Academic B W4 Formation Handoff`：

1. checkout PR exact HEAD；
2. pin INT `35d4e485bb96f17ceb0697ec75b506947a89b61f`，并证明 provenance sealed commit `6bb48f1c055d8fd16fe9563ba4ea64c36ac15f9a` 是其祖先；
3. runner-only ephemeral merge，绝不把 INT/main 写回 B；
4. fresh MySQL + 单 Alembic head；
5. 验 B-P1-09 read production contract；
6. 验 B formation dependency；
7. 验 SelectionCourse writer handoff：SELECTABLE 接受、非 SELECTABLE 拒绝、缺 direct provenance fail-closed；
8. 同跑 INT direct provenance MySQL truth 与 B supply owner contract。

任何 pending/queued/cancelled 都不计 GREEN；本提交移动 B HEAD 后，旧 `799a82e6` Final Gold 只作为历史 pre-upstream Gold，不冒充新 HEAD 的最终证据。

## 4. 仍属外部 Owner、不得在 PR #146 越权施工

- shared formation schema / Alembic：INT Owner；
- term/window/scope/rule 独立 version/hash 与 DB tightening：INT Owner；
- C 线考勤/考务/成绩对 TeachingRoster 的最终同头签字：C/INT Owner；
- main/INT 的正式集成与合并：未经明确授权不执行。

本轮目标是把 **B 自己任务单里能在 B consumer 边界完成的剩余项清零**，同时保持共享 Authority 单 Owner。
