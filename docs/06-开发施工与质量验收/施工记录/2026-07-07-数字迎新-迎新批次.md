# 2026-07-07 数字迎新 › 迎新批次（生产级全栈闭环）

## 结论
**迎新批次 = implemented（前后端真实闭环，已验证）。** 数字迎新点亮 +1（8/19 → 9/19）。

## 做了什么（真实、非 mock）
- **数据库表**：新增 `t_orientation_batch`（tenant_id/batch_name/batch_no/year/起止/报到起止/status/planned_count/remark + 公共审计字段 + 软删 + 乐观锁；批次编号租户内唯一）。经 `init_db.create_all` 建表（与既有 orientation 表同路径）。
- **状态机**：`DRAFT 草稿 → ACTIVE 进行中 → CLOSED 已结束`；非法流转拒绝（草稿不能直接结束、进行中不能作废、已结束不可编辑、重复启用拒绝）。
- **后端服务**（orientation_service.py）：list/get/create/update/activate/close/void 共 7 方法，租户过滤 + 编号查重 + 每步写 `OrientationAuditTrail`（bizType=BATCH）。
- **后端接口**（/api/v1/orientation/batches）：GET 列表 / GET 详情 / POST 新建 / PUT 编辑 / POST activate / POST close / POST void。
- **测试**：`test_batch_closed_loop`（新建→查重→非法流转拒绝→启用→重复启用拒绝→编辑→结束→已结束不可编辑→审计留痕）。
- **前端页**：`OrientationBatchListView.vue`（ModulePageShell + 筛选 + DataTable + 新建/编辑抽屉 + 启用/结束/作废二次确认 + loading/empty/error 状态），接真实 `/orientation/batches`（离线 mock 兜底）。
- **路由**：`/admin/orientation/batches`；**navPlan**：迎新批次 planned → implemented（侧栏已点亮、可点、高亮）。

## 验证
- 后端：`pytest tests/test_orientation.py` → **12 passed**（含新批次闭环）。
- 前端：`eslint` 改动文件 → **0 错**；`npm run build` → **通过**。
- 预览：进 `/admin/orientation/batches` 页面真实渲染、侧栏「迎新批次」点亮高亮、零控制台报错。

## 改动文件
后端：`models/orientation.py`、`models/__init__.py`、`schemas/orientation.py`、`services/orientation_service.py`、`api/v1/orientation.py`、`tests/test_orientation.py`
前端：`modules/orientation/api/orientation.api.js`、`views/admin/orientation/OrientationBatchListView.vue`、`modules/orientation/orientation.routes.js`、`config/navPlan.js`

## 待办（记账，不阻断）
- 权限 key：当前视图沿用 `orientation.student.view` 兜底；应补 `studentAffairs.orientation.batch.manage`（旧 key 保留兼容）——纳入"权限 key 迁移"统一处理。
- alembic 迁移 0015：把 orientation 全部表纳入迁移史（现靠 create_all）——单独任务。
- 批次与新生台账的 `batch_id` 关联（按批次筛新生）——下一页做"新生数据"时接。

## 下一个建议
按第一批继续：**新生数据 / 新生信息核验 / 报到资格**（都需新表+接口+页），或先把 partial 的**绿色通道独立页 / 未报到 / 迎新统计**补齐（后端已有接口，前端补页即可，更快点亮）。
