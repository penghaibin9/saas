# 施工记录 · 宿舍与公寓 后端权限+数据范围+SEC-4（2026-07-12 夜续）

> 承接交付前验收发现（历史欠账 2026-07-12 + 总闸门 P0）。本次先做宿舍后端安全闭环，前端 6 页另提交。仅改宿舍相关后端/测试 + 本记录，独立提交，禁 push。

## 1. 改动内容

| 文件 | 变更 |
|---|---|
| `backend/app/api/v1/student_affairs.py` | `/dorm/*` 15 端点由 `require_staff` 改 `require_permission("studentAffairs.dorm.<动作>")`（view/resource.manage/allocation.manage/transfer.create/transfer.approve/inspection.manage，对齐 13A §12）；**新增 5 端点**：`GET /dorm/transfers`(调宿列表)、`GET /dorm/check-tasks`(检查任务列表)、`GET /dorm/check-tasks/{id}/records`(检查记录列表)、`GET /dorm/exceptions`(异常列表含夜不归宿)、`POST /dorm/exceptions/{id}/handle`(处置异常)；`CheckRecordBody` 增 `studentId`。|
| `backend/app/services/affairs_dorm_service.py` | **SEC-4 修复**：`submit_check_record` 异常→风险不再硬编码 `student_id=0`——有涉事学生则绑真实 student（夜不归宿必填，否则 400），纯房间级异常只建异常记录、不造孤儿风险；**DORM_BUILDING 数据范围** `_dorm_scope_building_ids`（宿管仅见 `manager_teacher_key` 匹配的楼栋），应用到 `list_buildings`；**新增** `list_transfers/list_check_tasks/list_check_records/list_exceptions/handle_exception`（调宿/检查按楼栋收敛）。|
| `backend/app/core/permissions.py` | 新增 `DORM_MANAGER` 角色 → `{"studentAffairs.dorm.*"}`（宿管仅宿舍域，不得见学业/心理/困难/处分）。|
| `backend/app/services/mock_auth_service.py` | 新增 `dorm01`(DORM_MANAGER，DORM_BUILDING 范围) 测试身份。|
| `backend/tests/test_affairs_dorm.py` | M5 改为「纯房间级异常无孤儿风险」；新增 M8（涉事学生→绑真实风险 + 夜不归宿缺学生 400）、M9（宿管最小权限：dorm 200 / 心理·风险 403 / 无宿舍权限角色 403）、M10（DORM_BUILDING 范围：宿管只见本人楼栋）。|

## 2. 安全红线落地（可核验）

- **SEC-5(宿舍域)**：`/dorm/*` 全部接入细粒度权限；宿管 `dorm01` 可用宿舍(200)，但 `/mental/list`、`/risk/records` 均 **403**（宿管不得见心理/风险明细）；无宿舍权限的就业老师 `/dorm/*` **403**（M9 实证）。
- **DORM_BUILDING 数据范围**：宿管只看到 `manager_teacher_key` 匹配自己的楼栋（M10 实证：dorm01 只见「紫荆A」）。
- **SEC-4**：异常→风险绑真实学生；纯房间级异常不再生成 `student_id=0` 孤儿风险（M5/M8 实证）。

## 3. 验证（实际执行）

- **后端测试**：`pytest tests/test_affairs_dorm.py` → **10 passed**（0:03:12，真实 MySQL）。覆盖：建楼铺床/级联入住回写/占用·性别冲突409/调宿两级审批执行/自选开关/一步建楼 + SEC-4(M5/M8)/宿管最小权限(M9)/DORM_BUILDING范围(M10)。
- **迁移**：无 schema 变更（`student_ids_json` 列原已存在），`alembic heads` = 单一 `0044_13a_psy_referral`（未新增迁移）。
- 本次为后端，前端未改；前端 6 页(房源/入住/调宿退宿/检查/异常/统计)另做另提交。

## 4. 回滚
`git revert <本提交hash>`；无迁移需回滚。

## 5. 遗留（诚实登记）
- **前端 6 页未建**（本次后端；页面为下一提交）。
- `t_cs_dorm_exception` 无 building 列，宿管**异常列表**的楼栋级收敛为后续项（当前异常列表租户级；调宿/检查已按楼栋收敛）。
- 违纪/困难/资助/谈话/请假/归档 等其余域细粒度权限仍待各自模块接入（SEC-5 未清，见总闸门 P0）。
