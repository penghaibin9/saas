# 施工记录 · 宿舍与公寓前端 6 页（2026-07-12 夜续）

> 承接宿舍后端闭环 `6bc6c4d`。本次建宿舍 6 个 PC 前端页并真机验证。仅改前端 + 本记录，独立提交，禁 push。

## 1. 新增/改动文件

| 文件 | 变更 |
|---|---|
| `frontend/src/views/admin/student-affairs/dorm/DormResourceView.vue` **(新)** | 房源管理：楼栋→房间→床位三级级联，新建楼栋(可一键铺床)、铺床。|
| `.../dorm/DormCheckinView.vue` **(新)** | 入住管理：选楼→房→床入住/退宿，学生自选开关。|
| `.../dorm/DormTransferView.vue` **(新)** | 调宿与退宿：发起调宿 + 辅导员/宿管两级审批(通过/驳回)。|
| `.../dorm/DormCheckView.vue` **(新)** | 宿舍检查：检查任务 + 录结果(异常绑涉事学生，夜不归宿必填)，联动风险。|
| `.../dorm/DormExceptionView.vue` **(新)** | 宿舍异常：异常列表(含夜不归宿) + 逐条处置。|
| `.../dorm/DormStatsView.vue` **(新)** | 宿舍统计：入住率 + 按楼栋分布。|
| `frontend/src/modules/student-affairs/api/studentAffairs.api.js` | 增 14 个 dorm 方法(建楼/铺床/入住/退宿/自选开关/调宿列表+提交+审批/检查任务列表+建+记录列表+录/异常列表+处置)。|
| `frontend/src/modules/student-affairs/studentAffairs.routes.js` | 增 6 路由(dorm/resource·checkin·transfer·check·exception·stats)。|
| `frontend/src/config/navPlan.js` | `sa-dorm` 6 叶子由 `P()`/旧 campus 链改 `I()` 指向新路由(菜单接通)。|

## 2. 验证（实际执行）

- **前端 build**：`npm run build`(vite) → **✓ built in 14.48s**(2599 modules，exit 0)。
- **真机走查**(后端 uvicorn:8000 真连 dev MySQL + 前端 vite:5173)：
  - 播种 1 栋「紫荆1号楼」(3×4×4=48 床，managerTeacherKey=dorm01)。
  - 以 SCHOOL_ADMIN 逐页打开 6 页**全部正常渲染**；房源页实数据显示楼栋(48/48 床)+ 指标卡；统计/入住/调宿/检查/异常均渲染。
  - **宿管红线真机确认**：切 `dorm01`(DORM_MANAGER) → `/dorm/buildings` 只见 **1 栋(本人负责楼)**；`/mental/list` = **403001**(宿管不得见心理)；房源页对宿管正常渲染。
  - 说明：2 处 console「Failed to fetch dynamically imported module」为 vite dev 快速切页的 HMR 瞬态(页面均已正常渲染、production build 绿)，非页面缺陷。
- 后端此前已 `10 passed`(真 MySQL)；前端对接即该已测契约。

## 3. 遗留（诚实登记）
- 敏感/交互采用 `window.prompt`(与现有风险/心理页同范式)。
- 真机走查在 dev 库留下 1 栋演示楼栋(非生产库)。
- 宿管「异常列表」楼栋级收敛仍为后续(t_cs_dorm_exception 无 building 列；调宿/检查已按楼栋收敛)。
- 至此宿舍与公寓前后端闭环完整；其余域(违纪/困难/资助/谈话/请假/归档)细粒度权限仍待接入(SEC-5，见总闸门 P0)。
