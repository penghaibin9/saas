# 施工记录 · 心理关注前端 5 页接通（2026-07-12 夜续）

> 承接 `22bfff5`（心理关注后端闭环）。本次补齐心理关注 5 个 PC 前端页面并真机验证。仅改前端 + 本施工记录，独立提交，禁 push。

## 1. 新增/改动文件

| 文件 | 变更 |
|---|---|
| `frontend/src/views/admin/student-affairs/mental/MentalAttentionListView.vue` **(新)** | 心理关注名单：列表恒仅摘要（noteMasked），登记转介，查看明细(原因≥5字→reveal+审计)/回访/关闭。|
| `.../mental/MentalWarningSummaryView.vue` **(新)** | 心理预警摘要：按等级聚合 + 按学生查摘要（仅需关注标记/计数，无明细）。|
| `.../mental/MentalReferralFollowView.vue` **(新)** | 谈话转介与回访：待回访(REFERRED/FOLLOWING)工作台，转介登记 + 回访 + 关闭。|
| `.../mental/MentalCrisisView.vue` **(新)** | 危机升级：升级→接风险中枢（幂等），关联风险可跳风险处置页。|
| `.../mental/MentalStatsView.vue` **(新)** | 心理统计：仅聚合（按状态/等级 + 在办危机），禁个体下钻。|
| `frontend/src/modules/student-affairs/api/studentAffairs.api.js` | 增 `listMentalAttention/getMentalReferral/getMentalSummary/createMentalReferral/followMentalReferral/escalateMentalReferral/closeMentalReferral/getMentalStats` 对接 `/student-affairs/mental/*`。|
| `frontend/src/modules/student-affairs/studentAffairs.routes.js` | 增 5 路由（mental / mental/summary / mental/referrals / mental/crisis / mental/stats）。|
| `frontend/src/config/navPlan.js` | `sa-mental` 5 叶子由 `P()`(planned) 改为 `I()` 指向真实路由（菜单接通）。|

## 2. 验证（实际执行）

- **前端 build**：`npm run build`（vite）→ **✓ built in 15.31s**（2587 modules，exit 0）。
- **真机走查**（后端 uvicorn:8000 真连 dev MySQL `student_lifecycle_dev` + 前端 vite:5173 代理 /api）：
  - 以 SCHOOL_ADMIN 会话登录，逐页打开 5 个页面**全部正常渲染，浏览器控制台 0 错误**。
  - **名单页**实数据渲染 2 条转介，`心理明细` 列显示 `[心理明细·受限，需授权+原因查看]`——**列表明细遮蔽红线在真机生效**。
  - **统计页**实数据聚合（关注在册 2 / byStatus REFERRED:2 / byLevel FOCUS:1,GENERAL:1 / 在办危机 0）。
  - 预警摘要 / 转介回访 / 危机升级 三页正常渲染（AppPageShell 水印、指标卡、表格三态）。
- **后端 API 冒烟**（真连 dev MySQL）：`POST /student-affairs/mental/referrals` → SUCCESS；`GET /mental/list` → noteMasked=true；`GET /mental/stats` → 聚合正确。
- 后端闭环单测此前已 `8 passed`（真 MySQL），前端页面对接的即该已测契约。

## 3. 说明 / 遗留

- 本次为纯前端接通，未改后端/迁移；后端已在 `22bfff5` 落地并测试。
- 敏感查看原因、转介/回访/关闭/危机升级说明采用 `window.prompt` 交互（与现有风险页 `StudentAffairsRiskListView` 同一范式）；后续如需统一改弹窗组件另议。
- 真机走查在 dev 库留下 2 条演示转介数据（student_lifecycle_dev），非生产库。
- `.claude/launch.json`（前端 dev 启动配置）为本次真机验证所加的开发配置，非业务代码，未纳入本提交。
- PSY_STUDENT 逐生授权维护 UI 仍为后续项（当前经数据行授权）。
