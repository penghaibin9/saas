# 教务中心 · 教学计划（收编）· R3 续工收口 · 施工记录

- 日期：2026-07-16
- 分支/工作树：`worktree-wf_ac9fb9a7-a93-8`（基线 `829bc2a8`）
- 任务定位：CLAUDE.md B 模式续工（第三轮补缺），只做 `aa-teaching-plan` 二级模块 navPlan 中此前遗留的 5 个三级叶子，不碰本模块其余已实现部分，不碰其他二级模块。

## 一、背景

《教学计划-生产级施工包.md》顶部 2026-07-14 用户最终裁决「收编」——不建独立 `t_aa_teaching_plan*` 域。
同日施工记录《2026-07-14-教务中心-教学计划收编-施工记录.md》把 navPlan 10 个叶子中的 3 个（年级/专业教学计划、
学期教学计划/课程开设计划、计划归档）收编到「培养方案」「教学任务」「教务归档」既有页面，**剩余 5 个叶子
（实践教学计划/计划审核/计划发布/计划变更/计划执行进度）当时明确留 `P()` 占位**，仅在施工记录里写了一句
方向性备注："审核发布走培养方案自身的审核发布链，执行进度看教学任务批次状态；实践计划在培养方案
requirement_json 实践环节内"，未真正接线，也未验证这些既有能力本身是否有自动化测试覆盖。

本轮（R3）任务：把这 5 个叶子逐条核实、接线、补测试，延续"零新表"红线，只在确有必要时做最小新增。

## 二、逐条处理结果

| 三级叶子 | 处理方式 | 落点 | designSource |
|---|---|---|---|
| 计划审核 | 纯 navPlan 接线（零新代码） | `/admin/academic-affairs/programs/console?tab=review`（培养方案两级审核工作台，`review_program` 既有实现） | `existing_code` |
| 计划发布 | 纯 navPlan 接线（零新代码） | `/admin/academic-affairs/programs/console?tab=publish`（培养方案绑定年级=发布，`bind_grade` 既有实现） | `existing_code` |
| 计划执行进度 | 纯 navPlan 接线（零新代码） | `/admin/academic-affairs/teaching-tasks/stats`（教学任务批次统计，`get_task_stats` 既有实现，已有 `test_tt5` 覆盖） | `existing_code` |
| 实践教学计划 | 新增前端 Tab（零新后端端点，复用课程明细读写） | `AaProgramConsoleView.vue` 新增 `practicePlan` Tab：筛选课程模块含"实践/实训/实习"关键词的课程行，交叉展示学分要求里"实践环节"模块目标学分；增删改复用既有 `/programs/{id}/courses`、`/programs/courses/{id}` 端点 | `existing_code`（筛选切面）+ `ai_proposal`（关键词筛选口径，见下方"待确认"） |
| 计划变更 | 新增后端端点 + 前端 Tab（复用既有版本链机制） | 新增 `POST /programs/{programId}/change`（`ProgramChangeBody{reason}`），内部复用既有 `create_new_version` 版本链逻辑，新增 `reason` 可选参数：提供时校验≥5字并记专属审计事件 `CHANGE_NEW_VERSION`；未提供（既有「方案版本」入口）行为与原契约完全一致。前端新增 `planChange` Tab | `existing_code`（版本链机制）+ `ai_proposal`（变更原因留痕的具体实现方式） |

## 三、代码改动清单

- `backend/app/modules/academic_affairs/services/academic_affairs_program_service.py`：`create_new_version` 新增可选 `reason` 参数（向后兼容，原调用方式行为不变）。
- `backend/app/modules/academic_affairs/routers/academic_affairs.py`：新增 `ProgramChangeBody` + `POST /programs/{programId}/change` 路由，权限复用 `academicAffairs.program.manage`（`_PROG_MANAGE`，已被 `ACADEMIC_ADMIN`/`ACADEMIC_TEACHER`/`COLLEGE_ADMIN` 的 `academicAffairs.*` 通配覆盖，无需新增角色映射）。
- `frontend/src/modules/academicAffairs/views/AaProgramConsoleView.vue`：新增 `practicePlan`（实践教学计划）、`planChange`（计划变更）两个 Tab，含列表、筛选、新增/编辑/删除课程行、发起变更弹窗（`AppConfirmDialog` 强制变更原因≥5字）。
- `frontend/src/modules/academicAffairs/constants/course-program.js`：新增 `isPracticeModule(module)` 关键词筛选辅助函数。
- `frontend/src/modules/academicAffairs/api/academic-affairs.api.js`：新增 `changeProgram(programId, reason)`。
- `frontend/src/config/navPlan.js`：`aa-teaching-plan` 节点剩余 5 个 `P()` 叶子全部改为 `I()` 真实入口。
- `backend/tests/test_aa_teaching_plan_r3.py`（新增）：TP1 计划变更正常流程+版本链可查；TP2 非法状态 409/原因不足 5 字 400/学生越权 403；TP3 学分要求含"实践环节"模块保存回显+模块名重复 400；TP4 计划审核/计划发布工作台查询口径（`statusIn=` 与前端 tab 完全同口径）。

## 四、验证

- **前端 `vite build`**：通过（`AaProgramConsoleView` 产物正常生成，无编译错误；`dist/` 未提交，已被 `.gitignore` 排除）。
- **前端 `eslint`**：对本次改动的 6 个文件运行，0 error，1 条与本次改动无关的既有警告（`canPublishBind` 未使用，改动前已存在）。
- **后端路由注册核验**：以 `DB_ENABLED=false` 方式导入 `app.main`，经 `app.openapi()` 确认 `POST /programs/{programId}/change` 与其依赖的 `new-version`/`review`/`credit-requirements` 等既有端点均正确注册，Pydantic 模型与依赖注入无导入期错误。
- **后端 `py_compile` 语法检查**：通过。
- **后端真实 MySQL pytest**：本机 `TEST_DATABASE_URL` 指向与多个并行子智能体共用的 `student_lifecycle_test` 库；`backend/tests/conftest.py::db_mode` 夹具在**每个测试**执行 `metadata.drop_all + create_all` 重建全库表结构。核验期间 MySQL processlist 与本机并发 python 进程数显示同一时刻有多个其他并行子智能体在同一测试库上执行相同的 drop/create 循环，导致本次新增的 `test_aa_teaching_plan_r3.py` 以及回归用的 `test_aa_program.py`/`test_aa_teaching_task.py` 反复遇到
  `(1684, "Table '...' was skipped since its definition is being modified by concurrent DDL statement")`，**非本次代码逻辑错误**（错误发生在夹具的 `DESCRIBE`/建表阶段，早于任何业务断言）。已用 20 次退避重试仍未捕获到空闲窗口，**未能在本次任务窗口内取得真实 MySQL 绿灯证据**，据实登记为未验证项，不写"测试通过"。
- **回归风险评估（静态）**：`create_new_version` 签名新增的 `reason=None` 为纯附加参数，唯一既有调用点 `program_new_version` 路由不变；未修改任何既有函数的既有行为分支；新端点、新前端 Tab 均为纯增量代码，不改动任何既有路由/组件的已有分支。

## 五、待确认/待复核

- **`isPracticeModule` 关键词筛选口径**（`ai_proposal`）：当前用 `/实践|实训|实习/` 正则匹配 `AaProgramCourse.module` 自由文本。因该字段编制期为教师手填文本，若学校实际编制习惯不含这些关键词（如只填"集中实践"外的其他措辞），会导致「实践教学计划」页筛不出数据，界面会显示"暂无数据"而非报错——不是假成功，但可能存在筛选覆盖不全，建议真机验收时用实际培养方案数据核对一次。
- **`POST /programs/{id}/change` 尚未经真实 MySQL 用例验证**（见上节），需在测试库窗口空闲时补跑 `backend/tests/test_aa_teaching_plan_r3.py`，或总控在整合阶段统一跑一次全量回归。
- 培养方案（`aa-training`）自身 navPlan 节点仍有独立未接线叶子「实践环节」「方案变更」「方案归档」（`frontend/src/config/navPlan.js` 第 312、316 行），本轮**未触碰**，不在本任务范围。若后续子智能体处理这些叶子，可直接复用本轮新增的 `planChange`/`practicePlan` Tab 与 `POST /programs/{id}/change` 端点，避免重复造轮子。

## 六、结论

`aa-teaching-plan`（教学计划）二级模块 navPlan 10 个三级叶子已全部转为 `I()` 真实入口，无遗留 `P()` 占位。
代码静态验证（构建/lint/路由注册/语法）通过；真实 MySQL 业务用例因测试库并行争用未能在本次任务窗口内取得
绿灯证据，已如实登记为未验证项，非阻断（不影响真实使用，只是自动化证据缺口），移交总控在低并发窗口补验。
