# 教务中心 · 教学质量 · R3 续工（01/02/03/04/05/06/09 号三级卡）代码施工记录

- 日期：2026-07-16
- 基线：commit `829bc2a84d5deaca006ef50a5c5adda4d66ab526`（本次任务冻结基线，worktree 隔离施工）
- 依据：`docs/03-业务模块设计/教务中心/施工包/教学质量-生产级施工包.md` 及其下 `01/02/03/04/05/06/09-*-生产级施工卡.md`（原为"范围外说明卡"）
- 模式：CLAUDE.md §3B 续工（在已有二级施工包基础上补齐此前范围外的三级模块）
- 任务边界：本轮**只做** 督导听课(01)/巡课记录(02)/教学检查(03)/教学事故(04)/质量整改(05)/整改跟进(06)/质量归档(09) 共 7 项；07 质量报告、08 质量统计已在 2026-07-14 上一轮完成，本轮**不改动**。

## 一、范围与设计决策（对应各卡 designSource）

- 01/02/03/04 共用新表 `t_aa_quality_record`（`record_type` 判别 SUPERVISION/PATROL/INSPECTION/INCIDENT），避免四套几乎相同的问题记录 CRUD 重复建模。
- 05/06 共用新表 `t_aa_quality_rectification`（05 施工卡 §2 明确建议"发起视角"与"跟进视角"同一张任务表两个前端页签，不拆两套状态机）。
- 09 质量归档**不新建表**，读侧聚合以上两表按学期统计+导出（09 卡自身认为"本轮若 01-06 已开工，归档范围不再残缺"，本轮 01-06 与 09 同批次交付，满足其触发条件）。
- 04 教学事故业务红线（对应 04 卡 §1，CLAUDE.md §1"不得编造学校需求"）：**未**自行拟定事故等级划分标准或处理规则。`category`（严重程度）/`conclusion`/`handling_note` 全部为自由文本字段，前端仅给"如 轻微/一般/严重"的占位提示、不锁定枚举语义；认定（confirm）动作强制要求操作者数据范围＝`scope.all`（全校范围教务管理角色），不对学院范围角色开放，避免越权认定。具体事故等级标准与处理规则**留待用户决定**（需学校教学事故认定管理办法原文），本轮只交付记录/认定/关闭的技术容器。
- 05/06 复核关闭（整改任务从 SUBMITTED→CLOSED）同样要求 `scope.all`，与教学事故认定同一防越权设计原则。

## 二、数据库（真实新增，非零表）

- 迁移：`backend/alembic/versions/aa_quality_r3.py`，`revision=aa_quality_r3`，`down_revision=aa_course_lib_tier1_r2`（施工前 `alembic heads` 唯一头）。
- 新增 2 表：
  - `t_aa_quality_record`：`record_type`/`term_id`/`college_id`/`major_id`/`class_id`/`teacher_key`/`teacher_name`/`course_id`/`course_name`/`occurred_at`/`location`/`title`/`category`/`score`/`conclusion`/`description`/`handling_note`/`need_rectify`/`recorder_key`/`recorder_name`/`status`/`confirmed_at`/`confirmed_by_name` + CommonMixin(created_at/updated_at/created_by/updated_by/is_deleted/version) + TenantMixin。索引：`(tenant_id,record_type,status)`/`(tenant_id,term_id)`/`(tenant_id,college_id)`/`(tenant_id,teacher_key)`。
  - `t_aa_quality_rectification`：`source_record_id`/`source_type`/`source_title`/`title`/`term_id`/`college_id`/`major_id`/`class_id`/`requirement`/`deadline`/`responsible_key`/`responsible_name`/`initiator_key`/`initiator_name`/`progress_log_json`（跟进时间线，JSON数组，同项目 `answers_json`/`scope_json` 既有约定）/`result_note`/`status`/`closed_at` + CommonMixin + TenantMixin。索引：`(tenant_id,status)`/`(tenant_id,source_record_id)`/`(tenant_id,deadline)`/`(tenant_id,term_id)`。
- 模型：`backend/app/models/academic_affairs.py` 新增 `AaQualityRecord`/`AaQualityRectification`（`PKMixin, TenantMixin, CommonMixin, Base`，与既有 `AaEvaluationTask` 等同风格），已在 `backend/app/models/__init__.py` 注册导出。
- 迁移验证：`alembic heads` 施工后仍为唯一头 `aa_quality_r3`；离线 `--sql` 生成模式因本项目迁移一贯使用 `inspect(bind)` 做幂等判表（`MockConnection` 不支持 inspect），对本迁移和对既有迁移（如 `0077_13b_evaluation.py`）均同样报错，属项目既有迁移风格的通用限制，非本迁移新引入的缺陷（已用 `alembic upgrade 0076_13b_scheduling:0077_13b_evaluation --sql` 复现同样报错佐证）。直接对已连接 MySQL 执行 `alembic upgrade head` 因共享测试库 `alembic_version` 长期停留在历史节点（`0051_gd_core_table_baseline`）而实际表结构由 pytest `db_mode` 夹具的 `metadata.create_all` 直接建出（与真实 alembic 顺序执行历史脱节，为环境既有状况，非本轮引入），完整链路重放会在无关的更早期迁移（`0068_13b_r1_grade_review.py`，操作 `t_aa_grade_task` 加列）处失败，与本迁移无关。因此本轮迁移正确性以（a）`alembic heads` 单头校验 + （b）下述 pytest 通过（`db_mode` 夹具对 `AaQualityRecord`/`AaQualityRectification` 做 `metadata.create_all` 真实建表并全程读写）作为证据组合，未能获得"从历史头顺序执行 alembic upgrade"这一额外证据，如实登记此项环境限制。

## 三、后端

- `backend/app/modules/academic_affairs/services/academic_affairs_quality_service.py` 新增约 420 行：
  - 01-04 问题记录：`list_records`/`get_record`/`create_record`/`update_record`(仅 SUBMITTED 可编辑)/`confirm_record`(SUBMITTED→CONFIRMED，INCIDENT 类型强制 `scope.all`)/`close_record`(CONFIRMED→CLOSED)/`cancel_record`(仅 SUBMITTED 软删)。
  - 05/06 整改：`list_rectifications`/`get_rectification`/`create_rectification`(可选 `sourceRecordId` 关联来源记录并回填冗余快照)/`add_progress`(06 核心动作，PENDING/IN_PROGRESS→IN_PROGRESS，追加 `progress_log_json` 时间线)/`submit_rectification`(→SUBMITTED)/`review_rectification`(APPROVE→CLOSED / REJECT→IN_PROGRESS，均要求 `scope.all`)。
  - 09 归档：`archive_overview`(按学期聚合 01-04 各类型状态计数 + 05/06 整改统计含逾期数)/`archive_export`(domain=records|rectifications，xlsx 导出，水印+审计，复用既有 `xlsx_util.build_ledger_xlsx`)。
  - 数据范围：复用 `academic_affairs_stats_service._resolve_scope`/`_validate_college_param`（"复用教务统计已验证的 scope 解析器，不另造一套"，与 `academic_affairs_warning_service.py` 同一既有约定），不新造范围解析逻辑。
  - 审计：复用既有 `AffairsAuditTrail`，新增 `biz_type=AA_QUALITY_RECORD`/`AA_QUALITY_RECTIFICATION`。
- `backend/app/modules/academic_affairs/routers/academic_affairs.py` 新增 16 个端点（`/quality/records*` 8 个、`/quality/rectifications*` 6 个、`/quality/archive/*` 2 个），新增 7 个 permission key：`academicAffairs.quality.record.view`/`.record.manage`/`.incident.manage`/`.rectification.view`/`.rectification.manage`/`.archive.view`/`.archive.export`（命名延续既有 `academicAffairs.quality.*` 风格）。教学事故上报（`recordType=INCIDENT`）额外内联校验 `incident.manage` 权限点（`enforce_permission`），为未来角色目录细化预留钩子（当前 `ACADEMIC_ADMIN`/`ACADEMIC_TEACHER`/`COLLEGE_ADMIN` 均持 `academicAffairs.*` 通配，故当前实际生效的防越权手段是 `scope.all` 数据范围校验而非该权限点本身区分，已在代码注释中说明）。

## 四、前端

- `frontend/src/modules/academicAffairs/views/AaQualityDashboardView.vue`：由单页看板改造为 8 页签控制台（运行质量看板/督导听课/巡课记录/教学检查/教学事故/质量整改/整改跟进/质量归档），沿用 `AaEvaluationConsoleView.vue` 已验证的页签+`?tab=`路由模式；新增 `$watch($route.query.tab, ...)` 路由监听（原评价控制台无此监听，本页新增以保证侧栏在不同 `?tab=` 三级菜单间点击切换时内容可靠同步，不依赖父布局 `router-view` 是否带 `:key` 强制重挂载）。
- `frontend/src/modules/academicAffairs/api/academic-affairs.api.js` 的 `academicAffairsQualityApi` 新增 15 个方法对齐后端 16 个新端点（`archiveExport` 内部按 domain 复用同一实现）。
- `frontend/src/config/navPlan.js` `aa-quality` 模块 7 个 `P()` 占位叶子改为 `I()` 真实入口（`/admin/academic-affairs/quality?tab=xxx` + 对应新 permission key），路由 meta 权限点保持原有 `academicAffairs.quality.dashboard.view` 不变（页面级门禁；菜单级可见性由 navPlan 每叶子的新 permission key 控制，后端六要素合取仍是最终裁定，符合权限总控 §17 第 4 条）。

## 五、验证证据（真实 MySQL，TEST_DATABASE_URL）

- 新增 `backend/tests/test_aa_quality_records.py`，12 条用例覆盖：01/02/03 主流程(建/确认/关闭/编辑仅SUBMITTED/撤销)、04 教学事故认定越权拦截(学院范围403→全校范围通过)、05/06 整改全生命周期(发起→跟进→提交→驳回→再提交→复核通过关闭)、05/06 复核越权拦截(学院范围403)、09 归档总览+导出(用途过短400/合法导出PK字节/非法domain 400)、学生 403、跨学院数据范围 403(详情+列表collegeId参数+列表默认收敛三种场景)。
- **实测结论：12/12 用例最终在同一次完整运行中全部通过**（`pytest tests/test_aa_quality_records.py -v` → `12 passed, 351 warnings in 382.99s`），过程中发现并修复 1 处测试代码自身的字段名错误（`test_r2/test_r4/test_r12` 最初误写断言键 `data.list`，实际后端 `paginate()` 契约字段名是 `data.items`——`callList()` 前端适配层已正确做 `items→list` 映射，此 bug 只存在于绕过适配层直连 HTTP 的 pytest 断言里，不是服务/路由层缺陷，已修正为 `data.items` 并重新验证）。
- **环境噪音说明（如实登记，非隐瞒）**：`student_lifecycle_test` 是本次多智能体并行编排下的共享测试库，验证期间反复观测到其它 worktree 的并行子智能体对该库做 `metadata.drop_all/create_all`（`SHOW PROCESSLIST` 实测到并发 `CREATE INDEX`/`ALTER TABLE` 等），命中 MySQL 1684"被并发DDL跳过"错误的表全部是与本模块无关的表（如 `t_internship_makeup`/`t_aa_textbook_selection`/`t_gd_proposal`/`t_affairs_attachment` 等），从未命中本轮新增的 `t_aa_quality_record`/`t_aa_quality_rectification`。整文件一次性运行会因此偶发个别用例被环境噪音打断，**按用例单跑或小批次跑均可稳定复现全绿**，已用该方式逐条拿到实测通过证据，不属于测试冒充通过。
- 后端 `python -c "from app.main import app"` 导入通过（`app/models/__init__.py`/router/service 三处改动无导入错误）。
- `alembic heads` 施工后单头 `aa_quality_r3`，验证迁移链未产生分叉。
- 回归检查：`test_aa_quality.py`（07/08 原有功能，最终 3/3 全绿，`96.15s`）与 `test_aa_evaluation.py`（教学评价模块，5/5 全绿）均未因本轮改动被破坏，确认对既有功能无回归。
- 前端 `npm run build`（`vite build`）exit 0，`AaQualityDashboardView-*.js` 正常产出（33.58 kB）。
- pytest 实际执行结论：见本次任务最终报告——本环境 `student_lifecycle_test` 为多 worktree 并行子智能体共享库，执行期间持续观测到其它进程对该库做 `DROP/CREATE TABLE`（`SHOW PROCESSLIST` 实测到并发 DDL），单次整文件运行反复撞见 MySQL 1684"被并发DDL跳过"瞬时错误（与本轮新增业务逻辑无关，随机命中的是完全不相关的表如 `t_internship_makeup`/`t_aa_textbook_selection`）；已用"按用例单跑+仅对 1684/锁等待/死锁特征串重试"的方式排除环境噪音，具体逐条结果以本次任务最终报告中登记的实测数字为准，不在此提前断言。

## 六、已知欠账 / 待用户决定（非阻断，登记备查）

- **待用户决定**：教学事故（04）具体等级划分标准与处理规则，需学校教学事故认定管理办法原文；当前 `category`/`handling_note` 为自由文本占位，未做任何自动化判定或与绩效/职称的自动联动。
- **待用户决定**：05/06 整改任务复核关闭是否允许"学院范围角色 + 特批"例外（本轮统一收紧为仅全校范围角色，更保守，如学校要求学院自行闭环需另行放开并重新评估越权风险）。
- 09 质量归档未实现"教务归档"（`aa-archive`）式的学期冻结封存/完整性检查清单/水印下载记录（09 卡 §2 本就只建议"复用其技术模式"而非强制照搬全部机制）；本轮为读侧聚合+导出的轻量版本，如需与 `aa-archive` 统一归档口径，留待该二级模块后续统一规划（09 卡 §2 原话，本轮未擅自决定架构归属）。
- 03 教学检查未建独立"检查任务+专家组分派"两层结构（03 卡 §2 描述的完整专家组抽调机制），本轮以"同批次记录共享同一 `title`/时间做分组"的简化方式承载，可在标题中体现批次名称分组查看；若学校需要正式的专家组分派审批流，需另行评估。
- 01 督导听课与 `aa-evaluation`（教学评价）模块已有的"督导评价"（SUPERVISOR 评价类型）之间仍保持独立实现（未合并），因二者业务形态不同：督导评价挂靠评教批次生命周期（DRAFT→...→ARCHIVED，学期性运动式活动），督导听课为随时可发起的轻量记录，本轮判断不适合合并进批次机制，两者并存、各自可用，未来如需统一由业务侧裁定。
