# 手机号登录专项本地施工记录

## 当前边界

- 用户授权在本地 main 串行施工、显式按文件提交；仅本地代码、Git、Docker 和真实测试为依据。
- 初始 HEAD：9b2a2527b3c011c7f6445ca1adde6dca3ef1fcf3。
- 原有未提交内容：CLAUDE.md、README.md、AGENTS.md、使用说明-先看这个.md、.codex-remote-attachments/。保持原样，不纳入专项提交。
- 施工包只用于目标、安全边界与验收；F01 保留，F14 不施工。原 User、StudentAccountLink、账号密码、认证签发与身份导入体系保留。
- 上轮进度中的“已完成”不能作为验收。PH01 仍缺双连接及历史升级专项测试；PH02—PH08 为部分实现；PH09 尚未完整执行。

## C1 数据模型里程碑（提交前工作树）

实际改动：独立当前绑定/候选模型、User.credential_version、单一扩展迁移；同租户用户唯一、VERIFIED 号码唯一、非验证状态强制清空号码、BIGINT 非负版本；有凭据历史时拒绝破坏性降级。

直接启动阻断修复：现有 20260907_aa_classroom_buildings 迁移及正式服务已引用 AaTeachingBuilding，但 ORM 缺失；按既有迁移补齐模型注册与教室 building_id/floor_no，不更改历史迁移、不重写业务。

本地环境：Docker student-lifecycle-v8-mysql，MySQL 8.0.46，127.0.0.1:3307；全新空测试库 codex_phone_test_20260910_v1。专项 Redis：codex-phone-local-redis-1，127.0.0.1:6381。其它项目容器未改动。仓库旧 local compose 为 PostgreSQL，mysql compose 缺必需配置，均未启动其生产配置。

命令与证据（2026-09-10）：

| 命令 | 退出码 | 真实结果 |
| --- | --- | --- |
| backend/.venv/Scripts/python.exe .codex-temp/phone_local.py --init | 0 | 仅创建新的隔离空库，无删除/reset |
| backend/.venv/Scripts/python.exe .codex-temp/phone_local.py -m alembic upgrade head | 0 | 从 0001 到 20260910_phone_login_foundation 的完整 fresh upgrade |
| backend/.venv/Scripts/python.exe .codex-temp/phone_local.py -m pytest tests/test_phone_login_service.py tests/test_phone_login_mysql.py -q --tb=short | 0 | 23 passed；包含未提交的认证后续改动测试，不代表 C1 单独覆盖完整登录 |
| docker compose -f .codex-temp/phone-compose.yml exec -T redis redis-cli ping | 0 | PONG |
| git diff --check | 0 | 无 whitespace 错误 |

负向先行记录：启动 ImportError 已复现；号码数字类型/全角数字测试 2 failed 后修复；混合/空值/伪造字段 DTO 测试 6 failed 后修复；凭据版本转发测试 1 failed 后修复。MySQL CHECK 返回驱动 OperationalError 3819，测试已按真实错误码验证（不是降低数据库约束）。

尚未验证：历史库单步升级、双连接竞态、所有安全 writer、多进程撤销、完整认证/浏览器/导入回归、四端浏览器、真实短信、微信宿主。均不得引用构建结果代替业务通过。

## 待续工

PHONE 与 ACCOUNT 统一风控和验证码绑定的改动尚未形成稳定里程碑；本人验证/换号/找回与持久回执、短信 purpose 隔离、管理权限及 UI、导入加密 staging/预检/结果、四端安全办理落点继续完成。最终备份 NOT_RUN；当前不是生产候选。

## C2 认证核心增量（C1 = 4e7656813）

已修复：严格互斥 PHONE/ACCOUNT DTO；ASCII 号码与独立检索键准备度；按原 SysConfig 读取学校 PHONE 开关，缺省关闭；原 User 解析与原统一签发；User→绑定加锁复核；别名共享风险计数与图形挑战按类型/规范号码/学校/nonce/client 隔离；登录审计不写完整手机号；凭据版本在缓存前复核并传入请求上下文。

改密安全审计移至提交事务；缓存/refresh 清理失败不再误报已提交密码变更为失败。已补本人改密、学校单个/批量状态与管理员重置、平台原服务、迎新激活、风险账号处置的 epoch 更新（其它 writer/正式覆盖入口仍需继续穷尽复核）。候选 writer 复用用户锁和同事务审计，不覆盖既有 VERIFIED、不接受陈旧版本。

真实回归（本提交前增量工作树）：

- `backend/.venv/Scripts/python.exe .codex-temp/phone_local.py -m pytest tests/test_auth_challenge_service.py tests/test_auth_service_db_unit.py tests/test_browser_auth_session_scope.py tests/test_auth_captcha_redis_fail_closed.py tests/test_phone_login_service.py tests/test_phone_login_mysql.py -q --tb=short`：退出码 0，80 passed。真实 MySQL 测试包括 PHONE/ACCOUNT 同 User、原账号保持、旧 access/refresh 拒绝、真实改密后新 epoch 生效、关闭 PHONE 后 ACCOUNT 可用、两个独立连接抢同号、联系方式/候选不能认证、候选版本及导入覆盖拒绝。未用清库夹具。
- `npm --prefix student-portal test`：退出码 0，373 passed。
- `npm --prefix miniapp test`：退出码 0，576 passed。
- 学校 PC 全量 Node 回归正在执行，已暴露教务日历复制/工作台路由相关失败，尚未收口，不能记全绿。

以上不是完整 PH02/PH03 或四端验收：持久敏感操作回执、三进程故障/角色/范围全回归、HTTP/browser 黄金链及新客户端生命周期负向测试仍待完成。无短信投递、无真实数据库业务修改。

## C3 本人号码事务及恢复证明增量（父提交 c8b02c5c6）

实际代码：原短信队列增加 purpose/challenge_ref；旧 worker 的领取和过期清理都限定 RESET_PASSWORD，新调度分支仅处理 BIND_PHONE/CHANGE_PHONE。本人近期密码验证→冻结目标/会话/nonce/epoch/绑定版本→原 Redis OTP→MySQL 唯一证明及办理回执→绑定、epoch、审计和消息 outbox 同事务；解绑保留原行、原账号及递增版本；注销后的随机受限回执只可查询结果。新增正式本人办理 HTTP 接口，尚未完成前端接线。

找回复用原 request/verify/confirm：只读取 VERIFIED；学校策略缺省关闭；全部有效角色及敏感权限分类；独立 recovery_frozen 风险状态不改变原账号；proof 绝对期限不续命，版本改变拒绝，唯一持久回执防重复设密；响应不返回测试验证码。恢复权限读取失败必须拒绝，已有毕业模块权限包装仅补转发 strict 参数，不改变其权限语义。

本地迁移 head：20260910_phone_recovery_freeze；foundation→SMS purpose→recovery freeze 均实际 upgrade，退出码 0，未改已执行迁移。仍仅改隔离测试库结构。

负向先行：用途隔离 2 failed→3 passed；确认 DTO 缺失 1 failed→4 passed；找回旧证明/期限/回执/高权限 4 failed→修复；复跑发现既有毕业权限包装不转发新参数，已修；固定测试号码触发真实小时上限，改用各测试独立虚构号码，未清空生产风控或放宽限额。

真实联合回归（C3 提交前工作树）：

`backend/.venv/Scripts/python.exe .codex-temp/phone_local.py -m pytest tests/test_auth_challenge_service.py tests/test_auth_service_db_unit.py tests/test_browser_auth_session_scope.py tests/test_auth_captcha_redis_fail_closed.py tests/test_phone_login_service.py tests/test_phone_login_mysql.py tests/test_phone_binding_proofs.py tests/test_phone_binding_mysql.py tests/test_phone_reset_mysql.py tests/test_permissions_deny_unit.py -q --tb=short`

退出码 0，100 passed。包含真实 MySQL/Redis：换号、旧 epoch 拒绝、审计失败整事务回滚后原 proof 可重试、跨会话拒绝、解绑再绑保留行和版本、只经原 mock 短信适配器投递且队列清除密文、旧 reset worker 不领取新用途、恢复期限/版本/重放/高权限拒绝。

边界：真实腾讯云短信和微信宿主 BLOCKED_EXTERNAL；HTTP/browser 全故事、双用户同号最终提交竞争、严格权限故障、恢复申请治理及四端办理仍需继续。以上为安全后端增量，不是 PH04 全量通过。学校 PC 全量 Node 已真实退出 1，含教务/导航/材料流程等现存失败；未擅自扩成全仓修复。源码备份 NOT_RUN，未达到生产候选。

## C4 身份导入增量（父提交 256ffa17e，提交前工作树）

学生/教师原 xlsx 模板、bytes/path parser 均增加本人手机号/归属/联系方式；数字单元格警告，公式/科学计数文本/掩码/布尔/小数拒绝。原 staging 与批次 payload 复用 field_crypto 加密，20,000 行经 keyset 分页回读；原 User/StudentAccountLink writer 只创建 PENDING，重复导入不重置密码、不合并学生，已有 VERIFIED 不覆盖。预检在原身份校验外批量核对号码占用/候选冲突，页面增加服务端分类统计与警告，错误 xlsx 脱敏并防公式执行。

复现并修复两个事务缺口：导入审计失败后账号原已提交；初始回执生成失败后账号原已提交。现复用原 writer 的提交前内部回调，将账号/候选/审计、IdentityImportBatch、ImportJob、原 FileObject/ExportJob 受控回执引用同事务提交。未创建第二导入体系，未增加明文凭据 JSON。物理存储与 MySQL 不共享分布式事务，进程在物理落盘后崩溃的未引用文件回收仍需专项验证，不能冒称全故障闭环。

同时补齐 PHONE 找回 DTO/图形挑战类型，找回密码长度读取原学校有效策略；本人 HTTP 的匿名拒绝/掩码/伪造字段/未知回执负向通过。

实际命令/退出码：

- C3 联合命令增加 `tests/test_phone_import_xlsx.py`：0，117 passed（增加回执事务修复前的工作树）。
- `backend/.venv/Scripts/python.exe .codex-temp/phone_local.py -m pytest tests/test_phone_import_xlsx.py -q --tb=short`：0，17 passed，61.00 秒（含两事务故障、原师生 writer、20,000 行、受控回执保持）。回执单测仅隔离扫描 readiness，真实扫描器验收仍 NOT_RUN。
- `node --test frontend/tests/system-ui-identity-import.test.mjs`：先 1 failed/29 passed，修复后退出 0，30 passed（含 SFC 编译与生命周期行为；不是浏览器全故事）。
- `git diff --check`：0。

待续：真实文件上传→扫描→worker→浏览器确认/下载；MySQL 事务未知结果及物理孤儿回收；后台六处号码治理、四端本人流程、人工恢复审批与最终完整回归/备份。当前仍非生产候选。

## C5a 原学校后台号码治理（父提交 7c1c2be75，提交前工作树）

已落地原账号列表/详情、账号异常、登录政策与审计展示；不新增账号中心。手机号查看、完整号精确核对、候选登记/清除、撤销、提醒、导出与政策分别走 canonical 细权限；沿原 User/StudentAccountLink 数据范围，原学校模板发布 writer 仅在隔离库测试交付。完整号查询需用途并审计，响应始终掩码。候选占用/重复通过数据库关联动态识别冲突，不按号码合并主体。

学校 PHONE/恢复政策复用 ConfigDefinition/ConfigOverride/ConfigActivation，缺省关闭；学校父行锁及激活流水版本阻止首次创建/陈旧页面覆盖；关键审计与政策同事务。实际最终 route 位于 system_p1_closure，已补通用 config 写入口同等手机号细权与严格 DTO；禁止恢复继承旁路。此前只改 system_router 的非最终 wrapper 已移除。

站内提醒与脱敏导出走 5 分钟会话绑定预览→重新核对权限/范围/版本→同事务持久回执。提醒每次最多 200 名有效待验证本人，复用原消息 outbox 实际生成 IN_APP 个人消息，既不发 OTP 也不群发短信；台账上限 20,000 行，复用原私有 FileObject/ExportJob 和一次性下载票据，原账号文本防公式执行。导出附当前会话/原岗位范围指纹，换会话或范围变化拒绝；手机号下载细权不能下载其他业务导出。

管理员撤销要求经办人当前密码、明确原因、目标版本、范围及独立细权，禁止对自己操作；复用本人解绑 mutation，保留原绑定行/原账号/密码，epoch 增长、审计、通知、幂等回执同事务，提交后缓存清理失败不重做撤销。UI 冲突保留输入但禁重复发送，密码提交后清除；审计原展示器增加明确手机号动作中文名（不改变其他消费者语义）。

负向先行及真实证据：

- 最终通用政策旁路测试先失败；修复后 HTTP 匿名 401、正式账号 200、陈旧政策 409 与细权拒绝通过。早期 503 是匿名测试未指定隔离学校，改用已有 X-Tenant 请求合同，不放宽后端守卫。
- 预览跨会话/版本变化、通知幂等、管理员自操作/错密码/陈旧版本、导出跨会话及一次性票据，均先补测试再实现；导出跨会话负向真实复现 DID NOT RAISE 后修复。
- `backend/.venv/Scripts/python.exe .codex-temp/phone_local.py -m pytest tests/test_auth_challenge_service.py tests/test_auth_service_db_unit.py tests/test_browser_auth_session_scope.py tests/test_auth_captcha_redis_fail_closed.py tests/test_phone_login_service.py tests/test_phone_login_mysql.py tests/test_phone_binding_proofs.py tests/test_phone_binding_mysql.py tests/test_phone_reset_mysql.py tests/test_phone_import_xlsx.py tests/test_phone_governance.py tests/test_permissions_deny_unit.py -q --tb=short`：退出 0，131 passed，124.11 秒（包含真实 MySQL/Redis、20,000 行 staging、原消息消费、私有 xlsx 与票据）。随后撤销缓存状态回执增量定向同测试文件的 `test_admin_revoke_rejects_self_wrong_password_stale_and_revokes_epoch`：退出 0，1 passed。
- `node --test frontend/tests/phone-governance.test.mjs frontend/tests/system-ui-identity-import.test.mjs frontend/tests/system-ui-data-exchange-workspace.test.mjs`：退出 0，68 passed。
- frontend 目录 `./node_modules/.bin/eslint.cmd src/modules/system/components/PhoneGovernancePanel.vue src/modules/system/utils/phoneGovernance.js src/modules/system/api/phoneGovernance.api.js src/modules/system/views/SystemLoginPolicyView.vue src/modules/system/views/SystemAccountExceptionView.vue src/modules/system/views/SystemUserListView.vue`：退出 0。
- `npm --prefix frontend run build`：退出 0，50.85 秒；已有 CSS import 顺序/大块体积警告保留。此构建在最后审计动作纯标签增量前，标签由上方 Node 回归验证。
- `git diff --check`：退出 0。Docker MySQL/Redis 继续运行，未触碰其它 Docker 项目；未访问远端。

仍未完成：真实浏览器全链、上传扫描 worker 闭环、人工恢复及高风险换号独立核验、四端本人办理、完整 PH09 和最终源码备份。当前 PH06 为增量实现，不能标整个专项通过；真实短信/微信宿主仍为 BLOCKED_EXTERNAL。
