# 教务中心 R1 成绩审核发布更正 · 代码施工记录

- 日期：2026-07-14
- 分支：`feat/13b-r1-grade-review`（基于 `feat/13a-night2`）
- 依据：`docs/03-业务模块设计/教务中心/施工包/成绩审核发布更正-生产级施工包.md`（design_ready）+ 6 张三级施工卡
- 模式：CLAUDE.md A 模式，施工卡门禁通过后同任务连续施工（数据库→后端→前端→测试）

## 一、用户拍板/我方技术决定（区分记录）

| 项 | 决定 | 决定方 |
|---|---|---|
| 代码施工节奏 | 按优先级一个个模块顺序来，不并行授权 | 用户拍板 |
| 成绩构成默认比例 | 保持代码现有 30/70，不改 40/60 | 用户拍板 |
| 教学班名单快照表 | 不新建 `t_aa_teaching_class_roster`，改运行时按 `class_id` 联表推导 | 技术决定（避免绕开锁名规则新建表） |
| 成绩更正发起人 | 仅教师发起，不做学生小程序自主发起 | 技术决定（按任务原文口径，backlog） |
| 新增 `ACADEMIC_ADMIN` 角色码 | 同意新建，教务处终审专属角色，默认无人持有 | **用户拍板**（触发安全分类器二次确认） |

## 二、数据库

新增迁移 `0068_13b_r1_grade_review`（加列，幂等，不新建平行表）：
- `t_aa_grade_task` 加 `submitted_at/college_reviewed_at/college_reviewer_id/academic_reviewed_at/academic_reviewer_id/return_reason/workflow_instance_id`
- `t_aa_grade_record` 加 `source/prev_usual_score/prev_final_score/prev_total_score/change_reason/change_by/change_at/version_no/exception_flag`
- `t_acad_grade` 加 `source`，历史行回填 `LEGACY`

已在真实 MySQL（`student_lifecycle_test`）验证 `alembic upgrade head` 成功，当前 head=`0068_13b_r1_grade_review`。

## 三、后端

- `backend/app/core/permissions.py`：新增 `ACADEMIC_ADMIN` 角色（`academicAffairs.*` + `audit.view`）。
- `backend/app/modules/academic_affairs/services/academic_affairs_grade_service.py`：全量重写，新增 9 态状态机（NOT_STARTED/INPUTTING/SUBMITTED/COLLEGE_REVIEW/ACADEMIC_REVIEW/PUBLISHED/RETURNED/CHANGE_REVIEW(record级)/ARCHIVED）；`submit_task`/`college_review`/`return_task`/`archive_task`/`list_tasks`/`roster`；更正两级审 `change_request`/`change_college_review`/`change_academic_review`；`publish_grades` 升级为原子四件事（回写+台账刷新+预警扫描+`AffairsRiskRecord` 风险记录，唯一约束按 record.id 去重）。
- 数据范围：新增 `_check_course_scope`（COURSE，按 `AaGradeTask.teacher_key` 归属比对）、`_check_college_scope`（COLLEGE，**复用**学工中心已验证的 `affairs_security.build_affairs_context`，未重复造轮子）、`_require_review_role`（TENANT_ALL 超高危动作角色白名单，即使角色拥有 `academicAffairs.*` 权限点通配也需额外过角色门）。
- `backend/app/modules/academic_affairs/routers/academic_affairs.py`：全部成绩端点从 `Depends(require_staff)` 升级为 `Depends(require_permission("academicAffairs.grade.*"))`，新增 8 个端点（submit/college-review/return/archive/change-request/change-college-review/change-academic-review/GET grade-tasks 列表）。
- 修复真实数据缺口（施工包 §1 发现）：新写路径 `pass_status` 由 `FAIL` 简写统一为 `FAILED`（对齐旧路径值域），读侧 `IN`/`in_` 查询同时兼容 `FAIL`/`FAILED` 过渡期数据。

## 四、前端

- `AaGradeEntryView.vue`：改造为真实 9 态录入页——"发布"按钮替换为"提交进入学院审核"；新增异常标记（缺考/缓考/免修）选择、按班级自动圈定名单（`GET /grade-tasks/{id}/roster`）、"我的录入任务"队列。
- 新增 `AaGradeCollegeReviewView.vue`（学院审核工作台）、`AaGradePublishView.vue`（教务发布：终审/退回/归档两个标签页）、`AaGradeChangeView.vue`（更正申请+两级审核）、`AaTranscriptPrintView.vue`（成绩单打印，D7 独立路由）。
- 补充 `GET /grade-tasks?status=` 列表端点（施工包未预先设计，审核/发布工作台的"待处理队列"缺它无法可用，属必要补齐非扩大范围）。
- `navPlan.js`：`aa-grades`/`aa-grade-review` 对应叶子从 `planned` 翻 `implemented`。
- `academic-affairs.routes.js`：新增 4 条页面路由 + 1 条打印路由。

## 五、验证证据

- **前端 build**：`vite build` ✓ 16.46s，5 个新/改页面确认进产物。
- **后端 pytest（真实 MySQL）**：
  - `test_aa_grade.py`（7例，含新增 G7 未审核直接发布 409 回归红线）+ `test_aa_grade_review_flow.py`（8例新写：退回重提/退回原因过短422/教师跨范围403/角色墙403/归档后更正409/更正驳回保原值/更正两级通过新值生效+source=CHANGE/学生全端点403）：**15 passed**（382.83s）。
  - 全量 `test_aa_*` + `test_academic.py` + `test_affairs_security.py` + `test_graduation_permission_codes.py` 回归：另跑验证中，结果见下一条施工记录或最终报告。

## 六、已知欠账（非阻断，如实登记）

1. `list_tasks` 列表端点未做数据范围收敛（COLLEGE 角色能看到跨学院任务列表，仅在具体操作时被 `_check_college_scope` 拦截）——设计上明确记录在函数 docstring，非隐藏缺陷。
2. 成绩更正页（`AaGradeChangeView.vue`）审核侧靠手工输入 recordId，无"待我审核的更正申请"列表——后端未提供该维度的 GET 端点，属工作流待办体系（`t_unified_todo`）未接入本模块的欠账，留后续波次。
3. `_check_course_scope`/`create_grade_task` 在教学任务未关联（`teachingTaskId` 为空）时不做归属收敛——已在代码注释标注为已知欠账。

## 七、Git 状态

分支 `feat/13b-r1-grade-review`，改动：6 个后端文件（含1个新迁移+1个新测试）、8 个前端文件（含4个新页面）。未 `git add`/`commit`（用户未明确要求）。
