# 2026-07-14 · 教务中心 / 选课管理 · 施工包与三级施工卡（文档，不含代码）

## 任务边界

用户明确本次任务边界"只到卡，不写代码"（多模块并行写代码需用户对分支策略单独授权）。选课管理（`aa-course-selection`）原属《教务开工手册·下册》R8 触发式包（原规则"选修课占培养方案学分≥10%"才启用），本次由甲方（项目负责人）直接拍板"现在就做"，等同满足触发条件，产出二级施工包+外部对标证据包+12 张三级施工卡（`user_decision` 记录见施工包 §4 D-01）。

## 产出文件

- `docs/03-业务模块设计/教务中心/施工包/外部对标证据/01-选课管理-外部对标证据包.md`（状态 `partial`，实际联网核验强智/正方/青果三家系统，逐条记录 PASS/PARTIAL/FAIL）
- `docs/03-业务模块设计/教务中心/施工包/选课管理-生产级施工包.md`（状态 `design_ready`）
- `docs/03-业务模块设计/教务中心/施工包/选课管理/三级施工卡/01～12-*-生产级施工卡.md`（12 张，全部 `design_ready`：选课批次/可选课程/选课规则/学生选课/退课管理/补选管理/选课名单/人数控制/冲突检测/选课结果/选课统计/选课归档）
- 更新 `docs/03-业务模块设计/教务中心/文档关联索引.md` §1/§3（新增选课管理条目，未改动同一文件中其他并行子智能体已写入的调停课/学院专业班级/教务归档/成绩审核发布更正等条目）

## 关键结论

- 代码基线：`rg`/`Grep`全仓核实教务中心当前无任何 `Selection`/`selection`/`选课` 相关代码——`backend/app/models/academic_affairs.py` 无 `AaSelection*` 类，`backend/app/modules/academic_affairs/` 无 selection service/router，`frontend/src/modules/academicAffairs/` 无相关 view/route，`backend/tests/` 无相关测试文件；navPlan `aa-course-selection` 叶子（`frontend/src/config/navPlan.js:238`）12 项子菜单占位仍保持原状态，**本次未修改 `navPlan.js` 菜单状态字段**（无代码落地，不得翻绿）。
- 外部证据：强智（厦门软件职业技术学院真实高职采用+西南石油大学操作说明）、正方（盐城工学院真实采用+博客园技术解读）、青果（厦门软件职业技术学院曾用青果为旧系统+岭南职院操作指南）三家均已实际联网/浏览器核验，附教育部《关于在职业学校逐步推行学分制的若干意见》（教职成〔2004〕10号）作为国家政策依据，另补北京大学教务部选课/冲突选课页面作真实学校规则参照（非计入三家共同能力）。两条来源因二进制附件（`.doc`/`.rar`）工具无法完整解析，仅 WebSearch 摘要或元数据可核，已如实标注边界，未冒充完整核验，整体证据包判定 `partial` 但核心门禁（三家CAP+ADOPT、制度依据、三级模块映射、不可推断边界）均 PASS。
- 关键技术发现（已自行查明，未推给用户）：①`docs/03-业务模块设计/教务中心/13B-教务中心状态机与权限矩阵.md:52,283-312` SM-09 已锁定 `t_aa_selection_batch`/`t_aa_selection_record` 两表名，但该两表**未出现在** `docs/03-业务模块设计/跨模块融合/13A-13B-数据表与迁移策略草案.md` §4.8 预留表清单中（该清单只列grade_task/record、makeup_batch、retake_apply、schedule_change、exam_batch/deferred_exam、exemption 共8项），两份"当前有效"文档对选课族锁名表存在收录不一致，已在施工包 §1/§16 登记差异，按 SM-09（更贴近本次施工、且被手册R8指令直接引用）采用两表名，需总控复核阶段同步§4.8清单；②新增第三张表 `t_aa_selection_course`（批次-课程供给项，承载容量/限选规则）超出现有任何锁名清单，`designSource=ai_proposal`，已充分论证工程必要性（batch头与record明细之间必须有容量承载层），需总控复核确认表名；③审计发现 `backend/app/services/platform_service.py` 的 `feature_enabled()` 对未注册的点分二级key存在"默认判定为已启用"的实现陷阱，已给出替代方案（改用同模块 `academic_affairs_change_service.py:38` 已示范的 `get_config_json(tenant_id,"ACAD_RULE",key)` 模式），避免选课启用开关被误判为默认开启。
- 设计范围：完整覆盖 SM-09 冻结的批次6态（DRAFT/PUBLISHED/OPEN/CLOSED/LOCKED/ARCHIVED）+ 记录4态（SELECTED/DROPPED/COURSE_CANCELLED/LOCKED）、选课八条校验规则、P4并发拍板（DB行锁SELECT FOR UPDATE+唯一约束防重，不上消息队列）；「学生选课」三级卡已设计并发50请求容量10恰好10人成功的pytest测试点（`test_enroll_concurrent_capacity_10`，需在真实MySQL环境验证，不得SQLite冒充）与locust百人级简单压测报告设计。明确不做：自动递补、跨校区选课、抢课验证码、志愿调剂算法（手册红线）。
- 权限：完整读取 `docs/03-业务模块设计/系统管理中心/00-系统管理中心-权限角色模块授权与权责边界设计.md`（832行全文），实例化到12个三级页面的角色/数据范围/业务关系/敏感字段/审计/拒绝码/正反向测试，重点覆盖"任课教师选课名单只读需来自授课关系而非仅数据范围"（对应权限总控§8A.3第3条）与"SUSPENDED学生选课403"（手册D-07拍板）。新增 `academicAffairs.selection.*` 系列permission key（设计层面，未写代码），复用现有 `ACADEMIC_TEACHER`/`COLLEGE_ADMIN` 角色的 `academicAffairs.*` 通配授权，无需改角色映射。
- 待甲方确认（业务政策类，已收窄为2项）：①学院教务员是否需要参与批次课程范围建议的写权限（默认只读）；②教学班任课教师是否需要导出本人选课名单的权限（默认不需要，导出收敛给管理角色）——均登记于施工包§10末尾，技术问题（表名/接口/字段/状态机是否存在等）均已自查清楚，未推给用户。

## 未做事项（非阻断，登记备查）

- 未执行任何代码/迁移/测试（任务边界不含代码，§18连续施工计划待用户就分支/worktree策略明确授权后执行）。
- 未修改 `navPlan.js` 菜单状态（保持现状，代码施工完成后才允许改绿）。
- `t_aa_selection_course` 表名与 `13A-13B-数据表与迁移策略草案.md` §4.8 清单同步，留待总控在教务中心多个并行施工包（R1成绩审核发布更正、R2调停课、R4排课管理、R5考务管理、R8选课管理）合并阶段统一处理，避免单个子智能体擅自改写共享契约文件。
- 与并行的排课管理、考务管理、成绩审核发布更正三个施工包核实：三者当前均明确写"V1无选课时名单按行政班推导"（如 `考务管理-生产级施工包.md:110`"t_class（行政班学生名单，V1 无选课）"、`成绩审核发布更正-生产级施工包.md:50`"V1 范围内按任务书'V1 无选课时名单按行政班推导'处理"），本包设计的LOCKED选课名单消费切换位（`source:"ENROLLED"`）与三者当前假设一致（均为"未来切换位"而非"已实现对接"），无冲突，但实际切换代码需在四个包全部进入代码阶段后由总控统一调度对齐字段名，本次未擅自改动其余三包文件。
