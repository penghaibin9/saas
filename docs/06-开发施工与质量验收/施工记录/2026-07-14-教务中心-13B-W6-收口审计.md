# 教务中心 13B-W6 收口 · 审计进度

- 日期：2026-07-14
- 范围：上册 W6 收口的可静态核验项（RBAC 越权专项、单一写入口门禁）

## 任务2 · RBAC 越权专项（GREEN）

- `backend/app/modules/academic_affairs/routers/academic_affairs.py`：**64 个端点全部 `Depends(require_staff)`**（64/64），无一使用更弱的 `get_current_user`。学生 userType 一律 403（core/security.py `require_staff`）。
- 数据隔离：各 service 查询均带 `tenant_id == _tid()`，跨租户不可读写。
- 已由 pytest 覆盖：`test_aa_term::test_t6`、`test_aa_status_change::test_sc8`、`test_aa_fe_waves_authz`（3 例）——课程/方案/任务/课表/成绩/预警/毕业 学生令牌全 403，MySQL 全绿。
- 待办（未卡审）：租户内**跨学院数据范围**（A 学院查不到 B 学院）大多数读列表端点仅按 tenant 过滤、未下推 college scope——属下册 R4「COURSE scope 落地」与 W6 深化项，非 W1-W5 前端接入范围，登记欠账。

## 任务3 · 单一写入口门禁（GREEN）

- 全模块 `grep '.student_status ='`：唯一直写点是 `academic_affairs_status_service.py:70  s.student_status = to_status`，**位于 `change_student_status()` 内部**（即单一写入口本身）；其余匹配为读比较（`== status`）。
- 异动（change_service）、注册（service.register_student）、毕业终审（graduation_service.academic_final）均调用 `change_student_status` 生效，无绕过。
- P8 检查单「student_status 单一写入口」不变量在本模块**成立**。CI grep 门禁为全仓级横切项，涉及其它模块，未在本模块任务内加。

## 对抗式复核（多智能体工作流 aa-fe-review）+ 修复

5 个子智能体逐波核对前端调用 vs 后端真实契约，每条发现再派核实智能体对抗确认。候选 4 条、**确认 4 条真实运行时 bug，已全部修复**：

| # | 波 | 文件 | 问题 | 修复 |
|---|---|---|---|---|
| 1 | W3 | AaProgramEditorView.vue | 半学分课程 `credit` 传 float → 后端 `ProgramCourseBody.credit: Optional[int]` 且 `credit_snapshot` 为 Integer 列 → pydantic 422 | 前端 `Math.round(credit)`（方案内学分整数存，与列一致）；半学分精度限制登记欠账 |
| 2 | W4 | schedule_service.import_items | 导入行携带 courseName/teacherName/className 但后端只读 taskId 带出 → 导入项课程/教师名空白 | 后端 import_items 补 `row.get('courseName'/'teacherName'/'className')`，与 add_item 一致；`test_aa_schedule` 8 passed |
| 3 | W5 | AaGraduationResultView.vue | 三名单读大写 key，后端返回小写 `graduated/completed/delayed` → 人数恒 0 | 前端改小写 key |
| 4 | W5 | constants/grade-graduation.js | 预警级别 `MID` vs 后端 `MEDIUM` → 中度筛选恒空、标签显英文 | `MID`→`MEDIUM` + warningColor |

修复后：前端全量 `vite build` ✓；`test_aa_schedule.py` MySQL 8 passed。

## 未做（需运行环境/起服务，另行）

- 任务1 生产迁移演练（干净 MySQL8 全量 alembic upgrade + 备份恢复）——需独立 MySQL 实例。
- 任务4 演示固化（sandbox 种子 + 双主线演练）——需前后端起服务。
- 浏览器整链路走查（新建学期→发布→看板生效、排课冲突、终审写学籍）——需前后端起服务 + 教职工登录。
