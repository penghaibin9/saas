# PR #145 教务 A 线 × PR #133 Control Plane
## 共享 Authority 碰撞处理说明 · 2026-08-17

> 目标分支：`agent/academic-a-semester-core`
> 写入前审计 HEAD：`67d98c87f801589eed32d6e80b85dc3f3e2d4e55`
> Control Plane 基线：`main@4b706f392bd4213edcf4fbf81e4c292f90028c12`
> 用户只负责文档施工，不负责 Alembic、Git、冲突、Gate 或 owner 判断；施工智能体必须自行处理。

## 结论

PR #145 不停线，但立即启用两把共享锁：

```text
LOCK-A = DATA_EXCHANGE_CORE
LOCK-B = ALEMBIC
```

### LOCK-A：Data Exchange Core

PR #145 当前已经修改：

```text
backend/app/services/data_exchange_confirm_service.py
backend/app/services/data_exchange_confirm_legacy.py
```

这两个 dispatcher 在 Academic wiring 完成并 handoff 前由 #145 单一施工。Control Plane P-01 Identity Import 不得修改它们，只做前端 poll/state、学生/教师导入页、ImportDialog、identity worker 生产注册/健康与 targeted tests。

#145 修改双层 dispatcher 时必须保持：双层同步接线；不改变 Identity Import staging/confirm 语义；不把 GET 变成 side-effect；不接管 identity worker。

若 #145 必须触碰 Identity Import 专属语义，立即停止共享写，刷新 main、#145、Control Plane repair branch，做 collision audit；不要让用户决定。

### LOCK-B：Alembic

#145 当前已有：

```text
backend/alembic/versions/20260817_aa_prog_expand.py
```

Control Plane P-05 最终需要 `CustomRoleSource.role_id -> NOT NULL` contract migration。两者不能同时从旧 head 生长。

#145 可以继续完成自己已经开始的 Academic expand migration 和相关 targeted Gate，但禁止：

```text
修改 CustomRoleSource.role_id nullability
新建 Control Plane permission/role governance migration
替 P-05 创建 NOT NULL migration
私自创建 Alembic merge head
```

P-05 Phase D 只有在 #145 migration lineage 已冻结/进入集成真值、目标分支 Alembic heads=1、active role_id NULL=0、N-1 writer=0、reconcile unresolved=0 后，才能从最新唯一 head 创建 contract migration。用户不参与 down_revision 判断。

## #145 禁止反向侵入

禁止顺手修改：

```text
backend/app/modules/platform/**
Platform PAM
School IAM
CustomRoleSource runtime binding
Permission Catalog Authority
identity import UI/worker lifecycle
P-05 contract migration
```

若 Academic 需要全局 permission / route registration / module catalog，先 handoff 给共享 owner，不在 #145 自造第二 Authority。

## 每次共享写前自动检查

施工智能体必须自行：

```text
refresh #145 exact HEAD
refresh latest main
refresh active Control Plane repair branch
compare shared changed paths
check Alembic head lineage
```

重点：

```text
backend/app/services/data_exchange_confirm_service.py
backend/app/services/data_exchange_confirm_legacy.py
backend/alembic/versions/**
```

发现对方新改动时，先 collision audit 再写，禁止先写完再解决 merge conflict。

## Handoff 模板

共享刀封板后记录：

```text
PR145 exact HEAD:
main checked SHA:
Academic migration revision:
parent/down_revision:
Alembic heads observed:
data_exchange_confirm_service.py changed: YES/NO
data_exchange_confirm_legacy.py changed: YES/NO
Identity Import semantics changed: MUST BE NO
Targeted Gate:
MySQL Gate:
unresolved shared collision:
```

## 与 #146/#147/#148 的关系

当前不要求暂停 #146/#147/#148。真正需要协调的是：

```text
#145 ↔ P-01 : Data Exchange Core
#145 ↔ P-05 : Alembic
```

## STOP 条件

出现任一情况停止当前共享写：

```text
A. Control Plane 已修改两个 dispatcher 中任一个；
B. main/integration 出现新 Alembic head；
C. #145 需要修改 CustomRoleSource / Permission Catalog / Platform PAM；
D. Academic migration down_revision 不再是最新唯一 head；
E. 为了 Gate 变绿必须改 Identity Import staging/confirm Authority；
F. 两个分支正在修改同一个 shared writer。
```

STOP 后只输出阻断事实并继续不碰共享 Authority 的 Academic 工作，不把判断题交给用户。

## 给施工智能体的固定指令

> 从 #145 当前 exact-head 继续 Academic 主线；加载 `DATA_EXCHANGE_CORE` 与 `ALEMBIC` 两把共享锁。#145 当前拥有双层 Data Exchange dispatcher 的 Academic wiring 写权，但不得改变 Identity Import 语义；P-01 handoff 前不得修改它们。#145 可完成自己的 Academic expand migration，但不得创建 P-05 `role_id NOT NULL` migration 或 Alembic merge head。每次共享写前自行刷新 main/#145/Control Plane HEAD 与 Alembic heads；发现竞态先审计。用户只负责文档施工，不向用户询问 rebase、down_revision、Gate、owner 或冲突决策。禁止 force、禁止 `git add -A`、禁止先写后解冲突。
