# 后端 P0–P2 接入现状（BACKEND-OVERNIGHT 产出）

## 本轮完成
1. 技术栈统一 FastAPI（Node 残留归档 backend/_legacy_node_express/）。
2. 可启动可验证：/health /docs /openapi.json 均 200；pytest 27 passed。
3. 统一响应双码（code 数字 + bizCode 契约字符串）+ traceId + 全局异常。
4. mock 登录 8 账号 + me/switch-role/logout；租户品牌（产品名=高校学生全生命周期管理平台）；
   RBAC menus/permissions/data-scope/roles/current-context（11 角色 × 12 数据范围，平台/学校边界隔离）。
5. 学生主档 7 接口、审批任务 7 接口、看板/待办/消息、文件上传占位、导入导出占位、审计查询与埋点。
6. SQLAlchemy 2.x 第一批 19 模型（t_ 前缀，按 docs/05-数据接口权限与安全/database/00-数据库设计冻结总册）+ Alembic 0001。

## 接口状态分类
- **纯 mock（无 DB 分支）**：auth/tenant/rbac/dashboard/todos/messages/file/import-export。
- **已具备 DB_ENABLED=true 结构（service 内留 TODO P3 查询骨架）**：students、approvals、audit。
- **尚无真实业务**：流程引擎、Excel、对象存储、消息渠道、字段加密。

## 下一步怎么接（按模块）
- 学生主档：DB_ENABLED=true → alembic upgrade head → student_service 补 SQLAlchemy CRUD
 （tenant_id + is_deleted 过滤已定口径），敏感字段走 *_encrypted+*_hash 写入、响应恒脱敏。
- 审批：t_workflow_def/node 建模（11 中心 DDL）→ approval_service 换表驱动 → SoD 校验。
- RBAC：t_role/t_permission/t_user_role 数据化，current-context 改查 t_user_active_context。
- 文件：MinIO 接入（bucket/objectKey/signedUrl 字段已预留），下载写 t_file_access_log。
- 导入导出：openpyxl 解析/生成 + t_student_import_batch Dry-Run 两步、t_export_task 三档导出。
- 审计：audit_log 内存队列 → t_security_audit_log 落库（append-only）。
