# 05 · 后续开发接力说明

> **已停用，不作当前施工或现状判断依据。** 当前施工顺序以 [`下一步施工总控.md`](./下一步施工总控.md) 为准；五端产品和目录迁移以 [五端产品与 SaaS 总体架构规划](../../02-总体架构与公共底座/00-五端产品与SaaS总体架构规划.md) 为准。下文为历史记录，其中“纯 mock”“后端只有壳”等描述已经过时。

> **【已归档 · 2026-07-09】** 本文件写于 2026-07-04，其中「后端仅壳、主线 UI mock、数据库未建库」等表述**已失效**。
> 当前施工请以：`docs/00-只看这个-项目入口.md`、`docs/project-map/下一步施工总控.md`、`docs/施工记录/历史欠账.md` 为准。
> 下文仅供历史参考，不作为当前施工依据。

---

# 05 · 后续开发接力说明（历史原文）

> 给"下一个接手的人"（或下一轮 AI）。目标：**5 分钟知道现在到哪了、先看什么、接着做什么、什么不能碰**。

---

## 1. 回来先看这两处

1. **`AI执行状态.md`**（项目根）：当前进度、上一轮做了什么、红线。**每次回来先读它。**
2. **`docs/00-只看这个-项目入口.md`**：开发前必读入口（设计稿在哪、规则是什么）。

---

## 2. 现在项目到哪了（截至 2026-07-04）

- **PC 管理端（frontend/）**：正在做 **UI 母版对接**，页面用 mock。→ 这是当前主线。
- **小程序（miniapp/）**：已可运行、可构建（H5 + 微信），纯 mock，可演示。
- **后端（backend/）**：预留，只有壳，无真实接口。
- **数据库**：只有冻结设计文档（`docs/database/`），未建库。
- **文档**：部署/运维/交付/目录地图已补齐（`docs/deploy`、`docs/ops`、`docs/delivery`、`docs/project-map`）；部署示例在 `deploy/`。

---

## 3. 下一步该做什么（建议顺序）

1. **继续 PC UI 母版对接**（主线）：在 `frontend/src/` 按 `docs/ui/fable5-pc-final/` 与 UI 规范对接页面。**这是下一步的重点。**
2. 小程序按需继续完善（更多表单、入口），动 `miniapp/`。
3. （后续阶段）后端接入：按 `docs/api/` + `docs/database/` 开发后端、建库，再把前端从 mock 切真实接口。

---

## 4. 谁动谁：目录职责速记

| 你要做 | 动哪里 | 先读 |
|---|---|---|
| PC UI 对接 | `frontend/src/`（layouts/modules/views/components/styles） | `docs/ui/fable5-pc-final/`、UI 规范 |
| 小程序开发 | `miniapp/src/`（pages/pages.json/components/services/mock） | `docs/ui/fable5-miniapp-final/`、08A/08B |
| 部署/运维/交付 | `docs/deploy` `docs/ops` `docs/delivery`、`deploy/` | 各自 README |
| 后端（后续） | `backend/` | `docs/api/`、`docs/database/` |

---

## 5. 红线（一直遵守）

- ⛔ 不改数据库冻结文档（`docs/database/`）、不重设计数据库、不乱建表。
- ⛔ 不改 API 契约冻结（`docs/api/`）。
- ⛔ 不把 UI 设计稿 HTML 直接复制进 `frontend/src/`。
- ⛔ 不照抄老系统（`99-老…`）。
- ❌ 不手改 `dist/`、`node_modules/`。
- 🔒 改动前先在 `AI执行状态.md` 看红线，动完更新它。

---

## 6. 一句话交接

> 主线是 **PC UI 母版对接**（动 `frontend/src/`）；小程序已可演示（动 `miniapp/`）；后端/数据库是后续阶段（设计已冻结，只引用）；部署运维交付文档已就绪（看 `docs/deploy|ops|delivery|project-map`）。回来先读 `AI执行状态.md`。
