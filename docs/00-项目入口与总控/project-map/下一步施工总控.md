# 下一步施工总控

> 更新：2026-08-31
> 当前唯一目标：形成第一所学校可部署验收的统一 `main` 候选，不新增业务功能。

## 当前顺序

1. 在健康、独占的 MySQL 服务上复跑后端 4 分片全量矩阵；本机门禁容器在 50 分钟持续负载后连接超时，本轮不得放行。
2. 重新检查并只吸收其他会话已经提交且通过定向测试的增量；不读取或搬运未提交文件。
3. 运行第一所学校浏览器主链：登录、角色切换、学生主档、学工、教务、毕业设计、岗位实习、企业门户。
4. 补 Python 漏洞审计、备份恢复和容量 smoke。
5. 核对 GitHub 分支保护 required checks 后，再物理删除已登记替代关系的历史波次工作流。
6. 生成部署包、演示租户、验收清单和回滚说明。

已完成：仓库/文档/CI 清单、完全重复治理、卫生门禁、导航修正、四条功能线集成、空库与历史库迁移、四个前端工程测试/lint/build、四个前端生产依赖审计。

## 冻结项

- 不重组 `apps/services/packages`，不大规模移动源码目录。
- 不改现有业务 API 路径和数据库业务模型。
- 不从其他活跃 worktree 搬运未提交文件，不清理正在被其他会话使用的 worktree。
- 不推送、不打标签、不删除远程分支。
- 门禁未全部通过前，不继续按旧 `planned` 统计重复开发已有学工模块。

## 放行规则

只有 `docs/00-项目入口与总控/project-status.json` 中全部 `releaseGates` 为 `passed` 且 `deliverable=true`，才能标记“可交付”。任何失败必须保留失败命令、commit 和复现方式。
