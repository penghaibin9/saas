# 模块状态总表

> 验证日期：2026-08-31
> 精确数量以 `docs/00-项目入口与总控/project-status.json` 为准；本表只解释当前集成候选的模块级结论。

| 范围 | 代码状态 | 已验证事实 | 交付结论 |
|---|---|---|---|
| 控制面 IAM | integrated | 纯契约测试通过；代表性 MySQL 权限模板读取用例通过 | 候选，完整矩阵待统一门禁 |
| 教务主链数据闭环 | integrated | 调停课残差、多范围事实和沙箱关系闭环定向测试通过；空库与历史库迁移均到唯一 head | 候选，后端全量回归待健康 MySQL 门禁复跑 |
| 教务 V8.1 体验层 | integrated | 后端契约、PC 页面契约、小程序/学生端精确落点测试通过；四个前端工程测试与 build 通过 | 候选，浏览器主链待统一门禁 |
| 学工体验线 | integrated | 学工后端流转、PC 合同测试和小程序阶段测试通过 | 候选，真库与浏览器主链待统一门禁 |
| 毕业设计 | preserved | 已有 V8 终局裁决 JSON 被保留到 `artifacts/release-seals/` | 需要在统一 commit 上复验 |
| 岗位实习 | preserved | 已有 V8 小型裁决 JSON 被保留到 `artifacts/release-seals/` | 需要在统一 commit 上复验 |
| 导航 | corrected | 无首页但有真实子页的二级模块改为 `CONTAINER`；状态分类测试通过 | 不再把学工容器误报为 planned |
| 仓库治理 | governed | 临时目录与大体积运行证据退出主仓；未豁免完全重复文件、禁止文件和超限文件均为 0 | CI 物理删减待核对 GitHub required checks |

`implemented` 和“已通过交付门禁”是两个字段。当前统一基线仍是 `integration_candidate`，不得对外标记为可交付。

历史整理裁决见 `artifacts/release-seals/repo-consolidation-20260831.json`；它不代表当前 `main` 已通过上线门禁。当前候选裁决统一写入 `artifacts/release-seals/main-candidate.json`，并由 `project-status.json` 汇总。未在当前候选 commit 上重跑的门禁必须保持 `pending` 或明确的阻断状态。
