# PR #270 第二轮 CI 修复证据

失败基准：`72e3492e2167c7284612ac8dd3b979f8477568d2`。本轮未手动重跑整套 Actions；以下是代码修复与本地检查，不代表真实 MySQL/浏览器验收已通过。

## 共享根因与最小修改

| 分类 | 已核对证据 | 修改 |
|---|---|---|
| 生产集成回归 | S1 run 35434529422：H5 登录 200 后无工作台请求，出现导航失败提示。uni-h5-vite 的生产 API 重写将 `window.uni` 置空，静态 request/storage 调用也绕过认证适配器 | H5 manifest 关闭 uni API 重写优化，保留 Vite 普通 tree shaking；实际插件 transform 已验证保留 runtime 调用，增加认证适配器执行测试 |
| 权限查询回归 | Graduation targeted run 35434529443：非主席评委无待评分记录；panel 的 mentorId 有数字和字符串两种 JSON 表示 | 精确匹配两种 JSON ID，保留租户、批次和评委关系条件；MySQL 用例覆盖两种表示 |
| 请求合同缺口 | 前端任务选择器发送 termId/keyword，教学任务接口没有接收和过滤 | 在作用域约束内同时过滤计数与分页；关键词按字面匹配，不能通过 `%` 扩大结果 |
| 写入校验回归 | Main shard 0：资助调整审核接受错误目标等级 | 校验目标等级与申请一致，拒绝未知 action，传递驳回理由 |
| 测试身份失真 | 学工待办受理人、实习导师、毕设开题接收人及教学秘书已要求稳定账号；旧测试只有姓名或虚构 ID | 建立真实测试账号/角色/导师关系，学工使用真实登录；不放宽生产鉴权 |
| Sandbox 初始化缺口 | Student Portal V5 run 35434529631：关闭 mock login 后 student2 无正式角色上下文 | 补充 STUDENT、COUNSELOR 角色关联 |
| 测试合同漂移 | IAM 选择器用 `:checked`，点击后定位到另一权限；毕业设计批次条/身份按钮/附件展示已更新 | 固定权限 code，使用当前控件与 fileVersionId；保留业务结果断言 |
| 并发测试 harness | 学期测试线程未传播 ContextVar，新增 resource guard 未加入既有租户补丁 | 仅补齐测试中的 resource guard 租户上下文 |
| 证据锚点漂移 | Commerce foundation run 35434529587：旧源码行区间、IAM 模型及新增平台新闻表 | 核对并重锚证据，显式分类六张全局平台表；未授权 purge，未放宽拒绝条件 |

旧教务静态合同另按现行分页、公开 reasonCode、只读入口、路由归属及办理状态更新。新路由逐项列举权限，冻结旧路由仍核对，未知新增路由仍失败。

## 本地验证

- 61 项独立的非数据库 Python 检查通过（包含路由、页面结构和教学任务 SQL 构造）。这不是 MySQL pytest 验收。
- Commerce schema review 21 项、resource boundaries 25 项通过。
- H5 认证与导航 Node 测试 15 项通过。
- 修改文件 Python/JavaScript 语法检查、diff whitespace 检查通过。
- 本地 Chromium 的 socket 操作和完整 uni build 的网络接口枚举受环境权限限制；无本地 MySQL 服务。未绕过权限或改用假数据库宣称验收。

## 尚未关闭的证据

Production run 35434529425 最终是 29 个失败用例，不等于 29 个独立根因。除本轮覆盖项外，仍包括旧学生选课流程/官网故事断言、未供给的 `E2E_OPTIMIZER_FIXTURE` 与 denial 专项账号、视觉半径及截图差异。不得通过跳过用例、删除门禁或无审查更新截图消红。

CI run 35434529401 的四个 selected shard 汇总仍有失败/错误；其详细 artifact 下载返回 403，本轮不能声称已逐条核验。Academic D 视觉差异及上述剩余浏览器用例仍需逐项补齐证据与专项验收。推送后产生的 Actions 应按修复范围核对，不整套盲目重跑。


## 第三轮收口修复（2026-09-19）

收口基准：`b552062528f5078002cc38ac3fdb236b964b1713`。本轮继续遵守“不删门禁、不跳过真实失败、不整套盲跑”的原则，按共享根因拆解并连续提交。

### 本轮已提交修复

| 提交 | 修复范围 | 根因分类 |
|---|---|---|
| `0372207f` | 补齐 Commerce 智能排课动态表边界审查；IAM 直链拒绝页识别；毕业设计旧定位器；教师小程序装饰图可访问性；Academic D 旧文案/DOM；通用套件排除 optimizer 专项 | 资源边界漏审 / 测试合同漂移 / runner 误收录 |
| `481d986f` | IAM 平台工作区一级菜单；毕业设计服务大厅真实跳转；可访问性装饰图；Academic D 年度参数及学生毕业自查当前 DOM | 当前 UI/路由契约漂移 |
| `ef5eefc` | 学期管理新工作台、校历文本精确定位、Golden rollout 当前圆角样式 | 前端 UI 合同漂移 |
| `8518f3d` | 新增真实“未开通教务”教师身份，并向 Production E2E 注入 denial 账号 | 测试 harness 缺少真实拒绝身份 |
| `2165ff29` | 官网测试切换到 Approved Showcase 新首页（`#ykw-site` / `#students` / 三条产品线），保留 SEO、微信分享、咨询接口验收 | 官网故事/DOM 合同漂移 |
| `0f653c4e` + `b5520625` | 学生 PC 选课门禁改走“批次 → 查看与办理 → 预检 → 提交 → 正式记录/回执”，并修掉 stale locator | 学生门户业务 UI 已升级 |

### 已验证转绿的共享根因

- `Module commerce foundation` 在前一轮最新执行已经由红转绿，证明新增 optimizer 动态表属于边界合同漏审，不是业务数据越权。
- `Academic scheduling candidate acceptance` 已由红转绿；optimizer 专项继续在独立 `yueke_optimizer_test_*` MySQL 验收，不再被通用 Playwright 错误复跑。
- 教师 Today、Student Portal、Sandbox 20K、Security、File Center、Internship、Control Plane 多数组件/专项门禁继续保持绿灯。
- 最新 `b5520625` 统计时已有 **54 组 Actions 成功**。

### 当前剩余状态

截至本次收口记录：

- 完成态失败：`Control Plane School IAM E2E`、`Graduation Browser Functional Gate`。
- 仍在运行/排队：Production Playwright、Main、CI、Academic Exam、Academic D Concurrency、Student Affairs、Graduation Targeted、Control Plane I4 两组、Commerce foundation、容量与可观测性等 11 组。
- PR #270 仍为 **open / mergeable=true / 非 Draft**。

### 对剩余两组红灯的判断

1. **School IAM E2E**：前一轮已经越过原来的 direct-url 拒绝页断言，最新失败点转到平台工作区一级菜单定位；本轮已改为统一消费 `TeacherWorkspaceFrame` 的“一级菜单”可访问导航。需要看新 HEAD 结果确认是否继续下沉到下一处断言。
2. **Graduation Browser Functional Gate**：已确认至少包含三类：
   - 页面 resize 自动补 `page=1`，业务 batch/tab 没变，属于 URL 规范化；
   - 教师小程序“批阅中期”已不在默认前三常用服务，改走真实服务大厅；
   - 旧可访问性检查把 `aria-hidden` 装饰图也算缺 alt；已按可访问性语义修正。
   教师 U8 Gold actual 与 expected 的主要差异来自新增真实分页状态和当前批次卡间距；页面无横向溢出或遮挡。视觉 Gold 仍需按审查后的当前 UI 基线处理，不能简单放宽阈值。

### 合并原则

在剩余完成态红灯归零、必需门禁完成后再合并。若新 HEAD 只剩截图 Gold 差异，应先确认 actual 是当前批准 UI，再更新对应基线；不允许通过删除截图断言、扩大像素阈值或跳过用例获得绿灯。
