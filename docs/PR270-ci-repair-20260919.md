# PR #270 第二轮 CI 修复证据

失败基准：`72e3492e2167c7284612ac8dd3b979f8477568d2`。本轮未手动重跑整套 Actions；以下是代码修复与本地检查，不代表真实 MySQL/浏览器验收已通过。

## 共享根因与最小修改

| 分类 | 已核对证据 | 修改 |
|---|---|---|
| 生产集成回归 | S1 run 35434529422：H5 登录 200 后无工作台请求，出现导航失败提示。uni-h5-vite 的生产 API 重写将 `window.uni` 置空，静态 request/storage 调用也绕过认证适配器 | H5 manifest 关闭 uni API 重写优化，保留 Vite 普通 tree shaking；实际插件 transform 已验证保留 runtime 调用，增加认证适配器执行测试 |
| 权限查询回归 | Graduation targeted run 35434529443：非主席评委无待评分记录；panel 的 mentorId 有数字和字符串两种 JSON 表示 | 精确匹配两种 JSON ID，保留租户、批次和评委关系条件；MySQL 用例覆盖两种表示 |
| 请求合同缺口 | 前端任务选择器发送 termId/keyword，教学任务接口没有接收和过滤 | 在作用域约束内同时过滤计数与分页；关键词按字面匹配，不能通过 `%` 扩大结果 |
| 写入校验回归 | Main shard 0：资助调整审核接受错误目标等级 | 校验目标等级与申请一致，拒绝未知 action，传递驳回理由 |
| 测试身份失真 | 学工待办受理人、实习导师、毕设开题接收人及教学秘书已要求稳定账号；旧测试只有姓名或虚构 ID | 建立真实测试账号/角色/导师关系，学工使用真实登录；不放宽生产鉴权 |
| Sandbox 初始化缺口 | Student Portal V5 run 35434529631：关闭 mock login 后 student2 无正式角色上下文 | 补充 STUDENT、COUNSELOR 角色关联 |
| 测试合同漂移 | IAM 选择器用 `:checked`，点击后定位到另一权限；毕业设计批次条/身份按钮/附件展示已更新 | 固定权限 code，使用当前控件与 fileVersionId；保留业务结果断言 |
| 并发测试 harness | 学期测试线程未传播 ContextVar，新增 resource guard 未加入既有租户补丁 | 仅补齐测试中的 resource guard 租户上下文 |
| 证据锚点漂移 | Commerce foundation run 35434529587：旧源码行区间、IAM 模型及新增平台新闻表 | 核对并重锚证据，显式分类六张全局平台表；未授权 purge，未放宽拒绝条件 |

旧教务静态合同另按现行分页、公开 reasonCode、只读入口、路由归属及办理状态更新。新路由逐项列举权限，冻结旧路由仍核对，未知新增路由仍失败。

## 本地验证

- 61 项独立的非数据库 Python 检查通过（包含路由、页面结构和教学任务 SQL 构造）。这不是 MySQL pytest 验收。
- Commerce schema review 21 项、resource boundaries 25 项通过。
- H5 认证与导航 Node 测试 15 项通过。
- 修改文件 Python/JavaScript 语法检查、diff whitespace 检查通过。
- 本地 Chromium 的 socket 操作和完整 uni build 的网络接口枚举受环境权限限制；无本地 MySQL 服务。未绕过权限或改用假数据库宣称验收。

## 尚未关闭的证据

Production run 35434529425 最终是 29 个失败用例，不等于 29 个独立根因。除本轮覆盖项外，仍包括旧学生选课流程/官网故事断言、未供给的 `E2E_OPTIMIZER_FIXTURE` 与 denial 专项账号、视觉半径及截图差异。不得通过跳过用例、删除门禁或无审查更新截图消红。

CI run 35434529401 的四个 selected shard 汇总仍有失败/错误；其详细 artifact 下载返回 403，本轮不能声称已逐条核验。Academic D 视觉差异及上述剩余浏览器用例仍需逐项补齐证据与专项验收。推送后产生的 Actions 应按修复范围核对，不整套盲目重跑。
