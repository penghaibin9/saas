# 设计稿 ↔ 代码 对照表（PC-UI v2）

> 给 Claude Code：改哪个文件，照哪张图，用哪些组件。配合《07-ClaudeCode落地原则.md》使用。

## 0. 全局

| 设计内容 | 设计稿 | 代码文件 | 说明 |
| --- | --- | --- | --- |
| 设计令牌（含 6 主题） | 06 + `tokens-v2.css` | `frontend/src/styles/tokens.css` | 用 `tokens-v2.css` 整体替换；变量名向后兼容 |
| 全局基线 | 06 | `frontend/src/App.vue` | 背景 / 选区 / 字体基线 |
| 壳：顶栏 + 左栏 | 01（所有页共用） | `frontend/src/layouts/BasePortalLayout.vue` | 顶栏 52px 信息层；左栏 224px 三段式（锚点→角色过滤分组手风琴→主题）；菜单数据消费 `config/adminMenu.js` 的 `getVisibleAdminMenu(ctx)` |
| 角色切换器 | 01 顶栏右侧 | 各模块 Layout 已有 `switchRole` 上下文 | 从 header-right 插槽升级为统一组件 |
| 数据范围镜片 | 01/02 顶栏 | `ctx.dataScope` | 蓝底胶囊，锁定范围时实心 |

## 1. 工作台（设计稿 01）

| 区块 | 组件 / 文件 |
| --- | --- |
| 页面 | `views/AdminWorkbenchView.vue` |
| 问候行（替代渐变横幅） | 替代 `components/dashboard/AppLifecycleBanner.vue` 的使用位 |
| 质量提醒行 | `components/common/AppInlineAlert.vue` |
| 指标卡 ×4 | `components/common/AppMetricCard.vue`（trendQuality 决定色） |
| 「现在最该处理」队列 | `components/dashboard/TaskWorkbenchPanel.vue` + `TaskCard.vue` 改造为 QueueItem 行 |
| 阶段分布条 | `components/dashboard/LifecycleTimeline.vue` |
| 风险学生卡 | `components/dashboard/AppRiskStudentCard.vue` / `StudentRiskCard.vue` |
| 待我审批 | `components/common/AppTodoCard.vue` |
| 角色差异 | 工作台按「区块注册表 × ctx.currentRole」组装（见 01 底部说明） |

## 2. 学生主档列表（设计稿 02 · 样板页，第一个做）

| 区块 | 组件 / 文件 |
| --- | --- |
| 页面 | `views/admin/student/StudentListView.vue`（模块壳 `AdminStudentLayout.vue`） |
| 页头 + 导入导出 | `components/common/AppPageHeader.vue`、`views/admin/student/StudentImportExportView.vue`、`security/guards/export.guard.js` |
| 筛选区 + 状态 chips + 保存视图 | `components/business/AdvancedFilter.vue`、`ModuleToolbar.vue` |
| 批量操作条 | 新增小组件（放 `components/business/`），墨色 `--bg-batchbar` |
| 表格 | `components/business/DataTable.vue`（行高 `--table-row-*`） |
| 状态 / 风险标签 | `components/common/AppStatusTag.vue`、`AppRiskTag.vue`（映射见 06） |
| 学生 360 抽屉 | `components/ui/AppDrawer.vue` + `components/dashboard/AppHandleDrawer.vue` 模式 |
| 四态 | `components/business/EmptyState/LoadingState/ErrorState.vue`、`components/common/AppGlobalState.vue` |
| 脱敏 | `components/common/AppSensitiveText.vue` + `sensitive-mask.helper.js` |

## 3. 岗位实习 · 过程监管（设计稿 03）

| 区块 | 组件 / 文件 |
| --- | --- |
| 页面 | `views/admin/internship/InternshipDashboardView.vue`（壳 `AdminInternshipLayout.vue`） |
| 带目标线 KPI 卡 | `AppMetricCard.vue` 新增可选 prop `targetLine` |
| 异常处理队列 | `views/admin/internship/AttendanceExceptionListView.vue` 行组件复用 |
| 教师批阅积压排行 | 新增 BarRank 小组件（`components/dashboard/`） |
| 巡访列表 | `InternshipRiskView.vue` 相关行样式 |
| 处理抽屉（认定正常/提醒纠正/记异常） | `AttendanceExceptionDetailView.vue` → 抽屉化 |

## 4. 审批中心 · 待办（设计稿 04）

| 区块 | 组件 / 文件 |
| --- | --- |
| 页面 | `views/admin/approval/ApprovalTodoListView.vue` + `ApprovalDetailView.vue` 合并为三栏检件流（路由不变，Detail 变中栏） |
| 步骤条 | `components/common/AppStepBar.vue`（当前环节 + 停留时长） |
| 原值/新值 diff 表 | 新增 FieldDiff 小组件 |
| 驳回原因面板 | 必填 ≥5 字 + 常用短语（对齐小程序 T5 规则） |
| 审计留痕栏 | `components/dashboard/OperationLogList.vue`、`AppTimeline.vue` |
| 键盘流 | J/K/Enter/R，处理完自动切下一件 |

## 5. 系统管理 · 角色与权限（设计稿 05）

| 区块 | 组件 / 文件 |
| --- | --- |
| 页面 | `views/admin/system/SystemRoleListView.vue`（壳 `AdminSystemLayout.vue`） |
| 权限矩阵 | 新增 PermissionMatrix 组件（勾选/锁定/待保存三态） |
| 影响预览 + 变更留痕 | 右栏面板 + `audit-event.helper.js` |
| 保存条（二次确认 + 生效时间） | `components/common/AppConfirmDialog.vue` |
| 数据范围 tab | `SystemDataScopeView.vue` |

## 6. 建议扩展顺序

student（样板）→ approval/workflow → internship → orientation → campusService → academic → graduation → employment → dataCenter → system → platform → 工作台收尾。

## 7. 演示数据口径（保持一致）

南京职业技术学院 · 全校 12,486 / 信息工程学院 2,847 · 实习 862 · 打卡异常 24（超24h）· 周报积压 38 · 风险 12（高3中5低4）· 待核验 126 · 待我审批 3。人物：张晓明（学院管理员）、王莉/张伟（辅导员）、王建国（导师）、学生张一鸣/李雨桐/王晨/赵思远/陈浩宇等。
