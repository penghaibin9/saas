# Teacher PC V2 全量视觉与交互回归台账

> PR：#27  
> 分支：`codex/teacher-pc-v2-html-library`  
> 最新已验证页面来源 HEAD：`a7e4d92256874b95975155d5d7a9377fe0ec699b`  
> 状态：执行中，保持 Draft；未合并，未开始生产前端替换。

## 1. 当前冻结口径

当前原型库共有 **290 个独立 HTML**，最低基础浏览器回归量为：

```text
290 × 3 = 870 次渲染
```

三档视口：

- 1280 × 900
- 1440 × 1000
- 1920 × 1080

当前 Chromium 管理策略禁止 `page.goto()` 访问 `file://`、`localhost` 和外部 URL。为保持真实 DOM、CSS 和 JavaScript 执行，回归器把当前 GitHub Blob 对应的 HTML、CSS、JavaScript 和 SVG 按原加载顺序内联，再通过 Chromium `page.set_content()` 渲染。文件存在性、相对引用和跳转目标由独立一致性检查负责。该方式不得描述为“直接 file:// 打开通过”。

## 2. 已完成真实回归

### 2.1 冻结缺口工作区

覆盖页面：专业分流、排课管理、课堂考勤、数字迎新、班级与辅导员、谈心家校、活动二课与社团。

精确资源：

- `shared/v2-freeze-gap.css`：`ce09e724ecef5920a8d5d3a6aed0fb0a299afb82`
- `shared/v2-freeze-gap.js`：`2473993565153240860a0d6fd657d78344a20396`

| 页面数 | 三档渲染 | 通过 | 失败 |
|---:|---:|---:|---:|
| 7 | 21 | 21 | 0 |

本组 **21 / 21 PASS**。

### 2.2 课程库工作区

覆盖 13 页：课程列表、新建、详情、分类、性质、学分学时、大纲、考核方式、负责人、材料、停用、编辑、历史课程。

共享课程渲染器保持原始 Git Blob：

- `shared/v2-course-base-packed.js`：`89f24983cf485d001f8658bcd4f8feab18719352`

首次真实回归发现 9 页在“正常状态”和“长数据状态”同时渲染抽屉或弹层，产生重复 ID，造成 27 次失败。修复没有删除检查，也没有隐藏弹层：9 个入口在异步渲染完成后仅对重复的长数据副本增加 `-long` 作用域，并同步修改 `data-open`、`data-close` 和 ARIA 引用。原始课程渲染器保持不变。

修复后的 9 个入口 Blob SHA：

- 考核方式：`19bdde71e0b0aa99965e1fac389a76d87ebca236`
- 课程分类：`ffd3b86af03d5c041b0b0e940d3fd555f7df02bf`
- 课程性质：`433655c81b65fb3d0a1ef733c809fd71bc30666f`
- 学分学时：`b970acd379d61d11c410426fdd6722971ab56218`
- 课程详情：`0e417194e44f83cd56a3a0c583e7961629aee338`
- 课程停用：`da56ee71b72f8b90d51c6c0173f24286ba04cd7e`
- 课程材料：`1238c04dd7e9cb8eae930dad978cb99b38c4e87b`
- 课程大纲：`528731a5994657e5a61be011f14fec14ba85523b`
- 课程负责人：`a0a790bfc800c94d5973525fc3a00e405012e7d1`

| 页面数 | 三档渲染 | 首轮通过 | 首轮失败 | 修复后通过 | 修复后失败 |
|---:|---:|---:|---:|---:|---:|
| 13 | 39 | 12 | 27 | 39 | 0 |

本组最终 **39 / 39 PASS**。

### 2.3 成绩管理与成绩审核工作区

覆盖 12 页：成绩总览、成绩录入、挂科清单、学生成绩单、成绩异常、学院审核、教务发布、成绩更正、复查复审、操作审计、成绩认定与课程替代、成绩统计。

本组页面只依赖已经核准的公共 Token、壳、组件、通用交互脚本和 SVG 图标。逐页验证：

- 教务中心 29 个真实二级模块完整生成；
- 成绩管理与成绩审核发布更正的当前模块高亮正确；
- 面包屑和活动页签唯一；
- 页面脚本、控制台和 Promise 无异常；
- 无重复 ID、无根页面横向溢出；
- `grade-fail.html` 已提供的默认、加载、空、错误状态逐一切换成功；
- 其余页面未伪造不存在的状态按钮，静态业务结构正常。

| 页面数 | 三档渲染 | 通过 | 失败 |
|---:|---:|---:|---:|
| 12 | 36 | 36 | 0 |

本组 **36 / 36 PASS**。

## 3. 每次渲染检查

- 页面脚本执行完成并生成标题、主标题和业务工作区；
- 控制台 warning / error；
- `pageerror` 与未处理 Promise；
- CSS、JavaScript、图片和其他资源失败；
- 重复 ID；
- 根页面横向溢出；
- 页面实际提供的默认、加载、空、错误、403 / 无范围、只读和长数据状态；
- 弹层焦点进入、Escape 关闭与焦点归还（页面存在对应交互时）；
- 当前二级模块、三级页签和面包屑基本一致性。

## 4. 累计结果与历史证据边界

仓库早期曾记录 169 页口径下的 141 次历史回归，该口径已过时，不得与当前 290 页冻结回归直接相加并宣称完成。

当前冻结候选口径只确认：

```text
96 / 870 PASS
774 / 870 待执行
```

## 5. 已验证业务红线

- 专业分流试分不落库，只有最终确认才写学籍专业；
- 排课规则不覆盖人工排课结果，发布或归档批次只读；
- 课堂考勤 PC 只统计移动端已提交场次，不提供逐生补点名；
- 迎新阶段事实不复制学生主档；
- 辅导员未配置班级责任范围时 fail-closed；
- 谈话与家庭联系人明文受独立权限、用途和审计保护；
- 报名不等于参加，签到不等于积分或成绩确认；
- 课程已启用后的修改创建新版本，历史培养方案继续引用原版本；
- 课程停用受培养方案引用保护；
- 课程材料作废保留审计，不模拟真实删除；
- 成绩发布、更正、认定和复查页面保留分级权限、不可撤销提示、原值留痕和审计边界。

## 6. 尚未完成与不得误报事项

- 剩余 **258 页、774 次**三档回归尚未执行；
- 42 个实际共享依赖尚未全部材料化并完成 Blob SHA 核对；
- 当前完整 HEAD 的 Manifest / 文件 / 孤儿 HTML / 重复 route owner / 相对资源一致性检查尚未最终 PASS；
- 打印页、复杂宽表和各中心高风险业务状态尚未完成人工复核；
- 最终冻结 HEAD 的岗位实习 101 叶子 / 99 URL 审计尚未重跑；
- G0–G7 尚未全部 PASS；
- 当前不能记录冻结 HEAD，不能生成四条生产施工提示词，不能把 PR 转为 Ready，也不能合并 main。

## 7. 下一批执行顺序

1. 培养方案、教学任务、课表、调停课、选课、考务共享页面族；
2. 注册、异动、组织、学期校历、补考与预警页面族；
3. 评价、质量、归档、统计、学工、岗位实习和毕业设计工作区；
4. 修复全部控制台、资源、溢出、重复 ID、状态和焦点失败；
5. 全量重跑至 870 / 870；
6. 完成人工打印和业务红线复核；
7. 最终候选 HEAD 重跑一致性与岗位实习审计；
8. G0–G7 全部通过后记录冻结 HEAD。
