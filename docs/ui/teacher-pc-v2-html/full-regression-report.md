# Teacher PC V2 全量视觉与交互回归台账

> PR：#27  
> 分支：`codex/teacher-pc-v2-html-library`  
> 最新已验证页面来源 HEAD：`f30f4094b42ad42472d835ca0dc26ced59505b31`  
> 状态：执行中，保持 Draft；未合并，未开始生产前端替换。

## 1. 当前冻结口径

当前原型库共有 **290 个独立 HTML**，最低基础浏览器回归量为：

```text
290 × 3 = 870 次渲染
```

三档视口：1280 × 900、1440 × 1000、1920 × 1080。

当前 Chromium 管理策略禁止 `page.goto()` 访问 `file://`、`localhost` 和外部 URL。回归器将当前 GitHub Blob 对应的 HTML、CSS、JavaScript 和 SVG 按原加载顺序内联，再通过 Chromium `page.set_content()` 执行真实 DOM、CSS 与 JavaScript。文件存在性、相对引用和跳转目标由独立一致性检查负责，不把该方式描述为“直接 file:// 打开通过”。

## 2. 已完成真实回归

### 2.1 冻结缺口工作区

覆盖专业分流、排课管理、课堂考勤、数字迎新、班级与辅导员、谈心家校、活动二课与社团。

- `shared/v2-freeze-gap.css`：`ce09e724ecef5920a8d5d3a6aed0fb0a299afb82`
- `shared/v2-freeze-gap.js`：`2473993565153240860a0d6fd657d78344a20396`

| 页面数 | 三档渲染 | 通过 | 失败 |
|---:|---:|---:|---:|
| 7 | 21 | 21 | 0 |

本组 **21 / 21 PASS**。

### 2.2 课程库工作区

覆盖课程列表、新建、详情、分类、性质、学分学时、大纲、考核方式、负责人、材料、停用、编辑、历史课程共 13 页。

- 原始共享渲染器：`shared/v2-course-base-packed.js@89f24983cf485d001f8658bcd4f8feab18719352`
- 首轮：12 / 39 通过，27 / 39 因正常与长数据状态复制同一弹层 ID 失败。
- 修复：仅给 9 个受影响入口的长数据交互 ID 加 `-long` 作用域，并同步 `data-open`、`data-close` 与 ARIA 引用；不修改共享课程业务渲染器。
- 复测：**39 / 39 PASS**。

### 2.3 成绩管理与成绩审核工作区

覆盖成绩总览、录入、挂科清单、学生成绩单、异常、学院审核、教务发布、更正、复查复审、操作审计、成绩认定与课程替代、成绩统计共 12 页。

逐页验证 29 个真实教务二级模块、当前模块高亮、面包屑、活动页签、控制台、Promise、重复 ID、根溢出，以及页面实际提供的状态切换。

| 页面数 | 三档渲染 | 通过 | 失败 |
|---:|---:|---:|---:|
| 12 | 36 | 36 | 0 |

本组 **36 / 36 PASS**。

### 2.4 学年学期与校历节次工作区

覆盖学期管理、学年管理、当前学期、学期周次、教学周配置、学期状态、切换记录、学期归档、校历管理、节假日、补课日、教学周日历、校历发布、校历归档、节次管理和上课时间段等 19 页。

当前 HEAD 首轮真实执行：

- 33 / 57 通过；
- 24 / 57 失败；
- 失败集中在 8 页，原因均为正常状态和长数据状态复制相同弹层或抽屉 ID；
- 涉及 `edit-event`、`delete-event`、`publish-calendar`、`switch-current`、`publish-term`、`freeze-term`、`unfreeze-term`、`edit-band`、`delete-band`、`edit-slot`、`delete-slot`。

修复只修改 `shared/v2-time-base.js`：复制长数据状态时，对 `id`、`data-open`、`data-close`、`aria-labelledby`、`aria-describedby`、`aria-controls` 统一追加 `-long`，不改变页面字段、业务状态或动作。

- 修复 Blob：`f5a82fa33205e2652a1229d5317ffcac47165c7b`
- 修复提交：`f30f4094b42ad42472d835ca0dc26ced59505b31`
- 复测：**57 / 57 PASS**。

## 3. 每次渲染检查

- 页面脚本完成并生成标题、主标题和业务工作区；
- 控制台 warning / error、`pageerror`、未处理 Promise；
- CSS、JavaScript、SVG 和其他实际引用是否可材料化；
- 重复 ID、根页面横向溢出；
- 页面实际提供的默认、加载、空、错误、403 / 无范围、只读和长数据状态；
- 弹层或抽屉打开、Escape 关闭和引用目标；
- 当前二级模块、三级页签和面包屑基本一致性。

## 4. 当前累计

早期 169 页口径下的 141 次历史回归已经过时，不与当前冻结口径相加。

```text
153 / 870 PASS
717 / 870 待执行
```

已完成 51 / 290 个独立 HTML；剩余 239 页。

## 5. 已验证业务红线

- 专业分流试分不落库，只有最终确认才写学籍专业；
- 排课规则不覆盖人工排课结果，发布或归档批次只读；
- 课堂考勤 PC 只统计移动端已提交场次；
- 迎新阶段事实不复制学生主档；
- 辅导员未配置责任范围时 fail-closed；
- 谈话与家庭联系人明文受独立权限、用途和审计保护；
- 报名不等于参加，签到不等于积分或成绩确认；
- 课程修改、停用和材料作废保留版本及审计边界；
- 成绩发布、更正、认定和复查保留分级权限、不可撤销提示和原值留痕；
- 当前学期切换不改变学期状态，教学周结构只允许草稿修改，校历发布后事件锁定。

## 6. 尚未完成

- 剩余 **239 页、717 次**三档回归；
- 42 个实际共享依赖尚未全部材料化并完成 Blob SHA 核对；
- 当前完整 HEAD 的 Manifest / 文件 / 孤儿 HTML / 重复 route owner / 相对资源一致性检查尚未最终 PASS；
- 打印页、复杂宽表和各中心高风险业务状态尚未完成人工复核；
- 最终冻结 HEAD 的岗位实习 101 叶子 / 99 URL 审计尚未重跑；
- G0–G7 尚未全部 PASS。

因此仍不能记录冻结 HEAD、生成四条生产施工提示词、把 PR 转为 Ready 或合并 main。

## 7. 下一批执行顺序

1. 培养方案、教学任务、课表、调停课、选课、考务共享页面族；
2. 注册、异动、组织、补考与预警页面族；
3. 评价、质量、归档、统计、学工、岗位实习和毕业设计工作区；
4. 全量重跑至 870 / 870；
5. 完成人工打印、键盘、焦点和业务红线复核；
6. 最终候选 HEAD 重跑一致性与岗位实习审计；
7. G0–G7 全部通过后记录冻结 HEAD。
