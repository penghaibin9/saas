# Teacher PC V2 全量视觉与交互回归台账

> PR：#27  
> 分支：`codex/teacher-pc-v2-html-library`  
> 候选来源 HEAD：`3dc5431b84d27f49c52e13b53048cfbca54c54c2`  
> 状态：执行中，保持 Draft；未合并，未开始生产前端替换。

## 1. 当前冻结口径

当前原型库共有 **290 个独立 HTML**，最低基础浏览器回归量为：

```text
290 × 3 = 870 次渲染
```

三档视口：

- 1280 × 900
- 1440 × 1000
- 1920 × 1080

当前 Chromium 管理策略禁止 `page.goto()` 访问 `file://`、`localhost` 和外部 URL。为保持真实 DOM、CSS 和 JavaScript 执行，回归器把当前 GitHub Blob 对应的 HTML、CSS 和 JavaScript按原加载顺序内联，再通过 Chromium `page.set_content()` 渲染。文件存在性、相对引用和跳转目标由独立一致性检查负责。该方式不得描述为“直接 file:// 打开通过”。

## 2. 本轮新增真实回归

### 冻结缺口工作区

覆盖页面：

1. 专业分流
2. 排课管理
3. 课堂考勤
4. 数字迎新
5. 班级与辅导员
6. 谈心家校
7. 活动二课与社团

使用资源：

- `shared/v2-freeze-gap.css`
  - Git Blob SHA：`ce09e724ecef5920a8d5d3a6aed0fb0a299afb82`
- `shared/v2-freeze-gap.js`
  - Git Blob SHA：`2473993565153240860a0d6fd657d78344a20396`

本地材料化后重新计算 Git Blob SHA，与 GitHub 候选来源 HEAD 完全一致。

结果：

| 页面数 | 三档渲染 | 通过 | 失败 |
|---:|---:|---:|---:|
| 7 | 21 | 21 | 0 |

每次渲染检查：

- 页面脚本执行完成并生成标题、主标题和业务工作区；
- 控制台 warning / error；
- `pageerror` 与未处理 Promise；
- CSS、JavaScript 和其他请求失败；
- HTTP 4xx / 5xx（导航模式适用时）；
- 重复 ID；
- 根页面横向溢出；
- 默认、加载、空、错误、403 / 无范围、只读、长数据七种状态切换；
- 高风险确认弹层焦点进入；
- Escape 关闭；
- 关闭后焦点归还触发按钮。

本组 **21 / 21 PASS**。

## 3. 累计结果与历史证据边界

仓库早期曾记录 169 页口径下的 141 次历史回归，该口径已过时，不得与当前 290 页冻结回归直接相加并宣称完成。

当前冻结候选口径只确认：

```text
21 / 870 PASS
849 / 870 待执行
```

历史课程共享渲染包修复及早期页面族回归仍可作为问题追溯证据，但最终冻结结论必须以同一最终 HEAD 的重新执行结果为准。

## 4. 本轮验证的业务红线

- 专业分流试分不落库，只有最终确认才写学籍专业；
- 排课规则不覆盖人工排课结果，发布或归档批次只读；
- 课堂考勤 PC 只统计移动端已提交场次，不提供逐生补点名；
- 迎新阶段事实不复制学生主档；
- 辅导员未配置班级责任范围时 fail-closed；
- 谈话与家庭联系人明文受独立权限、用途和审计保护；
- 报名不等于参加，签到不等于积分或成绩确认。

## 5. 尚未完成与不得误报事项

- 剩余 **283 页、849 次**三档回归尚未执行；
- 42 个实际共享依赖尚未全部材料化并完成 Blob SHA 核对；
- 当前完整 HEAD 的 Manifest / 文件 / 孤儿 HTML / 重复 route owner / 相对资源一致性检查尚未最终 PASS；
- 打印页、复杂宽表和各中心高风险业务状态尚未完成人工复核；
- 最终冻结 HEAD 的岗位实习 101 叶子 / 99 URL 审计尚未重跑；
- G0–G7 尚未全部 PASS；
- 当前不能记录冻结 HEAD，不能生成四条生产施工提示词，不能把 PR 转为 Ready，也不能合并 main。

## 6. 下一批执行顺序

1. 恢复并校验公共壳、组件、工作流和通用交互资源；
2. 按共享渲染器页面族批量执行课程、培养方案、教学任务、课表、调停课、选课、考务；
3. 执行评价、质量、归档、统计、学工、岗位实习和毕业设计工作区；
4. 修复全部控制台、资源、溢出、重复 ID、状态和焦点失败；
5. 全量重跑至 870 / 870；
6. 完成人工打印和业务红线复核；
7. 最终候选 HEAD 重跑一致性与岗位实习审计；
8. G0–G7 全部通过后记录冻结 HEAD。
