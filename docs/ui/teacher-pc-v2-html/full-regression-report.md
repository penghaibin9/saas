# Teacher PC V2 全量视觉与交互回归台账

> PR：#27  
> 分支：`codex/teacher-pc-v2-html-library`  
> 状态：执行中，保持 Draft；未合并，未开始生产前端替换。

## 1. 当前冻结口径

当前原型库共有 **290 个独立 HTML**，最低基础浏览器回归量为：

```text
290 × 3 = 870 次渲染
```

三档视口：`1280 × 900`、`1440 × 1000`、`1920 × 1080`。

当前 Chromium 管理策略禁止 `page.goto()` 访问 `file://`、`localhost` 和外部 URL。历史回归器将对应 GitHub Blob 的 HTML、CSS、JavaScript 和 SVG 按原加载顺序内联，再通过 Chromium `page.set_content()` 执行真实 DOM、CSS 与 JavaScript。文件存在性、相对引用和跳转目标由独立一致性检查负责，不把该方式描述为“直接 file:// 打开通过”。

## 2. 当前累计结果

截至教务统计页面族结束，连续回归累计为：

```text
546 / 870 PASS
324 / 870 待执行
```

已完成：

```text
182 / 290 个独立 HTML
108 / 290 个独立 HTML 待执行
```

早期 169 页口径下的 141 次历史回归已经过时，不计入当前 546 次累计。

### 台账完整性说明

本文件此前只详细登记到 **219 / 870、73 / 290 页**。后续执行窗口继续完成了 **327 次、109 页**，运行器累计到 546 / 870；其中末端三个页面族已完成正式报告回填：

- 毕业资格审核：15 页，**45 / 45 PASS**；
- 教务归档：4 页，**12 / 12 PASS**；
- 教务统计：15 页，**45 / 45 PASS**。

其余中间批次的逐页说明没有完整回填到本总台账。该缺口不否定已经产生的累计计数，但会阻断最终冻结：冻结前必须在同一最终候选 HEAD 上重新执行 870 次并生成完整逐页结果，不能只凭历史累计宣告完成。

### 后续结构变更说明

546 次累计之后，毕业设计中心发现原型工作区投影错误并完成结构纠偏：

- 仍保持 8 个独立 HTML；
- 4 个旧 HTML 被 4 个现行工作区 HTML 替换；
- 8 页共享 JavaScript、菜单、页签、生产入口与内容已重写；
- 全库独立 HTML 总数仍为 290；
- 纠偏后的毕业设计 8 页没有进入既有 546 次累计，当前为 **0 / 24**。

因此历史 546 次仍是已实际执行页面族的累计事实，但不能作为当前所有文件内容的统一 HEAD 回归结果。

## 3. 已完成真实回归

### 3.1 冻结缺口工作区

覆盖专业分流、排课管理、课堂考勤、数字迎新、班级与辅导员、谈心家校、活动二课与社团。

- 结果：**21 / 21 PASS**。
- 学工 4 个冻结缺口页后续只修改契约注释，不改变运行 DOM、CSS、JavaScript 或页面参数；最终候选 HEAD 仍需同源重跑。

### 3.2 课程库工作区

覆盖课程列表、新建、详情、分类、性质、学分学时、大纲、考核方式、负责人、材料、停用、编辑、历史课程共 13 页。

- 首轮：12 / 39 通过，27 / 39 因正常与长数据状态复制同一弹层 ID 失败；
- 修复：只给受影响入口的长数据交互 ID 加 `-long` 作用域，并同步 `data-open`、`data-close` 与 ARIA 引用；
- 复测：**39 / 39 PASS**。

### 3.3 成绩管理与成绩审核工作区

覆盖成绩总览、录入、挂科清单、学生成绩单、异常、学院审核、教务发布、更正、复查复审、操作审计、成绩认定与课程替代、成绩统计共 12 页。

- 结果：**36 / 36 PASS**。

### 3.4 学年学期与校历节次工作区

覆盖学期管理、学年管理、当前学期、学期周次、教学周配置、学期状态、切换记录、学期归档、校历管理、节假日、补课日、教学周日历、校历发布、校历归档、节次管理和上课时间段等 19 页。

- 首轮：33 / 57 通过，24 / 57 因 8 页长数据副本重复弹层或抽屉 ID 失败；
- 修复：复制长数据状态时，对 `id`、`data-open`、`data-close` 和 ARIA 引用统一追加 `-long`；
- 复测：**57 / 57 PASS**。

### 3.5 组织与学籍工作区

覆盖学院、专业、行政班、年级、教学班、专业方向、班级学生、班级调整、组织树、组织统计和变更审计共 11 页。

- 首轮：18 / 33 通过，5 个含交互页面的 15 次渲染因正常与长数据状态重复 ID 失败；
- 修复：只为重复的长数据交互 ID 增加 `-long` 作用域，并同步打开、关闭与 ARIA 引用；
- 复测：**33 / 33 PASS**。

### 3.6 教务看板与学籍管理

覆盖教务看板 1 页、学籍管理 9 页，共 10 页。

- 结果：**30 / 30 PASS**。

### 3.7 我的工作台

覆盖 `workbench/my-workbench/index.html`。

- 结果：**3 / 3 PASS**。

### 3.8 毕业资格审核

覆盖 15 个生产入口对应的独立 HTML，完成生产 `routeName`、权限与跨域事实边界核准。

- 结果：**45 / 45 PASS**；
- 最终候选 HEAD 仍需同源重跑。

### 3.9 教务归档

覆盖归档批次、批量归档、预检和导出 4 页。执行前精确恢复共享归档 CSS / JavaScript 并完成 JavaScript 语法检查。

- 结果：**12 / 12 PASS**；
- 最终候选 HEAD 仍需同源重跑。

### 3.10 教务统计

覆盖教务总览、13 个统计 Tab 与工作量申报审核共 15 页。调停课统计继续复用独立生产页面，不重复制造第 16 个 HTML。

- 原运行时页面族：**45 / 45 PASS**；
- 后续只在 HTML 契约注释中补齐真实 `routeName`，未改变 DOM、CSS、JavaScript 或 `window.V2_PAGE` 运行参数；
- 最终候选 HEAD 仍需同源重跑。

## 4. 已完成静态契约核对，但浏览器未完成

### 4.1 学工中心

15 / 15 页已核对生产 routeName、入口权限、动作权限、数据范围和敏感字段边界：

- 13 页契约一致；
- 数字迎新存在 `studentAffairs.orientation.view` / `orientation.student.view` 菜单与路由权限冲突；
- 班级管理存在 `studentAffairs.class.view` / `campus.record.view` 菜单与路由权限冲突。

4 个冻结缺口页的 12 次历史回归已包含在 546 次累计中；11 个关键工作台仍为 **0 / 33**。

### 4.2 岗位实习

原机器审计实际通过 12 模块、101 叶子、99 URL、2 别名和 99 唯一 owner。比较确认自来源 SHA 起：

- 生产 `navPlan.js` 未变化；
- `310-internship-key.json` 未变化。

因此原审计仍适用于当前契约，但最终候选 HEAD 仍要重新执行脚本与页面回归。

### 4.3 毕业设计

旧原型错误地把生命周期概念拆成生产 8 工作区。现已纠偏为：

1. 我的工作台
2. 批次与实施
3. 题目与选题
4. 过程指导
5. 开题与成果
6. 答辩与成绩
7. 风险与归档
8. 模板与设置

静态生产投影：

- 8 个工作区；
- 50 个三级叶子；
- 48 个唯一 URL；
- 2 个共享 URL：`defense-scoring`、`stats-report`。

实际修改：替换 4 个 HTML、重定义 4 个 HTML、重建 320 Manifest、重写共享 JavaScript。共享 JavaScript 写入前 `node --check` 通过，但当前浏览器回归仍为 **0 / 24**。

## 5. 静态程序化一致性历史结果

历史上已对 290 个 HTML 执行过文件与相对资源扫描：

- 独立 HTML：290 / 290；
- 唯一相对资源：43 / 43；
- HTML 文档节点、标题、viewport、静态重复 ID、相对 HTML 目标、CSS / JavaScript / SVG 引用错误：0；
- 目录外文件：0。

毕业设计结构纠偏改变了 8 个 HTML 的实际文件集合、Manifest 引用和共享 JavaScript。因此历史一致性扫描**不能**覆盖当前最新 HEAD，必须重新执行并重点确认：

- 旧 `artifact.html`、`proposal.html`、`grade.html`、`archive.html` 无残留引用；
- 新 `batch-implementation.html`、`proposal-final.html`、`risk-archive.html`、`templates.html` 被正确引用；
- 290 / 290 HTML、43 / 43 shared、297 条 Manifest 和 9 条共享 HTML 口径仍一致；
- 新页签和左侧菜单无断链或孤儿 HTML。

## 6. 每次浏览器渲染检查

- 页面脚本完成并生成标题、主标题和业务工作区；
- 控制台 warning / error、`pageerror`、未处理 Promise；
- CSS、JavaScript、SVG 和其他实际引用是否可材料化；
- 重复 ID、根页面横向溢出；
- 页面实际提供的默认、加载、空、错误、403 / 无范围、只读和长数据状态；
- 弹层或抽屉打开、Escape 关闭和引用目标；
- 当前二级模块、三级页签和面包屑基本一致性；
- 生产入口、routeName、权限提示和工作区 key 是否一致。

## 7. 已验证业务红线

下列红线已在对应已回归页面族中验证：

- 专业分流试分不落库，只有最终确认才写学籍专业；
- 排课规则不覆盖人工排课结果，发布或归档批次只读；
- 课堂考勤 PC 只统计移动端已提交场次；
- 迎新阶段事实不复制学生主档；
- 辅导员未配置责任范围时 fail-closed；
- 谈话与家庭联系人明文受独立权限、用途和审计保护；
- 报名不等于参加，签到不等于积分或成绩确认；
- 课程修改、停用和材料作废保留版本及审计边界；
- 成绩发布、更正、认定和复查保留分级权限、不可撤销提示和原值留痕；
- 当前学期切换不改变学期状态，教学周结构只允许草稿修改，校历发布后事件锁定；
- 学院、专业和行政班删除均为软删并要求清空下级或在册学生；
- 批量班级调整不替代个体学生转班，不静默改写学生所属班级；
- 毕业资格中的未知外部事实保持 `UNKNOWN`，不由前端伪造 `FAIL`；
- 强制归档继续暴露缺失域，归档后默认只读；
- 教务统计百分比保留分子分母，资源统计保持学校级口径，工作量不冒充薪酬核算。

学工 11 个关键页和毕业设计现行 8 页的红线当前只是静态契约，不得写成“浏览器已验证”。

## 8. 尚未完成

- 剩余 **108 页、324 次**三档回归；
- 学工关键工作台 **11 页、33 次**；
- 毕业设计现行工作区 **8 页、24 次**；
- 最终候选 HEAD 的 290 / 290 文件、43 / 43 shared、Manifest 分片、重复 route owner 与业务映射检查；
- 最终冻结 HEAD 的岗位实习 101 叶子 / 99 URL 审计；
- 毕业设计 50 叶子 / 48 URL 最终投影核对；
- 打印页、复杂宽表、键盘、焦点、Escape 与焦点归还人工复核；
- 学工两个生产权限冲突的生产裁决；
- 中间批次逐页台账缺口由最终全量重跑报告彻底消除；
- G0–G7 尚未全部 PASS。

因此仍不能记录冻结 HEAD、生成四条生产施工提示词、把 PR 转为 Ready 或合并 main。

## 9. 下一批执行顺序

1. 完整分支快照上一致性冒烟，优先检查毕业设计旧 / 新文件引用；
2. 学工 11 个关键工作台执行 33 次回归；
3. 毕业设计现行 8 工作区执行 24 次回归；
4. 推进岗位实习关键工作台与剩余页面族，直至累计 870 / 870；
5. 最终候选 HEAD 全量重跑一致性、浏览器、岗位实习路由审计、毕业设计工作区投影和人工专项；
6. G0–G7 全部通过后记录冻结 HEAD。
