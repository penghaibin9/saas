# 岗位实习关键原型回归记录

## 当前结论

状态：**静态结构完成，真实浏览器回归待执行。**

## 静态核对

- 10 个关键页面均可独立打开并共享统一壳与样式。
- 生产 12 个二级模块和主要三级主链已映射。
- 批次、规则版本、学生主档、企业岗位、匹配协议、打卡请假、报告指导、风险、成绩和归档边界已登记。
- 申请、三方确认、成绩发布、风险关闭和档案生成没有被压缩成假按钮。

## 冻结前浏览器矩阵

每页验证 `1280×900`、`1440×1000`、`1920×1080`，并检查：

1. 直接打开无控制台错误和非预期横向溢出。
2. 12 个二级名称、顺序、当前高亮和三级面包屑正确。
3. 左侧菜单搜索、滚动和收起正常。
4. 10 个关键页签跳转正确。
5. 默认、加载、空、错误、403 / 只读和长数据状态不叠层。
6. 切换批次后摘要、列表和操作使用同一 batchId。
7. 已生效规则只读，草稿版本和适用范围明确。
8. 学生主档字段不在实习中心重复维护。
9. 企业通过、岗位发布和专业匹配分别显示状态。
10. 匹配冲突阻断最终确认，推荐不直接生成协议。
11. 三方协议的学生、企业和学校确认不可跳过。
12. 调岗退岗保留原岗位和协议历史。
13. 有效请假显示 LEAVE 留痕，不显示正常打卡。
14. 补卡、缺卡、位置异常和长期未打卡分别处理。
15. 报告退回保留旧版本，重交生成新版本。
16. 指导不足和巡访问题可进入整改复查。
17. 风险客观事实、责任人、时限和处置历史完整。
18. 成绩缺项阻断发布，审核 / 发布 / 复核按钮按权限显示。
19. 工作量和统计明细服务端分页。
20. 档案包缺失证据时明确失败，不输出假完整包。
21. 就业入口跳到就业模块，不复制就业数据。
22. 导出与下载用途、水印、范围和审计提示完整。
23. 弹层 Escape、焦点进入、陷阱和归还完成验证。

## 当前未验证

- 浏览器渲染：0
- 控制台：未执行
- 溢出：未执行
- 键盘 / 焦点：未执行
- 三档分辨率：未执行
- 截图提交：0

完成矩阵前，只能标记为“岗位实习关键原型结构完成”，不能标记为中心冻结。
