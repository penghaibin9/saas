# PROGRESS

## 当前状态

- 状态：**IN PROGRESS**
- 基线：`main@b0af2244e1c8d466fe8afbd7b2bc0ab067d68489`
- 分支：`codex/teacher-pc-v2-html-library`
- Draft PR：`#27`
- 共享设计系统：`teacher-pc-v2 / 0.4.0`
- 生产代码修改：**否**
- 允许目录外修改：**0**

## 本批完成

### 注册管理

- 注册批次默认页
- 入学注册、学年注册、学期注册三个 query 业务切面
- 批次注册名单详情
- 注册资格核验
- 未注册学生
- 暂缓注册
- 注册异常
- 注册归档
- 注册统计

### 学籍异动办理工作区

- 异动台账
- 通用发起表单
- 转专业专用表单切面
- 转班专用表单切面
- 休学、复学、退学、转专业、转班、保留学籍六个分类申请路由映射
- 异动审批
- 异动生效
- 异动统计
- 异动归档
- 在途审批详情
- 已生效只读详情
- A4 异动审批表打印页

### 学籍详情增强

- 学籍档案基础信息三列详情
- 长学院名、长备注和状态时间线
- 身份证脱敏展示
- 查看完整证件号理由弹窗及审计说明
- 发起异动、信息更正、成绩单、该生全部异动四个真实入口

## 累计数量

- manifest 条目：**52**
- 独立 HTML：**46**
- 共享路由条目：**8**
- shared 文件：**8**（新增 `v2-workflows.css`、`v2-registration-prototypes.js`、`v2-status-change-prototypes.js`）
- 仓库截图：**0**
- 本地累计渲染截图：**64**
- 已完成首轮工作区：**5**
- 完成一级中心：**0**

## 本批验证

- 新增/增强页面：**24**
- 1440 × 1000：24 页全部渲染
- 1280 × 900：8 个关键页额外渲染
- 1920 × 1080：8 个关键页额外渲染
- 本批总渲染：**40 次**
- 页面级横向溢出：**0**
- 控制台错误：**0**
- 相对资源缺失：**0**
- 本地交互检查：35 个抽屉/弹窗触发目标静态识别，逐页执行首个可用交互；138 个状态切换入口，异常 **0**

执行环境说明：当前容器 Chromium 对 `file://` 和 `localhost` 直接导航返回 `ERR_BLOCKED_BY_ADMINISTRATOR`。本批采用“HTML + 本地 CSS/JS/SVG 完全内联后渲染”验证布局与交互，同时独立检查原始 HTML 的相对资源路径是否真实存在。截图仅保存在执行环境，**没有写入 GitHub 仓库**。

## 已完成首轮工作区

1. 成绩管理
2. 成绩审核发布更正
3. 学籍管理
4. 注册管理
5. 学籍异动办理

“首轮完成”只表示当前工作区可达路由、重要 query/办理态和关键状态已进入 manifest 并完成本批检查，不代表教务中心全部完成。

## 已确认差异 / 疑问

1. `navPlan.js` 使用 `PRESERVED`，`AaRosterListView.vue` 使用 `RETAINED`。继续记录，不修改生产代码。
2. `PRESERVE` 是保留学籍，`RETAIN` 是留级，不能合并。
3. `/status-changes/retain` 仅为旧路由重定向，不生成原型。
4. 异动记录 `term_code` 当前无可靠回填，不在异动归档原型中虚构学期筛选。

## 下一批精确起点

按真实业务依赖，下一批从 **学院专业班级** 开始。它是注册资格、转专业、转班、教学任务和学生主档的共同组织底座。

顺序：

1. `/admin/academic-affairs/orgs` 默认学院页
2. `?tab=major` 专业管理
3. `?tab=grade` 年级管理
4. `?tab=class` 行政班
5. `?tab=teachingClass` 教学班
6. `?tab=tree` 组织树
7. `?tab=stats` 组织统计
8. 核对该控制台的重要新建/编辑/停用/导入/详情抽屉状态

完成后继续 **学期与校历**，再进入课程库与培养方案。

## Git 状态

- 保持 Draft PR #27
- 不合并 main
- 不创建新 PR
- 每批只修改 `docs/ui/teacher-pc-v2-html/`
