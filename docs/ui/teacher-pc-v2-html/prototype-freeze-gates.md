# Teacher PC V2 原型冻结闸门

> 只有本文件全部达到 PASS，才能把 PR #27 标记为“原型冻结”，并允许四个生产窗口开始替换 Vue 页面。当前状态：**BLOCKED / NOT FROZEN**。

## G0 · Git 与范围纪律

- [x] PR #27 保持 Open / Draft / 未合并。
- [x] 只修改 `docs/ui/teacher-pc-v2-html/`。
- [x] 生产代码只读。
- [x] 不创建新 PR，不合并 `main`。
- [ ] 冻结时重新核对最新 HEAD、变更文件数和目录外修改为 0。

## G1 · 事实源完整性

- [x] 教务导航按生产 29 个二级模块直接到达。
- [x] 学工关键页按生产菜单与 `StudentAffairsSecurityContext` 核对。
- [x] 岗位实习按生产 12 个二级与 99 个三级入口建立关键工作台与 `coveredRoutes`。
- [x] 毕业设计按生产 8 个 `GRADUATION_WORKSPACES` 一一对应。
- [ ] 教务专业分流、排课管理、课堂考勤等剩余模块完成独立原型收口。
- [ ] 学工数字迎新、班级辅导员、谈心家校、活动二课等非关键页逐路由核对。
- [ ] 岗位实习 99 个三级入口逐项复核字段、权限、状态和 API 参数。
- [ ] 四中心所有页面的生产组件、API、权限与状态候选不存在未解释 `to verify`。

## G2 · Manifest 与文件一致性

- [x] 总 manifest 加载 `00` 至 `320` 全部现有部分。
- [x] 当前登记 290 个业务切面、283 个独立 HTML、41 个共享设计文件。
- [x] 每个新中心有 README、回归清单和独立 manifest。
- [ ] 程序化检查所有 manifest 中的 HTML、CSS、JS 相对路径均存在。
- [ ] 检查重复 route、重复 HTML、孤儿 HTML 和未引用共享文件。
- [ ] README、PROGRESS、route-coverage、PR 描述与总 manifest 数量完全一致。

## G3 · 浏览器全量回归

对当前最新 HEAD 下全部 283 个 HTML：

- [ ] `1280 × 900` 全量打开。
- [ ] `1440 × 1000` 全量打开。
- [ ] `1920 × 1080` 全量打开。
- [ ] 控制台错误为 0。
- [ ] 根页面非预期横向溢出为 0。
- [ ] 左侧菜单搜索、独立滚动、收起、当前高亮正常。
- [ ] 面包屑严格为一级中心 / 二级模块 / 三级功能。
- [ ] 页面间相对链接无 404。
- [ ] 打印页 A4 媒体、分页、后台工具栏隐藏正确。

最低预期渲染量：`283 × 3 = 849` 次；打印与特殊状态另计。

## G4 · 状态与异常回归

每个页面至少核验：

- [ ] 默认状态。
- [ ] 加载状态。
- [ ] 真实空态。
- [ ] 请求失败。
- [ ] 403 / 无数据范围 / 只读。
- [ ] 长文本、长名称、大页数和宽表。
- [ ] 重复提交、并发更新或页面旧缓存时的阻断。
- [ ] 不可逆操作的重新读取与二次确认。

## G5 · 键盘与无障碍

- [ ] Tab 顺序合理。
- [ ] 可操作元素有清晰焦点。
- [ ] 抽屉 / 弹层打开后焦点进入。
- [ ] 焦点陷阱有效。
- [ ] Escape 关闭。
- [ ] 关闭后焦点归还触发按钮。
- [ ] 图表有可访问文字摘要。
- [ ] 状态不只依赖颜色表达。

## G6 · 业务红线回归

### 教务

- [ ] 发布、归档、解冻、成绩更正和统计导出不绕过后端状态。
- [ ] 统计百分比保留分子 / 分母与口径。
- [ ] 教学资源不制造不存在的学院 / 学期范围。

### 学工

- [ ] 未配置范围 fail-closed。
- [ ] 心理、家庭经济和联系人明细按权限、用途和审计展示。
- [ ] 风险 / 宿舍异常不直接变成处分或诊断。
- [ ] 资助评审通过与实际发放分开。

### 岗位实习

- [ ] 批次和规则版本贯穿页面。
- [ ] 三方协议确认不可跳过。
- [ ] 请假写 LEAVE 留痕，不伪造正常打卡。
- [ ] 成绩审核、发布和复核分开。

### 毕业设计

- [ ] 课题容量、专业、导师上限和重复分配冲突阻断。
- [ ] 材料重交保留版本。
- [ ] 答辩回避、时间、场地和人员冲突发布前重检。
- [ ] 缺评分阻断成绩发布，发布后更正保留原版本。
- [ ] 缺材料不生成假完整档案。

## G7 · 生产还原准备

- [ ] 四窗口生产施工映射表中每个窗口只有一个独占中心目录。
- [ ] 公共文件设立唯一公共层负责人。
- [ ] 四窗口禁止同时修改全局导航、布局、Token 和公共组件。
- [ ] 每个页面完成 HTML → Vue → Route → API → Permission → State → Test 映射。
- [ ] 四条总控提示词以冻结 HEAD 为输入，并写明禁止越界和禁止自行修改公共层。
- [ ] 四窗口的分支、Draft PR、合并顺序与冲突处理方案已确定。

## 冻结判定

只有 G0–G7 全部 PASS，才允许：

1. 把 `prototype-manifest.json.status` 改为 `FROZEN`。
2. 更新 README、PROGRESS 和 PR 描述为冻结口径。
3. 输出并启用四条生产施工总控提示词。
4. 同时开启教务、学工、岗位实习、毕业设计四个生产窗口。

当前阻断原因：浏览器全量回归未执行，教务与学工仍有非关键模块缺口，岗位实习 99 个三级入口尚未逐项复核。
