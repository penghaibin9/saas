# Teacher PC V2 原型冻结闸门

> 只有 G0–G7 全部达到 PASS，才能把 PR #27 标记为原型冻结并允许四个生产窗口开始施工。当前状态：**BLOCKED / NOT FROZEN**。

## 当前冻结规模

- Manifest 业务切面：**297**
- 唯一 HTML：**290**
- 共享设计文件：**43**
- 工作区：**60**
- 三档基础浏览器回归：**290 × 3 = 870 次**

## G0 · Git 与范围纪律

- [x] PR #27 保持 Open / Draft / 未合并。
- [x] 只修改 `docs/ui/teacher-pc-v2-html/`。
- [x] 生产代码只读。
- [x] 不创建新 PR，不合并 `main`。
- [ ] 冻结时重新核对最新 HEAD、提交数、变更文件数和目录外修改为 0。

## G1 · 事实源完整性

- [x] 教务导航按生产 29 个二级模块直接到达。
- [x] 专业分流、排课管理、课堂考勤独立原型已按生产 Vue / Route / API 边界补齐。
- [x] 学工 15 个工作区已按生产菜单与 `StudentAffairsSecurityContext` 建立追溯。
- [x] 岗位实习 12 个二级模块已建立 10 个关键工作台。
- [x] 岗位实习 Manifest 已重建为 101 个生产叶子、99 个唯一 URL、2 个显式共享 URL 的契约。
- [x] 毕业设计按生产 8 个 `GRADUATION_WORKSPACES` 一一对应。
- [ ] 实际运行岗位实习机器审计，确认字段、权限、状态、API 参数和 99 URL 全部 0 error。
- [ ] 四中心所有页面不存在未解释的 `to verify`、过时路由或未登记状态。

## G2 · Manifest、文件与资源一致性

- [x] 总 Manifest 加载 `00`–`220`、`300`、`310`、`320`、`330`。
- [x] 总 Manifest 当前声明 297 个业务切面、290 个独立 HTML、43 个共享设计文件。
- [x] 每个新增中心有 README、回归清单或机器 Manifest。
- [x] 已提供 `tools/check-prototype-consistency.mjs`。
- [x] 已提供 `tools/check-internship-route-audit.mjs`。
- [ ] 在完整最新 HEAD 实际运行一致性检查器。
- [ ] HTML、CSS、JS 和共享资源存在性 0 error。
- [ ] 重复 route、共享 HTML、孤儿 HTML、未引用共享文件和目录越界全部有明确结论。
- [ ] README、PROGRESS、route-coverage、PR 描述与总 Manifest 数量完全一致。
- [ ] 最终严格模式及截图证据策略通过。

## G3 · 浏览器全量回归

对同一冻结候选 HEAD 下全部 290 个 HTML：

- [ ] `1280 × 900` 全量打开，290 / 290 PASS。
- [ ] `1440 × 1000` 全量打开，290 / 290 PASS。
- [ ] `1920 × 1080` 全量打开，290 / 290 PASS。
- [ ] 实际渲染总数为 870 / 870。
- [ ] 页面非白屏、`document.readyState=complete`。
- [ ] `console.error`、运行时错误和未处理 Promise 为 0。
- [ ] 样式、图片和其他相对资源加载错误为 0。
- [ ] 重复 `id` 为 0。
- [ ] 非预期根页面横向溢出为 0。
- [ ] 页面间相对链接无 404。
- [ ] 左侧菜单搜索、独立滚动、收起和当前高亮正常。
- [ ] 面包屑严格为一级中心 / 二级模块 / 三级功能。
- [ ] 打印页 A4 媒体、分页和后台工具栏隐藏正确。

执行器：`tools/run-browser-regression.mjs`。

## G4 · 状态与异常回归

每个页面至少核验：

- [ ] 默认状态。
- [ ] 加载状态。
- [ ] 真实空态。
- [ ] 请求失败。
- [ ] 403 / 无数据范围。
- [ ] 只读 / 已发布 / 已归档。
- [ ] 长文本、长名称、大页数和宽表。
- [ ] 重复提交、并发更新或旧版本阻断。
- [ ] 不可逆操作重新读取、版本校验和二次确认。

## G5 · 键盘与无障碍

- [ ] Tab 顺序合理。
- [ ] 可操作元素有清晰焦点。
- [ ] 抽屉 / 弹层打开后焦点进入。
- [ ] 焦点陷阱有效。
- [ ] Escape 关闭。
- [ ] 关闭后焦点归还触发按钮。
- [ ] 图表有可访问文字摘要。
- [ ] 状态不只依赖颜色表达。
- [ ] 正 `tabindex` 和重复 ID 已清零或有合理说明。

新补 7 页的共享弹层已实现焦点进入、Tab 陷阱、Escape 和焦点归还，但必须由浏览器回归验证，不能仅凭代码标记通过。

## G6 · 业务红线回归

### 教务

- [ ] 专业分流试分不落库，待调剂或容量超限阻断确认。
- [ ] 排课规则不覆盖手工 / 导入结果，发布或归档批次只读。
- [ ] 课堂考勤 PC 不制造逐生补点名能力。
- [ ] 发布、归档、解冻、成绩更正和统计导出不绕过后端状态。
- [ ] 统计百分比保留分子、分母、口径、范围和更新时间。

### 学工

- [ ] 未配置范围 fail-closed。
- [ ] 学生360、迎新和班级画像不复制学生主档。
- [ ] 心理、家庭经济、谈话原文和联系人明文按权限、用途和审计展示。
- [ ] 风险 / 宿舍异常不直接变成处分或诊断。
- [ ] 资助评审通过与实际发放分开。
- [ ] 报名、签到、活动结果、志愿时长、积分和申诉分开留痕。

### 岗位实习

- [ ] 101 个生产叶子解析正确，99 个唯一 URL 全部唯一认领。
- [ ] 2 个共享 URL 与生产标签完全一致。
- [ ] 批次和规则版本贯穿页面。
- [ ] 推荐、匹配、申请和三方协议确认不可跳状态。
- [ ] 请假写 LEAVE 留痕，不伪造正常打卡。
- [ ] 报告退回保留版本，巡访整改必须复查。
- [ ] 成绩审核、发布和复核分开。

### 毕业设计

- [ ] 课题容量、专业、导师上限和重复分配冲突阻断。
- [ ] 材料重交保留版本。
- [ ] 答辩回避、时间、场地和人员冲突发布前重检。
- [ ] 缺评分阻断成绩发布，发布后更正保留原版本。
- [ ] 缺材料不生成假完整档案。

## G7 · 生产还原准备

- [x] 四窗口生产施工映射表已建立。
- [x] 教务、学工、岗位实习、毕业设计各有独占中心范围。
- [x] 第一轮全局导航、布局、Token 和公共组件设为只读。
- [x] 四窗口建议分支、Draft PR 纪律和合并顺序已确定。
- [ ] 每个页面完成 HTML → Vue → Route → API → Permission → State → Test 最终映射。
- [ ] 记录冻结 HEAD。
- [ ] 四条总控提示词以冻结 HEAD 为唯一输入并写明禁止越界。

## 冻结判定

只有 G0–G7 全部 PASS，才允许：

1. 把 `prototype-manifest.json.status` 改为 `FROZEN`。
2. 在 Manifest、README、PROGRESS、route-coverage 和 PR 描述记录同一个冻结 HEAD。
3. 生成并启用四条生产施工总控提示词。
4. 同时开启教务、学工、岗位实习、毕业设计四个生产窗口。

## 当前阻断原因

1. 当前会话容器无法解析 GitHub 域名，未能克隆完整最新分支。
2. 现有 CI 未配置执行 docs 内冻结工具。
3. 一致性检查和岗位实习机器审计尚未产生当前 HEAD 的真实报告。
4. 870 次浏览器渲染尚未执行。
5. 打印、状态、键盘、焦点与业务红线人工复核尚未完成。

因此当前不得记录冻结 HEAD，也不得输出四条可执行总控提示词。
