# 毕业资格审核原型回归记录

## 范围

- 独立 HTML：15
- 共享工作台脚本：1
- 模块样式：1
- 机器 Manifest：1
- 修改范围：仅 `docs/ui/teacher-pc-v2-html/`

## 当前结论

状态：**15 页 × 3 档浏览器回归 45/45 PASS；生产 routeName 与权限契约 15/15 已收口。最终冻结候选 HEAD 仍需全库重跑。**

本记录区分页面族已经真实执行的回归与最终冻结 HEAD 的全库回归，不用早期快速检查替代 290 × 3 冻结闸门。

## 已执行浏览器回归

本轮已对 15 个毕业资格审核 HTML 执行三档回归：

- `1280 × 900`
- `1440 × 1000`
- `1920 × 1080`
- 渲染次数：`15 × 3 = 45`
- 结果：**45/45 PASS**

检查覆盖页面生成、共享资源加载、控制台错误、未处理 Promise、静态与动态重复 ID、根页面横向溢出、29 个教务二级模块、当前模块高亮、页签、面包屑，以及页面实际提供的默认、加载、空、错误、403、只读和长数据状态。

## 生产事实核准

已读取并对齐：

- `academic-affairs.routes.js`：`aa-graduation`、`aa-graduation-audit-console`、`aa-certificates`
- `navPlan.js`：15 个精确入口和查看、终审、归档、证书权限
- `AaGraduationBatchView.vue`：预审与审核批次
- `AaGraduationAuditConsoleView.vue`：十项供数、课程 / 学分 / 实践、跨域联动、终审、结果和归档
- `AaCertificateView.vue`：证书生命周期
- `academic-affairs.api.js`：生产 API 适配器

本轮已确认：

- 15 页真实 `routeName`；
- 预审权限为 `academicAffairs.graduation.view`；
- 终审、归档和证书保留生产专用权限；
- `150-graduation.json` 与 15 个 HTML 一致；
- 费用 `UNKNOWN` / 软提醒 / 不阻断边界继续保留；
- 本轮没有修改毕业资格业务状态机、接口、权限实现或生产代码。

## 业务红线

- 未接入财务事实必须保持 `UNKNOWN`，不得由前端改写为 `FAIL`；
- 毕设、实习、处分等跨域事实只展示来源与新鲜度，不复制成第二套主数据；
- 终审不可绕过前置条件与证据摘要；
- 归档后只读，特批动作必须保留理由、权限与审计；
- 证书状态与毕业资格结果分离，不能把“资格通过”直接当作“证书已发放”。

## 最终冻结前仍需验证

- 最终候选 HEAD 的 45 次同源重跑；
- 纳入全库 290 × 3 回归；
- 十项来源证据、终审、学籍终态、归档和证书动作的生产联调；
- 键盘、焦点、Escape、焦点归还与打印检查；
- G0–G7 全部通过后再记录冻结 HEAD。

因此本页面族可标记为“页面族回归通过、契约收口完成”，不可单独宣称全库已经冻结。
