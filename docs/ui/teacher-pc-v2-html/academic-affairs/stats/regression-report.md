# 教务统计原型回归记录

## 当前结论

状态：**15 页 × 3 档浏览器回归 45/45 PASS；生产路由与权限元数据 15/15 已核对并修正。最终冻结候选 HEAD 仍需全量重跑。**

本记录区分“已经真实执行的页面族回归”和“最终冻结 HEAD 的全库回归”，不把代码阅读或文件存在冒充浏览器验证。

## 本轮真实核对（2026-07-31）

以当前生产 `frontend/src/config/navPlan.js` 与 `frontend/src/modules/academicAffairs/academic-affairs.routes.js` 为事实源，完成统计目录 15 个独立 HTML 的逐页核对：

- `stats-overview.html` 及 13 个统计 Tab 的生产路由名统一为 `aa-stats`；
- `workload-review.html` 的生产路由名为 `aa-workload-review`；
- 常规统计 Tab 权限为 `academicAffairs.stats.view`；
- 导出 Tab 权限为 `academicAffairs.stats.export`；
- 工作量申报审核权限为 `academicAffairs.stats.view`；
- 15 个 HTML 均已补入真实 `routeName`，页面内 `window.V2_PAGE.permission` 与生产菜单权限一致；
- 调停课统计继续复用 `/admin/academic-affairs/schedule-change/stats` 与 `aa-schedule-change-stats`，不在统计目录复制第 16 个 HTML。

本轮只修正原型契约注释，不改变业务字段、状态、交互、共享 CSS/JS 或生产代码。

## 已执行浏览器回归

在路由元数据修正前的同一套运行时 HTML、共享 CSS 与共享 JS 上已实际执行：

- 页面：15
- 分辨率：`1280 × 900`、`1440 × 1000`、`1920 × 1080`
- 渲染次数：`15 × 3 = 45`
- 结果：**45/45 PASS**
- 统计共享 JavaScript：语法检查通过

随后发生的变化仅为 HTML 注释内新增 `routeName`，未修改 DOM、样式、脚本或 `window.V2_PAGE` 运行参数，因此不改变该页面族的渲染行为。为满足最终冻结“同一候选 HEAD”要求，仍将在全库冻结轮次重新执行。

## 已完成的静态核对

- 生产 16 个菜单入口均进入 manifest 追踪。
- 14 个统计 Tab 与工作量申报审核均有独立可打开 HTML。
- 调停课统计复用既有独立原型，不复制页面。
- 所有新页面引用统一 Token、壳、组件、流程和统计共享资源。
- URL、routeName、权限、主要筛选、摘要、下钻与导出边界已按生产 `navPlan.js`、`academic-affairs.routes.js`、`AaStatsOverviewView.vue` 和 `AaWorkloadReviewView.vue` 核对。
- 统计页登记正常、加载、空、错误、403 / 无范围和长数据状态。
- 工作量非薪酬、资源学校级范围、导出同步下载无历史等边界已明确。

## 浏览器冻结矩阵

每个新 HTML 至少验证：

- `1280 × 900`
- `1440 × 1000`
- `1920 × 1080`

每页至少检查：

1. 直接打开无控制台错误。
2. 根页面无非预期横向溢出；Tab、宽表和图表只在容器内滚动。
3. 29 个教务二级模块名称与顺序正确，当前“教务统计”高亮。
4. 左侧菜单搜索、独立滚动、展开和收起正常。
5. 面包屑严格为“教务中心 / 教务统计 / 当前三级功能”。
6. 16 个三级入口可到达；调停课统计跳转到已有独立页面。
7. 正常、加载、空、错误、403 / 无范围和长数据状态不叠层。
8. 百分比卡同时显示分子分母，长口径不截断关键含义。
9. 下钻面板继承全部筛选条件，分页不丢筛选。
10. 无数据、真实零值、无范围和接口失败视觉与文案不同。
11. 无学院数据范围时只显示 blocked，不出现全校数字。
12. 教学资源页不出现学期 / 学院筛选。
13. 工作量页持续展示“非正式薪酬核算”警示。
14. 工作量申报审核只允许 `SUBMITTED` 显示通过 / 驳回。
15. 驳回原因不足 5 字不能提交；失败后保留输入。
16. 导出用途不足 5 字不能提交。
17. 导出页不得出现不存在的异步任务历史或失败重试队列。
18. 抽屉可通过按钮和 Escape 关闭；焦点进入、陷阱与归还需要重点验证。
19. 长指标名称、长学院专业名称、长原因与大页数下布局稳定。
20. 统计原型的 placeholder 不被误解为生产真实数据。

## 指标专项核对

- 总览：15 项指标数量、卡片顺序、下钻可用性。
- 学籍：只统计 `EFFECTIVE`。
- 注册：`registered / expected` 与未注册名单一致。
- 教学任务：教师确认和管理确认不混淆。
- 课表：发布覆盖率只认正式发布批次。
- 选课：填充率容量分母、低人数阈值与批次状态一致。
- 考务：课程确认率与缺考 / 违纪事实分开。
- 成绩：有效成绩去重，发布率区分流程阶段。
- 预警：未关闭口径、等级与来源分布一致。
- 毕业：`UNKNOWN` 不变成 `FAIL`。
- 工作量：任务学时与 APPROVED 申报分开，非薪酬。
- 资源：学校级口径，无伪学院筛选。
- 导出：同步 XLSX、用途、水印、权限和审计。

## 最终冻结前仍需验证

- 当前最终候选 HEAD 的 45 次统计目录重跑；
- 全库 290 × 3 的同一 HEAD 回归；
- 键盘、焦点、Escape 与焦点归还人工复核；
- 页面间真实跳转与打印专项；
- 冻结截图与 G0–G7 最终结论。

在最终候选 HEAD 完成上述事项前，本页面族可标记为“页面族回归通过、契约收口完成”，不可标记为“全库最终冻结”。
