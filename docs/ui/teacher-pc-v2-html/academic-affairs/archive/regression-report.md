# 教务归档原型回归记录

## 当前结论

状态：**静态结构完成，真实浏览器回归待执行。**

不把文件存在、代码阅读或历史测试结果冒充为当前 HTML 在最新 HEAD 下的浏览器验证。

## 已完成的静态核对

- 4 个生产入口均有独立 HTML。
- 路由与菜单入口对齐 `navPlan.js`。
- 主控制台结构对齐 `AaArchiveConsoleView.vue`。
- 语义预检结构对齐 `ArchivePrecheckView.vue`。
- 导出结构对齐 `ArchiveExportView.vue`。
- 批次状态登记为 `DRAFT / CHECKING / READY / MISSING_ITEMS / ARCHIVED / CANCELLED`。
- 普通归档、强制归档、取消、特批解冻和导出权限边界已登记。
- “批量归档”已明确为单学期批次内多域处理，不伪装为跨学期一键归档。
- 页面引用统一 Token、壳、组件、流程、质量检查样式和归档工作台资源。

## 浏览器冻结矩阵

每页至少验证：

- `1280 × 900`
- `1440 × 1000`
- `1920 × 1080`

每页至少检查：

1. 直接打开无控制台错误。
2. 根页面无非预期横向溢出；域卡片、表格和审计区域只在容器内滚动。
3. 29 个教务二级模块顺序正确，当前“教务归档”高亮。
4. 左侧菜单搜索、滚动、展开与收起正常。
5. 面包屑严格为“教务中心 / 教务归档 / 当前三级功能”。
6. 4 个三级入口互相跳转且当前项高亮。
7. 正常、加载、空、错误、403 / 只读和长数据状态正常。
8. 创建批次抽屉、强制归档、确认归档、特批解冻和导出用途弹层可开关。
9. Escape 关闭、焦点进入、焦点陷阱和焦点归还需要重点验证。
10. 一学期一批次的重复创建限制有清晰提示。
11. `READY` 与 `MISSING_ITEMS` 的操作按钮严格不同。
12. 强制归档继续展示缺失域，不得变为绿色完整状态。
13. `ARCHIVED` 只保留查看、导出和特批解冻入口。
14. 语义预检能区分“有记录”和“业务已完成”。
15. 阻断域有证据摘要和去处理入口。
16. 学院 / 学校口径局限清晰可见。
17. 正式导出只出现已归档批次。
18. 下载用途不足 5 字时不可提交。
19. XLSX / ZIP、水印、敏感字段和审计提醒完整。
20. 长批次、长域名称、长阻断摘要和长审计说明不破坏布局。

## 当前未验证项

- 浏览器渲染次数：0
- 控制台错误：未执行
- 页面横向溢出：未执行
- 键盘与焦点：未执行
- 三档分辨率：未执行
- 页面间实际跳转：未执行
- 截图提交：0

完成上述矩阵前，只能标记为“首轮原型结构完成”，不能标记为“最终回归完成”。
