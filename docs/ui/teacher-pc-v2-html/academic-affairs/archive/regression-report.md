# 教务归档原型回归记录

## 当前结论

状态：**4 页 × 3 档浏览器回归 12/12 PASS；生产 routeName 与权限契约 4/4 已收口。最终冻结候选 HEAD 仍需全库重跑。**

本记录区分已经真实执行的页面族回归与最终冻结 HEAD 的全库回归，不把文件存在、代码阅读或历史测试冒充为浏览器验证。

## 已执行浏览器回归

本轮已对教务归档 4 个独立 HTML 执行三档回归：

- `1280 × 900`
- `1440 × 1000`
- `1920 × 1080`
- 渲染次数：`4 × 3 = 12`
- 结果：**12/12 PASS**

执行前已从 GitHub Blob 精确恢复归档共享 CSS / JavaScript，并完成共享 JavaScript 语法检查。回归覆盖页面生成、共享资源加载、控制台错误、未处理 Promise、重复 ID、根页面横向溢出、29 个教务二级模块、当前模块高亮、页签、面包屑，以及页面实际提供的默认、加载、空、错误、403 / 只读和长数据状态。

## 已完成的生产契约核对

- 4 个生产入口均有独立 HTML。
- 路由与菜单入口对齐 `navPlan.js`。
- 主控制台结构对齐 `AaArchiveConsoleView.vue`。
- 语义预检结构对齐 `ArchivePrecheckView.vue`。
- 导出结构对齐 `ArchiveExportView.vue`。
- 生产路由名已核准：主控制台 `aa-archive`、预检 `aa-archive-precheck`、导出 `aa-archive-export`；批量归档复用主控制台路由。
- 批次状态登记为 `DRAFT / CHECKING / READY / MISSING_ITEMS / ARCHIVED / CANCELLED`。
- 普通归档、强制归档、取消、特批解冻和导出权限边界已登记。
- “批量归档”明确为单学期批次内多域处理，不伪装成跨学期一键归档。
- 页面引用统一 Token、壳、组件、流程、质量检查样式和归档工作台资源。
- 本轮只补齐原型契约元数据，不修改归档业务状态机、接口、权限实现或生产代码。

## 业务红线

- 一学期只允许一个有效归档批次；
- `READY` 与 `MISSING_ITEMS` 的可执行动作必须不同；
- 强制归档必须继续展示缺失域，不得伪装为完整绿色状态；
- `ARCHIVED` 默认只读，只保留查看、合规导出和特批解冻；
- 语义预检必须区分“存在记录”和“业务已经完成”；
- 正式导出只允许已归档批次，并要求用途、水印、权限和下载审计；
- 下载用途不足 5 字不得提交，不制造不存在的异步导出历史。

## 最终冻结前仍需验证

- 最终候选 HEAD 的 12 次同源重跑；
- 纳入全库 290 × 3 回归；
- 键盘、焦点、Escape、焦点归还与打印检查；
- 归档、强制归档、特批解冻和正式导出的生产联调；
- G0–G7 全部通过后再记录冻结 HEAD。

因此本页面族可标记为“页面族回归通过、契约收口完成”，不可单独宣称全库已经冻结。
