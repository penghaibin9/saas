# PC-UI-LOCK-AND-LANDING · 偏差记录（待设计侧裁决）

> 对接原则：以现有组件 props/emits 与业务逻辑为准（07-ClaudeCode落地原则 §六），
> 与母版 `00-基准-管理端v6三主题.dc.html` 存在以下受控偏差，均为「不改 script 业务逻辑」铁律下的取舍。

| # | 页面/组件 | 母版 | 现状 | 原因 |
| --- | --- | --- | --- | --- |
| 1 | 工作台 Hero orbit | 生命周期 6 阶段人数站点 | 模块分组入口站点（数字=可进入功能数） | 工作台现为菜单桥接页，无生命周期统计数据源；不造假数据 |
| 2 | 审批中心 | 三栏检件流（列表/决策面板/留痕） | 保持现有「待办看板 + 列表 + 详情」路由结构，视觉已母版化 | 合并三栏需改路由与 script，超出表现层范围 |
| 3 | 左二级导航分组小标题 | 概览 / 学生与岗位 / 过程监管… | 平铺菜单（无分组标题） | 各模块 MENUS 配置无分组字段，不改 script 配置；壳已支持 item.section 渐进启用 |
| 4 | 学生主档筛选区 | 状态 chips + 保存视图 + 卡片/表格切换 | AdvancedFilter 下拉 + 表格（已母版配色） | chips 化与视图切换需改 script；组件接口冻结 |
| 5 | 顶栏通知/⌘K | 可点击、有真实计数 | 视觉占位（无全局通知数据源） | 无对应 API，不造假交互 |
| 6 | 角色胶囊 | 点击弹出 Role Workspace 面板 | 展示胶囊 + 各模块原有「角色切换」下拉保留在顶栏 | 角色切换逻辑分散在各模块 ctx API，统一面板需改业务 |

## 已全局落地（无偏差）

- 母版 :root 令牌全部并入 `frontend/src/styles/tokens.css`（V1 变量名全部保留，只改值/新增）。
- 6 主题 `.thw.th-a/b/c/d/e/f`：顶栏主题圆点切换，localStorage `themePreference` 持久化；语义色不随主题变；th-e/th-f 通过 `--rail-*` / `--hero-*` 变量组反转深色区域。
- 统一壳：56px 玻璃顶栏（品牌=产品名+租户学校名 → ⌘K → 主题 → DEV → 数据范围镜片 → 通知 → 角色胶囊）+ 82px 深蓝渐变图标轨（数据源 getVisibleAdminMenu(ctx)，激活玻璃高亮+红角标位）+ 196px 浅色二级导航（标题+计数徽标）+ 内容区径向光晕网格底。
- 玻璃 Hero + orbit 数字站点：ModuleHero 统一改造，11 个模块看板（含学生中心看板）自动继承；趋势色仅由 trendQuality 决定。
- 双栏比例统一 1.62 : 1（module-page.css / orientation-page.css / employment-page.css）。
- 关键数字语义着色沿用 AppStatusTag/AppRiskTag/trendQuality 映射（异常红 / 积压黄 / 正常蓝墨）。
- 产品名统一「高校学生全生命周期管理平台」；学校名仅来自 tenantBrandConfig / 模块 ctx。
