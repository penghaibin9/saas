# 我的工作台精简落地 · 2026-09-06

本轮范围：生产教师 PC `/workbench`，沿用现有 UI 外壳。大屏设计仍暂停。

## 最终结构
按用户最后提供的原学工看板截图组织：左侧紧凑工作摘要、分行优先处理；右侧工作范围、常用入口。使用已有 `--surface`、`--surface-2`、`--pri`、`--pri-50`、`--line`、`--t1`、`--t3` 主题变量。移除大蓝色欢迎区、装饰、重复卡片和帮助墙。
学工事项包括我的待办、风险学生、逾期返校、请假、困难认定、奖助、处分。支持优先级/数量排序、按业务和有待处理筛选。右侧在窄窗口下移。个人待办明细、自定义、真实课程保留在可展开区域。其他无学工看板权限的岗位保留个人待办首屏。

## 数据审计与修复
- 学工聚合复用 `/student-affairs/dashboard`；权限过滤后使用服务端 drillPath，不硬编码截图数字。
- 范围学生、班级、未关闭风险均来自同一数据范围聚合。
- 危急与高风险人数可能重叠，未相加伪造比例。
- 原工作台汇总来自 WorkflowTask，列表来自 UnifiedTodo，两者不同。新增真实审批列表读取 `/approvals/tasks`，个人区分别展示待我审批/业务待办；审批按真实 taskId 打开详情，业务保留服务端 typedRouteTarget。
- 无数据与加载失败分开；未取得值显示横线。高风险业务入口不展示学生敏感明细。
- 保留原个人偏好 API，未改审批提交、角色切换、业务权限或数据库。

## 验证
- 定向 ESLint 无错误。
- 本轮最终定向回归 10 项通过；较早包含外壳与首屏回归共 17 项通过。
- Vite 生产构建与官网预渲染通过（日志 `.codex-temp/workbench-focus-build.log`）。
- 浏览器验收：真实待审批 1 件及奖助 1 件显示；有待处理筛选仅保留这两行；真实审批按钮打开 `/admin/approval/todos/13` 并读取业务信息。未提交审批动作。
- 宽窗口显示左右布局，窄窗口转单列。其他主题使用既有变量，未逐一进行截图验收。
- 文件已写入本地，未提交或推送 Git。

## 彩色快捷栏补齐
按教师 PC HTML 的编辑快捷栏补齐双栏弹窗：添加入口、外观设置、12 个标准图标、6 色圆点、16 字名称、拖动及箭头排序、移除和恢复默认。彩色实底图标与底部快捷栏一致。
原来的边改边存改为独立草稿；保存后写入当前身份的浏览器偏好，取消不写入。恢复与保存重新按有权限的页面列表过滤；未知图标/颜色回退，原有名称、顺序和已保存颜色保留。
验证：定向 ESLint 通过；快捷栏和外壳相关 13 项测试通过；生产构建通过。浏览器中验证取消改名/换色后仍为原设置、保存与刷新恢复。窗口较矮时编辑区独立滚动，底部保存/取消固定可见。原型截图与当前窗口尺寸不同，未声称逐像素还原。
