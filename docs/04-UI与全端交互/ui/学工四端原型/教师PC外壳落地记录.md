# 教师 PC 外壳落地记录

2026-09-06。还原基准为同目录 `教师PC-学工UI壳.html`，以用户随后标注为准：正文不要重复的身份、范围和流程说明，三级菜单展开宽度继续收窄。

## 本轮落地

- 现有工作区顶栏合并为学生、功能、帮助共用搜索框，使用原有学生查询和功能权限投影。学生查询更换关键词时废弃旧请求结果；失败不伪装成空结果。
- 顶栏仅保留账户按钮；账户浮层按 HTML 的个人信息、真实身份列表、外观设置、账号安全、退出分区。学校和身份从现有上下文读取，不复制原型的示例身份。支持关闭按钮、Escape 和点击外部关闭。
- 外观入口连接工作区原有四种主题；日期和工作计时采用实际时间；天气接 Open-Meteo，可搜索和保存城市，首次默认北京，未将其当作学校实际所在地。网络失败有可重试提示。
- 一级菜单右侧放当前范围和工作计时，页签操作改为图标，导航图标按业务区分。
- 学工公共页面标题不再重复呈现角色、数据范围、副标题；申诉页移除流程口号，将刷新并入标题操作区。业务确认、错误原因及权限校验保留。
- 根据最后一张标注，三级菜单展开宽度由 204px 收到 152px；悬浮展开同宽，长标签省略、按钮 title 保留全名。
- 随后的左侧第一列标注：二级业务菜单展开与悬浮宽度也从 204px 收到 152px，两列采用同一规则。

## 本轮核验

- 涉及的 7 个 Vue 文件定向 ESLint 通过。
- 现有外壳、教师聚焦、申诉流程 10 项测试通过。
- 前端生产构建通过（22.73 秒）；仍有现有大包体积提示。
- 浏览器确认申诉页实际数据展示，标题区已无重复说明，三级菜单已收窄；账户浮层结构与用户截图对照，外观入口能打开现有主题设置。天气实际返回北京天气，日期和计时正常显示。

## 尚未完成的范围

当前 BasePortalLayout 的工作区仍由各布局的 workspace 参数启用。学生主档等旧页面还会进入旧外壳，不能据此宣称所有教师 PC 路由已经完整还原。后续应统一布局接入并逐组核对权限菜单、深链接、页签和返回行为。本轮没有修改真实身份切换、退出、账号安全的后端逻辑，也没有新增虚拟身份。

所有改动已写入本地文件，本轮未提交或推送。
