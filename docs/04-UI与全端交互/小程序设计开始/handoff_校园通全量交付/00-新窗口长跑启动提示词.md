# 校园通小程序 UI 全量重做 · 新窗口长跑启动提示词

> 用法：在新 CC 窗口把下面「===== 粘贴以下内容 =====」之间的整段作为第一条消息发给它即可。
> 这是一个自包含任务，不依赖任何历史对话。

===== 粘贴以下内容 =====

# 任务：校园通小程序 UI 全量重做（照新设计稿 1:1 落地 + 接真实后端）

我是非技术产品负责人。我新设计了整套微信小程序页面（约 80 屏），现有小程序页面太丑，要照新设计在现有 `miniapp/` uni-app 工程里重做旧页 + 新建缺失页 + 接真实后端，分波交付。

**这是今晚授权的自主长跑**：按波次连续做完，别每做一点就停下来问我；每完成一波留可核验证据（build 通过 + 独立 commit），全部做完再一次性报告。遇到普通报错/缺口自己查证修复继续，不要中途退回让我决定。

## 一、先读这些（别猜文件名，别凭记忆）
- 仓库根：`C:/Users/10850/Desktop/职校学生全生命周期系统`
- **设计交付包**：`docs/04-UI与全端交互/小程序设计开始/handoff_校园通全量交付/`
  - `README.md` = **施工主索引**：一张页面对照表，每屏标了 角色 / 代码路径（`现有`=照设计重做UI，`新建`=需新增页）/ 一句话开发提示词。**先完整读它。**
  - `design/*.dc.html` = 11 个设计稿，每个含多屏（合计 ~80 屏）。当作观感参考照着重建，**别直接把 HTML 搬进来**。
  - `迎新入口与告知方案.md` = 数字迎新入口布局 + 报到状态机专项。
- **小程序工程**：`miniapp/`
  - 页面 `miniapp/src/pages/`（现有 43 页）
  - 组件 `miniapp/src/components/`（15 个 `Mobile*`：NavBar/StatusTag/Timeline/TabBar/GlobalState/Segmented/InlineAlert/BrandHeader/Progress/RiskTag/SensitiveText/StudentCard/TodoCard/ActionCard/SafeAreaBar）——**优先复用；能力不足先增强公共组件，别在页面里复制一套**
  - 令牌 `miniapp/src/styles/tokens.css`（颜色/字号/间距/圆角/阴影）——**禁止硬编码颜色和尺寸，一律用令牌**
  - 路由/tabBar `miniapp/src/pages.json`——**新建页必须在此注册**
  - 数据层 `miniapp/src/services/`：`request.js`（realRequest + realFirst/realFirstStrict：真接口优先、网络失败才回退 mock、401 刷新单飞、提交锁）/ `realApi.js`（真后端 `/mobile/*` 调用）/ `studentApi.js`+`teacherApi.js`（页面消费的数据服务）/ `config/env.js`（`ENV.useMock`/`allowMockFallback` 开关）
  - mock `miniapp/src/mock/*`
  - 后端移动端端点在 `backend/app/api/v1/mobile.py`（前缀 `/mobile/*`）

## 二、设计规范（所有页统一，见 README §1）
- 学生端主色蓝 `#2563EB`；教师端强调青绿 `#0D9488`（用 tokens 变量，别写死）
- 顶部导航渐变：学生 `linear-gradient(162deg,#0e3a80,#1a56be,#2f76dd)`；教师 `linear-gradient(160deg,#0b5f57,#0d9488,#14b8a6)`
- 卡片：背景 #fff / 圆角 16px / 阴影 `0 3px 14px -8px rgba(15,40,90,.14)`
- 底部 TabBar：学生「首页/服务/消息/我的」；教师「工作台/审批/消息/我的」
- 列表页统一 空 / 加载失败 / 无网络 占位（用 `MobileGlobalState`）

## 三、接后端口径
工程已是「真接口优先 + mock 回退」。每屏这样接：
1. 先查后端有没有对应端点：在 `backend/app` 搜 `/mobile/`，看 `mobile.py` 及各业务域 router。
2. 端点已存在 → 用 `realApi.js`/`realFirst` 接真数据。
3. 端点缺失 → 优先在后端补 `/mobile/*` 端点（各域后端大多已建，复用已有 service，别造重复逻辑）；实在补不了的先 mock 并登记欠账。
4. **禁止假成功 / 假按钮 / 空页面冒充完成**；业务错误（403/409/422/404）必须透出，不得回退 mock 掩盖。

## 四、分波计划（高曝光优先，按 README 章节）
- **波1**：登录与入口(§2.1) + 一级 Tab 页(§2.2) ← 最丑最高频，先做，让我先看到风格对不对
- **波2**：数字迎新完整报到旅程(§2.9，核心卖点，配合 `迎新入口与告知方案.md` 的状态机)
- **波3**：岗位实习(§2.3)
- **波4**：毕业设计(§2.4)
- **波5**：教务(§2.5)
- **波6**：学工(§2.6)
- **波7**：就业(§2.7)
- **波8**：教师端·学生与工作(§2.8)
- **波9**：账号与系统状态(§2.10) + 内容详情(§2.11)

每波流程：读该章节对照表 → 逐屏读对应 `design/*.dc.html` → 重做/新建 `.vue` → 注册 `pages.json` → 接接口 → build 验证 → 独立 commit。

## 五、红线（必须守，违反即返工）
1. **绝对别碰学工**：工作区现有大量甲方未提交的学工 WIP（`backend/app/services/affairs_*`、`frontend/src/modules/studentAffairs/*`、`backend/tests/test_affairs_*`、若干 `校园通-*.dc.html` 删除、`_run/`）。**一律不动、不提交、不 stash、不 reset、不 checkout 覆盖。** 你只在 `miniapp/`（+ 必要时 `backend/app/api/v1/mobile.py` 等 `/mobile/*` 端点）范围内工作。提交时只 `git add` 你 miniapp/后端移动端范围的文件，绝不把学工 WIP 带进提交。
2. **Git**：当前 `master = origin/master = f0525f2`（干净基线，当前 HEAD 也在此）。先 `git checkout -b feat/miniapp-redesign` 建短命分支再干（此操作不会动到工作区里的学工 WIP）。每波独立 commit。**未经我明确授权不许 push、不许并 master。**
3. 复用组件 + 令牌，禁硬编码；旧丑页**原地重做**，别新增并列入口造成双份；新建页注册 `pages.json` 后刷新/返回不 404。
4. **真机可验**：至少跑通 `cd miniapp && npm run build:h5`（或 `npm run build:mp-weixin`），报错必修，不许"没跑通就说完成"。
5. 数据库仍是 MySQL-only；若你补了后端 `/mobile/*` 且涉及新表/字段，走现有 Alembic 迁移，别绕过。
6. 别启动全仓破坏性格式化；别顺手重构无关代码。

## 六、收尾报告（做完全部波次后一次性给）
1. 每波交付页清单（重做 N 页 + 新建 N 页）
2. `npm run build:h5` / `build:mp-weixin` 实际结果
3. 接了哪些真 `/mobile/*` 接口；哪些仍 mock（欠账，写清原因）
4. 后端补了哪些 `/mobile/*` 端点 + 是否含迁移
5. 当前 git 分支与各波 commit 短哈希
6. 未验证项与真实风险
7. `git status --short`（区分学工 WIP 与你本次改动，证明没碰学工）

===== 粘贴到此结束 =====
