# 系统管理视觉精修：PR #262

## 基线与边界

视觉比较基线是 `3c64a1260e076e87cf900d84ce39d9c3a194665f`。提交前核对到原分支又前进至 `00f0407dbda19911728d2c5a98b860b66b184a6e`，因此以这笔新提交为父提交，并保留它新增的两个 `aria-label` 属性。视觉依据仍为用户批准的《19页离线预览》《16个关键交互状态》和《工程施工对照台》（20260906），不重新建立第二套导航或权限系统。

本轮只修改 `workspace.css`、`SystemRoleListView.vue`、`SystemModuleFeatureView.vue`、`RolePermissionPanel.vue`、`RoleMembersPanel.vue`，以及新增的视觉边界测试和本文。未修改 API、后端、路由、权限键、依赖锁、数据库、公共门户壳、品牌页或其他施工分支。保持 Draft，不合并、不部署。

## 精修内容

角色目录统一图标、名称、类型与状态的层级，真实成员数、范围、版本分区对齐；长名称允许换行，操作区贴合卡片底部。当前角色加入相同视觉标识，左侧选中项保留明显边界。

权限矩阵保留双列结构，名称与技术编码分层，选中与键盘焦点可辨识；菜单预览可键盘滚动，窄屏移动到矩阵下方而不是隐藏。默认范围、只读保留权限、核对差异和保存回执继续沿用原语义。

成员表格使用真实姓名生成装饰性头像，保留姓名、工号、状态和分页；完整成员清单具有可聚焦的滚动区域。模块卡片统一四种独立事实和开关呈现，不在前端重新计算授权。状态说明缺失时显示待核对提示，原原因码保留在提示属性中。

创建角色的四个原生控件改为明确的 `label for` / `id` 关联。保留上游对来源模板选择器可访问名称的修复，额外关联可见标签，不修改既有端到端测试的精确标签定位，也不改变提交字段。

样式全部限制在 `.system-workspace`。容器查询按实际业务区宽度决定列数，并提供窄视口回退；保留已有主题变量，支持减少动态效果和高对比模式。不引入字体下载、外部资源、全局覆盖或 `transition: all`。

## 已完成的本地验证

- 45 项定向 Node 测试通过：原 34 项，加 11 项视觉边界测试。
- 修改的四个 Vue 文件 ESLint 通过，编译后的真实 Vue 组件构建通过。
- 15 项组件浏览器行为回归通过，覆盖权限读取失败、只读、保存字段、缓存警告、不重复提交、成员追加、影响失败阻断等已有保护。
- 26 项额外视觉/浏览器检查通过，含 3 个修改前基线、角色/权限/模块的 1440/1280/768/390 宽度、成员表格、明确标签选择、长名称及实际工作区宽度、六个现有主题变量集、键盘焦点与减少动态效果。

新增 `frontend/tests/system-ui-visual-polish.test.mjs` 对业务脚本及受保护的模板指令保留基线摘要；本轮除了 AppIcon 的导入和注册，不改变业务脚本。后续确需变更业务时，必须另行审阅后更新摘要，禁止为了通过检查盲目刷新锚点。

额外检查初次运行的一项失败来自测试容器使用 content-box 导致实际宽度不符；改为明确 border-box 的 748px 业务区后复验通过，未放宽单列断言。

## 不能替代的验收

上述浏览器证据使用合成接口夹具，页面来自实际生产 Vue 编译，不是原型 HTML；但它不证明真实 HTTP、MySQL、多租户、整套公共壳或全部19页验收通过。本地恢复环境不是最新 main 的完整逐文件快照，原项目的精确提交构建仍以本轮 GitHub Actions 回执为准。

`3c64a12` 既有真实 School IAM 新增 UI 流程在来源模板定位处失败。上游 `00f0407` 已修正可访问名称，本轮保留修复并完善原生标签；后续创建、权限保存、追加成员与数据库回读必须等待新提交的真实端到端回执，不因本地夹具通过而提前计为成功。

后续沿原 PR 收口真实请求和浏览器验收；#261/#259 合入后再核对共享后端与公共布局兼容，不把无文本冲突当成语义兼容证据。
