# 学工工作区平铺设计验收

日期：2026-09-06。范围：用户 8 张截图对应页面，以及当前处分统计页，共 9 页。用户最新方向是取消卡片、保留现有外壳和色彩体系、只修改前端。

## 改动与验证

| 页面 | 工作区改动 | 实际浏览器检查 |
| --- | --- | --- |
| 学生列表 | 移除蓝色横幅；常用筛选默认 3 项；其余筛选折叠；表格平铺 | 有数据列表、展开/收起、隐藏条件计数与重置 |
| 学籍异动台账 | 标题、筛选、台账平铺；纠正不存在的按钮提示 | 空状态与真实发起入口呈现 |
| 风险标签 | 紧凑筛选、平铺列表；去重复身份说明 | 空状态和筛选控件 |
| 迎新工作台 | 批次＋文字指标＋待办行＋进度分布；删除 ModuleHero | 空批次、零待办、报到进度与风险空状态；绿色通道跳转正确 |
| 请假统计 | 指标直接排列；统计口径按需展开；分组表前移 | 真实指标与班级分组表；分组切换触发加载 |
| 风险预警 | 删除 TaskContextBar；低矮指标、队列、筛选与记录平铺；父布局办理时限默认折叠 | 空队列与完整业务区；筛选紧邻排列 |
| 处分工作台 | 标题操作；队列/详情平铺；空数据只显示一个状态 | 登记入口、筛选与单个空状态 |
| 处分台账 | 文字指标；数据核对默认折叠，异常默认展开；平铺记录 | 空台账；展开数据核对显示真实计数 |
| 处分统计 | 删除总结横幅与重复说明；指标、类型分布、状态分布平铺 | 两个统计表与空状态 |

## 视觉检查

- 字体与层级：沿用系统字体；页标题 21px，指标 25px，列表正文 13px。学生表格操作恢复明确间距；NORMAL 显示为在读。
- 布局：整个业务区不再用外边框、圆角和阴影包装；以 12px 间隔和细分隔线组织内容。原有表格、表单和业务动作仍来自原组件。
- 配色：使用当前外壳的背景、表面、文字、强调色变量；异常指标保留语义色。未创建新配色或装饰素材。
- 图像：原有品牌与导航图标沿用；移除装饰横幅，没有新增图像。
- 文案：删去重复身份/范围和技术说明；必要统计口径、办理期限和核对信息仍可展开查看。
- 本次是按用户反馈改版，不是原图像素复刻。提供的截图含浏览器边框、缩放和裁切，不能据此给出像素一致性分数；对照重点为同一业务内容和去卡片方向。

## 迭代记录

1. 首轮：真实页面发现学生页历史样式覆盖、风险父布局办理时限常驻、风险空状态与台账筛选仍带底板。
2. 修正：学生页面退出旧专属装饰选择器，办理时限改为折叠，删除残留底板；修复绿色通道待办落到缴费页的前端目标。
3. 复查：浏览器并列输入原始学生/风险截图及改后实际截图；学生列表、风险队列、台账和统计表均使用平铺结构。1366 桌面实际截图可读；另检查了窄窗口与 1920 桌面布局，临时视口覆盖已重置。

## 工程验证与限制

- 本批修改的 Vue 文件 ESLint 通过；17 项工作区和导航回归测试通过；生产构建通过。
- 浏览器检查未发现控制台 error。未提交新增、审批、生效等业务写操作；验收环境部分模块为空，非空办理详情沿用原业务逻辑，尚未逐状态重新验收。
- 后端、数据库、业务接口及权限规则未改动。全系统其他未选页面不在此次平铺范围内。
- 字号、图标、主题沿用既有体系；现存水印保持原有行为。

final result: passed

## 2026-09-07 心理关注、谈心与家校工作区精修

范围：教师 PC 学工中心的心理关注 5 页、谈心 3 个三级页、重点学生跟进、家校联系和家校回执。沿用已认可的新壳、导航宽度和色彩，只压缩工作区装饰与重复文字；接口、权限、状态机和敏感数据规则不变。

### 视觉证据

- 源视觉真值：`C:/Users/10850/AppData/Local/Temp/codex-clipboard-b65b564d-a876-4b79-ac7e-d7098a74f879.png`，1920×650。该图用于校准新壳的平铺层级、导航密度和细分隔线；其业务内容是学工总览，和本批页面状态不同，因此不作逐像素复刻。
- 实现截图：`C:/Users/10850/Desktop/职校学生全生命周期系统/.codex-artifacts/design-qa/talk-workbench-20260907.png`，993×596，浏览器 CSS 视口同尺寸，device scale 1。
- 并列比较：`C:/Users/10850/Desktop/职校学生全生命周期系统/.codex-artifacts/design-qa/talk-shell-comparison-20260907.png`，1944×618。源图等比缩至 960×325，实现图等比缩至 960×576，保留各自完整画面，未裁切业务内容。
- 状态：已登录学校管理员；谈心谈话为空列表；筛选栏、发起入口和双栏空状态可见。另在同一应用浏览器检查心理关注空列表、家校联系未选学生状态。
- 交互：点击“发起谈话”后打开真实 AppDrawer，谈话类型、主题、学生多选和计划时间均可聚焦；未提交业务写操作。

### 对比与迭代

1. 首轮发现 P2：旧 `golden-cyclical-work-rollout.css` 重新给谈心标题、工具栏、列表和详情加上大圆角卡片；`TaskContextBar` 又把角色、范围、待处理和下一步拆成多块，首屏工作区被挤压。
2. 修正：在学工工作区范围内提高平铺规则权重；任务上下文压成“待处理 / 完成率 / 当前筛选”一行；列表与详情用一条竖向分隔线组织。
3. 第二轮发现 P2：共享空状态自动出现两个“返回”按钮，和当前页面任务无关。修正为本页轻量空状态，保留发起谈话主动作。
4. 复查：并列图中标题、上下分隔线、筛选标签和双栏工作区与新壳方向一致；未见剩余 P0/P1/P2。心理与家校页面同步去掉大块提示底板、时间线卡片和重复动作。

### 必查视觉面

- 字体：沿用系统字体；标题 21px，业务数字 14px 加重，表格和说明 12–14px，层级与壳一致。
- 间距：标题、概况、筛选和正文依次用 10–12px 节奏与细线分隔；弹窗承担录入，正文不再常驻表单。
- 色彩：只使用当前主题 token；蓝色仅用于主动作和选中态，心理危机与敏感查看保留必要语义色。
- 图像与图标：沿用现有品牌和导航图标；没有新增位图、装饰插画、手绘 SVG、CSS 图形或 emoji。
- 文案：删除重复角色/范围、长篇查看顺序和解释性说明；保留审计原因、危机升级后果、客观记录要求。
- 聚焦区域：源图未包含谈心、心理或家校表单，无法对录入控件做同状态视觉对照；本批只对外壳、密度和工作区层级做并列检查，弹窗按现有 AppDrawer 规范验证。

### 验证与限制

- 修改文件 ESLint 通过；学工跨域链接、后端动作契约、心理四端约束、权限目录和全局状态共 20 项测试通过；生产构建通过。
- 构建保留仓库已有的 CSS `@import` 顺序和大 chunk 提醒，未由本批引入。
- 当前演示数据在谈心、心理和家校页面为空；未验证非空记录下的逐状态视觉，也未提交转介、危机升级或家校联系。

final result: passed

结论范围：9 页平铺设计和上述已验证交互；不代表全部业务流程端到端验收完成。

## 2026-09-06 追加：房态可视化与迎新住宿入口

本轮范围：教师 PC 房源工作区、宿舍入住页、分配计划页，以及迎新“新生宿舍安排/新生住宿核对”入口。跨业务服务端统一属于规划中的下一批，不计入本次视觉验收。

参考：用户 `codex-clipboard-621dbfeb-816e-4d97-9969-75cc02ee52aa.png` 红框内楼栋/房间/床位三栏。已在同一浏览器工具输入中同时打开参考与实现截图。保留当前产品壳与主题，采用参考的同屏联动结构，不复制其接口参数、演示说明或右侧技术介绍。

实现：`http://localhost:5173/admin/student-affairs/dorm/resource`。桌面 1440×900 检查三栏结构；当前默认窄窗口约 650×596 检查楼栋/房间并排、床位顺序下移。虚拟桌面截图有浏览器合成缩放，因此只用于结构检查，文字细节用恢复默认窗口后的清晰截图核对；不声称逐像素同尺寸复刻。

视觉检查：标题、分隔线、原主题颜色、房间/床位选择态、文字层级清楚；未加入横幅或包裹卡片。源图房间数量多，实现保留接口真实返回的 1 栋/1 房/1 床，不以补造房源模拟满屏。容量 4 但只建 1 床的差异已明确标注，不把未建床位当空床。输入与原生按钮支持键盘焦点；窄屏不强压三栏。

操作检查：楼栋默认选择、房间默认选择、空床筛选及清除、床位详情、带 buildingId/roomId/bedId 进入入住管理、返回原房间、迎新进入同一分配计划、来源返回入口、入住核对旧写入口撤下。浏览器 error 日志为空。没有提交真实入住、退宿或分配发布。

自动检查：5 项新增实际视图状态测试（空床/预留/无权限、过时响应、提交验证、服务端冲突、成功刷新与迎新写入口约束）与既有宿舍/新壳检查合计 14 项通过；修改 Vue 文件 ESLint 通过；前端 build 通过；导航契约已重新生成并校验。构建保留已有大包提醒。

限制：当前真实房源没有空床，普通入住确认与成功/冲突分支由隔离 API 的实际视图方法测试覆盖；未在真实数据提交入住，也未验证全量新生自动分配或四端闭环。完整模块规划见 `docs/03-业务模块设计/学工中心/迎新与住宿统一流程-教师PC规划.md`。

final result: passed（仅本轮教师 PC 视觉与入口范围）

## 2026-09-06 迎新与住宿四端接续检查

范围：统一住宿事实、教师 PC 分配/现场报到/资格与归档、学生 PC 与小程序的迎新状态和补材料、教师小程序办理队列。沿用既定 UI 壳；本轮主要验证业务接入，未把新增页面称为参考图逐像素复刻。

实际浏览器证据：教师 PC 现场报到页加载报到点；学生 PC 未关联迎新记录空态；教师小程序管理员读取空队列，无迎新查看权限账号被拒绝。非空业务写入链由隔离 API 测试验证，未操作学校现有批次发布或实际入住。

回归：后端定向 10 项、教师前端定向 18 项、小程序既有迎新契约 7 项通过；另新增教师小程序实际方法权限测试 2 项通过，覆盖只读、无管理权限、权限服务失败及合法管理权限。教师 PC、学生 PC、H5、微信构建通过，最后权限修改后微信重新构建成功。导航契约最新，490 个可见页面路由对应检查通过。

尚未验收：四端全部非空页面往返、微信真机扫码和消息送达、全部日常调退宿/整改关联场景。不能据本轮检查宣布整个目标完成。后续清单记录在《迎新四端联调-执行记录.md》。

## 2026-09-07 学工三级工作区与四级办理窗

- Source visual truth: `C:\Users\10850\AppData\Local\Temp\codex-clipboard-b65b564d-a876-4b79-ac7e-d7098a74f879.png`，1920 × 650，教师 PC 学工壳的紧凑导航、平铺内容和弱卡片方向。
- Rendered implementation: Codex in-app Browser inline captures，`/admin/student-affairs/activity`、新建活动窗口、`/admin/student-affairs/funding/projects`；浏览器内容区 541 × 606 CSS px，device density 1。浏览器 API 本轮未提供可落盘截图路径，截图已随本轮工具证据保留。
- State: 学校管理员登录、学院蓝主题；活动为空状态与新建状态，资助项目为非空列表状态。

**Findings**

- 首轮 P2：窄工作区下活动状态被放大成整行胶囊，列表前的新建表单占据正文高度；资助项目概览仍有完整卡片外框。
- 修复：状态筛选改为可换行的文字标签和当前态下划线；概览改为文字指标带；七个新建、补录、代录入口统一进入居中办理窗口，标题、正文与底部动作独立滚动和固定。
- 复查：活动状态在 541px 内容宽度下保持同一筛选区连续排列，新建窗口没有横向溢出，主按钮固定可见；资助概览保留数字层级且去掉圆角底板。未发现遮挡主动作、裁切字段或重复内容的 P0/P1/P2 问题。

**Required fidelity surfaces**

- Fonts and typography: 沿用系统中文字体与现有 21px 页标题、15px 区块标题、13–14px 正文；长菜单名称已缩短，窄页签不再显示流程说明。
- Spacing and layout rhythm: 页面按标题、筛选、列表三段平铺；筛选与概览使用细分隔线，办理窗正文单独滚动，底部动作固定。
- Colors and visual tokens: 继续使用当前学院蓝主题变量；只保留主动作蓝、异常琥珀色和语义状态色，没有新增纯白主题或装饰色。
- Image quality and asset fidelity: 本批没有新增图片或装饰资产；现有品牌与导航图标保持不变，也未用表情符号代替附件图标。
- Copy and content: 删除重复的流程说明，保留“保存后为草稿”“提交后待认定”等会改变办理判断的短提示。

**Focused region comparison**

- 重点检查页签名称、筛选行、指标带和新建活动窗口。源图的核心约束是内容平铺、弱底板和主工作区优先；实现保持相同视觉层级。两份截图视口不同，因此不做像素尺寸一致性判断，只验证响应式结构和层级。

**Implementation checklist**

- [x] 三级菜单和页签短名称
- [x] 平铺状态筛选与概览指标
- [x] 七个四级新建/补录/代录办理窗口
- [x] 空状态、非空列表和办理窗口浏览器复查
- [x] 定向 ESLint、导航契约、工作区回归与生产构建

final result: passed

## 最新验收结果

最新批次为上文“2026-09-07 心理关注、谈心与家校工作区精修”。视觉并列证据、两轮 P2 修正、交互检查和工程验证均已记录；没有剩余 P0/P1/P2。

final result: passed

## 2026-09-08：学工运行大屏参考图重构

- Source visual truth: `C:\Users\10850\AppData\Local\Temp\codex-clipboard-b4b93cb9-ba66-442e-a24e-5f50661ae4eb.png`，1672 × 943。目标是蓝青科技感的 16:9 密集总览：顶部 8 项核心指标、左侧预警与趋势、中部校园态势、右侧宿舍/资助/辅导员协同、底部业务分布与闭环率。
- Rendered implementation: `http://127.0.0.1:5178/admin/student-affairs/stats/wall`，Codex in-app Browser 全屏内联截图；浏览器输出约 1365 × 768，页面内部使用 1920 × 1080 设计画布并按视口等比缩放。参考图与实现图已在同一任务上下文并列检查，浏览器 API 没有提供可落盘截图路径。
- State: 学校管理员登录、全校范围、真实聚合接口非空状态；学生主档 5,007、班级 105、已入住床位 5,005 等均来自现有接口。没有把参考图中的 28,416、27,362 等样例常量写入生产组件。

### 对比与迭代

1. 首轮 P1：组件模板已切成三栏，但 Vite 热更新仍挂着原组件的旧 scoped CSS，导致主体 `display:block`、图标放大、面板纵向错位。修正为新的 `StudentAffairsOperationsWall.vue` 组件入口，清除旧样式描述符后重新加载；实测主体三栏宽度约为 474 / 924 / 474。
2. 第二轮 P2：旧校园图片资源本身带模糊遮挡，中心态势无法像参考图一样成为视觉中心。更换为按中心画幅生成的干净夜景校园资产，保留“校园示意、非实际地理分布”声明，并把真实业务数值作为可点击标记覆盖其上。
3. 复查：全屏下标题、8 项指标、左右四段面板、中部校园、业务条形分布和闭环环图均在一屏内；没有页面级纵向滚动，也没有遮挡主动作。未发现剩余 P0/P1/P2。

### 必查视觉面

- 字体：沿用系统中文字体；40px 大屏标题、28px KPI、16px 面板标题和 8–13px 高密度辅助信息形成清楚层级。
- 间距：固定 16:9 画布，12px 外边距、8–10px 网格间距；所有主要信息在一屏完成，不向下堆长卡片。
- 色彩：深海军蓝底、青蓝发光边框、红/琥珀/紫仅表达风险和业务语义；没有改变教师 PC 外壳主题。
- 图像与图标：校园背景使用专门生成的位图资产；业务图标来自现有 Element Plus 图标库，没有 emoji、CSS 手绘图标或占位框。
- 文案：采用“学生主档、待审请假、逾期未销假、已入住床位”等现有业务口径；心理区域只提示敏感信息保护，不展示人数、姓名或具体事由。
- 交互：今日态势、风险处置、住宿资源三种场景可切换；指标弹窗能显示口径、范围和业务台账入口；刷新、自动刷新、口径和全屏可用，键盘保留 F / Esc 操作。

### 验证与限制

- 修改文件 ESLint 通过；学工大屏与工作台导航定向测试 9 项通过；示例数据扫描通过；生产构建通过并生成 21 条官网预渲染路由。
- 浏览器实测“风险处置”场景进入 active 状态，“学生主档”指标弹窗正常；全屏截图中三栏和全部底部模块完整。当前控制台仅保留组件改名前一次旧 Vite HMR 失败记录，改名后页面加载和构建无新增错误。
- 构建仍有仓库既存的 CSS `@import` 顺序、大 chunk 和混合静态/动态导入提醒；本轮未扩大处理。
- 校园图是展示示意图，业务数值是真实聚合数据；大屏不替代各业务台账，也不表示心理专项数据已授权展示。

final result: passed

## 2026-09-07：辅导员考评与归档工作区续工

- 范围：教师 PC 的 CounselorEvalView、ArchiveManageView、StudentArchivePackageView；沿用现有学工壳与主题，仅改前端。
- 考评：从三段常驻卡片/表单改为筛选与记录列表；指标配置、评分按需打开，草稿评分可从原记录回填；增加发布确认。接口支持的周期、状态、分页现已接入。评分输入按当前页面的对象分别保留，刷新不保留，不宣称服务端草稿自动保存。
- 归档：批次列表和详情改为分隔线双栏，去掉无作用的返回按钮和重复说明；保留预检、生成、推进接口与版本参数，补充推进确认。
- 档案包：移除重复清单卡片标题，按后端 studentName 展示姓名，识别实际生成状态；批次切换丢弃迟到响应，加载失败明确显示错误。
- 前后证据：浏览器同为 993×596。原考评首屏仅露出两块配置/录入表单，记录在下方；新首屏直接展示筛选、配置入口、录入入口与记录区域。原归档上下堆叠两块白卡，新归档双栏同时可见。未减少必要确认步骤。
- 设计依据：用户认可的教师壳截图（1920×650）与新归档截图已拼图比较：`.codex-artifacts/design-qa/archive-shell-comparison-20260907.png`。该对照只用于外壳方向和视觉层级，不是相同视口/业务状态的像素还原证明；原型壳与正式应用的顶栏尺寸差异未在本批调整。
- 浏览器验证：真实应用三个页面正常打开；考评无指标时“录入评分”进入指标配置，空名称提示必填；新建归档批次窗口正常，空名称提示必填。当前批次和考评数据均为空，未创建测试记录，未实测真实评分保存/发布、非空档案清单、预检生成或归档终态。未进行四端联调或微信宿主验收。
- 代码验证：本批 3 个 Vue 文件及新增测试文件定向 ESLint 通过；node --test 的 4 个测试文件共 16 项通过，其中 6 项为实际组件方法行为测试（隔离 API），其余是现有契约回归。不能将其视为真实服务端闭环验收。
- 构建：npm --prefix frontend run build 通过，54.63 秒，21 条官网预渲染路由；仍有既存 @import 顺序和大包警告。构建后仅把归档空工作区最小高度从 400px 收为 320px，该局部样式经 Vite 热更新和浏览器截图核对，未为此重跑全构建。
- 基线：main / f138667d7，本轮改动保留在工作区，未提交、推送、合并或部署。原有未提交改动未回退。

## 2026-09-07：材料办理工作区收敛

- 沿用用户确认的教师 UI 壳与配色；仅调整材料内容区，师生移动深链同步聚焦当前申请。
- PC 登记表按需打开，本页草稿关闭重开仍在；压缩上下文/隐私提示、重复刷新和空批次区。浏览器实际窄窗口首屏已出现清单与当前版本；未缩小全局字体或改导航。
- 同单材料四端均读到 V2 已验收；返回原申请分别定位申请 1。原单仍在辅导员初审，本次未重新办理完整奖助流程。
- 证据：.codex-artifacts/verified-flows/material-20260907/ 四张实景截图；HTML 内嵌副本见学工四端原型/学工核心流程-实景展示.html。PC 993×596 布局与移动 390px 渲染分别检查；未完成等视口原型像素对照，不能宣称整个 UI 无视觉差异。
- 24 项 PC + 33 项移动端定向测试通过，PC 定向 ESLint 通过。移动材料空编号 400 已用真实浏览器确认恢复 200。原有后端与其他模块改动未回退。
- 展示稿 file URL 被浏览器策略阻止；未绕过，HTML 仅完成静态脚本语法及图片检查，浏览器交互仍未验。
- 最终候选：PC 构建 37.82 秒通过，H5 与微信小程序构建通过；diff --check 无空白错误。存在既有 CSS 导入顺序和体积提示，微信宿主/完整业务闭环不在本批通过声明中。
