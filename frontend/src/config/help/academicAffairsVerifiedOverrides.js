/**
 * 教务历史帮助的代码级真值修正。
 *
 * 保留仍有高频价值的稳定 help id，但以当前动态成绩项与成绩任务状态机覆盖旧固定口径。
 */
export const ACADEMIC_AFFAIRS_VERIFIED_OVERRIDES = {
  'aa-card-grade-entry': {
    title: '任课教师如何按当前成绩项方案录入并提交成绩',
    keywords: ['成绩录入', '录入成绩', '提交成绩', '动态成绩项', '成绩项方案', '特殊成绩', '缺考', '缓考', '免修', '作弊', '教务'],
    summary: '成绩任务支持 1–12 个动态成绩项，权重必须合计 100；未单独配置时才兼容映射任务已有的平时/期中/期末比例。首次录分后成绩项方案锁定，教师按当前方案录分并完成完整性检查后提交审核。',
    steps: [
      '进入「成绩录入」，选择本人授课范围内的成绩任务，先查看该任务当前成绩项方案和正式学生名单。',
      '当前任务可配置 1–12 个成绩项，每项有名称、代码、权重和是否必填；所有权重必须严格合计 100。没有单独配置动态方案时，系统才从任务已有的平时 / 期中 / 期末有效比例生成兼容默认项。',
      '成绩项方案只能在任务仍为 NOT_STARTED、且尚无任何录分记录时调整；首次正式录分后方案会锁定，不能为了改变总评结果再追改权重。',
      '正常成绩按当前方案逐项录入 0–100 分。必填成绩项缺失时后端拒绝保存；可选成绩项未提交按 0 分进入证据和权重计算，不能通过漏填可选项缩小总权重抬高总评。',
      '缺考、缓考、免修、作弊请选择对应特殊状态 ABSENT / DEFERRED / EXEMPT / CHEAT；特殊状态不是 0 分，会清空普通分项、总分和及格状态。',
      '可编辑任务状态只有 NOT_STARTED / INPUTTING / RETURNED。教师保存完整成绩后按页面质量检查提交审核；提交后进入审核链路并转为只读，被退回后才重新开放修改。'
    ],
    fields: [
      '成绩项：1–12 项，代码唯一，名称必填',
      '成绩项权重：每项 >0 且 ≤100，合计必须为 100',
      '正常分项成绩：0–100',
      '特殊状态：NORMAL / ABSENT / DEFERRED / EXEMPT / CHEAT',
      '可编辑状态：NOT_STARTED / INPUTTING / RETURNED'
    ],
    faq: [
      { q: '成绩一定是“平时分 + 期末分”吗？', a: '不是。当前后端支持 1–12 个动态成绩项；只有未配置动态方案时，才兼容使用任务已有的平时 / 期中 / 期末比例生成默认成绩项。' },
      { q: '为什么开始录分后不能改成绩项权重？', a: '首次录分后成绩方案会锁定，避免录分过程中改变权重造成同一任务不同学生使用不同口径。' },
      { q: '可选成绩项不填会怎么样？', a: '按 0 分写入该项证据并保留原权重，不能通过不填可选项缩小总权重抬高总评。' },
      { q: '缺考能填 0 分吗？', a: '不要。应使用 ABSENT 等特殊状态；特殊状态会清空普通成绩分项和总分，保留真实业务语义。' },
      { q: '为什么我改不了成绩？', a: '只有 NOT_STARTED / INPUTTING / RETURNED 可编辑；提交审核后只读，需被正式退回后才能继续修改。' }
    ],
    related: [
      { label: '挂科清单', route: '/admin/academic-affairs/grade-fail' },
      { label: '成绩总览', route: '/admin/academic-affairs/grade-overview' },
      { label: '成绩更正', route: '/admin/academic-affairs/grade-change' }
    ]
  }
}
