# ACCEPTANCE.md

> 全仓统一的 Definition of Done（完成定义）与生产验收契约。
>
> 本文件回答一个问题：**什么证据足够让 GPT / Codex 声明“这个任务真的完成了”？**

## 1. 基本原则

“代码写完”“接口存在”“页面有按钮”“pytest 通过”“Actions 有一个绿灯”都不自动等于业务完成。

完成结论必须与改动风险匹配，并有可复现证据。

模块自己的验收文档、施工总控、SPEC（规格）可以增加更严格的门禁，但不能在没有明确授权的情况下降低本文件的生产底线。

## 2. 证据等级

从低到高分为以下层级：

### L0 · Source Evidence（源码证据）

证明：实现存在，代码路径合理，调用关系可解释。

不证明：运行时一定可用。

### L1 · Static / Build Evidence（静态检查 / 构建证据）

例如：

- lint（代码规范检查）；
- type / syntax check（类型 / 语法检查）；
- frontend build（前端构建）；
- import / compile（导入 / 编译）。

证明：代码在这一层没有明显结构错误。

不证明：业务闭环可用。

### L2 · Unit / Service / API Evidence（单元 / 服务 / 接口证据）

证明：局部业务规则、接口合同、错误分支按测试工作。

不证明：真实 MySQL 行为和真实用户页面一定可用。

### L3 · MySQL Integration Evidence（MySQL 集成证据）

证明：

- 真实持久化；
- 迁移；
- 约束；
- 事务；
- 并发；
- 数据完整性；
- 需要时的审计记录。

正式数据库结论不得用 SQLite 替代。

### L4 · Browser E2E Evidence（真实浏览器端到端证据）

证明：真实用户可以通过真实页面完成目标动作。

要求遵守 `e2e/AGENTS.md`，核心业务动作不能由 API、SQL、Store 注入或 seed 替代。

### L5 · CI / Same-SHA Seal（持续集成 / 同提交封板）

证明：需要合并、发布或封板的门禁在目标 exact head SHA（精确提交版本）上通过。

旧 SHA 的 PASS 不能自动证明新 SHA。

## 3. 改动类型与最低验收要求

| 改动类型 | 最低要求 |
|---|---|
| 纯文档 | L0；检查链接、命令和事实是否与仓库一致 |
| 后端局部纯逻辑 | L0 + L2；涉及数据库时增加 L3 |
| API 合同 | L0 + L2；涉及持久化 / 权限时增加 L3 |
| 数据库 Model / Migration | L0 + L2 + L3 |
| 权限 / 租户 / 数据范围 | L0 + L2 + L3；用户可见流程通常增加 L4 |
| 前端纯样式 | L1；关键交互受影响时增加 L4 |
| 前端交互 / 路由 / 表单 | L1 + 相关接口验证；关键业务增加 L4 |
| 跨端业务流程 | L2 + L3 + L4 |
| 状态机 / 审批 / 归档 / 锁定 | L2 + L3 + L4 |
| 并发 / 幂等 | L2 + L3；必要时 L4 |
| E2E Runner | 精准 E2E + 对应受影响 gate；不能用 Runner 修改降低产品合同 |
| 发布 / 合并封板 | 所有受影响等级 + L5 |

这是最低线。模块规格要求更严格时，以更严格要求为准。

## 4. 生产业务闭环的完成定义

一个用户可见业务流程如果要声明 **PASS / CLOSED / DONE**，至少确认与该流程相关的以下维度。

### 4.1 入口可达

- 正确角色能登录；
- 能找到真实菜单 / 路由 / 入口；
- 直接打开、刷新、返回等必要导航正常；
- 无权限角色不能靠直接 URL 越权。

### 4.2 输入真实

- 表单真实输入而不是脚本直接改内部状态；
- 必填、格式、范围和业务校验有效；
- 错误时用户得到可理解反馈；
- 重复点击不会轻易制造重复业务记录。

### 4.3 状态流转真实

涉及状态机时至少验证：

- 合法状态可以流转；
- 非法状态不能跳转；
- 已完成 / 已锁定 / 已归档记录按合同保持不可修改；
- 驳回 → 修改 → 重交等实际存在的逆向流程不能只测顺向成功；
- 并发动作不能产生不允许的双成功。

### 4.4 持久化真实

提交后至少按风险验证：

- 数据写入 MySQL；
- 页面 reload（刷新）后仍正确；
- re-login（重新登录）后仍正确；
- 需要跨角色时，另一角色看到的是同一条真实数据；
- 不依赖浏览器内存中的临时假状态。

### 4.5 权限与数据范围真实

确认：

- 正确角色能做；
- 错误角色不能做；
- 正确数据范围能看；
- 越范围 / 越租户不能看或不能操作；
- 前端隐藏按钮之外，后端仍有真实授权保护。

### 4.6 审计真实

对审批、导入、导出、归档、锁定、敏感数据修改等关键动作，按系统现有审计合同确认：

- 谁做的；
- 对什么对象做的；
- 做了什么动作；
- 何时发生；
- 必要时能追踪前后状态或结果。

测试不得为了简化流程关闭审计。

### 4.7 用户结果真实

最终不是只看接口 200，而是看用户能否获得正确结果，例如：

- 页面出现正确状态；
- 列表 / 详情一致；
- 统计口径更新；
- 下载 / 导出文件真实可用；
- 跨端 / 跨角色结果一致；
- 刷新后没有回滚成旧状态。

## 5. 失败分类契约

任何门禁失败，先分类再修改：

| 分类 | 含义 | 允许修改 |
|---|---|---|
| `PRODUCT_BUG` | 生产业务代码真的有问题 | 对应生产代码 + 必要测试 |
| `RUNNER_BUG` | 测试执行器 / locator / 等待 / 脚本有问题 | Runner / 测试代码 |
| `ENVIRONMENT_BUG` | CI、依赖、端口、浏览器、服务环境问题 | 工作流 / 环境 |
| `TEST_DATA_BUG` | 前置数据、隔离、污染问题 | bootstrap / fixture / 清理逻辑 |

禁止跨层“治疗”：

- Runner Bug 不应通过放宽产品权限修复；
- Product Bug 不应通过删断言修复；
- Environment Bug 不应通过改业务状态机修复；
- Test Data Bug 不应通过允许非法数据修复。

## 6. Anti-Fake-Green（禁止假绿）

出现以下任一情况，不得判定生产业务通过：

- 跳过失败测试；
- 删除关键断言而没有等价替代；
- 把精确结果断言降级为“元素存在”；
- 核心浏览器动作改成 API 代点；
- SQL 直接制造最终状态；
- mock / fake / stub（模拟实现）提供了本应由真实系统提供的核心结果；
- `try/catch` 吞掉失败后继续执行；
- 元素不存在就条件跳过；
- 大量固定 sleep 让偶发通过冒充稳定通过；
- 放宽权限、租户、数据范围、限流、状态机或输入校验来迎合测试；
- 使用旧代码 SHA 的绿灯证明当前代码；
- 后续 gate 因前置失败没执行，却被写成 PASS。

## 7. 精准重跑原则

每次红灯都不应该从头重跑全部系统。

推荐顺序：

`第一处失败 → 单断言 / 单测试 → 当前服务 / 页面 → 当前业务 gate → 受影响跨端 / MySQL gate → 最终 canonical gate`

只有满足以下情况才扩大回归范围：

- 修改了共享 Service / Schema / Model；
- 修改了公共 Router / Component / Store；
- 修改了权限 / 租户 / 数据范围；
- 修改了共享状态机；
- 修改了 Alembic migration；
- 修改了统一认证 / session；
- 修改了 E2E 公共 bootstrap / helper；
- 修改确实可能影响已通过流程。

不要因为“保险”机械重跑所有已经与改动无关的长流程。

## 8. Same-SHA（同提交版本）规则

最终合并 / 发布证据必须对应最终候选代码。

如果在 PASS 之后又有新提交：

1. 判断新提交影响范围；
2. 重新执行受影响门禁；
3. 最终需要封板的权威门禁必须能够指向当前 exact head SHA；
4. 不拿历史绿灯替代当前版本证据。

Runner 和 Product 分支分离时，报告中同时记录对应版本，避免把 Runner 更新误写成 Product 已验证。

## 9. PR / 合并前验收清单

提交合并结论前检查：

- [ ] 需求 / Bug 根因明确；
- [ ] 改动没有重复创建已有实现；
- [ ] 改动范围最小，没有无关重构；
- [ ] 相关静态检查 / 构建已运行；
- [ ] 相关单元 / Service / API 测试已运行；
- [ ] 涉及数据库时，MySQL 验证已完成；
- [ ] 涉及 Migration 时，迁移行为已验证；
- [ ] 涉及权限 / 租户 / 数据范围时，正反权限均已验证；
- [ ] 涉及关键写操作时，审计要求已确认；
- [ ] 用户可见关键闭环由真实浏览器完成；
- [ ] Browser E2E 没有 API / SQL 代替核心动作；
- [ ] 失败证据已区分 Product / Runner / Environment / Test Data；
- [ ] 没有 skipped / swallowed / weakened assertion（跳过 / 吞错 / 弱断言）制造假绿；
- [ ] 最终门禁证据对应正确 SHA；
- [ ] 未运行的项目明确写为“未验证”；
- [ ] 仍有红灯时，不使用“已封板 / 可合并”结论。

## 10. AI 最终报告格式

GPT / Codex 完成施工后，至少输出以下事实：

### 判断

`PASS / PARTIAL / RED / BLOCKED`

### 根因

一句话说明实际问题属于哪一层。

### 修改

列出实际修改文件及目的。

### 已验证

列出真实执行的命令 / gate / 浏览器流程及结果。

### 未验证

明确列出没有运行或没有证据的部分。

### 风险

只写仍存在的真实风险，不堆通用提醒。

### 下一步

给出最窄的下一动作，不默认从头重跑全量系统。

---

**最终原则：没有证据，就写“未验证”；有红灯，就写“RED”；只有当前版本的必要门禁真实通过，才写“PASS”。**
